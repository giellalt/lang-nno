# Lemma-tests for *verbs* in ...`verbs.lexc`


**nibede** failures:

* `nibede+V+Inf` => `nibea`
* `nibede+V+Inf` => `nibee`
* `nibede` has no analyses either

**stødve** failures:

* `stødve+V+Inf` => `stødv`
* `stødve` has no analyses either

**kronikkere** failures:

* `kronikkere+V+Inf` => `kronikker`
* `kronikkere` has no analyses either

**åverke** failures:

* `åverke+V+Inf` => `åverk`
* `åverke` has no analyses either

**stane** failures:

* `stane+V+Inf` does not generate!
* `stane` has no analyses either

**skaffe** failures:

* `skaffe+V+Inf` => `skaff`
* `skaffe` has no analyses either

**lekke** failures:

* `lekke+V+Inf` does not generate!
* `lekke` has no analyses either

**likvidere** failures:

* `likvidere+V+Inf` => `likvider`
* `likvidere` has no analyses either

**smike** failures:

* `smike+V+Inf` => `smik`
* `smike` has no analyses either

**sælast** failures:

* `sælast+V+Inf` => `sælastast`
* `sælast` has following analyses:
  * `sæl+A+A+Superl+Indef`

**sprike** failures:

* `sprike+V+Inf` => `sprik`
* `sprike` has no analyses either

**akkreditere** failures:

* `akkreditere+V+Inf` => `akkrediter`
* `akkreditere` has no analyses either

**diskontere** failures:

* `diskontere+V+Inf` => `diskonter`
* `diskontere` has no analyses either

**skrue** failures:

* `skrue+V+Inf` does not generate!
* `skrue` has no analyses either

**pinne** failures:

* `pinne+V+Inf` => `pinn`
* `pinne` has no analyses either

**ugge** failures:

* `ugge+V+Inf` => `ugg`
* `ugge` has no analyses either

**slamre** failures:

* `slamre+V+Inf` => `slamr`
* `slamre` has no analyses either

**esle** failures:

* `esle+V+Inf` => `esl`
* `esle` has no analyses either

**gampe** failures:

* `gampe+V+Inf` => `gamp`
* `gampe` has following analyses:
  * `gampe+A+A+Neu+Sg+Indef`
  * `gampe+A+A+Fem+Sg+Indef`
  * `gampe+A+A+Msc+Sg+Indef`

**versere** failures:

* `versere+V+Inf` => `verser`
* `versere` has no analyses either

**ekserpere** failures:

* `ekserpere+V+Inf` => `ekserper`
* `ekserpere` has no analyses either

**eskortere** failures:

* `eskortere+V+Inf` => `eskorter`
* `eskortere` has no analyses either

**sløve** failures:

* `sløve+V+Inf` => `sløv`
* `sløve` has following analyses:
  * `sløv+A+Pl+Indef`
  * `sløv+A+Sg+Def`

**nype** failures:

* `nype+V+Inf` => `nya`
* `nype+V+Inf` => `nye`
* `nype` has no analyses either

**tumle** failures:

* `tumle+V+Inf` => `tuml`
* `tumle` has following analyses:
  * `tumlen+A+Neu+Sg+Indef`

**belaste** failures:

* `belaste+V+Inf` => `belast`
* `belaste` has no analyses either

**dengje** failures:

* `dengje+V+Inf` does not generate!
* `dengje` has no analyses either

**bøkje** failures:

* `bøkje+V+Inf` does not generate!
* `bøkje` has no analyses either

**dyne** failures:

* `dyne+V+Inf` => `dyn`
* `dyne` has no analyses either

**slakte** failures:

* `slakte+V+Inf` => `slakt`
* `slakte` has no analyses either

**øydelegge** failures:

* `øydelegge+V+Inf` does not generate!
* `øydelegge` has no analyses either

**skrakle** failures:

* `skrakle+V+Inf` => `skrakl`
* `skrakle` has no analyses either

**flakse** failures:

* `flakse+V+Inf` => `flaks`
* `flakse` has no analyses either

**bragde** failures:

* `bragde+V+Inf` => `bragd`
* `bragde` has no analyses either

**gjenoppbygge** failures:

* `gjenoppbygge+V+Inf` => `gjenoppbyga`
* `gjenoppbygge+V+Inf` => `gjenoppbyge`
* `gjenoppbygge` has no analyses either

**filere** failures:

* `filere+V+Inf` => `filer`
* `filere` has no analyses either

**mangfaldiggjere** failures:

* `mangfaldiggjere+V+Inf` does not generate!
* `mangfaldiggjere` has no analyses either

**belyse** failures:

* `belyse+V+Inf` => `belys`
* `belyse` has no analyses either

**flolegge** failures:

* `flolegge+V+Inf` does not generate!
* `flolegge` has no analyses either

**ilskast** failures:

* `ilskast+V+Inf` => `ilskastast`
* `ilskast` has following analyses:
  * `ilsk+A+A+Superl+Indef`

**misgjere** failures:

* `misgjere+V+Inf` does not generate!
* `misgjere` has no analyses either

**messe** failures:

* `messe+V+Inf` => `mess`
* `messe` has no analyses either

**massere** failures:

* `massere+V+Inf` => `masser`
* `massere` has no analyses either

**ragge** failures:

* `ragge+V+Inf` => `ragg`
* `ragge` has no analyses either

**svinse** failures:

* `svinse+V+Inf` => `svins`
* `svinse` has no analyses either

**kalligrafere** failures:

* `kalligrafere+V+Inf` => `kalligrafer`
* `kalligrafere` has no analyses either

**trikse** failures:

* `trikse+V+Inf` => `triks`
* `trikse` has no analyses either

**pitle** failures:

* `pitle+V+Inf` => `pitl`
* `pitle` has no analyses either

**digge** failures:

* `digge+V+Inf` => `digg`
* `digge` has following analyses:
  * `digg+A+A+Pl+Indef`
  * `digg+A+A+Sg+Def`
  * `digg+A+A+Pl+Def`

**mørklegge** failures:

* `mørklegge+V+Inf` does not generate!
* `mørklegge` has no analyses either

**kujonere** failures:

* `kujonere+V+Inf` => `kujoner`
* `kujonere` has no analyses either

**kopiere** failures:

* `kopiere+V+Inf` => `kopier`
* `kopiere` has no analyses either

**skvelpe** failures:

* `skvelpe+V+Inf` => `skvea`
* `skvelpe+V+Inf` => `skvee`
* `skvelpe` has no analyses either

**tjuvslakte** failures:

* `tjuvslakte+V+Inf` => `tjuvslakt`
* `tjuvslakte` has no analyses either

**fricampe** failures:

* `fricampe+V+Inf` => `fricamp`
* `fricampe` has no analyses either

**kikse** failures:

* `kikse+V+Inf` => `kiks`
* `kikse` has no analyses either

**skvelje** failures:

* `skvelje+V+Inf` => `skvelj`
* `skvelje` has no analyses either

**avdra** failures:

* `avdra+V+Inf` => `avdrdra`
* `avdra` has no analyses either

**løde** failures:

* `løde+V+Inf` does not generate!
* `løde` has no analyses either

**oppfylle** failures:

* `oppfylle+V+Inf` does not generate!
* `oppfylle` has no analyses either

**skimre** failures:

* `skimre+V+Inf` => `skimr`
* `skimre` has no analyses either

**avspasere** failures:

* `avspasere+V+Inf` => `avspaser`
* `avspasere` has no analyses either

**dase** failures:

* `dase+V+Inf` => `das`
* `dase` has no analyses either

**trustifisere** failures:

* `trustifisere+V+Inf` => `trustifiser`
* `trustifisere` has no analyses either

**klykke** failures:

* `klykke+V+Inf` does not generate!
* `klykke` has no analyses either

**vone** failures:

* `vone+V+Inf` => `von`
* `vone` has following analyses:
  * `verpe+V+Adj+Bu+Eint`
  * `verte+V+Adj+Bu+Eint`
  * `veve+V+Adj+Bu+Eint`
  * `verpe+V+Adj+Fl`
  * `verte+V+Adj+Fl`
  * `von+A+A+Pl+Def`
  * `von+A+A+Sg+Def`
  * `veve+V+Adj+Fl`
  * `von+A+A+Pl+Indef`

**underskrive** failures:

* `underskrive+V+Inf` => `underskrie`
* `underskrive+V+Inf` => `underskria`
* `underskrive` has following analyses:
  * `underskriven+A+Neu+Sg+Indef`

**involvere** failures:

* `involvere+V+Inf` => `involver`
* `involvere` has no analyses either

**spanske** failures:

* `spanske+V+Inf` => `spansk`
* `spanske` has following analyses:
  * `spansk+A+A+Sg+Def`
  * `spansk+A+A+Pl+Def`
  * `spansk+A+A+Pl+Indef`

**tørke** failures:

* `tørke+V+Inf` => `tørk`
* `tørke` has no analyses either

**førehandsdøme** failures:

* `førehandsdøme+V+Inf` does not generate!
* `førehandsdøme` has no analyses either

**medverke** failures:

* `medverke+V+Inf` => `medverk`
* `medverke` has no analyses either

**mekre** failures:

* `mekre+V+Inf` => `mekr`
* `mekre` has no analyses either

**spjette** failures:

* `spjette+V+Inf` => `spjett`
* `spjette` has no analyses either

**tildøme** failures:

* `tildøme+V+Inf` does not generate!
* `tildøme` has no analyses either

**distribuere** failures:

* `distribuere+V+Inf` => `distribuer`
* `distribuere` has no analyses either

**teppeleggje** failures:

* `teppeleggje+V+Inf` does not generate!
* `teppeleggje` has no analyses either

**ettergje** failures:

* `ettergje+V+Inf` => `etterggje`
* `ettergje` has no analyses either

**stave** failures:

* `stave+V+Inf` => `stav`
* `stave` has no analyses either

**bragle** failures:

* `bragle+V+Inf` => `bragl`
* `bragle` has no analyses either

**ristornere** failures:

* `ristornere+V+Inf` => `ristorner`
* `ristornere` has no analyses either

**lasere** failures:

* `lasere+V+Inf` => `laser`
* `lasere` has no analyses either

**destillere** failures:

* `destillere+V+Inf` => `destiller`
* `destillere` has no analyses either

**pulse** failures:

* `pulse+V+Inf` => `puls`
* `pulse` has no analyses either

**blistre** failures:

* `blistre+V+Inf` => `blistr`
* `blistre` has no analyses either

**avgjere** failures:

* `avgjere+V+Inf` does not generate!
* `avgjere` has no analyses either

**mengje** failures:

* `mengje+V+Inf` does not generate!
* `mengje` has no analyses either

**bløffe** failures:

* `bløffe+V+Inf` => `bløff`
* `bløffe` has no analyses either

**galvanisere** failures:

* `galvanisere+V+Inf` => `galvaniser`
* `galvanisere` has no analyses either

**grublisere** failures:

* `grublisere+V+Inf` => `grubliser`
* `grublisere` has no analyses either

**krydde** failures:

* `krydde+V+Inf` => `krydd`
* `krydde` has following analyses:
  * `krydde+N+N+Fem+Sg+Indef`
  * `kry+V+Ind+Prt`

**-escere** failures:

* `-escere+V+Inf` => `-escer`
* `-escere` has no analyses either

**blasonere** failures:

* `blasonere+V+Inf` => `blasoner`
* `blasonere` has no analyses either

**volle** failures:

* `volle+V+Inf` => `voll`
* `volle` has no analyses either

**kolke** failures:

* `kolke+V+Inf` => `kolk`
* `kolke` has no analyses either

**antirefleksbehandle** failures:

* `antirefleksbehandle+V+Inf` => `antirefleksbehandl`
* `antirefleksbehandle` has no analyses either

**hevje** failures:

* `hevje+V+Inf` does not generate!
* `hevje` has no analyses either

**æse** failures:

* `æse+V+Inf` => `æs`
* `æse` has no analyses either

**forvrøvle** failures:

* `forvrøvle+V+Inf` => `forvrøvl`
* `forvrøvle` has no analyses either

**preskribere** failures:

* `preskribere+V+Inf` => `preskriber`
* `preskribere` has no analyses either

**prøveskyte** failures:

* `prøveskyte+V+Inf` => `prøveskye`
* `prøveskyte+V+Inf` => `prøveskya`
* `prøveskyte` has no analyses either

**avtale** failures:

* `avtale+V+Inf` does not generate!
* `avtale` has no analyses either

**eksploatere** failures:

* `eksploatere+V+Inf` => `eksploater`
* `eksploatere` has no analyses either

**område** failures:

* `område+V+Inf` does not generate!
* `område` has no analyses either

**runde** failures:

* `runde+V+Inf` => `rund`
* `runde` has following analyses:
  * `rund+A+Sg+Def`
  * `rund+A+Pl+Indef`

**notifisere** failures:

* `notifisere+V+Inf` => `notifiser`
* `notifisere` has no analyses either

**glippe** failures:

* `glippe+V+Inf` => `glipp`
* `glippe` has no analyses either

**rune** failures:

* `rune+V+Inf` => `run`
* `rune` has no analyses either

**hyrgje** failures:

* `hyrgje+V+Inf` does not generate!
* `hyrgje` has no analyses either

**rite** failures:

* `rite+V+Inf` => `rit`
* `rite` has no analyses either

**formgjeve** failures:

* `formgjeve+V+Inf` => `formggjeva`
* `formgjeve+V+Inf` => `formggjeve`
* `formgjeve` has no analyses either

**domisiliere** failures:

* `domisiliere+V+Inf` => `domisilier`
* `domisiliere` has no analyses either

**glupe** failures:

* `glupe+V+Inf` => `glue`
* `glupe+V+Inf` => `glua`
* `glupe` has following analyses:
  * `glup+A+Pl+Indef`
  * `glup+A+Sg+Def`

**rive** failures:

* `rive+V+Inf` => `rie`
* `rive+V+Inf` => `ria`
* `rive` has following analyses:
  * `riv+A+Pl+Indef`
  * `riv+A+Sg+Def`

**tygle** failures:

* `tygle+V+Inf` => `tygl`
* `tygle` has no analyses either

**inflatere** failures:

* `inflatere+V+Inf` => `inflater`
* `inflatere` has no analyses either

**barke** failures:

* `barke+V+Inf` => `bark`
* `barke` has no analyses either

**hamle** failures:

* `hamle+V+Inf` => `haml`
* `hamle` has no analyses either

**ljuge** failures:

* `ljuge+V+Inf` => `ljue`
* `ljuge+V+Inf` => `ljua`
* `ljuge` has no analyses either

**vakte** failures:

* `vakte+V+Inf` => `vakt`
* `vakte` has following analyses:
  * `vakt+A+A+Pl+Indef`
  * `vake+V+PrfPrc`
  * `vakt+A+A+Sg+Def`
  * `vakt+A+A+Pl+Def`

**avsetje** failures:

* `avsetje+V+Inf` does not generate!
* `avsetje` has no analyses either

**poetisere** failures:

* `poetisere+V+Inf` => `poetiser`
* `poetisere` has no analyses either

**slettbyggje** failures:

* `slettbyggje+V+Inf` does not generate!
* `slettbyggje` has no analyses either

**trekke** failures:

* `trekke+V+Inf` does not generate!
* `trekke` has no analyses either

**lunsje** failures:

* `lunsje+V+Inf` => `lunsj`
* `lunsje` has no analyses either

**undre** failures:

* `undre+V+Inf` => `undr`
* `undre` has following analyses:
  * `undre+A+Comp`

**skeine** failures:

* `skeine+V+Inf` => `skein`
* `skeine` has no analyses either

**katalysere** failures:

* `katalysere+V+Inf` => `katalyser`
* `katalysere` has no analyses either

**snylte** failures:

* `snylte+V+Inf` => `snylt`
* `snylte` has no analyses either

**skjørpe** failures:

* `skjørpe+V+Inf` => `skjørp`
* `skjørpe` has no analyses either

**prale** failures:

* `prale+V+Inf` => `pral`
* `prale` has no analyses either

**føreorde** failures:

* `føreorde+V+Inf` => `føreord`
* `føreorde` has no analyses either

**angripe** failures:

* `angripe+V+Inf` => `angrie`
* `angripe+V+Inf` => `angria`
* `angripe` has no analyses either

**drisle** failures:

* `drisle+V+Inf` => `drisl`
* `drisle` has no analyses either

**konsolidere** failures:

* `konsolidere+V+Inf` => `konsolider`
* `konsolidere` has no analyses either

**neitte** failures:

* `neitte+V+Inf` => `neitt`
* `neitte` has no analyses either

**intrigere** failures:

* `intrigere+V+Inf` => `intriger`
* `intrigere` has no analyses either

**egse** failures:

* `egse+V+Inf` does not generate!
* `egse` has no analyses either

**strupe** failures:

* `strupe+V+Inf` does not generate!
* `strupe` has no analyses either

**sufflere** failures:

* `sufflere+V+Inf` => `suffler`
* `sufflere` has no analyses either

**kneble** failures:

* `kneble+V+Inf` => `knebl`
* `kneble` has no analyses either

**klorere** failures:

* `klorere+V+Inf` => `klorer`
* `klorere` has no analyses either

**baule** failures:

* `baule+V+Inf` => `baul`
* `baule` has no analyses either

**avskaffe** failures:

* `avskaffe+V+Inf` => `avskaff`
* `avskaffe` has no analyses either

**ravle** failures:

* `ravle+V+Inf` => `ravl`
* `ravle` has no analyses either

**kneppe** failures:

* `kneppe+V+Inf` does not generate!
* `kneppe` has no analyses either

**molestere** failures:

* `molestere+V+Inf` => `molester`
* `molestere` has no analyses either

**skifte** failures:

* `skifte+V+Inf` does not generate!
* `skifte` has no analyses either

**skjøtte** failures:

* `skjøtte+V+Inf` => `skjøtt`
* `skjøtte` has no analyses either

**hate** failures:

* `hate+V+Inf` => `hat`
* `hate` has no analyses either

**kamse** failures:

* `kamse+V+Inf` => `kams`
* `kamse` has no analyses either

**bysse** failures:

* `bysse+V+Inf` => `byss`
* `bysse` has no analyses either

**tidoble** failures:

* `tidoble+V+Inf` => `tidobl`
* `tidoble` has no analyses either

**avrunde** failures:

* `avrunde+V+Inf` => `avrund`
* `avrunde` has no analyses either

**hende** failures:

* `hende+V+Inf` does not generate!
* `hende` has following analyses:
  * `hende+A+A+Msc+Sg+Indef`
  * `hende+A+A+Neu+Sg+Indef`
  * `hende+A+A+Fem+Sg+Indef`

**gørrstikke** failures:

* `gørrstikke+V+Inf` => `gørrstie`
* `gørrstikke+V+Inf` => `gørrstia`
* `gørrstikke` has no analyses either

**kontrahere** failures:

* `kontrahere+V+Inf` => `kontraher`
* `kontrahere` has no analyses either

**lite** failures:

* `lite+V+Inf` => `lia`
* `lite+V+Inf` => `lie`
* `lite` has following analyses:
  * `lite+A+A+Neu+Sg+Indef`
  * `lite+A+A+Fem+Sg+Indef`
  * `lite+A+A+Msc+Sg+Indef`

**postdatere** failures:

* `postdatere+V+Inf` => `postdater`
* `postdatere` has no analyses either

**skrangle** failures:

* `skrangle+V+Inf` => `skrangl`
* `skrangle` has following analyses:
  * `skranglen+A+Neu+Sg+Indef`

**hute** failures:

* `hute+V+Inf` => `hut`
* `hute` has no analyses either

**kure** failures:

* `kure+V+Inf` does not generate!
* `kure` has following analyses:
  * `kurr+A+A+Pl+Def`
  * `kurr+A+A+Pl+Indef`
  * `kurr+A+A+Sg+Def`

**klisse** failures:

* `klisse+V+Inf` => `kliss`
* `klisse` has no analyses either

**usse** failures:

* `usse+V+Inf` => `uss`
* `usse` has no analyses either

**småskunde** failures:

* `småskunde+V+Inf` => `småskund`
* `småskunde` has no analyses either

**mone** failures:

* `mone+V+Inf` => `mon`
* `mone` has following analyses:
  * `moen+A+Pl+Indef`
  * `moen+A+Sg+Def`

**naukre** failures:

* `naukre+V+Inf` => `naukr`
* `naukre` has no analyses either

**sole** failures:

* `sole+V+Inf` => `sol`
* `sole` has no analyses either

**misjonere** failures:

* `misjonere+V+Inf` => `misjoner`
* `misjonere` has no analyses either

**øre** failures:

* `øre+V+Inf` does not generate!
* `øre` has following analyses:
  * `ør+A+Pl+Indef`
  * `ør+A+Sg+Def`

**musle** failures:

* `musle+V+Inf` => `musl`
* `musle` has no analyses either

**svimerke** failures:

* `svimerke+V+Inf` => `svimerk`
* `svimerke` has no analyses either

**vegleie** failures:

* `vegleie+V+Inf` does not generate!
* `vegleie` has no analyses either

**amme** failures:

* `amme+V+Inf` => `am`
* `amme` has no analyses either

**vri** failures:

* `vri+V+Inf` does not generate!
* `vri` has following analyses:
  * `vrine+V+Imprt`
  * `vri+N+N+Msc+Sg+Indef`
  * `vrine+V+Ind+Prs`

**feise** failures:

* `feise+V+Inf` => `feis`
* `feise` has no analyses either

**skrøyve** failures:

* `skrøyve+V+Inf` does not generate!
* `skrøyve` has no analyses either

**hypje** failures:

* `hypje+V+Inf` => `hypj`
* `hypje` has no analyses either

**tendere** failures:

* `tendere+V+Inf` => `tender`
* `tendere` has no analyses either

**bakke** failures:

* `bakke+V+Inf` => `bakk`
* `bakke` has no analyses either

**stynje** failures:

* `stynje+V+Inf` does not generate!
* `stynje` has no analyses either

**høle** failures:

* `høle+V+Inf` => `høl`
* `høle` has no analyses either

**mompe** failures:

* `mompe+V+Inf` => `momp`
* `mompe` has no analyses either

**forkvakle** failures:

* `forkvakle+V+Inf` => `forkvakl`
* `forkvakle` has no analyses either

**autentifisere** failures:

* `autentifisere+V+Inf` => `autentifiser`
* `autentifisere` has no analyses either

**fargeleggje** failures:

* `fargeleggje+V+Inf` does not generate!
* `fargeleggje` has no analyses either

**gale** failures:

* `gale+V+Inf` => `ggala`
* `gale+V+Inf` => `ggale`
* `gale` has following analyses:
  * `galen+A+Neu+Sg+Indef`

**reinvestere** failures:

* `reinvestere+V+Inf` => `reinvester`
* `reinvestere` has no analyses either

**runge** failures:

* `runge+V+Inf` => `rung`
* `runge` has no analyses either

**sperre** failures:

* `sperre+V+Inf` => `sperr`
* `sperre` has no analyses either

**bråstogge** failures:

* `bråstogge+V+Inf` => `bråstogg`
* `bråstogge` has no analyses either

**perfeksjonere** failures:

* `perfeksjonere+V+Inf` => `perfeksjoner`
* `perfeksjonere` has no analyses either

**campe** failures:

* `campe+V+Inf` => `camp`
* `campe` has following analyses:
  * `camp+A+A+Sg+Def`
  * `camp+A+A+Pl+Def`
  * `camp+A+A+Pl+Indef`

**defilere** failures:

* `defilere+V+Inf` => `defiler`
* `defilere` has no analyses either

**samføre** failures:

* `samføre+V+Inf` => `samfør`
* `samføre` has no analyses either

**fluorisere** failures:

* `fluorisere+V+Inf` => `fluoriser`
* `fluorisere` has no analyses either

**krasje** failures:

* `krasje+V+Inf` => `krasj`
* `krasje` has no analyses either

**pågripe** failures:

* `pågripe+V+Inf` => `pågrie`
* `pågripe+V+Inf` => `pågria`
* `pågripe` has no analyses either

**uteslutte** failures:

* `uteslutte+V+Inf` => `uteslutt`
* `uteslutte` has no analyses either

**grepje** failures:

* `grepje+V+Inf` => `grepj`
* `grepje` has no analyses either

**hæse** failures:

* `hæse+V+Inf` => `hæs`
* `hæse` has no analyses either

**fasilitere** failures:

* `fasilitere+V+Inf` => `fasiliter`
* `fasilitere` has no analyses either

**sjuske** failures:

* `sjuske+V+Inf` => `sjusk`
* `sjuske` has no analyses either

**detaljere** failures:

* `detaljere+V+Inf` => `detaljer`
* `detaljere` has no analyses either

**okkultere** failures:

* `okkultere+V+Inf` => `okkulter`
* `okkultere` has no analyses either

**ettersøkje** failures:

* `ettersøkje+V+Inf` does not generate!
* `ettersøkje` has no analyses either

**grisle** failures:

* `grisle+V+Inf` => `grisl`
* `grisle` has no analyses either

**tulle** failures:

* `tulle+V+Inf` => `tull`
* `tulle` has no analyses either

**prikle** failures:

* `prikle+V+Inf` => `prikl`
* `prikle` has no analyses either

**korte** failures:

* `korte+V+Inf` => `kort`
* `korte` has following analyses:
  * `kort+A+A+Sg+Def`
  * `kort+A+A+Pl+Indef`
  * `kort+A+A+Pl+Def`

**snøvle** failures:

* `snøvle+V+Inf` => `snøvl`
* `snøvle` has no analyses either

**fostre** failures:

* `fostre+V+Inf` => `fostr`
* `fostre` has no analyses either

**kjønnssortere** failures:

* `kjønnssortere+V+Inf` => `kjønnssorter`
* `kjønnssortere` has no analyses either

**firedoble** failures:

* `firedoble+V+Inf` => `firedobl`
* `firedoble` has no analyses either

**presentere** failures:

* `presentere+V+Inf` => `presenter`
* `presentere` has no analyses either

**handplukke** failures:

* `handplukke+V+Inf` => `handplukk`
* `handplukke` has no analyses either

**adressere** failures:

* `adressere+V+Inf` => `adresser`
* `adressere` has no analyses either

**oktroajere** failures:

* `oktroajere+V+Inf` => `oktroajer`
* `oktroajere` has no analyses either

**rode** failures:

* `rode+V+Inf` => `rod`
* `rode` has no analyses either

**utykkje** failures:

* `utykkje+V+Inf` does not generate!
* `utykkje` has no analyses either

**herpe** failures:

* `herpe+V+Inf` => `herp`
* `herpe` has no analyses either

**omsette** failures:

* `omsette+V+Inf` does not generate!
* `omsette` has no analyses either

**prompe** failures:

* `prompe+V+Inf` => `promp`
* `prompe` has no analyses either

**skjegle** failures:

* `skjegle+V+Inf` => `skjegl`
* `skjegle` has following analyses:
  * `skjegl+A+Pl+Indef`
  * `skjegl+A+Sg+Def`

**overta** failures:

* `overta+V+Inf` => `overtta`
* `overta` has following analyses:
  * `overtann+N+N+Fem+Sg+Def`

**forvenje** failures:

* `forvenje+V+Inf` does not generate!
* `forvenje` has no analyses either

**forplante** failures:

* `forplante+V+Inf` => `forplant`
* `forplante` has no analyses either

**forvrenge** failures:

* `forvrenge+V+Inf` => `forvreng`
* `forvrenge` has no analyses either

**omkomple** failures:

* `omkomple+V+Inf` => `omkompl`
* `omkomple` has no analyses either

**forhorne** failures:

* `forhorne+V+Inf` => `forhorn`
* `forhorne` has no analyses either

**exe** failures:

* `exe+V+Inf` => `ex`
* `exe` has no analyses either

**hyse** failures:

* `hyse+V+Inf` => `hys`
* `hyse` has no analyses either

**arbeide** failures:

* `arbeide+V+Inf` does not generate!
* `arbeide` has no analyses either

**kvilte** failures:

* `kvilte+V+Inf` => `kvilt`
* `kvilte` has following analyses:
  * `kvilt+A+A+Sg+Def`
  * `kvilt+A+A+Pl+Indef`
  * `kvilt+A+A+Pl+Def`
  * `kvile+V+PrfPrc`

**slindre** failures:

* `slindre+V+Inf` => `slindr`
* `slindre` has no analyses either

**kaldrøyke** failures:

* `kaldrøyke+V+Inf` => `kaldrøyk`
* `kaldrøyke` has no analyses either

**kartonere** failures:

* `kartonere+V+Inf` => `kartoner`
* `kartonere` has no analyses either

**oljegruse** failures:

* `oljegruse+V+Inf` does not generate!
* `oljegruse` has no analyses either

**undertrykke** failures:

* `undertrykke+V+Inf` does not generate!
* `undertrykke` has no analyses either

**rykke** failures:

* `rykke+V+Inf` does not generate!
* `rykke` has no analyses either

**skvinkle** failures:

* `skvinkle+V+Inf` => `skvinkl`
* `skvinkle` has no analyses either

**bruleggje** failures:

* `bruleggje+V+Inf` does not generate!
* `bruleggje` has no analyses either

**knive** failures:

* `knive+V+Inf` => `kniv`
* `knive` has no analyses either

**eksersere** failures:

* `eksersere+V+Inf` => `ekserser`
* `eksersere` has no analyses either

**suse** failures:

* `suse+V+Inf` does not generate!
* `suse` has no analyses either

**dose** failures:

* `dose+V+Inf` => `dos`
* `dose` has following analyses:
  * `dossen+A+Neu+Sg+Indef`

**øygle** failures:

* `øygle+V+Inf` => `øygl`
* `øygle` has no analyses either

**blygje** failures:

* `blygje+V+Inf` does not generate!
* `blygje` has no analyses either

**avloe** failures:

* `avloe+V+Inf` => `avlo`
* `avloe` has no analyses either

**deformere** failures:

* `deformere+V+Inf` => `deformer`
* `deformere` has no analyses either

**materialisere** failures:

* `materialisere+V+Inf` => `materialiser`
* `materialisere` has no analyses either

**tøve** failures:

* `tøve+V+Inf` does not generate!
* `tøve` has no analyses either

**tykkjast** failures:

* `tykkjast+V+Inf` => `tykjast`
* `tykkjast` has no analyses either

**skvulpe** failures:

* `skvulpe+V+Inf` => `skvulp`
* `skvulpe` has no analyses either

**dimittere** failures:

* `dimittere+V+Inf` => `dimitter`
* `dimittere` has no analyses either

**glose** failures:

* `glose+V+Inf` => `glos`
* `glose` has no analyses either

**galdtrakke** failures:

* `galdtrakke+V+Inf` => `galdtrakk`
* `galdtrakke` has no analyses either

**geminere** failures:

* `geminere+V+Inf` => `geminer`
* `geminere` has no analyses either

**gaupne** failures:

* `gaupne+V+Inf` => `gaupn`
* `gaupne` has no analyses either

**taime** failures:

* `taime+V+Inf` => `taim`
* `taime` has no analyses either

**xylografere** failures:

* `xylografere+V+Inf` => `xylografer`
* `xylografere` has no analyses either

**brake** failures:

* `brake+V+Inf` => `brak`
* `brake` has following analyses:
  * `brakk+A+A+Pl+Def`
  * `brakk+A+A+Sg+Def`
  * `brakk+A+A+Pl+Indef`

**loke** failures:

* `loke+V+Inf` => `lok`
* `loke` has no analyses either

**maltraktere** failures:

* `maltraktere+V+Inf` => `maltrakter`
* `maltraktere` has no analyses either

**antistatbehandle** failures:

* `antistatbehandle+V+Inf` => `antistatbehandl`
* `antistatbehandle` has no analyses either

**sovne** failures:

* `sovne+V+Inf` => `sovn`
* `sovne` has no analyses either

**time** failures:

* `time+V+Inf` => `tim`
* `time` has no analyses either

**ute** failures:

* `ute+V+Inf` does not generate!
* `ute` has no analyses either

**jamføre** failures:

* `jamføre+V+Inf` => `jamfør`
* `jamføre` has no analyses either

**vikje** failures:

* `vikje+V+Inf` does not generate!
* `vikje` has no analyses either

**stole** failures:

* `stole+V+Inf` does not generate!
* `stole` has no analyses either

**olme** failures:

* `olme+V+Inf` => `olm`
* `olme` has following analyses:
  * `olm+A+Sg+Def`
  * `olm+A+Pl+Indef`

**skarve** failures:

* `skarve+V+Inf` => `skarv`
* `skarve` has following analyses:
  * `skarve+A+A+Msc+Sg+Indef`
  * `skarve+A+A+Fem+Sg+Indef`
  * `skarve+A+A+Neu+Sg+Indef`

**skulptere** failures:

* `skulptere+V+Inf` => `skulpter`
* `skulptere` has no analyses either

**bannast** failures:

* `bannast+V+Inf` => `banast`
* `bannast` has no analyses either

**breste** failures:

* `breste+V+Inf` does not generate!
* `breste` has following analyses:
  * `brese+V+PrfPrc`

**rable** failures:

* `rable+V+Inf` => `rabl`
* `rable` has no analyses either

**beundre** failures:

* `beundre+V+Inf` => `beundr`
* `beundre` has no analyses either

**huge** failures:

* `huge+V+Inf` => `hug`
* `huge` has following analyses:
  * `hug+A+Sg+Def`
  * `hug+A+Pl+Indef`

**hevle** failures:

* `hevle+V+Inf` => `hevl`
* `hevle` has no analyses either

**denaturere** failures:

* `denaturere+V+Inf` => `denaturer`
* `denaturere` has no analyses either

**stile** failures:

* `stile+V+Inf` does not generate!
* `stile` has following analyses:
  * `still+A+A+Sg+Def`
  * `still+A+A+Pl+Def`
  * `still+A+A+Pl+Indef`

**mugge** failures:

* `mugge+V+Inf` => `mugg`
* `mugge` has no analyses either

**strante** failures:

* `strante+V+Inf` => `strant`
* `strante` has no analyses either

**remittere** failures:

* `remittere+V+Inf` => `remitter`
* `remittere` has no analyses either

**filetere** failures:

* `filetere+V+Inf` => `fileter`
* `filetere` has no analyses either

**ordinere** failures:

* `ordinere+V+Inf` => `ordiner`
* `ordinere` has no analyses either

**nikke** failures:

* `nikke+V+Inf` => `nikk`
* `nikke` has no analyses either

**allegorisere** failures:

* `allegorisere+V+Inf` => `allegoriser`
* `allegorisere` has no analyses either

**avekse** failures:

* `avekse+V+Inf` does not generate!
* `avekse` has no analyses either

**lakesalte** failures:

* `lakesalte+V+Inf` => `lakesalt`
* `lakesalte` has no analyses either

**krøkje** failures:

* `krøkje+V+Inf` does not generate!
* `krøkje` has no analyses either

**attre** failures:

* `attre+V+Inf` => `attr`
* `attre` has following analyses:
  * `attre+A+Comp`

**blekke** failures:

* `blekke+V+Inf` => `bl`
* `blekke` has no analyses either

**såte** failures:

* `såte+V+Inf` => `såt`
* `såte` has no analyses either

**brede** failures:

* `brede+V+Inf` does not generate!
* `brede` has no analyses either

**yrkje** failures:

* `yrkje+V+Inf` does not generate!
* `yrkje` has no analyses either

**brolte** failures:

* `brolte+V+Inf` => `brolt`
* `brolte` has no analyses either

**bale** failures:

* `bale+V+Inf` => `bal`
* `bale` has following analyses:
  * `balen+A+Neu+Sg+Indef`

**dråtte** failures:

* `dråtte+V+Inf` => `drått`
* `dråtte` has no analyses either

**knope** failures:

* `knope+V+Inf` => `knop`
* `knope` has no analyses either

**bæle** failures:

* `bæle+V+Inf` => `bæl`
* `bæle` has following analyses:
  * `bælen+A+Neu+Sg+Indef`

**oppleve** failures:

* `oppleve+V+Inf` does not generate!
* `oppleve` has no analyses either

**teleksere** failures:

* `teleksere+V+Inf` => `telekser`
* `teleksere` has no analyses either

**rediskontere** failures:

* `rediskontere+V+Inf` => `rediskonter`
* `rediskontere` has no analyses either

**tase** failures:

* `tase+V+Inf` => `tas`
* `tase` has no analyses either

**minnast** failures:

* `minnast+V+Inf` => `minast`
* `minnast` has no analyses either

**trollta** failures:

* `trollta+V+Inf` => `trolltta`
* `trollta` has no analyses either

**bakvaske** failures:

* `bakvaske+V+Inf` => `bakvask`
* `bakvaske` has no analyses either

**kommunalisere** failures:

* `kommunalisere+V+Inf` => `kommunaliser`
* `kommunalisere` has no analyses either

**skjekte** failures:

* `skjekte+V+Inf` => `skjekt`
* `skjekte` has no analyses either

**misbyte** failures:

* `misbyte+V+Inf` => `misbyt`
* `misbyte` has no analyses either

**flekke** failures:

* `flekke+V+Inf` => `flekk`
* `flekke` has no analyses either

**harpunere** failures:

* `harpunere+V+Inf` => `harpuner`
* `harpunere` has no analyses either

**snikkontorisere** failures:

* `snikkontorisere+V+Inf` => `snikkontoriser`
* `snikkontorisere` has no analyses either

**diktere** failures:

* `diktere+V+Inf` => `dikter`
* `diktere` has no analyses either

**blamere** failures:

* `blamere+V+Inf` => `blamer`
* `blamere` has no analyses either

**solturke** failures:

* `solturke+V+Inf` => `solturk`
* `solturke` has no analyses either

**drifse** failures:

* `drifse+V+Inf` => `drifs`
* `drifse` has no analyses either

**sanere** failures:

* `sanere+V+Inf` => `saner`
* `sanere` has no analyses either

**øyremerke** failures:

* `øyremerke+V+Inf` => `øyremerk`
* `øyremerke` has no analyses either

**forbarme** failures:

* `forbarme+V+Inf` => `forbarm`
* `forbarme` has no analyses either

**purke** failures:

* `purke+V+Inf` => `purk`
* `purke` has no analyses either

**sloe** failures:

* `sloe+V+Inf` => `slo`
* `sloe` has no analyses either

**fullfylgje** failures:

* `fullfylgje+V+Inf` does not generate!
* `fullfylgje` has no analyses either

**risse** failures:

* `risse+V+Inf` => `riss`
* `risse` has no analyses either

**sjenere** failures:

* `sjenere+V+Inf` => `sjener`
* `sjenere` has no analyses either

**fire** failures:

* `fire+V+Inf` does not generate!
* `fire` has following analyses:
  * `fire+Num`

**streite** failures:

* `streite+V+Inf` => `streit`
* `streite` has following analyses:
  * `streit+A+A+Pl+Def`
  * `streit+A+A+Pl+Indef`
  * `streit+A+A+Sg+Def`

**verdsleggjere** failures:

* `verdsleggjere+V+Inf` does not generate!
* `verdsleggjere` has no analyses either

**hamre** failures:

* `hamre+V+Inf` => `hamr`
* `hamre` has no analyses either

**kavere** failures:

* `kavere+V+Inf` => `kaver`
* `kavere` has no analyses either

**plugge** failures:

* `plugge+V+Inf` => `plugg`
* `plugge` has no analyses either

**forbløffe** failures:

* `forbløffe+V+Inf` => `forbløff`
* `forbløffe` has no analyses either

**opphøgje** failures:

* `opphøgje+V+Inf` does not generate!
* `opphøgje` has no analyses either

**forvare** failures:

* `forvare+V+Inf` => `forvar`
* `forvare` has no analyses either

**utstråle** failures:

* `utstråle+V+Inf` => `utstrål`
* `utstråle` has no analyses either

**skikke** failures:

* `skikke+V+Inf` => `skikk`
* `skikke` has no analyses either

**klutre** failures:

* `klutre+V+Inf` => `klutr`
* `klutre` has no analyses either

**innskjerpe** failures:

* `innskjerpe+V+Inf` => `innskjerp`
* `innskjerpe` has no analyses either

**gurgle** failures:

* `gurgle+V+Inf` => `gurgl`
* `gurgle` has no analyses either

**dadre** failures:

* `dadre+V+Inf` => `dadr`
* `dadre` has no analyses either

**bureise** failures:

* `bureise+V+Inf` => `bureis`
* `bureise` has no analyses either

**ome** failures:

* `ome+V+Inf` => `om`
* `ome` has no analyses either

**godtgjere** failures:

* `godtgjere+V+Inf` does not generate!
* `godtgjere` has no analyses either

**rante** failures:

* `rante+V+Inf` => `rant`
* `rante` has no analyses either

**støyre** failures:

* `støyre+V+Inf` does not generate!
* `støyre` has no analyses either

**døyste** failures:

* `døyste+V+Inf` => `døyst`
* `døyste` has no analyses either

**ylme** failures:

* `ylme+V+Inf` does not generate!
* `ylme` has following analyses:
  * `ylmen+A+Neu+Sg+Indef`

**teste** failures:

* `teste+V+Inf` => `test`
* `teste` has no analyses either

**akkvirere** failures:

* `akkvirere+V+Inf` => `akkvirer`
* `akkvirere` has no analyses either

**stie** failures:

* `stie+V+Inf` => `sti`
* `stie` has following analyses:
  * `stige+V+Inf`
  * `stige+V+Adj+Nøyt+Ub+Eint`
  * `stikke+V+Inf`
  * `stige+V+PrfPrc`

**bløde** failures:

* `bløde+V+Inf` does not generate!
* `bløde` has no analyses either

**tamponere** failures:

* `tamponere+V+Inf` => `tamponer`
* `tamponere` has no analyses either

**elongere** failures:

* `elongere+V+Inf` => `elonger`
* `elongere` has no analyses either

**kjenne** failures:

* `kjenne+V+Inf` does not generate!
* `kjenne` has no analyses either

**installere** failures:

* `installere+V+Inf` => `installer`
* `installere` has no analyses either

**spase** failures:

* `spase+V+Inf` => `spas`
* `spase` has no analyses either

**blingse** failures:

* `blingse+V+Inf` => `blings`
* `blingse` has no analyses either

**overhøvle** failures:

* `overhøvle+V+Inf` => `overhøvl`
* `overhøvle` has no analyses either

**niskrike** failures:

* `niskrike+V+Inf` => `niskria`
* `niskrike+V+Inf` => `niskrie`
* `niskrike` has no analyses either

**forkjøle** failures:

* `forkjøle+V+Inf` => `forkjøl`
* `forkjøle` has no analyses either

**forskylde** failures:

* `forskylde+V+Inf` does not generate!
* `forskylde` has no analyses either

**divertere** failures:

* `divertere+V+Inf` => `diverter`
* `divertere` has no analyses either

**baklaste** failures:

* `baklaste+V+Inf` => `baklast`
* `baklaste` has no analyses either

**skive** failures:

* `skive+V+Inf` => `skiv`
* `skive` has no analyses either

**rige** failures:

* `rige+V+Inf` => `rig`
* `rige` has no analyses either

**befrukte** failures:

* `befrukte+V+Inf` => `befrukt`
* `befrukte` has no analyses either

**knyte** failures:

* `knyte+V+Inf` => `knyt`
* `knyte` has no analyses either

**aktorere** failures:

* `aktorere+V+Inf` => `aktorer`
* `aktorere` has no analyses either

**jamne** failures:

* `jamne+V+Inf` => `jamn`
* `jamne` has following analyses:
  * `jamn+A+Sg+Def`
  * `jamn+A+Pl+Indef`

**vesle** failures:

* `vesle+V+Inf` => `vesl`
* `vesle` has following analyses:
  * `vesle+A+A+Neu+Sg+Indef`
  * `vesle+A+A+Msc+Sg+Indef`
  * `vesle+A+A+Fem+Sg+Indef`

**joke** failures:

* `joke+V+Inf` => `jok`
* `joke` has no analyses either

**flengje** failures:

* `flengje+V+Inf` does not generate!
* `flengje` has no analyses either

**kroppsvisitere** failures:

* `kroppsvisitere+V+Inf` => `kroppsvisiter`
* `kroppsvisitere` has no analyses either

**forfriske** failures:

* `forfriske+V+Inf` => `forfrisk`
* `forfriske` has no analyses either

**hinkre** failures:

* `hinkre+V+Inf` => `hinkr`
* `hinkre` has no analyses either

**slåtre** failures:

* `slåtre+V+Inf` => `slåtr`
* `slåtre` has no analyses either

**blandast** failures:

* `blandast+V+Inf` => `blandastast`
* `blandast` has no analyses either

**partere** failures:

* `partere+V+Inf` => `parter`
* `partere` has no analyses either

**antecedere** failures:

* `antecedere+V+Inf` => `anteceder`
* `antecedere` has no analyses either

**tveite** failures:

* `tveite+V+Inf` => `tveit`
* `tveite` has no analyses either

**hallusinere** failures:

* `hallusinere+V+Inf` => `hallusiner`
* `hallusinere` has no analyses either

**kruse** failures:

* `kruse+V+Inf` does not generate!
* `kruse` has no analyses either

**meistre** failures:

* `meistre+V+Inf` => `meistr`
* `meistre` has no analyses either

**snerte** failures:

* `snerte+V+Inf` does not generate!
* `snerte` has following analyses:
  * `snerten+A+Neu+Sg+Indef`

**forråde** failures:

* `forråde+V+Inf` does not generate!
* `forråde` has no analyses either

**avslutte** failures:

* `avslutte+V+Inf` => `avslutt`
* `avslutte` has no analyses either

**trippe** failures:

* `trippe+V+Inf` => `tripp`
* `trippe` has no analyses either

**substituere** failures:

* `substituere+V+Inf` => `substituer`
* `substituere` has no analyses either

**vanhevde** failures:

* `vanhevde+V+Inf` => `vanhevd`
* `vanhevde` has no analyses either

**drenere** failures:

* `drenere+V+Inf` => `drener`
* `drenere` has no analyses either

**kryss-subsidiere** failures:

* `kryss-subsidiere+V+Inf` => `kryss-subsidier`
* `kryss-subsidiere` has no analyses either

**varveitsle** failures:

* `varveitsle+V+Inf` => `varveitsl`
* `varveitsle` has no analyses either

**sluske** failures:

* `sluske+V+Inf` => `slusk`
* `sluske` has no analyses either

**trykkje** failures:

* `trykkje+V+Inf` does not generate!
* `trykkje` has no analyses either

**tilbyde** failures:

* `tilbyde+V+Inf` does not generate!
* `tilbyde` has no analyses either

**floleggje** failures:

* `floleggje+V+Inf` does not generate!
* `floleggje` has no analyses either

**rumple** failures:

* `rumple+V+Inf` => `rumpl`
* `rumple` has no analyses either

**rasere** failures:

* `rasere+V+Inf` => `raser`
* `rasere` has no analyses either

**knistre** failures:

* `knistre+V+Inf` => `knistr`
* `knistre` has no analyses either

**kjølhale** failures:

* `kjølhale+V+Inf` does not generate!
* `kjølhale` has no analyses either

**forde** failures:

* `forde+V+Inf` => `ford`
* `forde` has no analyses either

**nose** failures:

* `nose+V+Inf` => `nos`
* `nose` has no analyses either

**tolke** failures:

* `tolke+V+Inf` => `tolk`
* `tolke` has no analyses either

**snikje** failures:

* `snikje+V+Inf` does not generate!
* `snikje` has no analyses either

**mergle** failures:

* `mergle+V+Inf` => `mergl`
* `mergle` has no analyses either

**faktorisere** failures:

* `faktorisere+V+Inf` => `faktoriser`
* `faktorisere` has no analyses either

**spee** failures:

* `spee+V+Inf` => `spe`
* `spee` has no analyses either

**sysselsetje** failures:

* `sysselsetje+V+Inf` does not generate!
* `sysselsetje` has no analyses either

**trolltake** failures:

* `trolltake+V+Inf` => `trollttaka`
* `trolltake+V+Inf` => `trollttake`
* `trolltake` has no analyses either

**keike** failures:

* `keike+V+Inf` => `keik`
* `keike` has following analyses:
  * `keik+A+Pl+Indef`
  * `keiken+A+Neu+Sg+Indef`
  * `keik+A+Sg+Def`

**handgje** failures:

* `handgje+V+Inf` => `handggje`
* `handgje` has no analyses either

**båsfore** failures:

* `båsfore+V+Inf` => `båsfor`
* `båsfore` has no analyses either

**ågre** failures:

* `ågre+V+Inf` => `ågr`
* `ågre` has no analyses either

**avbilde** failures:

* `avbilde+V+Inf` => `avbild`
* `avbilde` has no analyses either

**frakte** failures:

* `frakte+V+Inf` => `frakt`
* `frakte` has no analyses either

**datastyre** failures:

* `datastyre+V+Inf` does not generate!
* `datastyre` has no analyses either

**supprimere** failures:

* `supprimere+V+Inf` => `supprimer`
* `supprimere` has no analyses either

**ame** failures:

* `ame+V+Inf` => `am`
* `ame` has no analyses either

**stotre** failures:

* `stotre+V+Inf` => `stotr`
* `stotre` has no analyses either

**spite** failures:

* `spite+V+Inf` => `spit`
* `spite` has no analyses either

**krølle** failures:

* `krølle+V+Inf` => `krøll`
* `krølle` has no analyses either

**kalne** failures:

* `kalne+V+Inf` => `kaln`
* `kalne` has following analyses:
  * `kalen+A+Sg+Def`
  * `kalen+A+Pl+Indef`

**lågne** failures:

* `lågne+V+Inf` => `lågn`
* `lågne` has no analyses either

**stultre** failures:

* `stultre+V+Inf` => `stultr`
* `stultre` has no analyses either

**ugjerast** failures:

* `ugjerast+V+Inf` => `ugjeast`
* `ugjerast` has no analyses either

**pønse** failures:

* `pønse+V+Inf` => `pøns`
* `pønse` has no analyses either

**pete** failures:

* `pete+V+Inf` => `pet`
* `pete` has no analyses either

**netje** failures:

* `netje+V+Inf` => `netj`
* `netje` has no analyses either

**forske** failures:

* `forske+V+Inf` => `forsk`
* `forske` has following analyses:
  * `forskyve+V+Inf`

**stakre** failures:

* `stakre+V+Inf` => `stakr`
* `stakre` has no analyses either

**pore** failures:

* `pore+V+Inf` => `por`
* `pore` has no analyses either

**subbe** failures:

* `subbe+V+Inf` => `subb`
* `subbe` has no analyses either

**tendre** failures:

* `tendre+V+Inf` => `tendr`
* `tendre` has no analyses either

**lyste** failures:

* `lyste+V+Inf` does not generate!
* `lyste` has following analyses:
  * `lysten+A+Neu+Sg+Indef`
  * `lyse+V+PrfPrc`

**antesipere** failures:

* `antesipere+V+Inf` => `antesiper`
* `antesipere` has no analyses either

**blome** failures:

* `blome+V+Inf` => `blom`
* `blome` has no analyses either

**belgje** failures:

* `belgje+V+Inf` => `belg`
* `belgje` has no analyses either

**utløyse** failures:

* `utløyse+V+Inf` => `utløys`
* `utløyse` has no analyses either

**sludre** failures:

* `sludre+V+Inf` => `sludr`
* `sludre` has no analyses either

**trollstemme** failures:

* `trollstemme+V+Inf` does not generate!
* `trollstemme` has no analyses either

**refse** failures:

* `refse+V+Inf` does not generate!
* `refse` has no analyses either

**klipe** failures:

* `klipe+V+Inf` => `klie`
* `klipe+V+Inf` => `klia`
* `klipe` has no analyses either

**tvangsdirigere** failures:

* `tvangsdirigere+V+Inf` => `tvangsdiriger`
* `tvangsdirigere` has no analyses either

**rike** failures:

* `rike+V+Inf` => `rik`
* `rike` has following analyses:
  * `rik+A+Pl+Indef`
  * `rik+A+Sg+Def`

**sløse** failures:

* `sløse+V+Inf` => `sløs`
* `sløse` has following analyses:
  * `sløsen+A+Neu+Sg+Indef`

**kokevaske** failures:

* `kokevaske+V+Inf` => `kokevask`
* `kokevaske` has no analyses either

**tryggje** failures:

* `tryggje+V+Inf` does not generate!
* `tryggje` has no analyses either

**take** failures:

* `take+V+Inf` => `ttaka`
* `take+V+Inf` => `ttake`
* `take` has no analyses either

**jelke** failures:

* `jelke+V+Inf` => `jelk`
* `jelke` has no analyses either

**svinne** failures:

* `svinne+V+Inf` => `svia`
* `svinne+V+Inf` => `svie`
* `svinne` has no analyses either

**ræle** failures:

* `ræle+V+Inf` => `ræl`
* `ræle` has no analyses either

**etterleve** failures:

* `etterleve+V+Inf` does not generate!
* `etterleve` has no analyses either

**gestalte** failures:

* `gestalte+V+Inf` => `gestalt`
* `gestalte` has no analyses either

**hagle** failures:

* `hagle+V+Inf` => `hagl`
* `hagle` has no analyses either

**floppe** failures:

* `floppe+V+Inf` => `flopp`
* `floppe` has no analyses either

**skrattle** failures:

* `skrattle+V+Inf` => `skrattlle`
* `skrattle` has no analyses either

**nøyde** failures:

* `nøyde+V+Inf` does not generate!
* `nøyde` has no analyses either

**implisere** failures:

* `implisere+V+Inf` => `impliser`
* `implisere` has no analyses either

**skvadronere** failures:

* `skvadronere+V+Inf` => `skvadroner`
* `skvadronere` has no analyses either

**moppe** failures:

* `moppe+V+Inf` => `mopp`
* `moppe` has no analyses either

**forsøke** failures:

* `forsøke+V+Inf` => `forsøk`
* `forsøke` has no analyses either

**fånast** failures:

* `fånast+V+Inf` => `fånastast`
* `fånast` has following analyses:
  * `fåen+A+Superl+Indef`

**innvie** failures:

* `innvie+V+Inf` does not generate!
* `innvie` has no analyses either

**sladre** failures:

* `sladre+V+Inf` => `sladr`
* `sladre` has no analyses either

**klinsje** failures:

* `klinsje+V+Inf` => `klinsj`
* `klinsje` has no analyses either

**bysje** failures:

* `bysje+V+Inf` => `bysj`
* `bysje` has no analyses either

**stengje** failures:

* `stengje+V+Inf` does not generate!
* `stengje` has no analyses either

**takle** failures:

* `takle+V+Inf` => `takl`
* `takle` has no analyses either

**veikje** failures:

* `veikje+V+Inf` does not generate!
* `veikje` has no analyses either

**gjennomsyre** failures:

* `gjennomsyre+V+Inf` does not generate!
* `gjennomsyre` has no analyses either

**halvte** failures:

* `halvte+V+Inf` => `halvt`
* `halvte` has no analyses either

**forvise** failures:

* `forvise+V+Inf` => `forvis`
* `forvise` has no analyses either

**ymse** failures:

* `ymse+V+Inf` => `yms`
* `ymse` has following analyses:
  * `ymis+A+Pl+Indef`
  * `ymsen+A+Neu+Sg+Indef`
  * `ymis+A+Sg+Def`

**bortforklare** failures:

* `bortforklare+V+Inf` does not generate!
* `bortforklare` has no analyses either

**forgifte** failures:

* `forgifte+V+Inf` => `forgift`
* `forgifte` has no analyses either

**kope** failures:

* `kope+V+Inf` => `kop`
* `kope` has no analyses either

**kastebyte** failures:

* `kastebyte+V+Inf` => `kastebyt`
* `kastebyte` has no analyses either

**overvintre** failures:

* `overvintre+V+Inf` => `overvintr`
* `overvintre` has no analyses either

**hegre** failures:

* `hegre+V+Inf` => `hegr`
* `hegre` has no analyses either

**småspringe** failures:

* `småspringe+V+Inf` => `småsprie`
* `småspringe+V+Inf` => `småspria`
* `småspringe` has no analyses either

**snobbe** failures:

* `snobbe+V+Inf` => `snobb`
* `snobbe` has no analyses either

**banepålegge** failures:

* `banepålegge+V+Inf` does not generate!
* `banepålegge` has no analyses either

**differere** failures:

* `differere+V+Inf` => `differer`
* `differere` has no analyses either

**uroe** failures:

* `uroe+V+Inf` => `uro`
* `uroe` has no analyses either

**handse** failures:

* `handse+V+Inf` => `hands`
* `handse` has no analyses either

**semaforere** failures:

* `semaforere+V+Inf` => `semaforer`
* `semaforere` has no analyses either

**stalme** failures:

* `stalme+V+Inf` => `stalm`
* `stalme` has no analyses either

**beite** failures:

* `beite+V+Inf` does not generate!
* `beite` has no analyses either

**mentalundersøkje** failures:

* `mentalundersøkje+V+Inf` does not generate!
* `mentalundersøkje` has no analyses either

**myklast** failures:

* `myklast+V+Inf` => `myklastast`
* `myklast` has no analyses either

**stivne** failures:

* `stivne+V+Inf` => `stivn`
* `stivne` has no analyses either

**vake** failures:

* `vake+V+Inf` => `vak`
* `vake` has following analyses:
  * `vaken+A+Neu+Sg+Indef`
  * `vakker+A+A+Pl+Indef`
  * `vak+A+Sg+Def`
  * `vak+A+Pl+Indef`
  * `vakker+A+A+Pl+Def`
  * `vakker+A+A+Sg+Def`

**desarmere** failures:

* `desarmere+V+Inf` => `desarmer`
* `desarmere` has no analyses either

**forstrekke** failures:

* `forstrekke+V+Inf` does not generate!
* `forstrekke` has no analyses either

**ferdigstille** failures:

* `ferdigstille+V+Inf` => `ferdigstile`
* `ferdigstille+V+Inf` => `ferdigstila`
* `ferdigstille` has no analyses either

**adoptere** failures:

* `adoptere+V+Inf` => `adopter`
* `adoptere` has no analyses either

**følgje** failures:

* `følgje+V+Inf` does not generate!
* `følgje` has no analyses either

**inhibere** failures:

* `inhibere+V+Inf` => `inhiber`
* `inhibere` has no analyses either

**boikotte** failures:

* `boikotte+V+Inf` => `boikott`
* `boikotte` has no analyses either

**glutte** failures:

* `glutte+V+Inf` => `glutt`
* `glutte` has no analyses either

**luske** failures:

* `luske+V+Inf` => `lusk`
* `luske` has no analyses either

**resitere** failures:

* `resitere+V+Inf` => `resiter`
* `resitere` has no analyses either

**forsegne** failures:

* `forsegne+V+Inf` => `forsegn`
* `forsegne` has no analyses either

**fortone** failures:

* `fortone+V+Inf` => `forton`
* `fortone` has no analyses either

**foredle** failures:

* `foredle+V+Inf` => `foredl`
* `foredle` has no analyses either

**avstive** failures:

* `avstive+V+Inf` => `avstiv`
* `avstive` has no analyses either

**amstre** failures:

* `amstre+V+Inf` => `amstr`
* `amstre` has no analyses either

**illuminere** failures:

* `illuminere+V+Inf` => `illuminer`
* `illuminere` has no analyses either

**kontrollere** failures:

* `kontrollere+V+Inf` => `kontroller`
* `kontrollere` has no analyses either

**bowle** failures:

* `bowle+V+Inf` => `bowl`
* `bowle` has no analyses either

**krøkne** failures:

* `krøkne+V+Inf` => `krøkn`
* `krøkne` has following analyses:
  * `krøken+A+Sg+Def`
  * `krøken+A+Pl+Indef`

**fylke** failures:

* `fylke+V+Inf` => `fylk`
* `fylke` has no analyses either

**støye** failures:

* `støye+V+Inf` => `støy`
* `støye` has no analyses either

**spekulere** failures:

* `spekulere+V+Inf` => `spekuler`
* `spekulere` has no analyses either

**rate** failures:

* `rate+V+Inf` => `rat`
* `rate` has following analyses:
  * `raten+A+Neu+Sg+Indef`

**stikne** failures:

* `stikne+V+Inf` => `stikn`
* `stikne` has no analyses either

**vandre** failures:

* `vandre+V+Inf` => `vandr`
* `vandre` has no analyses either

**debutere** failures:

* `debutere+V+Inf` => `debuter`
* `debutere` has no analyses either

**legere** failures:

* `legere+V+Inf` => `leger`
* `legere` has no analyses either

**breie** failures:

* `breie+V+Inf` does not generate!
* `breie` has following analyses:
  * `brei+A+Pl+Indef`
  * `brei+A+Sg+Def`

**væte** failures:

* `væte+V+Inf` => `væt`
* `væte` has no analyses either

**heilgardere** failures:

* `heilgardere+V+Inf` => `heilgarder`
* `heilgardere` has no analyses either

**transformere** failures:

* `transformere+V+Inf` => `transformer`
* `transformere` has no analyses either

**tjuvfiske** failures:

* `tjuvfiske+V+Inf` => `tjuvfisk`
* `tjuvfiske` has no analyses either

**marknadsføre** failures:

* `marknadsføre+V+Inf` => `marknadsfør`
* `marknadsføre` has no analyses either

**knurre** failures:

* `knurre+V+Inf` => `knurr`
* `knurre` has no analyses either

**framkalle** failures:

* `framkalle+V+Inf` => `framkall`
* `framkalle` has no analyses either

**spildre** failures:

* `spildre+V+Inf` => `spildr`
* `spildre` has following analyses:
  * `spildren+A+Neu+Sg+Indef`

**snøse** failures:

* `snøse+V+Inf` => `snøs`
* `snøse` has no analyses either

**ramle** failures:

* `ramle+V+Inf` => `raml`
* `ramle` has no analyses either

**bunke** failures:

* `bunke+V+Inf` => `bunk`
* `bunke` has no analyses either

**trone** failures:

* `trone+V+Inf` => `tron`
* `trone` has no analyses either

**sjefe** failures:

* `sjefe+V+Inf` => `sjef`
* `sjefe` has no analyses either

**avhjelpe** failures:

* `avhjelpe+V+Inf` => `avhjelp`
* `avhjelpe` has no analyses either

**tjuvlese** failures:

* `tjuvlese+V+Inf` => `tjuvlea`
* `tjuvlese+V+Inf` => `tjuvlee`
* `tjuvlese` has no analyses either

**karte** failures:

* `karte+V+Inf` => `kart`
* `karte` has no analyses either

**smøre** failures:

* `smøre+V+Inf` => `smør`
* `smøre` has no analyses either

**sæte** failures:

* `sæte+V+Inf` => `sæt`
* `sæte` has no analyses either

**klundre** failures:

* `klundre+V+Inf` => `klundr`
* `klundre` has no analyses either

**uttverve** failures:

* `uttverve+V+Inf` does not generate!
* `uttverve` has no analyses either

**kolte** failures:

* `kolte+V+Inf` => `kolt`
* `kolte` has following analyses:
  * `kolten+A+Neu+Sg+Indef`

**spækje** failures:

* `spækje+V+Inf` does not generate!
* `spækje` has no analyses either

**bordklede** failures:

* `bordklede+V+Inf` does not generate!
* `bordklede` has no analyses either

**jibbe** failures:

* `jibbe+V+Inf` => `jibb`
* `jibbe` has no analyses either

**feide** failures:

* `feide+V+Inf` => `feid`
* `feide` has no analyses either

**kulte** failures:

* `kulte+V+Inf` => `kult`
* `kulte` has following analyses:
  * `kulten+A+Neu+Sg+Indef`

**skimte** failures:

* `skimte+V+Inf` => `skimt`
* `skimte` has no analyses either

**durle** failures:

* `durle+V+Inf` => `durl`
* `durle` has no analyses either

**utnytte** failures:

* `utnytte+V+Inf` => `utnytt`
* `utnytte` has no analyses either

**profanere** failures:

* `profanere+V+Inf` => `profaner`
* `profanere` has no analyses either

**fleirfalde** failures:

* `fleirfalde+V+Inf` => `fleirfald`
* `fleirfalde` has no analyses either

**olje** failures:

* `olje+V+Inf` => `olj`
* `olje` has no analyses either

**dåre** failures:

* `dåre+V+Inf` => `dår`
* `dåre` has no analyses either

**mulkte** failures:

* `mulkte+V+Inf` => `mulkt`
* `mulkte` has no analyses either

**piruettere** failures:

* `piruettere+V+Inf` => `piruetter`
* `piruettere` has no analyses either

**jare** failures:

* `jare+V+Inf` => `jar`
* `jare` has following analyses:
  * `jarren+A+Neu+Sg+Indef`

**oksidere** failures:

* `oksidere+V+Inf` => `oksider`
* `oksidere` has no analyses either

**trale** failures:

* `trale+V+Inf` does not generate!
* `trale` has no analyses either

**gildre** failures:

* `gildre+V+Inf` => `gildr`
* `gildre` has no analyses either

**kontakte** failures:

* `kontakte+V+Inf` => `kontakt`
* `kontakte` has no analyses either

**vòke** failures:

* `vòke+V+Inf` => `vòk`
* `vòke` has no analyses either

**vifte** failures:

* `vifte+V+Inf` => `vift`
* `vifte` has no analyses either

**hore** failures:

* `hore+V+Inf` => `hor`
* `hore` has no analyses either

**harvle** failures:

* `harvle+V+Inf` => `harvl`
* `harvle` has no analyses either

**insitere** failures:

* `insitere+V+Inf` => `insiter`
* `insitere` has no analyses either

**innvilge** failures:

* `innvilge+V+Inf` => `innvilg`
* `innvilge` has no analyses either

**pludre** failures:

* `pludre+V+Inf` => `pludr`
* `pludre` has no analyses either

**kveikje** failures:

* `kveikje+V+Inf` does not generate!
* `kveikje` has no analyses either

**hysse** failures:

* `hysse+V+Inf` => `hyss`
* `hysse` has no analyses either

**slikke** failures:

* `slikke+V+Inf` => `slikk`
* `slikke` has no analyses either

**instruere** failures:

* `instruere+V+Inf` => `instruer`
* `instruere` has no analyses either

**forblende** failures:

* `forblende+V+Inf` => `forblend`
* `forblende` has no analyses either

**glyfse** failures:

* `glyfse+V+Inf` => `glyfs`
* `glyfse` has no analyses either

**umenneskjeleggjere** failures:

* `umenneskjeleggjere+V+Inf` does not generate!
* `umenneskjeleggjere` has no analyses either

**datere** failures:

* `datere+V+Inf` => `dater`
* `datere` has no analyses either

**kjeppast** failures:

* `kjeppast+V+Inf` => `kjepast`
* `kjeppast` has no analyses either

**jaksle** failures:

* `jaksle+V+Inf` => `jaksl`
* `jaksle` has no analyses either

**spenne** failures:

* `spenne+V+Inf` does not generate!
* `spenne` has no analyses either

**konspirere** failures:

* `konspirere+V+Inf` => `konspirer`
* `konspirere` has no analyses either

**profetere** failures:

* `profetere+V+Inf` => `profeter`
* `profetere` has no analyses either

**portere** failures:

* `portere+V+Inf` => `porter`
* `portere` has no analyses either

**objektivere** failures:

* `objektivere+V+Inf` => `objektiver`
* `objektivere` has no analyses either

**møte** failures:

* `møte+V+Inf` => `møt`
* `møte` has no analyses either

**bølgje** failures:

* `bølgje+V+Inf` => `bølg`
* `bølgje` has no analyses either

**sentre** failures:

* `sentre+V+Inf` => `sentr`
* `sentre` has no analyses either

**brynne** failures:

* `brynne+V+Inf` => `brynn`
* `brynne` has no analyses either

**omtale** failures:

* `omtale+V+Inf` does not generate!
* `omtale` has no analyses either

**pire** failures:

* `pire+V+Inf` => `pir`
* `pire` has no analyses either

**setre** failures:

* `setre+V+Inf` => `setr`
* `setre` has no analyses either

**trassere** failures:

* `trassere+V+Inf` => `trasser`
* `trassere` has no analyses either

**forrenne** failures:

* `forrenne+V+Inf` does not generate!
* `forrenne` has no analyses either

**premiere** failures:

* `premiere+V+Inf` => `premier`
* `premiere` has no analyses either

**bulke** failures:

* `bulke+V+Inf` => `bulk`
* `bulke` has no analyses either

**smatte** failures:

* `smatte+V+Inf` => `smatt`
* `smatte` has no analyses either

**skrense** failures:

* `skrense+V+Inf` => `skrens`
* `skrense` has no analyses either

**avblåse** failures:

* `avblåse+V+Inf` => `avbl`
* `avblåse` has following analyses:
  * `avblåsen+A+Neu+Sg+Indef`

**ringle** failures:

* `ringle+V+Inf` => `ringl`
* `ringle` has no analyses either

**semje** failures:

* `semje+V+Inf` does not generate!
* `semje` has no analyses either

**sjakre** failures:

* `sjakre+V+Inf` => `sjakr`
* `sjakre` has no analyses either

**dulte** failures:

* `dulte+V+Inf` => `dult`
* `dulte` has no analyses either

**forvalte** failures:

* `forvalte+V+Inf` => `forvalt`
* `forvalte` has no analyses either

**kontingentere** failures:

* `kontingentere+V+Inf` => `kontingenter`
* `kontingentere` has no analyses either

**husbankfinansiere** failures:

* `husbankfinansiere+V+Inf` => `husbankfinansier`
* `husbankfinansiere` has no analyses either

**duke** failures:

* `duke+V+Inf` => `duk`
* `duke` has no analyses either

**vræle** failures:

* `vræle+V+Inf` does not generate!
* `vræle` has no analyses either

**galoppere** failures:

* `galoppere+V+Inf` => `galopper`
* `galoppere` has no analyses either

**omstrukturere** failures:

* `omstrukturere+V+Inf` => `omstrukturer`
* `omstrukturere` has no analyses either

**fororde** failures:

* `fororde+V+Inf` => `forord`
* `fororde` has no analyses either

**våtsalte** failures:

* `våtsalte+V+Inf` => `våtsalt`
* `våtsalte` has no analyses either

**kamme** failures:

* `kamme+V+Inf` => `kamm`
* `kamme` has no analyses either

**parallellskyve** failures:

* `parallellskyve+V+Inf` => `parallellskye`
* `parallellskyve+V+Inf` => `parallellskya`
* `parallellskyve` has no analyses either

**skygge** failures:

* `skygge+V+Inf` does not generate!
* `skygge` has no analyses either

**teksle** failures:

* `teksle+V+Inf` => `teksl`
* `teksle` has no analyses either

**trotne** failures:

* `trotne+V+Inf` => `trotn`
* `trotne` has following analyses:
  * `troten+A+Sg+Def`
  * `troten+A+Pl+Indef`

**vitalisere** failures:

* `vitalisere+V+Inf` => `vitaliser`
* `vitalisere` has no analyses either

**strekke** failures:

* `strekke+V+Inf` does not generate!
* `strekke` has no analyses either

**dumme** failures:

* `dumme+V+Inf` => `dum`
* `dumme` has no analyses either

**bivre** failures:

* `bivre+V+Inf` => `bivr`
* `bivre` has no analyses either

**sakte** failures:

* `sakte+V+Inf` => `sakt`
* `sakte` has following analyses:
  * `sakte+A+Neu+Sg+Indef`
  * `sakte+A+Sg+Def`
  * `sakte+A+Msc+Sg+Indef`
  * `sakte+A+Fem+Sg+Indef`
  * `sakte+A+Pl+Indef`

**utrangere** failures:

* `utrangere+V+Inf` => `utranger`
* `utrangere` has no analyses either

**ale** failures:

* `ale+V+Inf` does not generate!
* `ale` has following analyses:
  * `ale+V`
  * `alen+A+Neu+Sg+Indef`

**larke** failures:

* `larke+V+Inf` => `lark`
* `larke` has no analyses either

**sprenglese** failures:

* `sprenglese+V+Inf` => `sprenglee`
* `sprenglese+V+Inf` => `sprenglea`
* `sprenglese` has no analyses either

**blekkje** failures:

* `blekkje+V+Inf` does not generate!
* `blekkje` has no analyses either

**bolke** failures:

* `bolke+V+Inf` => `bolk`
* `bolke` has no analyses either

**trumfe** failures:

* `trumfe+V+Inf` => `trumf`
* `trumfe` has no analyses either

**ampast** failures:

* `ampast+V+Inf` => `ampastast`
* `ampast` has following analyses:
  * `amper+A+A+Superl+Indef`

**kave** failures:

* `kave+V+Inf` => `kav`
* `kave` has no analyses either

**jakte** failures:

* `jakte+V+Inf` => `jakt`
* `jakte` has no analyses either

**røse** failures:

* `røse+V+Inf` => `røs`
* `røse` has following analyses:
  * `røsen+A+Neu+Sg+Indef`

**susse** failures:

* `susse+V+Inf` => `suss`
* `susse` has no analyses either

**redusere** failures:

* `redusere+V+Inf` => `reduser`
* `redusere` has no analyses either

**rande** failures:

* `rande+V+Inf` => `rand`
* `rande` has no analyses either

**utpante** failures:

* `utpante+V+Inf` => `utpant`
* `utpante` has no analyses either

**håtte** failures:

* `håtte+V+Inf` => `hått`
* `håtte` has no analyses either

**fosne** failures:

* `fosne+V+Inf` => `fosn`
* `fosne` has following analyses:
  * `fosen+A+Sg+Def`
  * `fosen+A+Pl+Indef`

**dressere** failures:

* `dressere+V+Inf` => `dresser`
* `dressere` has no analyses either

**balkanisere** failures:

* `balkanisere+V+Inf` => `balkaniser`
* `balkanisere` has no analyses either

**ondulere** failures:

* `ondulere+V+Inf` => `onduler`
* `ondulere` has no analyses either

**fabrikkere** failures:

* `fabrikkere+V+Inf` => `fabrikker`
* `fabrikkere` has no analyses either

**fortake** failures:

* `fortake+V+Inf` => `forttaka`
* `fortake+V+Inf` => `forttake`
* `fortake` has no analyses either

**øksne** failures:

* `øksne+V+Inf` => `øksn`
* `øksne` has following analyses:
  * `øksen+A+Sg+Def`
  * `øksen+A+Pl+Indef`

**finmale** failures:

* `finmale+V+Inf` => `finmae`
* `finmale+V+Inf` => `finmaa`
* `finmale` has following analyses:
  * `finmalen+A+Neu+Sg+Indef`

**heimsjå** failures:

* `heimsjå+V+Inf` => `heimssjå`
* `heimsjå` has no analyses either

**kvinke** failures:

* `kvinke+V+Inf` => `kvink`
* `kvinke` has no analyses either

**akklimatisere** failures:

* `akklimatisere+V+Inf` => `akklimatiser`
* `akklimatisere` has no analyses either

**flabbe** failures:

* `flabbe+V+Inf` => `flabb`
* `flabbe` has no analyses either

**jule** failures:

* `jule+V+Inf` => `jul`
* `jule` has no analyses either

**krumme** failures:

* `krumme+V+Inf` => `krum`
* `krumme` has no analyses either

**heine** failures:

* `heine+V+Inf` => `hein`
* `heine` has no analyses either

**umenneskeleggjere** failures:

* `umenneskeleggjere+V+Inf` does not generate!
* `umenneskeleggjere` has no analyses either

**marginalisere** failures:

* `marginalisere+V+Inf` => `marginaliser`
* `marginalisere` has no analyses either

**pimpe** failures:

* `pimpe+V+Inf` => `pimp`
* `pimpe` has no analyses either

**gode** failures:

* `gode+V+Inf` => `god`
* `gode` has following analyses:
  * `god+A+Sg+Def`
  * `god+A+Pl+Indef`
  * `god+A+Pl+Def`

**grafse** failures:

* `grafse+V+Inf` => `grafs`
* `grafse` has no analyses either

**kappdrikke** failures:

* `kappdrikke+V+Inf` => `kappdrie`
* `kappdrikke+V+Inf` => `kappdria`
* `kappdrikke` has no analyses either

**iterere** failures:

* `iterere+V+Inf` => `iterer`
* `iterere` has no analyses either

**konkretisere** failures:

* `konkretisere+V+Inf` => `konkretiser`
* `konkretisere` has no analyses either

**slabbe** failures:

* `slabbe+V+Inf` => `slabb`
* `slabbe` has no analyses either

**tjadre** failures:

* `tjadre+V+Inf` => `tjadr`
* `tjadre` has following analyses:
  * `tjadren+A+Neu+Sg+Indef`

**shake** failures:

* `shake+V+Inf` => `shak`
* `shake` has no analyses either

**nødlande** failures:

* `nødlande+V+Inf` => `nødland`
* `nødlande` has no analyses either

**ylje** failures:

* `ylje+V+Inf` does not generate!
* `ylje` has following analyses:
  * `ylje+V`

**vevje** failures:

* `vevje+V+Inf` does not generate!
* `vevje` has no analyses either

**hæle** failures:

* `hæle+V+Inf` => `hæl`
* `hæle` has no analyses either

**sparre** failures:

* `sparre+V+Inf` => `sparr`
* `sparre` has no analyses either

**urydde** failures:

* `urydde+V+Inf` => `urydd`
* `urydde` has no analyses either

**wokke** failures:

* `wokke+V+Inf` => `wokk`
* `wokke` has no analyses either

**skante** failures:

* `skante+V+Inf` => `skant`
* `skante` has no analyses either

**fortinne** failures:

* `fortinne+V+Inf` => `fortinn`
* `fortinne` has no analyses either

**handvalke** failures:

* `handvalke+V+Inf` => `handvalk`
* `handvalke` has no analyses either

**avspegle** failures:

* `avspegle+V+Inf` => `avspegl`
* `avspegle` has no analyses either

**kinkse** failures:

* `kinkse+V+Inf` => `kinks`
* `kinkse` has no analyses either

**attgje** failures:

* `attgje+V+Inf` => `attggje`
* `attgje` has no analyses either

**floe** failures:

* `floe+V+Inf` => `flo`
* `floe` has following analyses:
  * `flyge+V+PrfPrc`
  * `flyge+V+Adj+Nøyt+Ub+Eint`

**svive** failures:

* `svive+V+Inf` => `svia`
* `svive+V+Inf` => `svie`
* `svive` has no analyses either

**kvile** failures:

* `kvile+V+Inf` => `kvil`
* `kvile` has no analyses either

**brunke** failures:

* `brunke+V+Inf` => `brunk`
* `brunke` has no analyses either

**fabulere** failures:

* `fabulere+V+Inf` => `fabuler`
* `fabulere` has no analyses either

**smeite** failures:

* `smeite+V+Inf` => `smeit`
* `smeite` has no analyses either

**loe** failures:

* `loe+V+Inf` => `lo`
* `loe` has following analyses:
  * `lo+A+A+Pl+Indef`
  * `lyge+V+Adj+Nøyt+Ub+Eint`
  * `lo+A+A+Pl+Def`
  * `lyge+V+PrfPrc`
  * `ljuge+V+Adj+Nøyt+Ub+Eint`
  * `lo+A+A+Sg+Def`
  * `ljuge+V+PrfPrc`

**glåme** failures:

* `glåme+V+Inf` does not generate!
* `glåme` has no analyses either

**kvekkje** failures:

* `kvekkje+V+Inf` does not generate!
* `kvekkje` has no analyses either

**fullbyrde** failures:

* `fullbyrde+V+Inf` => `fullbyrd`
* `fullbyrde` has no analyses either

**gimle** failures:

* `gimle+V+Inf` => `giml`
* `gimle` has no analyses either

**grenje** failures:

* `grenje+V+Inf` => `grenj`
* `grenje` has no analyses either

**kvittere** failures:

* `kvittere+V+Inf` => `kvitter`
* `kvittere` has no analyses either

**moe** failures:

* `moe+V+Inf` => `mo`
* `moe` has following analyses:
  * `moen+A+Neu+Sg+Indef`
  * `mo+A+A+Sg+Def`
  * `mo+A+Pl+Indef`
  * `mo+A+Sg+Def`
  * `mo+A+A+Pl+Indef`
  * `mo+A+A+Pl+Def`

**arrivere** failures:

* `arrivere+V+Inf` => `arriver`
* `arrivere` has no analyses either

**ransake** failures:

* `ransake+V+Inf` => `ransak`
* `ransake` has no analyses either

**ominnreie** failures:

* `ominnreie+V+Inf` does not generate!
* `ominnreie` has no analyses either

**knappe** failures:

* `knappe+V+Inf` => `knapp`
* `knappe` has no analyses either

**fastsetje** failures:

* `fastsetje+V+Inf` does not generate!
* `fastsetje` has no analyses either

**ombestemme** failures:

* `ombestemme+V+Inf` => `ombestema`
* `ombestemme+V+Inf` => `ombesteme`
* `ombestemme` has no analyses either

**kjede** failures:

* `kjede+V+Inf` => `kjed`
* `kjede` has no analyses either

**skunse** failures:

* `skunse+V+Inf` => `skuns`
* `skunse` has no analyses either

**tane** failures:

* `tane+V+Inf` => `tan`
* `tane` has no analyses either

**gardfare** failures:

* `gardfare+V+Inf` => `gardfaa`
* `gardfare+V+Inf` => `gardfae`
* `gardfare` has no analyses either

**honkle** failures:

* `honkle+V+Inf` => `honkl`
* `honkle` has no analyses either

**gnudre** failures:

* `gnudre+V+Inf` => `gnudr`
* `gnudre` has no analyses either

**deponere** failures:

* `deponere+V+Inf` => `deponer`
* `deponere` has no analyses either

**matte** failures:

* `matte+V+Inf` => `matt`
* `matte` has following analyses:
  * `matt+A+A+Sg+Def`
  * `matt+A+A+Pl+Def`
  * `matt+A+A+Pl+Indef`

**steinhogge** failures:

* `steinhogge+V+Inf` does not generate!
* `steinhogge` has no analyses either

**avreie** failures:

* `avreie+V+Inf` does not generate!
* `avreie` has no analyses either

**krokne** failures:

* `krokne+V+Inf` => `krokn`
* `krokne` has following analyses:
  * `kroken+A+Pl+Indef`
  * `kroken+A+Sg+Def`

**radikalisere** failures:

* `radikalisere+V+Inf` => `radikaliser`
* `radikalisere` has no analyses either

**svelte** failures:

* `svelte+V+Inf` does not generate!
* `svelte` has no analyses either

**brune** failures:

* `brune+V+Inf` => `brun`
* `brune` has following analyses:
  * `brun+A+Pl+Indef`
  * `brun+A+Sg+Def`

**melde** failures:

* `melde+V+Inf` does not generate!
* `melde` has no analyses either

**mjukne** failures:

* `mjukne+V+Inf` => `mjukn`
* `mjukne` has no analyses either

**skeise** failures:

* `skeise+V+Inf` => `skeis`
* `skeise` has no analyses either

**frykte** failures:

* `frykte+V+Inf` => `frykt`
* `frykte` has no analyses either

**førehandsbestille** failures:

* `førehandsbestille+V+Inf` => `førehandsbestila`
* `førehandsbestille+V+Inf` => `førehandsbestile`
* `førehandsbestille` has no analyses either

**åbyrgsle** failures:

* `åbyrgsle+V+Inf` => `åbyrgsl`
* `åbyrgsle` has no analyses either

**patte** failures:

* `patte+V+Inf` => `patt`
* `patte` has following analyses:
  * `patt+A+A+Pl+Def`
  * `patt+A+A+Pl+Indef`
  * `patt+A+A+Sg+Def`

**føyrast** failures:

* `føyrast+V+Inf` => `føyrastast`
* `føyrast` has following analyses:
  * `føyr+A+Superl+Indef`

**råke** failures:

* `råke+V+Inf` => `råk`
* `råke` has no analyses either

**spotte** failures:

* `spotte+V+Inf` => `spott`
* `spotte` has no analyses either

**plikte** failures:

* `plikte+V+Inf` => `plikt`
* `plikte` has no analyses either

**namngjeve** failures:

* `namngjeve+V+Inf` => `namnggjeva`
* `namngjeve+V+Inf` => `namnggjeve`
* `namngjeve` has no analyses either

**snope** failures:

* `snope+V+Inf` => `snop`
* `snope` has no analyses either

**leite** failures:

* `leite+V+Inf` does not generate!
* `leite` has no analyses either

**skye** failures:

* `skye+V+Inf` => `sky`
* `skye` has following analyses:
  * `skyte+V+Inf`
  * `sky+A+A+Pl+Def`
  * `sky+A+A+Pl+Indef`
  * `sky+A+A+Sg+Def`

**selje** failures:

* `selje+V+Inf` => `sel`
* `selje` has no analyses either

**aptere** failures:

* `aptere+V+Inf` => `apter`
* `aptere` has no analyses either

**kelatere** failures:

* `kelatere+V+Inf` => `kelater`
* `kelatere` has no analyses either

**holme** failures:

* `holme+V+Inf` => `holm`
* `holme` has no analyses either

**væpne** failures:

* `væpne+V+Inf` => `væpn`
* `væpne` has no analyses either

**småle** failures:

* `småle+V+Inf` => `smålle`
* `småle` has no analyses either

**sluse** failures:

* `sluse+V+Inf` => `slus`
* `sluse` has no analyses either

**hildre** failures:

* `hildre+V+Inf` => `hildr`
* `hildre` has no analyses either

**oreigne** failures:

* `oreigne+V+Inf` => `oreign`
* `oreigne` has no analyses either

**sterilisere** failures:

* `sterilisere+V+Inf` => `steriliser`
* `sterilisere` has no analyses either

**blautne** failures:

* `blautne+V+Inf` => `blautn`
* `blautne` has no analyses either

**vokke** failures:

* `vokke+V+Inf` => `vokk`
* `vokke` has no analyses either

**attdikte** failures:

* `attdikte+V+Inf` => `attdikt`
* `attdikte` has no analyses either

**flakne** failures:

* `flakne+V+Inf` => `flakn`
* `flakne` has no analyses either

**helge** failures:

* `helge+V+Inf` => `helg`
* `helge` has no analyses either

**sikne** failures:

* `sikne+V+Inf` => `sikn`
* `sikne` has no analyses either

**rikke** failures:

* `rikke+V+Inf` => `rikk`
* `rikke` has no analyses either

**høyste** failures:

* `høyste+V+Inf` => `høyst`
* `høyste` has no analyses either

**grunnsetje** failures:

* `grunnsetje+V+Inf` does not generate!
* `grunnsetje` has no analyses either

**konfirmere** failures:

* `konfirmere+V+Inf` => `konfirmer`
* `konfirmere` has no analyses either

**amputere** failures:

* `amputere+V+Inf` => `amputer`
* `amputere` has no analyses either

**hikstegråte** failures:

* `hikstegråte+V+Inf` => `hikstegråa`
* `hikstegråte+V+Inf` => `hikstegråe`
* `hikstegråte` has no analyses either

**minere** failures:

* `minere+V+Inf` => `miner`
* `minere` has no analyses either

**mygle** failures:

* `mygle+V+Inf` => `mygl`
* `mygle` has no analyses either

**norske** failures:

* `norske+V+Inf` => `norsk`
* `norske` has following analyses:
  * `norsk+A+A+Pl+Indef`
  * `norsk+A+A+Pl+Def`
  * `norsk+A+A+Sg+Def`

**orkestrere** failures:

* `orkestrere+V+Inf` => `orkestrer`
* `orkestrere` has no analyses either

**vampe** failures:

* `vampe+V+Inf` => `vamp`
* `vampe` has following analyses:
  * `vampen+A+Neu+Sg+Indef`

**særmerkje** failures:

* `særmerkje+V+Inf` does not generate!
* `særmerkje` has no analyses either

**tilføye** failures:

* `tilføye+V+Inf` does not generate!
* `tilføye` has no analyses either

**blokkere** failures:

* `blokkere+V+Inf` => `blokker`
* `blokkere` has no analyses either

**forteikne** failures:

* `forteikne+V+Inf` => `forteikn`
* `forteikne` has no analyses either

**lisitere** failures:

* `lisitere+V+Inf` => `lisiter`
* `lisitere` has no analyses either

**falmast** failures:

* `falmast+V+Inf` => `falmastast`
* `falmast` has no analyses either

**semjast** failures:

* `semjast+V+Inf` => `sejast`
* `semjast` has no analyses either

**hybridisere** failures:

* `hybridisere+V+Inf` => `hybridiser`
* `hybridisere` has no analyses either

**skreide** failures:

* `skreide+V+Inf` does not generate!
* `skreide` has no analyses either

**maktstele** failures:

* `maktstele+V+Inf` => `maktstea`
* `maktstele+V+Inf` => `maktstee`
* `maktstele` has no analyses either

**svivørde** failures:

* `svivørde+V+Inf` does not generate!
* `svivørde` has no analyses either

**tåkelegge** failures:

* `tåkelegge+V+Inf` does not generate!
* `tåkelegge` has no analyses either

**avvigsle** failures:

* `avvigsle+V+Inf` => `avvigsl`
* `avvigsle` has no analyses either

**tilgodesjå** failures:

* `tilgodesjå+V+Inf` => `tilgodessjå`
* `tilgodesjå` has no analyses either

**emulere** failures:

* `emulere+V+Inf` => `emuler`
* `emulere` has no analyses either

**føyse** failures:

* `føyse+V+Inf` => `føys`
* `føyse` has no analyses either

**dudre** failures:

* `dudre+V+Inf` => `dudr`
* `dudre` has no analyses either

**utørste** failures:

* `utørste+V+Inf` => `utørst`
* `utørste` has following analyses:
  * `utørst+A+A+Sg+Def`
  * `utørst+A+A+Pl+Indef`
  * `utørst+A+A+Pl+Def`

**yre** failures:

* `yre+V+Inf` does not generate!
* `yre` has following analyses:
  * `yr+A+Pl+Indef`
  * `yren+A+Neu+Sg+Indef`
  * `yr+A+Sg+Def`

**stjorne** failures:

* `stjorne+V+Inf` => `stjorn`
* `stjorne` has no analyses either

**fyle** failures:

* `fyle+V+Inf` => `fyl`
* `fyle` has no analyses either

**trygge** failures:

* `trygge+V+Inf` does not generate!
* `trygge` has no analyses either

**pantgjeve** failures:

* `pantgjeve+V+Inf` => `pantggjeve`
* `pantgjeve+V+Inf` => `pantggjeva`
* `pantgjeve` has no analyses either

**slyngje** failures:

* `slyngje+V+Inf` does not generate!
* `slyngje` has no analyses either

**glimre** failures:

* `glimre+V+Inf` => `glimr`
* `glimre` has no analyses either

**muske** failures:

* `muske+V+Inf` => `musk`
* `muske` has following analyses:
  * `musken+A+Neu+Sg+Indef`

**edle** failures:

* `edle+V+Inf` => `edl`
* `edle` has no analyses either

**knurve** failures:

* `knurve+V+Inf` => `knurv`
* `knurve` has no analyses either

**reike** failures:

* `reike+V+Inf` => `reik`
* `reike` has no analyses either

**angje** failures:

* `angje+V+Inf` => `anggje`
* `angje` has no analyses either

**råkøyre** failures:

* `råkøyre+V+Inf` does not generate!
* `råkøyre` has no analyses either

**skudre** failures:

* `skudre+V+Inf` => `skudr`
* `skudre` has no analyses either

**gjerde** failures:

* `gjerde+V+Inf` does not generate!
* `gjerde` has no analyses either

**transistorisere** failures:

* `transistorisere+V+Inf` => `transistoriser`
* `transistorisere` has no analyses either

**fastne** failures:

* `fastne+V+Inf` => `fastn`
* `fastne` has no analyses either

**sangre** failures:

* `sangre+V+Inf` => `sangr`
* `sangre` has following analyses:
  * `sangren+A+Neu+Sg+Indef`

**kvalitetsstemple** failures:

* `kvalitetsstemple+V+Inf` => `kvalitetsstempl`
* `kvalitetsstemple` has no analyses either

**sekundere** failures:

* `sekundere+V+Inf` => `sekunder`
* `sekundere` has no analyses either

**kantstikke** failures:

* `kantstikke+V+Inf` => `kantstia`
* `kantstikke+V+Inf` => `kantstie`
* `kantstikke` has no analyses either

**svikte** failures:

* `svikte+V+Inf` => `svikt`
* `svikte` has no analyses either

**tætte** failures:

* `tætte+V+Inf` => `tætt`
* `tætte` has no analyses either

**kritisere** failures:

* `kritisere+V+Inf` => `kritiser`
* `kritisere` has no analyses either

**blunde** failures:

* `blunde+V+Inf` => `blund`
* `blunde` has no analyses either

**verast** failures:

* `verast+V+Inf` => `verastast`
* `verast` has no analyses either

**tille** failures:

* `tille+V+Inf` => `till`
* `tille` has no analyses either

**åre** failures:

* `åre+V+Inf` => `år`
* `åre` has no analyses either

**tvibrenne** failures:

* `tvibrenne+V+Inf` => `tvibrene`
* `tvibrenne+V+Inf` => `tvibrena`
* `tvibrenne` has no analyses either

**snase** failures:

* `snase+V+Inf` => `snas`
* `snase` has following analyses:
  * `snasen+A+Neu+Sg+Indef`

**førpensjonere** failures:

* `førpensjonere+V+Inf` => `førpensjoner`
* `førpensjonere` has no analyses either

**støype** failures:

* `støype+V+Inf` => `støyp`
* `støype` has no analyses either

**tyste** failures:

* `tyste+V+Inf` => `tyst`
* `tyste` has following analyses:
  * `tyst+A+A+Sg+Def`
  * `tyst+A+A+Pl+Def`
  * `tung+A+Superl+Def`
  * `tyst+A+A+Pl+Indef`

**kjempe** failures:

* `kjempe+V+Inf` => `kjemp`
* `kjempe` has no analyses either

**gantast** failures:

* `gantast+V+Inf` => `gantastast`
* `gantast` has no analyses either

**flotte** failures:

* `flotte+V+Inf` => `flott`
* `flotte` has following analyses:
  * `flott+A+A+Sg+Def`
  * `flott+A+A+Pl+Def`
  * `flott+A+A+Pl+Indef`

**skrike** failures:

* `skrike+V+Inf` => `skr`
* `skrike` has no analyses either

**forby** failures:

* `forby+V+Inf` does not generate!
* `forby` has no analyses either

**prakke** failures:

* `prakke+V+Inf` => `prakk`
* `prakke` has no analyses either

**kollapse** failures:

* `kollapse+V+Inf` => `kollaps`
* `kollapse` has no analyses either

**miste** failures:

* `miste+V+Inf` does not generate!
* `miste` has no analyses either

**hygienisere** failures:

* `hygienisere+V+Inf` => `hygieniser`
* `hygienisere` has no analyses either

**differensiere** failures:

* `differensiere+V+Inf` => `differensier`
* `differensiere` has no analyses either

**målføre** failures:

* `målføre+V+Inf` does not generate!
* `målføre` has following analyses:
  * `målfør+A+A+Sg+Def`
  * `målfør+A+A+Pl+Def`
  * `målfør+A+A+Pl+Indef`

**røkje** failures:

* `røkje+V+Inf` does not generate!
* `røkje` has no analyses either

**finere** failures:

* `finere+V+Inf` => `finer`
* `finere` has no analyses either

**sjofle** failures:

* `sjofle+V+Inf` => `sjofl`
* `sjofle` has no analyses either

**pirre** failures:

* `pirre+V+Inf` => `pirr`
* `pirre` has no analyses either

**snerpe** failures:

* `snerpe+V+Inf` => `sn`
* `snerpe` has following analyses:
  * `snerpen+A+Neu+Sg+Indef`

**nuske** failures:

* `nuske+V+Inf` => `nusk`
* `nuske` has no analyses either

**skyldgje** failures:

* `skyldgje+V+Inf` => `skyldggje`
* `skyldgje` has no analyses either

**traske** failures:

* `traske+V+Inf` => `trask`
* `traske` has no analyses either

**destruere** failures:

* `destruere+V+Inf` => `destruer`
* `destruere` has no analyses either

**desidere** failures:

* `desidere+V+Inf` => `desider`
* `desidere` has no analyses either

**fordøme** failures:

* `fordøme+V+Inf` does not generate!
* `fordøme` has no analyses either

**linde** failures:

* `linde+V+Inf` => `lind`
* `linde` has no analyses either

**dynge** failures:

* `dynge+V+Inf` does not generate!
* `dynge` has no analyses either

**vikte** failures:

* `vikte+V+Inf` => `vikt`
* `vikte` has no analyses either

**krokere** failures:

* `krokere+V+Inf` => `kroker`
* `krokere` has no analyses either

**bedekkje** failures:

* `bedekkje+V+Inf` does not generate!
* `bedekkje` has no analyses either

**farte** failures:

* `farte+V+Inf` => `fart`
* `farte` has no analyses either

**skutte** failures:

* `skutte+V+Inf` => `skutt`
* `skutte` has following analyses:
  * `skute+V+PrfPrc`

**bekymre** failures:

* `bekymre+V+Inf` => `bekymr`
* `bekymre` has no analyses either

**ulykkeforsikre** failures:

* `ulykkeforsikre+V+Inf` => `ulykkeforsikr`
* `ulykkeforsikre` has no analyses either

**forbere** failures:

* `forbere+V+Inf` => `forbea`
* `forbere+V+Inf` => `forbee`
* `forbere` has no analyses either

**småstele** failures:

* `småstele+V+Inf` => `småstee`
* `småstele+V+Inf` => `småstea`
* `småstele` has no analyses either

**utmatte** failures:

* `utmatte+V+Inf` => `utmatt`
* `utmatte` has no analyses either

**verke** failures:

* `verke+V+Inf` => `verk`
* `verke` has no analyses either

**heime** failures:

* `heime+V+Inf` => `heim`
* `heime` has following analyses:
  * `heime+N+N+Neu+Sg+Indef`

**forunne** failures:

* `forunne+V+Inf` => `forunn`
* `forunne` has no analyses either

**pantstille** failures:

* `pantstille+V+Inf` does not generate!
* `pantstille` has no analyses either

**brumle** failures:

* `brumle+V+Inf` => `bruml`
* `brumle` has no analyses either

**industrialisere** failures:

* `industrialisere+V+Inf` => `industrialiser`
* `industrialisere` has no analyses either

**fordølje** failures:

* `fordølje+V+Inf` does not generate!
* `fordølje` has no analyses either

**grunnstøyte** failures:

* `grunnstøyte+V+Inf` => `grunnstøyt`
* `grunnstøyte` has no analyses either

**mjaue** failures:

* `mjaue+V+Inf` => `mjau`
* `mjaue` has no analyses either

**hoe** failures:

* `hoe+V+Inf` => `ho`
* `hoe` has no analyses either

**blånekte** failures:

* `blånekte+V+Inf` => `blånekt`
* `blånekte` has no analyses either

**tippe** failures:

* `tippe+V+Inf` => `tipp`
* `tippe` has no analyses either

**tvinge** failures:

* `tvinge+V+Inf` => `tving`
* `tvinge` has no analyses either

**prestevie** failures:

* `prestevie+V+Inf` does not generate!
* `prestevie` has no analyses either

**larme** failures:

* `larme+V+Inf` => `larm`
* `larme` has no analyses either

**skole** failures:

* `skole+V+Inf` => `skol`
* `skole` has no analyses either

**spleise** failures:

* `spleise+V+Inf` does not generate!
* `spleise` has no analyses either

**plastre** failures:

* `plastre+V+Inf` => `plastr`
* `plastre` has no analyses either

**maksimere** failures:

* `maksimere+V+Inf` => `maksimer`
* `maksimere` has no analyses either

**blakke** failures:

* `blakke+V+Inf` => `blakk`
* `blakke` has no analyses either

**dralte** failures:

* `dralte+V+Inf` => `dralt`
* `dralte` has no analyses either

**gnidre** failures:

* `gnidre+V+Inf` => `gnidr`
* `gnidre` has no analyses either

**snugge** failures:

* `snugge+V+Inf` => `snugg`
* `snugge` has no analyses either

**funkle** failures:

* `funkle+V+Inf` => `funkl`
* `funkle` has no analyses either

**lyse** failures:

* `lyse+V+Inf` => `lys`
* `lyse` has following analyses:
  * `lys+A+Pl+Indef`
  * `lys+A+Sg+Def`

**sysselsette** failures:

* `sysselsette+V+Inf` does not generate!
* `sysselsette` has following analyses:
  * `sysselsett+A+A+Pl+Def`
  * `sysselsett+A+A+Pl+Indef`
  * `sysselsett+A+A+Sg+Def`

**klage** failures:

* `klage+V+Inf` => `klag`
* `klage` has no analyses either

**botnfryse** failures:

* `botnfryse+V+Inf` => `botnfrye`
* `botnfryse+V+Inf` => `botnfrya`
* `botnfryse` has no analyses either

**dokksetje** failures:

* `dokksetje+V+Inf` does not generate!
* `dokksetje` has no analyses either

**snyte** failures:

* `snyte+V+Inf` => `snyt`
* `snyte` has no analyses either

**godne** failures:

* `godne+V+Inf` => `godn`
* `godne` has no analyses either

**ranke** failures:

* `ranke+V+Inf` => `rank`
* `ranke` has following analyses:
  * `rank+A+Sg+Def`
  * `rank+A+Pl+Indef`

**forfylgje** failures:

* `forfylgje+V+Inf` does not generate!
* `forfylgje` has no analyses either

**snauklippe** failures:

* `snauklippe+V+Inf` does not generate!
* `snauklippe` has no analyses either

**harve** failures:

* `harve+V+Inf` => `harv`
* `harve` has no analyses either

**kansellere** failures:

* `kansellere+V+Inf` => `kanseller`
* `kansellere` has no analyses either

**stytte** failures:

* `stytte+V+Inf` does not generate!
* `stytte` has no analyses either

**stimulere** failures:

* `stimulere+V+Inf` => `stimuler`
* `stimulere` has no analyses either

**valdtake** failures:

* `valdtake+V+Inf` => `valdttaka`
* `valdtake+V+Inf` => `valdttake`
* `valdtake` has no analyses either

**vedtake** failures:

* `vedtake+V+Inf` => `vedttake`
* `vedtake+V+Inf` => `vedttaka`
* `vedtake` has no analyses either

**godte** failures:

* `godte+V+Inf` => `godt`
* `godte` has following analyses:
  * `godter+N`

**harskne** failures:

* `harskne+V+Inf` => `harskn`
* `harskne` has no analyses either

**gaule** failures:

* `gaule+V+Inf` => `gaul`
* `gaule` has no analyses either

**avverke** failures:

* `avverke+V+Inf` => `avverk`
* `avverke` has no analyses either

**olmast** failures:

* `olmast+V+Inf` => `olmastast`
* `olmast` has following analyses:
  * `olm+A+Superl+Indef`

**tillyse** failures:

* `tillyse+V+Inf` => `tillys`
* `tillyse` has no analyses either

**folne** failures:

* `folne+V+Inf` => `foln`
* `folne` has following analyses:
  * `folen+A+Pl+Indef`
  * `folen+A+Sg+Def`

**attrappere** failures:

* `attrappere+V+Inf` => `attrapper`
* `attrappere` has no analyses either

**fuse** failures:

* `fuse+V+Inf` => `fus`
* `fuse` has following analyses:
  * `fus+A+Pl+Indef`
  * `fus+A+Sg+Def`

**klisjere** failures:

* `klisjere+V+Inf` => `klisjer`
* `klisjere` has no analyses either

**sladde** failures:

* `sladde+V+Inf` => `sladd`
* `sladde` has no analyses either

**kartfeste** failures:

* `kartfeste+V+Inf` does not generate!
* `kartfeste` has no analyses either

**smætte** failures:

* `smætte+V+Inf` => `smætt`
* `smætte` has no analyses either

**ydde** failures:

* `ydde+V+Inf` does not generate!
* `ydde` has following analyses:
  * `y+V+Ind+Prt`

**bøte** failures:

* `bøte+V+Inf` => `bøt`
* `bøte` has no analyses either

**skyldgi** failures:

* `skyldgi+V+Inf` => `skyldggi`
* `skyldgi` has no analyses either

**forlike** failures:

* `forlike+V+Inf` => `forlik`
* `forlike` has no analyses either

**krakke** failures:

* `krakke+V+Inf` => `krakk`
* `krakke` has no analyses either

**neppe** failures:

* `neppe+V+Inf` does not generate!
* `neppe` has no analyses either

**strile** failures:

* `strile+V+Inf` => `stril`
* `strile` has no analyses either

**endossere** failures:

* `endossere+V+Inf` => `endosser`
* `endossere` has no analyses either

**fasle** failures:

* `fasle+V+Inf` => `fasl`
* `fasle` has no analyses either

**pisseprate** failures:

* `pisseprate+V+Inf` => `pisseprat`
* `pisseprate` has no analyses either

**symje** failures:

* `symje+V+Inf` does not generate!
* `symje` has no analyses either

**snide** failures:

* `snide+V+Inf` => `snia`
* `snide+V+Inf` => `snie`
* `snide` has no analyses either

**amre** failures:

* `amre+V+Inf` => `amr`
* `amre` has no analyses either

**svire** failures:

* `svire+V+Inf` => `svir`
* `svire` has no analyses either

**etle** failures:

* `etle+V+Inf` => `etl`
* `etle` has no analyses either

**ekstrapolere** failures:

* `ekstrapolere+V+Inf` => `ekstrapoler`
* `ekstrapolere` has no analyses either

**arrestere** failures:

* `arrestere+V+Inf` => `arrester`
* `arrestere` has no analyses either

**dasle** failures:

* `dasle+V+Inf` => `dasl`
* `dasle` has no analyses either

**støre** failures:

* `støre+V+Inf` does not generate!
* `støre` has following analyses:
  * `stør+A+Pl+Indef`
  * `stør+A+Sg+Def`

**straffedømme** failures:

* `straffedømme+V+Inf` does not generate!
* `straffedømme` has no analyses either

**bode** failures:

* `bode+V+Inf` => `bod`
* `bode` has following analyses:
  * `boden+A+Neu+Sg+Indef`

**supponere** failures:

* `supponere+V+Inf` => `supponer`
* `supponere` has no analyses either

**bratte** failures:

* `bratte+V+Inf` => `bratt`
* `bratte` has following analyses:
  * `bratt+A+A+Sg+Def`
  * `bratt+A+A+Pl+Indef`
  * `bratt+A+A+Pl+Def`

**dublere** failures:

* `dublere+V+Inf` => `dubler`
* `dublere` has no analyses either

**pjoltre** failures:

* `pjoltre+V+Inf` => `pjoltr`
* `pjoltre` has no analyses either

**låte** failures:

* `låte+V+Inf` => `låa`
* `låte+V+Inf` => `låe`
* `låte` has no analyses either

**arte** failures:

* `arte+V+Inf` => `art`
* `arte` has no analyses either

**prolongere** failures:

* `prolongere+V+Inf` => `prolonger`
* `prolongere` has no analyses either

**overstyre** failures:

* `overstyre+V+Inf` => `overstyr`
* `overstyre` has no analyses either

**jaske** failures:

* `jaske+V+Inf` => `jask`
* `jaske` has following analyses:
  * `jasken+A+Neu+Sg+Indef`

**skurtreske** failures:

* `skurtreske+V+Inf` => `skurtresk`
* `skurtreske` has no analyses either

**trengje** failures:

* `trengje+V+Inf` does not generate!
* `trengje` has no analyses either

**handrekkjast** failures:

* `handrekkjast+V+Inf` => `handrekjast`
* `handrekkjast` has no analyses either

**høvle** failures:

* `høvle+V+Inf` => `høvl`
* `høvle` has no analyses either

**vanvørde** failures:

* `vanvørde+V+Inf` does not generate!
* `vanvørde` has no analyses either

**skage** failures:

* `skage+V+Inf` => `skag`
* `skage` has no analyses either

**forklede** failures:

* `forklede+V+Inf` does not generate!
* `forklede` has no analyses either

**fryse** failures:

* `fryse+V+Inf` => `fr`
* `fryse` has no analyses either

**fornorske** failures:

* `fornorske+V+Inf` => `fornorsk`
* `fornorske` has no analyses either

**rembe** failures:

* `rembe+V+Inf` does not generate!
* `rembe` has no analyses either

**emigrere** failures:

* `emigrere+V+Inf` => `emigrer`
* `emigrere` has no analyses either

**stople** failures:

* `stople+V+Inf` => `stopl`
* `stople` has no analyses either

**sære** failures:

* `sære+V+Inf` => `sær`
* `sære` has following analyses:
  * `sær+A+Sg+Def`
  * `sær+A+Pl+Indef`

**avdempe** failures:

* `avdempe+V+Inf` => `avdemp`
* `avdempe` has no analyses either

**erstatte** failures:

* `erstatte+V+Inf` => `erstatt`
* `erstatte` has no analyses either

**patronisere** failures:

* `patronisere+V+Inf` => `patroniser`
* `patronisere` has no analyses either

**debitere** failures:

* `debitere+V+Inf` => `debiter`
* `debitere` has no analyses either

**fortelje** failures:

* `fortelje+V+Inf` does not generate!
* `fortelje` has no analyses either

**glamse** failures:

* `glamse+V+Inf` => `glams`
* `glamse` has no analyses either

**rotfeste** failures:

* `rotfeste+V+Inf` does not generate!
* `rotfeste` has no analyses either

**niste** failures:

* `niste+V+Inf` => `nist`
* `niste` has no analyses either

**puppe** failures:

* `puppe+V+Inf` => `pupp`
* `puppe` has no analyses either

**stuve** failures:

* `stuve+V+Inf` => `stuv`
* `stuve` has no analyses either

**une** failures:

* `une+V+Inf` => `un`
* `une` has no analyses either

**djupne** failures:

* `djupne+V+Inf` => `djupn`
* `djupne` has no analyses either

**hoste** failures:

* `hoste+V+Inf` => `host`
* `hoste` has no analyses either

**bratle** failures:

* `bratle+V+Inf` => `bratl`
* `bratle` has no analyses either

**vitre** failures:

* `vitre+V+Inf` => `vitr`
* `vitre` has no analyses either

**ærekrenkje** failures:

* `ærekrenkje+V+Inf` does not generate!
* `ærekrenkje` has no analyses either

**forvite** failures:

* `forvite+V+Inf` => `forvit`
* `forvite` has following analyses:
  * `forviten+A+Neu+Sg+Indef`

**misakte** failures:

* `misakte+V+Inf` => `misakt`
* `misakte` has no analyses either

**underkue** failures:

* `underkue+V+Inf` => `underku`
* `underkue` has no analyses either

**estetisere** failures:

* `estetisere+V+Inf` => `estetiser`
* `estetisere` has no analyses either

**tyrannisere** failures:

* `tyrannisere+V+Inf` => `tyranniser`
* `tyrannisere` has no analyses either

**globalisere** failures:

* `globalisere+V+Inf` => `globaliser`
* `globalisere` has no analyses either

**interfoliere** failures:

* `interfoliere+V+Inf` => `interfolier`
* `interfoliere` has no analyses either

**krave** failures:

* `krave+V+Inf` => `krav`
* `krave` has no analyses either

**raude** failures:

* `raude+V+Inf` => `raud`
* `raude` has following analyses:
  * `raud+A+Pl+Indef`
  * `raud+A+Sg+Def`

**pine** failures:

* `pine+V+Inf` => `pin`
* `pine` has following analyses:
  * `pipe+V+Adj+Fl`
  * `pipe+V+Adj+Bu+Eint`

**reipe** failures:

* `reipe+V+Inf` => `reip`
* `reipe` has no analyses either

**avgifte** failures:

* `avgifte+V+Inf` => `avgift`
* `avgifte` has no analyses either

**deile** failures:

* `deile+V+Inf` => `deil`
* `deile` has no analyses either

**skamskyte** failures:

* `skamskyte+V+Inf` => `skamskye`
* `skamskyte+V+Inf` => `skamskya`
* `skamskyte` has no analyses either

**tvette** failures:

* `tvette+V+Inf` => `tvett`
* `tvette` has no analyses either

**vitte** failures:

* `vitte+V+Inf` => `vitt`
* `vitte` has no analyses either

**diskutere** failures:

* `diskutere+V+Inf` => `diskuter`
* `diskutere` has no analyses either

**forære** failures:

* `forære+V+Inf` => `forær`
* `forære` has no analyses either

**skambite** failures:

* `skambite+V+Inf` => `skambie`
* `skambite+V+Inf` => `skambia`
* `skambite` has no analyses either

**ture** failures:

* `ture+V+Inf` => `tur`
* `ture` has following analyses:
  * `turr+A+A+Pl+Indef`
  * `turr+A+A+Pl+Def`
  * `turr+A+A+Sg+Def`

**husere** failures:

* `husere+V+Inf` => `huser`
* `husere` has no analyses either

**kjerve** failures:

* `kjerve+V+Inf` => `kjerv`
* `kjerve` has no analyses either

**endefete** failures:

* `endefete+V+Inf` => `endefet`
* `endefete` has no analyses either

**hugbere** failures:

* `hugbere+V+Inf` => `hugbea`
* `hugbere+V+Inf` => `hugbee`
* `hugbere` has no analyses either

**stumse** failures:

* `stumse+V+Inf` => `stums`
* `stumse` has no analyses either

**forleggje** failures:

* `forleggje+V+Inf` does not generate!
* `forleggje` has no analyses either

**turrlegge** failures:

* `turrlegge+V+Inf` does not generate!
* `turrlegge` has no analyses either

**ferde** failures:

* `ferde+V+Inf` => `ferd`
* `ferde` has no analyses either

**skalkeherme** failures:

* `skalkeherme+V+Inf` does not generate!
* `skalkeherme` has no analyses either

**sifrere** failures:

* `sifrere+V+Inf` => `sifrer`
* `sifrere` has no analyses either

**klesse** failures:

* `klesse+V+Inf` does not generate!
* `klesse` has no analyses either

**forlise** failures:

* `forlise+V+Inf` => `forlis`
* `forlise` has no analyses either

**prøvebore** failures:

* `prøvebore+V+Inf` => `prøvebor`
* `prøvebore` has no analyses either

**forgylle** failures:

* `forgylle+V+Inf` does not generate!
* `forgylle` has no analyses either

**drodle** failures:

* `drodle+V+Inf` => `drodl`
* `drodle` has no analyses either

**gise** failures:

* `gise+V+Inf` does not generate!
* `gise` has following analyses:
  * `gissen+A+Neu+Sg+Indef`

**ekspropriere** failures:

* `ekspropriere+V+Inf` => `eksproprier`
* `ekspropriere` has no analyses either

**gløyme** failures:

* `gløyme+V+Inf` does not generate!
* `gløyme` has following analyses:
  * `gløymen+A+Neu+Sg+Indef`

**omorganisere** failures:

* `omorganisere+V+Inf` => `omorganiser`
* `omorganisere` has no analyses either

**forråe** failures:

* `forråe+V+Inf` => `forrå`
* `forråe` has no analyses either

**tøfle** failures:

* `tøfle+V+Inf` => `tøfl`
* `tøfle` has no analyses either

**livstrygde** failures:

* `livstrygde+V+Inf` => `livstrygd`
* `livstrygde` has no analyses either

**overvelde** failures:

* `overvelde+V+Inf` => `overveld`
* `overvelde` has no analyses either

**samtykke** failures:

* `samtykke+V+Inf` does not generate!
* `samtykke` has no analyses either

**stake** failures:

* `stake+V+Inf` => `stak`
* `stake` has no analyses either

**modernisere** failures:

* `modernisere+V+Inf` => `moderniser`
* `modernisere` has no analyses either

**snarke** failures:

* `snarke+V+Inf` => `snark`
* `snarke` has no analyses either

**kaldsveitte** failures:

* `kaldsveitte+V+Inf` => `kaldsveitt`
* `kaldsveitte` has following analyses:
  * `kaldsveitt+A+A+Pl+Indef`
  * `kaldsveitt+A+A+Pl+Def`
  * `kaldsveitt+A+A+Sg+Def`

**gløse** failures:

* `gløse+V+Inf` => `gløs`
* `gløse` has no analyses either

**none** failures:

* `none+V+Inf` => `non`
* `none` has following analyses:
  * `nyggje+V+Adj+Fl`
  * `nype+V+Adj+Fl`
  * `nygge+V+Adj+Bu+Eint`
  * `nøgge+V+Adj+Bu+Eint`
  * `nygge+V+Adj+Fl`
  * `nyse+V+Adj+Bu+Eint`
  * `njode+V+Adj+Bu+Eint`
  * `njode+V+Adj+Fl`
  * `nøgge+V+Adj+Fl`
  * `nyse+V+Adj+Fl`
  * `nyte+V+Adj+Bu+Eint`
  * `nype+V+Adj+Bu+Eint`
  * `nyte+V+Adj+Fl`
  * `nyggje+V+Adj+Bu+Eint`

**tvirenne** failures:

* `tvirenne+V+Inf` => `tvirea`
* `tvirenne+V+Inf` => `tviree`
* `tvirenne` has no analyses either

**logge** failures:

* `logge+V+Inf` => `logg`
* `logge` has no analyses either

**skilte** failures:

* `skilte+V+Inf` => `skilt`
* `skilte` has no analyses either

**gjennomdebattere** failures:

* `gjennomdebattere+V+Inf` => `gjennomdebatter`
* `gjennomdebattere` has no analyses either

**tileigne** failures:

* `tileigne+V+Inf` => `tileign`
* `tileigne` has no analyses either

**forvanske** failures:

* `forvanske+V+Inf` => `forvansk`
* `forvanske` has no analyses either

**mogne** failures:

* `mogne+V+Inf` => `mogn`
* `mogne` has following analyses:
  * `mogen+A+Pl+Indef`
  * `mogen+A+Sg+Def`

**motta** failures:

* `motta+V+Inf` => `mottta`
* `motta` has no analyses either

**utelukke** failures:

* `utelukke+V+Inf` => `utelukk`
* `utelukke` has no analyses either

**ulage** failures:

* `ulage+V+Inf` => `ulag`
* `ulage` has no analyses either

**fantasere** failures:

* `fantasere+V+Inf` => `fantaser`
* `fantasere` has no analyses either

**blåne** failures:

* `blåne+V+Inf` => `blån`
* `blåne` has following analyses:
  * `blåse+V+Adj+Bu+Eint`
  * `blåse+V+Adj+Fl`

**snelle** failures:

* `snelle+V+Inf` => `snell`
* `snelle` has no analyses either

**fræve** failures:

* `fræve+V+Inf` => `fræv`
* `fræve` has following analyses:
  * `fræv+A+Pl+Indef`
  * `fræv+A+Sg+Def`

**kvæve** failures:

* `kvæve+V+Inf` does not generate!
* `kvæve` has following analyses:
  * `kvæven+A+Neu+Sg+Indef`
  * `kvæv+A+Sg+Def`
  * `kvæv+A+Pl+Indef`

**finnast** failures:

* `finnast+V+Inf` => `ffinnast`
* `finnast` has no analyses either

**smykke** failures:

* `smykke+V+Inf` => `smykk`
* `smykke` has no analyses either

**frambringe** failures:

* `frambringe+V+Inf` => `frambring`
* `frambringe` has no analyses either

**imøtekome** failures:

* `imøtekome+V+Inf` => `imøtekkoma`
* `imøtekome+V+Inf` => `imøtekkome`
* `imøtekome` has no analyses either

**utvikle** failures:

* `utvikle+V+Inf` => `utvikl`
* `utvikle` has no analyses either

**grusleggje** failures:

* `grusleggje+V+Inf` does not generate!
* `grusleggje` has no analyses either

**fasongere** failures:

* `fasongere+V+Inf` => `fasonger`
* `fasongere` has no analyses either

**tvihalde** failures:

* `tvihalde+V+Inf` => `tvihaa`
* `tvihalde+V+Inf` => `tvihae`
* `tvihalde` has no analyses either

**utøve** failures:

* `utøve+V+Inf` does not generate!
* `utøve` has no analyses either

**drunte** failures:

* `drunte+V+Inf` => `drunt`
* `drunte` has no analyses either

**svale** failures:

* `svale+V+Inf` => `sval`
* `svale` has following analyses:
  * `sval+A+A+Pl+Def`
  * `sval+A+A+Pl+Indef`
  * `sval+A+A+Sg+Def`

**våme** failures:

* `våme+V+Inf` => `våm`
* `våme` has no analyses either

**sensitivere** failures:

* `sensitivere+V+Inf` => `sensitiver`
* `sensitivere` has no analyses either

**snaue** failures:

* `snaue+V+Inf` => `snau`
* `snaue` has following analyses:
  * `snau+A+Sg+Def`
  * `snau+A+Pl+Indef`

**venne** failures:

* `venne+V+Inf` does not generate!
* `venne` has no analyses either

**meinke** failures:

* `meinke+V+Inf` => `meink`
* `meinke` has no analyses either

**kylle** failures:

* `kylle+V+Inf` => `kyll`
* `kylle` has no analyses either

**safte** failures:

* `safte+V+Inf` => `saft`
* `safte` has no analyses either

**skjørte** failures:

* `skjørte+V+Inf` => `skjørt`
* `skjørte` has no analyses either

**fernissere** failures:

* `fernissere+V+Inf` => `fernisser`
* `fernissere` has no analyses either

**overnatte** failures:

* `overnatte+V+Inf` => `overnatt`
* `overnatte` has no analyses either

**skramme** failures:

* `skramme+V+Inf` => `skram`
* `skramme` has no analyses either

**avkristne** failures:

* `avkristne+V+Inf` => `avkristn`
* `avkristne` has no analyses either

**hungre** failures:

* `hungre+V+Inf` => `hungr`
* `hungre` has no analyses either

**finne** failures:

* `finne+V+Inf` => `fia`
* `finne+V+Inf` => `fie`
* `finne` has no analyses either

**intervenere** failures:

* `intervenere+V+Inf` => `intervener`
* `intervenere` has no analyses either

**trasse** failures:

* `trasse+V+Inf` => `trass`
* `trasse` has no analyses either

**navle** failures:

* `navle+V+Inf` => `navl`
* `navle` has no analyses either

**mangdoble** failures:

* `mangdoble+V+Inf` => `mangdobl`
* `mangdoble` has no analyses either

**snuse** failures:

* `snuse+V+Inf` does not generate!
* `snuse` has no analyses either

**prosedere** failures:

* `prosedere+V+Inf` => `proseder`
* `prosedere` has no analyses either

**forgude** failures:

* `forgude+V+Inf` => `forgud`
* `forgude` has no analyses either

**tjærebrede** failures:

* `tjærebrede+V+Inf` does not generate!
* `tjærebrede` has no analyses either

**attreise** failures:

* `attreise+V+Inf` => `attreis`
* `attreise` has no analyses either

**skrote** failures:

* `skrote+V+Inf` => `skrot`
* `skrote` has no analyses either

**føregripe** failures:

* `føregripe+V+Inf` => `føregria`
* `føregripe+V+Inf` => `føregrie`
* `føregripe` has no analyses either

**brigsle** failures:

* `brigsle+V+Inf` => `brigsl`
* `brigsle` has no analyses either

**dunste** failures:

* `dunste+V+Inf` => `dunst`
* `dunste` has no analyses either

**telgje** failures:

* `telgje+V+Inf` does not generate!
* `telgje` has no analyses either

**feriere** failures:

* `feriere+V+Inf` => `ferier`
* `feriere` has no analyses either

**minne** failures:

* `minne+V+Inf` does not generate!
* `minne` has no analyses either

**lale** failures:

* `lale+V+Inf` => `lal`
* `lale` has no analyses either

**grense** failures:

* `grense+V+Inf` => `grens`
* `grense` has no analyses either

**skume** failures:

* `skume+V+Inf` => `skum`
* `skume` has following analyses:
  * `skummel+A+A+Pl+Def`
  * `skummel+A+A+Sg+Def`
  * `skum+A+Pl+Indef`
  * `skummel+A+A+Pl+Indef`
  * `skum+A+Sg+Def`

**konfigurere** failures:

* `konfigurere+V+Inf` => `konfigurer`
* `konfigurere` has no analyses either

**demarkere** failures:

* `demarkere+V+Inf` => `demarker`
* `demarkere` has no analyses either

**snakke** failures:

* `snakke+V+Inf` => `snakk`
* `snakke` has no analyses either

**ljodisolere** failures:

* `ljodisolere+V+Inf` => `ljodisoler`
* `ljodisolere` has no analyses either

**satse** failures:

* `satse+V+Inf` => `sats`
* `satse` has no analyses either

**grupe** failures:

* `grupe+V+Inf` => `grue`
* `grupe+V+Inf` => `grua`
* `grupe` has no analyses either

**pierce** failures:

* `pierce+V+Inf` => `pierc`
* `pierce` has no analyses either

**vimle** failures:

* `vimle+V+Inf` => `viml`
* `vimle` has following analyses:
  * `vimlen+A+Neu+Sg+Indef`

**heite** failures:

* `heite+V+Inf` => `he`
* `heite` has following analyses:
  * `heit+A+Pl+Indef`
  * `heit+A+Sg+Def`

**tòne** failures:

* `tòne+V+Inf` => `tòn`
* `tòne` has no analyses either

**røntgenfotografere** failures:

* `røntgenfotografere+V+Inf` => `røntgenfotografer`
* `røntgenfotografere` has no analyses either

**sondre** failures:

* `sondre+V+Inf` => `sondr`
* `sondre` has no analyses either

**lisensiere** failures:

* `lisensiere+V+Inf` => `lisensier`
* `lisensiere` has no analyses either

**fasthalde** failures:

* `fasthalde+V+Inf` => `fasthaa`
* `fasthalde+V+Inf` => `fasthae`
* `fasthalde` has no analyses either

**hugvende** failures:

* `hugvende+V+Inf` does not generate!
* `hugvende` has no analyses either

**morgne** failures:

* `morgne+V+Inf` => `morgn`
* `morgne` has no analyses either

**gnikke** failures:

* `gnikke+V+Inf` => `gnikk`
* `gnikke` has no analyses either

**ofse** failures:

* `ofse+V+Inf` => `ofs`
* `ofse` has following analyses:
  * `ofsen+A+Neu+Sg+Indef`

**ramponere** failures:

* `ramponere+V+Inf` => `ramponer`
* `ramponere` has no analyses either

**skripe** failures:

* `skripe+V+Inf` => `skrip`
* `skripe` has following analyses:
  * `skripen+A+Neu+Sg+Indef`

**sveltefôre** failures:

* `sveltefôre+V+Inf` => `sveltefôr`
* `sveltefôre` has no analyses either

**blenkje** failures:

* `blenkje+V+Inf` does not generate!
* `blenkje` has no analyses either

**applisere** failures:

* `applisere+V+Inf` => `appliser`
* `applisere` has no analyses either

**bikke** failures:

* `bikke+V+Inf` => `bikk`
* `bikke` has no analyses either

**flotre** failures:

* `flotre+V+Inf` => `flotr`
* `flotre` has no analyses either

**grisebanke** failures:

* `grisebanke+V+Inf` => `grisebank`
* `grisebanke` has no analyses either

**tvita** failures:

* `tvita+V+Inf` => `tvitta`
* `tvita` has following analyses:
  * `tvitann+N+N+Fem+Sg+Def`

**sove** failures:

* `sove+V+Inf` => `soe`
* `sove+V+Inf` => `soa`
* `sove` has no analyses either

**blomstre** failures:

* `blomstre+V+Inf` => `blomstr`
* `blomstre` has no analyses either

**bardunere** failures:

* `bardunere+V+Inf` => `barduner`
* `bardunere` has no analyses either

**forkomast** failures:

* `forkomast+V+Inf` => `forkkomast`
* `forkomast` has no analyses either

**skamkøyre** failures:

* `skamkøyre+V+Inf` does not generate!
* `skamkøyre` has no analyses either

**minke** failures:

* `minke+V+Inf` => `mink`
* `minke` has no analyses either

**tatovere** failures:

* `tatovere+V+Inf` => `tatover`
* `tatovere` has no analyses either

**ønske** failures:

* `ønske+V+Inf` => `ønsk`
* `ønske` has no analyses either

**ansle** failures:

* `ansle+V+Inf` => `ansl`
* `ansle` has no analyses either

**omstille** failures:

* `omstille+V+Inf` does not generate!
* `omstille` has no analyses either

**kubere** failures:

* `kubere+V+Inf` => `kuber`
* `kubere` has no analyses either

**mede** failures:

* `mede+V+Inf` => `med`
* `mede` has no analyses either

**kverne** failures:

* `kverne+V+Inf` => `kvern`
* `kverne` has no analyses either

**omrømme** failures:

* `omrømme+V+Inf` => `omrøm`
* `omrømme` has no analyses either

**fordumme** failures:

* `fordumme+V+Inf` => `fordum`
* `fordumme` has no analyses either

**småprate** failures:

* `småprate+V+Inf` => `småprat`
* `småprate` has no analyses either

**kurre** failures:

* `kurre+V+Inf` => `kurr`
* `kurre` has no analyses either

**fole** failures:

* `fole+V+Inf` => `fol`
* `fole` has following analyses:
  * `folen+A+Neu+Sg+Indef`

**språke** failures:

* `språke+V+Inf` => `språk`
* `språke` has no analyses either

**bevisstgjere** failures:

* `bevisstgjere+V+Inf` does not generate!
* `bevisstgjere` has no analyses either

**granske** failures:

* `granske+V+Inf` => `gransk`
* `granske` has no analyses either

**utveksle** failures:

* `utveksle+V+Inf` => `utveksl`
* `utveksle` has no analyses either

**fråkjenne** failures:

* `fråkjenne+V+Inf` does not generate!
* `fråkjenne` has no analyses either

**forsere** failures:

* `forsere+V+Inf` => `forser`
* `forsere` has no analyses either

**sende** failures:

* `sende+V+Inf` does not generate!
* `sende` has following analyses:
  * `send+A+A+Sg+Def`
  * `send+A+A+Pl+Indef`
  * `send+A+A+Pl+Def`

**takast** failures:

* `takast+V+Inf` => `taast`
* `takast` has no analyses either

**skrinne** failures:

* `skrinne+V+Inf` does not generate!
* `skrinne` has no analyses either

**skramle** failures:

* `skramle+V+Inf` => `skraml`
* `skramle` has no analyses either

**idealisere** failures:

* `idealisere+V+Inf` => `idealiser`
* `idealisere` has no analyses either

**stoltsere** failures:

* `stoltsere+V+Inf` => `stoltser`
* `stoltsere` has no analyses either

**teppelegge** failures:

* `teppelegge+V+Inf` does not generate!
* `teppelegge` has no analyses either

**kapsle** failures:

* `kapsle+V+Inf` => `kapsl`
* `kapsle` has no analyses either

**ensilere** failures:

* `ensilere+V+Inf` => `ensiler`
* `ensilere` has no analyses either

**slakne** failures:

* `slakne+V+Inf` => `slakn`
* `slakne` has no analyses either

**fotostatkopiere** failures:

* `fotostatkopiere+V+Inf` => `fotostatkopier`
* `fotostatkopiere` has no analyses either

**gjennomdiskutere** failures:

* `gjennomdiskutere+V+Inf` => `gjennomdiskuter`
* `gjennomdiskutere` has no analyses either

**pantgje** failures:

* `pantgje+V+Inf` => `pantggje`
* `pantgje` has no analyses either

**klingre** failures:

* `klingre+V+Inf` => `klingr`
* `klingre` has no analyses either

**modellere** failures:

* `modellere+V+Inf` => `modeller`
* `modellere` has no analyses either

**dekkje** failures:

* `dekkje+V+Inf` does not generate!
* `dekkje` has no analyses either

**dispensere** failures:

* `dispensere+V+Inf` => `dispenser`
* `dispensere` has no analyses either

**lease** failures:

* `lease+V+Inf` => `leas`
* `lease` has no analyses either

**tryne** failures:

* `tryne+V+Inf` => `tryn`
* `tryne` has no analyses either

**snaltre** failures:

* `snaltre+V+Inf` => `snaltr`
* `snaltre` has no analyses either

**unnsjå** failures:

* `unnsjå+V+Inf` => `unnssjå`
* `unnsjå` has no analyses either

**realitetshandsame** failures:

* `realitetshandsame+V+Inf` => `realitetshandsam`
* `realitetshandsame` has no analyses either

**transkribere** failures:

* `transkribere+V+Inf` => `transkriber`
* `transkribere` has no analyses either

**venje** failures:

* `venje+V+Inf` does not generate!
* `venje` has no analyses either

**suppe** failures:

* `suppe+V+Inf` => `supp`
* `suppe` has no analyses either

**strene** failures:

* `strene+V+Inf` does not generate!
* `strene` has no analyses either

**normere** failures:

* `normere+V+Inf` => `normer`
* `normere` has no analyses either

**patentere** failures:

* `patentere+V+Inf` => `patenter`
* `patentere` has no analyses either

**projisere** failures:

* `projisere+V+Inf` => `projiser`
* `projisere` has no analyses either

**væde** failures:

* `væde+V+Inf` does not generate!
* `væde` has no analyses either

**overskride** failures:

* `overskride+V+Inf` does not generate!
* `overskride` has no analyses either

**sette** failures:

* `sette+V+Inf` does not generate!
* `sette` has following analyses:
  * `sett+A+A+Pl+Def`
  * `sett+A+A+Sg+Def`
  * `sett+A+A+Pl+Indef`

**kurle** failures:

* `kurle+V+Inf` => `kurl`
* `kurle` has no analyses either

**tvinne** failures:

* `tvinne+V+Inf` => `tvinn`
* `tvinne` has no analyses either

**muljere** failures:

* `muljere+V+Inf` => `muljer`
* `muljere` has no analyses either

**sæme** failures:

* `sæme+V+Inf` => `sæm`
* `sæme` has following analyses:
  * `sæmen+A+Neu+Sg+Indef`

**frede** failures:

* `frede+V+Inf` => `fred`
* `frede` has no analyses either

**velsigne** failures:

* `velsigne+V+Inf` => `velsign`
* `velsigne` has no analyses either

**parafere** failures:

* `parafere+V+Inf` => `parafer`
* `parafere` has no analyses either

**omskule** failures:

* `omskule+V+Inf` => `omskul`
* `omskule` has no analyses either

**dividere** failures:

* `dividere+V+Inf` => `divider`
* `dividere` has no analyses either

**-ere** failures:

* `-ere+V+Inf` => `-er`
* `-ere` has no analyses either

**lovfeste** failures:

* `lovfeste+V+Inf` does not generate!
* `lovfeste` has no analyses either

**forstoppe** failures:

* `forstoppe+V+Inf` => `forstopp`
* `forstoppe` has no analyses either

**underkaste** failures:

* `underkaste+V+Inf` => `underkast`
* `underkaste` has no analyses either

**trivialisere** failures:

* `trivialisere+V+Inf` => `trivialiser`
* `trivialisere` has no analyses either

**antyde** failures:

* `antyde+V+Inf` => `antyd`
* `antyde` has no analyses either

**kontemplere** failures:

* `kontemplere+V+Inf` => `kontempler`
* `kontemplere` has no analyses either

**sampe** failures:

* `sampe+V+Inf` => `samp`
* `sampe` has no analyses either

**ratte** failures:

* `ratte+V+Inf` => `ratt`
* `ratte` has no analyses either

**knirke** failures:

* `knirke+V+Inf` => `knirk`
* `knirke` has no analyses either

**rivalisere** failures:

* `rivalisere+V+Inf` => `rivaliser`
* `rivalisere` has no analyses either

**vindtørke** failures:

* `vindtørke+V+Inf` => `vindtørk`
* `vindtørke` has no analyses either

**overbelaste** failures:

* `overbelaste+V+Inf` => `overbelast`
* `overbelaste` has no analyses either

**talge** failures:

* `talge+V+Inf` => `talg`
* `talge` has no analyses either

**fjerre** failures:

* `fjerre+V+Inf` => `fjerr`
* `fjerre` has no analyses either

**våne** failures:

* `våne+V+Inf` => `vån`
* `våne` has no analyses either

**safe** failures:

* `safe+V+Inf` => `saf`
* `safe` has following analyses:
  * `safe+A+A+Msc+Sg+Indef`
  * `safe+A+A+Fem+Sg+Indef`
  * `safe+A+A+Neu+Sg+Indef`

**åta** failures:

* `åta+V+Inf` => `åtta`
* `åta` has following analyses:
  * `åte+N+N+Fem+Sg+Def`
  * `åte+V+Ind+Prt`
  * `åt+N+N+Neu+Pl+Def`
  * `åte+V+PrfPrc`

**skamme** failures:

* `skamme+V+Inf` => `skam`
* `skamme` has no analyses either

**dille** failures:

* `dille+V+Inf` => `dill`
* `dille` has no analyses either

**innbille** failures:

* `innbille+V+Inf` does not generate!
* `innbille` has no analyses either

**heitne** failures:

* `heitne+V+Inf` => `heitn`
* `heitne` has no analyses either

**huke** failures:

* `huke+V+Inf` => `huk`
* `huke` has no analyses either

**lytte** failures:

* `lytte+V+Inf` => `lytt`
* `lytte` has no analyses either

**gjespe** failures:

* `gjespe+V+Inf` => `gjesp`
* `gjespe` has no analyses either

**bløyte** failures:

* `bløyte+V+Inf` => `bløyt`
* `bløyte` has no analyses either

**forfordele** failures:

* `forfordele+V+Inf` => `forfordel`
* `forfordele` has no analyses either

**restere** failures:

* `restere+V+Inf` => `rester`
* `restere` has no analyses either

**slubre** failures:

* `slubre+V+Inf` => `slubr`
* `slubre` has no analyses either

**vafse** failures:

* `vafse+V+Inf` => `vafs`
* `vafse` has no analyses either

**svabre** failures:

* `svabre+V+Inf` => `svabr`
* `svabre` has no analyses either

**øle** failures:

* `øle+V+Inf` => `øl`
* `øle` has no analyses either

**stritte** failures:

* `stritte+V+Inf` => `stritt`
* `stritte` has no analyses either

**firre** failures:

* `firre+V+Inf` => `firr`
* `firre` has no analyses either

**forføre** failures:

* `forføre+V+Inf` => `forfør`
* `forføre` has no analyses either

**preparere** failures:

* `preparere+V+Inf` => `preparer`
* `preparere` has no analyses either

**kontraktere** failures:

* `kontraktere+V+Inf` => `kontrakter`
* `kontraktere` has no analyses either

**bokle** failures:

* `bokle+V+Inf` => `bokl`
* `bokle` has no analyses either

**hærvinne** failures:

* `hærvinne+V+Inf` => `hærvie`
* `hærvinne+V+Inf` => `hærvia`
* `hærvinne` has no analyses either

**forskreve** failures:

* `forskreve+V+Inf` => `forskrev`
* `forskreve` has no analyses either

**finske** failures:

* `finske+V+Inf` => `finsk`
* `finske` has following analyses:
  * `finsk+A+A+Pl+Indef`
  * `finsk+A+A+Sg+Def`
  * `finsk+A+A+Pl+Def`

**stokke** failures:

* `stokke+V+Inf` => `stokk`
* `stokke` has no analyses either

**påskunde** failures:

* `påskunde+V+Inf` => `påskund`
* `påskunde` has no analyses either

**glispe** failures:

* `glispe+V+Inf` => `glisp`
* `glispe` has no analyses either

**smørje** failures:

* `smørje+V+Inf` does not generate!
* `smørje` has no analyses either

**enervere** failures:

* `enervere+V+Inf` => `enerver`
* `enervere` has no analyses either

**forhindre** failures:

* `forhindre+V+Inf` => `forhindr`
* `forhindre` has no analyses either

**litografere** failures:

* `litografere+V+Inf` => `litografer`
* `litografere` has no analyses either

**fetje** failures:

* `fetje+V+Inf` => `fetj`
* `fetje` has no analyses either

**sleikje** failures:

* `sleikje+V+Inf` does not generate!
* `sleikje` has no analyses either

**holke** failures:

* `holke+V+Inf` => `holk`
* `holke` has no analyses either

**hyppe** failures:

* `hyppe+V+Inf` => `hypp`
* `hyppe` has no analyses either

**reservere** failures:

* `reservere+V+Inf` => `reserver`
* `reservere` has no analyses either

**vorre** failures:

* `vorre+V+Inf` => `vorr`
* `vorre` has no analyses either

**betale** failures:

* `betale+V+Inf` does not generate!
* `betale` has no analyses either

**overhalde** failures:

* `overhalde+V+Inf` => `overhaa`
* `overhalde+V+Inf` => `overhae`
* `overhalde` has no analyses either

**avkode** failures:

* `avkode+V+Inf` => `avkod`
* `avkode` has no analyses either

**etterløyve** failures:

* `etterløyve+V+Inf` does not generate!
* `etterløyve` has no analyses either

**forakte** failures:

* `forakte+V+Inf` => `forakt`
* `forakte` has no analyses either

**vêrast** failures:

* `vêrast+V+Inf` => `vêrastast`
* `vêrast` has no analyses either

**taumkøyre** failures:

* `taumkøyre+V+Inf` does not generate!
* `taumkøyre` has no analyses either

**alkoholisere** failures:

* `alkoholisere+V+Inf` => `alkoholiser`
* `alkoholisere` has no analyses either

**sæle** failures:

* `sæle+V+Inf` => `sæl`
* `sæle` has following analyses:
  * `sæl+A+A+Pl+Def`
  * `sæl+A+A+Sg+Def`
  * `sæl+A+A+Pl+Indef`

**stupe** failures:

* `stupe+V+Inf` does not generate!
* `stupe` has no analyses either

**knekte** failures:

* `knekte+V+Inf` => `knekt`
* `knekte` has no analyses either

**lukkast** failures:

* `lukkast+V+Inf` => `lukkastast`
* `lukkast` has no analyses either

**gjennomtråle** failures:

* `gjennomtråle+V+Inf` => `gjennomtrål`
* `gjennomtråle` has no analyses either

**sameksistere** failures:

* `sameksistere+V+Inf` => `sameksister`
* `sameksistere` has no analyses either

**særprege** failures:

* `særprege+V+Inf` => `særpreg`
* `særprege` has no analyses either

**trunkere** failures:

* `trunkere+V+Inf` => `trunker`
* `trunkere` has no analyses either

**kue** failures:

* `kue+V+Inf` => `ku`
* `kue` has following analyses:
  * `kunne+V+Inf`
  * `kunne+V+Ind+Prt`

**friske** failures:

* `friske+V+Inf` => `frisk`
* `friske` has following analyses:
  * `frisk+A+A+Pl+Def`
  * `frisk+A+A+Sg+Def`
  * `frisk+A+A+Pl+Indef`

**fengje** failures:

* `fengje+V+Inf` does not generate!
* `fengje` has no analyses either

**trisse** failures:

* `trisse+V+Inf` => `triss`
* `trisse` has no analyses either

**stri** failures:

* `stri+V+Inf` does not generate!
* `stri` has following analyses:
  * `strand+N+N+Fem+Sg+Def`
  * `stri+A+Msc+Sg+Indef`
  * `stri+A+Fem+Sg+Indef`
  * `strie+N+N+Msc+Sg+Indef`
  * `stri+N+N+Fem+Sg+Indef`

**ulukkestrygde** failures:

* `ulukkestrygde+V+Inf` => `ulukkestrygd`
* `ulukkestrygde` has no analyses either

**braske** failures:

* `braske+V+Inf` => `brask`
* `braske` has no analyses either

**fjelge** failures:

* `fjelge+V+Inf` => `fjelg`
* `fjelge` has following analyses:
  * `fjelg+A+Sg+Def`
  * `fjelg+A+Pl+Indef`

**covre** failures:

* `covre+V+Inf` => `covr`
* `covre` has no analyses either

**disharmonere** failures:

* `disharmonere+V+Inf` => `disharmoner`
* `disharmonere` has no analyses either

**parlamentere** failures:

* `parlamentere+V+Inf` => `parlamenter`
* `parlamentere` has no analyses either

**spræne** failures:

* `spræne+V+Inf` => `spræn`
* `spræne` has no analyses either

**søte** failures:

* `søte+V+Inf` => `søt`
* `søte` has following analyses:
  * `søt+A+Pl+Indef`
  * `søt+A+Sg+Def`

**væse** failures:

* `væse+V+Inf` => `væs`
* `væse` has no analyses either

**ettersøke** failures:

* `ettersøke+V+Inf` => `ettersøk`
* `ettersøke` has no analyses either

**vill-leie** failures:

* `vill-leie+V+Inf` does not generate!
* `vill-leie` has no analyses either

**kvee** failures:

* `kvee+V+Inf` => `kve`
* `kvee` has following analyses:
  * `kvelle+V+Inf`
  * `kvede+V+PrfPrc`
  * `kvede+V+Inf`
  * `kvede+V+Adj+Nøyt+Ub+Eint`

**lespe** failures:

* `lespe+V+Inf` => `lesp`
* `lespe` has following analyses:
  * `lesp+A+Pl+Indef`
  * `lesp+A+Sg+Def`

**rengle** failures:

* `rengle+V+Inf` => `rengl`
* `rengle` has no analyses either

**ryktast** failures:

* `ryktast+V+Inf` => `ryktastast`
* `ryktast` has no analyses either

**tvirle** failures:

* `tvirle+V+Inf` => `tvirl`
* `tvirle` has no analyses either

**deke** failures:

* `deke+V+Inf` => `dek`
* `deke` has no analyses either

**devaluere** failures:

* `devaluere+V+Inf` => `devaluer`
* `devaluere` has no analyses either

**omadressere** failures:

* `omadressere+V+Inf` => `omadresser`
* `omadressere` has no analyses either

**smakle** failures:

* `smakle+V+Inf` => `smakl`
* `smakle` has no analyses either

**vass-slipe** failures:

* `vass-slipe+V+Inf` does not generate!
* `vass-slipe` has no analyses either

**bergtake** failures:

* `bergtake+V+Inf` => `bergttaka`
* `bergtake+V+Inf` => `bergttake`
* `bergtake` has no analyses either

**overgjeve** failures:

* `overgjeve+V+Inf` => `overggjeva`
* `overgjeve+V+Inf` => `overggjeve`
* `overgjeve` has following analyses:
  * `overgjeven+A+Neu+Sg+Indef`

**sjokkopne** failures:

* `sjokkopne+V+Inf` => `sjokkopn`
* `sjokkopne` has no analyses either

**horve** failures:

* `horve+V+Inf` => `horv`
* `horve` has no analyses either

**parsellere** failures:

* `parsellere+V+Inf` => `parseller`
* `parsellere` has no analyses either

**innovere** failures:

* `innovere+V+Inf` => `innover`
* `innovere` has no analyses either

**diskriminere** failures:

* `diskriminere+V+Inf` => `diskriminer`
* `diskriminere` has no analyses either

**råde** failures:

* `råde+V+Inf` does not generate!
* `råde` has no analyses either

**reile** failures:

* `reile+V+Inf` => `reil`
* `reile` has no analyses either

**stelpe** failures:

* `stelpe+V+Inf` => `stelp`
* `stelpe` has no analyses either

**jogge** failures:

* `jogge+V+Inf` => `jogg`
* `jogge` has no analyses either

**aldersbestemme** failures:

* `aldersbestemme+V+Inf` => `aldersbestema`
* `aldersbestemme+V+Inf` => `aldersbesteme`
* `aldersbestemme` has no analyses either

**flate** failures:

* `flate+V+Inf` => `flat`
* `flate` has following analyses:
  * `flat+A+Pl+Indef`
  * `flat+A+Sg+Def`

**patruljere** failures:

* `patruljere+V+Inf` => `patruljer`
* `patruljere` has no analyses either

**spesialisere** failures:

* `spesialisere+V+Inf` => `spesialiser`
* `spesialisere` has no analyses either

**pelme** failures:

* `pelme+V+Inf` => `pelm`
* `pelme` has no analyses either

**antedatere** failures:

* `antedatere+V+Inf` => `antedater`
* `antedatere` has no analyses either

**kyrkne** failures:

* `kyrkne+V+Inf` => `kyrkn`
* `kyrkne` has following analyses:
  * `kyrken+A+Pl+Indef`
  * `kyrken+A+Sg+Def`

**kondemnere** failures:

* `kondemnere+V+Inf` => `kondemner`
* `kondemnere` has no analyses either

**skvasle** failures:

* `skvasle+V+Inf` => `skvasl`
* `skvasle` has no analyses either

**optere** failures:

* `optere+V+Inf` => `opter`
* `optere` has no analyses either

**famntake** failures:

* `famntake+V+Inf` => `famnttake`
* `famntake+V+Inf` => `famnttaka`
* `famntake` has no analyses either

**gnide** failures:

* `gnide+V+Inf` does not generate!
* `gnide` has no analyses either

**sanksjonere** failures:

* `sanksjonere+V+Inf` => `sanksjoner`
* `sanksjonere` has no analyses either

**overrumple** failures:

* `overrumple+V+Inf` => `overrumpl`
* `overrumple` has no analyses either

**vede** failures:

* `vede+V+Inf` => `ved`
* `vede` has no analyses either

**forskotere** failures:

* `forskotere+V+Inf` => `forskoter`
* `forskotere` has no analyses either

**soltørke** failures:

* `soltørke+V+Inf` => `soltørk`
* `soltørke` has no analyses either

**underhalde** failures:

* `underhalde+V+Inf` => `underhae`
* `underhalde+V+Inf` => `underhaa`
* `underhalde` has no analyses either

**anglisere** failures:

* `anglisere+V+Inf` => `angliser`
* `anglisere` has no analyses either

**demaskere** failures:

* `demaskere+V+Inf` => `demasker`
* `demaskere` has no analyses either

**tjukne** failures:

* `tjukne+V+Inf` => `tjukn`
* `tjukne` has no analyses either

**imaginere** failures:

* `imaginere+V+Inf` => `imaginer`
* `imaginere` has no analyses either

**obstruere** failures:

* `obstruere+V+Inf` => `obstruer`
* `obstruere` has no analyses either

**skrinleggje** failures:

* `skrinleggje+V+Inf` does not generate!
* `skrinleggje` has no analyses either

**skubbe** failures:

* `skubbe+V+Inf` => `skubb`
* `skubbe` has no analyses either

**drøvtygge** failures:

* `drøvtygge+V+Inf` => `drøvtye`
* `drøvtygge+V+Inf` => `drøvtya`
* `drøvtygge` has no analyses either

**gjalle** failures:

* `gjalle+V+Inf` => `gjall`
* `gjalle` has no analyses either

**jodisere** failures:

* `jodisere+V+Inf` => `jodiser`
* `jodisere` has no analyses either

**støkkje** failures:

* `støkkje+V+Inf` does not generate!
* `støkkje` has no analyses either

**endevende** failures:

* `endevende+V+Inf` does not generate!
* `endevende` has no analyses either

**kloakkere** failures:

* `kloakkere+V+Inf` => `kloakker`
* `kloakkere` has no analyses either

**neglisjere** failures:

* `neglisjere+V+Inf` => `neglisjer`
* `neglisjere` has no analyses either

**gymme** failures:

* `gymme+V+Inf` => `gym`
* `gymme` has no analyses either

**naske** failures:

* `naske+V+Inf` => `nask`
* `naske` has following analyses:
  * `nask+A+Sg+Def`
  * `nask+A+Pl+Indef`

**kurse** failures:

* `kurse+V+Inf` => `kurs`
* `kurse` has no analyses either

**prospektere** failures:

* `prospektere+V+Inf` => `prospekter`
* `prospektere` has no analyses either

**kunngjere** failures:

* `kunngjere+V+Inf` does not generate!
* `kunngjere` has no analyses either

**forkromme** failures:

* `forkromme+V+Inf` => `forkrom`
* `forkromme` has no analyses either

**resipere** failures:

* `resipere+V+Inf` => `resiper`
* `resipere` has no analyses either

**gravsette** failures:

* `gravsette+V+Inf` does not generate!
* `gravsette` has no analyses either

**bibre** failures:

* `bibre+V+Inf` => `bibr`
* `bibre` has no analyses either

**imitere** failures:

* `imitere+V+Inf` => `imiter`
* `imitere` has no analyses either

**pjuskast** failures:

* `pjuskast+V+Inf` => `pjuskastast`
* `pjuskast` has no analyses either

**skøyre** failures:

* `skøyre+V+Inf` => `skøyr`
* `skøyre` has following analyses:
  * `skøyr+A+Pl+Indef`
  * `skøyr+A+Sg+Def`

**forurette** failures:

* `forurette+V+Inf` => `forurett`
* `forurette` has no analyses either

**tape** failures:

* `tape+V+Inf` => `tap`
* `tape` has following analyses:
  * `tapper+A+A+Pl+Indef`
  * `tapper+A+A+Sg+Def`
  * `tapper+A+A+Pl+Def`

**drabbe** failures:

* `drabbe+V+Inf` => `drabb`
* `drabbe` has no analyses either

**avmerke** failures:

* `avmerke+V+Inf` => `avmerk`
* `avmerke` has no analyses either

**sneise** failures:

* `sneise+V+Inf` does not generate!
* `sneise` has no analyses either

**animalisere** failures:

* `animalisere+V+Inf` => `animaliser`
* `animalisere` has no analyses either

**sitere** failures:

* `sitere+V+Inf` => `siter`
* `sitere` has no analyses either

**ignorere** failures:

* `ignorere+V+Inf` => `ignorer`
* `ignorere` has no analyses either

**medunderskrive** failures:

* `medunderskrive+V+Inf` => `medunderskria`
* `medunderskrive+V+Inf` => `medunderskrie`
* `medunderskrive` has no analyses either

**blidne** failures:

* `blidne+V+Inf` => `blidn`
* `blidne` has no analyses either

**kjøpe** failures:

* `kjøpe+V+Inf` => `kjøp`
* `kjøpe` has no analyses either

**tykkje** failures:

* `tykkje+V+Inf` does not generate!
* `tykkje` has no analyses either

**nappast** failures:

* `nappast+V+Inf` => `nappastast`
* `nappast` has no analyses either

**avtrappe** failures:

* `avtrappe+V+Inf` => `avtrapp`
* `avtrappe` has no analyses either

**brife** failures:

* `brife+V+Inf` => `brif`
* `brife` has no analyses either

**truske** failures:

* `truske+V+Inf` => `trusk`
* `truske` has no analyses either

**kvele** failures:

* `kvele+V+Inf` => `kvel`
* `kvele` has following analyses:
  * `kvell+A+A+Pl+Def`
  * `kvell+A+A+Pl+Indef`
  * `kvell+A+A+Sg+Def`

**kvitkalke** failures:

* `kvitkalke+V+Inf` => `kvitkalk`
* `kvitkalke` has no analyses either

**høye** failures:

* `høye+V+Inf` => `høy`
* `høye` has no analyses either

**forplikte** failures:

* `forplikte+V+Inf` => `forplikt`
* `forplikte` has no analyses either

**dobbeltklikke** failures:

* `dobbeltklikke+V+Inf` => `dobbeltklikk`
* `dobbeltklikke` has no analyses either

**drøvtyggje** failures:

* `drøvtyggje+V+Inf` => `drøvtyja`
* `drøvtyggje+V+Inf` => `drøvtyje`
* `drøvtyggje` has no analyses either

**teipe** failures:

* `teipe+V+Inf` => `teip`
* `teipe` has no analyses either

**ralle** failures:

* `ralle+V+Inf` => `rall`
* `ralle` has no analyses either

**handmjølke** failures:

* `handmjølke+V+Inf` => `handmjølk`
* `handmjølke` has no analyses either

**væske** failures:

* `væske+V+Inf` => `væsk`
* `væske` has following analyses:
  * `væsken+A+Neu+Sg+Indef`

**hugge** failures:

* `hugge+V+Inf` => `hugg`
* `hugge` has no analyses either

**livje** failures:

* `livje+V+Inf` => `livj`
* `livje` has no analyses either

**ansvarsforsikre** failures:

* `ansvarsforsikre+V+Inf` => `ansvarsforsikr`
* `ansvarsforsikre` has no analyses either

**asne** failures:

* `asne+V+Inf` => `asn`
* `asne` has following analyses:
  * `asen+A+Sg+Def`
  * `asen+A+Pl+Indef`

**førehandsstemme** failures:

* `førehandsstemme+V+Inf` => `førehandssteme`
* `førehandsstemme+V+Inf` => `førehandsstema`
* `førehandsstemme` has no analyses either

**skumtyggje** failures:

* `skumtyggje+V+Inf` => `skumtyja`
* `skumtyggje+V+Inf` => `skumtyje`
* `skumtyggje` has no analyses either

**passivisere** failures:

* `passivisere+V+Inf` => `passiviser`
* `passivisere` has no analyses either

**attskape** failures:

* `attskape+V+Inf` => `attskap`
* `attskape` has no analyses either

**sage** failures:

* `sage+V+Inf` => `sag`
* `sage` has following analyses:
  * `saggen+A+Neu+Sg+Indef`

**TV-overvake** failures:

* `TV-overvake+V+Inf` => `TV-overvak`
* `TV-overvake` has no analyses either

**knarre** failures:

* `knarre+V+Inf` => `knarr`
* `knarre` has no analyses either

**slarve** failures:

* `slarve+V+Inf` => `slarv`
* `slarve` has no analyses either

**damle** failures:

* `damle+V+Inf` => `daml`
* `damle` has no analyses either

**korke** failures:

* `korke+V+Inf` => `kork`
* `korke` has no analyses either

**opptre** failures:

* `opptre+V+Inf` does not generate!
* `opptre` has following analyses:
  * `opptre+V`

**jumle** failures:

* `jumle+V+Inf` => `juml`
* `jumle` has no analyses either

**fjerte** failures:

* `fjerte+V+Inf` => `fjert`
* `fjerte` has no analyses either

**kjønnskvotere** failures:

* `kjønnskvotere+V+Inf` => `kjønnskvoter`
* `kjønnskvotere` has no analyses either

**bukte** failures:

* `bukte+V+Inf` => `bukt`
* `bukte` has no analyses either

**springe** failures:

* `springe+V+Inf` => `sprie`
* `springe+V+Inf` => `spria`
* `springe` has no analyses either

**mangfalde** failures:

* `mangfalde+V+Inf` => `mangfald`
* `mangfalde` has following analyses:
  * `mangfald+A+Pl+Indef`
  * `mangfald+A+Sg+Def`

**gjengle** failures:

* `gjengle+V+Inf` => `gjengl`
* `gjengle` has no analyses either

**småete** failures:

* `småete+V+Inf` => `småee`
* `småete+V+Inf` => `småea`
* `småete` has following analyses:
  * `småeten+A+Neu+Sg+Indef`

**hudrose** failures:

* `hudrose+V+Inf` => `hudros`
* `hudrose` has no analyses either

**spanne** failures:

* `spanne+V+Inf` => `spann`
* `spanne` has no analyses either

**knele** failures:

* `knele+V+Inf` => `knel`
* `knele` has no analyses either

**langkoke** failures:

* `langkoke+V+Inf` does not generate!
* `langkoke` has no analyses either

**skjegge** failures:

* `skjegge+V+Inf` => `skjegg`
* `skjegge` has no analyses either

**klatteløyve** failures:

* `klatteløyve+V+Inf` does not generate!
* `klatteløyve` has no analyses either

**anke** failures:

* `anke+V+Inf` => `ank`
* `anke` has no analyses either

**kaldsmide** failures:

* `kaldsmide+V+Inf` does not generate!
* `kaldsmide` has no analyses either

**bærle** failures:

* `bærle+V+Inf` => `bærl`
* `bærle` has no analyses either

**mylje** failures:

* `mylje+V+Inf` does not generate!
* `mylje` has no analyses either

**flesje** failures:

* `flesje+V+Inf` => `flesj`
* `flesje` has no analyses either

**skreve** failures:

* `skreve+V+Inf` => `skrev`
* `skreve` has no analyses either

**grunngjeve** failures:

* `grunngjeve+V+Inf` => `grunnggjeva`
* `grunngjeve+V+Inf` => `grunnggjeve`
* `grunngjeve` has no analyses either

**ølje** failures:

* `ølje+V+Inf` => `ølj`
* `ølje` has no analyses either

**skurtreskje** failures:

* `skurtreskje+V+Inf` does not generate!
* `skurtreskje` has no analyses either

**grysje** failures:

* `grysje+V+Inf` does not generate!
* `grysje` has no analyses either

**menge** failures:

* `menge+V+Inf` does not generate!
* `menge` has no analyses either

**joike** failures:

* `joike+V+Inf` => `joik`
* `joike` has no analyses either

**burde** failures:

* `burde+V+Inf` does not generate!
* `burde` has no analyses either

**akkumulere** failures:

* `akkumulere+V+Inf` => `akkumuler`
* `akkumulere` has no analyses either

**feilvurdere** failures:

* `feilvurdere+V+Inf` => `feilvurder`
* `feilvurdere` has no analyses either

**vanakte** failures:

* `vanakte+V+Inf` => `vanakt`
* `vanakte` has no analyses either

**topphogge** failures:

* `topphogge+V+Inf` does not generate!
* `topphogge` has no analyses either

**vaksinere** failures:

* `vaksinere+V+Inf` => `vaksiner`
* `vaksinere` has no analyses either

**ostrakere** failures:

* `ostrakere+V+Inf` => `ostraker`
* `ostrakere` has no analyses either

**delikatere** failures:

* `delikatere+V+Inf` => `delikater`
* `delikatere` has no analyses either

**nazifisere** failures:

* `nazifisere+V+Inf` => `nazifiser`
* `nazifisere` has no analyses either

**telefonere** failures:

* `telefonere+V+Inf` => `telefoner`
* `telefonere` has no analyses either

**kroppe** failures:

* `kroppe+V+Inf` => `kropp`
* `kroppe` has no analyses either

**ovete** failures:

* `ovete+V+Inf` => `ovee`
* `ovete+V+Inf` => `ovea`
* `ovete` has no analyses either

**konstatere** failures:

* `konstatere+V+Inf` => `konstater`
* `konstatere` has no analyses either

**melte** failures:

* `melte+V+Inf` does not generate!
* `melte` has no analyses either

**avleie** failures:

* `avleie+V+Inf` does not generate!
* `avleie` has no analyses either

**kalfatre** failures:

* `kalfatre+V+Inf` => `kalfatr`
* `kalfatre` has no analyses either

**lunte** failures:

* `lunte+V+Inf` => `lunt`
* `lunte` has no analyses either

**rundspele** failures:

* `rundspele+V+Inf` => `rundspel`
* `rundspele` has no analyses either

**hauglegge** failures:

* `hauglegge+V+Inf` does not generate!
* `hauglegge` has no analyses either

**navigere** failures:

* `navigere+V+Inf` => `naviger`
* `navigere` has no analyses either

**overstige** failures:

* `overstige+V+Inf` => `overstia`
* `overstige+V+Inf` => `overstie`
* `overstige` has no analyses either

**sirkulere** failures:

* `sirkulere+V+Inf` => `sirkuler`
* `sirkulere` has no analyses either

**snufse** failures:

* `snufse+V+Inf` => `snufs`
* `snufse` has no analyses either

**klattemåle** failures:

* `klattemåle+V+Inf` => `klattemål`
* `klattemåle` has no analyses either

**bile** failures:

* `bile+V+Inf` does not generate!
* `bile` has no analyses either

**vigsle** failures:

* `vigsle+V+Inf` => `vigsl`
* `vigsle` has no analyses either

**gløype** failures:

* `gløype+V+Inf` => `gløyp`
* `gløype` has following analyses:
  * `gløypen+A+Neu+Sg+Indef`

**kontrastere** failures:

* `kontrastere+V+Inf` => `kontraster`
* `kontrastere` has no analyses either

**tverre** failures:

* `tverre+V+Inf` => `tvee`
* `tverre+V+Inf` => `tvea`
* `tverre` has no analyses either

**beinke** failures:

* `beinke+V+Inf` => `beink`
* `beinke` has no analyses either

**druse** failures:

* `druse+V+Inf` => `drus`
* `druse` has following analyses:
  * `drusen+A+Neu+Sg+Indef`
  * `drus+A+Pl+Indef`
  * `drus+A+Sg+Def`

**putre** failures:

* `putre+V+Inf` => `putr`
* `putre` has no analyses either

**dale** failures:

* `dale+V+Inf` does not generate!
* `dale` has no analyses either

**smite** failures:

* `smite+V+Inf` => `sm`
* `smite` has no analyses either

**lekte** failures:

* `lekte+V+Inf` => `lekt`
* `lekte` has no analyses either

**forfarast** failures:

* `forfarast+V+Inf` => `forfaast`
* `forfarast` has no analyses either

**neste** failures:

* `neste+V+Inf` does not generate!
* `neste` has following analyses:
  * `neste+A+A+Msc+Sg+Indef`
  * `neste+A+A+Fem+Sg+Indef`
  * `neste+A+A+Neu+Sg+Indef`

**krekle** failures:

* `krekle+V+Inf` => `krekl`
* `krekle` has no analyses either

**realisere** failures:

* `realisere+V+Inf` => `realiser`
* `realisere` has no analyses either

**bause** failures:

* `bause+V+Inf` => `baus`
* `bause` has following analyses:
  * `baus+A+Sg+Def`
  * `baus+A+Pl+Indef`

**affisere** failures:

* `affisere+V+Inf` => `affiser`
* `affisere` has no analyses either

**veite** failures:

* `veite+V+Inf` does not generate!
* `veite` has no analyses either

**borke** failures:

* `borke+V+Inf` => `bork`
* `borke` has no analyses either

**sjarmantisere** failures:

* `sjarmantisere+V+Inf` => `sjarmantiser`
* `sjarmantisere` has no analyses either

**fjase** failures:

* `fjase+V+Inf` => `fjas`
* `fjase` has no analyses either

**forborde** failures:

* `forborde+V+Inf` => `forbord`
* `forborde` has no analyses either

**tverrstanse** failures:

* `tverrstanse+V+Inf` => `tverrstans`
* `tverrstanse` has no analyses either

**fjåme** failures:

* `fjåme+V+Inf` => `fjåm`
* `fjåme` has no analyses either

**vispe** failures:

* `vispe+V+Inf` => `visp`
* `vispe` has no analyses either

**slumse** failures:

* `slumse+V+Inf` => `slums`
* `slumse` has no analyses either

**bløgge** failures:

* `bløgge+V+Inf` => `bløgg`
* `bløgge` has no analyses either

**gnagle** failures:

* `gnagle+V+Inf` => `gnagl`
* `gnagle` has no analyses either

**mjølke** failures:

* `mjølke+V+Inf` => `mjølk`
* `mjølke` has no analyses either

**grovreinske** failures:

* `grovreinske+V+Inf` => `grovreinsk`
* `grovreinske` has no analyses either

**letne** failures:

* `letne+V+Inf` => `letn`
* `letne` has no analyses either

**skrimsle** failures:

* `skrimsle+V+Inf` => `skrimsl`
* `skrimsle` has no analyses either

**opplyse** failures:

* `opplyse+V+Inf` => `opplys`
* `opplyse` has following analyses:
  * `opplys+A+A+Sg+Def`
  * `opplys+A+A+Pl+Indef`
  * `opplys+A+A+Pl+Def`

**vinke** failures:

* `vinke+V+Inf` => `vink`
* `vinke` has no analyses either

**kombinere** failures:

* `kombinere+V+Inf` => `kombiner`
* `kombinere` has no analyses either

**kløtsje** failures:

* `kløtsje+V+Inf` => `kløtsj`
* `kløtsje` has no analyses either

**forklare** failures:

* `forklare+V+Inf` does not generate!
* `forklare` has no analyses either

**akvarellere** failures:

* `akvarellere+V+Inf` => `akvareller`
* `akvarellere` has no analyses either

**vege** failures:

* `vege+V+Inf` => `veg`
* `vege` has no analyses either

**åskode** failures:

* `åskode+V+Inf` => `åskod`
* `åskode` has no analyses either

**pensle** failures:

* `pensle+V+Inf` => `pensl`
* `pensle` has no analyses either

**pissprate** failures:

* `pissprate+V+Inf` => `pissprat`
* `pissprate` has no analyses either

**glotte** failures:

* `glotte+V+Inf` => `glott`
* `glotte` has no analyses either

**karikere** failures:

* `karikere+V+Inf` => `kariker`
* `karikere` has no analyses either

**erklære** failures:

* `erklære+V+Inf` => `erklær`
* `erklære` has no analyses either

**massakrere** failures:

* `massakrere+V+Inf` => `massakrer`
* `massakrere` has no analyses either

**harmast** failures:

* `harmast+V+Inf` => `harmastast`
* `harmast` has following analyses:
  * `harm+A+Superl+Indef`

**speile** failures:

* `speile+V+Inf` does not generate!
* `speile` has no analyses either

**mekanisere** failures:

* `mekanisere+V+Inf` => `mekaniser`
* `mekanisere` has no analyses either

**skillinge** failures:

* `skillinge+V+Inf` => `skilling`
* `skillinge` has no analyses either

**skrappe** failures:

* `skrappe+V+Inf` => `skrapp`
* `skrappe` has no analyses either

**forkopre** failures:

* `forkopre+V+Inf` => `forkopr`
* `forkopre` has no analyses either

**mørkleggje** failures:

* `mørkleggje+V+Inf` does not generate!
* `mørkleggje` has no analyses either

**parafrasere** failures:

* `parafrasere+V+Inf` => `parafraser`
* `parafrasere` has no analyses either

**sterte** failures:

* `sterte+V+Inf` does not generate!
* `sterte` has no analyses either

**fakse** failures:

* `fakse+V+Inf` => `faks`
* `fakse` has no analyses either

**returnere** failures:

* `returnere+V+Inf` => `returner`
* `returnere` has no analyses either

**internere** failures:

* `internere+V+Inf` => `interner`
* `internere` has no analyses either

**semsgarve** failures:

* `semsgarve+V+Inf` => `semsgarv`
* `semsgarve` has no analyses either

**kolonisere** failures:

* `kolonisere+V+Inf` => `koloniser`
* `kolonisere` has no analyses either

**tørrlegge** failures:

* `tørrlegge+V+Inf` does not generate!
* `tørrlegge` has no analyses either

**haste** failures:

* `haste+V+Inf` => `hast`
* `haste` has no analyses either

**hjale** failures:

* `hjale+V+Inf` => `hjal`
* `hjale` has no analyses either

**datostemple** failures:

* `datostemple+V+Inf` => `datostempl`
* `datostemple` has no analyses either

**gylle** failures:

* `gylle+V+Inf` does not generate!
* `gylle` has no analyses either

**tinglese** failures:

* `tinglese+V+Inf` => `tinglea`
* `tinglese+V+Inf` => `tinglee`
* `tinglese` has no analyses either

**fremde** failures:

* `fremde+V+Inf` => `fremd`
* `fremde` has no analyses either

**innhente** failures:

* `innhente+V+Inf` => `innhent`
* `innhente` has no analyses either

**rekonstruere** failures:

* `rekonstruere+V+Inf` => `rekonstruer`
* `rekonstruere` has no analyses either

**striple** failures:

* `striple+V+Inf` => `stripl`
* `striple` has no analyses either

**toppmåle** failures:

* `toppmåle+V+Inf` => `toppmål`
* `toppmåle` has no analyses either

**avhøyre** failures:

* `avhøyre+V+Inf` does not generate!
* `avhøyre` has no analyses either

**subskribere** failures:

* `subskribere+V+Inf` => `subskriber`
* `subskribere` has no analyses either

**pode** failures:

* `pode+V+Inf` => `pod`
* `pode` has no analyses either

**forstumme** failures:

* `forstumme+V+Inf` => `forstum`
* `forstumme` has no analyses either

**avstyre** failures:

* `avstyre+V+Inf` does not generate!
* `avstyre` has no analyses either

**forstire** failures:

* `forstire+V+Inf` does not generate!
* `forstire` has no analyses either

**bråvende** failures:

* `bråvende+V+Inf` does not generate!
* `bråvende` has no analyses either

**assurere** failures:

* `assurere+V+Inf` => `assurer`
* `assurere` has no analyses either

**finregne** failures:

* `finregne+V+Inf` does not generate!
* `finregne` has no analyses either

**spegelvende** failures:

* `spegelvende+V+Inf` does not generate!
* `spegelvende` has no analyses either

**manikyrere** failures:

* `manikyrere+V+Inf` => `manikyrer`
* `manikyrere` has no analyses either

**eliminere** failures:

* `eliminere+V+Inf` => `eliminer`
* `eliminere` has no analyses either

**slarre** failures:

* `slarre+V+Inf` => `slarr`
* `slarre` has no analyses either

**smergle** failures:

* `smergle+V+Inf` => `smergl`
* `smergle` has no analyses either

**hyrpe** failures:

* `hyrpe+V+Inf` => `hyrp`
* `hyrpe` has no analyses either

**tanke** failures:

* `tanke+V+Inf` => `tank`
* `tanke` has no analyses either

**fjære** failures:

* `fjære+V+Inf` => `fjær`
* `fjære` has no analyses either

**sildre** failures:

* `sildre+V+Inf` => `sildr`
* `sildre` has no analyses either

**stangast** failures:

* `stangast+V+Inf` => `stangastast`
* `stangast` has no analyses either

**utdatere** failures:

* `utdatere+V+Inf` => `utdater`
* `utdatere` has no analyses either

**berrast** failures:

* `berrast+V+Inf` => `berrastast`
* `berrast` has no analyses either

**stresse** failures:

* `stresse+V+Inf` => `stress`
* `stresse` has no analyses either

**gylte** failures:

* `gylte+V+Inf` => `gylt`
* `gylte` has no analyses either

**mentalundersøke** failures:

* `mentalundersøke+V+Inf` => `mentalundersøk`
* `mentalundersøke` has no analyses either

**børse** failures:

* `børse+V+Inf` => `børs`
* `børse` has no analyses either

**skoddelegge** failures:

* `skoddelegge+V+Inf` does not generate!
* `skoddelegge` has no analyses either

**glinse** failures:

* `glinse+V+Inf` => `glins`
* `glinse` has no analyses either

**yte** failures:

* `yte+V+Inf` => `yt`
* `yte` has no analyses either

**folle** failures:

* `folle+V+Inf` => `foll`
* `folle` has no analyses either

**grynnast** failures:

* `grynnast+V+Inf` => `grynast`
* `grynnast` has no analyses either

**slodde** failures:

* `slodde+V+Inf` => `slodd`
* `slodde` has no analyses either

**resetje** failures:

* `resetje+V+Inf` does not generate!
* `resetje` has no analyses either

**hølje** failures:

* `hølje+V+Inf` => `hølj`
* `hølje` has no analyses either

**snøe** failures:

* `snøe+V+Inf` => `snø`
* `snøe` has no analyses either

**interpellere** failures:

* `interpellere+V+Inf` => `interpeller`
* `interpellere` has no analyses either

**vassmørje** failures:

* `vassmørje+V+Inf` => `vassmøje`
* `vassmørje+V+Inf` => `vassmøja`
* `vassmørje` has no analyses either

**kipe** failures:

* `kipe+V+Inf` => `kip`
* `kipe` has following analyses:
  * `kippen+A+Neu+Sg+Indef`

**geiple** failures:

* `geiple+V+Inf` => `geipl`
* `geiple` has no analyses either

**overeksponere** failures:

* `overeksponere+V+Inf` => `overeksponer`
* `overeksponere` has no analyses either

**stripe** failures:

* `stripe+V+Inf` => `strip`
* `stripe` has no analyses either

**gleipe** failures:

* `gleipe+V+Inf` does not generate!
* `gleipe` has no analyses either

**drasle** failures:

* `drasle+V+Inf` => `drasl`
* `drasle` has no analyses either

**kveikne** failures:

* `kveikne+V+Inf` => `kveikn`
* `kveikne` has no analyses either

**glosteike** failures:

* `glosteike+V+Inf` => `glosteik`
* `glosteike` has no analyses either

**heile** failures:

* `heile+V+Inf` => `heil`
* `heile` has following analyses:
  * `heil+A+A+Pl+Def`
  * `heil+A+A+Sg+Def`
  * `heil+A+A+Pl+Indef`

**skolme** failures:

* `skolme+V+Inf` => `skolm`
* `skolme` has no analyses either

**vrengje** failures:

* `vrengje+V+Inf` does not generate!
* `vrengje` has no analyses either

**moske** failures:

* `moske+V+Inf` => `mosk`
* `moske` has no analyses either

**overopphete** failures:

* `overopphete+V+Inf` => `overopphet`
* `overopphete` has no analyses either

**tikle** failures:

* `tikle+V+Inf` => `tikl`
* `tikle` has no analyses either

**humpe** failures:

* `humpe+V+Inf` => `hump`
* `humpe` has no analyses either

**slute** failures:

* `slute+V+Inf` => `slut`
* `slute` has following analyses:
  * `slut+A+Pl+Indef`
  * `sluten+A+Neu+Sg+Indef`
  * `slut+A+Sg+Def`

**strukturrasjonalisere** failures:

* `strukturrasjonalisere+V+Inf` => `strukturrasjonaliser`
* `strukturrasjonalisere` has no analyses either

**tvitake** failures:

* `tvitake+V+Inf` => `tvittake`
* `tvitake+V+Inf` => `tvittaka`
* `tvitake` has no analyses either

**filtse** failures:

* `filtse+V+Inf` => `filts`
* `filtse` has no analyses either

**tverrvende** failures:

* `tverrvende+V+Inf` does not generate!
* `tverrvende` has no analyses either

**grillsteikje** failures:

* `grillsteikje+V+Inf` does not generate!
* `grillsteikje` has no analyses either

**pynte** failures:

* `pynte+V+Inf` => `pynt`
* `pynte` has no analyses either

**gruble** failures:

* `gruble+V+Inf` => `grubl`
* `gruble` has no analyses either

**avkjøle** failures:

* `avkjøle+V+Inf` => `avkjøl`
* `avkjøle` has no analyses either

**skorde** failures:

* `skorde+V+Inf` => `skord`
* `skorde` has no analyses either

**refusere** failures:

* `refusere+V+Inf` => `refuser`
* `refusere` has no analyses either

**tykke** failures:

* `tykke+V+Inf` does not generate!
* `tykke` has no analyses either

**kåte** failures:

* `kåte+V+Inf` => `kåt`
* `kåte` has following analyses:
  * `kåt+A+Sg+Def`
  * `kåt+A+Pl+Indef`

**frette** failures:

* `frette+V+Inf` does not generate!
* `frette` has no analyses either

**dusjregne** failures:

* `dusjregne+V+Inf` does not generate!
* `dusjregne` has no analyses either

**kjafse** failures:

* `kjafse+V+Inf` => `kjafs`
* `kjafse` has no analyses either

**korrelere** failures:

* `korrelere+V+Inf` => `korreler`
* `korrelere` has no analyses either

**vogge** failures:

* `vogge+V+Inf` => `vogg`
* `vogge` has no analyses either

**våtne** failures:

* `våtne+V+Inf` => `våtn`
* `våtne` has no analyses either

**grime** failures:

* `grime+V+Inf` => `grim`
* `grime` has following analyses:
  * `grim+A+A+Pl+Def`
  * `grim+A+A+Sg+Def`
  * `grim+A+A+Pl+Indef`

**venge** failures:

* `venge+V+Inf` => `veng`
* `venge` has no analyses either

**blyge** failures:

* `blyge+V+Inf` does not generate!
* `blyge` has following analyses:
  * `blyg+A+Sg+Def`
  * `blyg+A+Pl+Indef`
  * `blygen+A+Neu+Sg+Indef`

**stappe** failures:

* `stappe+V+Inf` => `stapp`
* `stappe` has no analyses either

**dekupere** failures:

* `dekupere+V+Inf` => `dekuper`
* `dekupere` has no analyses either

**innlemme** failures:

* `innlemme+V+Inf` => `innlem`
* `innlemme` has no analyses either

**tørrprate** failures:

* `tørrprate+V+Inf` => `tørrprat`
* `tørrprate` has no analyses either

**tekkje** failures:

* `tekkje+V+Inf` does not generate!
* `tekkje` has no analyses either

**jazzifisere** failures:

* `jazzifisere+V+Inf` => `jazzifiser`
* `jazzifisere` has no analyses either

**skiple** failures:

* `skiple+V+Inf` => `skipl`
* `skiple` has no analyses either

**synse** failures:

* `synse+V+Inf` => `syns`
* `synse` has no analyses either

**deite** failures:

* `deite+V+Inf` => `deit`
* `deite` has no analyses either

**fauskne** failures:

* `fauskne+V+Inf` => `fauskn`
* `fauskne` has following analyses:
  * `fausken+A+Sg+Def`
  * `fausken+A+Pl+Indef`

**skjønse** failures:

* `skjønse+V+Inf` => `skjøns`
* `skjønse` has no analyses either

**fullføre** failures:

* `fullføre+V+Inf` => `fullfør`
* `fullføre` has following analyses:
  * `fullfør+A+A+Pl+Indef`
  * `fullfør+A+A+Pl+Def`
  * `fullfør+A+A+Sg+Def`

**lukke** failures:

* `lukke+V+Inf` => `lukk`
* `lukke` has no analyses either

**avleggje** failures:

* `avleggje+V+Inf` does not generate!
* `avleggje` has no analyses either

**dølje** failures:

* `dølje+V+Inf` does not generate!
* `dølje` has no analyses either

**fryseturke** failures:

* `fryseturke+V+Inf` => `fryseturk`
* `fryseturke` has no analyses either

**vire** failures:

* `vire+V+Inf` => `vir`
* `vire` has following analyses:
  * `virren+A+Neu+Sg+Indef`

**søme** failures:

* `søme+V+Inf` does not generate!
* `søme` has no analyses either

**namngi** failures:

* `namngi+V+Inf` => `namnggi`
* `namngi` has no analyses either

**votere** failures:

* `votere+V+Inf` => `voter`
* `votere` has no analyses either

**treffast** failures:

* `treffast+V+Inf` => `trefast`
* `treffast` has no analyses either

**skanne** failures:

* `skanne+V+Inf` => `skann`
* `skanne` has no analyses either

**spikke** failures:

* `spikke+V+Inf` => `spikk`
* `spikke` has no analyses either

**finkjemme** failures:

* `finkjemme+V+Inf` does not generate!
* `finkjemme` has no analyses either

**gjære** failures:

* `gjære+V+Inf` => `gjær`
* `gjære` has no analyses either

**nedbemanne** failures:

* `nedbemanne+V+Inf` => `nedbemann`
* `nedbemanne` has no analyses either

**nevetakast** failures:

* `nevetakast+V+Inf` => `nevetaast`
* `nevetakast` has no analyses either

**spade** failures:

* `spade+V+Inf` does not generate!
* `spade` has no analyses either

**utfordre** failures:

* `utfordre+V+Inf` => `utfordr`
* `utfordre` has no analyses either

**sikksakke** failures:

* `sikksakke+V+Inf` => `sikksakk`
* `sikksakke` has no analyses either

**dulke** failures:

* `dulke+V+Inf` => `dulk`
* `dulke` has no analyses either

**helake** failures:

* `helake+V+Inf` => `helak`
* `helake` has no analyses either

**vintre** failures:

* `vintre+V+Inf` => `vintr`
* `vintre` has no analyses either

**kaldhamre** failures:

* `kaldhamre+V+Inf` => `kaldhamr`
* `kaldhamre` has no analyses either

**underordne** failures:

* `underordne+V+Inf` => `underordn`
* `underordne` has no analyses either

**parlere** failures:

* `parlere+V+Inf` => `parler`
* `parlere` has no analyses either

**siselere** failures:

* `siselere+V+Inf` => `siseler`
* `siselere` has no analyses either

**mislykkast** failures:

* `mislykkast+V+Inf` => `mislykkastast`
* `mislykkast` has no analyses either

**riste** failures:

* `riste+V+Inf` does not generate!
* `riste` has no analyses either

**auge** failures:

* `auge+V+Inf` => `aug`
* `auge` has no analyses either

**flikke** failures:

* `flikke+V+Inf` => `flikk`
* `flikke` has no analyses either

**kjørne** failures:

* `kjørne+V+Inf` => `kjørn`
* `kjørne` has no analyses either

**radere** failures:

* `radere+V+Inf` => `rader`
* `radere` has no analyses either

**tvare** failures:

* `tvare+V+Inf` => `tvar`
* `tvare` has no analyses either

**vildre** failures:

* `vildre+V+Inf` => `vildr`
* `vildre` has no analyses either

**kvide** failures:

* `kvide+V+Inf` does not generate!
* `kvide` has no analyses either

**kviste** failures:

* `kviste+V+Inf` => `kvist`
* `kviste` has no analyses either

**glante** failures:

* `glante+V+Inf` => `glant`
* `glante` has no analyses either

**livberge** failures:

* `livberge+V+Inf` => `livberg`
* `livberge` has no analyses either

**glafse** failures:

* `glafse+V+Inf` => `glafs`
* `glafse` has no analyses either

**verdsette** failures:

* `verdsette+V+Inf` does not generate!
* `verdsette` has no analyses either

**grovne** failures:

* `grovne+V+Inf` => `grovn`
* `grovne` has no analyses either

**trikke** failures:

* `trikke+V+Inf` => `trikk`
* `trikke` has no analyses either

**døyde** failures:

* `døyde+V+Inf` does not generate!
* `døyde` has no analyses either

**deviere** failures:

* `deviere+V+Inf` => `devier`
* `deviere` has no analyses either

**forskjellsbehandle** failures:

* `forskjellsbehandle+V+Inf` => `forskjellsbehandl`
* `forskjellsbehandle` has no analyses either

**stange** failures:

* `stange+V+Inf` => `stang`
* `stange` has no analyses either

**rulle** failures:

* `rulle+V+Inf` => `rull`
* `rulle` has no analyses either

**bråke** failures:

* `bråke+V+Inf` => `bråk`
* `bråke` has no analyses either

**verkje** failures:

* `verkje+V+Inf` does not generate!
* `verkje` has no analyses either

**holne** failures:

* `holne+V+Inf` => `holn`
* `holne` has following analyses:
  * `holen+A+Pl+Indef`
  * `holen+A+Sg+Def`

**plire** failures:

* `plire+V+Inf` does not generate!
* `plire` has no analyses either

**tralle** failures:

* `tralle+V+Inf` => `trall`
* `tralle` has no analyses either

**russifisere** failures:

* `russifisere+V+Inf` => `russifiser`
* `russifisere` has no analyses either

**groove** failures:

* `groove+V+Inf` => `groov`
* `groove` has no analyses either

**herberge** failures:

* `herberge+V+Inf` => `herberg`
* `herberge` has no analyses either

**vegre** failures:

* `vegre+V+Inf` => `vegr`
* `vegre` has no analyses either

**ulke** failures:

* `ulke+V+Inf` => `ulk`
* `ulke` has following analyses:
  * `ulken+A+Neu+Sg+Indef`

**grynte** failures:

* `grynte+V+Inf` => `grynt`
* `grynte` has no analyses either

**tverrstane** failures:

* `tverrstane+V+Inf` does not generate!
* `tverrstane` has no analyses either

**hærtake** failures:

* `hærtake+V+Inf` => `hærttake`
* `hærtake+V+Inf` => `hærttaka`
* `hærtake` has no analyses either

**forsjå** failures:

* `forsjå+V+Inf` => `forssjå`
* `forsjå` has following analyses:
  * `forse+V+Imprt`
  * `forse+V+Inf`

**sprotne** failures:

* `sprotne+V+Inf` => `sprotn`
* `sprotne` has no analyses either

**dedisere** failures:

* `dedisere+V+Inf` => `dediser`
* `dedisere` has no analyses either

**framheve** failures:

* `framheve+V+Inf` => `framhev`
* `framheve` has no analyses either

**tråraute** failures:

* `tråraute+V+Inf` => `tråraut`
* `tråraute` has no analyses either

**presupponere** failures:

* `presupponere+V+Inf` => `presupponer`
* `presupponere` has no analyses either

**girere** failures:

* `girere+V+Inf` => `girer`
* `girere` has no analyses either

**losjere** failures:

* `losjere+V+Inf` => `losjer`
* `losjere` has no analyses either

**nøgge** failures:

* `nøgge+V+Inf` => `nøe`
* `nøgge+V+Inf` => `nøa`
* `nøgge` has no analyses either

**omfarast** failures:

* `omfarast+V+Inf` => `omfaast`
* `omfarast` has no analyses either

**pigmentere** failures:

* `pigmentere+V+Inf` => `pigmenter`
* `pigmentere` has no analyses either

**forderve** failures:

* `forderve+V+Inf` => `forderv`
* `forderve` has no analyses either

**fluorescere** failures:

* `fluorescere+V+Inf` => `fluorescer`
* `fluorescere` has no analyses either

**slevje** failures:

* `slevje+V+Inf` => `slevj`
* `slevje` has no analyses either

**galde** failures:

* `galde+V+Inf` => `gald`
* `galde` has no analyses either

**kvesse** failures:

* `kvesse+V+Inf` => `kvess`
* `kvesse` has no analyses either

**tære** failures:

* `tære+V+Inf` does not generate!
* `tære` has no analyses either

**klivre** failures:

* `klivre+V+Inf` => `klivr`
* `klivre` has no analyses either

**svike** failures:

* `svike+V+Inf` => `svie`
* `svike+V+Inf` => `svia`
* `svike` has no analyses either

**farke** failures:

* `farke+V+Inf` => `fark`
* `farke` has no analyses either

**frigjere** failures:

* `frigjere+V+Inf` does not generate!
* `frigjere` has no analyses either

**karnøfle** failures:

* `karnøfle+V+Inf` => `karnøfl`
* `karnøfle` has no analyses either

**forkole** failures:

* `forkole+V+Inf` => `forkol`
* `forkole` has no analyses either

**gradbøye** failures:

* `gradbøye+V+Inf` does not generate!
* `gradbøye` has no analyses either

**tanspringe** failures:

* `tanspringe+V+Inf` => `tansprie`
* `tanspringe+V+Inf` => `tanspria`
* `tanspringe` has no analyses either

**spole** failures:

* `spole+V+Inf` => `spol`
* `spole` has no analyses either

**tvispringe** failures:

* `tvispringe+V+Inf` => `tvisprie`
* `tvispringe+V+Inf` => `tvispria`
* `tvispringe` has no analyses either

**pelse** failures:

* `pelse+V+Inf` => `pels`
* `pelse` has no analyses either

**nusle** failures:

* `nusle+V+Inf` => `nusl`
* `nusle` has no analyses either

**kremere** failures:

* `kremere+V+Inf` => `kremer`
* `kremere` has no analyses either

**perkolere** failures:

* `perkolere+V+Inf` => `perkoler`
* `perkolere` has no analyses either

**intellektualisere** failures:

* `intellektualisere+V+Inf` => `intellektualiser`
* `intellektualisere` has no analyses either

**dimmast** failures:

* `dimmast+V+Inf` => `dimast`
* `dimmast` has no analyses either

**antikrympehandsame** failures:

* `antikrympehandsame+V+Inf` => `antikrympehandsam`
* `antikrympehandsame` has no analyses either

**postleggje** failures:

* `postleggje+V+Inf` does not generate!
* `postleggje` has no analyses either

**seksualisere** failures:

* `seksualisere+V+Inf` => `seksualiser`
* `seksualisere` has no analyses either

**chartre** failures:

* `chartre+V+Inf` => `chartr`
* `chartre` has no analyses either

**rane** failures:

* `rane+V+Inf` does not generate!
* `rane` has following analyses:
  * `rand+N+N+Msc+Pl+Def`

**fløyte** failures:

* `fløyte+V+Inf` => `fløyt`
* `fløyte` has following analyses:
  * `fløyt+A+Pl+Indef`
  * `fløyt+A+Sg+Def`

**regenerere** failures:

* `regenerere+V+Inf` => `regenerer`
* `regenerere` has no analyses either

**detronisere** failures:

* `detronisere+V+Inf` => `detroniser`
* `detronisere` has no analyses either

**forkjetre** failures:

* `forkjetre+V+Inf` => `forkjetr`
* `forkjetre` has no analyses either

**makadamisere** failures:

* `makadamisere+V+Inf` => `makadamiser`
* `makadamisere` has no analyses either

**stroke** failures:

* `stroke+V+Inf` => `strok`
* `stroke` has following analyses:
  * `stroken+A+Neu+Sg+Indef`

**pappe** failures:

* `pappe+V+Inf` => `papp`
* `pappe` has no analyses either

**gjølme** failures:

* `gjølme+V+Inf` => `gjølm`
* `gjølme` has no analyses either

**forvandle** failures:

* `forvandle+V+Inf` => `forvandl`
* `forvandle` has no analyses either

**spegle** failures:

* `spegle+V+Inf` => `spegl`
* `spegle` has no analyses either

**dokterere** failures:

* `dokterere+V+Inf` => `dokterer`
* `dokterere` has no analyses either

**matche** failures:

* `matche+V+Inf` => `match`
* `matche` has no analyses either

**vingle** failures:

* `vingle+V+Inf` => `vingl`
* `vingle` has following analyses:
  * `vinglen+A+Neu+Sg+Indef`

**tonesette** failures:

* `tonesette+V+Inf` does not generate!
* `tonesette` has no analyses either

**dutle** failures:

* `dutle+V+Inf` => `dutl`
* `dutle` has no analyses either

**reinske** failures:

* `reinske+V+Inf` => `reinsk`
* `reinske` has no analyses either

**kontinuere** failures:

* `kontinuere+V+Inf` => `kontinuer`
* `kontinuere` has no analyses either

**korsmerke** failures:

* `korsmerke+V+Inf` => `korsmerk`
* `korsmerke` has no analyses either

**kalve** failures:

* `kalve+V+Inf` => `kalv`
* `kalve` has following analyses:
  * `kalven+A+Neu+Sg+Indef`

**spekne** failures:

* `spekne+V+Inf` => `spekn`
* `spekne` has following analyses:
  * `speken+A+Pl+Indef`
  * `speken+A+Sg+Def`

**instrumentere** failures:

* `instrumentere+V+Inf` => `instrumenter`
* `instrumentere` has no analyses either

**skulke** failures:

* `skulke+V+Inf` => `skulk`
* `skulke` has no analyses either

**koke** failures:

* `koke+V+Inf` does not generate!
* `koke` has no analyses either

**vikle** failures:

* `vikle+V+Inf` => `vikl`
* `vikle` has no analyses either

**tørrsvømme** failures:

* `tørrsvømme+V+Inf` => `tørrsvøma`
* `tørrsvømme+V+Inf` => `tørrsvøme`
* `tørrsvømme` has no analyses either

**gløtte** failures:

* `gløtte+V+Inf` does not generate!
* `gløtte` has no analyses either

**jasse** failures:

* `jasse+V+Inf` => `jass`
* `jasse` has no analyses either

**tone** failures:

* `tone+V+Inf` => `ton`
* `tone` has following analyses:
  * `tygge+V+Adj+Bu+Eint`
  * `tygge+V+Adj+Fl`
  * `tyggje+V+Adj+Bu+Eint`
  * `tyggje+V+Adj+Fl`

**migrere** failures:

* `migrere+V+Inf` => `migrer`
* `migrere` has no analyses either

**angi** failures:

* `angi+V+Inf` => `anggi`
* `angi` has no analyses either

**tørrkoke** failures:

* `tørrkoke+V+Inf` does not generate!
* `tørrkoke` has no analyses either

**appellere** failures:

* `appellere+V+Inf` => `appeller`
* `appellere` has no analyses either

**laske** failures:

* `laske+V+Inf` => `lask`
* `laske` has no analyses either

**kommandere** failures:

* `kommandere+V+Inf` => `kommander`
* `kommandere` has no analyses either

**vomere** failures:

* `vomere+V+Inf` => `vomer`
* `vomere` has no analyses either

**rekast** failures:

* `rekast+V+Inf` => `reast`
* `rekast` has no analyses either

**sveie** failures:

* `sveie+V+Inf` => `svei`
* `sveie` has no analyses either

**kjake** failures:

* `kjake+V+Inf` => `kjak`
* `kjake` has no analyses either

**sakke** failures:

* `sakke+V+Inf` => `sakk`
* `sakke` has no analyses either

**smerte** failures:

* `smerte+V+Inf` => `smert`
* `smerte` has no analyses either

**sprøsteikje** failures:

* `sprøsteikje+V+Inf` does not generate!
* `sprøsteikje` has no analyses either

**fornye** failures:

* `fornye+V+Inf` => `forny`
* `fornye` has no analyses either

**webbe** failures:

* `webbe+V+Inf` => `webb`
* `webbe` has no analyses either

**byste** failures:

* `byste+V+Inf` does not generate!
* `byste` has following analyses:
  * `bysten+A+Neu+Sg+Indef`

**kveppe** failures:

* `kveppe+V+Inf` does not generate!
* `kveppe` has no analyses either

**mistenkeleggjere** failures:

* `mistenkeleggjere+V+Inf` does not generate!
* `mistenkeleggjere` has no analyses either

**daffe** failures:

* `daffe+V+Inf` => `daff`
* `daffe` has no analyses either

**attergjelde** failures:

* `attergjelde+V+Inf` does not generate!
* `attergjelde` has no analyses either

**valte** failures:

* `valte+V+Inf` => `valt`
* `valte` has no analyses either

**skale** failures:

* `skale+V+Inf` => `skal`
* `skale` has no analyses either

**svine** failures:

* `svine+V+Inf` => `svin`
* `svine` has following analyses:
  * `svike+V+Adj+Bu+Eint`
  * `svinn+A+A+Pl+Indef`
  * `svinn+A+A+Pl+Def`
  * `svike+V+Adj+Fl`
  * `svive+V+Adj+Fl`
  * `svinn+A+A+Sg+Def`
  * `svive+V+Adj+Bu+Eint`

**yle** failures:

* `yle+V+Inf` => `yl`
* `yle` has no analyses either

**radde** failures:

* `radde+V+Inf` => `radd`
* `radde` has no analyses either

**ause** failures:

* `ause+V+Inf` => `aus`
* `ause` has no analyses either

**innløyse** failures:

* `innløyse+V+Inf` => `innløys`
* `innløyse` has no analyses either

**forskjønne** failures:

* `forskjønne+V+Inf` => `forskjønn`
* `forskjønne` has no analyses either

**øygje** failures:

* `øygje+V+Inf` does not generate!
* `øygje` has no analyses either

**hylme** failures:

* `hylme+V+Inf` => `hylm`
* `hylme` has no analyses either

**takse** failures:

* `takse+V+Inf` => `taks`
* `takse` has no analyses either

**avléte** failures:

* `avléte+V+Inf` => `avlét`
* `avléte` has no analyses either

**vørde** failures:

* `vørde+V+Inf` does not generate!
* `vørde` has following analyses:
  * `vørden+A+Neu+Sg+Indef`

**pjote** failures:

* `pjote+V+Inf` => `pjot`
* `pjote` has no analyses either

**pledere** failures:

* `pledere+V+Inf` => `pleder`
* `pledere` has no analyses either

**stimle** failures:

* `stimle+V+Inf` => `stiml`
* `stimle` has no analyses either

**linnkoke** failures:

* `linnkoke+V+Inf` does not generate!
* `linnkoke` has no analyses either

**forskuve** failures:

* `forskuve+V+Inf` => `forskuv`
* `forskuve` has no analyses either

**inhalere** failures:

* `inhalere+V+Inf` => `inhaler`
* `inhalere` has no analyses either

**resultere** failures:

* `resultere+V+Inf` => `resulter`
* `resultere` has no analyses either

**isolere** failures:

* `isolere+V+Inf` => `isoler`
* `isolere` has no analyses either

**djupdykke** failures:

* `djupdykke+V+Inf` => `djupdykk`
* `djupdykke` has no analyses either

**gummiere** failures:

* `gummiere+V+Inf` => `gummier`
* `gummiere` has no analyses either

**skvitle** failures:

* `skvitle+V+Inf` => `skvitl`
* `skvitle` has no analyses either

**dage** failures:

* `dage+V+Inf` => `dag`
* `dage` has no analyses either

**opprette** failures:

* `opprette+V+Inf` => `opprett`
* `opprette` has following analyses:
  * `opprett+A+A+Pl+Indef`
  * `opprett+A+A+Pl+Def`
  * `opprett+A+A+Sg+Def`

**paralysere** failures:

* `paralysere+V+Inf` => `paralyser`
* `paralysere` has no analyses either

**teie** failures:

* `teie+V+Inf` does not generate!
* `teie` has no analyses either

**gleppe** failures:

* `gleppe+V+Inf` => `glea`
* `gleppe+V+Inf` => `glee`
* `gleppe` has no analyses either

**ongle** failures:

* `ongle+V+Inf` => `ongl`
* `ongle` has no analyses either

**filleriste** failures:

* `filleriste+V+Inf` does not generate!
* `filleriste` has no analyses either

**numre** failures:

* `numre+V+Inf` => `numr`
* `numre` has no analyses either

**mørbanke** failures:

* `mørbanke+V+Inf` => `mørbank`
* `mørbanke` has no analyses either

**traversere** failures:

* `traversere+V+Inf` => `traverser`
* `traversere` has no analyses either

**danke** failures:

* `danke+V+Inf` => `dank`
* `danke` has no analyses either

**preike** failures:

* `preike+V+Inf` => `preik`
* `preike` has no analyses either

**rime** failures:

* `rime+V+Inf` => `rim`
* `rime` has no analyses either

**panere** failures:

* `panere+V+Inf` => `paner`
* `panere` has no analyses either

**prisgje** failures:

* `prisgje+V+Inf` => `prisggje`
* `prisgje` has no analyses either

**møyrbanke** failures:

* `møyrbanke+V+Inf` => `møyrbank`
* `møyrbanke` has no analyses either

**kapitalisere** failures:

* `kapitalisere+V+Inf` => `kapitaliser`
* `kapitalisere` has no analyses either

**kanne** failures:

* `kanne+V+Inf` => `kann`
* `kanne` has no analyses either

**revle** failures:

* `revle+V+Inf` => `revl`
* `revle` has no analyses either

**stikle** failures:

* `stikle+V+Inf` => `stikl`
* `stikle` has no analyses either

**rogge** failures:

* `rogge+V+Inf` => `rogg`
* `rogge` has no analyses either

**terre** failures:

* `terre+V+Inf` => `terr`
* `terre` has no analyses either

**bannlyse** failures:

* `bannlyse+V+Inf` => `bannlys`
* `bannlyse` has no analyses either

**tyfte** failures:

* `tyfte+V+Inf` does not generate!
* `tyfte` has no analyses either

**stampe** failures:

* `stampe+V+Inf` => `stamp`
* `stampe` has no analyses either

**skeive** failures:

* `skeive+V+Inf` => `skeiv`
* `skeive` has following analyses:
  * `skeiv+A+Pl+Indef`
  * `skeiv+A+Sg+Def`

**glade** failures:

* `glade+V+Inf` does not generate!
* `glade` has following analyses:
  * `glad+A+A+Sg+Def`
  * `glad+A+A+Pl+Def`
  * `glad+A+A+Pl+Indef`

**reversere** failures:

* `reversere+V+Inf` => `reverser`
* `reversere` has no analyses either

**skufle** failures:

* `skufle+V+Inf` => `skufl`
* `skufle` has no analyses either

**missjå** failures:

* `missjå+V+Inf` => `misssjå`
* `missjå` has no analyses either

**planleggje** failures:

* `planleggje+V+Inf` does not generate!
* `planleggje` has no analyses either

**gnaule** failures:

* `gnaule+V+Inf` => `gnaul`
* `gnaule` has no analyses either

**snorke** failures:

* `snorke+V+Inf` => `snork`
* `snorke` has no analyses either

**fortrylle** failures:

* `fortrylle+V+Inf` does not generate!
* `fortrylle` has no analyses either

**naudlande** failures:

* `naudlande+V+Inf` => `naudland`
* `naudlande` has no analyses either

**single** failures:

* `single+V+Inf` => `singl`
* `single` has following analyses:
  * `single+A+A+Fem+Sg+Indef`
  * `single+A+A+Msc+Sg+Indef`
  * `single+A+A+Neu+Sg+Indef`

**dope** failures:

* `dope+V+Inf` => `dop`
* `dope` has no analyses either

**flerre** failures:

* `flerre+V+Inf` => `flerr`
* `flerre` has no analyses either

**grue** failures:

* `grue+V+Inf` does not generate!
* `grue` has following analyses:
  * `grupe+V+Inf`

**realitetsbehandle** failures:

* `realitetsbehandle+V+Inf` => `realitetsbehandl`
* `realitetsbehandle` has no analyses either

**helse** failures:

* `helse+V+Inf` does not generate!
* `helse` has no analyses either

**berike** failures:

* `berike+V+Inf` => `berik`
* `berike` has no analyses either

**valdta** failures:

* `valdta+V+Inf` => `valdtta`
* `valdta` has no analyses either

**druknast** failures:

* `druknast+V+Inf` => `druknastast`
* `druknast` has no analyses either

**skaute** failures:

* `skaute+V+Inf` => `skaut`
* `skaute` has no analyses either

**atterreise** failures:

* `atterreise+V+Inf` => `atterreis`
* `atterreise` has no analyses either

**prekludere** failures:

* `prekludere+V+Inf` => `prekluder`
* `prekludere` has no analyses either

**streike** failures:

* `streike+V+Inf` => `streik`
* `streike` has no analyses either

**unne** failures:

* `unne+V+Inf` => `unn`
* `unne` has no analyses either

**kommunisere** failures:

* `kommunisere+V+Inf` => `kommuniser`
* `kommunisere` has no analyses either

**skråne** failures:

* `skråne+V+Inf` => `skrån`
* `skråne` has no analyses either

**forfalle** failures:

* `forfalle+V+Inf` => `forfae`
* `forfalle+V+Inf` => `forfaa`
* `forfalle` has no analyses either

**mime** failures:

* `mime+V+Inf` => `mim`
* `mime` has no analyses either

**herme** failures:

* `herme+V+Inf` does not generate!
* `herme` has no analyses either

**masseprodusere** failures:

* `masseprodusere+V+Inf` => `masseproduser`
* `masseprodusere` has no analyses either

**feilparkere** failures:

* `feilparkere+V+Inf` => `feilparker`
* `feilparkere` has no analyses either

**slinde** failures:

* `slinde+V+Inf` => `slind`
* `slinde` has no analyses either

**vindisere** failures:

* `vindisere+V+Inf` => `vindiser`
* `vindisere` has no analyses either

**innkassere** failures:

* `innkassere+V+Inf` => `innkasser`
* `innkassere` has no analyses either

**finpusse** failures:

* `finpusse+V+Inf` => `finpuss`
* `finpusse` has no analyses either

**avløyse** failures:

* `avløyse+V+Inf` => `avløys`
* `avløyse` has no analyses either

**trabelere** failures:

* `trabelere+V+Inf` => `trabeler`
* `trabelere` has no analyses either

**avpolitisere** failures:

* `avpolitisere+V+Inf` => `avpolitiser`
* `avpolitisere` has no analyses either

**gnåle** failures:

* `gnåle+V+Inf` => `gnål`
* `gnåle` has no analyses either

**korporativisere** failures:

* `korporativisere+V+Inf` => `korporativiser`
* `korporativisere` has no analyses either

**løfte** failures:

* `løfte+V+Inf` does not generate!
* `løfte` has no analyses either

**sokke** failures:

* `sokke+V+Inf` => `sokk`
* `sokke` has no analyses either

**droppe** failures:

* `droppe+V+Inf` => `dropp`
* `droppe` has no analyses either

**vappe** failures:

* `vappe+V+Inf` => `vapp`
* `vappe` has no analyses either

**lande** failures:

* `lande+V+Inf` => `land`
* `lande` has following analyses:
  * `leke+V+PrsPrc`
  * `late+V+PrsPrc`

**skrøne** failures:

* `skrøne+V+Inf` does not generate!
* `skrøne` has no analyses either

**sømme** failures:

* `sømme+V+Inf` does not generate!
* `sømme` has no analyses either

**førehandsrøyste** failures:

* `førehandsrøyste+V+Inf` => `førehandsrøyst`
* `førehandsrøyste` has no analyses either

**godge** failures:

* `godge+V+Inf` => `godg`
* `godge` has no analyses either

**male** failures:

* `male+V+Inf` => `m`
* `male` has following analyses:
  * `malen+A+Neu+Sg+Indef`

**kontre** failures:

* `kontre+V+Inf` => `kontr`
* `kontre` has no analyses either

**lobotomere** failures:

* `lobotomere+V+Inf` => `lobotomer`
* `lobotomere` has no analyses either

**likestille** failures:

* `likestille+V+Inf` does not generate!
* `likestille` has no analyses either

**fråge** failures:

* `fråge+V+Inf` => `fråg`
* `fråge` has no analyses either

**føresjå** failures:

* `føresjå+V+Inf` => `føressjå`
* `føresjå` has no analyses either

**stråle** failures:

* `stråle+V+Inf` does not generate!
* `stråle` has no analyses either

**harkle** failures:

* `harkle+V+Inf` => `harkl`
* `harkle` has no analyses either

**primsigne** failures:

* `primsigne+V+Inf` => `primsign`
* `primsigne` has no analyses either

**forhaste** failures:

* `forhaste+V+Inf` => `forhast`
* `forhaste` has no analyses either

**skunke** failures:

* `skunke+V+Inf` => `skunk`
* `skunke` has no analyses either

**smugrøyke** failures:

* `smugrøyke+V+Inf` => `smugrøyk`
* `smugrøyke` has no analyses either

**hindre** failures:

* `hindre+V+Inf` => `hindr`
* `hindre` has no analyses either

**knuvle** failures:

* `knuvle+V+Inf` => `knuvl`
* `knuvle` has no analyses either

**tvifalde** failures:

* `tvifalde+V+Inf` => `tvifald`
* `tvifalde` has following analyses:
  * `tvifald+A+Pl+Indef`
  * `tvifald+A+Sg+Def`

**ømme** failures:

* `ømme+V+Inf` => `øm`
* `ømme` has no analyses either

**brumme** failures:

* `brumme+V+Inf` => `brum`
* `brumme` has no analyses either

**niglane** failures:

* `niglane+V+Inf` does not generate!
* `niglane` has no analyses either

**tutre** failures:

* `tutre+V+Inf` => `tutr`
* `tutre` has no analyses either

**kåre** failures:

* `kåre+V+Inf` => `kår`
* `kåre` has no analyses either

**gruse** failures:

* `gruse+V+Inf` does not generate!
* `gruse` has following analyses:
  * `grus+A+Sg+Def`
  * `grus+A+Pl+Indef`

**botne** failures:

* `botne+V+Inf` => `botn`
* `botne` has no analyses either

**hundjage** failures:

* `hundjage+V+Inf` => `hundjag`
* `hundjage` has no analyses either

**parallellforskyve** failures:

* `parallellforskyve+V+Inf` => `parallellforskya`
* `parallellforskyve+V+Inf` => `parallellforskye`
* `parallellforskyve` has no analyses either

**tide** failures:

* `tide+V+Inf` does not generate!
* `tide` has following analyses:
  * `tid+A+Sg+Def`
  * `tid+A+Pl+Indef`

**leme** failures:

* `leme+V+Inf` => `lem`
* `leme` has no analyses either

**stagnere** failures:

* `stagnere+V+Inf` => `stagner`
* `stagnere` has no analyses either

**fase** failures:

* `fase+V+Inf` => `fas`
* `fase` has no analyses either

**luksere** failures:

* `luksere+V+Inf` => `lukser`
* `luksere` has no analyses either

**buhage** failures:

* `buhage+V+Inf` => `buhag`
* `buhage` has no analyses either

**helleleggje** failures:

* `helleleggje+V+Inf` does not generate!
* `helleleggje` has no analyses either

**knurke** failures:

* `knurke+V+Inf` => `knurk`
* `knurke` has no analyses either

**bestemme** failures:

* `bestemme+V+Inf` does not generate!
* `bestemme` has no analyses either

**strikke** failures:

* `strikke+V+Inf` => `strikk`
* `strikke` has no analyses either

**vringle** failures:

* `vringle+V+Inf` => `vringl`
* `vringle` has no analyses either

**skitre** failures:

* `skitre+V+Inf` => `skitr`
* `skitre` has no analyses either

**agglutinere** failures:

* `agglutinere+V+Inf` => `agglutiner`
* `agglutinere` has no analyses either

**geie** failures:

* `geie+V+Inf` => `gei`
* `geie` has no analyses either

**skamete** failures:

* `skamete+V+Inf` => `skamee`
* `skamete+V+Inf` => `skamea`
* `skamete` has no analyses either

**tabuisere** failures:

* `tabuisere+V+Inf` => `tabuiser`
* `tabuisere` has no analyses either

**klapse** failures:

* `klapse+V+Inf` => `klaps`
* `klapse` has no analyses either

**kapre** failures:

* `kapre+V+Inf` => `kapr`
* `kapre` has no analyses either

**skine** failures:

* `skine+V+Inf` => `skie`
* `skine+V+Inf` => `skia`
* `skine` has following analyses:
  * `skite+V+Adj+Fl`
  * `skine+V+Adj+Fl`
  * `skine+V+Adj+Bu+Eint`
  * `skite+V+Adj+Bu+Eint`

**strylle** failures:

* `strylle+V+Inf` => `stryll`
* `strylle` has no analyses either

**regne** failures:

* `regne+V+Inf` does not generate!
* `regne` has no analyses either

**offentleggjere** failures:

* `offentleggjere+V+Inf` does not generate!
* `offentleggjere` has no analyses either

**overtake** failures:

* `overtake+V+Inf` => `overttaka`
* `overtake+V+Inf` => `overttake`
* `overtake` has no analyses either

**kjage** failures:

* `kjage+V+Inf` => `kjag`
* `kjage` has no analyses either

**knatre** failures:

* `knatre+V+Inf` => `knatr`
* `knatre` has no analyses either

**asfaltere** failures:

* `asfaltere+V+Inf` => `asfalter`
* `asfaltere` has no analyses either

**avteikne** failures:

* `avteikne+V+Inf` => `avteikn`
* `avteikne` has no analyses either

**etterisolere** failures:

* `etterisolere+V+Inf` => `etterisoler`
* `etterisolere` has no analyses either

**méde** failures:

* `méde+V+Inf` => `méd`
* `méde` has no analyses either

**skåle** failures:

* `skåle+V+Inf` does not generate!
* `skåle` has no analyses either

**kjose** failures:

* `kjose+V+Inf` => `kjoe`
* `kjose+V+Inf` => `kjoa`
* `kjose` has no analyses either

**vitaminisere** failures:

* `vitaminisere+V+Inf` => `vitaminiser`
* `vitaminisere` has no analyses either

**lamre** failures:

* `lamre+V+Inf` => `lamr`
* `lamre` has no analyses either

**førtidspensjonere** failures:

* `førtidspensjonere+V+Inf` => `førtidspensjoner`
* `førtidspensjonere` has no analyses either

**overtale** failures:

* `overtale+V+Inf` does not generate!
* `overtale` has no analyses either

**drukne** failures:

* `drukne+V+Inf` => `drukn`
* `drukne` has following analyses:
  * `drukken+A+Pl+Indef`
  * `drukken+A+Sg+Def`

**mistolke** failures:

* `mistolke+V+Inf` => `mistolk`
* `mistolke` has no analyses either

**forkue** failures:

* `forkue+V+Inf` => `forku`
* `forkue` has no analyses either

**ynke** failures:

* `ynke+V+Inf` => `ynk`
* `ynke` has no analyses either

**lystre** failures:

* `lystre+V+Inf` => `lystr`
* `lystre` has no analyses either

**vedde** failures:

* `vedde+V+Inf` => `vedd`
* `vedde` has no analyses either

**splitte** failures:

* `splitte+V+Inf` => `splitt`
* `splitte` has no analyses either

**repetere** failures:

* `repetere+V+Inf` => `repeter`
* `repetere` has no analyses either

**sprake** failures:

* `sprake+V+Inf` => `sprak`
* `sprake` has no analyses either

**avherde** failures:

* `avherde+V+Inf` does not generate!
* `avherde` has no analyses either

**halvsteike** failures:

* `halvsteike+V+Inf` => `halvsteik`
* `halvsteike` has no analyses either

**slapne** failures:

* `slapne+V+Inf` => `slapn`
* `slapne` has no analyses either

**våse** failures:

* `våse+V+Inf` => `vås`
* `våse` has no analyses either

**baske** failures:

* `baske+V+Inf` => `bask`
* `baske` has following analyses:
  * `bask+A+Pl+Indef`
  * `bask+A+Sg+Def`

**klimaregulere** failures:

* `klimaregulere+V+Inf` => `klimareguler`
* `klimaregulere` has no analyses either

**jarpe** failures:

* `jarpe+V+Inf` => `jarp`
* `jarpe` has no analyses either

**risikere** failures:

* `risikere+V+Inf` => `risiker`
* `risikere` has no analyses either

**servere** failures:

* `servere+V+Inf` => `server`
* `servere` has no analyses either

**dimme** failures:

* `dimme+V+Inf` => `dim`
* `dimme` has no analyses either

**ebbe** failures:

* `ebbe+V+Inf` => `ebb`
* `ebbe` has no analyses either

**pale** failures:

* `pale+V+Inf` does not generate!
* `pale` has no analyses either

**prute** failures:

* `prute+V+Inf` => `prut`
* `prute` has no analyses either

**tiple** failures:

* `tiple+V+Inf` => `tipl`
* `tiple` has no analyses either

**proe** failures:

* `proe+V+Inf` => `pro`
* `proe` has no analyses either

**blåhate** failures:

* `blåhate+V+Inf` => `blåhat`
* `blåhate` has no analyses either

**tråkle** failures:

* `tråkle+V+Inf` => `tråkl`
* `tråkle` has no analyses either

**innrømme** failures:

* `innrømme+V+Inf` does not generate!
* `innrømme` has no analyses either

**rembursere** failures:

* `rembursere+V+Inf` => `remburser`
* `rembursere` has no analyses either

**bole** failures:

* `bole+V+Inf` => `bol`
* `bole` has no analyses either

**overskygge** failures:

* `overskygge+V+Inf` does not generate!
* `overskygge` has no analyses either

**oppspore** failures:

* `oppspore+V+Inf` => `oppspor`
* `oppspore` has no analyses either

**skute** failures:

* `skute+V+Inf` => `skut`
* `skute` has no analyses either

**svulne** failures:

* `svulne+V+Inf` => `svuln`
* `svulne` has following analyses:
  * `svullen+A+Pl+Indef`
  * `svullen+A+Sg+Def`

**falske** failures:

* `falske+V+Inf` => `falsk`
* `falske` has following analyses:
  * `falsk+A+A+Pl+Def`
  * `falsk+A+A+Sg+Def`
  * `falsk+A+A+Pl+Indef`

**tvage** failures:

* `tvage+V+Inf` => `tvag`
* `tvage` has no analyses either

**tuste** failures:

* `tuste+V+Inf` => `tust`
* `tuste` has following analyses:
  * `tusten+A+Neu+Sg+Indef`

**fundamentere** failures:

* `fundamentere+V+Inf` => `fundamenter`
* `fundamentere` has no analyses either

**gnisse** failures:

* `gnisse+V+Inf` => `gniss`
* `gnisse` has no analyses either

**svåle** failures:

* `svåle+V+Inf` => `svål`
* `svåle` has no analyses either

**klekke** failures:

* `klekke+V+Inf` does not generate!
* `klekke` has no analyses either

**omkome** failures:

* `omkome+V+Inf` => `omkkoma`
* `omkome+V+Inf` => `omkkome`
* `omkome` has following analyses:
  * `omkommen+A+Neu+Sg+Indef`
  * `omkomen+A+Neu+Sg+Indef`

**tingleggjere** failures:

* `tingleggjere+V+Inf` does not generate!
* `tingleggjere` has no analyses either

**spisse** failures:

* `spisse+V+Inf` => `spiss`
* `spisse` has no analyses either

**omskulere** failures:

* `omskulere+V+Inf` => `omskuler`
* `omskulere` has no analyses either

**jalme** failures:

* `jalme+V+Inf` => `jalm`
* `jalme` has no analyses either

**tufsast** failures:

* `tufsast+V+Inf` => `tufsastast`
* `tufsast` has no analyses either

**transjere** failures:

* `transjere+V+Inf` => `transjer`
* `transjere` has no analyses either

**kringle** failures:

* `kringle+V+Inf` => `kringl`
* `kringle` has no analyses either

**styrte** failures:

* `styrte+V+Inf` => `styrt`
* `styrte` has no analyses either

**forvride** failures:

* `forvride+V+Inf` does not generate!
* `forvride` has no analyses either

**smere** failures:

* `smere+V+Inf` => `smer`
* `smere` has following analyses:
  * `smeren+A+Neu+Sg+Indef`

**halvgardere** failures:

* `halvgardere+V+Inf` => `halvgarder`
* `halvgardere` has no analyses either

**retusjere** failures:

* `retusjere+V+Inf` => `retusjer`
* `retusjere` has no analyses either

**kostymere** failures:

* `kostymere+V+Inf` => `kostymer`
* `kostymere` has no analyses either

**oppmane** failures:

* `oppmane+V+Inf` does not generate!
* `oppmane` has no analyses either

**sutle** failures:

* `sutle+V+Inf` => `sutl`
* `sutle` has no analyses either

**hegde** failures:

* `hegde+V+Inf` => `hegd`
* `hegde` has no analyses either

**sjikanere** failures:

* `sjikanere+V+Inf` => `sjikaner`
* `sjikanere` has no analyses either

**bauge** failures:

* `bauge+V+Inf` => `baug`
* `bauge` has no analyses either

**hesje** failures:

* `hesje+V+Inf` => `hesj`
* `hesje` has no analyses either

**ljossetje** failures:

* `ljossetje+V+Inf` does not generate!
* `ljossetje` has no analyses either

**pårekne** failures:

* `pårekne+V+Inf` => `pårekn`
* `pårekne` has no analyses either

**rekke** failures:

* `rekke+V+Inf` does not generate!
* `rekke` has no analyses either

**fakle** failures:

* `fakle+V+Inf` => `fakl`
* `fakle` has no analyses either

**sponse** failures:

* `sponse+V+Inf` => `spons`
* `sponse` has no analyses either

**transpirere** failures:

* `transpirere+V+Inf` => `transpirer`
* `transpirere` has no analyses either

**rauke** failures:

* `rauke+V+Inf` => `rauk`
* `rauke` has no analyses either

**utlevere** failures:

* `utlevere+V+Inf` => `utlever`
* `utlevere` has no analyses either

**konsentrere** failures:

* `konsentrere+V+Inf` => `konsentrer`
* `konsentrere` has no analyses either

**kloroformere** failures:

* `kloroformere+V+Inf` => `kloroformer`
* `kloroformere` has no analyses either

**nevehelse** failures:

* `nevehelse+V+Inf` does not generate!
* `nevehelse` has no analyses either

**synkopere** failures:

* `synkopere+V+Inf` => `synkoper`
* `synkopere` has no analyses either

**fjernstyre** failures:

* `fjernstyre+V+Inf` does not generate!
* `fjernstyre` has no analyses either

**vanvyrde** failures:

* `vanvyrde+V+Inf` does not generate!
* `vanvyrde` has no analyses either

**innreie** failures:

* `innreie+V+Inf` does not generate!
* `innreie` has no analyses either

**mønje** failures:

* `mønje+V+Inf` => `mønj`
* `mønje` has no analyses either

**deklamere** failures:

* `deklamere+V+Inf` => `deklamer`
* `deklamere` has no analyses either

**frita** failures:

* `frita+V+Inf` => `fritta`
* `frita` has no analyses either

**nødslakte** failures:

* `nødslakte+V+Inf` => `nødslakt`
* `nødslakte` has no analyses either

**hye** failures:

* `hye+V+Inf` => `hy`
* `hye` has no analyses either

**seigne** failures:

* `seigne+V+Inf` => `seign`
* `seigne` has no analyses either

**avkriminalisere** failures:

* `avkriminalisere+V+Inf` => `avkriminaliser`
* `avkriminalisere` has no analyses either

**benefisere** failures:

* `benefisere+V+Inf` => `benefiser`
* `benefisere` has no analyses either

**glattrake** failures:

* `glattrake+V+Inf` => `glattrak`
* `glattrake` has no analyses either

**vassflyge** failures:

* `vassflyge+V+Inf` => `vassflya`
* `vassflyge+V+Inf` => `vassflye`
* `vassflyge` has no analyses either

**rigge** failures:

* `rigge+V+Inf` => `rigg`
* `rigge` has no analyses either

**etterfølgje** failures:

* `etterfølgje+V+Inf` does not generate!
* `etterfølgje` has no analyses either

**grisne** failures:

* `grisne+V+Inf` => `grisn`
* `grisne` has following analyses:
  * `grisen+A+Sg+Def`
  * `grisen+A+Pl+Indef`
  * `grissen+A+Pl+Indef`
  * `grissen+A+Sg+Def`

**modne** failures:

* `modne+V+Inf` => `modn`
* `modne` has following analyses:
  * `moden+A+Sg+Def`
  * `moden+A+Pl+Indef`

**skofte** failures:

* `skofte+V+Inf` => `skoft`
* `skofte` has no analyses either

**grumse** failures:

* `grumse+V+Inf` => `grums`
* `grumse` has following analyses:
  * `grumsen+A+Neu+Sg+Indef`

**sample** failures:

* `sample+V+Inf` => `sampl`
* `sample` has no analyses either

**kreere** failures:

* `kreere+V+Inf` => `kreer`
* `kreere` has no analyses either

**frate** failures:

* `frate+V+Inf` => `frat`
* `frate` has no analyses either

**konskribere** failures:

* `konskribere+V+Inf` => `konskriber`
* `konskribere` has no analyses either

**kokmale** failures:

* `kokmale+V+Inf` => `kokmae`
* `kokmale+V+Inf` => `kokmaa`
* `kokmale` has following analyses:
  * `kokmalen+A+Neu+Sg+Indef`

**hie** failures:

* `hie+V+Inf` => `hi`
* `hie` has no analyses either

**forhefte** failures:

* `forhefte+V+Inf` => `forheft`
* `forhefte` has no analyses either

**inkorporere** failures:

* `inkorporere+V+Inf` => `inkorporer`
* `inkorporere` has no analyses either

**mørkje** failures:

* `mørkje+V+Inf` does not generate!
* `mørkje` has no analyses either

**svimle** failures:

* `svimle+V+Inf` => `sviml`
* `svimle` has no analyses either

**viske** failures:

* `viske+V+Inf` => `visk`
* `viske` has following analyses:
  * `visken+A+Neu+Sg+Indef`

**glytte** failures:

* `glytte+V+Inf` does not generate!
* `glytte` has no analyses either

**seinke** failures:

* `seinke+V+Inf` => `seink`
* `seinke` has no analyses either

**rigle** failures:

* `rigle+V+Inf` => `rigl`
* `rigle` has no analyses either

**steikje** failures:

* `steikje+V+Inf` does not generate!
* `steikje` has no analyses either

**dorge** failures:

* `dorge+V+Inf` => `dorg`
* `dorge` has no analyses either

**krame** failures:

* `krame+V+Inf` => `kram`
* `krame` has following analyses:
  * `kram+A+A+Sg+Def`
  * `kram+A+A+Pl+Indef`
  * `kram+A+A+Pl+Def`

**pulle** failures:

* `pulle+V+Inf` => `pull`
* `pulle` has no analyses either

**promenere** failures:

* `promenere+V+Inf` => `promener`
* `promenere` has no analyses either

**forvri** failures:

* `forvri+V+Inf` does not generate!
* `forvri` has no analyses either

**sanse** failures:

* `sanse+V+Inf` => `sans`
* `sanse` has no analyses either

**korsleggje** failures:

* `korsleggje+V+Inf` does not generate!
* `korsleggje` has no analyses either

**varte** failures:

* `varte+V+Inf` => `vart`
* `varte` has no analyses either

**skjenke** failures:

* `skjenke+V+Inf` => `skjenk`
* `skjenke` has no analyses either

**bondefange** failures:

* `bondefange+V+Inf` => `bondefang`
* `bondefange` has no analyses either

**kvise** failures:

* `kvise+V+Inf` => `kvis`
* `kvise` has no analyses either

**sjølvdaude** failures:

* `sjølvdaude+V+Inf` => `sjølvdaud`
* `sjølvdaude` has following analyses:
  * `sjølvdaud+A+A+Pl+Def`
  * `sjølvdaud+A+A+Sg+Def`
  * `sjølvdaud+A+A+Pl+Indef`

**grovsortere** failures:

* `grovsortere+V+Inf` => `grovsorter`
* `grovsortere` has no analyses either

**styrme** failures:

* `styrme+V+Inf` does not generate!
* `styrme` has no analyses either

**fordjupe** failures:

* `fordjupe+V+Inf` => `fordjup`
* `fordjupe` has no analyses either

**avstraffe** failures:

* `avstraffe+V+Inf` => `avstraff`
* `avstraffe` has no analyses either

**stikte** failures:

* `stikte+V+Inf` => `stikt`
* `stikte` has no analyses either

**fordre** failures:

* `fordre+V+Inf` => `fordr`
* `fordre` has no analyses either

**avsperre** failures:

* `avsperre+V+Inf` => `avsperr`
* `avsperre` has no analyses either

**sjalte** failures:

* `sjalte+V+Inf` => `sjalt`
* `sjalte` has no analyses either

**svenge** failures:

* `svenge+V+Inf` does not generate!
* `svenge` has no analyses either

**krase** failures:

* `krase+V+Inf` => `kras`
* `krase` has following analyses:
  * `krass+A+A+Pl+Def`
  * `krass+A+A+Pl+Indef`
  * `krass+A+A+Sg+Def`

**glanse** failures:

* `glanse+V+Inf` => `glans`
* `glanse` has no analyses either

**glefse** failures:

* `glefse+V+Inf` does not generate!
* `glefse` has no analyses either

**skøyte** failures:

* `skøyte+V+Inf` => `skøyt`
* `skøyte` has no analyses either

**brannskatte** failures:

* `brannskatte+V+Inf` => `brannskatt`
* `brannskatte` has no analyses either

**gjenge** failures:

* `gjenge+V+Inf` => `gjeng`
* `gjenge` has following analyses:
  * `gjengen+A+Neu+Sg+Indef`

**bokse** failures:

* `bokse+V+Inf` => `boks`
* `bokse` has no analyses either

**kontekstualisere** failures:

* `kontekstualisere+V+Inf` => `kontekstualiser`
* `kontekstualisere` has no analyses either

**gilje** failures:

* `gilje+V+Inf` => `gilj`
* `gilje` has no analyses either

**skulme** failures:

* `skulme+V+Inf` => `skulm`
* `skulme` has no analyses either

**tverrykke** failures:

* `tverrykke+V+Inf` does not generate!
* `tverrykke` has no analyses either

**ryggjast** failures:

* `ryggjast+V+Inf` => `rygjast`
* `ryggjast` has no analyses either

**dingle** failures:

* `dingle+V+Inf` => `dingl`
* `dingle` has no analyses either

**freiste** failures:

* `freiste+V+Inf` => `freist`
* `freiste` has no analyses either

**jabbe** failures:

* `jabbe+V+Inf` => `jabb`
* `jabbe` has no analyses either

**gistne** failures:

* `gistne+V+Inf` => `gistn`
* `gistne` has following analyses:
  * `gisten+A+Pl+Indef`
  * `gisten+A+Sg+Def`

**plinge** failures:

* `plinge+V+Inf` => `pling`
* `plinge` has no analyses either

**etse** failures:

* `etse+V+Inf` => `ets`
* `etse` has no analyses either

**curle** failures:

* `curle+V+Inf` => `curl`
* `curle` has no analyses either

**blike** failures:

* `blike+V+Inf` => `blik`
* `blike` has no analyses either

**devle** failures:

* `devle+V+Inf` => `devl`
* `devle` has no analyses either

**varpe** failures:

* `varpe+V+Inf` => `varp`
* `varpe` has no analyses either

**vivisekere** failures:

* `vivisekere+V+Inf` => `viviseker`
* `vivisekere` has no analyses either

**rundjule** failures:

* `rundjule+V+Inf` does not generate!
* `rundjule` has no analyses either

**tupere** failures:

* `tupere+V+Inf` => `tuper`
* `tupere` has no analyses either

**terge** failures:

* `terge+V+Inf` => `terg`
* `terge` has no analyses either

**meditere** failures:

* `meditere+V+Inf` => `mediter`
* `meditere` has no analyses either

**frie** failures:

* `frie+V+Inf` => `fri`
* `frie` has following analyses:
  * `fri+A+Sg+Def`
  * `fri+A+Pl+Indef`

**veile** failures:

* `veile+V+Inf` => `veil`
* `veile` has following analyses:
  * `veil+A+Pl+Indef`
  * `veil+A+Sg+Def`

**rekvirere** failures:

* `rekvirere+V+Inf` => `rekvirer`
* `rekvirere` has no analyses either

**innfatte** failures:

* `innfatte+V+Inf` => `innfatt`
* `innfatte` has no analyses either

**innskipe** failures:

* `innskipe+V+Inf` => `innskip`
* `innskipe` has no analyses either

**korrugere** failures:

* `korrugere+V+Inf` => `korruger`
* `korrugere` has no analyses either

**brurevie** failures:

* `brurevie+V+Inf` does not generate!
* `brurevie` has no analyses either

**bakse** failures:

* `bakse+V+Inf` => `baks`
* `bakse` has no analyses either

**pomadisere** failures:

* `pomadisere+V+Inf` => `pomadiser`
* `pomadisere` has no analyses either

**utvinne** failures:

* `utvinne+V+Inf` => `utvia`
* `utvinne+V+Inf` => `utvie`
* `utvinne` has no analyses either

**gjenoppbyggje** failures:

* `gjenoppbyggje+V+Inf` => `gjenoppbygja`
* `gjenoppbyggje+V+Inf` => `gjenoppbygje`
* `gjenoppbyggje` has no analyses either

**ekvipere** failures:

* `ekvipere+V+Inf` => `ekviper`
* `ekvipere` has no analyses either

**oppjustere** failures:

* `oppjustere+V+Inf` => `oppjuster`
* `oppjustere` has no analyses either

**skjesse** failures:

* `skjesse+V+Inf` does not generate!
* `skjesse` has no analyses either

**småregne** failures:

* `småregne+V+Inf` does not generate!
* `småregne` has no analyses either

**pisse** failures:

* `pisse+V+Inf` => `piss`
* `pisse` has no analyses either

**kveike** failures:

* `kveike+V+Inf` => `kveik`
* `kveike` has following analyses:
  * `kveik+A+Sg+Def`
  * `kveik+A+Pl+Indef`

**trugle** failures:

* `trugle+V+Inf` => `trugl`
* `trugle` has no analyses either

**konsignere** failures:

* `konsignere+V+Inf` => `konsigner`
* `konsignere` has no analyses either

**mastre** failures:

* `mastre+V+Inf` => `mastr`
* `mastre` has no analyses either

**tverrkaste** failures:

* `tverrkaste+V+Inf` => `tverrkast`
* `tverrkaste` has no analyses either

**distingvere** failures:

* `distingvere+V+Inf` => `distingver`
* `distingvere` has no analyses either

**glunke** failures:

* `glunke+V+Inf` => `glunk`
* `glunke` has no analyses either

**dreise** failures:

* `dreise+V+Inf` does not generate!
* `dreise` has no analyses either

**misbruke** failures:

* `misbruke+V+Inf` does not generate!
* `misbruke` has no analyses either

**løne** failures:

* `løne+V+Inf` => `løn`
* `løne` has no analyses either

**bøle** failures:

* `bøle+V+Inf` => `bøl`
* `bøle` has no analyses either

**bylgje** failures:

* `bylgje+V+Inf` => `bylgj`
* `bylgje` has no analyses either

**lunne** failures:

* `lunne+V+Inf` => `lunn`
* `lunne` has no analyses either

**kryptere** failures:

* `kryptere+V+Inf` => `krypter`
* `kryptere` has no analyses either

**vareta** failures:

* `vareta+V+Inf` => `varetta`
* `vareta` has no analyses either

**alke** failures:

* `alke+V+Inf` => `alk`
* `alke` has following analyses:
  * `alken+A+Neu+Sg+Indef`

**omringe** failures:

* `omringe+V+Inf` => `omring`
* `omringe` has no analyses either

**gramse** failures:

* `gramse+V+Inf` => `grams`
* `gramse` has no analyses either

**kryssjekke** failures:

* `kryssjekke+V+Inf` => `kryssjekk`
* `kryssjekke` has no analyses either

**sjabbe** failures:

* `sjabbe+V+Inf` => `sjabb`
* `sjabbe` has no analyses either

**glattbarbere** failures:

* `glattbarbere+V+Inf` => `glattbarber`
* `glattbarbere` has no analyses either

**grøfte** failures:

* `grøfte+V+Inf` => `grøft`
* `grøfte` has no analyses either

**fusjonere** failures:

* `fusjonere+V+Inf` => `fusjoner`
* `fusjonere` has no analyses either

**toppe** failures:

* `toppe+V+Inf` => `topp`
* `toppe` has following analyses:
  * `topp+A+A+Pl+Indef`
  * `topp+A+A+Pl+Def`
  * `topp+A+A+Sg+Def`

**sentimentalisere** failures:

* `sentimentalisere+V+Inf` => `sentimentaliser`
* `sentimentalisere` has no analyses either

**buføre** failures:

* `buføre+V+Inf` => `bufør`
* `buføre` has no analyses either

**framskrive** failures:

* `framskrive+V+Inf` => `framskrie`
* `framskrive+V+Inf` => `framskria`
* `framskrive` has no analyses either

**glume** failures:

* `glume+V+Inf` => `glum`
* `glume` has no analyses either

**dyktiggjere** failures:

* `dyktiggjere+V+Inf` does not generate!
* `dyktiggjere` has no analyses either

**tylle** failures:

* `tylle+V+Inf` => `tyl`
* `tylle` has no analyses either

**parodiere** failures:

* `parodiere+V+Inf` => `parodier`
* `parodiere` has no analyses either

**einlyde** failures:

* `einlyde+V+Inf` does not generate!
* `einlyde` has no analyses either

**sidrikke** failures:

* `sidrikke+V+Inf` => `sidria`
* `sidrikke+V+Inf` => `sidrie`
* `sidrikke` has no analyses either

**vatne** failures:

* `vatne+V+Inf` => `vatn`
* `vatne` has no analyses either

**stegle** failures:

* `stegle+V+Inf` => `stegl`
* `stegle` has no analyses either

**omdele** failures:

* `omdele+V+Inf` => `omdel`
* `omdele` has no analyses either

**flækje** failures:

* `flækje+V+Inf` does not generate!
* `flækje` has no analyses either

**sjåke** failures:

* `sjåke+V+Inf` => `sjåk`
* `sjåke` has no analyses either

**tampe** failures:

* `tampe+V+Inf` => `tamp`
* `tampe` has no analyses either

**fininnstille** failures:

* `fininnstille+V+Inf` => `fininnstile`
* `fininnstille+V+Inf` => `fininnstila`
* `fininnstille` has no analyses either

**perspirere** failures:

* `perspirere+V+Inf` => `perspirer`
* `perspirere` has no analyses either

**pøse** failures:

* `pøse+V+Inf` does not generate!
* `pøse` has no analyses either

**røykleggje** failures:

* `røykleggje+V+Inf` does not generate!
* `røykleggje` has no analyses either

**determinere** failures:

* `determinere+V+Inf` => `determiner`
* `determinere` has no analyses either

**kropne** failures:

* `kropne+V+Inf` => `kropn`
* `kropne` has following analyses:
  * `kroppen+A+Pl+Indef`
  * `kroppen+A+Sg+Def`

**svide** failures:

* `svide+V+Inf` does not generate!
* `svide` has no analyses either

**saumfare** failures:

* `saumfare+V+Inf` => `saumfae`
* `saumfare+V+Inf` => `saumfaa`
* `saumfare` has no analyses either

**ekse** failures:

* `ekse+V+Inf` => `eks`
* `ekse` has no analyses either

**gulpe** failures:

* `gulpe+V+Inf` => `gulp`
* `gulpe` has no analyses either

**botnlegge** failures:

* `botnlegge+V+Inf` does not generate!
* `botnlegge` has no analyses either

**fluktuere** failures:

* `fluktuere+V+Inf` => `fluktuer`
* `fluktuere` has no analyses either

**strangle** failures:

* `strangle+V+Inf` => `strangl`
* `strangle` has no analyses either

**hardsalte** failures:

* `hardsalte+V+Inf` => `hardsalt`
* `hardsalte` has no analyses either

**fingere** failures:

* `fingere+V+Inf` => `finger`
* `fingere` has no analyses either

**gløyre** failures:

* `gløyre+V+Inf` does not generate!
* `gløyre` has no analyses either

**lukte** failures:

* `lukte+V+Inf` => `lukt`
* `lukte` has no analyses either

**blaskre** failures:

* `blaskre+V+Inf` => `blaskr`
* `blaskre` has no analyses either

**støyte** failures:

* `støyte+V+Inf` => `støyt`
* `støyte` has no analyses either

**reglementere** failures:

* `reglementere+V+Inf` => `reglementer`
* `reglementere` has no analyses either

**hyvekse** failures:

* `hyvekse+V+Inf` => `hyvea`
* `hyvekse+V+Inf` => `hyvee`
* `hyvekse` has no analyses either

**gisne** failures:

* `gisne+V+Inf` => `gisn`
* `gisne` has following analyses:
  * `gissen+A+Pl+Indef`
  * `gisen+A+Sg+Def`
  * `gissen+A+Sg+Def`
  * `gisen+A+Pl+Indef`

**aggregere** failures:

* `aggregere+V+Inf` => `aggreger`
* `aggregere` has no analyses either

**gjeldne** failures:

* `gjeldne+V+Inf` => `gjeldn`
* `gjeldne` has no analyses either

**taste** failures:

* `taste+V+Inf` => `tast`
* `taste` has no analyses either

**brikje** failures:

* `brikje+V+Inf` does not generate!
* `brikje` has no analyses either

**ekspandere** failures:

* `ekspandere+V+Inf` => `ekspander`
* `ekspandere` has no analyses either

**hyde** failures:

* `hyde+V+Inf` does not generate!
* `hyde` has no analyses either

**skile** failures:

* `skile+V+Inf` => `skil`
* `skile` has no analyses either

**knette** failures:

* `knette+V+Inf` => `knee`
* `knette+V+Inf` => `knea`
* `knette` has no analyses either

**enke** failures:

* `enke+V+Inf` => `enk`
* `enke` has following analyses:
  * `enkel+A+A+Pl+Indef`
  * `enkel+A+A+Pl+Def`
  * `enkel+A+A+Sg+Def`

**vedtektsfeste** failures:

* `vedtektsfeste+V+Inf` does not generate!
* `vedtektsfeste` has no analyses either

**chiffrere** failures:

* `chiffrere+V+Inf` => `chiffrer`
* `chiffrere` has no analyses either

**kafse** failures:

* `kafse+V+Inf` => `kafs`
* `kafse` has no analyses either

**bye** failures:

* `bye+V+Inf` => `by`
* `bye` has no analyses either

**bråstanse** failures:

* `bråstanse+V+Inf` => `bråstans`
* `bråstanse` has no analyses either

**buske** failures:

* `buske+V+Inf` => `busk`
* `buske` has no analyses either

**snigle** failures:

* `snigle+V+Inf` => `snigl`
* `snigle` has no analyses either

**forhandle** failures:

* `forhandle+V+Inf` => `forhandl`
* `forhandle` has no analyses either

**sinkleggje** failures:

* `sinkleggje+V+Inf` does not generate!
* `sinkleggje` has no analyses either

**raule** failures:

* `raule+V+Inf` => `raul`
* `raule` has no analyses either

**digitalisere** failures:

* `digitalisere+V+Inf` => `digitaliser`
* `digitalisere` has no analyses either

**bløme** failures:

* `bløme+V+Inf` does not generate!
* `bløme` has no analyses either

**false** failures:

* `false+V+Inf` => `fals`
* `false` has no analyses either

**motorisere** failures:

* `motorisere+V+Inf` => `motoriser`
* `motorisere` has no analyses either

**toge** failures:

* `toge+V+Inf` => `tog`
* `toge` has no analyses either

**føreha** failures:

* `føreha+V+Inf` => `førehaha`
* `føreha` has following analyses:
  * `førehand+N+N+Fem+Sg+Def`

**skrukke** failures:

* `skrukke+V+Inf` => `skrukk`
* `skrukke` has no analyses either

**intervjue** failures:

* `intervjue+V+Inf` => `intervju`
* `intervjue` has no analyses either

**søkke** failures:

* `søkke+V+Inf` => `s`
* `søkke` has no analyses either

**observere** failures:

* `observere+V+Inf` => `observer`
* `observere` has no analyses either

**onne** failures:

* `onne+V+Inf` => `onn`
* `onne` has no analyses either

**tekkjast** failures:

* `tekkjast+V+Inf` => `tekjast`
* `tekkjast` has no analyses either

**manudusere** failures:

* `manudusere+V+Inf` => `manuduser`
* `manudusere` has no analyses either

**blake** failures:

* `blake+V+Inf` => `blak`
* `blake` has following analyses:
  * `blakk+A+A+Pl+Def`
  * `blakk+A+A+Pl+Indef`
  * `blakk+A+A+Sg+Def`

**pumpe** failures:

* `pumpe+V+Inf` => `pump`
* `pumpe` has no analyses either

**sanne** failures:

* `sanne+V+Inf` => `sann`
* `sanne` has no analyses either

**revje** failures:

* `revje+V+Inf` => `revj`
* `revje` has no analyses either

**gante** failures:

* `gante+V+Inf` => `gant`
* `gante` has no analyses either

**avspore** failures:

* `avspore+V+Inf` => `avspor`
* `avspore` has no analyses either

**seigpine** failures:

* `seigpine+V+Inf` => `seigpin`
* `seigpine` has no analyses either

**trefalde** failures:

* `trefalde+V+Inf` => `trefald`
* `trefalde` has following analyses:
  * `trefald+A+A+Pl+Def`
  * `trefald+A+A+Pl+Indef`
  * `trefald+A+A+Sg+Def`

**snage** failures:

* `snage+V+Inf` => `snag`
* `snage` has no analyses either

**vantakke** failures:

* `vantakke+V+Inf` => `vantakk`
* `vantakke` has no analyses either

**framskunde** failures:

* `framskunde+V+Inf` => `framskund`
* `framskunde` has no analyses either

**respirere** failures:

* `respirere+V+Inf` => `respirer`
* `respirere` has no analyses either

**minimere** failures:

* `minimere+V+Inf` => `minimer`
* `minimere` has no analyses either

**tørrsalte** failures:

* `tørrsalte+V+Inf` => `tørrsalt`
* `tørrsalte` has no analyses either

**pryle** failures:

* `pryle+V+Inf` => `pryl`
* `pryle` has no analyses either

**kapitulere** failures:

* `kapitulere+V+Inf` => `kapituler`
* `kapitulere` has no analyses either

**jumpe** failures:

* `jumpe+V+Inf` => `jump`
* `jumpe` has no analyses either

**ferdast** failures:

* `ferdast+V+Inf` => `ferdastast`
* `ferdast` has no analyses either

**subordinere** failures:

* `subordinere+V+Inf` => `subordiner`
* `subordinere` has no analyses either

**grufse** failures:

* `grufse+V+Inf` => `grufs`
* `grufse` has following analyses:
  * `grufsen+A+Neu+Sg+Indef`

**pruste** failures:

* `pruste+V+Inf` => `prust`
* `pruste` has no analyses either

**greipe** failures:

* `greipe+V+Inf` => `greip`
* `greipe` has no analyses either

**arme** failures:

* `arme+V+Inf` => `arm`
* `arme` has following analyses:
  * `arm+A+Pl+Indef`
  * `arm+A+Sg+Def`
  * `arme+N+N+Msc+Sg+Indef`

**sukle** failures:

* `sukle+V+Inf` => `sukl`
* `sukle` has no analyses either

**snavle** failures:

* `snavle+V+Inf` => `snavl`
* `snavle` has no analyses either

**skattlegge** failures:

* `skattlegge+V+Inf` does not generate!
* `skattlegge` has no analyses either

**kviskre** failures:

* `kviskre+V+Inf` => `kviskr`
* `kviskre` has no analyses either

**inaktivere** failures:

* `inaktivere+V+Inf` => `inaktiver`
* `inaktivere` has no analyses either

**notere** failures:

* `notere+V+Inf` => `noter`
* `notere` has no analyses either

**hyne** failures:

* `hyne+V+Inf` => `hyn`
* `hyne` has no analyses either

**paradere** failures:

* `paradere+V+Inf` => `parader`
* `paradere` has no analyses either

**kikre** failures:

* `kikre+V+Inf` => `kikr`
* `kikre` has no analyses either

**skimle** failures:

* `skimle+V+Inf` => `skiml`
* `skimle` has no analyses either

**forskusle** failures:

* `forskusle+V+Inf` => `forskusl`
* `forskusle` has no analyses either

**skjeggje** failures:

* `skjeggje+V+Inf` => `skjegg`
* `skjeggje` has no analyses either

**ombere** failures:

* `ombere+V+Inf` => `ombea`
* `ombere+V+Inf` => `ombee`
* `ombere` has no analyses either

**snøfte** failures:

* `snøfte+V+Inf` => `snøft`
* `snøfte` has no analyses either

**stumre** failures:

* `stumre+V+Inf` => `stumr`
* `stumre` has no analyses either

**stygge** failures:

* `stygge+V+Inf` does not generate!
* `stygge` has no analyses either

**einstire** failures:

* `einstire+V+Inf` does not generate!
* `einstire` has no analyses either

**orge** failures:

* `orge+V+Inf` => `org`
* `orge` has no analyses either

**anglifisere** failures:

* `anglifisere+V+Inf` => `anglifiser`
* `anglifisere` has no analyses either

**flåhakke** failures:

* `flåhakke+V+Inf` => `flåhakk`
* `flåhakke` has no analyses either

**breke** failures:

* `breke+V+Inf` => `brek`
* `breke` has following analyses:
  * `breken+A+Neu+Sg+Indef`

**utleie** failures:

* `utleie+V+Inf` does not generate!
* `utleie` has no analyses either

**skrasle** failures:

* `skrasle+V+Inf` => `skrasl`
* `skrasle` has following analyses:
  * `skraslen+A+Neu+Sg+Indef`

**diversifisere** failures:

* `diversifisere+V+Inf` => `diversifiser`
* `diversifisere` has no analyses either

**lade** failures:

* `lade+V+Inf` does not generate!
* `lade` has no analyses either

**glore** failures:

* `glore+V+Inf` => `glor`
* `glore` has following analyses:
  * `gloren+A+Neu+Sg+Indef`

**trøytne** failures:

* `trøytne+V+Inf` => `trøytn`
* `trøytne` has following analyses:
  * `trøyten+A+Sg+Def`
  * `trøyten+A+Pl+Indef`

**blakre** failures:

* `blakre+V+Inf` => `blakr`
* `blakre` has no analyses either

**ringast** failures:

* `ringast+V+Inf` => `ringastast`
* `ringast` has following analyses:
  * `ring+A+Superl+Indef`

**gripe** failures:

* `gripe+V+Inf` => `grie`
* `gripe+V+Inf` => `gria`
* `gripe` has following analyses:
  * `gripen+A+Neu+Sg+Indef`

**korsfeste** failures:

* `korsfeste+V+Inf` does not generate!
* `korsfeste` has no analyses either

**bune** failures:

* `bune+V+Inf` => `bun`
* `bune` has following analyses:
  * `buen+A+Pl+Indef`
  * `buen+A+Sg+Def`

**forlengje** failures:

* `forlengje+V+Inf` does not generate!
* `forlengje` has no analyses either

**motprove** failures:

* `motprove+V+Inf` => `motprov`
* `motprove` has no analyses either

**stakke** failures:

* `stakke+V+Inf` => `stakk`
* `stakke` has no analyses either

**kalkulere** failures:

* `kalkulere+V+Inf` => `kalkuler`
* `kalkulere` has no analyses either

**sjevre** failures:

* `sjevre+V+Inf` => `sjevr`
* `sjevre` has no analyses either

**bere** failures:

* `bere+V+Inf` => `bea`
* `bere+V+Inf` => `bee`
* `bere` has following analyses:
  * `berr+A+A+Pl+Def`
  * `berr+A+A+Sg+Def`
  * `berr+A+A+Pl+Indef`

**korrumpere** failures:

* `korrumpere+V+Inf` => `korrumper`
* `korrumpere` has no analyses either

**temporisere** failures:

* `temporisere+V+Inf` => `temporiser`
* `temporisere` has no analyses either

**utmåle** failures:

* `utmåle+V+Inf` => `utmål`
* `utmåle` has no analyses either

**aksjonere** failures:

* `aksjonere+V+Inf` => `aksjoner`
* `aksjonere` has no analyses either

**tekkast** failures:

* `tekkast+V+Inf` => `tekast`
* `tekkast` has no analyses either

**marke** failures:

* `marke+V+Inf` => `mark`
* `marke` has no analyses either

**råskrelle** failures:

* `råskrelle+V+Inf` does not generate!
* `råskrelle` has no analyses either

**fomme** failures:

* `fomme+V+Inf` => `fom`
* `fomme` has no analyses either

**slepe** failures:

* `slepe+V+Inf` does not generate!
* `slepe` has following analyses:
  * `sleppen+A+Neu+Sg+Indef`

**dedusere** failures:

* `dedusere+V+Inf` => `deduser`
* `dedusere` has no analyses either

**åtake** failures:

* `åtake+V+Inf` => `åttake`
* `åtake+V+Inf` => `åttaka`
* `åtake` has no analyses either

**fyre** failures:

* `fyre+V+Inf` does not generate!
* `fyre` has no analyses either

**fline** failures:

* `fline+V+Inf` => `flia`
* `fline+V+Inf` => `flie`
* `fline` has following analyses:
  * `fline+V+Adj+Fl`
  * `fline+V+Adj+Bu+Eint`

**ryggje** failures:

* `ryggje+V+Inf` does not generate!
* `ryggje` has no analyses either

**sloke** failures:

* `sloke+V+Inf` => `slok`
* `sloke` has no analyses either

**kaldflire** failures:

* `kaldflire+V+Inf` does not generate!
* `kaldflire` has no analyses either

**klinge** failures:

* `klinge+V+Inf` => `klia`
* `klinge+V+Inf` => `klie`
* `klinge` has no analyses either

**effektuere** failures:

* `effektuere+V+Inf` => `effektuer`
* `effektuere` has no analyses either

**trengast** failures:

* `trengast+V+Inf` => `trtrengast`
* `trengast` has no analyses either

**elje** failures:

* `elje+V+Inf` => `eljeeja`
* `elje+V+Inf` => `eljeeje`
* `elje` has no analyses either

**karakterisere** failures:

* `karakterisere+V+Inf` => `karakteriser`
* `karakterisere` has no analyses either

**klukke** failures:

* `klukke+V+Inf` => `klukk`
* `klukke` has no analyses either

**godskrive** failures:

* `godskrive+V+Inf` => `godskria`
* `godskrive+V+Inf` => `godskrie`
* `godskrive` has no analyses either

**grøde** failures:

* `grøde+V+Inf` does not generate!
* `grøde` has no analyses either

**grønast** failures:

* `grønast+V+Inf` => `grønastast`
* `grønast` has following analyses:
  * `grøn+A+Superl+Indef`

**berre** failures:

* `berre+V+Inf` => `berr`
* `berre` has no analyses either

**støne** failures:

* `støne+V+Inf` => `støn`
* `støne` has no analyses either

**vigle** failures:

* `vigle+V+Inf` => `vigl`
* `vigle` has following analyses:
  * `viglen+A+Neu+Sg+Indef`

**omfordele** failures:

* `omfordele+V+Inf` => `omfordel`
* `omfordele` has no analyses either

**avfyre** failures:

* `avfyre+V+Inf` => `avfyr`
* `avfyre` has no analyses either

**mognast** failures:

* `mognast+V+Inf` => `mognastast`
* `mognast` has following analyses:
  * `mogen+A+Superl+Indef`

**eloksere** failures:

* `eloksere+V+Inf` => `elokser`
* `eloksere` has no analyses either

**skjåsse** failures:

* `skjåsse+V+Inf` => `skjåss`
* `skjåsse` has no analyses either

**vårast** failures:

* `vårast+V+Inf` => `vårastast`
* `vårast` has no analyses either

**forsinke** failures:

* `forsinke+V+Inf` => `forsink`
* `forsinke` has no analyses either

**kaldne** failures:

* `kaldne+V+Inf` => `kaldn`
* `kaldne` has no analyses either

**sterre** failures:

* `sterre+V+Inf` => `sterr`
* `sterre` has no analyses either

**attrahere** failures:

* `attrahere+V+Inf` => `attraher`
* `attrahere` has no analyses either

**gote** failures:

* `gote+V+Inf` => `got`
* `gote` has following analyses:
  * `goten+A+Neu+Sg+Indef`

**dinere** failures:

* `dinere+V+Inf` => `diner`
* `dinere` has no analyses either

**dagne** failures:

* `dagne+V+Inf` => `dagn`
* `dagne` has no analyses either

**gande** failures:

* `gande+V+Inf` => `gand`
* `gande` has following analyses:
  * `gyse+V+PrsPrc`

**tiltale** failures:

* `tiltale+V+Inf` does not generate!
* `tiltale` has no analyses either

**utvide** failures:

* `utvide+V+Inf` => `utvid`
* `utvide` has no analyses either

**forulykke** failures:

* `forulykke+V+Inf` => `forulykk`
* `forulykke` has no analyses either

**misleghalde** failures:

* `misleghalde+V+Inf` => `misleghae`
* `misleghalde+V+Inf` => `misleghaa`
* `misleghalde` has no analyses either

**rekommandere** failures:

* `rekommandere+V+Inf` => `rekommander`
* `rekommandere` has no analyses either

**persistere** failures:

* `persistere+V+Inf` => `persister`
* `persistere` has no analyses either

**stivle** failures:

* `stivle+V+Inf` => `stivl`
* `stivle` has no analyses either

**henge** failures:

* `henge+V+Inf` => `h`
* `henge` has no analyses either

**visse** failures:

* `visse+V+Inf` => `viss`
* `visse` has following analyses:
  * `viss+A+Pl+Indef`
  * `viss+A+Sg+Def`

**angre** failures:

* `angre+V+Inf` => `angr`
* `angre` has following analyses:
  * `angren+A+Neu+Sg+Indef`

**tilarbeide** failures:

* `tilarbeide+V+Inf` does not generate!
* `tilarbeide` has no analyses either

**stratifisere** failures:

* `stratifisere+V+Inf` => `stratifiser`
* `stratifisere` has no analyses either

**tvile** failures:

* `tvile+V+Inf` does not generate!
* `tvile` has no analyses either

**kleise** failures:

* `kleise+V+Inf` => `kleis`
* `kleise` has following analyses:
  * `kleis+A+Sg+Def`
  * `kleis+A+Pl+Indef`

**knabbe** failures:

* `knabbe+V+Inf` => `knabb`
* `knabbe` has no analyses either

**kruke** failures:

* `kruke+V+Inf` does not generate!
* `kruke` has no analyses either

**lokalisere** failures:

* `lokalisere+V+Inf` => `lokaliser`
* `lokalisere` has no analyses either

**av** failures:

* `av+V+Inf` does not generate!
* `av` has following analyses:
  * `ave+N`
  * `av+Pr`

**stenografere** failures:

* `stenografere+V+Inf` => `stenografer`
* `stenografere` has no analyses either

**skvalpe** failures:

* `skvalpe+V+Inf` => `skvalp`
* `skvalpe` has no analyses either

**forsumpe** failures:

* `forsumpe+V+Inf` => `forsump`
* `forsumpe` has no analyses either

**smashe** failures:

* `smashe+V+Inf` => `smash`
* `smashe` has no analyses either

**gjennomsteikje** failures:

* `gjennomsteikje+V+Inf` does not generate!
* `gjennomsteikje` has no analyses either

**sideordne** failures:

* `sideordne+V+Inf` => `sideordn`
* `sideordne` has no analyses either

**flåne** failures:

* `flåne+V+Inf` => `flån`
* `flåne` has no analyses either

**fimre** failures:

* `fimre+V+Inf` => `fimr`
* `fimre` has following analyses:
  * `fimren+A+Neu+Sg+Indef`

**rustbehandle** failures:

* `rustbehandle+V+Inf` => `rustbehandl`
* `rustbehandle` has no analyses either

**duske** failures:

* `duske+V+Inf` => `dusk`
* `duske` has no analyses either

**tilkjenne** failures:

* `tilkjenne+V+Inf` does not generate!
* `tilkjenne` has no analyses either

**utfalde** failures:

* `utfalde+V+Inf` => `utfald`
* `utfalde` has no analyses either

**laminere** failures:

* `laminere+V+Inf` => `laminer`
* `laminere` has no analyses either

**galdre** failures:

* `galdre+V+Inf` => `galdr`
* `galdre` has no analyses either

**slikje** failures:

* `slikje+V+Inf` does not generate!
* `slikje` has no analyses either

**ugilde** failures:

* `ugilde+V+Inf` => `ugild`
* `ugilde` has following analyses:
  * `ugild+A+Pl+Indef`
  * `ugild+A+Sg+Def`

**fjeske** failures:

* `fjeske+V+Inf` => `fjesk`
* `fjeske` has no analyses either

**øye** failures:

* `øye+V+Inf` => `øy`
* `øye` has no analyses either

**gaupe** failures:

* `gaupe+V+Inf` => `gaup`
* `gaupe` has no analyses either

**knote** failures:

* `knote+V+Inf` => `knot`
* `knote` has no analyses either

**sympatisere** failures:

* `sympatisere+V+Inf` => `sympatiser`
* `sympatisere` has no analyses either

**frakke** failures:

* `frakke+V+Inf` => `frakk`
* `frakke` has no analyses either

**pronominalisere** failures:

* `pronominalisere+V+Inf` => `pronominaliser`
* `pronominalisere` has no analyses either

**lignifisere** failures:

* `lignifisere+V+Inf` => `lignifiser`
* `lignifisere` has no analyses either

**dirre** failures:

* `dirre+V+Inf` => `dirr`
* `dirre` has no analyses either

**svulme** failures:

* `svulme+V+Inf` => `svulm`
* `svulme` has no analyses either

**føyke** failures:

* `føyke+V+Inf` => `føyk`
* `føyke` has no analyses either

**abstrahere** failures:

* `abstrahere+V+Inf` => `abstraher`
* `abstrahere` has no analyses either

**fliselegge** failures:

* `fliselegge+V+Inf` does not generate!
* `fliselegge` has no analyses either

**naudbe** failures:

* `naudbe+V+Inf` => `naudbbe`
* `naudbe` has following analyses:
  * `naudbede+V+Imprt`
  * `naudbede+V+Ind+Prs`

**invitere** failures:

* `invitere+V+Inf` => `inviter`
* `invitere` has no analyses either

**nettprate** failures:

* `nettprate+V+Inf` => `nettprat`
* `nettprate` has no analyses either

**genmodifisere** failures:

* `genmodifisere+V+Inf` => `genmodifiser`
* `genmodifisere` has no analyses either

**støkke** failures:

* `støkke+V+Inf` does not generate!
* `støkke` has no analyses either

**kjeftast** failures:

* `kjeftast+V+Inf` => `kjeftastast`
* `kjeftast` has no analyses either

**ølne** failures:

* `ølne+V+Inf` => `øln`
* `ølne` has following analyses:
  * `ølen+A+Pl+Indef`
  * `ølen+A+Sg+Def`

**sjanghaie** failures:

* `sjanghaie+V+Inf` => `sjanghai`
* `sjanghaie` has no analyses either

**klinke** failures:

* `klinke+V+Inf` => `klink`
* `klinke` has following analyses:
  * `klink+A+A+Pl+Indef`
  * `klink+A+A+Sg+Def`
  * `klink+A+A+Pl+Def`

**forvitnast** failures:

* `forvitnast+V+Inf` => `forvitnastast`
* `forvitnast` has no analyses either

**intensivere** failures:

* `intensivere+V+Inf` => `intensiver`
* `intensivere` has no analyses either

**søye** failures:

* `søye+V+Inf` does not generate!
* `søye` has no analyses either

**tinkle** failures:

* `tinkle+V+Inf` => `tinkl`
* `tinkle` has no analyses either

**utmerkje** failures:

* `utmerkje+V+Inf` does not generate!
* `utmerkje` has no analyses either

**fortapast** failures:

* `fortapast+V+Inf` => `fortapastast`
* `fortapast` has no analyses either

**bunte** failures:

* `bunte+V+Inf` => `bunt`
* `bunte` has no analyses either

**pjaske** failures:

* `pjaske+V+Inf` => `pjask`
* `pjaske` has following analyses:
  * `pjasken+A+Neu+Sg+Indef`

**fosforisere** failures:

* `fosforisere+V+Inf` => `fosforiser`
* `fosforisere` has no analyses either

**transcendere** failures:

* `transcendere+V+Inf` => `transcender`
* `transcendere` has no analyses either

**hugleggje** failures:

* `hugleggje+V+Inf` does not generate!
* `hugleggje` has no analyses either

**resymere** failures:

* `resymere+V+Inf` => `resymer`
* `resymere` has no analyses either

**serve** failures:

* `serve+V+Inf` => `serv`
* `serve` has no analyses either

**kverke** failures:

* `kverke+V+Inf` => `kverk`
* `kverke` has no analyses either

**rivne** failures:

* `rivne+V+Inf` => `rivn`
* `rivne` has no analyses either

**stimreinse** failures:

* `stimreinse+V+Inf` => `stimreins`
* `stimreinse` has no analyses either

**kontaminere** failures:

* `kontaminere+V+Inf` => `kontaminer`
* `kontaminere` has no analyses either

**laborere** failures:

* `laborere+V+Inf` => `laborer`
* `laborere` has no analyses either

**skafte** failures:

* `skafte+V+Inf` => `skaft`
* `skafte` has no analyses either

**subvenere** failures:

* `subvenere+V+Inf` => `subvener`
* `subvenere` has no analyses either

**skirsle** failures:

* `skirsle+V+Inf` => `skirsl`
* `skirsle` has no analyses either

**sote** failures:

* `sote+V+Inf` => `sot`
* `sote` has no analyses either

**lulle** failures:

* `lulle+V+Inf` => `lull`
* `lulle` has no analyses either

**snørekøyre** failures:

* `snørekøyre+V+Inf` does not generate!
* `snørekøyre` has no analyses either

**framsnakke** failures:

* `framsnakke+V+Inf` => `framsnakk`
* `framsnakke` has no analyses either

**hemle** failures:

* `hemle+V+Inf` => `heml`
* `hemle` has no analyses either

**hæsne** failures:

* `hæsne+V+Inf` => `hæsn`
* `hæsne` has following analyses:
  * `hæsen+A+Pl+Indef`
  * `hæsen+A+Sg+Def`

**lygne** failures:

* `lygne+V+Inf` => `lygn`
* `lygne` has following analyses:
  * `lygen+A+Pl+Indef`
  * `lygen+A+Sg+Def`

**njode** failures:

* `njode+V+Inf` => `njoe`
* `njode+V+Inf` => `njoa`
* `njode` has no analyses either

**lastbalansere** failures:

* `lastbalansere+V+Inf` => `lastbalanser`
* `lastbalansere` has no analyses either

**tappast** failures:

* `tappast+V+Inf` => `tappastast`
* `tappast` has no analyses either

**dedikere** failures:

* `dedikere+V+Inf` => `dediker`
* `dedikere` has no analyses either

**lape** failures:

* `lape+V+Inf` => `lap`
* `lape` has following analyses:
  * `lapen+A+Neu+Sg+Indef`

**publisere** failures:

* `publisere+V+Inf` => `publiser`
* `publisere` has no analyses either

**pelletere** failures:

* `pelletere+V+Inf` => `pelleter`
* `pelletere` has no analyses either

**rafse** failures:

* `rafse+V+Inf` => `rafs`
* `rafse` has no analyses either

**filosofere** failures:

* `filosofere+V+Inf` => `filosofer`
* `filosofere` has no analyses either

**bane** failures:

* `bane+V+Inf` => `ban`
* `bane` has no analyses either

**meie** failures:

* `meie+V+Inf` does not generate!
* `meie` has no analyses either

**divergere** failures:

* `divergere+V+Inf` => `diverger`
* `divergere` has no analyses either

**slepekøyre** failures:

* `slepekøyre+V+Inf` does not generate!
* `slepekøyre` has no analyses either

**grute** failures:

* `grute+V+Inf` => `grut`
* `grute` has no analyses either

**kaskoforsikre** failures:

* `kaskoforsikre+V+Inf` => `kaskoforsikr`
* `kaskoforsikre` has no analyses either

**relativisere** failures:

* `relativisere+V+Inf` => `relativiser`
* `relativisere` has no analyses either

**heimfeste** failures:

* `heimfeste+V+Inf` does not generate!
* `heimfeste` has no analyses either

**flankere** failures:

* `flankere+V+Inf` => `flanker`
* `flankere` has no analyses either

**glanke** failures:

* `glanke+V+Inf` => `glank`
* `glanke` has no analyses either

**maskinere** failures:

* `maskinere+V+Inf` => `maskiner`
* `maskinere` has no analyses either

**mikse** failures:

* `mikse+V+Inf` => `miks`
* `mikse` has no analyses either

**beinflyge** failures:

* `beinflyge+V+Inf` => `beinflya`
* `beinflyge+V+Inf` => `beinflye`
* `beinflyge` has no analyses either

**sputre** failures:

* `sputre+V+Inf` => `sputr`
* `sputre` has no analyses either

**tove** failures:

* `tove+V+Inf` => `tov`
* `tove` has no analyses either

**sveise** failures:

* `sveise+V+Inf` does not generate!
* `sveise` has no analyses either

**reddast** failures:

* `reddast+V+Inf` => `reddastast`
* `reddast` has following analyses:
  * `redd+A+A+Superl+Indef`

**inn** failures:

* `inn+V+Inf` does not generate!
* `inn` has following analyses:
  * `inn+A+A+Msc+Sg+Indef`
  * `inn+A+A+Fem+Sg+Indef`
  * `inn+A+A+Neu+Sg+Indef`
  * `inn+Adv`

**skjelve** failures:

* `skjelve+V+Inf` => `skjee`
* `skjelve+V+Inf` => `skjea`
* `skjelve` has following analyses:
  * `skjelv+A+A+Pl+Indef`
  * `skjelven+A+Neu+Sg+Indef`
  * `skjelv+A+A+Pl+Def`
  * `skjelv+A+A+Sg+Def`

**dementere** failures:

* `dementere+V+Inf` => `dementer`
* `dementere` has no analyses either

**fullrose** failures:

* `fullrose+V+Inf` does not generate!
* `fullrose` has no analyses either

**stelle** failures:

* `stelle+V+Inf` does not generate!
* `stelle` has no analyses either

**omgi** failures:

* `omgi+V+Inf` => `omggi`
* `omgi` has no analyses either

**gjorne** failures:

* `gjorne+V+Inf` => `gjorn`
* `gjorne` has no analyses either

**ljode** failures:

* `ljode+V+Inf` => `ljod`
* `ljode` has no analyses either

**tugle** failures:

* `tugle+V+Inf` => `tugl`
* `tugle` has no analyses either

**absolvere** failures:

* `absolvere+V+Inf` => `absolver`
* `absolvere` has no analyses either

**olboge** failures:

* `olboge+V+Inf` => `olbog`
* `olboge` has no analyses either

**blinde** failures:

* `blinde+V+Inf` => `blind`
* `blinde` has following analyses:
  * `blind+A+Sg+Def`
  * `blind+A+Pl+Indef`

**mineleggje** failures:

* `mineleggje+V+Inf` does not generate!
* `mineleggje` has no analyses either

**misrekne** failures:

* `misrekne+V+Inf` => `misrekn`
* `misrekne` has no analyses either

**telne** failures:

* `telne+V+Inf` => `teln`
* `telne` has no analyses either

**forgje** failures:

* `forgje+V+Inf` => `forggje`
* `forgje` has no analyses either

**skjele** failures:

* `skjele+V+Inf` => `skjel`
* `skjele` has no analyses either

**tilstoppe** failures:

* `tilstoppe+V+Inf` => `tilstopp`
* `tilstoppe` has no analyses either

**idyllisere** failures:

* `idyllisere+V+Inf` => `idylliser`
* `idyllisere` has no analyses either

**kommittere** failures:

* `kommittere+V+Inf` => `kommitter`
* `kommittere` has no analyses either

**vimse** failures:

* `vimse+V+Inf` => `vims`
* `vimse` has no analyses either

**stille** failures:

* `stille+V+Inf` does not generate!
* `stille` has following analyses:
  * `stille+A+Sg+Def`
  * `stille+A+Neu+Sg+Indef`
  * `stille+A+Msc+Sg+Indef`
  * `stille+A+Fem+Sg+Indef`
  * `stille+A+Pl+Indef`

**arretere** failures:

* `arretere+V+Inf` => `arreter`
* `arretere` has no analyses either

**hurpe** failures:

* `hurpe+V+Inf` => `hurp`
* `hurpe` has no analyses either

**påskjønne** failures:

* `påskjønne+V+Inf` does not generate!
* `påskjønne` has no analyses either

**strangulere** failures:

* `strangulere+V+Inf` => `stranguler`
* `strangulere` has no analyses either

**degenerere** failures:

* `degenerere+V+Inf` => `degenerer`
* `degenerere` has no analyses either

**forankre** failures:

* `forankre+V+Inf` => `forankr`
* `forankre` has no analyses either

**bråsvinge** failures:

* `bråsvinge+V+Inf` => `bråsving`
* `bråsvinge` has no analyses either

**motverke** failures:

* `motverke+V+Inf` => `motverk`
* `motverke` has no analyses either

**utfolde** failures:

* `utfolde+V+Inf` => `utfold`
* `utfolde` has no analyses either

**eksekvere** failures:

* `eksekvere+V+Inf` => `eksekver`
* `eksekvere` has no analyses either

**kjærteikne** failures:

* `kjærteikne+V+Inf` => `kjærteikn`
* `kjærteikne` has no analyses either

**kimse** failures:

* `kimse+V+Inf` => `kims`
* `kimse` has no analyses either

**heidre** failures:

* `heidre+V+Inf` => `heidr`
* `heidre` has no analyses either

**nøre** failures:

* `nøre+V+Inf` => `nør`
* `nøre` has no analyses either

**maure** failures:

* `maure+V+Inf` => `maur`
* `maure` has no analyses either

**avråde** failures:

* `avråde+V+Inf` does not generate!
* `avråde` has no analyses either

**fatte** failures:

* `fatte+V+Inf` => `fatt`
* `fatte` has following analyses:
  * `fatt+A+A+Pl+Def`
  * `fatt+A+A+Sg+Def`
  * `fatt+A+A+Pl+Indef`

**erotisere** failures:

* `erotisere+V+Inf` => `erotiser`
* `erotisere` has no analyses either

**rolle** failures:

* `rolle+V+Inf` => `roll`
* `rolle` has no analyses either

**sjå** failures:

* `sjå+V+Inf` => `ssjå`
* `sjå` has following analyses:
  * `sjå+N+N+Neu+Pl+Indef`
  * `se+V+Inf`
  * `se+V+Imprt`
  * `sjå+N+N+Neu+Sg+Indef`

**potensere** failures:

* `potensere+V+Inf` => `potenser`
* `potensere` has no analyses either

**avsløre** failures:

* `avsløre+V+Inf` does not generate!
* `avsløre` has no analyses either

**pole** failures:

* `pole+V+Inf` => `pol`
* `pole` has no analyses either

**lyske** failures:

* `lyske+V+Inf` => `lysk`
* `lyske` has no analyses either

**blesme** failures:

* `blesme+V+Inf` => `blesm`
* `blesme` has following analyses:
  * `blesm+A+A+Pl+Indef`
  * `blesm+A+A+Sg+Def`
  * `blesm+A+A+Pl+Def`

**forstrekkje** failures:

* `forstrekkje+V+Inf` does not generate!
* `forstrekkje` has no analyses either

**hurke** failures:

* `hurke+V+Inf` => `hurk`
* `hurke` has following analyses:
  * `hurken+A+Neu+Sg+Indef`

**innynde** failures:

* `innynde+V+Inf` => `innynd`
* `innynde` has no analyses either

**jassifisere** failures:

* `jassifisere+V+Inf` => `jassifiser`
* `jassifisere` has no analyses either

**forveksle** failures:

* `forveksle+V+Inf` => `forveksl`
* `forveksle` has no analyses either

**reiste** failures:

* `reiste+V+Inf` => `reist`
* `reiste` has following analyses:
  * `reise+V+PrfPrc`

**halvtekkje** failures:

* `halvtekkje+V+Inf` does not generate!
* `halvtekkje` has no analyses either

**køyre** failures:

* `køyre+V+Inf` does not generate!
* `køyre` has no analyses either

**magnetisere** failures:

* `magnetisere+V+Inf` => `magnetiser`
* `magnetisere` has no analyses either

**buksere** failures:

* `buksere+V+Inf` => `bukser`
* `buksere` has no analyses either

**fallere** failures:

* `fallere+V+Inf` => `faller`
* `fallere` has no analyses either

**skake** failures:

* `skake+V+Inf` does not generate!
* `skake` has following analyses:
  * `skakk+A+A+Pl+Def`
  * `skakk+A+A+Pl+Indef`
  * `skakk+A+A+Sg+Def`

**demisjonere** failures:

* `demisjonere+V+Inf` => `demisjoner`
* `demisjonere` has no analyses either

**kløfte** failures:

* `kløfte+V+Inf` => `kløft`
* `kløfte` has no analyses either

**drible** failures:

* `drible+V+Inf` => `dribl`
* `drible` has no analyses either

**belage** failures:

* `belage+V+Inf` => `belag`
* `belage` has no analyses either

**brytje** failures:

* `brytje+V+Inf` => `brytj`
* `brytje` has no analyses either

**eksilere** failures:

* `eksilere+V+Inf` => `eksiler`
* `eksilere` has no analyses either

**indoktrinere** failures:

* `indoktrinere+V+Inf` => `indoktriner`
* `indoktrinere` has no analyses either

**taske** failures:

* `taske+V+Inf` => `task`
* `taske` has no analyses either

**hospitere** failures:

* `hospitere+V+Inf` => `hospiter`
* `hospitere` has no analyses either

**jasle** failures:

* `jasle+V+Inf` => `jasl`
* `jasle` has no analyses either

**danse** failures:

* `danse+V+Inf` => `dans`
* `danse` has no analyses either

**pente** failures:

* `pente+V+Inf` => `pent`
* `pente` has no analyses either

**skratle** failures:

* `skratle+V+Inf` => `skratl`
* `skratle` has no analyses either

**snyde** failures:

* `snyde+V+Inf` does not generate!
* `snyde` has no analyses either

**gnavle** failures:

* `gnavle+V+Inf` => `gnavl`
* `gnavle` has no analyses either

**misoppfatte** failures:

* `misoppfatte+V+Inf` => `misoppfatt`
* `misoppfatte` has no analyses either

**prime** failures:

* `prime+V+Inf` => `prim`
* `prime` has no analyses either

**slaglodde** failures:

* `slaglodde+V+Inf` => `slaglodd`
* `slaglodde` has no analyses either

**vrimle** failures:

* `vrimle+V+Inf` => `vriml`
* `vrimle` has no analyses either

**klarne** failures:

* `klarne+V+Inf` => `klarn`
* `klarne` has no analyses either

**ime** failures:

* `ime+V+Inf` => `im`
* `ime` has following analyses:
  * `imen+A+Neu+Sg+Indef`

**greine** failures:

* `greine+V+Inf` => `grein`
* `greine` has no analyses either

**forsove** failures:

* `forsove+V+Inf` => `forsoe`
* `forsove+V+Inf` => `forsoa`
* `forsove` has no analyses either

**reve** failures:

* `reve+V+Inf` => `rev`
* `reve` has no analyses either

**tråne** failures:

* `tråne+V+Inf` => `trån`
* `tråne` has following analyses:
  * `tråen+A+Pl+Indef`
  * `tråen+A+Sg+Def`

**ettergjeve** failures:

* `ettergjeve+V+Inf` => `etterggjeva`
* `ettergjeve+V+Inf` => `etterggjeve`
* `ettergjeve` has following analyses:
  * `ettergjeven+A+Neu+Sg+Indef`

**brikettere** failures:

* `brikettere+V+Inf` => `briketter`
* `brikettere` has no analyses either

**anerkjenne** failures:

* `anerkjenne+V+Inf` => `anerkjena`
* `anerkjenne+V+Inf` => `anerkjene`
* `anerkjenne` has no analyses either

**utvise** failures:

* `utvise+V+Inf` => `utvis`
* `utvise` has no analyses either

**hitte** failures:

* `hitte+V+Inf` => `hitt`
* `hitte` has no analyses either

**likerette** failures:

* `likerette+V+Inf` => `likerett`
* `likerette` has no analyses either

**tilheimle** failures:

* `tilheimle+V+Inf` => `tilheiml`
* `tilheimle` has no analyses either

**oppdrive** failures:

* `oppdrive+V+Inf` => `oppdrie`
* `oppdrive+V+Inf` => `oppdria`
* `oppdrive` has following analyses:
  * `oppdriven+A+Neu+Sg+Indef`

**krungle** failures:

* `krungle+V+Inf` => `krungl`
* `krungle` has no analyses either

**tøyre** failures:

* `tøyre+V+Inf` does not generate!
* `tøyre` has no analyses either

**fokusere** failures:

* `fokusere+V+Inf` => `fokuser`
* `fokusere` has no analyses either

**tørne** failures:

* `tørne+V+Inf` => `tørn`
* `tørne` has no analyses either

**skorte** failures:

* `skorte+V+Inf` => `skort`
* `skorte` has no analyses either

**falby** failures:

* `falby+V+Inf` does not generate!
* `falby` has no analyses either

**gape** failures:

* `gape+V+Inf` does not generate!
* `gape` has no analyses either

**kaue** failures:

* `kaue+V+Inf` => `kau`
* `kaue` has no analyses either

**låne** failures:

* `låne+V+Inf` => `lån`
* `låne` has following analyses:
  * `låte+V+Adj+Bu+Eint`
  * `låte+V+Adj+Fl`

**foge** failures:

* `foge+V+Inf` => `fog`
* `foge` has no analyses either

**fullautomatisere** failures:

* `fullautomatisere+V+Inf` => `fullautomatiser`
* `fullautomatisere` has no analyses either

**krevje** failures:

* `krevje+V+Inf` does not generate!
* `krevje` has no analyses either

**hurve** failures:

* `hurve+V+Inf` => `hurv`
* `hurve` has no analyses either

**blenge** failures:

* `blenge+V+Inf` does not generate!
* `blenge` has no analyses either

**fortære** failures:

* `fortære+V+Inf` => `fortær`
* `fortære` has no analyses either

**innehalde** failures:

* `innehalde+V+Inf` => `innehae`
* `innehalde+V+Inf` => `innehaa`
* `innehalde` has no analyses either

**ofre** failures:

* `ofre+V+Inf` => `ofr`
* `ofre` has no analyses either

**syfte** failures:

* `syfte+V+Inf` does not generate!
* `syfte` has no analyses either

**dragne** failures:

* `dragne+V+Inf` => `dragn`
* `dragne` has no analyses either

**omprioritere** failures:

* `omprioritere+V+Inf` => `omprioriter`
* `omprioritere` has no analyses either

**rolpe** failures:

* `rolpe+V+Inf` => `rolp`
* `rolpe` has no analyses either

**underkjøle** failures:

* `underkjøle+V+Inf` => `underkjøl`
* `underkjøle` has no analyses either

**forfine** failures:

* `forfine+V+Inf` => `forfin`
* `forfine` has no analyses either

**konkludere** failures:

* `konkludere+V+Inf` => `konkluder`
* `konkludere` has no analyses either

**forhage** failures:

* `forhage+V+Inf` => `forhag`
* `forhage` has no analyses either

**lovprise** failures:

* `lovprise+V+Inf` does not generate!
* `lovprise` has no analyses either

**kastrere** failures:

* `kastrere+V+Inf` => `kastrer`
* `kastrere` has no analyses either

**hysje** failures:

* `hysje+V+Inf` => `hysj`
* `hysje` has no analyses either

**gåre** failures:

* `gåre+V+Inf` => `går`
* `gåre` has no analyses either

**eksemplifisere** failures:

* `eksemplifisere+V+Inf` => `eksemplifiser`
* `eksemplifisere` has no analyses either

**duse** failures:

* `duse+V+Inf` => `dus`
* `duse` has following analyses:
  * `dus+A+Pl+Indef`
  * `dus+A+Sg+Def`

**våtte** failures:

* `våtte+V+Inf` => `vått`
* `våtte` has no analyses either

**justere** failures:

* `justere+V+Inf` => `juster`
* `justere` has no analyses either

**kalke** failures:

* `kalke+V+Inf` => `kalk`
* `kalke` has no analyses either

**stykkje** failures:

* `stykkje+V+Inf` => `stykk`
* `stykkje` has no analyses either

**typisere** failures:

* `typisere+V+Inf` => `typiser`
* `typisere` has no analyses either

**formolde** failures:

* `formolde+V+Inf` => `formold`
* `formolde` has no analyses either

**konse** failures:

* `konse+V+Inf` => `kons`
* `konse` has no analyses either

**femne** failures:

* `femne+V+Inf` does not generate!
* `femne` has no analyses either

**smile** failures:

* `smile+V+Inf` => `smil`
* `smile` has no analyses either

**behandle** failures:

* `behandle+V+Inf` => `behandl`
* `behandle` has no analyses either

**rutle** failures:

* `rutle+V+Inf` => `rutl`
* `rutle` has no analyses either

**kjekke** failures:

* `kjekke+V+Inf` => `kjekk`
* `kjekke` has no analyses either

**skarifisere** failures:

* `skarifisere+V+Inf` => `skarifiser`
* `skarifisere` has no analyses either

**pugge** failures:

* `pugge+V+Inf` => `pugg`
* `pugge` has no analyses either

**vride** failures:

* `vride+V+Inf` does not generate!
* `vride` has no analyses either

**forspørje** failures:

* `forspørje+V+Inf` does not generate!
* `forspørje` has no analyses either

**påvise** failures:

* `påvise+V+Inf` => `påvis`
* `påvise` has no analyses either

**allitterere** failures:

* `allitterere+V+Inf` => `allitterer`
* `allitterere` has no analyses either

**dehydrere** failures:

* `dehydrere+V+Inf` => `dehydrer`
* `dehydrere` has no analyses either

**tute** failures:

* `tute+V+Inf` => `tut`
* `tute` has following analyses:
  * `turve+V+Ind+Prt`

**snirkle** failures:

* `snirkle+V+Inf` => `snirkl`
* `snirkle` has no analyses either

**framvise** failures:

* `framvise+V+Inf` => `framvis`
* `framvise` has following analyses:
  * `framvis+A+A+Pl+Indef`
  * `framvis+A+A+Pl+Def`
  * `framvis+A+A+Sg+Def`

**pusse** failures:

* `pusse+V+Inf` => `puss`
* `pusse` has no analyses either

**lepe** failures:

* `lepe+V+Inf` => `lep`
* `lepe` has no analyses either

**forbetre** failures:

* `forbetre+V+Inf` => `forbetr`
* `forbetre` has no analyses either

**skremme** failures:

* `skremme+V+Inf` does not generate!
* `skremme` has no analyses either

**sverte** failures:

* `sverte+V+Inf` => `svert`
* `sverte` has no analyses either

**snubbe** failures:

* `snubbe+V+Inf` => `snubb`
* `snubbe` has no analyses either

**saldere** failures:

* `saldere+V+Inf` => `salder`
* `saldere` has no analyses either

**beke** failures:

* `beke+V+Inf` => `bek`
* `beke` has no analyses either

**abonnere** failures:

* `abonnere+V+Inf` => `abonner`
* `abonnere` has no analyses either

**negle** failures:

* `negle+V+Inf` does not generate!
* `negle` has no analyses either

**senge** failures:

* `senge+V+Inf` => `seng`
* `senge` has no analyses either

**avdekkje** failures:

* `avdekkje+V+Inf` does not generate!
* `avdekkje` has no analyses either

**bine** failures:

* `bine+V+Inf` => `bin`
* `bine` has following analyses:
  * `bide+V+Adj+Fl`
  * `bide+V+Adj+Bu+Eint`
  * `bite+V+Adj+Fl`
  * `bite+V+Adj+Bu+Eint`

**desorganisere** failures:

* `desorganisere+V+Inf` => `desorganiser`
* `desorganisere` has no analyses either

**handtakast** failures:

* `handtakast+V+Inf` => `handtaast`
* `handtakast` has no analyses either

**renovere** failures:

* `renovere+V+Inf` => `renover`
* `renovere` has no analyses either

**brette** failures:

* `brette+V+Inf` does not generate!
* `brette` has no analyses either

**sinkle** failures:

* `sinkle+V+Inf` => `sinkl`
* `sinkle` has no analyses either

**frøye** failures:

* `frøye+V+Inf` does not generate!
* `frøye` has following analyses:
  * `frøyen+A+Neu+Sg+Indef`

**visualisere** failures:

* `visualisere+V+Inf` => `visualiser`
* `visualisere` has no analyses either

**laksere** failures:

* `laksere+V+Inf` => `lakser`
* `laksere` has no analyses either

**bønhøyre** failures:

* `bønhøyre+V+Inf` does not generate!
* `bønhøyre` has no analyses either

**umake** failures:

* `umake+V+Inf` => `umak`
* `umake` has following analyses:
  * `umake+A+A+Msc+Sg+Indef`
  * `umake+A+A+Fem+Sg+Indef`
  * `umake+A+A+Neu+Sg+Indef`

**rulte** failures:

* `rulte+V+Inf` => `rult`
* `rulte` has following analyses:
  * `rulten+A+Neu+Sg+Indef`

**overkome** failures:

* `overkome+V+Inf` => `overkkoma`
* `overkome+V+Inf` => `overkkome`
* `overkome` has no analyses either

**farborde** failures:

* `farborde+V+Inf` => `farbord`
* `farborde` has no analyses either

**hyke** failures:

* `hyke+V+Inf` => `hyk`
* `hyke` has following analyses:
  * `hyken+A+Neu+Sg+Indef`

**frådømme** failures:

* `frådømme+V+Inf` does not generate!
* `frådømme` has no analyses either

**gjødsle** failures:

* `gjødsle+V+Inf` => `gjødsl`
* `gjødsle` has no analyses either

**sjaue** failures:

* `sjaue+V+Inf` => `sjau`
* `sjaue` has no analyses either

**node** failures:

* `node+V+Inf` does not generate!
* `node` has no analyses either

**loggføre** failures:

* `loggføre+V+Inf` => `loggfør`
* `loggføre` has no analyses either

**korrigere** failures:

* `korrigere+V+Inf` => `korriger`
* `korrigere` has no analyses either

**kommatere** failures:

* `kommatere+V+Inf` => `kommater`
* `kommatere` has no analyses either

**måte** failures:

* `måte+V+Inf` => `måt`
* `måte` has following analyses:
  * `måte+A+A+Msc+Sg+Indef`
  * `måte+A+A+Fem+Sg+Indef`
  * `måte+A+A+Neu+Sg+Indef`

**krykke** failures:

* `krykke+V+Inf` does not generate!
* `krykke` has no analyses either

**kollektivisere** failures:

* `kollektivisere+V+Inf` => `kollektiviser`
* `kollektivisere` has no analyses either

**sundre** failures:

* `sundre+V+Inf` => `sundr`
* `sundre` has no analyses either

**alarmere** failures:

* `alarmere+V+Inf` => `alarmer`
* `alarmere` has no analyses either

**prøve** failures:

* `prøve+V+Inf` does not generate!
* `prøve` has no analyses either

**breiside** failures:

* `breiside+V+Inf` => `breisid`
* `breiside` has no analyses either

**høgne** failures:

* `høgne+V+Inf` => `høgn`
* `høgne` has no analyses either

**froste** failures:

* `froste+V+Inf` => `frost`
* `froste` has no analyses either

**straighte** failures:

* `straighte+V+Inf` => `straight`
* `straighte` has following analyses:
  * `straight+A+A+Pl+Def`
  * `straight+A+A+Sg+Def`
  * `straight+A+A+Pl+Indef`

**flekse** failures:

* `flekse+V+Inf` => `fleks`
* `flekse` has no analyses either

**fubbe** failures:

* `fubbe+V+Inf` => `fubb`
* `fubbe` has no analyses either

**manipulere** failures:

* `manipulere+V+Inf` => `manipuler`
* `manipulere` has no analyses either

**bytelåne** failures:

* `bytelåne+V+Inf` => `bytelån`
* `bytelåne` has no analyses either

**sveive** failures:

* `sveive+V+Inf` does not generate!
* `sveive` has no analyses either

**piffe** failures:

* `piffe+V+Inf` => `piff`
* `piffe` has no analyses either

**lykkynskje** failures:

* `lykkynskje+V+Inf` does not generate!
* `lykkynskje` has no analyses either

**emne** failures:

* `emne+V+Inf` => `emn`
* `emne` has following analyses:
  * `emmen+A+Pl+Indef`
  * `emmen+A+Sg+Def`

**leggjast** failures:

* `leggjast+V+Inf` => `lejast`
* `leggjast` has no analyses either

**prokrastinere** failures:

* `prokrastinere+V+Inf` => `prokrastiner`
* `prokrastinere` has no analyses either

**bute** failures:

* `bute+V+Inf` => `but`
* `bute` has no analyses either

**komplementere** failures:

* `komplementere+V+Inf` => `komplementer`
* `komplementere` has no analyses either

**sløre** failures:

* `sløre+V+Inf` does not generate!
* `sløre` has no analyses either

**konke** failures:

* `konke+V+Inf` => `konk`
* `konke` has following analyses:
  * `konk+A+A+Sg+Def`
  * `konk+A+A+Pl+Indef`
  * `konk+A+A+Pl+Def`

**løpe** failures:

* `løpe+V+Inf` => `løp`
* `løpe` has no analyses either

**nullstille** failures:

* `nullstille+V+Inf` => `nullstile`
* `nullstille+V+Inf` => `nullstila`
* `nullstille` has no analyses either

**rekruttere** failures:

* `rekruttere+V+Inf` => `rekrutter`
* `rekruttere` has no analyses either

**slumpe** failures:

* `slumpe+V+Inf` => `slump`
* `slumpe` has no analyses either

**tuse** failures:

* `tuse+V+Inf` => `tus`
* `tuse` has no analyses either

**utstaffere** failures:

* `utstaffere+V+Inf` => `utstaffer`
* `utstaffere` has no analyses either

**fjasle** failures:

* `fjasle+V+Inf` => `fjasl`
* `fjasle` has no analyses either

**fime** failures:

* `fime+V+Inf` => `fim`
* `fime` has following analyses:
  * `fim+A+Pl+Indef`
  * `fim+A+Sg+Def`

**coache** failures:

* `coache+V+Inf` => `coach`
* `coache` has no analyses either

**ulykkestrygde** failures:

* `ulykkestrygde+V+Inf` => `ulykkestrygd`
* `ulykkestrygde` has no analyses either

**tabulere** failures:

* `tabulere+V+Inf` => `tabuler`
* `tabulere` has no analyses either

**garpe** failures:

* `garpe+V+Inf` => `garp`
* `garpe` has no analyses either

**bonitere** failures:

* `bonitere+V+Inf` => `boniter`
* `bonitere` has no analyses either

**deprivatisere** failures:

* `deprivatisere+V+Inf` => `deprivatiser`
* `deprivatisere` has no analyses either

**reime** failures:

* `reime+V+Inf` => `reim`
* `reime` has no analyses either

**satirisere** failures:

* `satirisere+V+Inf` => `satiriser`
* `satirisere` has no analyses either

**gigre** failures:

* `gigre+V+Inf` => `gigr`
* `gigre` has no analyses either

**avkrefte** failures:

* `avkrefte+V+Inf` => `avkreft`
* `avkrefte` has no analyses either

**trinse** failures:

* `trinse+V+Inf` => `trins`
* `trinse` has no analyses either

**hyle** failures:

* `hyle+V+Inf` => `hyl`
* `hyle` has no analyses either

**kvalpe** failures:

* `kvalpe+V+Inf` => `kvalp`
* `kvalpe` has no analyses either

**tøtsje** failures:

* `tøtsje+V+Inf` => `tøtsj`
* `tøtsje` has no analyses either

**snøbake** failures:

* `snøbake+V+Inf` does not generate!
* `snøbake` has no analyses either

**eire** failures:

* `eire+V+Inf` => `eir`
* `eire` has no analyses either

**seipe** failures:

* `seipe+V+Inf` => `seip`
* `seipe` has no analyses either

**sevjeberke** failures:

* `sevjeberke+V+Inf` => `sevjeberk`
* `sevjeberke` has no analyses either

**dise** failures:

* `dise+V+Inf` => `dis`
* `dise` has following analyses:
  * `dis+A+A+Sg+Def`
  * `dis+A+A+Pl+Def`
  * `dis+A+A+Pl+Indef`

**stekke** failures:

* `stekke+V+Inf` does not generate!
* `stekke` has no analyses either

**gjevast** failures:

* `gjevast+V+Inf` => `ggjevast`
* `gjevast` has no analyses either

**vadre** failures:

* `vadre+V+Inf` => `vadr`
* `vadre` has no analyses either

**oppmode** failures:

* `oppmode+V+Inf` => `oppmod`
* `oppmode` has no analyses either

**entre** failures:

* `entre+V+Inf` => `entr`
* `entre` has following analyses:
  * `entre+N+N+Msc+Sg+Indef`

**bleggje** failures:

* `bleggje+V+Inf` does not generate!
* `bleggje` has no analyses either

**klamre** failures:

* `klamre+V+Inf` => `klamr`
* `klamre` has no analyses either

**pule** failures:

* `pule+V+Inf` => `pul`
* `pule` has no analyses either

**rubrisere** failures:

* `rubrisere+V+Inf` => `rubriser`
* `rubrisere` has no analyses either

**interessere** failures:

* `interessere+V+Inf` => `interesser`
* `interessere` has no analyses either

**dumle** failures:

* `dumle+V+Inf` => `duml`
* `dumle` has following analyses:
  * `dumlen+A+Neu+Sg+Indef`

**tamse** failures:

* `tamse+V+Inf` => `tams`
* `tamse` has no analyses either

**pusle** failures:

* `pusle+V+Inf` => `pusl`
* `pusle` has no analyses either

**sprenge** failures:

* `sprenge+V+Inf` => `spreng`
* `sprenge` has no analyses either

**skrykkje** failures:

* `skrykkje+V+Inf` does not generate!
* `skrykkje` has no analyses either

**pulsere** failures:

* `pulsere+V+Inf` => `pulser`
* `pulsere` has no analyses either

**prosentlønne** failures:

* `prosentlønne+V+Inf` does not generate!
* `prosentlønne` has no analyses either

**nykke** failures:

* `nykke+V+Inf` does not generate!
* `nykke` has no analyses either

**turrprate** failures:

* `turrprate+V+Inf` => `turrprat`
* `turrprate` has no analyses either

**tæse** failures:

* `tæse+V+Inf` => `tæs`
* `tæse` has no analyses either

**kjære** failures:

* `kjære+V+Inf` => `kjær`
* `kjære` has following analyses:
  * `kjær+A+Pl+Indef`
  * `kjære+N+N+Sg`
  * `kjær+A+Sg+Def`

**tine** failures:

* `tine+V+Inf` => `tin`
* `tine` has following analyses:
  * `tien+A+Pl+Indef`
  * `tien+A+Sg+Def`

**svolke** failures:

* `svolke+V+Inf` => `svolk`
* `svolke` has no analyses either

**innbyde** failures:

* `innbyde+V+Inf` does not generate!
* `innbyde` has following analyses:
  * `innbydd+A+A+Pl+Indef`
  * `innbydd+A+A+Pl+Def`
  * `innbydd+A+A+Sg+Def`

**passe** failures:

* `passe+V+Inf` => `pass`
* `passe` has following analyses:
  * `passe+A+A+Neu+Sg+Indef`
  * `passe+A+A+Msc+Sg+Indef`
  * `passe+A+A+Fem+Sg+Indef`

**titulere** failures:

* `titulere+V+Inf` => `tituler`
* `titulere` has no analyses either

**fete** failures:

* `fete+V+Inf` => `fet`
* `fete` has no analyses either

**valde** failures:

* `valde+V+Inf` => `vald`
* `valde` has no analyses either

**fargelegge** failures:

* `fargelegge+V+Inf` does not generate!
* `fargelegge` has no analyses either

**nemne** failures:

* `nemne+V+Inf` does not generate!
* `nemne` has no analyses either

**stasjonere** failures:

* `stasjonere+V+Inf` => `stasjoner`
* `stasjonere` has no analyses either

**skamstele** failures:

* `skamstele+V+Inf` => `skamstea`
* `skamstele+V+Inf` => `skamstee`
* `skamstele` has no analyses either

**skolere** failures:

* `skolere+V+Inf` => `skoler`
* `skolere` has no analyses either

**evaluere** failures:

* `evaluere+V+Inf` => `evaluer`
* `evaluere` has no analyses either

**djerve** failures:

* `djerve+V+Inf` => `djerv`
* `djerve` has following analyses:
  * `djerv+A+Pl+Indef`
  * `djerv+A+Sg+Def`

**drunse** failures:

* `drunse+V+Inf` => `druns`
* `drunse` has no analyses either

**forseinke** failures:

* `forseinke+V+Inf` => `forseink`
* `forseinke` has no analyses either

**smelle** failures:

* `smelle+V+Inf` does not generate!
* `smelle` has no analyses either

**sleike** failures:

* `sleike+V+Inf` => `sleik`
* `sleike` has no analyses either

**dehumanisere** failures:

* `dehumanisere+V+Inf` => `dehumaniser`
* `dehumanisere` has no analyses either

**hykle** failures:

* `hykle+V+Inf` => `hykl`
* `hykle` has no analyses either

**vrikle** failures:

* `vrikle+V+Inf` => `vrikl`
* `vrikle` has following analyses:
  * `vrikle+N+N+Neu+Sg+Indef`
  * `vrikle+N+N+Neu+Pl+Indef`

**feikte** failures:

* `feikte+V+Inf` => `feikt`
* `feikte` has following analyses:
  * `feike+V+Adj+Fl`
  * `feike+V+Adj+Bu+Eint`
  * `feike+V+Ind+Prt`

**emaljere** failures:

* `emaljere+V+Inf` => `emaljer`
* `emaljere` has no analyses either

**forvitre** failures:

* `forvitre+V+Inf` => `forvitr`
* `forvitre` has no analyses either

**skjemme** failures:

* `skjemme+V+Inf` does not generate!
* `skjemme` has no analyses either

**klimatisere** failures:

* `klimatisere+V+Inf` => `klimatiser`
* `klimatisere` has no analyses either

**hauke** failures:

* `hauke+V+Inf` => `hauk`
* `hauke` has no analyses either

**save** failures:

* `save+V+Inf` => `sav`
* `save` has no analyses either

**gjenopprette** failures:

* `gjenopprette+V+Inf` => `gjenopprett`
* `gjenopprette` has no analyses either

**dable** failures:

* `dable+V+Inf` => `dabl`
* `dable` has no analyses either

**snippe** failures:

* `snippe+V+Inf` => `snipp`
* `snippe` has no analyses either

**kompromisse** failures:

* `kompromisse+V+Inf` => `kompromiss`
* `kompromisse` has no analyses either

**deplassere** failures:

* `deplassere+V+Inf` => `deplasser`
* `deplassere` has no analyses either

**alfabetisere** failures:

* `alfabetisere+V+Inf` => `alfabetiser`
* `alfabetisere` has no analyses either

**gruppevaldtake** failures:

* `gruppevaldtake+V+Inf` => `gruppevaldttake`
* `gruppevaldtake+V+Inf` => `gruppevaldttaka`
* `gruppevaldtake` has no analyses either

**dobbelsjekke** failures:

* `dobbelsjekke+V+Inf` => `dobbelsjekk`
* `dobbelsjekke` has no analyses either

**misunne** failures:

* `misunne+V+Inf` => `misunn`
* `misunne` has no analyses either

**presisere** failures:

* `presisere+V+Inf` => `presiser`
* `presisere` has no analyses either

**kampere** failures:

* `kampere+V+Inf` => `kamper`
* `kampere` has no analyses either

**avskoge** failures:

* `avskoge+V+Inf` => `avskog`
* `avskoge` has no analyses either

**forfølgje** failures:

* `forfølgje+V+Inf` does not generate!
* `forfølgje` has no analyses either

**mannast** failures:

* `mannast+V+Inf` => `mannastast`
* `mannast` has no analyses either

**vakle** failures:

* `vakle+V+Inf` => `vakl`
* `vakle` has no analyses either

**stæle** failures:

* `stæle+V+Inf` => `stæl`
* `stæle` has no analyses either

**intonere** failures:

* `intonere+V+Inf` => `intoner`
* `intonere` has no analyses either

**nilese** failures:

* `nilese+V+Inf` => `nilea`
* `nilese+V+Inf` => `nilee`
* `nilese` has no analyses either

**hipe** failures:

* `hipe+V+Inf` => `hip`
* `hipe` has following analyses:
  * `hipp+A+A+Sg+Def`
  * `hipen+A+Neu+Sg+Indef`
  * `hipp+A+A+Pl+Indef`
  * `hippen+A+Neu+Sg+Indef`
  * `hipp+A+A+Pl+Def`

**signe** failures:

* `signe+V+Inf` => `sign`
* `signe` has following analyses:
  * `sigen+A+Sg+Def`
  * `sigen+A+Pl+Indef`

**bøyke** failures:

* `bøyke+V+Inf` => `bøyk`
* `bøyke` has no analyses either

**samle** failures:

* `samle+V+Inf` => `saml`
* `samle` has no analyses either

**samringje** failures:

* `samringje+V+Inf` does not generate!
* `samringje` has no analyses either

**streife** failures:

* `streife+V+Inf` => `streif`
* `streife` has no analyses either

**gnike** failures:

* `gnike+V+Inf` => `gnik`
* `gnike` has no analyses either

**forvente** failures:

* `forvente+V+Inf` => `forvent`
* `forvente` has following analyses:
  * `forvent+A+A+Sg+Def`
  * `forvent+A+A+Pl+Def`
  * `forvent+A+A+Pl+Indef`

**svartne** failures:

* `svartne+V+Inf` => `svartn`
* `svartne` has no analyses either

**vindturke** failures:

* `vindturke+V+Inf` => `vindturk`
* `vindturke` has no analyses either

**klapre** failures:

* `klapre+V+Inf` => `klapr`
* `klapre` has no analyses either

**maktast** failures:

* `maktast+V+Inf` => `maktastast`
* `maktast` has no analyses either

**kore** failures:

* `kore+V+Inf` => `kor`
* `kore` has no analyses either

**luve** failures:

* `luve+V+Inf` does not generate!
* `luve` has no analyses either

**føreprosjektere** failures:

* `føreprosjektere+V+Inf` => `føreprosjekter`
* `føreprosjektere` has no analyses either

**isenesetje** failures:

* `isenesetje+V+Inf` does not generate!
* `isenesetje` has no analyses either

**vrinske** failures:

* `vrinske+V+Inf` => `vrinsk`
* `vrinske` has following analyses:
  * `vrinsk+A+A+Pl+Indef`
  * `vrinsk+A+A+Sg+Def`
  * `vrinsk+A+A+Pl+Def`

**vinterfore** failures:

* `vinterfore+V+Inf` => `vinterfor`
* `vinterfore` has no analyses either

**kopulere** failures:

* `kopulere+V+Inf` => `kopuler`
* `kopulere` has no analyses either

**samlast** failures:

* `samlast+V+Inf` => `samlastast`
* `samlast` has following analyses:
  * `samlast+N+N+Fem+Sg+Indef`
  * `samlast+N+N+Msc+Sg+Indef`

**tørkast** failures:

* `tørkast+V+Inf` => `tørkastast`
* `tørkast` has no analyses either

**skvakle** failures:

* `skvakle+V+Inf` => `skvakl`
* `skvakle` has no analyses either

**filialisere** failures:

* `filialisere+V+Inf` => `filialiser`
* `filialisere` has no analyses either

**valle** failures:

* `valle+V+Inf` => `vall`
* `valle` has no analyses either

**træle** failures:

* `træle+V+Inf` => `træl`
* `træle` has following analyses:
  * `trælen+A+Neu+Sg+Indef`

**tørrbanke** failures:

* `tørrbanke+V+Inf` => `tørrbank`
* `tørrbanke` has no analyses either

**demontere** failures:

* `demontere+V+Inf` => `demonter`
* `demontere` has no analyses either

**planere** failures:

* `planere+V+Inf` => `planer`
* `planere` has no analyses either

**pingse** failures:

* `pingse+V+Inf` => `pings`
* `pingse` has no analyses either

**ekstemporere** failures:

* `ekstemporere+V+Inf` => `ekstemporer`
* `ekstemporere` has no analyses either

**naudslakte** failures:

* `naudslakte+V+Inf` => `naudslakt`
* `naudslakte` has no analyses either

**date** failures:

* `date+V+Inf` => `dat`
* `date` has no analyses either

**sørgje** failures:

* `sørgje+V+Inf` does not generate!
* `sørgje` has no analyses either

**tigge** failures:

* `tigge+V+Inf` does not generate!
* `tigge` has no analyses either

**strimle** failures:

* `strimle+V+Inf` => `striml`
* `strimle` has no analyses either

**autentisere** failures:

* `autentisere+V+Inf` => `autentiser`
* `autentisere` has no analyses either

**absoluttere** failures:

* `absoluttere+V+Inf` => `absolutter`
* `absoluttere` has no analyses either

**varmhamre** failures:

* `varmhamre+V+Inf` => `varmhamr`
* `varmhamre` has no analyses either

**trollstille** failures:

* `trollstille+V+Inf` does not generate!
* `trollstille` has no analyses either

**flagne** failures:

* `flagne+V+Inf` => `flagn`
* `flagne` has no analyses either

**verme** failures:

* `verme+V+Inf` does not generate!
* `verme` has no analyses either

**forferde** failures:

* `forferde+V+Inf` => `forferd`
* `forferde` has no analyses either

**tise** failures:

* `tise+V+Inf` => `tis`
* `tise` has no analyses either

**pollinere** failures:

* `pollinere+V+Inf` => `polliner`
* `pollinere` has no analyses either

**stupne** failures:

* `stupne+V+Inf` => `stupn`
* `stupne` has no analyses either

**dosme** failures:

* `dosme+V+Inf` => `dosm`
* `dosme` has following analyses:
  * `dosmen+A+Neu+Sg+Indef`

**fundere** failures:

* `fundere+V+Inf` => `funder`
* `fundere` has no analyses either

**kynne** failures:

* `kynne+V+Inf` does not generate!
* `kynne` has no analyses either

**snure** failures:

* `snure+V+Inf` => `snur`
* `snure` has following analyses:
  * `snurren+A+Neu+Sg+Indef`

**innvarsle** failures:

* `innvarsle+V+Inf` => `innvarsl`
* `innvarsle` has no analyses either

**speke** failures:

* `speke+V+Inf` => `spek`
* `speke` has following analyses:
  * `speken+A+Neu+Sg+Indef`

**brese** failures:

* `brese+V+Inf` => `bres`
* `brese` has no analyses either

**ulukkeforsikre** failures:

* `ulukkeforsikre+V+Inf` => `ulukkeforsikr`
* `ulukkeforsikre` has no analyses either

**glyrne** failures:

* `glyrne+V+Inf` => `glyrn`
* `glyrne` has no analyses either

**fortryte** failures:

* `fortryte+V+Inf` => `fortrya`
* `fortryte+V+Inf` => `fortrye`
* `fortryte` has no analyses either

**forhøgje** failures:

* `forhøgje+V+Inf` does not generate!
* `forhøgje` has no analyses either

**kollabere** failures:

* `kollabere+V+Inf` => `kollaber`
* `kollabere` has no analyses either

**forfatte** failures:

* `forfatte+V+Inf` => `forfatt`
* `forfatte` has no analyses either

**parfymere** failures:

* `parfymere+V+Inf` => `parfymer`
* `parfymere` has no analyses either

**skarke** failures:

* `skarke+V+Inf` => `skark`
* `skarke` has no analyses either

**skorpe** failures:

* `skorpe+V+Inf` => `skorp`
* `skorpe` has no analyses either

**gjennomsøke** failures:

* `gjennomsøke+V+Inf` => `gjennomsøk`
* `gjennomsøke` has no analyses either

**sjuge** failures:

* `sjuge+V+Inf` => `sjug`
* `sjuge` has no analyses either

**elge** failures:

* `elge+V+Inf` => `elg`
* `elge` has no analyses either

**skrible** failures:

* `skrible+V+Inf` => `skribl`
* `skrible` has no analyses either

**vrøvle** failures:

* `vrøvle+V+Inf` => `vrøvl`
* `vrøvle` has no analyses either

**agge** failures:

* `agge+V+Inf` => `agg`
* `agge` has no analyses either

**ulykkesforsikre** failures:

* `ulykkesforsikre+V+Inf` => `ulykkesforsikr`
* `ulykkesforsikre` has no analyses either

**klykkje** failures:

* `klykkje+V+Inf` does not generate!
* `klykkje` has no analyses either

**flire** failures:

* `flire+V+Inf` does not generate!
* `flire` has no analyses either

**spekke** failures:

* `spekke+V+Inf` => `spekk`
* `spekke` has no analyses either

**flektere** failures:

* `flektere+V+Inf` => `flekter`
* `flektere` has no analyses either

**hanskast** failures:

* `hanskast+V+Inf` => `hanskastast`
* `hanskast` has no analyses either

**vekkje** failures:

* `vekkje+V+Inf` does not generate!
* `vekkje` has no analyses either

**puffe** failures:

* `puffe+V+Inf` => `puff`
* `puffe` has no analyses either

**hemse** failures:

* `hemse+V+Inf` => `hems`
* `hemse` has no analyses either

**rekkje** failures:

* `rekkje+V+Inf` does not generate!
* `rekkje` has no analyses either

**strande** failures:

* `strande+V+Inf` => `strand`
* `strande` has no analyses either

**singre** failures:

* `singre+V+Inf` => `singr`
* `singre` has no analyses either

**tilsette** failures:

* `tilsette+V+Inf` does not generate!
* `tilsette` has following analyses:
  * `tilsett+A+A+Pl+Indef`
  * `tilsett+A+A+Pl+Def`
  * `tilsett+A+A+Sg+Def`

**erobre** failures:

* `erobre+V+Inf` => `erobr`
* `erobre` has no analyses either

**deportere** failures:

* `deportere+V+Inf` => `deporter`
* `deportere` has no analyses either

**cashe** failures:

* `cashe+V+Inf` => `cash`
* `cashe` has following analyses:
  * `cash-and-carry+A+A+Sg+Def`
  * `cash+A+A+Pl+Indef`
  * `cash-and-carry+A+A+Pl+Def`
  * `cash+A+A+Pl+Def`
  * `cash-and-carry+A+A+Pl+Indef`
  * `cash+A+A+Sg+Def`

**prelle** failures:

* `prelle+V+Inf` => `prell`
* `prelle` has no analyses either

**undrast** failures:

* `undrast+V+Inf` => `undrastast`
* `undrast` has no analyses either

**skade** failures:

* `skade+V+Inf` does not generate!
* `skade` has no analyses either

**forgløyme** failures:

* `forgløyme+V+Inf` does not generate!
* `forgløyme` has no analyses either

**dømme** failures:

* `dømme+V+Inf` does not generate!
* `dømme` has no analyses either

**koreografere** failures:

* `koreografere+V+Inf` => `koreografer`
* `koreografere` has no analyses either

**proviantere** failures:

* `proviantere+V+Inf` => `provianter`
* `proviantere` has no analyses either

**saksøke** failures:

* `saksøke+V+Inf` => `saksøk`
* `saksøke` has no analyses either

**same** failures:

* `same+V+Inf` => `sam`
* `same` has no analyses either

**forundre** failures:

* `forundre+V+Inf` => `forundr`
* `forundre` has no analyses either

**dente** failures:

* `dente+V+Inf` => `dent`
* `dente` has no analyses either

**pakte** failures:

* `pakte+V+Inf` => `pakt`
* `pakte` has no analyses either

**snede** failures:

* `snede+V+Inf` => `sned`
* `snede` has following analyses:
  * `snedden+A+Neu+Sg+Indef`

**våge** failures:

* `våge+V+Inf` does not generate!
* `våge` has no analyses either

**sokne** failures:

* `sokne+V+Inf` => `sokn`
* `sokne` has no analyses either

**lode** failures:

* `lode+V+Inf` does not generate!
* `lode` has following analyses:
  * `loden+A+Neu+Sg+Indef`

**adjektivere** failures:

* `adjektivere+V+Inf` => `adjektiver`
* `adjektivere` has no analyses either

**montere** failures:

* `montere+V+Inf` => `monter`
* `montere` has no analyses either

**djervast** failures:

* `djervast+V+Inf` => `djervastast`
* `djervast` has following analyses:
  * `djerv+A+Superl+Indef`

**rapportere** failures:

* `rapportere+V+Inf` => `rapporter`
* `rapportere` has no analyses either

**kingse** failures:

* `kingse+V+Inf` => `kings`
* `kingse` has no analyses either

**konvertere** failures:

* `konvertere+V+Inf` => `konverter`
* `konvertere` has no analyses either

**kverrsetje** failures:

* `kverrsetje+V+Inf` does not generate!
* `kverrsetje` has no analyses either

**drikke** failures:

* `drikke+V+Inf` => `drie`
* `drikke+V+Inf` => `dria`
* `drikke` has no analyses either

**deige** failures:

* `deige+V+Inf` => `deig`
* `deige` has following analyses:
  * `deigen+A+Neu+Sg+Indef`

**gnitre** failures:

* `gnitre+V+Inf` => `gnitr`
* `gnitre` has no analyses either

**skipe** failures:

* `skipe+V+Inf` => `skip`
* `skipe` has no analyses either

**slåpe** failures:

* `slåpe+V+Inf` => `slåp`
* `slåpe` has no analyses either

**doggfalle** failures:

* `doggfalle+V+Inf` => `doggfaa`
* `doggfalle+V+Inf` => `doggfae`
* `doggfalle` has no analyses either

**fingle** failures:

* `fingle+V+Inf` => `fingl`
* `fingle` has no analyses either

**skolregne** failures:

* `skolregne+V+Inf` => `skolregn`
* `skolregne` has no analyses either

**karde** failures:

* `karde+V+Inf` => `kard`
* `karde` has no analyses either

**tenne** failures:

* `tenne+V+Inf` => `ten`
* `tenne` has no analyses either

**skokle** failures:

* `skokle+V+Inf` => `skokl`
* `skokle` has no analyses either

**ausregne** failures:

* `ausregne+V+Inf` does not generate!
* `ausregne` has no analyses either

**passiare** failures:

* `passiare+V+Inf` => `passiar`
* `passiare` has no analyses either

**forgape** failures:

* `forgape+V+Inf` => `forgap`
* `forgape` has no analyses either

**blodvekke** failures:

* `blodvekke+V+Inf` does not generate!
* `blodvekke` has no analyses either

**underbetale** failures:

* `underbetale+V+Inf` does not generate!
* `underbetale` has no analyses either

**augefeste** failures:

* `augefeste+V+Inf` does not generate!
* `augefeste` has no analyses either

**skjersette** failures:

* `skjersette+V+Inf` does not generate!
* `skjersette` has no analyses either

**overkomme** failures:

* `overkomme+V+Inf` => `overkkomma`
* `overkomme+V+Inf` => `overkkomme`
* `overkomme` has no analyses either

**geigle** failures:

* `geigle+V+Inf` => `geigl`
* `geigle` has no analyses either

**hetse** failures:

* `hetse+V+Inf` => `hets`
* `hetse` has no analyses either

**uffe** failures:

* `uffe+V+Inf` => `uff`
* `uffe` has no analyses either

**munnhoggast** failures:

* `munnhoggast+V+Inf` => `munnhhoggast`
* `munnhoggast` has no analyses either

**ordlegge** failures:

* `ordlegge+V+Inf` does not generate!
* `ordlegge` has no analyses either

**yrve** failures:

* `yrve+V+Inf` => `yrv`
* `yrve` has no analyses either

**snudde** failures:

* `snudde+V+Inf` => `snudd`
* `snudde` has following analyses:
  * `snu+V+Ind+Prt`

**slarke** failures:

* `slarke+V+Inf` => `slark`
* `slarke` has no analyses either

**generalisere** failures:

* `generalisere+V+Inf` => `generaliser`
* `generalisere` has no analyses either

**tilfredsstille** failures:

* `tilfredsstille+V+Inf` does not generate!
* `tilfredsstille` has no analyses either

**uge** failures:

* `uge+V+Inf` => `ug`
* `uge` has following analyses:
  * `uggen+A+Neu+Sg+Indef`

**kuve** failures:

* `kuve+V+Inf` => `kuv`
* `kuve` has following analyses:
  * `kuven+A+Neu+Sg+Indef`

**trygle** failures:

* `trygle+V+Inf` => `trygl`
* `trygle` has no analyses either

**underselje** failures:

* `underselje+V+Inf` => `undersel`
* `underselje` has no analyses either

**nedkjempe** failures:

* `nedkjempe+V+Inf` => `nedkjemp`
* `nedkjempe` has no analyses either

**hugfeste** failures:

* `hugfeste+V+Inf` does not generate!
* `hugfeste` has no analyses either

**nibe** failures:

* `nibe+V+Inf` => `nibbe`
* `nibe` has following analyses:
  * `nibede+V+Ind+Prs`
  * `nibede+V+Imprt`

**daudne** failures:

* `daudne+V+Inf` => `daudn`
* `daudne` has no analyses either

**røyre** failures:

* `røyre+V+Inf` => `røyr`
* `røyre` has no analyses either

**prosentlikne** failures:

* `prosentlikne+V+Inf` => `prosentlikn`
* `prosentlikne` has no analyses either

**bygge** failures:

* `bygge+V+Inf` does not generate!
* `bygge` has no analyses either

**tvære** failures:

* `tvære+V+Inf` => `tvær`
* `tvære` has no analyses either

**kikje** failures:

* `kikje+V+Inf` does not generate!
* `kikje` has no analyses either

**kløvkøyre** failures:

* `kløvkøyre+V+Inf` does not generate!
* `kløvkøyre` has no analyses either

**oppdage** failures:

* `oppdage+V+Inf` => `oppdag`
* `oppdage` has no analyses either

**kage** failures:

* `kage+V+Inf` => `kag`
* `kage` has no analyses either

**råskale** failures:

* `råskale+V+Inf` => `råskal`
* `råskale` has no analyses either

**puldre** failures:

* `puldre+V+Inf` => `puldr`
* `puldre` has no analyses either

**størne** failures:

* `størne+V+Inf` => `størn`
* `størne` has no analyses either

**trase** failures:

* `trase+V+Inf` => `tras`
* `trase` has following analyses:
  * `trasen+A+Neu+Sg+Indef`

**sveve** failures:

* `sveve+V+Inf` does not generate!
* `sveve` has no analyses either

**beverte** failures:

* `beverte+V+Inf` => `bevert`
* `beverte` has no analyses either

**forsynde** failures:

* `forsynde+V+Inf` => `forsynd`
* `forsynde` has no analyses either

**turrtenke** failures:

* `turrtenke+V+Inf` => `turrtenk`
* `turrtenke` has no analyses either

**voffe** failures:

* `voffe+V+Inf` => `voff`
* `voffe` has no analyses either

**brengje** failures:

* `brengje+V+Inf` does not generate!
* `brengje` has no analyses either

**spæke** failures:

* `spæke+V+Inf` => `spæk`
* `spæke` has no analyses either

**koltre** failures:

* `koltre+V+Inf` => `koltr`
* `koltre` has no analyses either

**gaume** failures:

* `gaume+V+Inf` => `gaum`
* `gaume` has no analyses either

**avlevere** failures:

* `avlevere+V+Inf` => `avlever`
* `avlevere` has no analyses either

**bøtelegge** failures:

* `bøtelegge+V+Inf` does not generate!
* `bøtelegge` has no analyses either

**turrsymje** failures:

* `turrsymje+V+Inf` does not generate!
* `turrsymje` has no analyses either

**nyve** failures:

* `nyve+V+Inf` does not generate!
* `nyve` has following analyses:
  * `nyven+A+Neu+Sg+Indef`

**tyrme** failures:

* `tyrme+V+Inf` => `tyrm`
* `tyrme` has no analyses either

**avskilte** failures:

* `avskilte+V+Inf` => `avskilt`
* `avskilte` has no analyses either

**herke** failures:

* `herke+V+Inf` => `herk`
* `herke` has no analyses either

**jazze** failures:

* `jazze+V+Inf` => `jazz`
* `jazze` has no analyses either

**tygge** failures:

* `tygge+V+Inf` => `tye`
* `tygge+V+Inf` => `tya`
* `tygge` has no analyses either

**bedekke** failures:

* `bedekke+V+Inf` does not generate!
* `bedekke` has no analyses either

**tvangsevakuere** failures:

* `tvangsevakuere+V+Inf` => `tvangsevakuer`
* `tvangsevakuere` has no analyses either

**skryfte** failures:

* `skryfte+V+Inf` => `skryft`
* `skryfte` has no analyses either

**æreskjelle** failures:

* `æreskjelle+V+Inf` does not generate!
* `æreskjelle` has no analyses either

**daude** failures:

* `daude+V+Inf` => `daud`
* `daude` has following analyses:
  * `daud+A+Pl+Indef`
  * `daud+A+Sg+Def`

**vanheidre** failures:

* `vanheidre+V+Inf` => `vanheidr`
* `vanheidre` has no analyses either

**kladde** failures:

* `kladde+V+Inf` => `kladd`
* `kladde` has no analyses either

**emje** failures:

* `emje+V+Inf` => `emj`
* `emje` has no analyses either

**dechiffrere** failures:

* `dechiffrere+V+Inf` => `dechiffrer`
* `dechiffrere` has no analyses either

**bjore** failures:

* `bjore+V+Inf` => `bjor`
* `bjore` has no analyses either

**elte** failures:

* `elte+V+Inf` does not generate!
* `elte` has no analyses either

**profittere** failures:

* `profittere+V+Inf` => `profitter`
* `profittere` has no analyses either

**forløyse** failures:

* `forløyse+V+Inf` => `forløys`
* `forløyse` has no analyses either

**endestupe** failures:

* `endestupe+V+Inf` does not generate!
* `endestupe` has no analyses either

**ventilere** failures:

* `ventilere+V+Inf` => `ventiler`
* `ventilere` has no analyses either

**brudevie** failures:

* `brudevie+V+Inf` does not generate!
* `brudevie` has no analyses either

**utarbeide** failures:

* `utarbeide+V+Inf` does not generate!
* `utarbeide` has no analyses either

**bombardere** failures:

* `bombardere+V+Inf` => `bombarder`
* `bombardere` has no analyses either

**brøle** failures:

* `brøle+V+Inf` does not generate!
* `brøle` has no analyses either

**stroppe** failures:

* `stroppe+V+Inf` => `stropp`
* `stroppe` has no analyses either

**avlete** failures:

* `avlete+V+Inf` => `avlet`
* `avlete` has no analyses either

**dyvje** failures:

* `dyvje+V+Inf` does not generate!
* `dyvje` has no analyses either

**heimedøype** failures:

* `heimedøype+V+Inf` => `heimedøyp`
* `heimedøype` has no analyses either

**-isere** failures:

* `-isere+V+Inf` => `-iser`
* `-isere` has no analyses either

**protte** failures:

* `protte+V+Inf` => `prott`
* `protte` has no analyses either

**borge** failures:

* `borge+V+Inf` => `borg`
* `borge` has no analyses either

**tromme** failures:

* `tromme+V+Inf` => `trom`
* `tromme` has no analyses either

**nyte** failures:

* `nyte+V+Inf` => `nya`
* `nyte+V+Inf` => `nye`
* `nyte` has following analyses:
  * `nytten+A+Neu+Sg+Indef`

**spidde** failures:

* `spidde+V+Inf` => `spidd`
* `spidde` has no analyses either

**presse** failures:

* `presse+V+Inf` => `press`
* `presse` has no analyses either

**validere** failures:

* `validere+V+Inf` => `valider`
* `validere` has no analyses either

**bitast** failures:

* `bitast+V+Inf` => `biast`
* `bitast` has following analyses:
  * `bitter+A+A+Superl+Indef`

**jangle** failures:

* `jangle+V+Inf` => `jangl`
* `jangle` has no analyses either

**yngle** failures:

* `yngle+V+Inf` => `yngl`
* `yngle` has no analyses either

**aure** failures:

* `aure+V+Inf` => `aur`
* `aure` has no analyses either

**ottast** failures:

* `ottast+V+Inf` => `ottastast`
* `ottast` has no analyses either

**semihurtiglade** failures:

* `semihurtiglade+V+Inf` => `semihurtiglad`
* `semihurtiglade` has no analyses either

**paffe** failures:

* `paffe+V+Inf` => `paff`
* `paffe` has no analyses either

**sule** failures:

* `sule+V+Inf` => `sul`
* `sule` has no analyses either

**assortere** failures:

* `assortere+V+Inf` => `assorter`
* `assortere` has no analyses either

**aktivisere** failures:

* `aktivisere+V+Inf` => `aktiviser`
* `aktivisere` has no analyses either

**diskreditere** failures:

* `diskreditere+V+Inf` => `diskrediter`
* `diskreditere` has no analyses either

**sørve** failures:

* `sørve+V+Inf` => `sørv`
* `sørve` has no analyses either

**ekspatriere** failures:

* `ekspatriere+V+Inf` => `ekspatrier`
* `ekspatriere` has no analyses either

**sykle** failures:

* `sykle+V+Inf` => `sykl`
* `sykle` has no analyses either

**dyste** failures:

* `dyste+V+Inf` does not generate!
* `dyste` has following analyses:
  * `dyster+A+A+Pl+Def`
  * `dyster+A+A+Sg+Def`
  * `dyster+A+A+Pl+Indef`

**reite** failures:

* `reite+V+Inf` => `reit`
* `reite` has no analyses either

**avheidre** failures:

* `avheidre+V+Inf` => `avheidr`
* `avheidre` has no analyses either

**gjenskaffe** failures:

* `gjenskaffe+V+Inf` => `gjenskaff`
* `gjenskaffe` has no analyses either

**betle** failures:

* `betle+V+Inf` => `betl`
* `betle` has no analyses either

**kraule** failures:

* `kraule+V+Inf` does not generate!
* `kraule` has no analyses either

**improvisere** failures:

* `improvisere+V+Inf` => `improviser`
* `improvisere` has no analyses either

**gisle** failures:

* `gisle+V+Inf` => `gisl`
* `gisle` has no analyses either

**sentrifugere** failures:

* `sentrifugere+V+Inf` => `sentrifuger`
* `sentrifugere` has no analyses either

**forspille** failures:

* `forspille+V+Inf` does not generate!
* `forspille` has no analyses either

**skyldskrive** failures:

* `skyldskrive+V+Inf` => `skyldskria`
* `skyldskrive+V+Inf` => `skyldskrie`
* `skyldskrive` has no analyses either

**skandalisere** failures:

* `skandalisere+V+Inf` => `skandaliser`
* `skandalisere` has no analyses either

**forkommast** failures:

* `forkommast+V+Inf` => `forkkommast`
* `forkommast` has no analyses either

**fliske** failures:

* `fliske+V+Inf` => `flisk`
* `fliske` has no analyses either

**åtvare** failures:

* `åtvare+V+Inf` => `åtvar`
* `åtvare` has no analyses either

**snuske** failures:

* `snuske+V+Inf` => `snusk`
* `snuske` has no analyses either

**etterplapre** failures:

* `etterplapre+V+Inf` => `etterplapr`
* `etterplapre` has no analyses either

**ferje** failures:

* `ferje+V+Inf` => `ferj`
* `ferje` has no analyses either

**dekode** failures:

* `dekode+V+Inf` => `dekod`
* `dekode` has no analyses either

**troppe** failures:

* `troppe+V+Inf` => `tropp`
* `troppe` has no analyses either

**style** failures:

* `style+V+Inf` => `styl`
* `style` has no analyses either

**levne** failures:

* `levne+V+Inf` => `levn`
* `levne` has no analyses either

**krosse** failures:

* `krosse+V+Inf` => `kross`
* `krosse` has no analyses either

**skrykke** failures:

* `skrykke+V+Inf` does not generate!
* `skrykke` has no analyses either

**chevrotere** failures:

* `chevrotere+V+Inf` => `chevroter`
* `chevrotere` has no analyses either

**røkte** failures:

* `røkte+V+Inf` => `røkt`
* `røkte` has no analyses either

**tåttspringe** failures:

* `tåttspringe+V+Inf` => `tåttspria`
* `tåttspringe+V+Inf` => `tåttsprie`
* `tåttspringe` has no analyses either

**kadreie** failures:

* `kadreie+V+Inf` => `kadrei`
* `kadreie` has no analyses either

**revidere** failures:

* `revidere+V+Inf` => `revider`
* `revidere` has no analyses either

**florere** failures:

* `florere+V+Inf` => `florer`
* `florere` has no analyses either

**pantgi** failures:

* `pantgi+V+Inf` => `pantggi`
* `pantgi` has no analyses either

**humle** failures:

* `humle+V+Inf` => `huml`
* `humle` has no analyses either

**lauge** failures:

* `lauge+V+Inf` => `laug`
* `lauge` has no analyses either

**olstre** failures:

* `olstre+V+Inf` => `olstr`
* `olstre` has no analyses either

**bangse** failures:

* `bangse+V+Inf` => `bangs`
* `bangse` has no analyses either

**avdømme** failures:

* `avdømme+V+Inf` does not generate!
* `avdømme` has no analyses either

**dispergere** failures:

* `dispergere+V+Inf` => `disperger`
* `dispergere` has no analyses either

**iklede** failures:

* `iklede+V+Inf` does not generate!
* `iklede` has no analyses either

**gnelle** failures:

* `gnelle+V+Inf` => `gnee`
* `gnelle+V+Inf` => `gnea`
* `gnelle` has no analyses either

**avrime** failures:

* `avrime+V+Inf` => `avrim`
* `avrime` has no analyses either

**bræle** failures:

* `bræle+V+Inf` => `bræl`
* `bræle` has no analyses either

**tvilegge** failures:

* `tvilegge+V+Inf` does not generate!
* `tvilegge` has no analyses either

**skumre** failures:

* `skumre+V+Inf` => `skumr`
* `skumre` has no analyses either

**klarere** failures:

* `klarere+V+Inf` => `klarer`
* `klarere` has no analyses either

**åvirke** failures:

* `åvirke+V+Inf` => `åvirk`
* `åvirke` has no analyses either

**grynne** failures:

* `grynne+V+Inf` does not generate!
* `grynne` has no analyses either

**sammale** failures:

* `sammale+V+Inf` => `sammae`
* `sammale+V+Inf` => `sammaa`
* `sammale` has no analyses either

**tryste** failures:

* `tryste+V+Inf` does not generate!
* `tryste` has no analyses either

**dure** failures:

* `dure+V+Inf` => `dur`
* `dure` has no analyses either

**batne** failures:

* `batne+V+Inf` => `batn`
* `batne` has no analyses either

**guste** failures:

* `guste+V+Inf` => `gust`
* `guste` has no analyses either

**suge** failures:

* `suge+V+Inf` => `sue`
* `suge+V+Inf` => `sua`
* `suge` has no analyses either

**annullere** failures:

* `annullere+V+Inf` => `annuller`
* `annullere` has no analyses either

**partisipere** failures:

* `partisipere+V+Inf` => `partisiper`
* `partisipere` has no analyses either

**formulere** failures:

* `formulere+V+Inf` => `formuler`
* `formulere` has no analyses either

**knoppe** failures:

* `knoppe+V+Inf` => `knopp`
* `knoppe` has no analyses either

**ruste** failures:

* `ruste+V+Inf` => `rust`
* `ruste` has following analyses:
  * `rusten+A+Neu+Sg+Indef`

**slamme** failures:

* `slamme+V+Inf` => `slam`
* `slamme` has no analyses either

**skuldgje** failures:

* `skuldgje+V+Inf` => `skuldggje`
* `skuldgje` has no analyses either

**stevjast** failures:

* `stevjast+V+Inf` => `stevjastast`
* `stevjast` has no analyses either

**pelle** failures:

* `pelle+V+Inf` => `pell`
* `pelle` has no analyses either

**pønske** failures:

* `pønske+V+Inf` => `pønsk`
* `pønske` has no analyses either

**sleiske** failures:

* `sleiske+V+Inf` => `sleisk`
* `sleiske` has following analyses:
  * `sleisk+A+Pl+Indef`
  * `sleisk+A+Sg+Def`

**palatalisere** failures:

* `palatalisere+V+Inf` => `palataliser`
* `palatalisere` has no analyses either

**rufse** failures:

* `rufse+V+Inf` => `rufs`
* `rufse` has following analyses:
  * `rufsen+A+Neu+Sg+Indef`

**repatriere** failures:

* `repatriere+V+Inf` => `repatrier`
* `repatriere` has no analyses either

**fliskre** failures:

* `fliskre+V+Inf` => `fliskr`
* `fliskre` has no analyses either

**glasere** failures:

* `glasere+V+Inf` => `glaser`
* `glasere` has no analyses either

**førelese** failures:

* `førelese+V+Inf` => `førelea`
* `førelese+V+Inf` => `førelee`
* `førelese` has no analyses either

**humre** failures:

* `humre+V+Inf` => `humr`
* `humre` has no analyses either

**undergrave** failures:

* `undergrave+V+Inf` => `undergrae`
* `undergrave+V+Inf` => `undergraa`
* `undergrave` has no analyses either

**åthuge** failures:

* `åthuge+V+Inf` => `åthug`
* `åthuge` has no analyses either

**pukksetje** failures:

* `pukksetje+V+Inf` does not generate!
* `pukksetje` has no analyses either

**maskinstrikke** failures:

* `maskinstrikke+V+Inf` => `maskinstrikk`
* `maskinstrikke` has no analyses either

**lune** failures:

* `lune+V+Inf` => `lun`
* `lune` has following analyses:
  * `lun+A+Sg+Def`
  * `lun+A+Pl+Indef`

**reprodusere** failures:

* `reprodusere+V+Inf` => `reproduser`
* `reprodusere` has no analyses either

**dagse** failures:

* `dagse+V+Inf` => `dags`
* `dagse` has no analyses either

**førekomme** failures:

* `førekomme+V+Inf` => `førekkomma`
* `førekomme+V+Inf` => `førekkomme`
* `førekomme` has no analyses either

**frikople** failures:

* `frikople+V+Inf` => `frikopl`
* `frikople` has no analyses either

**hote** failures:

* `hote+V+Inf` => `hot`
* `hote` has no analyses either

**lese** failures:

* `lese+V+Inf` => `lee`
* `lese+V+Inf` => `lea`
* `lese` has no analyses either

**herdne** failures:

* `herdne+V+Inf` => `herdn`
* `herdne` has following analyses:
  * `herden+A+Pl+Indef`
  * `herden+A+Sg+Def`

**mishøyre** failures:

* `mishøyre+V+Inf` does not generate!
* `mishøyre` has no analyses either

**skamraudne** failures:

* `skamraudne+V+Inf` => `skamraudn`
* `skamraudne` has no analyses either

**valtre** failures:

* `valtre+V+Inf` => `valtr`
* `valtre` has no analyses either

**hutle** failures:

* `hutle+V+Inf` => `hutl`
* `hutle` has no analyses either

**avsoppe** failures:

* `avsoppe+V+Inf` => `avsopp`
* `avsoppe` has no analyses either

**glise** failures:

* `glise+V+Inf` => `glis`
* `glise` has following analyses:
  * `glissen+A+Neu+Sg+Indef`
  * `glisen+A+Neu+Sg+Indef`

**immortalisere** failures:

* `immortalisere+V+Inf` => `immortaliser`
* `immortalisere` has no analyses either

**bidra** failures:

* `bidra+V+Inf` => `bidrdra`
* `bidra` has no analyses either

**formeire** failures:

* `formeire+V+Inf` => `formeir`
* `formeire` has following analyses:
  * `formeir+A+A+Pl+Def`
  * `formeir+A+A+Pl+Indef`
  * `formeir+A+A+Sg+Def`

**forpleie** failures:

* `forpleie+V+Inf` does not generate!
* `forpleie` has no analyses either

**grovhogge** failures:

* `grovhogge+V+Inf` does not generate!
* `grovhogge` has no analyses either

**åbende** failures:

* `åbende+V+Inf` does not generate!
* `åbende` has no analyses either

**dunke** failures:

* `dunke+V+Inf` => `dunk`
* `dunke` has following analyses:
  * `dunkel+A+A+Sg+Def`
  * `dunkel+A+A+Pl+Def`
  * `dunkel+A+A+Pl+Indef`

**klattmåle** failures:

* `klattmåle+V+Inf` => `klattmål`
* `klattmåle` has no analyses either

**ane** failures:

* `ane+V+Inf` => `an`
* `ane` has following analyses:
  * `ann+A+A+Pl+Def`
  * `ann+A+A+Pl+Indef`
  * `ann+A+A+Sg+Def`

**tilgi** failures:

* `tilgi+V+Inf` => `tilggi`
* `tilgi` has no analyses either

**skissere** failures:

* `skissere+V+Inf` => `skisser`
* `skissere` has no analyses either

**bingse** failures:

* `bingse+V+Inf` => `bings`
* `bingse` has no analyses either

**cyanidisere** failures:

* `cyanidisere+V+Inf` => `cyanidiser`
* `cyanidisere` has no analyses either

**movere** failures:

* `movere+V+Inf` => `mover`
* `movere` has no analyses either

**kvatne** failures:

* `kvatne+V+Inf` => `kvatn`
* `kvatne` has no analyses either

**røde** failures:

* `røde+V+Inf` does not generate!
* `røde` has no analyses either

**bekre** failures:

* `bekre+V+Inf` => `bekr`
* `bekre` has no analyses either

**spavende** failures:

* `spavende+V+Inf` does not generate!
* `spavende` has no analyses either

**mattere** failures:

* `mattere+V+Inf` => `matter`
* `mattere` has no analyses either

**polere** failures:

* `polere+V+Inf` => `poler`
* `polere` has no analyses either

**sensurere** failures:

* `sensurere+V+Inf` => `sensurer`
* `sensurere` has no analyses either

**tappe** failures:

* `tappe+V+Inf` => `tapp`
* `tappe` has no analyses either

**torast** failures:

* `torast+V+Inf` => `tast`
* `torast` has no analyses either

**be** failures:

* `be+V+Inf` => `bbe`
* `be` has following analyses:
  * `bere+V+Imprt`
  * `b+A+A+Sg+Def`
  * `b+A+A+Pl+Indef`
  * `bere+V+Ind+Prs`
  * `b+A+A+Pl+Def`

**fangtake** failures:

* `fangtake+V+Inf` => `fangttaka`
* `fangtake+V+Inf` => `fangttake`
* `fangtake` has no analyses either

**bare** failures:

* `bare+V+Inf` => `bar`
* `bare` has no analyses either

**reformere** failures:

* `reformere+V+Inf` => `reformer`
* `reformere` has no analyses either

**skamføre** failures:

* `skamføre+V+Inf` => `skamfør`
* `skamføre` has no analyses either

**evne** failures:

* `evne+V+Inf` => `evn`
* `evne` has no analyses either

**forlegge** failures:

* `forlegge+V+Inf` does not generate!
* `forlegge` has no analyses either

**brodde** failures:

* `brodde+V+Inf` => `brodd`
* `brodde` has no analyses either

**smatre** failures:

* `smatre+V+Inf` => `smatr`
* `smatre` has no analyses either

**skyggje** failures:

* `skyggje+V+Inf` does not generate!
* `skyggje` has no analyses either

**utruste** failures:

* `utruste+V+Inf` => `utrust`
* `utruste` has no analyses either

**turnere** failures:

* `turnere+V+Inf` => `turner`
* `turnere` has no analyses either

**hellelegge** failures:

* `hellelegge+V+Inf` does not generate!
* `hellelegge` has no analyses either

**fornikle** failures:

* `fornikle+V+Inf` => `fornikl`
* `fornikle` has no analyses either

**masseundersøke** failures:

* `masseundersøke+V+Inf` => `masseundersøk`
* `masseundersøke` has no analyses either

**hamne** failures:

* `hamne+V+Inf` => `hamn`
* `hamne` has no analyses either

**eksitere** failures:

* `eksitere+V+Inf` => `eksiter`
* `eksitere` has no analyses either

**blikte** failures:

* `blikte+V+Inf` => `blikt`
* `blikte` has no analyses either

**kvitvaske** failures:

* `kvitvaske+V+Inf` => `kvitvask`
* `kvitvaske` has no analyses either

**overdøyve** failures:

* `overdøyve+V+Inf` does not generate!
* `overdøyve` has no analyses either

**ommøblere** failures:

* `ommøblere+V+Inf` => `ommøbler`
* `ommøblere` has no analyses either

**vagabondere** failures:

* `vagabondere+V+Inf` => `vagabonder`
* `vagabondere` has no analyses either

**gjennombore** failures:

* `gjennombore+V+Inf` => `gjennombor`
* `gjennombore` has no analyses either

**kjetle** failures:

* `kjetle+V+Inf` => `kjetl`
* `kjetle` has no analyses either

**stevje** failures:

* `stevje+V+Inf` => `stevj`
* `stevje` has no analyses either

**merke** failures:

* `merke+V+Inf` => `merk`
* `merke` has no analyses either

**gravsetje** failures:

* `gravsetje+V+Inf` does not generate!
* `gravsetje` has no analyses either

**brunsteikje** failures:

* `brunsteikje+V+Inf` does not generate!
* `brunsteikje` has no analyses either

**avgude** failures:

* `avgude+V+Inf` => `avgud`
* `avgude` has no analyses either

**keive** failures:

* `keive+V+Inf` => `keiv`
* `keive` has following analyses:
  * `keiv+A+Sg+Def`
  * `keiv+A+Pl+Indef`

**kneste** failures:

* `kneste+V+Inf` => `knea`
* `kneste+V+Inf` => `knee`
* `kneste` has no analyses either

**prosessere** failures:

* `prosessere+V+Inf` => `prosesser`
* `prosessere` has no analyses either

**adherere** failures:

* `adherere+V+Inf` => `adherer`
* `adherere` has no analyses either

**antistathandsame** failures:

* `antistathandsame+V+Inf` => `antistathandsam`
* `antistathandsame` has no analyses either

**omkalfatre** failures:

* `omkalfatre+V+Inf` => `omkalfatr`
* `omkalfatre` has no analyses either

**hirre** failures:

* `hirre+V+Inf` => `hirr`
* `hirre` has no analyses either

**forulukke** failures:

* `forulukke+V+Inf` => `forulukk`
* `forulukke` has no analyses either

**pruppe** failures:

* `pruppe+V+Inf` => `prupp`
* `pruppe` has no analyses either

**snøype** failures:

* `snøype+V+Inf` => `snøyp`
* `snøype` has no analyses either

**forsikre** failures:

* `forsikre+V+Inf` => `forsikr`
* `forsikre` has no analyses either

**grøte** failures:

* `grøte+V+Inf` => `grøt`
* `grøte` has no analyses either

**pådra** failures:

* `pådra+V+Inf` => `pådrdra`
* `pådra` has no analyses either

**tinglyse** failures:

* `tinglyse+V+Inf` => `tinglys`
* `tinglyse` has no analyses either

**trille** failures:

* `trille+V+Inf` => `trill`
* `trille` has no analyses either

**besøkje** failures:

* `besøkje+V+Inf` does not generate!
* `besøkje` has no analyses either

**vie** failures:

* `vie+V+Inf` does not generate!
* `vie` has no analyses either

**diagnostisere** failures:

* `diagnostisere+V+Inf` => `diagnostiser`
* `diagnostisere` has no analyses either

**brakke** failures:

* `brakke+V+Inf` => `brakk`
* `brakke` has no analyses either

**demonstrere** failures:

* `demonstrere+V+Inf` => `demonstrer`
* `demonstrere` has no analyses either

**kryne** failures:

* `kryne+V+Inf` => `kryn`
* `kryne` has no analyses either

**rustifisere** failures:

* `rustifisere+V+Inf` => `rustifiser`
* `rustifisere` has no analyses either

**temperere** failures:

* `temperere+V+Inf` => `temperer`
* `temperere` has no analyses either

**koordinere** failures:

* `koordinere+V+Inf` => `koordiner`
* `koordinere` has no analyses either

**kappsegle** failures:

* `kappsegle+V+Inf` does not generate!
* `kappsegle` has no analyses either

**smusle** failures:

* `smusle+V+Inf` => `smusl`
* `smusle` has no analyses either

**brannforsikre** failures:

* `brannforsikre+V+Inf` => `brannforsikr`
* `brannforsikre` has no analyses either

**opendage** failures:

* `opendage+V+Inf` => `opendag`
* `opendage` has no analyses either

**jamstille** failures:

* `jamstille+V+Inf` does not generate!
* `jamstille` has no analyses either

**rasjonere** failures:

* `rasjonere+V+Inf` => `rasjoner`
* `rasjonere` has no analyses either

**blodvekkje** failures:

* `blodvekkje+V+Inf` does not generate!
* `blodvekkje` has no analyses either

**mumse** failures:

* `mumse+V+Inf` => `mums`
* `mumse` has no analyses either

**poststemple** failures:

* `poststemple+V+Inf` => `poststempl`
* `poststemple` has no analyses either

**sille** failures:

* `sille+V+Inf` => `sill`
* `sille` has no analyses either

**freelanse** failures:

* `freelanse+V+Inf` => `freelans`
* `freelanse` has following analyses:
  * `freelans+A+A+Pl+Def`
  * `freelans+A+A+Sg+Def`
  * `freelans+A+A+Pl+Indef`

**marme** failures:

* `marme+V+Inf` => `marm`
* `marme` has no analyses either

**ivareta** failures:

* `ivareta+V+Inf` => `ivaretta`
* `ivareta` has no analyses either

**omskrive** failures:

* `omskrive+V+Inf` => `omskria`
* `omskrive+V+Inf` => `omskrie`
* `omskrive` has no analyses either

**svengje** failures:

* `svengje+V+Inf` does not generate!
* `svengje` has no analyses either

**røre** failures:

* `røre+V+Inf` does not generate!
* `røre` has following analyses:
  * `røren+A+Neu+Sg+Indef`

**trylle** failures:

* `trylle+V+Inf` does not generate!
* `trylle` has no analyses either

**takke** failures:

* `takke+V+Inf` => `takk`
* `takke` has no analyses either

**dikke** failures:

* `dikke+V+Inf` => `dikk`
* `dikke` has no analyses either

**rele** failures:

* `rele+V+Inf` => `rel`
* `rele` has following analyses:
  * `rele+N+N+Neu+Pl+Indef`
  * `rele+N+N+Neu+Sg+Indef`

**størkne** failures:

* `størkne+V+Inf` => `størkn`
* `størkne` has no analyses either

**styrtregne** failures:

* `styrtregne+V+Inf` does not generate!
* `styrtregne` has no analyses either

**gomle** failures:

* `gomle+V+Inf` => `goml`
* `gomle` has no analyses either

**fornedre** failures:

* `fornedre+V+Inf` => `fornedr`
* `fornedre` has no analyses either

**bråle** failures:

* `bråle+V+Inf` => `brål`
* `bråle` has no analyses either

**brase** failures:

* `brase+V+Inf` => `bras`
* `brase` has no analyses either

**elde** failures:

* `elde+V+Inf` => `eld`
* `elde` has no analyses either

**gøyse** failures:

* `gøyse+V+Inf` => `gøys`
* `gøyse` has no analyses either

**byde** failures:

* `byde+V+Inf` does not generate!
* `byde` has no analyses either

**elektrisere** failures:

* `elektrisere+V+Inf` => `elektriser`
* `elektrisere` has no analyses either

**samanlikne** failures:

* `samanlikne+V+Inf` => `samanlikn`
* `samanlikne` has no analyses either

**romanisere** failures:

* `romanisere+V+Inf` => `romaniser`
* `romanisere` has no analyses either

**parkere** failures:

* `parkere+V+Inf` => `parker`
* `parkere` has no analyses either

**rekreere** failures:

* `rekreere+V+Inf` => `rekreer`
* `rekreere` has no analyses either

**spile** failures:

* `spile+V+Inf` => `spil`
* `spile` has no analyses either

**fikle** failures:

* `fikle+V+Inf` => `fikl`
* `fikle` has no analyses either

**gyne** failures:

* `gyne+V+Inf` => `gyn`
* `gyne` has no analyses either

**fribanke** failures:

* `fribanke+V+Inf` => `fribank`
* `fribanke` has no analyses either

**kagge** failures:

* `kagge+V+Inf` => `kagg`
* `kagge` has no analyses either

**glamre** failures:

* `glamre+V+Inf` => `glamr`
* `glamre` has no analyses either

**uvete** failures:

* `uvete+V+Inf` => `uvet`
* `uvete` has no analyses either

**vralte** failures:

* `vralte+V+Inf` => `vralt`
* `vralte` has no analyses either

**koagulere** failures:

* `koagulere+V+Inf` => `koaguler`
* `koagulere` has no analyses either

**erve** failures:

* `erve+V+Inf` does not generate!
* `erve` has no analyses either

**bangle** failures:

* `bangle+V+Inf` => `bangl`
* `bangle` has no analyses either

**demonisere** failures:

* `demonisere+V+Inf` => `demoniser`
* `demonisere` has no analyses either

**stykke** failures:

* `stykke+V+Inf` => `stykk`
* `stykke` has no analyses either

**fosse** failures:

* `fosse+V+Inf` => `foss`
* `fosse` has no analyses either

**dyrne** failures:

* `dyrne+V+Inf` => `dyrn`
* `dyrne` has no analyses either

**døse** failures:

* `døse+V+Inf` => `døs`
* `døse` has following analyses:
  * `døsen+A+Neu+Sg+Indef`

**knipe** failures:

* `knipe+V+Inf` => `knia`
* `knipe+V+Inf` => `knie`
* `knipe` has following analyses:
  * `knipen+A+Neu+Sg+Indef`

**ele** failures:

* `ele+V+Inf` => `el`
* `ele` has no analyses either

**smeise** failures:

* `smeise+V+Inf` does not generate!
* `smeise` has no analyses either

**underkjenne** failures:

* `underkjenne+V+Inf` does not generate!
* `underkjenne` has no analyses either

**blage** failures:

* `blage+V+Inf` => `blag`
* `blage` has no analyses either

**klausulere** failures:

* `klausulere+V+Inf` => `klausuler`
* `klausulere` has no analyses either

**telje** failures:

* `telje+V+Inf` does not generate!
* `telje` has no analyses either

**tufte** failures:

* `tufte+V+Inf` => `tuft`
* `tufte` has no analyses either

**lekse** failures:

* `lekse+V+Inf` => `leks`
* `lekse` has no analyses either

**ulykketrygde** failures:

* `ulykketrygde+V+Inf` => `ulykketrygd`
* `ulykketrygde` has no analyses either

**friliste** failures:

* `friliste+V+Inf` => `frilist`
* `friliste` has no analyses either

**optimalisere** failures:

* `optimalisere+V+Inf` => `optimaliser`
* `optimalisere` has no analyses either

**kroppsskanne** failures:

* `kroppsskanne+V+Inf` => `kroppsskann`
* `kroppsskanne` has no analyses either

**sløyme** failures:

* `sløyme+V+Inf` does not generate!
* `sløyme` has no analyses either

**slagne** failures:

* `slagne+V+Inf` => `slagn`
* `slagne` has following analyses:
  * `slagen+A+Sg+Def`
  * `slagen+A+Pl+Indef`

**debrife** failures:

* `debrife+V+Inf` => `debrif`
* `debrife` has no analyses either

**harmonisere** failures:

* `harmonisere+V+Inf` => `harmoniser`
* `harmonisere` has no analyses either

**forpakte** failures:

* `forpakte+V+Inf` => `forpakt`
* `forpakte` has no analyses either

**stanse** failures:

* `stanse+V+Inf` => `stans`
* `stanse` has no analyses either

**lagre** failures:

* `lagre+V+Inf` => `lagr`
* `lagre` has no analyses either

**svorte** failures:

* `svorte+V+Inf` => `svort`
* `svorte` has no analyses either

**tvangsauksjonere** failures:

* `tvangsauksjonere+V+Inf` => `tvangsauksjoner`
* `tvangsauksjonere` has no analyses either

**tvibyte** failures:

* `tvibyte+V+Inf` => `tvibyt`
* `tvibyte` has no analyses either

**dryse** failures:

* `dryse+V+Inf` => `drya`
* `dryse+V+Inf` => `drye`
* `dryse` has no analyses either

**førehandsgodkjenne** failures:

* `førehandsgodkjenne+V+Inf` => `førehandsgodkjene`
* `førehandsgodkjenne+V+Inf` => `førehandsgodkjena`
* `førehandsgodkjenne` has no analyses either

**hemje** failures:

* `hemje+V+Inf` => `hemj`
* `hemje` has no analyses either

**stripse** failures:

* `stripse+V+Inf` => `strips`
* `stripse` has no analyses either

**avstyggjast** failures:

* `avstyggjast+V+Inf` => `avstygjast`
* `avstyggjast` has no analyses either

**plotte** failures:

* `plotte+V+Inf` => `plott`
* `plotte` has no analyses either

**overlappe** failures:

* `overlappe+V+Inf` => `overlapp`
* `overlappe` has no analyses either

**skjerde** failures:

* `skjerde+V+Inf` does not generate!
* `skjerde` has no analyses either

**oie** failures:

* `oie+V+Inf` => `oi`
* `oie` has no analyses either

**storskrike** failures:

* `storskrike+V+Inf` => `storskria`
* `storskrike+V+Inf` => `storskrie`
* `storskrike` has no analyses either

**etterlyse** failures:

* `etterlyse+V+Inf` => `etterlys`
* `etterlyse` has no analyses either

**hente** failures:

* `hente+V+Inf` => `hent`
* `hente` has following analyses:
  * `hente+A+A+Neu+Sg+Indef`
  * `hente+A+A+Fem+Sg+Indef`
  * `hente+A+A+Msc+Sg+Indef`

**gire** failures:

* `gire+V+Inf` => `gir`
* `gire` has following analyses:
  * `giren+A+Neu+Sg+Indef`

**magrast** failures:

* `magrast+V+Inf` => `magrastast`
* `magrast` has no analyses either

**deklinere** failures:

* `deklinere+V+Inf` => `dekliner`
* `deklinere` has no analyses either

**kravellbyggje** failures:

* `kravellbyggje+V+Inf` does not generate!
* `kravellbyggje` has no analyses either

**panorere** failures:

* `panorere+V+Inf` => `panorer`
* `panorere` has no analyses either

**kokkelere** failures:

* `kokkelere+V+Inf` => `kokkeler`
* `kokkelere` has no analyses either

**galonere** failures:

* `galonere+V+Inf` => `galoner`
* `galonere` has no analyses either

**baksnakke** failures:

* `baksnakke+V+Inf` => `baksnakk`
* `baksnakke` has no analyses either

**teite** failures:

* `teite+V+Inf` => `teit`
* `teite` has following analyses:
  * `teit+A+A+Sg+Def`
  * `teit+A+A+Pl+Def`
  * `teit+A+A+Pl+Indef`

**snykte** failures:

* `snykte+V+Inf` => `snykt`
* `snykte` has no analyses either

**stubbe** failures:

* `stubbe+V+Inf` => `stubb`
* `stubbe` has no analyses either

**skite** failures:

* `skite+V+Inf` => `skia`
* `skite+V+Inf` => `skie`
* `skite` has following analyses:
  * `skitten+A+Neu+Sg+Indef`
  * `skit+A+A+Pl+Def`
  * `skit+A+A+Pl+Indef`
  * `skiten+A+Neu+Sg+Indef`
  * `skit+A+A+Sg+Def`

**trafse** failures:

* `trafse+V+Inf` => `trafs`
* `trafse` has no analyses either

**vidareføre** failures:

* `vidareføre+V+Inf` => `vidarefør`
* `vidareføre` has no analyses either

**kaldskratte** failures:

* `kaldskratte+V+Inf` => `kaldskratt`
* `kaldskratte` has no analyses either

**tabloidisere** failures:

* `tabloidisere+V+Inf` => `tabloidiser`
* `tabloidisere` has no analyses either

**omformulere** failures:

* `omformulere+V+Inf` => `omformuler`
* `omformulere` has no analyses either

**dugleggjere** failures:

* `dugleggjere+V+Inf` does not generate!
* `dugleggjere` has no analyses either

**kuratere** failures:

* `kuratere+V+Inf` => `kurater`
* `kuratere` has no analyses either

**skvample** failures:

* `skvample+V+Inf` => `skvampl`
* `skvample` has no analyses either

**distansere** failures:

* `distansere+V+Inf` => `distanser`
* `distansere` has no analyses either

**innprente** failures:

* `innprente+V+Inf` => `innprent`
* `innprente` has no analyses either

**snøye** failures:

* `snøye+V+Inf` does not generate!
* `snøye` has no analyses either

**stike** failures:

* `stike+V+Inf` => `stik`
* `stike` has no analyses either

**sjogse** failures:

* `sjogse+V+Inf` => `sjogs`
* `sjogse` has no analyses either

**kverulere** failures:

* `kverulere+V+Inf` => `kveruler`
* `kverulere` has no analyses either

**avskrekke** failures:

* `avskrekke+V+Inf` => `avskrekk`
* `avskrekke` has no analyses either

**høgtide** failures:

* `høgtide+V+Inf` => `høgtid`
* `høgtide` has no analyses either

**kommentere** failures:

* `kommentere+V+Inf` => `kommenter`
* `kommentere` has no analyses either

**tørrbanne** failures:

* `tørrbanne+V+Inf` does not generate!
* `tørrbanne` has no analyses either

**egle** failures:

* `egle+V+Inf` => `egl`
* `egle` has no analyses either

**tilgjeve** failures:

* `tilgjeve+V+Inf` => `tilggjeve`
* `tilgjeve+V+Inf` => `tilggjeva`
* `tilgjeve` has no analyses either

**recensere** failures:

* `recensere+V+Inf` => `recenser`
* `recensere` has no analyses either

**operere** failures:

* `operere+V+Inf` => `operer`
* `operere` has no analyses either

**banelegge** failures:

* `banelegge+V+Inf` does not generate!
* `banelegge` has no analyses either

**skrankast** failures:

* `skrankast+V+Inf` => `skrankastast`
* `skrankast` has no analyses either

**pukksette** failures:

* `pukksette+V+Inf` does not generate!
* `pukksette` has no analyses either

**sannkjenne** failures:

* `sannkjenne+V+Inf` does not generate!
* `sannkjenne` has no analyses either

**ynske** failures:

* `ynske+V+Inf` => `ynsk`
* `ynske` has no analyses either

**trakassere** failures:

* `trakassere+V+Inf` => `trakasser`
* `trakassere` has no analyses either

**seminere** failures:

* `seminere+V+Inf` => `seminer`
* `seminere` has no analyses either

**knubbe** failures:

* `knubbe+V+Inf` => `knubb`
* `knubbe` has no analyses either

**gneiste** failures:

* `gneiste+V+Inf` => `gneist`
* `gneiste` has no analyses either

**klimpre** failures:

* `klimpre+V+Inf` => `klimpr`
* `klimpre` has no analyses either

**granulere** failures:

* `granulere+V+Inf` => `granuler`
* `granulere` has no analyses either

**utagere** failures:

* `utagere+V+Inf` => `utager`
* `utagere` has no analyses either

**mynte** failures:

* `mynte+V+Inf` => `mynt`
* `mynte` has no analyses either

**ferme** failures:

* `ferme+V+Inf` => `ferm`
* `ferme` has following analyses:
  * `ferm+A+Pl+Indef`
  * `ferm+A+Sg+Def`

**glenne** failures:

* `glenne+V+Inf` does not generate!
* `glenne` has no analyses either

**fortifisere** failures:

* `fortifisere+V+Inf` => `fortifiser`
* `fortifisere` has no analyses either

**forta** failures:

* `forta+V+Inf` => `fortta`
* `forta` has following analyses:
  * `fort+N+N+Neu+Pl+Def`
  * `forte+V+Ind+Prt`
  * `forte+V+PrfPrc`

**fullgjere** failures:

* `fullgjere+V+Inf` does not generate!
* `fullgjere` has no analyses either

**vibrere** failures:

* `vibrere+V+Inf` => `vibrer`
* `vibrere` has no analyses either

**hyfse** failures:

* `hyfse+V+Inf` => `hyfs`
* `hyfse` has no analyses either

**kongruere** failures:

* `kongruere+V+Inf` => `kongruer`
* `kongruere` has no analyses either

**flase** failures:

* `flase+V+Inf` => `flas`
* `flase` has no analyses either

**agglomerere** failures:

* `agglomerere+V+Inf` => `agglomerer`
* `agglomerere` has no analyses either

**leste** failures:

* `leste+V+Inf` => `lest`
* `leste` has no analyses either

**avdage** failures:

* `avdage+V+Inf` => `avdag`
* `avdage` has no analyses either

**kanthogge** failures:

* `kanthogge+V+Inf` does not generate!
* `kanthogge` has no analyses either

**selektere** failures:

* `selektere+V+Inf` => `selekter`
* `selektere` has no analyses either

**vende** failures:

* `vende+V+Inf` does not generate!
* `vende` has no analyses either

**krafse** failures:

* `krafse+V+Inf` => `krafs`
* `krafse` has no analyses either

**rope** failures:

* `rope+V+Inf` does not generate!
* `rope` has no analyses either

**skate** failures:

* `skate+V+Inf` => `skat`
* `skate` has no analyses either

**keivle** failures:

* `keivle+V+Inf` => `keivl`
* `keivle` has no analyses either

**furdast** failures:

* `furdast+V+Inf` => `furdastast`
* `furdast` has no analyses either

**innsette** failures:

* `innsette+V+Inf` does not generate!
* `innsette` has following analyses:
  * `innsett+A+A+Pl+Def`
  * `innsett+A+A+Sg+Def`
  * `innsett+A+A+Pl+Indef`

**jugle** failures:

* `jugle+V+Inf` => `jugl`
* `jugle` has no analyses either

**forevige** failures:

* `forevige+V+Inf` => `forevig`
* `forevige` has no analyses either

**loge** failures:

* `loge+V+Inf` => `log`
* `loge` has no analyses either

**stagvende** failures:

* `stagvende+V+Inf` does not generate!
* `stagvende` has no analyses either

**kråle** failures:

* `kråle+V+Inf` => `krål`
* `kråle` has no analyses either

**sipe** failures:

* `sipe+V+Inf` does not generate!
* `sipe` has no analyses either

**mekke** failures:

* `mekke+V+Inf` => `mekk`
* `mekke` has no analyses either

**gjennomprøve** failures:

* `gjennomprøve+V+Inf` does not generate!
* `gjennomprøve` has no analyses either

**spakne** failures:

* `spakne+V+Inf` => `spakn`
* `spakne` has no analyses either

**hjelpe** failures:

* `hjelpe+V+Inf` => `hjelp`
* `hjelpe` has following analyses:
  * `hjelpen+A+Neu+Sg+Indef`

**hardstappe** failures:

* `hardstappe+V+Inf` => `hardstapp`
* `hardstappe` has no analyses either

**tilverke** failures:

* `tilverke+V+Inf` => `tilverk`
* `tilverke` has no analyses either

**fagne** failures:

* `fagne+V+Inf` => `fagn`
* `fagne` has no analyses either

**brodere** failures:

* `brodere+V+Inf` => `broder`
* `brodere` has no analyses either

**overfalle** failures:

* `overfalle+V+Inf` => `overfaa`
* `overfalle+V+Inf` => `overfae`
* `overfalle` has no analyses either

**sjåast** failures:

* `sjåast+V+Inf` => `ssjåast`
* `sjåast` has no analyses either

**burle** failures:

* `burle+V+Inf` => `burl`
* `burle` has no analyses either

**svige** failures:

* `svige+V+Inf` => `svig`
* `svige` has no analyses either

**vagge** failures:

* `vagge+V+Inf` => `vagg`
* `vagge` has no analyses either

**floke** failures:

* `floke+V+Inf` => `flok`
* `floke` has following analyses:
  * `floken+A+Neu+Sg+Indef`

**glame** failures:

* `glame+V+Inf` => `glam`
* `glame` has no analyses either

**revoltere** failures:

* `revoltere+V+Inf` => `revolter`
* `revoltere` has no analyses either

**kryssubsidiere** failures:

* `kryssubsidiere+V+Inf` => `kryssubsidier`
* `kryssubsidiere` has no analyses either

**harselere** failures:

* `harselere+V+Inf` => `harseler`
* `harselere` has no analyses either

**strengle** failures:

* `strengle+V+Inf` => `strengl`
* `strengle` has no analyses either

**sevjeberkje** failures:

* `sevjeberkje+V+Inf` does not generate!
* `sevjeberkje` has no analyses either

**leige** failures:

* `leige+V+Inf` does not generate!
* `leige` has no analyses either

**kylje** failures:

* `kylje+V+Inf` => `kyl`
* `kylje` has no analyses either

**søke** failures:

* `søke+V+Inf` => `søk`
* `søke` has following analyses:
  * `søken+A+Neu+Sg+Indef`

**drysje** failures:

* `drysje+V+Inf` does not generate!
* `drysje` has no analyses either

**figurere** failures:

* `figurere+V+Inf` => `figurer`
* `figurere` has no analyses either

**kortne** failures:

* `kortne+V+Inf` => `kortn`
* `kortne` has no analyses either

**snusmale** failures:

* `snusmale+V+Inf` => `snusmae`
* `snusmale+V+Inf` => `snusmaa`
* `snusmale` has no analyses either

**repe** failures:

* `repe+V+Inf` => `rep`
* `repe` has no analyses either

**snuvle** failures:

* `snuvle+V+Inf` => `snuvl`
* `snuvle` has no analyses either

**forsvenske** failures:

* `forsvenske+V+Inf` => `forsvensk`
* `forsvenske` has no analyses either

**skumreinse** failures:

* `skumreinse+V+Inf` => `skumreins`
* `skumreinse` has no analyses either

**die** failures:

* `die+V+Inf` => `di`
* `die` has following analyses:
  * `die+N+N+Sg`

**imponere** failures:

* `imponere+V+Inf` => `imponer`
* `imponere` has no analyses either

**sope** failures:

* `sope+V+Inf` does not generate!
* `sope` has following analyses:
  * `soppen+A+Neu+Sg+Indef`

**endereise** failures:

* `endereise+V+Inf` => `endereis`
* `endereise` has no analyses either

**gulke** failures:

* `gulke+V+Inf` => `gulk`
* `gulke` has no analyses either

**krusle** failures:

* `krusle+V+Inf` => `krusl`
* `krusle` has following analyses:
  * `kruslen+A+Neu+Sg+Indef`

**geine** failures:

* `geine+V+Inf` does not generate!
* `geine` has no analyses either

**tilby** failures:

* `tilby+V+Inf` does not generate!
* `tilby` has no analyses either

**kaldrøykje** failures:

* `kaldrøykje+V+Inf` does not generate!
* `kaldrøykje` has no analyses either

**hikste** failures:

* `hikste+V+Inf` => `hikst`
* `hikste` has no analyses either

**tukte** failures:

* `tukte+V+Inf` => `tukt`
* `tukte` has no analyses either

**berge** failures:

* `berge+V+Inf` => `berg`
* `berge` has no analyses either

**avhenge** failures:

* `avhenge+V+Inf` => `avhhenge`
* `avhenge+V+Inf` => `avhhenga`
* `avhenge` has no analyses either

**gnage** failures:

* `gnage+V+Inf` => `gn`
* `gnage` has no analyses either

**feire** failures:

* `feire+V+Inf` => `feir`
* `feire` has no analyses either

**klakke** failures:

* `klakke+V+Inf` => `klakk`
* `klakke` has no analyses either

**pace** failures:

* `pace+V+Inf` does not generate!
* `pace` has no analyses either

**kjølne** failures:

* `kjølne+V+Inf` => `kjøln`
* `kjølne` has following analyses:
  * `kjølen+A+Pl+Indef`
  * `kjølen+A+Sg+Def`

**irritere** failures:

* `irritere+V+Inf` => `irriter`
* `irritere` has no analyses either

**peike** failures:

* `peike+V+Inf` does not generate!
* `peike` has no analyses either

**sparke** failures:

* `sparke+V+Inf` => `spark`
* `sparke` has no analyses either

**koste** failures:

* `koste+V+Inf` => `kost`
* `koste` has no analyses either

**funksjonere** failures:

* `funksjonere+V+Inf` => `funksjoner`
* `funksjonere` has no analyses either

**attføre** failures:

* `attføre+V+Inf` => `attfør`
* `attføre` has no analyses either

**forbause** failures:

* `forbause+V+Inf` => `forbaus`
* `forbause` has no analyses either

**tverrstogge** failures:

* `tverrstogge+V+Inf` => `tverrstogg`
* `tverrstogge` has no analyses either

**segmentere** failures:

* `segmentere+V+Inf` => `segmenter`
* `segmentere` has no analyses either

**knyste** failures:

* `knyste+V+Inf` does not generate!
* `knyste` has no analyses either

**svolne** failures:

* `svolne+V+Inf` => `svoln`
* `svolne` has following analyses:
  * `svollen+A+Pl+Indef`
  * `svollen+A+Sg+Def`

**piple** failures:

* `piple+V+Inf` => `pipl`
* `piple` has no analyses either

**famne** failures:

* `famne+V+Inf` => `famn`
* `famne` has no analyses either

**skylje** failures:

* `skylje+V+Inf` => `skyl`
* `skylje` has no analyses either

**slaure** failures:

* `slaure+V+Inf` => `slaur`
* `slaure` has no analyses either

**snoke** failures:

* `snoke+V+Inf` => `snok`
* `snoke` has no analyses either

**rokke** failures:

* `rokke+V+Inf` => `rokk`
* `rokke` has no analyses either

**piske** failures:

* `piske+V+Inf` => `pisk`
* `piske` has no analyses either

**trilte** failures:

* `trilte+V+Inf` => `trilt`
* `trilte` has no analyses either

**kjekse** failures:

* `kjekse+V+Inf` => `kjeks`
* `kjekse` has no analyses either

**uttyde** failures:

* `uttyde+V+Inf` does not generate!
* `uttyde` has no analyses either

**forløfte** failures:

* `forløfte+V+Inf` does not generate!
* `forløfte` has no analyses either

**bande** failures:

* `bande+V+Inf` => `band`
* `bande` has no analyses either

**prostituere** failures:

* `prostituere+V+Inf` => `prostituer`
* `prostituere` has no analyses either

**idolisere** failures:

* `idolisere+V+Inf` => `idoliser`
* `idolisere` has no analyses either

**fure** failures:

* `fure+V+Inf` => `fur`
* `fure` has no analyses either

**croone** failures:

* `croone+V+Inf` => `croon`
* `croone` has no analyses either

**stase** failures:

* `stase+V+Inf` => `stas`
* `stase` has no analyses either

**frelse** failures:

* `frelse+V+Inf` => `frels`
* `frelse` has following analyses:
  * `frels+A+Pl+Indef`
  * `frels+A+Sg+Def`

**agere** failures:

* `agere+V+Inf` => `ager`
* `agere` has no analyses either

**betrakte** failures:

* `betrakte+V+Inf` => `betrakt`
* `betrakte` has no analyses either

**roe** failures:

* `roe+V+Inf` => `ro`
* `roe` has following analyses:
  * `ryke+V+Adj+Nøyt+Ub+Eint`
  * `ryte+V+Adj+Nøyt+Ub+Eint`
  * `ryke+V+PrfPrc`
  * `ryte+V+PrfPrc`

**reklamere** failures:

* `reklamere+V+Inf` => `reklamer`
* `reklamere` has no analyses either

**fortette** failures:

* `fortette+V+Inf` => `fortett`
* `fortette` has no analyses either

**medgjeve** failures:

* `medgjeve+V+Inf` => `medggjeve`
* `medgjeve+V+Inf` => `medggjeva`
* `medgjeve` has no analyses either

**synergere** failures:

* `synergere+V+Inf` => `synerger`
* `synergere` has no analyses either

**illsvide** failures:

* `illsvide+V+Inf` does not generate!
* `illsvide` has following analyses:
  * `illsviden+A+Neu+Sg+Indef`

**flislegge** failures:

* `flislegge+V+Inf` does not generate!
* `flislegge` has no analyses either

**frisere** failures:

* `frisere+V+Inf` => `friser`
* `frisere` has no analyses either

**stige** failures:

* `stige+V+Inf` => `stie`
* `stige+V+Inf` => `stia`
* `stige` has no analyses either

**suksedere** failures:

* `suksedere+V+Inf` => `sukseder`
* `suksedere` has no analyses either

**streke** failures:

* `streke+V+Inf` => `strek`
* `streke` has no analyses either

**bokstavere** failures:

* `bokstavere+V+Inf` => `bokstaver`
* `bokstavere` has no analyses either

**legalisere** failures:

* `legalisere+V+Inf` => `legaliser`
* `legalisere` has no analyses either

**kranse** failures:

* `kranse+V+Inf` => `krans`
* `kranse` has no analyses either

**pulverisere** failures:

* `pulverisere+V+Inf` => `pulveriser`
* `pulverisere` has no analyses either

**posjere** failures:

* `posjere+V+Inf` => `posjer`
* `posjere` has no analyses either

**hudstryke** failures:

* `hudstryke+V+Inf` => `hudstrya`
* `hudstryke+V+Inf` => `hudstrye`
* `hudstryke` has no analyses either

**spytte** failures:

* `spytte+V+Inf` => `spytt`
* `spytte` has no analyses either

**hurgle** failures:

* `hurgle+V+Inf` => `hurgl`
* `hurgle` has no analyses either

**dasse** failures:

* `dasse+V+Inf` => `dass`
* `dasse` has no analyses either

**loppe** failures:

* `loppe+V+Inf` => `lopp`
* `loppe` has no analyses either

**parallellskuve** failures:

* `parallellskuve+V+Inf` => `parallellskuv`
* `parallellskuve` has no analyses either

**fasinere** failures:

* `fasinere+V+Inf` => `fasiner`
* `fasinere` has no analyses either

**javle** failures:

* `javle+V+Inf` => `javl`
* `javle` has no analyses either

**finsikte** failures:

* `finsikte+V+Inf` => `finsikt`
* `finsikte` has no analyses either

**savle** failures:

* `savle+V+Inf` => `savl`
* `savle` has no analyses either

**sverje** failures:

* `sverje+V+Inf` => `sveje`
* `sverje+V+Inf` => `sveja`
* `sverje` has no analyses either

**trose** failures:

* `trose+V+Inf` => `tros`
* `trose` has no analyses either

**dragse** failures:

* `dragse+V+Inf` => `drags`
* `dragse` has following analyses:
  * `dragsen+A+Neu+Sg+Indef`

**konferere** failures:

* `konferere+V+Inf` => `konferer`
* `konferere` has no analyses either

**misby** failures:

* `misby+V+Inf` does not generate!
* `misby` has no analyses either

**utfylle** failures:

* `utfylle+V+Inf` does not generate!
* `utfylle` has no analyses either

**trekkje** failures:

* `trekkje+V+Inf` does not generate!
* `trekkje` has no analyses either

**yrje** failures:

* `yrje+V+Inf` does not generate!
* `yrje` has following analyses:
  * `yrje+V`

**oppføre** failures:

* `oppføre+V+Inf` => `oppfør`
* `oppføre` has no analyses either

**slisse** failures:

* `slisse+V+Inf` => `sliss`
* `slisse` has no analyses either

**smekke** failures:

* `smekke+V+Inf` => `smekk`
* `smekke` has no analyses either

**tvileggje** failures:

* `tvileggje+V+Inf` does not generate!
* `tvileggje` has no analyses either

**traktere** failures:

* `traktere+V+Inf` => `trakter`
* `traktere` has no analyses either

**avvege** failures:

* `avvege+V+Inf` => `avvea`
* `avvege+V+Inf` => `avvee`
* `avvege` has no analyses either

**etikettere** failures:

* `etikettere+V+Inf` => `etiketter`
* `etikettere` has no analyses either

**pasteurisere** failures:

* `pasteurisere+V+Inf` => `pasteuriser`
* `pasteurisere` has no analyses either

**ete** failures:

* `ete+V+Inf` => `eteee`
* `ete+V+Inf` => `eteea`
* `ete` has following analyses:
  * `eten+A+Neu+Sg+Indef`

**æle** failures:

* `æle+V+Inf` => `æl`
* `æle` has no analyses either

**identifisere** failures:

* `identifisere+V+Inf` => `identifiser`
* `identifisere` has no analyses either

**sludde** failures:

* `sludde+V+Inf` => `sludd`
* `sludde` has no analyses either

**lumpe** failures:

* `lumpe+V+Inf` => `lump`
* `lumpe` has no analyses either

**planke** failures:

* `planke+V+Inf` => `plank`
* `planke` has no analyses either

**mistenke** failures:

* `mistenke+V+Inf` => `mistenk`
* `mistenke` has no analyses either

**rause** failures:

* `rause+V+Inf` => `raus`
* `rause` has following analyses:
  * `raus+A+Pl+Indef`
  * `raus+A+Sg+Def`

**konkurranseutsette** failures:

* `konkurranseutsette+V+Inf` does not generate!
* `konkurranseutsette` has following analyses:
  * `konkurranseutsett+A+A+Sg+Def`
  * `konkurranseutsett+A+A+Pl+Def`
  * `konkurranseutsett+A+A+Pl+Indef`

**gauske** failures:

* `gauske+V+Inf` => `gausk`
* `gauske` has no analyses either

**subsidiere** failures:

* `subsidiere+V+Inf` => `subsidier`
* `subsidiere` has no analyses either

**skripte** failures:

* `skripte+V+Inf` => `skript`
* `skripte` has no analyses either

**prille** failures:

* `prille+V+Inf` => `prill`
* `prille` has no analyses either

**vitje** failures:

* `vitje+V+Inf` => `vitj`
* `vitje` has no analyses either

**kondolere** failures:

* `kondolere+V+Inf` => `kondoler`
* `kondolere` has no analyses either

**ile** failures:

* `ile+V+Inf` => `il`
* `ile` has following analyses:
  * `ill+A+A+Pl+Def`
  * `ill+A+A+Pl+Indef`
  * `ill+A+A+Sg+Def`

**juryere** failures:

* `juryere+V+Inf` => `juryer`
* `juryere` has no analyses either

**inndra** failures:

* `inndra+V+Inf` => `inndrdra`
* `inndra` has no analyses either

**gjennomgranske** failures:

* `gjennomgranske+V+Inf` => `gjennomgransk`
* `gjennomgranske` has no analyses either

**godtake** failures:

* `godtake+V+Inf` => `godttaka`
* `godtake+V+Inf` => `godttake`
* `godtake` has no analyses either

**grunnfeste** failures:

* `grunnfeste+V+Inf` does not generate!
* `grunnfeste` has following analyses:
  * `grunnfest+A+A+Pl+Indef`
  * `grunnfest+A+A+Sg+Def`
  * `grunnfest+A+A+Pl+Def`

**lagdele** failures:

* `lagdele+V+Inf` => `lagdel`
* `lagdele` has no analyses either

**spille** failures:

* `spille+V+Inf` does not generate!
* `spille` has no analyses either

**bjeffe** failures:

* `bjeffe+V+Inf` => `bjeff`
* `bjeffe` has no analyses either

**midtmåle** failures:

* `midtmåle+V+Inf` => `midtmål`
* `midtmåle` has no analyses either

**stege** failures:

* `stege+V+Inf` => `steg`
* `stege` has no analyses either

**tjåke** failures:

* `tjåke+V+Inf` => `tjåk`
* `tjåke` has following analyses:
  * `tjåken+A+Neu+Sg+Indef`

**bule** failures:

* `bule+V+Inf` => `bul`
* `bule` has no analyses either

**miskjenne** failures:

* `miskjenne+V+Inf` does not generate!
* `miskjenne` has no analyses either

**glygge** failures:

* `glygge+V+Inf` does not generate!
* `glygge` has no analyses either

**lorte** failures:

* `lorte+V+Inf` => `lort`
* `lorte` has no analyses either

**beste** failures:

* `beste+V+Inf` does not generate!
* `beste` has following analyses:
  * `best+A+A+Sg+Def`
  * `best+A+A+Pl+Indef`
  * `best+A+A+Pl+Def`

**subsumere** failures:

* `subsumere+V+Inf` => `subsumer`
* `subsumere` has no analyses either

**higre** failures:

* `higre+V+Inf` => `higr`
* `higre` has no analyses either

**lense** failures:

* `lense+V+Inf` => `lens`
* `lense` has following analyses:
  * `lens+A+A+Sg+Def`
  * `lens+A+A+Pl+Def`
  * `lens+A+A+Pl+Indef`

**styrje** failures:

* `styrje+V+Inf` => `styrj`
* `styrje` has no analyses either

**gimpe** failures:

* `gimpe+V+Inf` => `gimp`
* `gimpe` has no analyses either

**ylle** failures:

* `ylle+V+Inf` does not generate!
* `ylle` has no analyses either

**kompe** failures:

* `kompe+V+Inf` => `komp`
* `kompe` has no analyses either

**kulsle** failures:

* `kulsle+V+Inf` => `kulsl`
* `kulsle` has no analyses either

**ved** failures:

* `ved+V+Inf` does not generate!
* `ved` has following analyses:
  * `vede+V+Imprt`
  * `ved+N+N+Msc+Sg+Indef`
  * `ved+Pr`
  * `vede+V+Inf`

**smugrøykje** failures:

* `smugrøykje+V+Inf` does not generate!
* `smugrøykje` has no analyses either

**internalisere** failures:

* `internalisere+V+Inf` => `internaliser`
* `internalisere` has no analyses either

**geivle** failures:

* `geivle+V+Inf` => `geivl`
* `geivle` has no analyses either

**krumpe** failures:

* `krumpe+V+Inf` => `krump`
* `krumpe` has no analyses either

**rådgjeve** failures:

* `rådgjeve+V+Inf` => `rådggjeva`
* `rådgjeve+V+Inf` => `rådggjeve`
* `rådgjeve` has no analyses either

**sikre** failures:

* `sikre+V+Inf` => `sikr`
* `sikre` has no analyses either

**glense** failures:

* `glense+V+Inf` => `glens`
* `glense` has no analyses either

**tilpasse** failures:

* `tilpasse+V+Inf` => `tilpass`
* `tilpasse` has no analyses either

**individualisere** failures:

* `individualisere+V+Inf` => `individualiser`
* `individualisere` has no analyses either

**frekventere** failures:

* `frekventere+V+Inf` => `frekventer`
* `frekventere` has no analyses either

**kike** failures:

* `kike+V+Inf` => `k`
* `kike` has no analyses either

**fritake** failures:

* `fritake+V+Inf` => `frittaka`
* `fritake+V+Inf` => `frittake`
* `fritake` has no analyses either

**renonsere** failures:

* `renonsere+V+Inf` => `renonser`
* `renonsere` has no analyses either

**gjennomleve** failures:

* `gjennomleve+V+Inf` does not generate!
* `gjennomleve` has no analyses either

**respektere** failures:

* `respektere+V+Inf` => `respekter`
* `respektere` has no analyses either

**innrullere** failures:

* `innrullere+V+Inf` => `innruller`
* `innrullere` has no analyses either

**kjekle** failures:

* `kjekle+V+Inf` => `kjekl`
* `kjekle` has no analyses either

**rakke** failures:

* `rakke+V+Inf` => `rakk`
* `rakke` has no analyses either

**ekskommunisere** failures:

* `ekskommunisere+V+Inf` => `ekskommuniser`
* `ekskommunisere` has no analyses either

**rykkje** failures:

* `rykkje+V+Inf` does not generate!
* `rykkje` has no analyses either

**ekspedere** failures:

* `ekspedere+V+Inf` => `ekspeder`
* `ekspedere` has no analyses either

**demme** failures:

* `demme+V+Inf` does not generate!
* `demme` has no analyses either

**seife** failures:

* `seife+V+Inf` => `seif`
* `seife` has following analyses:
  * `seif+A+Sg+Def`
  * `seif+A+Pl+Indef`

**knerte** failures:

* `knerte+V+Inf` => `knert`
* `knerte` has no analyses either

**intimidere** failures:

* `intimidere+V+Inf` => `intimider`
* `intimidere` has no analyses either

**vrangstrikke** failures:

* `vrangstrikke+V+Inf` => `vrangstrikk`
* `vrangstrikke` has no analyses either

**lyte** failures:

* `lyte+V+Inf` does not generate!
* `lyte` has no analyses either

**sublimere** failures:

* `sublimere+V+Inf` => `sublimer`
* `sublimere` has no analyses either

**hjule** failures:

* `hjule+V+Inf` => `hjul`
* `hjule` has no analyses either

**fore** failures:

* `fore+V+Inf` => `for`
* `fore` has following analyses:
  * `for+A+Sg+Def`
  * `forete+V+Imprt`
  * `for+A+Pl+Indef`
  * `forete+V+Ind+Prs`

**drite** failures:

* `drite+V+Inf` => `dria`
* `drite+V+Inf` => `drie`
* `drite` has following analyses:
  * `driten+A+Neu+Sg+Indef`

**hetne** failures:

* `hetne+V+Inf` => `hetn`
* `hetne` has no analyses either

**lindre** failures:

* `lindre+V+Inf` => `lindr`
* `lindre` has no analyses either

**gråne** failures:

* `gråne+V+Inf` => `grån`
* `gråne` has following analyses:
  * `gråte+V+Adj+Bu+Eint`
  * `gråte+V+Adj+Fl`

**malte** failures:

* `malte+V+Inf` => `malt`
* `malte` has no analyses either

**bøtleggje** failures:

* `bøtleggje+V+Inf` does not generate!
* `bøtleggje` has no analyses either

**egalisere** failures:

* `egalisere+V+Inf` => `egaliser`
* `egalisere` has no analyses either

**retne** failures:

* `retne+V+Inf` => `retn`
* `retne` has no analyses either

**nærme** failures:

* `nærme+V+Inf` => `nærm`
* `nærme` has no analyses either

**mase** failures:

* `mase+V+Inf` => `mas`
* `mase` has following analyses:
  * `masen+A+Neu+Sg+Indef`

**tilleggje** failures:

* `tilleggje+V+Inf` does not generate!
* `tilleggje` has no analyses either

**snerke** failures:

* `snerke+V+Inf` => `snerk`
* `snerke` has no analyses either

**dratte** failures:

* `dratte+V+Inf` => `dratt`
* `dratte` has following analyses:
  * `dratt+A+A+Pl+Indef`
  * `dratt+A+A+Pl+Def`
  * `dratt+A+A+Sg+Def`

**nate** failures:

* `nate+V+Inf` => `nat`
* `nate` has following analyses:
  * `naten+A+Neu+Sg+Indef`
  * `nater+A+A+Pl+Def`
  * `nater+A+A+Sg+Def`
  * `nater+A+A+Pl+Indef`

**samstavast** failures:

* `samstavast+V+Inf` => `samstavastast`
* `samstavast` has no analyses either

**age** failures:

* `age+V+Inf` => `ag`
* `age` has no analyses either

**sjøsetje** failures:

* `sjøsetje+V+Inf` does not generate!
* `sjøsetje` has no analyses either

**fryselagre** failures:

* `fryselagre+V+Inf` => `fryselagr`
* `fryselagre` has no analyses either

**sone** failures:

* `sone+V+Inf` => `son`
* `sone` has following analyses:
  * `suge+V+Adj+Fl`
  * `suge+V+Adj+Bu+Eint`
  * `sove+V+Adj+Bu+Eint`
  * `syde+V+Adj+Fl`
  * `sove+V+Adj+Fl`
  * `syde+V+Adj+Bu+Eint`

**sutre** failures:

* `sutre+V+Inf` => `sutr`
* `sutre` has no analyses either

**fôrhauste** failures:

* `fôrhauste+V+Inf` => `fôrhaust`
* `fôrhauste` has no analyses either

**pauke** failures:

* `pauke+V+Inf` => `pauk`
* `pauke` has no analyses either

**informere** failures:

* `informere+V+Inf` => `informer`
* `informere` has no analyses either

**ringe** failures:

* `ringe+V+Inf` => `ring`
* `ringe` has following analyses:
  * `ring+A+Pl+Indef`
  * `ring+A+Sg+Def`

**fele** failures:

* `fele+V+Inf` => `fel`
* `fele` has following analyses:
  * `fellen+A+Neu+Sg+Indef`

**dekomponere** failures:

* `dekomponere+V+Inf` => `dekomponer`
* `dekomponere` has no analyses either

**rokere** failures:

* `rokere+V+Inf` => `roker`
* `rokere` has no analyses either

**innstifte** failures:

* `innstifte+V+Inf` => `innstift`
* `innstifte` has no analyses either

**skli** failures:

* `skli+V+Inf` does not generate!
* `skli` has following analyses:
  * `sklie+N+N+Fem+Sg+Indef`

**falsifisere** failures:

* `falsifisere+V+Inf` => `falsifiser`
* `falsifisere` has no analyses either

**nøffe** failures:

* `nøffe+V+Inf` => `nøff`
* `nøffe` has no analyses either

**kjee** failures:

* `kjee+V+Inf` => `kje`
* `kjee` has no analyses either

**sengje** failures:

* `sengje+V+Inf` => `seng`
* `sengje` has no analyses either

**transferere** failures:

* `transferere+V+Inf` => `transferer`
* `transferere` has no analyses either

**aspirere** failures:

* `aspirere+V+Inf` => `aspirer`
* `aspirere` has no analyses either

**likke** failures:

* `likke+V+Inf` => `likk`
* `likke` has no analyses either

**grunde** failures:

* `grunde+V+Inf` => `grund`
* `grunde` has no analyses either

**poste** failures:

* `poste+V+Inf` => `post`
* `poste` has no analyses either

**tauvre** failures:

* `tauvre+V+Inf` => `tauvr`
* `tauvre` has no analyses either

**svirpe** failures:

* `svirpe+V+Inf` => `svirp`
* `svirpe` has following analyses:
  * `svirp+A+Pl+Indef`
  * `svirp+A+Sg+Def`

**handsetje** failures:

* `handsetje+V+Inf` does not generate!
* `handsetje` has no analyses either

**effektivisere** failures:

* `effektivisere+V+Inf` => `effektiviser`
* `effektivisere` has no analyses either

**utbetale** failures:

* `utbetale+V+Inf` does not generate!
* `utbetale` has no analyses either

**ise** failures:

* `ise+V+Inf` does not generate!
* `ise` has no analyses either

**bråstoppe** failures:

* `bråstoppe+V+Inf` => `bråstopp`
* `bråstoppe` has no analyses either

**svæle** failures:

* `svæle+V+Inf` => `svæl`
* `svæle` has no analyses either

**lime** failures:

* `lime+V+Inf` does not generate!
* `lime` has no analyses either

**journalisere** failures:

* `journalisere+V+Inf` => `journaliser`
* `journalisere` has no analyses either

**fjakle** failures:

* `fjakle+V+Inf` => `fjakl`
* `fjakle` has no analyses either

**beise** failures:

* `beise+V+Inf` => `beis`
* `beise` has no analyses either

**glorifisere** failures:

* `glorifisere+V+Inf` => `glorifiser`
* `glorifisere` has no analyses either

**erverve** failures:

* `erverve+V+Inf` => `erverv`
* `erverve` has no analyses either

**redoble** failures:

* `redoble+V+Inf` => `redobl`
* `redoble` has no analyses either

**nedbetale** failures:

* `nedbetale+V+Inf` does not generate!
* `nedbetale` has no analyses either

**jenke** failures:

* `jenke+V+Inf` => `jenk`
* `jenke` has no analyses either

**make** failures:

* `make+V+Inf` => `mak`
* `make` has following analyses:
  * `mak+A+Sg+Def`
  * `mak+A+Pl+Indef`

**favorisere** failures:

* `favorisere+V+Inf` => `favoriser`
* `favorisere` has no analyses either

**kvitne** failures:

* `kvitne+V+Inf` => `kvitn`
* `kvitne` has no analyses either

**rise** failures:

* `rise+V+Inf` => `ris`
* `rise` has no analyses either

**vårkunne** failures:

* `vårkunne+V+Inf` => `vårkunn`
* `vårkunne` has no analyses either

**brekte** failures:

* `brekte+V+Inf` => `brekt`
* `brekte` has no analyses either

**grøne** failures:

* `grøne+V+Inf` => `grøn`
* `grøne` has following analyses:
  * `grøn+A+Pl+Indef`
  * `grøn+A+Sg+Def`

**vave** failures:

* `vave+V+Inf` => `vav`
* `vave` has no analyses either

**ripostere** failures:

* `ripostere+V+Inf` => `riposter`
* `ripostere` has no analyses either

**nynne** failures:

* `nynne+V+Inf` => `nynn`
* `nynne` has no analyses either

**bolle** failures:

* `bolle+V+Inf` => `boll`
* `bolle` has no analyses either

**trote** failures:

* `trote+V+Inf` => `trot`
* `trote` has following analyses:
  * `troten+A+Neu+Sg+Indef`

**satisfisere** failures:

* `satisfisere+V+Inf` => `satisfiser`
* `satisfisere` has no analyses either

**skråsette** failures:

* `skråsette+V+Inf` does not generate!
* `skråsette` has no analyses either

**sprelle** failures:

* `sprelle+V+Inf` does not generate!
* `sprelle` has no analyses either

**simulere** failures:

* `simulere+V+Inf` => `simuler`
* `simulere` has no analyses either

**rene** failures:

* `rene+V+Inf` => `ren`
* `rene` has following analyses:
  * `rand+N+N+Fem+Pl+Def`
  * `reke+V+Adj+Fl`
  * `reke+V+Adj+Bu+Eint`

**modifisere** failures:

* `modifisere+V+Inf` => `modifiser`
* `modifisere` has no analyses either

**fråfalle** failures:

* `fråfalle+V+Inf` => `fråfaa`
* `fråfalle+V+Inf` => `fråfae`
* `fråfalle` has no analyses either

**avtinge** failures:

* `avtinge+V+Inf` => `avting`
* `avtinge` has no analyses either

**bagatellisere** failures:

* `bagatellisere+V+Inf` => `bagatelliser`
* `bagatellisere` has no analyses either

**salve** failures:

* `salve+V+Inf` => `salv`
* `salve` has no analyses either

**filtrere** failures:

* `filtrere+V+Inf` => `filtrer`
* `filtrere` has no analyses either

**avle** failures:

* `avle+V+Inf` => `avl`
* `avle` has no analyses either

**dildre** failures:

* `dildre+V+Inf` => `dildr`
* `dildre` has no analyses either

**smulne** failures:

* `smulne+V+Inf` => `smuln`
* `smulne` has no analyses either

**arre** failures:

* `arre+V+Inf` => `arr`
* `arre` has no analyses either

**narre** failures:

* `narre+V+Inf` => `narr`
* `narre` has no analyses either

**brotne** failures:

* `brotne+V+Inf` => `brotn`
* `brotne` has following analyses:
  * `broten+A+Pl+Indef`
  * `broten+A+Sg+Def`

**otte** failures:

* `otte+V+Inf` => `ott`
* `otte` has no analyses either

**kròkne** failures:

* `kròkne+V+Inf` => `kròkn`
* `kròkne` has following analyses:
  * `kròken+A+Pl+Indef`
  * `kròken+A+Sg+Def`

**nare** failures:

* `nare+V+Inf` => `nar`
* `nare` has no analyses either

**skyldgjeve** failures:

* `skyldgjeve+V+Inf` => `skyldggjeve`
* `skyldgjeve+V+Inf` => `skyldggjeva`
* `skyldgjeve` has following analyses:
  * `skyldgjeven+A+Neu+Sg+Indef`

**strende** failures:

* `strende+V+Inf` => `strend`
* `strende` has no analyses either

**blidge** failures:

* `blidge+V+Inf` => `blidg`
* `blidge` has no analyses either

**gauve** failures:

* `gauve+V+Inf` => `gauv`
* `gauve` has following analyses:
  * `gauv+A+Pl+Indef`
  * `gauv+A+Sg+Def`

**ovre** failures:

* `ovre+V+Inf` => `ovr`
* `ovre` has no analyses either

**farge** failures:

* `farge+V+Inf` => `farg`
* `farge` has no analyses either

**desintegrere** failures:

* `desintegrere+V+Inf` => `desintegrer`
* `desintegrere` has no analyses either

**jamme** failures:

* `jamme+V+Inf` => `jam`
* `jamme` has no analyses either

**kandisere** failures:

* `kandisere+V+Inf` => `kandiser`
* `kandisere` has no analyses either

**balsamere** failures:

* `balsamere+V+Inf` => `balsamer`
* `balsamere` has no analyses either

**steike** failures:

* `steike+V+Inf` => `steik`
* `steike` has no analyses either

**snurkle** failures:

* `snurkle+V+Inf` => `snurkl`
* `snurkle` has no analyses either

**varmheve** failures:

* `varmheve+V+Inf` does not generate!
* `varmheve` has no analyses either

**korslegge** failures:

* `korslegge+V+Inf` does not generate!
* `korslegge` has no analyses either

**kravellbygge** failures:

* `kravellbygge+V+Inf` does not generate!
* `kravellbygge` has no analyses either

**bortføre** failures:

* `bortføre+V+Inf` => `bortfør`
* `bortføre` has no analyses either

**snevre** failures:

* `snevre+V+Inf` => `snevr`
* `snevre` has no analyses either

**handdra** failures:

* `handdra+V+Inf` => `handdrdra`
* `handdra` has no analyses either

**demoralisere** failures:

* `demoralisere+V+Inf` => `demoraliser`
* `demoralisere` has no analyses either

**skumpiske** failures:

* `skumpiske+V+Inf` => `skumpisk`
* `skumpiske` has no analyses either

**myse** failures:

* `myse+V+Inf` => `mys`
* `myse` has no analyses either

**trotte** failures:

* `trotte+V+Inf` => `trott`
* `trotte` has no analyses either

**esje** failures:

* `esje+V+Inf` => `esj`
* `esje` has no analyses either

**fullende** failures:

* `fullende+V+Inf` => `fullend`
* `fullende` has no analyses either

**handgjeve** failures:

* `handgjeve+V+Inf` => `handggjeve`
* `handgjeve+V+Inf` => `handggjeva`
* `handgjeve` has no analyses either

**brike** failures:

* `brike+V+Inf` => `brik`
* `brike` has following analyses:
  * `briken+A+Neu+Sg+Indef`

**ambulere** failures:

* `ambulere+V+Inf` => `ambuler`
* `ambulere` has no analyses either

**illgråte** failures:

* `illgråte+V+Inf` => `illgråa`
* `illgråte+V+Inf` => `illgråe`
* `illgråte` has no analyses either

**klovne** failures:

* `klovne+V+Inf` => `klovn`
* `klovne` has following analyses:
  * `kloven+A+Pl+Indef`
  * `kloven+A+Sg+Def`

**kaldfline** failures:

* `kaldfline+V+Inf` => `kaldflia`
* `kaldfline+V+Inf` => `kaldflie`
* `kaldfline` has following analyses:
  * `kaldfline+V+Adj+Bu+Eint`
  * `kaldfline+V+Adj+Fl`

**kondisjonere** failures:

* `kondisjonere+V+Inf` => `kondisjoner`
* `kondisjonere` has no analyses either

**resignere** failures:

* `resignere+V+Inf` => `resigner`
* `resignere` has no analyses either

**fragmentere** failures:

* `fragmentere+V+Inf` => `fragmenter`
* `fragmentere` has no analyses either

**avtake** failures:

* `avtake+V+Inf` => `avttaka`
* `avtake+V+Inf` => `avttake`
* `avtake` has no analyses either

**lakre** failures:

* `lakre+V+Inf` => `lakr`
* `lakre` has no analyses either

**skjenne** failures:

* `skjenne+V+Inf` does not generate!
* `skjenne` has no analyses either

**germanisere** failures:

* `germanisere+V+Inf` => `germaniser`
* `germanisere` has no analyses either

**krode** failures:

* `krode+V+Inf` => `krod`
* `krode` has no analyses either

**purle** failures:

* `purle+V+Inf` => `purl`
* `purle` has no analyses either

**stue** failures:

* `stue+V+Inf` => `stu`
* `stue` has following analyses:
  * `stikke+V+PrfPrc`
  * `stikke+V+Adj+Nøyt+Ub+Eint`

**forsvare** failures:

* `forsvare+V+Inf` does not generate!
* `forsvare` has no analyses either

**gjette** failures:

* `gjette+V+Inf` => `gjett`
* `gjette` has following analyses:
  * `gjete+V+PrfPrc`

**pele** failures:

* `pele+V+Inf` => `pel`
* `pele` has no analyses either

**atterskipe** failures:

* `atterskipe+V+Inf` => `atterskip`
* `atterskipe` has no analyses either

**skyldsetje** failures:

* `skyldsetje+V+Inf` does not generate!
* `skyldsetje` has no analyses either

**vanære** failures:

* `vanære+V+Inf` => `vanær`
* `vanære` has no analyses either

**bøke** failures:

* `bøke+V+Inf` => `bøk`
* `bøke` has no analyses either

**underminere** failures:

* `underminere+V+Inf` => `underminer`
* `underminere` has no analyses either

**nigle** failures:

* `nigle+V+Inf` => `nigl`
* `nigle` has no analyses either

**frilanse** failures:

* `frilanse+V+Inf` => `frilans`
* `frilanse` has following analyses:
  * `frilans+A+A+Sg+Def`
  * `frilans+A+A+Pl+Def`
  * `frilans+A+A+Pl+Indef`

**lirke** failures:

* `lirke+V+Inf` => `lirk`
* `lirke` has no analyses either

**avregulere** failures:

* `avregulere+V+Inf` => `avreguler`
* `avregulere` has no analyses either

**fortrolle** failures:

* `fortrolle+V+Inf` => `fortroll`
* `fortrolle` has no analyses either

**revurdere** failures:

* `revurdere+V+Inf` => `revurder`
* `revurdere` has no analyses either

**oppbevare** failures:

* `oppbevare+V+Inf` => `oppbevar`
* `oppbevare` has no analyses either

**snurre** failures:

* `snurre+V+Inf` => `snurr`
* `snurre` has no analyses either

**forne** failures:

* `forne+V+Inf` => `forn`
* `forne` has following analyses:
  * `forn+A+Sg+Def`
  * `forn+A+Pl+Indef`

**svekkje** failures:

* `svekkje+V+Inf` does not generate!
* `svekkje` has no analyses either

**reingjere** failures:

* `reingjere+V+Inf` does not generate!
* `reingjere` has no analyses either

**kvinkre** failures:

* `kvinkre+V+Inf` => `kvinkr`
* `kvinkre` has no analyses either

**chatte** failures:

* `chatte+V+Inf` => `chatt`
* `chatte` has no analyses either

**etterfylle** failures:

* `etterfylle+V+Inf` does not generate!
* `etterfylle` has no analyses either

**praie** failures:

* `praie+V+Inf` => `prai`
* `praie` has no analyses either

**iføre** failures:

* `iføre+V+Inf` does not generate!
* `iføre` has no analyses either

**knusle** failures:

* `knusle+V+Inf` => `knusl`
* `knusle` has no analyses either

**svalne** failures:

* `svalne+V+Inf` => `svaln`
* `svalne` has no analyses either

**sveivle** failures:

* `sveivle+V+Inf` => `sveivl`
* `sveivle` has no analyses either

**utvandre** failures:

* `utvandre+V+Inf` => `utvandr`
* `utvandre` has no analyses either

**forhekse** failures:

* `forhekse+V+Inf` => `forheks`
* `forhekse` has no analyses either

**lurre** failures:

* `lurre+V+Inf` => `lurr`
* `lurre` has no analyses either

**botanisere** failures:

* `botanisere+V+Inf` => `botaniser`
* `botanisere` has no analyses either

**tvingle** failures:

* `tvingle+V+Inf` => `tvingl`
* `tvingle` has no analyses either

**gangast** failures:

* `gangast+V+Inf` => `ggangast`
* `gangast` has no analyses either

**drosle** failures:

* `drosle+V+Inf` => `drosl`
* `drosle` has no analyses either

**knekkje** failures:

* `knekkje+V+Inf` does not generate!
* `knekkje` has no analyses either

**rundstele** failures:

* `rundstele+V+Inf` => `rundstee`
* `rundstele+V+Inf` => `rundstea`
* `rundstele` has no analyses either

**ironisere** failures:

* `ironisere+V+Inf` => `ironiser`
* `ironisere` has no analyses either

**fordøye** failures:

* `fordøye+V+Inf` does not generate!
* `fordøye` has no analyses either

**samråde** failures:

* `samråde+V+Inf` does not generate!
* `samråde` has no analyses either

**fra** failures:

* `fra+V+Inf` does not generate!
* `fra` has no analyses either

**tilhevde** failures:

* `tilhevde+V+Inf` => `tilhevd`
* `tilhevde` has no analyses either

**knøse** failures:

* `knøse+V+Inf` => `knøs`
* `knøse` has no analyses either

**pilotere** failures:

* `pilotere+V+Inf` => `piloter`
* `pilotere` has no analyses either

**rygge** failures:

* `rygge+V+Inf` => `ryg`
* `rygge` has no analyses either

**bivuakkere** failures:

* `bivuakkere+V+Inf` => `bivuakker`
* `bivuakkere` has no analyses either

**hòpe** failures:

* `hòpe+V+Inf` => `hòp`
* `hòpe` has no analyses either

**lemje** failures:

* `lemje+V+Inf` does not generate!
* `lemje` has no analyses either

**skingle** failures:

* `skingle+V+Inf` => `skingl`
* `skingle` has no analyses either

**gjegne** failures:

* `gjegne+V+Inf` does not generate!
* `gjegne` has following analyses:
  * `gjegn+A+Pl+Indef`
  * `gjegn+A+Sg+Def`

**ljome** failures:

* `ljome+V+Inf` => `ljom`
* `ljome` has no analyses either

**glimte** failures:

* `glimte+V+Inf` => `glimt`
* `glimte` has no analyses either

**lykkønske** failures:

* `lykkønske+V+Inf` => `lykkønsk`
* `lykkønske` has no analyses either

**håle** failures:

* `håle+V+Inf` does not generate!
* `håle` has following analyses:
  * `hål+A+Pl+Indef`
  * `hål+A+Sg+Def`

**gauke** failures:

* `gauke+V+Inf` => `gauk`
* `gauke` has no analyses either

**bringe** failures:

* `bringe+V+Inf` => `bring`
* `bringe` has no analyses either

**argast** failures:

* `argast+V+Inf` => `argastast`
* `argast` has following analyses:
  * `arg+A+Superl+Indef`

**knue** failures:

* `knue+V+Inf` => `knu`
* `knue` has no analyses either

**rapse** failures:

* `rapse+V+Inf` => `raps`
* `rapse` has no analyses either

**håve** failures:

* `håve+V+Inf` => `håv`
* `håve` has no analyses either

**stupregne** failures:

* `stupregne+V+Inf` does not generate!
* `stupregne` has no analyses either

**fuske** failures:

* `fuske+V+Inf` => `fusk`
* `fuske` has no analyses either

**svime** failures:

* `svime+V+Inf` => `svim`
* `svime` has following analyses:
  * `svim+A+Sg+Def`
  * `svim+A+Pl+Indef`
  * `svimmel+A+A+Pl+Indef`
  * `svimmel+A+A+Sg+Def`
  * `svimmel+A+A+Pl+Def`

**forkludre** failures:

* `forkludre+V+Inf` => `forkludr`
* `forkludre` has no analyses either

**være** failures:

* `være+V+Inf` => `vær`
* `være` has following analyses:
  * `vær+A+Sg+Def`
  * `vær+A+Pl+Indef`

**vektlegge** failures:

* `vektlegge+V+Inf` does not generate!
* `vektlegge` has no analyses either

**kikke** failures:

* `kikke+V+Inf` => `kikk`
* `kikke` has no analyses either

**mentalgranske** failures:

* `mentalgranske+V+Inf` => `mentalgransk`
* `mentalgranske` has no analyses either

**meisle** failures:

* `meisle+V+Inf` => `meisl`
* `meisle` has no analyses either

**skumkøyre** failures:

* `skumkøyre+V+Inf` does not generate!
* `skumkøyre` has no analyses either

**diske** failures:

* `diske+V+Inf` => `disk`
* `diske` has no analyses either

**apokopere** failures:

* `apokopere+V+Inf` => `apokoper`
* `apokopere` has no analyses either

**radbrekke** failures:

* `radbrekke+V+Inf` => `radbreka`
* `radbrekke+V+Inf` => `radbreke`
* `radbrekke` has no analyses either

**inklinere** failures:

* `inklinere+V+Inf` => `inkliner`
* `inklinere` has no analyses either

**marmorere** failures:

* `marmorere+V+Inf` => `marmorer`
* `marmorere` has no analyses either

**kontorisere** failures:

* `kontorisere+V+Inf` => `kontoriser`
* `kontorisere` has no analyses either

**skorsette** failures:

* `skorsette+V+Inf` does not generate!
* `skorsette` has no analyses either

**bagle** failures:

* `bagle+V+Inf` => `bagl`
* `bagle` has no analyses either

**abdisere** failures:

* `abdisere+V+Inf` => `abdiser`
* `abdisere` has no analyses either

**plenarhandsame** failures:

* `plenarhandsame+V+Inf` => `plenarhandsam`
* `plenarhandsame` has no analyses either

**svåsse** failures:

* `svåsse+V+Inf` => `svåss`
* `svåsse` has no analyses either

**definere** failures:

* `definere+V+Inf` => `definer`
* `definere` has no analyses either

**lokke** failures:

* `lokke+V+Inf` => `lokk`
* `lokke` has no analyses either

**pote** failures:

* `pote+V+Inf` => `pot`
* `pote` has no analyses either

**inspisere** failures:

* `inspisere+V+Inf` => `inspiser`
* `inspisere` has no analyses either

**trøytte** failures:

* `trøytte+V+Inf` => `trøytt`
* `trøytte` has following analyses:
  * `trøytt+A+A+Sg+Def`
  * `trøytt+A+A+Pl+Indef`
  * `trøytt+A+A+Pl+Def`
  * `trøyte+V+PrfPrc`

**akkompagnere** failures:

* `akkompagnere+V+Inf` => `akkompagner`
* `akkompagnere` has no analyses either

**bufse** failures:

* `bufse+V+Inf` => `bufs`
* `bufse` has no analyses either

**krekse** failures:

* `krekse+V+Inf` => `kreks`
* `krekse` has following analyses:
  * `kreksen+A+Neu+Sg+Indef`

**pokulere** failures:

* `pokulere+V+Inf` => `pokuler`
* `pokulere` has no analyses either

**uttrykke** failures:

* `uttrykke+V+Inf` does not generate!
* `uttrykke` has no analyses either

**advisere** failures:

* `advisere+V+Inf` => `adviser`
* `advisere` has no analyses either

**fitle** failures:

* `fitle+V+Inf` => `fitl`
* `fitle` has no analyses either

**festne** failures:

* `festne+V+Inf` => `festn`
* `festne` has no analyses either

**allergiteste** failures:

* `allergiteste+V+Inf` => `allergitest`
* `allergiteste` has no analyses either

**telegrafere** failures:

* `telegrafere+V+Inf` => `telegrafer`
* `telegrafere` has no analyses either

**skralle** failures:

* `skralle+V+Inf` => `skrall`
* `skralle` has no analyses either

**heroisere** failures:

* `heroisere+V+Inf` => `heroiser`
* `heroisere` has no analyses either

**fluse** failures:

* `fluse+V+Inf` => `flus`
* `fluse` has following analyses:
  * `flus+A+Pl+Indef`
  * `flus+A+Sg+Def`

**skeivle** failures:

* `skeivle+V+Inf` => `skeivl`
* `skeivle` has no analyses either

**skumast** failures:

* `skumast+V+Inf` => `skumastast`
* `skumast` has following analyses:
  * `skum+A+Superl+Indef`
  * `skummel+A+A+Superl+Indef`

**kappast** failures:

* `kappast+V+Inf` => `kappastast`
* `kappast` has no analyses either

**flaume** failures:

* `flaume+V+Inf` => `flaum`
* `flaume` has no analyses either

**påbyde** failures:

* `påbyde+V+Inf` does not generate!
* `påbyde` has following analyses:
  * `påbydd+A+A+Pl+Indef`
  * `påbydd+A+A+Sg+Def`
  * `påbydd+A+A+Pl+Def`

**introdusere** failures:

* `introdusere+V+Inf` => `introduser`
* `introdusere` has no analyses either

**hylje** failures:

* `hylje+V+Inf` => `hyl`
* `hylje` has no analyses either

**blankslite** failures:

* `blankslite+V+Inf` => `blankslia`
* `blankslite+V+Inf` => `blankslie`
* `blankslite` has no analyses either

**løyve** failures:

* `løyve+V+Inf` does not generate!
* `løyve` has no analyses either

**marsjere** failures:

* `marsjere+V+Inf` => `marsjer`
* `marsjere` has no analyses either

**krosslegge** failures:

* `krosslegge+V+Inf` does not generate!
* `krosslegge` has no analyses either

**vinkle** failures:

* `vinkle+V+Inf` => `vinkl`
* `vinkle` has no analyses either

**turrdestillere** failures:

* `turrdestillere+V+Inf` => `turrdestiller`
* `turrdestillere` has no analyses either

**forpuppe** failures:

* `forpuppe+V+Inf` => `forpupp`
* `forpuppe` has no analyses either

**utykke** failures:

* `utykke+V+Inf` does not generate!
* `utykke` has no analyses either

**nennast** failures:

* `nennast+V+Inf` => `nenast`
* `nennast` has no analyses either

**akte** failures:

* `akte+V+Inf` => `akt`
* `akte` has no analyses either

**etterlikne** failures:

* `etterlikne+V+Inf` => `etterlikn`
* `etterlikne` has no analyses either

**fortale** failures:

* `fortale+V+Inf` does not generate!
* `fortale` has no analyses either

**snaugnage** failures:

* `snaugnage+V+Inf` does not generate!
* `snaugnage` has no analyses either

**longere** failures:

* `longere+V+Inf` => `longer`
* `longere` has no analyses either

**båle** failures:

* `båle+V+Inf` => `bål`
* `båle` has no analyses either

**finansiere** failures:

* `finansiere+V+Inf` => `finansier`
* `finansiere` has no analyses either

**leire** failures:

* `leire+V+Inf` => `leir`
* `leire` has no analyses either

**godprate** failures:

* `godprate+V+Inf` => `godprat`
* `godprate` has no analyses either

**standardisere** failures:

* `standardisere+V+Inf` => `standardiser`
* `standardisere` has no analyses either

**syngje** failures:

* `syngje+V+Inf` => `syje`
* `syngje+V+Inf` => `syja`
* `syngje` has no analyses either

**stomle** failures:

* `stomle+V+Inf` => `stoml`
* `stomle` has no analyses either

**myke** failures:

* `myke+V+Inf` => `myk`
* `myke` has no analyses either

**klårlegge** failures:

* `klårlegge+V+Inf` does not generate!
* `klårlegge` has no analyses either

**verpe** failures:

* `verpe+V+Inf` => `vee`
* `verpe+V+Inf` => `vea`
* `verpe` has no analyses either

**visne** failures:

* `visne+V+Inf` => `visn`
* `visne` has following analyses:
  * `visen+A+Sg+Def`
  * `vissen+A+Sg+Def`
  * `vissen+A+Pl+Indef`
  * `visen+A+Pl+Indef`

**travestere** failures:

* `travestere+V+Inf` => `travester`
* `travestere` has no analyses either

**ørte** failures:

* `ørte+V+Inf` => `ørt`
* `ørte` has no analyses either

**dykje** failures:

* `dykje+V+Inf` does not generate!
* `dykje` has no analyses either

**gyltre** failures:

* `gyltre+V+Inf` => `gyltr`
* `gyltre` has no analyses either

**nuppe** failures:

* `nuppe+V+Inf` => `nupp`
* `nuppe` has no analyses either

**oppgjeve** failures:

* `oppgjeve+V+Inf` => `oppggjeva`
* `oppgjeve+V+Inf` => `oppggjeve`
* `oppgjeve` has following analyses:
  * `oppgjeven+A+Neu+Sg+Indef`

**smide** failures:

* `smide+V+Inf` does not generate!
* `smide` has no analyses either

**didre** failures:

* `didre+V+Inf` => `didr`
* `didre` has no analyses either

**rakne** failures:

* `rakne+V+Inf` => `rakn`
* `rakne` has no analyses either

**tagge** failures:

* `tagge+V+Inf` => `tagg`
* `tagge` has no analyses either

**gnistre** failures:

* `gnistre+V+Inf` => `gnistr`
* `gnistre` has no analyses either

**høgakte** failures:

* `høgakte+V+Inf` => `høgakt`
* `høgakte` has no analyses either

**skrokne** failures:

* `skrokne+V+Inf` => `skrokn`
* `skrokne` has following analyses:
  * `skrokken+A+Pl+Indef`
  * `skrokken+A+Sg+Def`

**appretere** failures:

* `appretere+V+Inf` => `appreter`
* `appretere` has no analyses either

**slingre** failures:

* `slingre+V+Inf` => `slingr`
* `slingre` has no analyses either

**hanke** failures:

* `hanke+V+Inf` => `hank`
* `hanke` has following analyses:
  * `hank+A+Sg+Def`
  * `hank+A+Pl+Indef`

**sege** failures:

* `sege+V+Inf` => `seg`
* `sege` has no analyses either

**strinte** failures:

* `strinte+V+Inf` => `strint`
* `strinte` has no analyses either

**lukkønskje** failures:

* `lukkønskje+V+Inf` does not generate!
* `lukkønskje` has no analyses either

**syne** failures:

* `syne+V+Inf` => `syn`
* `syne` has no analyses either

**rotte** failures:

* `rotte+V+Inf` => `rott`
* `rotte` has no analyses either

**markere** failures:

* `markere+V+Inf` => `marker`
* `markere` has no analyses either

**fjåge** failures:

* `fjåge+V+Inf` => `fjåg`
* `fjåge` has following analyses:
  * `fjåg+A+Sg+Def`
  * `fjåg+A+Pl+Indef`

**kappete** failures:

* `kappete+V+Inf` => `kappee`
* `kappete+V+Inf` => `kappea`
* `kappete` has no analyses either

**førespegle** failures:

* `førespegle+V+Inf` => `førespegl`
* `førespegle` has no analyses either

**konfrontere** failures:

* `konfrontere+V+Inf` => `konfronter`
* `konfrontere` has no analyses either

**bade** failures:

* `bade+V+Inf` => `bad`
* `bade` has no analyses either

**dette** failures:

* `dette+V+Inf` => `dea`
* `dette+V+Inf` => `dee`
* `dette` has following analyses:
  * `dette+Det+Dem+Sg+Neu`

**snøre** failures:

* `snøre+V+Inf` does not generate!
* `snøre` has no analyses either

**kakle** failures:

* `kakle+V+Inf` => `kakl`
* `kakle` has no analyses either

**kase** failures:

* `kase+V+Inf` => `kas`
* `kase` has following analyses:
  * `kasen+A+Neu+Sg+Indef`

**halvklare** failures:

* `halvklare+V+Inf` does not generate!
* `halvklare` has following analyses:
  * `halvklar+A+A+Sg+Def`
  * `halvklar+A+A+Pl+Def`
  * `halvklar+A+A+Pl+Indef`

**blidkast** failures:

* `blidkast+V+Inf` => `blidkastast`
* `blidkast` has no analyses either

**avtene** failures:

* `avtene+V+Inf` => `avten`
* `avtene` has no analyses either

**ynkast** failures:

* `ynkast+V+Inf` => `ynkastast`
* `ynkast` has no analyses either

**kvelde** failures:

* `kvelde+V+Inf` => `kveld`
* `kvelde` has no analyses either

**fakturere** failures:

* `fakturere+V+Inf` => `fakturer`
* `fakturere` has no analyses either

**sleve** failures:

* `sleve+V+Inf` => `slev`
* `sleve` has no analyses either

**vrikke** failures:

* `vrikke+V+Inf` => `vrikk`
* `vrikke` has no analyses either

**forgi** failures:

* `forgi+V+Inf` => `forggi`
* `forgi` has no analyses either

**disponere** failures:

* `disponere+V+Inf` => `disponer`
* `disponere` has no analyses either

**tvibykse** failures:

* `tvibykse+V+Inf` => `tvibyks`
* `tvibykse` has no analyses either

**løype** failures:

* `løype+V+Inf` => `løyp`
* `løype` has no analyses either

**hardkoke** failures:

* `hardkoke+V+Inf` does not generate!
* `hardkoke` has no analyses either

**trege** failures:

* `trege+V+Inf` => `treg`
* `trege` has following analyses:
  * `tregg+A+A+Pl+Indef`
  * `tregg+A+A+Sg+Def`
  * `tregg+A+A+Pl+Def`
  * `treg+A+Sg+Def`
  * `treg+A+Pl+Indef`

**inkludere** failures:

* `inkludere+V+Inf` => `inkluder`
* `inkludere` has no analyses either

**grypje** failures:

* `grypje+V+Inf` does not generate!
* `grypje` has no analyses either

**mikroskopere** failures:

* `mikroskopere+V+Inf` => `mikroskoper`
* `mikroskopere` has no analyses either

**beherske** failures:

* `beherske+V+Inf` => `behersk`
* `beherske` has no analyses either

**gjekke** failures:

* `gjekke+V+Inf` => `gjekk`
* `gjekke` has no analyses either

**preppe** failures:

* `preppe+V+Inf` => `prepp`
* `preppe` has no analyses either

**skrækje** failures:

* `skrækje+V+Inf` does not generate!
* `skrækje` has no analyses either

**snikkere** failures:

* `snikkere+V+Inf` => `snikker`
* `snikkere` has no analyses either

**gasjere** failures:

* `gasjere+V+Inf` => `gasjer`
* `gasjere` has no analyses either

**innordne** failures:

* `innordne+V+Inf` => `innordn`
* `innordne` has no analyses either

**avvise** failures:

* `avvise+V+Inf` => `avvis`
* `avvise` has no analyses either

**grunngi** failures:

* `grunngi+V+Inf` => `grunnggi`
* `grunngi` has no analyses either

**konsertere** failures:

* `konsertere+V+Inf` => `konserter`
* `konsertere` has no analyses either

**sarve** failures:

* `sarve+V+Inf` => `sarv`
* `sarve` has no analyses either

**skamfile** failures:

* `skamfile+V+Inf` does not generate!
* `skamfile` has no analyses either

**vanrøkte** failures:

* `vanrøkte+V+Inf` => `vanrøkt`
* `vanrøkte` has no analyses either

**kvikke** failures:

* `kvikke+V+Inf` => `kvikk`
* `kvikke` has no analyses either

**flysje** failures:

* `flysje+V+Inf` does not generate!
* `flysje` has no analyses either

**forblinde** failures:

* `forblinde+V+Inf` => `forblind`
* `forblinde` has no analyses either

**pjutre** failures:

* `pjutre+V+Inf` => `pjutr`
* `pjutre` has no analyses either

**knise** failures:

* `knise+V+Inf` does not generate!
* `knise` has no analyses either

**lave** failures:

* `lave+V+Inf` does not generate!
* `lave` has no analyses either

**sollisitere** failures:

* `sollisitere+V+Inf` => `sollisiter`
* `sollisitere` has no analyses either

**plante** failures:

* `plante+V+Inf` => `plant`
* `plante` has no analyses either

**bebreide** failures:

* `bebreide+V+Inf` => `bebreid`
* `bebreide` has no analyses either

**forstørre** failures:

* `forstørre+V+Inf` => `forstørr`
* `forstørre` has no analyses either

**konsipere** failures:

* `konsipere+V+Inf` => `konsiper`
* `konsipere` has no analyses either

**skurke** failures:

* `skurke+V+Inf` => `skurk`
* `skurke` has no analyses either

**ploge** failures:

* `ploge+V+Inf` => `plog`
* `ploge` has no analyses either

**rushe** failures:

* `rushe+V+Inf` => `rush`
* `rushe` has no analyses either

**stride** failures:

* `stride+V+Inf` does not generate!
* `stride` has following analyses:
  * `strid+A+Sg+Def`
  * `strid+A+Pl+Indef`

**velje** failures:

* `velje+V+Inf` does not generate!
* `velje` has no analyses either

**fraue** failures:

* `fraue+V+Inf` => `frau`
* `fraue` has no analyses either

**kline** failures:

* `kline+V+Inf` => `klin`
* `kline` has following analyses:
  * `klipe+V+Adj+Fl`
  * `klive+V+Adj+Fl`
  * `klipe+V+Adj+Bu+Eint`
  * `klive+V+Adj+Bu+Eint`

**gidre** failures:

* `gidre+V+Inf` => `gidr`
* `gidre` has no analyses either

**caste** failures:

* `caste+V+Inf` => `cast`
* `caste` has no analyses either

**omrokere** failures:

* `omrokere+V+Inf` => `omroker`
* `omrokere` has no analyses either

**administrere** failures:

* `administrere+V+Inf` => `administrer`
* `administrere` has no analyses either

**skjøve** failures:

* `skjøve+V+Inf` does not generate!
* `skjøve` has no analyses either

**lempe** failures:

* `lempe+V+Inf` => `lemp`
* `lempe` has no analyses either

**armast** failures:

* `armast+V+Inf` => `armastast`
* `armast` has following analyses:
  * `arm+A+Superl+Indef`

**uheppast** failures:

* `uheppast+V+Inf` => `uheppastast`
* `uheppast` has no analyses either

**parallellisere** failures:

* `parallellisere+V+Inf` => `parallelliser`
* `parallellisere` has no analyses either

**rekuperere** failures:

* `rekuperere+V+Inf` => `rekuperer`
* `rekuperere` has no analyses either

**dysse** failures:

* `dysse+V+Inf` => `dyss`
* `dysse` has no analyses either

**omgje** failures:

* `omgje+V+Inf` => `omggje`
* `omgje` has no analyses either

**hufse** failures:

* `hufse+V+Inf` => `hufs`
* `hufse` has following analyses:
  * `hufsen+A+Neu+Sg+Indef`

**lefle** failures:

* `lefle+V+Inf` => `lefl`
* `lefle` has no analyses either

**organisere** failures:

* `organisere+V+Inf` => `organiser`
* `organisere` has no analyses either

**drynje** failures:

* `drynje+V+Inf` does not generate!
* `drynje` has no analyses either

**ruinere** failures:

* `ruinere+V+Inf` => `ruiner`
* `ruinere` has no analyses either

**mistyde** failures:

* `mistyde+V+Inf` does not generate!
* `mistyde` has no analyses either

**myldre** failures:

* `myldre+V+Inf` => `myldr`
* `myldre` has no analyses either

**avmagnetisere** failures:

* `avmagnetisere+V+Inf` => `avmagnetiser`
* `avmagnetisere` has no analyses either

**angle** failures:

* `angle+V+Inf` => `angl`
* `angle` has no analyses either

**tylte** failures:

* `tylte+V+Inf` => `tylt`
* `tylte` has no analyses either

**geise** failures:

* `geise+V+Inf` => `geis`
* `geise` has no analyses either

**konfiskere** failures:

* `konfiskere+V+Inf` => `konfisker`
* `konfiskere` has no analyses either

**yve** failures:

* `yve+V+Inf` does not generate!
* `yve` has following analyses:
  * `yven+A+Neu+Sg+Indef`

**bedra** failures:

* `bedra+V+Inf` => `bedrdra`
* `bedra` has no analyses either

**eikle** failures:

* `eikle+V+Inf` => `eikl`
* `eikle` has no analyses either

**skolne** failures:

* `skolne+V+Inf` => `skoln`
* `skolne` has no analyses either

**smole** failures:

* `smole+V+Inf` => `smol`
* `smole` has no analyses either

**rase** failures:

* `rase+V+Inf` => `ras`
* `rase` has no analyses either

**posisjonere** failures:

* `posisjonere+V+Inf` => `posisjoner`
* `posisjonere` has no analyses either

**glindre** failures:

* `glindre+V+Inf` => `glindr`
* `glindre` has no analyses either

**ruple** failures:

* `ruple+V+Inf` => `rupl`
* `ruple` has no analyses either

**deflatere** failures:

* `deflatere+V+Inf` => `deflater`
* `deflatere` has no analyses either

**seise** failures:

* `seise+V+Inf` => `seis`
* `seise` has no analyses either

**skyve** failures:

* `skyve+V+Inf` => `ske`
* `skyve+V+Inf` => `ska`
* `skyve` has following analyses:
  * `skyver+A+A+Sg+Def`
  * `skyver+A+A+Pl+Def`
  * `skyver+A+A+Pl+Indef`

**fikte** failures:

* `fikte+V+Inf` => `fikt`
* `fikte` has no analyses either

**se** failures:

* `se+V+Inf` => `sjå`
* `se` has no analyses either

**snåve** failures:

* `snåve+V+Inf` => `snåv`
* `snåve` has no analyses either

**fordrive** failures:

* `fordrive+V+Inf` => `fordrie`
* `fordrive+V+Inf` => `fordria`
* `fordrive` has no analyses either

**gneldre** failures:

* `gneldre+V+Inf` => `gneldr`
* `gneldre` has no analyses either

**kikne** failures:

* `kikne+V+Inf` => `kikn`
* `kikne` has no analyses either

**gravlegge** failures:

* `gravlegge+V+Inf` does not generate!
* `gravlegge` has no analyses either

**varmrøyke** failures:

* `varmrøyke+V+Inf` => `varmrøyk`
* `varmrøyke` has no analyses either

**grøppe** failures:

* `grøppe+V+Inf` does not generate!
* `grøppe` has no analyses either

**spinke** failures:

* `spinke+V+Inf` => `spink`
* `spinke` has following analyses:
  * `spinkel+A+A+Pl+Def`
  * `spinkel+A+A+Pl+Indef`
  * `spinkel+A+A+Sg+Def`
  * `spinken+A+Neu+Sg+Indef`

**vokse** failures:

* `vokse+V+Inf` => `voks`
* `vokse` has no analyses either

**felle** failures:

* `felle+V+Inf` does not generate!
* `felle` has no analyses either

**segle** failures:

* `segle+V+Inf` does not generate!
* `segle` has no analyses either

**grovhakke** failures:

* `grovhakke+V+Inf` => `grovhakk`
* `grovhakke` has no analyses either

**bolse** failures:

* `bolse+V+Inf` => `bols`
* `bolse` has no analyses either

**touche** failures:

* `touche+V+Inf` => `touch`
* `touche` has no analyses either

**ulne** failures:

* `ulne+V+Inf` => `uln`
* `ulne` has following analyses:
  * `ullen+A+Pl+Indef`
  * `ullen+A+Sg+Def`
  * `ulen+A+Sg+Def`
  * `ulen+A+Pl+Indef`

**generere** failures:

* `generere+V+Inf` => `generer`
* `generere` has no analyses either

**forklåre** failures:

* `forklåre+V+Inf` => `forklår`
* `forklåre` has no analyses either

**grine** failures:

* `grine+V+Inf` => `grie`
* `grine+V+Inf` => `gria`
* `grine` has following analyses:
  * `gripe+V+Adj+Fl`
  * `gripe+V+Adj+Bu+Eint`
  * `grine+V+Adj+Fl`
  * `grine+V+Adj+Bu+Eint`

**griljere** failures:

* `griljere+V+Inf` => `griljer`
* `griljere` has no analyses either

**røske** failures:

* `røske+V+Inf` => `røsk`
* `røske` has no analyses either

**seigdra** failures:

* `seigdra+V+Inf` => `seigdrdra`
* `seigdra` has no analyses either

**stoffe** failures:

* `stoffe+V+Inf` => `stoff`
* `stoffe` has no analyses either

**delje** failures:

* `delje+V+Inf` => `delj`
* `delje` has no analyses either

**kanse** failures:

* `kanse+V+Inf` => `kans`
* `kanse` has no analyses either

**gyse** failures:

* `gyse+V+Inf` => `g`
* `gyse` has no analyses either

**snulle** failures:

* `snulle+V+Inf` => `snull`
* `snulle` has no analyses either

**konjugere** failures:

* `konjugere+V+Inf` => `konjuger`
* `konjugere` has no analyses either

**føye** failures:

* `føye+V+Inf` does not generate!
* `føye` has following analyses:
  * `føyen+A+Neu+Sg+Indef`

**regelfeste** failures:

* `regelfeste+V+Inf` does not generate!
* `regelfeste` has no analyses either

**tverrstoppe** failures:

* `tverrstoppe+V+Inf` => `tverrstopp`
* `tverrstoppe` has no analyses either

**hundsetje** failures:

* `hundsetje+V+Inf` does not generate!
* `hundsetje` has no analyses either

**avhende** failures:

* `avhende+V+Inf` does not generate!
* `avhende` has no analyses either

**openberre** failures:

* `openberre+V+Inf` => `openberr`
* `openberre` has no analyses either

**dupere** failures:

* `dupere+V+Inf` => `duper`
* `dupere` has no analyses either

**ære** failures:

* `ære+V+Inf` => `ær`
* `ære` has no analyses either

**kode** failures:

* `kode+V+Inf` => `kod`
* `kode` has no analyses either

**øydsle** failures:

* `øydsle+V+Inf` => `øydsl`
* `øydsle` has no analyses either

**bælflyge** failures:

* `bælflyge+V+Inf` => `bælflye`
* `bælflyge+V+Inf` => `bælflya`
* `bælflyge` has no analyses either

**radreinse** failures:

* `radreinse+V+Inf` => `radreins`
* `radreinse` has no analyses either

**vêre** failures:

* `vêre+V+Inf` => `vêr`
* `vêre` has no analyses either

**slatre** failures:

* `slatre+V+Inf` => `slatr`
* `slatre` has no analyses either

**evaporere** failures:

* `evaporere+V+Inf` => `evaporer`
* `evaporere` has no analyses either

**forbyde** failures:

* `forbyde+V+Inf` does not generate!
* `forbyde` has following analyses:
  * `forbydd+A+A+Pl+Def`
  * `forbydd+A+A+Sg+Def`
  * `forbydd+A+A+Pl+Indef`

**attvinne** failures:

* `attvinne+V+Inf` => `attvia`
* `attvinne+V+Inf` => `attvie`
* `attvinne` has no analyses either

**flanere** failures:

* `flanere+V+Inf` => `flaner`
* `flanere` has no analyses either

**monoftongere** failures:

* `monoftongere+V+Inf` => `monoftonger`
* `monoftongere` has no analyses either

**kjegle** failures:

* `kjegle+V+Inf` => `kjegl`
* `kjegle` has no analyses either

**virilisere** failures:

* `virilisere+V+Inf` => `viriliser`
* `virilisere` has no analyses either

**fortape** failures:

* `fortape+V+Inf` => `fortap`
* `fortape` has no analyses either

**kverrsette** failures:

* `kverrsette+V+Inf` does not generate!
* `kverrsette` has no analyses either

**rotere** failures:

* `rotere+V+Inf` => `roter`
* `rotere` has no analyses either

**fighte** failures:

* `fighte+V+Inf` => `fight`
* `fighte` has no analyses either

**grylte** failures:

* `grylte+V+Inf` => `grylt`
* `grylte` has following analyses:
  * `gryle+V+PrfPrc`

**skeide** failures:

* `skeide+V+Inf` => `skeid`
* `skeide` has no analyses either

**tjå** failures:

* `tjå+V+Inf` does not generate!
* `tjå` has following analyses:
  * `tjå+V`

**ruke** failures:

* `ruke+V+Inf` => `ruk`
* `ruke` has no analyses either

**medgje** failures:

* `medgje+V+Inf` => `medggje`
* `medgje` has no analyses either

**grase** failures:

* `grase+V+Inf` => `gras`
* `grase` has no analyses either

**rådføre** failures:

* `rådføre+V+Inf` does not generate!
* `rådføre` has no analyses either

**flytte** failures:

* `flytte+V+Inf` does not generate!
* `flytte` has no analyses either

**sjamponere** failures:

* `sjamponere+V+Inf` => `sjamponer`
* `sjamponere` has no analyses either

**skortne** failures:

* `skortne+V+Inf` => `skortn`
* `skortne` has no analyses either

**svæve** failures:

* `svæve+V+Inf` does not generate!
* `svæve` has no analyses either

**øydeleggje** failures:

* `øydeleggje+V+Inf` does not generate!
* `øydeleggje` has no analyses either

**kutte** failures:

* `kutte+V+Inf` => `kutt`
* `kutte` has no analyses either

**bannstøyte** failures:

* `bannstøyte+V+Inf` => `bannstøyt`
* `bannstøyte` has no analyses either

**manne** failures:

* `manne+V+Inf` => `mann`
* `manne` has no analyses either

**fiske** failures:

* `fiske+V+Inf` => `fisk`
* `fiske` has following analyses:
  * `fisken+A+Neu+Sg+Indef`

**ufarleggjere** failures:

* `ufarleggjere+V+Inf` does not generate!
* `ufarleggjere` has no analyses either

**emanere** failures:

* `emanere+V+Inf` => `emaner`
* `emanere` has no analyses either

**brydde** failures:

* `brydde+V+Inf` does not generate!
* `brydde` has following analyses:
  * `bry+V+Ind+Prt`

**fløde** failures:

* `fløde+V+Inf` does not generate!
* `fløde` has no analyses either

**hultre** failures:

* `hultre+V+Inf` => `hultr`
* `hultre` has no analyses either

**fale** failures:

* `fale+V+Inf` => `fal`
* `fale` has following analyses:
  * `fallen+A+Neu+Sg+Indef`
  * `fal+A+Pl+Indef`
  * `fal+A+Sg+Def`

**plyndre** failures:

* `plyndre+V+Inf` => `plyndr`
* `plyndre` has no analyses either

**brume** failures:

* `brume+V+Inf` => `brum`
* `brume` has no analyses either

**krossleggje** failures:

* `krossleggje+V+Inf` does not generate!
* `krossleggje` has no analyses either

**delabialisere** failures:

* `delabialisere+V+Inf` => `delabialiser`
* `delabialisere` has no analyses either

**kryle** failures:

* `kryle+V+Inf` => `kryl`
* `kryle` has following analyses:
  * `kryl+A+Pl+Indef`
  * `kryl+A+Sg+Def`

**gjennomskode** failures:

* `gjennomskode+V+Inf` => `gjennomskod`
* `gjennomskode` has no analyses either

**framelske** failures:

* `framelske+V+Inf` => `framelsk`
* `framelske` has no analyses either

**draske** failures:

* `draske+V+Inf` => `drask`
* `draske` has following analyses:
  * `drasken+A+Neu+Sg+Indef`

**egne** failures:

* `egne+V+Inf` does not generate!
* `egne` has following analyses:
  * `egen+A+Pl+Indef`
  * `egen+A+Sg+Def`

**evangelisere** failures:

* `evangelisere+V+Inf` => `evangeliser`
* `evangelisere` has no analyses either

**glosteikje** failures:

* `glosteikje+V+Inf` does not generate!
* `glosteikje` has no analyses either

**sile** failures:

* `sile+V+Inf` does not generate!
* `sile` has no analyses either

**flagge** failures:

* `flagge+V+Inf` => `flagg`
* `flagge` has no analyses either

**vente** failures:

* `vente+V+Inf` => `vent`
* `vente` has no analyses either

**dagsetje** failures:

* `dagsetje+V+Inf` does not generate!
* `dagsetje` has no analyses either

**gjennomhole** failures:

* `gjennomhole+V+Inf` => `gjennomhol`
* `gjennomhole` has no analyses either

**nekte** failures:

* `nekte+V+Inf` => `nekt`
* `nekte` has no analyses either

**trevle** failures:

* `trevle+V+Inf` => `trevl`
* `trevle` has no analyses either

**fange** failures:

* `fange+V+Inf` => `fang`
* `fange` has no analyses either

**høgvørde** failures:

* `høgvørde+V+Inf` does not generate!
* `høgvørde` has following analyses:
  * `høgvørd+A+A+Sg+Def`
  * `høgvørd+A+A+Pl+Indef`
  * `høgvørd+A+A+Pl+Def`

**knurpe** failures:

* `knurpe+V+Inf` => `knurp`
* `knurpe` has no analyses either

**skjule** failures:

* `skjule+V+Inf` => `skjul`
* `skjule` has no analyses either

**grønkast** failures:

* `grønkast+V+Inf` => `grønkastast`
* `grønkast` has no analyses either

**kaure** failures:

* `kaure+V+Inf` => `kaur`
* `kaure` has no analyses either

**krangle** failures:

* `krangle+V+Inf` => `krangl`
* `krangle` has no analyses either

**amalgamere** failures:

* `amalgamere+V+Inf` => `amalgamer`
* `amalgamere` has no analyses either

**tråle** failures:

* `tråle+V+Inf` => `trål`
* `tråle` has no analyses either

**bandleggje** failures:

* `bandleggje+V+Inf` does not generate!
* `bandleggje` has no analyses either

**sprale** failures:

* `sprale+V+Inf` => `spral`
* `sprale` has no analyses either

**krakelere** failures:

* `krakelere+V+Inf` => `krakeler`
* `krakelere` has no analyses either

**blankpusse** failures:

* `blankpusse+V+Inf` => `blankpuss`
* `blankpusse` has no analyses either

**tidfeste** failures:

* `tidfeste+V+Inf` does not generate!
* `tidfeste` has no analyses either

**gravleggje** failures:

* `gravleggje+V+Inf` does not generate!
* `gravleggje` has no analyses either

**grassere** failures:

* `grassere+V+Inf` => `grasser`
* `grassere` has no analyses either

**trandle** failures:

* `trandle+V+Inf` => `trandl`
* `trandle` has no analyses either

**lutre** failures:

* `lutre+V+Inf` => `lutr`
* `lutre` has no analyses either

**supplere** failures:

* `supplere+V+Inf` => `suppler`
* `supplere` has no analyses either

**summere** failures:

* `summere+V+Inf` => `summer`
* `summere` has no analyses either

**føre** failures:

* `føre+V+Inf` does not generate!
* `føre` has following analyses:
  * `før+A+Sg+Def`
  * `før+A+Pl+Indef`

**bastardisere** failures:

* `bastardisere+V+Inf` => `bastardiser`
* `bastardisere` has no analyses either

**knoge** failures:

* `knoge+V+Inf` => `knog`
* `knoge` has no analyses either

**hulle** failures:

* `hulle+V+Inf` => `hull`
* `hulle` has no analyses either

**fortynne** failures:

* `fortynne+V+Inf` does not generate!
* `fortynne` has no analyses either

**finrekne** failures:

* `finrekne+V+Inf` => `finrekn`
* `finrekne` has no analyses either

**liberalisere** failures:

* `liberalisere+V+Inf` => `liberaliser`
* `liberalisere` has no analyses either

**hærta** failures:

* `hærta+V+Inf` => `hærtta`
* `hærta` has no analyses either

**stumpe** failures:

* `stumpe+V+Inf` => `stump`
* `stumpe` has following analyses:
  * `stump+A+Pl+Indef`
  * `stump+A+Sg+Def`

**avnasjonalisere** failures:

* `avnasjonalisere+V+Inf` => `avnasjonaliser`
* `avnasjonalisere` has no analyses either

**gnette** failures:

* `gnette+V+Inf` => `gnee`
* `gnette+V+Inf` => `gnea`
* `gnette` has no analyses either

**gradere** failures:

* `gradere+V+Inf` => `grader`
* `gradere` has no analyses either

**mannsoppdekkje** failures:

* `mannsoppdekkje+V+Inf` does not generate!
* `mannsoppdekkje` has no analyses either

**sivilisere** failures:

* `sivilisere+V+Inf` => `siviliser`
* `sivilisere` has no analyses either

**databehandle** failures:

* `databehandle+V+Inf` => `databehandl`
* `databehandle` has no analyses either

**synleggjere** failures:

* `synleggjere+V+Inf` does not generate!
* `synleggjere` has no analyses either

**signalisere** failures:

* `signalisere+V+Inf` => `signaliser`
* `signalisere` has no analyses either

**silrenne** failures:

* `silrenne+V+Inf` => `silree`
* `silrenne+V+Inf` => `silrea`
* `silrenne` has no analyses either

**kause** failures:

* `kause+V+Inf` => `kaus`
* `kause` has no analyses either

**jukse** failures:

* `jukse+V+Inf` => `juks`
* `jukse` has no analyses either

**bengle** failures:

* `bengle+V+Inf` => `bengl`
* `bengle` has no analyses either

**homogenisere** failures:

* `homogenisere+V+Inf` => `homogeniser`
* `homogenisere` has no analyses either

**kalibrere** failures:

* `kalibrere+V+Inf` => `kalibrer`
* `kalibrere` has no analyses either

**igle** failures:

* `igle+V+Inf` => `igl`
* `igle` has no analyses either

**sture** failures:

* `sture+V+Inf` does not generate!
* `sture` has following analyses:
  * `stur+A+Pl+Indef`
  * `stur+A+Sg+Def`
  * `sturen+A+Neu+Sg+Indef`

**plissere** failures:

* `plissere+V+Inf` => `plisser`
* `plissere` has no analyses either

**permutere** failures:

* `permutere+V+Inf` => `permuter`
* `permutere` has no analyses either

**vemmast** failures:

* `vemmast+V+Inf` => `vemmastast`
* `vemmast` has no analyses either

**fjonge** failures:

* `fjonge+V+Inf` => `fjong`
* `fjonge` has following analyses:
  * `fjong+A+Sg+Def`
  * `fjong+A+Pl+Indef`

**temme** failures:

* `temme+V+Inf` does not generate!
* `temme` has no analyses either

**svemje** failures:

* `svemje+V+Inf` => `sveja`
* `svemje+V+Inf` => `sveje`
* `svemje` has no analyses either

**håre** failures:

* `håre+V+Inf` => `hår`
* `håre` has no analyses either

**mingle** failures:

* `mingle+V+Inf` => `mingl`
* `mingle` has no analyses either

**lye** failures:

* `lye+V+Inf` does not generate!
* `lye` has following analyses:
  * `ly+A+Sg+Def`
  * `ly+A+Pl+Indef`
  * `lyge+V+Inf`

**verse** failures:

* `verse+V+Inf` => `vers`
* `verse` has no analyses either

**blode** failures:

* `blode+V+Inf` => `blod`
* `blode` has no analyses either

**murke** failures:

* `murke+V+Inf` => `murk`
* `murke` has no analyses either

**antikrøllbehandle** failures:

* `antikrøllbehandle+V+Inf` => `antikrøllbehandl`
* `antikrøllbehandle` has no analyses either

**mukke** failures:

* `mukke+V+Inf` => `mukk`
* `mukke` has no analyses either

**lukkynskje** failures:

* `lukkynskje+V+Inf` does not generate!
* `lukkynskje` has no analyses either

**bedøme** failures:

* `bedøme+V+Inf` does not generate!
* `bedøme` has no analyses either

**hærsetje** failures:

* `hærsetje+V+Inf` does not generate!
* `hærsetje` has no analyses either

**tilkome** failures:

* `tilkome+V+Inf` => `tilkkome`
* `tilkome+V+Inf` => `tilkkoma`
* `tilkome` has following analyses:
  * `tilkomen+A+Neu+Sg+Indef`
  * `tilkommen+A+Neu+Sg+Indef`

**lemstre** failures:

* `lemstre+V+Inf` => `lemstr`
* `lemstre` has no analyses either

**føretrekke** failures:

* `føretrekke+V+Inf` does not generate!
* `føretrekke` has no analyses either

**mine** failures:

* `mine+V+Inf` does not generate!
* `mine` has following analyses:
  * `mine+Det+Poss+Pl`
  * `mige+V+Adj+Fl`
  * `mige+V+Adj+Bu+Eint`

**dåne** failures:

* `dåne+V+Inf` => `dån`
* `dåne` has following analyses:
  * `dåen+A+Pl+Indef`
  * `dåen+A+Sg+Def`

**eksegere** failures:

* `eksegere+V+Inf` => `ekseger`
* `eksegere` has no analyses either

**flane** failures:

* `flane+V+Inf` => `flan`
* `flane` has following analyses:
  * `flå+N+N+Msc+Pl+Def`

**føle** failures:

* `føle+V+Inf` => `føl`
* `føle` has no analyses either

**rullere** failures:

* `rullere+V+Inf` => `ruller`
* `rullere` has no analyses either

**blikre** failures:

* `blikre+V+Inf` => `blikr`
* `blikre` has no analyses either

**gyrde** failures:

* `gyrde+V+Inf` does not generate!
* `gyrde` has no analyses either

**kromgarve** failures:

* `kromgarve+V+Inf` => `kromgarv`
* `kromgarve` has no analyses either

**hengsle** failures:

* `hengsle+V+Inf` => `hengsl`
* `hengsle` has no analyses either

**kappsymje** failures:

* `kappsymje+V+Inf` does not generate!
* `kappsymje` has no analyses either

**mogeleggjere** failures:

* `mogeleggjere+V+Inf` does not generate!
* `mogeleggjere` has no analyses either

**hyperventilere** failures:

* `hyperventilere+V+Inf` => `hyperventiler`
* `hyperventilere` has no analyses either

**krisemaksimere** failures:

* `krisemaksimere+V+Inf` => `krisemaksimer`
* `krisemaksimere` has no analyses either

**ore** failures:

* `ore+V+Inf` => `or`
* `ore` has following analyses:
  * `orr+A+A+Pl+Def`
  * `orr+A+A+Pl+Indef`
  * `orr+A+A+Sg+Def`

**morkne** failures:

* `morkne+V+Inf` => `morkn`
* `morkne` has following analyses:
  * `morken+A+Pl+Indef`
  * `morken+A+Sg+Def`

**hutre** failures:

* `hutre+V+Inf` => `hutr`
* `hutre` has no analyses either

**valse** failures:

* `valse+V+Inf` => `vals`
* `valse` has no analyses either

**kubbe** failures:

* `kubbe+V+Inf` => `kubb`
* `kubbe` has no analyses either

**nedstamme** failures:

* `nedstamme+V+Inf` => `nedstam`
* `nedstamme` has no analyses either

**sprinte** failures:

* `sprinte+V+Inf` => `sprint`
* `sprinte` has no analyses either

**klype** failures:

* `klype+V+Inf` => `kl`
* `klype` has no analyses either

**hamstre** failures:

* `hamstre+V+Inf` => `hamstr`
* `hamstre` has no analyses either

**padle** failures:

* `padle+V+Inf` => `padl`
* `padle` has no analyses either

**spandere** failures:

* `spandere+V+Inf` => `spander`
* `spandere` has no analyses either

**hukre** failures:

* `hukre+V+Inf` => `hukr`
* `hukre` has following analyses:
  * `hukren+A+Neu+Sg+Indef`

**fonne** failures:

* `fonne+V+Inf` => `fonn`
* `fonne` has no analyses either

**påverke** failures:

* `påverke+V+Inf` => `påverk`
* `påverke` has no analyses either

**seide** failures:

* `seide+V+Inf` => `seid`
* `seide` has no analyses either

**tyrste** failures:

* `tyrste+V+Inf` does not generate!
* `tyrste` has following analyses:
  * `tyrst+A+A+Pl+Def`
  * `tyrst+A+A+Sg+Def`
  * `tyrst+A+A+Pl+Indef`

**lydisolere** failures:

* `lydisolere+V+Inf` => `lydisoler`
* `lydisolere` has no analyses either

**avsverje** failures:

* `avsverje+V+Inf` => `avsveje`
* `avsverje+V+Inf` => `avsveja`
* `avsverje` has no analyses either

**perennere** failures:

* `perennere+V+Inf` => `perenner`
* `perennere` has no analyses either

**godrøde** failures:

* `godrøde+V+Inf` does not generate!
* `godrøde` has no analyses either

**karamellisere** failures:

* `karamellisere+V+Inf` => `karamelliser`
* `karamellisere` has no analyses either

**småfryse** failures:

* `småfryse+V+Inf` => `småfrye`
* `småfryse+V+Inf` => `småfrya`
* `småfryse` has no analyses either

**klumpe** failures:

* `klumpe+V+Inf` => `klump`
* `klumpe` has following analyses:
  * `klumpen+A+Neu+Sg+Indef`

**gyte** failures:

* `gyte+V+Inf` does not generate!
* `gyte` has no analyses either

**kogle** failures:

* `kogle+V+Inf` => `kogl`
* `kogle` has no analyses either

**gjete** failures:

* `gjete+V+Inf` => `gjet`
* `gjete` has following analyses:
  * `gjeten+A+Neu+Sg+Indef`

**ilske** failures:

* `ilske+V+Inf` => `ilsk`
* `ilske` has following analyses:
  * `ilsk+A+A+Pl+Def`
  * `ilsken+A+Neu+Sg+Indef`
  * `ilsk+A+A+Sg+Def`
  * `ilsk+A+A+Pl+Indef`

**ut** failures:

* `ut+V+Inf` does not generate!
* `ut` has following analyses:
  * `ut+Adv`

**vandalisere** failures:

* `vandalisere+V+Inf` => `vandaliser`
* `vandalisere` has no analyses either

**kjave** failures:

* `kjave+V+Inf` => `kjav`
* `kjave` has no analyses either

**arge** failures:

* `arge+V+Inf` => `arg`
* `arge` has following analyses:
  * `arg+A+Pl+Indef`
  * `arg+A+Sg+Def`

**fysse** failures:

* `fysse+V+Inf` => `fyss`
* `fysse` has no analyses either

**nedvurdere** failures:

* `nedvurdere+V+Inf` => `nedvurder`
* `nedvurdere` has no analyses either

**hugast** failures:

* `hugast+V+Inf` => `hugastast`
* `hugast` has following analyses:
  * `hug+A+Superl+Indef`

**teppe** failures:

* `teppe+V+Inf` does not generate!
* `teppe` has no analyses either

**gruvle** failures:

* `gruvle+V+Inf` => `gruvl`
* `gruvle` has no analyses either

**skjende** failures:

* `skjende+V+Inf` => `skjend`
* `skjende` has no analyses either

**beve** failures:

* `beve+V+Inf` => `bev`
* `beve` has no analyses either

**bauke** failures:

* `bauke+V+Inf` => `bauk`
* `bauke` has no analyses either

**nøste** failures:

* `nøste+V+Inf` => `nøst`
* `nøste` has no analyses either

**nøkke** failures:

* `nøkke+V+Inf` => `nøka`
* `nøkke+V+Inf` => `nøke`
* `nøkke` has no analyses either

**sørpe** failures:

* `sørpe+V+Inf` => `sørp`
* `sørpe` has no analyses either

**billettere** failures:

* `billettere+V+Inf` => `billetter`
* `billettere` has no analyses either

**forfremme** failures:

* `forfremme+V+Inf` does not generate!
* `forfremme` has no analyses either

**oke** failures:

* `oke+V+Inf` => `ok`
* `oke` has no analyses either

**tørrete** failures:

* `tørrete+V+Inf` => `tørrea`
* `tørrete+V+Inf` => `tørree`
* `tørrete` has no analyses either

**fornekte** failures:

* `fornekte+V+Inf` => `fornekt`
* `fornekte` has no analyses either

**gagle** failures:

* `gagle+V+Inf` => `gagl`
* `gagle` has no analyses either

**forsarve** failures:

* `forsarve+V+Inf` => `forsarv`
* `forsarve` has no analyses either

**tørrsymje** failures:

* `tørrsymje+V+Inf` does not generate!
* `tørrsymje` has no analyses either

**uglesjå** failures:

* `uglesjå+V+Inf` => `uglessjå`
* `uglesjå` has no analyses either

**keise** failures:

* `keise+V+Inf` => `keis`
* `keise` has no analyses either

**klassifisere** failures:

* `klassifisere+V+Inf` => `klassifiser`
* `klassifisere` has no analyses either

**stare** failures:

* `stare+V+Inf` => `star`
* `stare` has no analyses either

**tjelde** failures:

* `tjelde+V+Inf` => `tjeld`
* `tjelde` has no analyses either

**gapskratte** failures:

* `gapskratte+V+Inf` => `gapskratt`
* `gapskratte` has no analyses either

**alternere** failures:

* `alternere+V+Inf` => `alterner`
* `alternere` has no analyses either

**sinklegge** failures:

* `sinklegge+V+Inf` does not generate!
* `sinklegge` has no analyses either

**stolle** failures:

* `stolle+V+Inf` => `stoll`
* `stolle` has no analyses either

**tire** failures:

* `tire+V+Inf` does not generate!
* `tire` has no analyses either

**innsnevre** failures:

* `innsnevre+V+Inf` => `innsnevr`
* `innsnevre` has no analyses either

**rugge** failures:

* `rugge+V+Inf` => `rugg`
* `rugge` has no analyses either

**piste** failures:

* `piste+V+Inf` does not generate!
* `piste` has following analyses:
  * `pisten+A+Neu+Sg+Indef`

**sarre** failures:

* `sarre+V+Inf` => `sarr`
* `sarre` has no analyses either

**predisponere** failures:

* `predisponere+V+Inf` => `predisponer`
* `predisponere` has no analyses either

**lykte** failures:

* `lykte+V+Inf` => `lykt`
* `lykte` has no analyses either

**hole** failures:

* `hole+V+Inf` => `hol`
* `hole` has following analyses:
  * `hol+A+Pl+Indef`
  * `hol+A+Sg+Def`

**skape** failures:

* `skape+V+Inf` does not generate!
* `skape` has no analyses either

**sevje** failures:

* `sevje+V+Inf` => `sevj`
* `sevje` has no analyses either

**begynne** failures:

* `begynne+V+Inf` does not generate!
* `begynne` has no analyses either

**øse** failures:

* `øse+V+Inf` => `øs`
* `øse` has following analyses:
  * `øsen+A+Neu+Sg+Indef`
  * `øs+A+Sg+Def`
  * `øs+A+Pl+Indef`

**formidle** failures:

* `formidle+V+Inf` => `formidl`
* `formidle` has no analyses either

**leive** failures:

* `leive+V+Inf` does not generate!
* `leive` has no analyses either

**flise** failures:

* `flise+V+Inf` => `flis`
* `flise` has no analyses either

**styrtbløde** failures:

* `styrtbløde+V+Inf` does not generate!
* `styrtbløde` has no analyses either

**tilstøyte** failures:

* `tilstøyte+V+Inf` => `tilstøyt`
* `tilstøyte` has no analyses either

**oppsede** failures:

* `oppsede+V+Inf` => `oppsed`
* `oppsede` has no analyses either

**permittere** failures:

* `permittere+V+Inf` => `permitter`
* `permittere` has no analyses either

**hæde** failures:

* `hæde+V+Inf` does not generate!
* `hæde` has no analyses either

**pjòte** failures:

* `pjòte+V+Inf` => `pjòt`
* `pjòte` has no analyses either

**komparere** failures:

* `komparere+V+Inf` => `komparer`
* `komparere` has no analyses either

**gjøle** failures:

* `gjøle+V+Inf` => `gjøl`
* `gjøle` has following analyses:
  * `gjøle+A+A+Neu+Sg+Indef`
  * `gjøle+A+A+Msc+Sg+Indef`
  * `gjøle+A+A+Fem+Sg+Indef`

**spike** failures:

* `spike+V+Inf` does not generate!
* `spike` has no analyses either

**træde** failures:

* `træde+V+Inf` does not generate!
* `træde` has no analyses either

**rådgi** failures:

* `rådgi+V+Inf` => `rådggi`
* `rådgi` has no analyses either

**lognast** failures:

* `lognast+V+Inf` => `lognastast`
* `lognast` has following analyses:
  * `logn+A+Superl+Indef`

**fleine** failures:

* `fleine+V+Inf` => `flein`
* `fleine` has following analyses:
  * `flein+A+Sg+Def`
  * `flein+A+Pl+Indef`

**livne** failures:

* `livne+V+Inf` => `livn`
* `livne` has no analyses either

**godmæte** failures:

* `godmæte+V+Inf` => `godmæt`
* `godmæte` has no analyses either

**tuppe** failures:

* `tuppe+V+Inf` => `tupp`
* `tuppe` has no analyses either

**kvine** failures:

* `kvine+V+Inf` => `kvie`
* `kvine+V+Inf` => `kvia`
* `kvine` has following analyses:
  * `kvine+V+Adj+Bu+Eint`
  * `kvine+V+Adj+Fl`

**feie** failures:

* `feie+V+Inf` => `fei`
* `feie` has no analyses either

**predestinere** failures:

* `predestinere+V+Inf` => `predestiner`
* `predestinere` has no analyses either

**fjagge** failures:

* `fjagge+V+Inf` => `fjagg`
* `fjagge` has no analyses either

**dunge** failures:

* `dunge+V+Inf` => `dung`
* `dunge` has no analyses either

**gnikse** failures:

* `gnikse+V+Inf` => `gniks`
* `gnikse` has no analyses either

**trigge** failures:

* `trigge+V+Inf` => `trigg`
* `trigge` has no analyses either

**flure** failures:

* `flure+V+Inf` => `flur`
* `flure` has following analyses:
  * `fluren+A+Neu+Sg+Indef`

**sensibilisere** failures:

* `sensibilisere+V+Inf` => `sensibiliser`
* `sensibilisere` has no analyses either

**bemanne** failures:

* `bemanne+V+Inf` => `bemann`
* `bemanne` has no analyses either

**kvimse** failures:

* `kvimse+V+Inf` => `kvims`
* `kvimse` has following analyses:
  * `kvimsen+A+Neu+Sg+Indef`

**forfare** failures:

* `forfare+V+Inf` => `forfae`
* `forfare+V+Inf` => `forfaa`
* `forfare` has following analyses:
  * `forfaren+A+Neu+Sg+Indef`

**erindre** failures:

* `erindre+V+Inf` => `erindr`
* `erindre` has no analyses either

**forelese** failures:

* `forelese+V+Inf` => `forelea`
* `forelese+V+Inf` => `forelee`
* `forelese` has no analyses either

**helvte** failures:

* `helvte+V+Inf` => `helvt`
* `helvte` has no analyses either

**testamentere** failures:

* `testamentere+V+Inf` => `testamenter`
* `testamentere` has no analyses either

**retirere** failures:

* `retirere+V+Inf` => `retirer`
* `retirere` has no analyses either

**dæse** failures:

* `dæse+V+Inf` => `dæs`
* `dæse` has no analyses either

**famle** failures:

* `famle+V+Inf` => `faml`
* `famle` has no analyses either

**flue** failures:

* `flue+V+Inf` => `flu`
* `flue` has no analyses either

**nyskape** failures:

* `nyskape+V+Inf` does not generate!
* `nyskape` has no analyses either

**konnektere** failures:

* `konnektere+V+Inf` => `konnekter`
* `konnektere` has no analyses either

**kløkkje** failures:

* `kløkkje+V+Inf` does not generate!
* `kløkkje` has no analyses either

**kringsette** failures:

* `kringsette+V+Inf` does not generate!
* `kringsette` has no analyses either

**skride** failures:

* `skride+V+Inf` does not generate!
* `skride` has no analyses either

**pønitere** failures:

* `pønitere+V+Inf` => `pøniter`
* `pønitere` has no analyses either

**danske** failures:

* `danske+V+Inf` => `dansk`
* `danske` has following analyses:
  * `dansk+A+A+Sg+Def`
  * `dansk+A+A+Pl+Def`
  * `dansk+A+A+Pl+Indef`

**gare** failures:

* `gare+V+Inf` => `gar`
* `gare` has no analyses either

**naudbede** failures:

* `naudbede+V+Inf` => `naudbea`
* `naudbede+V+Inf` => `naudbee`
* `naudbede` has no analyses either

**omkomle** failures:

* `omkomle+V+Inf` => `omkoml`
* `omkomle` has no analyses either

**slengje** failures:

* `slengje+V+Inf` does not generate!
* `slengje` has no analyses either

**havarere** failures:

* `havarere+V+Inf` => `havarer`
* `havarere` has no analyses either

**fordanske** failures:

* `fordanske+V+Inf` => `fordansk`
* `fordanske` has no analyses either

**styrke** failures:

* `styrke+V+Inf` => `styrk`
* `styrke` has no analyses either

**tusse** failures:

* `tusse+V+Inf` => `tuss`
* `tusse` has no analyses either

**brime** failures:

* `brime+V+Inf` => `brim`
* `brime` has no analyses either

**innvende** failures:

* `innvende+V+Inf` does not generate!
* `innvende` has no analyses either

**kreppe** failures:

* `kreppe+V+Inf` does not generate!
* `kreppe` has no analyses either

**kontrasignere** failures:

* `kontrasignere+V+Inf` => `kontrasigner`
* `kontrasignere` has no analyses either

**oppdatere** failures:

* `oppdatere+V+Inf` => `oppdater`
* `oppdatere` has no analyses either

**samordne** failures:

* `samordne+V+Inf` => `samordn`
* `samordne` has no analyses either

**halvsteikje** failures:

* `halvsteikje+V+Inf` does not generate!
* `halvsteikje` has no analyses either

**nevast** failures:

* `nevast+V+Inf` => `nevastast`
* `nevast` has no analyses either

**berke** failures:

* `berke+V+Inf` => `berk`
* `berke` has no analyses either

**silregne** failures:

* `silregne+V+Inf` does not generate!
* `silregne` has no analyses either

**tannast** failures:

* `tannast+V+Inf` => `tannastast`
* `tannast` has no analyses either

**tyggje** failures:

* `tyggje+V+Inf` => `tyje`
* `tyggje+V+Inf` => `tyja`
* `tyggje` has no analyses either

**skylpe** failures:

* `skylpe+V+Inf` => `skylp`
* `skylpe` has no analyses either

**ampe** failures:

* `ampe+V+Inf` => `amp`
* `ampe` has following analyses:
  * `amper+A+A+Pl+Def`
  * `ampen+A+Neu+Sg+Indef`
  * `amper+A+A+Sg+Def`
  * `amper+A+A+Pl+Indef`

**abandonere** failures:

* `abandonere+V+Inf` => `abandoner`
* `abandonere` has no analyses either

**maste** failures:

* `maste+V+Inf` => `mast`
* `maste` has no analyses either

**gruppevaldta** failures:

* `gruppevaldta+V+Inf` => `gruppevaldtta`
* `gruppevaldta` has no analyses either

**grabbe** failures:

* `grabbe+V+Inf` => `grabb`
* `grabbe` has no analyses either

**skvise** failures:

* `skvise+V+Inf` => `skvis`
* `skvise` has no analyses either

**møblere** failures:

* `møblere+V+Inf` => `møbler`
* `møblere` has no analyses either

**hugstele** failures:

* `hugstele+V+Inf` => `hugstea`
* `hugstele+V+Inf` => `hugstee`
* `hugstele` has no analyses either

**visitere** failures:

* `visitere+V+Inf` => `visiter`
* `visitere` has no analyses either

**plumpe** failures:

* `plumpe+V+Inf` => `plump`
* `plumpe` has following analyses:
  * `plump+A+Pl+Indef`
  * `plump+A+Sg+Def`

**endre** failures:

* `endre+V+Inf` => `endr`
* `endre` has no analyses either

**varde** failures:

* `varde+V+Inf` => `vard`
* `varde` has no analyses either

**krympe** failures:

* `krympe+V+Inf` => `krymp`
* `krympe` has no analyses either

**skure** failures:

* `skure+V+Inf` => `skur`
* `skure` has no analyses either

**misbyde** failures:

* `misbyde+V+Inf` does not generate!
* `misbyde` has no analyses either

**perkutere** failures:

* `perkutere+V+Inf` => `perkuter`
* `perkutere` has no analyses either

**pryde** failures:

* `pryde+V+Inf` does not generate!
* `pryde` has no analyses either

**framstille** failures:

* `framstille+V+Inf` does not generate!
* `framstille` has no analyses either

**augnefare** failures:

* `augnefare+V+Inf` => `augnefae`
* `augnefare+V+Inf` => `augnefaa`
* `augnefare` has no analyses either

**svervle** failures:

* `svervle+V+Inf` => `svervl`
* `svervle` has no analyses either

**kryssklippe** failures:

* `kryssklippe+V+Inf` does not generate!
* `kryssklippe` has no analyses either

**trøye** failures:

* `trøye+V+Inf` does not generate!
* `trøye` has no analyses either

**medføre** failures:

* `medføre+V+Inf` does not generate!
* `medføre` has no analyses either

**kryvje** failures:

* `kryvje+V+Inf` => `kryja`
* `kryvje+V+Inf` => `kryje`
* `kryvje` has no analyses either

**choke** failures:

* `choke+V+Inf` => `chok`
* `choke` has no analyses either

**fukte** failures:

* `fukte+V+Inf` => `fukt`
* `fukte` has no analyses either

**harpe** failures:

* `harpe+V+Inf` => `harp`
* `harpe` has no analyses either

**sneve** failures:

* `sneve+V+Inf` => `snev`
* `sneve` has following analyses:
  * `snever+A+A+Pl+Indef`
  * `snever+A+A+Sg+Def`
  * `snever+A+A+Pl+Def`

**årast** failures:

* `årast+V+Inf` => `årastast`
* `årast` has no analyses either

**true** failures:

* `true+V+Inf` => `tru`
* `true` has following analyses:
  * `trudd+A+A+Pl+Def`
  * `tru+A+Sg+Def`
  * `trudd+A+A+Sg+Def`
  * `tru+A+Pl+Indef`
  * `trudd+A+A+Pl+Indef`

**inkarnere** failures:

* `inkarnere+V+Inf` => `inkarner`
* `inkarnere` has no analyses either

**rundvaske** failures:

* `rundvaske+V+Inf` => `rundvask`
* `rundvaske` has no analyses either

**aue** failures:

* `aue+V+Inf` => `au`
* `aue` has no analyses either

**nage** failures:

* `nage+V+Inf` => `nag`
* `nage` has no analyses either

**køe** failures:

* `køe+V+Inf` => `kø`
* `køe` has no analyses either

**dotere** failures:

* `dotere+V+Inf` => `doter`
* `dotere` has no analyses either

**sesse** failures:

* `sesse+V+Inf` => `sess`
* `sesse` has no analyses either

**inkriminere** failures:

* `inkriminere+V+Inf` => `inkriminer`
* `inkriminere` has no analyses either

**hovere** failures:

* `hovere+V+Inf` => `hover`
* `hovere` has no analyses either

**sliske** failures:

* `sliske+V+Inf` => `slisk`
* `sliske` has no analyses either

**latterleggjere** failures:

* `latterleggjere+V+Inf` does not generate!
* `latterleggjere` has no analyses either

**gråe** failures:

* `gråe+V+Inf` => `grå`
* `gråe` has following analyses:
  * `gråte+V+Inf`
  * `grå+A+Pl+Indef`
  * `gråte+V+PrfPrc`
  * `grå+A+Sg+Def`
  * `gråte+V+Adj+Nøyt+Ub+Eint`

**hòle** failures:

* `hòle+V+Inf` => `hòl`
* `hòle` has no analyses either

**intimere** failures:

* `intimere+V+Inf` => `intimer`
* `intimere` has no analyses either

**muntre** failures:

* `muntre+V+Inf` => `muntr`
* `muntre` has no analyses either

**avertere** failures:

* `avertere+V+Inf` => `averter`
* `avertere` has no analyses either

**turve** failures:

* `turve+V+Inf` => `tue`
* `turve+V+Inf` => `tua`
* `turve` has no analyses either

**puske** failures:

* `puske+V+Inf` => `pusk`
* `puske` has no analyses either

**mutere** failures:

* `mutere+V+Inf` => `muter`
* `mutere` has no analyses either

**fósne** failures:

* `fósne+V+Inf` => `fósn`
* `fósne` has following analyses:
  * `fósen+A+Sg+Def`
  * `fósen+A+Pl+Indef`

**dynte** failures:

* `dynte+V+Inf` => `dynt`
* `dynte` has no analyses either

**sindre** failures:

* `sindre+V+Inf` => `sindr`
* `sindre` has no analyses either

**rekylere** failures:

* `rekylere+V+Inf` => `rekyler`
* `rekylere` has no analyses either

**baneleggje** failures:

* `baneleggje+V+Inf` does not generate!
* `baneleggje` has no analyses either

**nite** failures:

* `nite+V+Inf` => `nia`
* `nite+V+Inf` => `nie`
* `nite` has following analyses:
  * `niten+A+Neu+Sg+Indef`

**brulegge** failures:

* `brulegge+V+Inf` does not generate!
* `brulegge` has no analyses either

**snikmelde** failures:

* `snikmelde+V+Inf` does not generate!
* `snikmelde` has no analyses either

**utsetje** failures:

* `utsetje+V+Inf` does not generate!
* `utsetje` has no analyses either

**flokse** failures:

* `flokse+V+Inf` => `floks`
* `flokse` has no analyses either

**kløne** failures:

* `kløne+V+Inf` => `kløn`
* `kløne` has no analyses either

**rikgjere** failures:

* `rikgjere+V+Inf` does not generate!
* `rikgjere` has no analyses either

**tankføre** failures:

* `tankføre+V+Inf` => `tankfør`
* `tankføre` has no analyses either

**demobilisere** failures:

* `demobilisere+V+Inf` => `demobiliser`
* `demobilisere` has no analyses either

**husle** failures:

* `husle+V+Inf` => `husl`
* `husle` has no analyses either

**gassforgifte** failures:

* `gassforgifte+V+Inf` => `gassforgift`
* `gassforgifte` has no analyses either

**skulse** failures:

* `skulse+V+Inf` => `skuls`
* `skulse` has following analyses:
  * `skuls+A+A+Sg+Def`
  * `skuls+A+A+Pl+Indef`
  * `skuls+A+A+Pl+Def`

**ståsse** failures:

* `ståsse+V+Inf` => `ståss`
* `ståsse` has no analyses either

**promovere** failures:

* `promovere+V+Inf` => `promover`
* `promovere` has no analyses either

**mistake** failures:

* `mistake+V+Inf` => `misttake`
* `mistake+V+Inf` => `misttaka`
* `mistake` has no analyses either

**rektifisere** failures:

* `rektifisere+V+Inf` => `rektifiser`
* `rektifisere` has no analyses either

**kumle** failures:

* `kumle+V+Inf` => `kuml`
* `kumle` has following analyses:
  * `kumle+N+N+Fem+Sg+Indef`

**lagast** failures:

* `lagast+V+Inf` => `lagastast`
* `lagast` has no analyses either

**målbere** failures:

* `målbere+V+Inf` => `målbea`
* `målbere+V+Inf` => `målbee`
* `målbere` has no analyses either

**balle** failures:

* `balle+V+Inf` => `ball`
* `balle` has no analyses either

**katte** failures:

* `katte+V+Inf` => `katt`
* `katte` has no analyses either

**blafre** failures:

* `blafre+V+Inf` => `blafr`
* `blafre` has no analyses either

**vasle** failures:

* `vasle+V+Inf` => `vasl`
* `vasle` has no analyses either

**spele** failures:

* `spele+V+Inf` does not generate!
* `spele` has no analyses either

**døype** failures:

* `døype+V+Inf` => `døyp`
* `døype` has no analyses either

**laste** failures:

* `laste+V+Inf` => `last`
* `laste` has following analyses:
  * `låg+A+Superl+Def`

**løyse** failures:

* `løyse+V+Inf` => `løys`
* `løyse` has no analyses either

**intimisere** failures:

* `intimisere+V+Inf` => `intimiser`
* `intimisere` has no analyses either

**mangedoble** failures:

* `mangedoble+V+Inf` => `mangedobl`
* `mangedoble` has no analyses either

**sårke** failures:

* `sårke+V+Inf` => `sårk`
* `sårke` has no analyses either

**vike** failures:

* `vike+V+Inf` => `v`
* `vike` has no analyses either

**pigge** failures:

* `pigge+V+Inf` => `pigg`
* `pigge` has following analyses:
  * `pigg+A+A+Pl+Indef`
  * `pigg+A+A+Sg+Def`
  * `pigg+A+A+Pl+Def`

**oppdra** failures:

* `oppdra+V+Inf` => `oppdrdra`
* `oppdra` has no analyses either

**erfare** failures:

* `erfare+V+Inf` does not generate!
* `erfare` has following analyses:
  * `erfaren+A+Neu+Sg+Indef`

**gjeppe** failures:

* `gjeppe+V+Inf` => `gjepp`
* `gjeppe` has no analyses either

**dominere** failures:

* `dominere+V+Inf` => `dominer`
* `dominere` has no analyses either

**garnere** failures:

* `garnere+V+Inf` => `garner`
* `garnere` has no analyses either

**vantrivast** failures:

* `vantrivast+V+Inf` => `vantriast`
* `vantrivast` has no analyses either

**krossmerkje** failures:

* `krossmerkje+V+Inf` does not generate!
* `krossmerkje` has no analyses either

**sautere** failures:

* `sautere+V+Inf` => `sauter`
* `sautere` has no analyses either

**fasettere** failures:

* `fasettere+V+Inf` => `fasetter`
* `fasettere` has no analyses either

**prøvekøyre** failures:

* `prøvekøyre+V+Inf` does not generate!
* `prøvekøyre` has no analyses either

**slaske** failures:

* `slaske+V+Inf` => `slask`
* `slaske` has no analyses either

**livskremme** failures:

* `livskremme+V+Inf` does not generate!
* `livskremme` has no analyses either

**hinte** failures:

* `hinte+V+Inf` => `hint`
* `hinte` has no analyses either

**forskale** failures:

* `forskale+V+Inf` => `forskal`
* `forskale` has no analyses either

**gremje** failures:

* `gremje+V+Inf` does not generate!
* `gremje` has no analyses either

**sluntre** failures:

* `sluntre+V+Inf` => `sluntr`
* `sluntre` has no analyses either

**påtale** failures:

* `påtale+V+Inf` does not generate!
* `påtale` has no analyses either

**arkebusere** failures:

* `arkebusere+V+Inf` => `arkebuser`
* `arkebusere` has no analyses either

**glyme** failures:

* `glyme+V+Inf` does not generate!
* `glyme` has no analyses either

**utplassere** failures:

* `utplassere+V+Inf` => `utplasser`
* `utplassere` has no analyses either

**knuppe** failures:

* `knuppe+V+Inf` => `knupp`
* `knuppe` has no analyses either

**benke** failures:

* `benke+V+Inf` => `benk`
* `benke` has no analyses either

**klore** failures:

* `klore+V+Inf` => `klor`
* `klore` has no analyses either

**befale** failures:

* `befale+V+Inf` => `befal`
* `befale` has no analyses either

**raljere** failures:

* `raljere+V+Inf` => `raljer`
* `raljere` has no analyses either

**atomisere** failures:

* `atomisere+V+Inf` => `atomiser`
* `atomisere` has no analyses either

**kneskure** failures:

* `kneskure+V+Inf` => `kneskur`
* `kneskure` has no analyses either

**ryle** failures:

* `ryle+V+Inf` => `ryl`
* `ryle` has no analyses either

**klone** failures:

* `klone+V+Inf` => `klon`
* `klone` has following analyses:
  * `klyve+V+Adj+Fl`
  * `klyve+V+Adj+Bu+Eint`
  * `klette+V+Adj+Bu+Eint`
  * `klette+V+Adj+Fl`

**lukrere** failures:

* `lukrere+V+Inf` => `lukrer`
* `lukrere` has no analyses either

**strigråte** failures:

* `strigråte+V+Inf` => `strigråe`
* `strigråte+V+Inf` => `strigråa`
* `strigråte` has no analyses either

**nedverdige** failures:

* `nedverdige+V+Inf` => `nedverdig`
* `nedverdige` has no analyses either

**skurne** failures:

* `skurne+V+Inf` => `skurn`
* `skurne` has no analyses either

**klinkbygge** failures:

* `klinkbygge+V+Inf` does not generate!
* `klinkbygge` has no analyses either

**provosere** failures:

* `provosere+V+Inf` => `provoser`
* `provosere` has no analyses either

**demokratisere** failures:

* `demokratisere+V+Inf` => `demokratiser`
* `demokratisere` has no analyses either

**forrette** failures:

* `forrette+V+Inf` => `forrett`
* `forrette` has no analyses either

**krasle** failures:

* `krasle+V+Inf` => `krasl`
* `krasle` has following analyses:
  * `kraslen+A+Neu+Sg+Indef`

**designe** failures:

* `designe+V+Inf` => `design`
* `designe` has no analyses either

**omskape** failures:

* `omskape+V+Inf` does not generate!
* `omskape` has no analyses either

**glidke** failures:

* `glidke+V+Inf` => `glidk`
* `glidke` has no analyses either

**rine** failures:

* `rine+V+Inf` => `ria`
* `rine+V+Inf` => `rie`
* `rine` has following analyses:
  * `rive+V+Adj+Bu+Eint`
  * `rine+V+Adj+Fl`
  * `rive+V+Adj+Fl`
  * `rine+V+Adj+Bu+Eint`

**radiere** failures:

* `radiere+V+Inf` => `radier`
* `radiere` has no analyses either

**kjappe** failures:

* `kjappe+V+Inf` => `kjapp`
* `kjappe` has no analyses either

**rauste** failures:

* `rauste+V+Inf` => `raust`
* `rauste` has following analyses:
  * `raust+A+A+Sg+Def`
  * `raust+A+A+Pl+Indef`
  * `raust+A+A+Pl+Def`

**ettergi** failures:

* `ettergi+V+Inf` => `etterggi`
* `ettergi` has no analyses either

**hikre** failures:

* `hikre+V+Inf` => `hikr`
* `hikre` has no analyses either

**geberde** failures:

* `geberde+V+Inf` => `geberd`
* `geberde` has no analyses either

**sevjebarke** failures:

* `sevjebarke+V+Inf` => `sevjebark`
* `sevjebarke` has no analyses either

**ekstrudere** failures:

* `ekstrudere+V+Inf` => `ekstruder`
* `ekstrudere` has no analyses either

**desillusjonere** failures:

* `desillusjonere+V+Inf` => `desillusjoner`
* `desillusjonere` has no analyses either

**akkommodere** failures:

* `akkommodere+V+Inf` => `akkommoder`
* `akkommodere` has no analyses either

**omskjere** failures:

* `omskjere+V+Inf` => `omskjee`
* `omskjere+V+Inf` => `omskjea`
* `omskjere` has no analyses either

**lyde** failures:

* `lyde+V+Inf` does not generate!
* `lyde` has following analyses:
  * `lyd+A+Pl+Indef`
  * `lyd+A+Sg+Def`

**legne** failures:

* `legne+V+Inf` => `legn`
* `legne` has no analyses either

**ry** failures:

* `ry+V+Inf` does not generate!
* `ry` has following analyses:
  * `rye+N+N+Fem+Sg+Indef`
  * `ry+N+N+Neu+Sg+Indef`
  * `ryte+V+Imprt`
  * `ry+N+N+Neu+Pl+Indef`
  * `ryte+V+Ind+Prs`
  * `ryke+V+Ind+Prs`
  * `rye+N+N+Msc+Sg+Indef`
  * `ryke+V+Imprt`

**indusere** failures:

* `indusere+V+Inf` => `induser`
* `indusere` has no analyses either

**annast** failures:

* `annast+V+Inf` => `annastast`
* `annast` has no analyses either

**fukle** failures:

* `fukle+V+Inf` => `fukl`
* `fukle` has no analyses either

**terse** failures:

* `terse+V+Inf` => `ters`
* `terse` has no analyses either

**flasse** failures:

* `flasse+V+Inf` => `flass`
* `flasse` has no analyses either

**betrast** failures:

* `betrast+V+Inf` => `betrastast`
* `betrast` has no analyses either

**lurtenkje** failures:

* `lurtenkje+V+Inf` does not generate!
* `lurtenkje` has no analyses either

**mate** failures:

* `mate+V+Inf` => `mat`
* `mate` has no analyses either

**oppvurdere** failures:

* `oppvurdere+V+Inf` => `oppvurder`
* `oppvurdere` has no analyses either

**trave** failures:

* `trave+V+Inf` => `trav`
* `trave` has following analyses:
  * `travel+A+A+Pl+Indef`
  * `travel+A+A+Pl+Def`
  * `travel+A+A+Sg+Def`

**grunnleggje** failures:

* `grunnleggje+V+Inf` does not generate!
* `grunnleggje` has no analyses either

**tørrhoste** failures:

* `tørrhoste+V+Inf` => `tørrhost`
* `tørrhoste` has no analyses either

**egge** failures:

* `egge+V+Inf` => `egg`
* `egge` has no analyses either

**skrøkke** failures:

* `skrøkke+V+Inf` => `skrøa`
* `skrøkke+V+Inf` => `skrøe`
* `skrøkke` has no analyses either

**frigi** failures:

* `frigi+V+Inf` => `friggi`
* `frigi` has no analyses either

**varmherde** failures:

* `varmherde+V+Inf` does not generate!
* `varmherde` has no analyses either

**ptroe** failures:

* `ptroe+V+Inf` => `ptro`
* `ptroe` has no analyses either

**fyllekøyre** failures:

* `fyllekøyre+V+Inf` does not generate!
* `fyllekøyre` has no analyses either

**vidke** failures:

* `vidke+V+Inf` => `vidk`
* `vidke` has no analyses either

**milde** failures:

* `milde+V+Inf` => `mild`
* `milde` has following analyses:
  * `mild+A+Sg+Def`
  * `mild+A+Pl+Indef`

**rende** failures:

* `rende+V+Inf` => `rend`
* `rende` has no analyses either

**jamre** failures:

* `jamre+V+Inf` => `jamr`
* `jamre` has no analyses either

**saure** failures:

* `saure+V+Inf` => `saur`
* `saure` has no analyses either

**secernere** failures:

* `secernere+V+Inf` => `secerner`
* `secernere` has no analyses either

**sexdebutere** failures:

* `sexdebutere+V+Inf` => `sexdebuter`
* `sexdebutere` has no analyses either

**tudre** failures:

* `tudre+V+Inf` => `tudr`
* `tudre` has no analyses either

**spasere** failures:

* `spasere+V+Inf` => `spaser`
* `spasere` has no analyses either

**overstråle** failures:

* `overstråle+V+Inf` => `overstrål`
* `overstråle` has no analyses either

**varmeisolere** failures:

* `varmeisolere+V+Inf` => `varmeisoler`
* `varmeisolere` has no analyses either

**skyte** failures:

* `skyte+V+Inf` => `skya`
* `skyte+V+Inf` => `skye`
* `skyte` has no analyses either

**proletarisere** failures:

* `proletarisere+V+Inf` => `proletariser`
* `proletarisere` has no analyses either

**dolme** failures:

* `dolme+V+Inf` => `dolm`
* `dolme` has following analyses:
  * `dolmen+A+Neu+Sg+Indef`

**prenumerere** failures:

* `prenumerere+V+Inf` => `prenumerer`
* `prenumerere` has no analyses either

**skjøne** failures:

* `skjøne+V+Inf` does not generate!
* `skjøne` has following analyses:
  * `skjønn+A+A+Sg+Def`
  * `skjønn+A+A+Pl+Indef`
  * `skjønn+A+A+Pl+Def`

**drupse** failures:

* `drupse+V+Inf` => `drups`
* `drupse` has no analyses either

**våre** failures:

* `våre+V+Inf` => `vår`
* `våre` has following analyses:
  * `våre+Det+Poss+Pl`

**ynge** failures:

* `ynge+V+Inf` does not generate!
* `ynge` has no analyses either

**flepe** failures:

* `flepe+V+Inf` => `flep`
* `flepe` has no analyses either

**skumpe** failures:

* `skumpe+V+Inf` => `skump`
* `skumpe` has no analyses either

**belge** failures:

* `belge+V+Inf` => `belg`
* `belge` has no analyses either

**plane** failures:

* `plane+V+Inf` => `plan`
* `plane` has following analyses:
  * `plan+A+Sg+Def`
  * `plan+A+Pl+Indef`

**vattere** failures:

* `vattere+V+Inf` => `vatter`
* `vattere` has no analyses either

**dunse** failures:

* `dunse+V+Inf` => `duns`
* `dunse` has no analyses either

**avruse** failures:

* `avruse+V+Inf` => `avrus`
* `avruse` has no analyses either

**eksistere** failures:

* `eksistere+V+Inf` => `eksister`
* `eksistere` has no analyses either

**finfordele** failures:

* `finfordele+V+Inf` => `finfordel`
* `finfordele` has no analyses either

**handrekkast** failures:

* `handrekkast+V+Inf` => `handrekast`
* `handrekkast` has no analyses either

**plagast** failures:

* `plagast+V+Inf` => `plagastast`
* `plagast` has no analyses either

**låtteleggjere** failures:

* `låtteleggjere+V+Inf` does not generate!
* `låtteleggjere` has no analyses either

**fenne** failures:

* `fenne+V+Inf` does not generate!
* `fenne` has no analyses either

**lånta** failures:

* `lånta+V+Inf` => `låntta`
* `lånta` has no analyses either

**sporte** failures:

* `sporte+V+Inf` => `sport`
* `sporte` has no analyses either

**mørne** failures:

* `mørne+V+Inf` => `mørn`
* `mørne` has no analyses either

**guve** failures:

* `guve+V+Inf` => `guv`
* `guve` has following analyses:
  * `guv+A+Pl+Indef`
  * `guv+A+Sg+Def`

**kokemale** failures:

* `kokemale+V+Inf` => `kokemaa`
* `kokemale+V+Inf` => `kokemae`
* `kokemale` has following analyses:
  * `kokemalen+A+Neu+Sg+Indef`

**halse** failures:

* `halse+V+Inf` => `hals`
* `halse` has no analyses either

**kvite** failures:

* `kvite+V+Inf` => `kvit`
* `kvite` has following analyses:
  * `kvit+A+A+Sg+Def`
  * `kvit+A+A+Pl+Def`
  * `kvit+A+A+Pl+Indef`

**stede** failures:

* `stede+V+Inf` does not generate!
* `stede` has following analyses:
  * `stedd+A+A+Pl+Def`
  * `stedd+A+A+Pl+Indef`
  * `stedd+A+A+Sg+Def`

**sandblåse** failures:

* `sandblåse+V+Inf` => `sandblåa`
* `sandblåse+V+Inf` => `sandblåe`
* `sandblåse` has no analyses either

**hånle** failures:

* `hånle+V+Inf` => `hånlle`
* `hånle` has no analyses either

**tusle** failures:

* `tusle+V+Inf` => `tusl`
* `tusle` has no analyses either

**bramme** failures:

* `bramme+V+Inf` => `bram`
* `bramme` has no analyses either

**kise** failures:

* `kise+V+Inf` does not generate!
* `kise` has no analyses either

**detonere** failures:

* `detonere+V+Inf` => `detoner`
* `detonere` has no analyses either

**lynsje** failures:

* `lynsje+V+Inf` => `lynsj`
* `lynsje` has no analyses either

**diskvalifisere** failures:

* `diskvalifisere+V+Inf` => `diskvalifiser`
* `diskvalifisere` has no analyses either

**øksle** failures:

* `øksle+V+Inf` => `øksl`
* `øksle` has no analyses either

**spjelke** failures:

* `spjelke+V+Inf` => `spjelk`
* `spjelke` has no analyses either

**spekkmåle** failures:

* `spekkmåle+V+Inf` => `spekkmål`
* `spekkmåle` has no analyses either

**runke** failures:

* `runke+V+Inf` => `runk`
* `runke` has no analyses either

**skuldgjeve** failures:

* `skuldgjeve+V+Inf` => `skuldggjeve`
* `skuldgjeve+V+Inf` => `skuldggjeva`
* `skuldgjeve` has following analyses:
  * `skuldgjeven+A+Neu+Sg+Indef`

**slume** failures:

* `slume+V+Inf` => `slum`
* `slume` has following analyses:
  * `slumen+A+Neu+Sg+Indef`

**bedøve** failures:

* `bedøve+V+Inf` does not generate!
* `bedøve` has no analyses either

**greie** failures:

* `greie+V+Inf` does not generate!
* `greie` has following analyses:
  * `grei+A+Pl+Indef`
  * `grei+A+Sg+Def`

**kivast** failures:

* `kivast+V+Inf` => `kivastast`
* `kivast` has no analyses either

**radreinske** failures:

* `radreinske+V+Inf` => `radreinsk`
* `radreinske` has no analyses either

**gjennomsteike** failures:

* `gjennomsteike+V+Inf` => `gjennomsteik`
* `gjennomsteike` has no analyses either

**gjen** failures:

* `gjen+V+Inf` does not generate!
* `gjen` has no analyses either

**eksotisere** failures:

* `eksotisere+V+Inf` => `eksotiser`
* `eksotisere` has no analyses either

**drive** failures:

* `drive+V+Inf` => `drie`
* `drive+V+Inf` => `dria`
* `drive` has following analyses:
  * `driven+A+Neu+Sg+Indef`

**lage** failures:

* `lage+V+Inf` => `lag`
* `lage` has following analyses:
  * `lage+A+A+Neu+Sg+Indef`
  * `lage+A+A+Fem+Sg+Indef`
  * `lage+A+A+Msc+Sg+Indef`

**bønfalle** failures:

* `bønfalle+V+Inf` => `bønfae`
* `bønfalle+V+Inf` => `bønfaa`
* `bønfalle` has no analyses either

**jerpe** failures:

* `jerpe+V+Inf` => `jerp`
* `jerpe` has no analyses either

**ote** failures:

* `ote+V+Inf` => `ot`
* `ote` has no analyses either

**forstyrre** failures:

* `forstyrre+V+Inf` => `forstyrr`
* `forstyrre` has no analyses either

**skriftfeste** failures:

* `skriftfeste+V+Inf` does not generate!
* `skriftfeste` has no analyses either

**butte** failures:

* `butte+V+Inf` => `butt`
* `butte` has following analyses:
  * `butt+A+A+Pl+Def`
  * `butt+A+A+Sg+Def`
  * `butt+A+A+Pl+Indef`

**negere** failures:

* `negere+V+Inf` => `neger`
* `negere` has no analyses either

**åte** failures:

* `åte+V+Inf` => `åt`
* `åte` has no analyses either

**skuffe** failures:

* `skuffe+V+Inf` => `skuff`
* `skuffe` has no analyses either

**omhandle** failures:

* `omhandle+V+Inf` => `omhandl`
* `omhandle` has no analyses either

**føresetje** failures:

* `føresetje+V+Inf` does not generate!
* `føresetje` has no analyses either

**lysne** failures:

* `lysne+V+Inf` => `lysn`
* `lysne` has no analyses either

**tysje** failures:

* `tysje+V+Inf` does not generate!
* `tysje` has no analyses either

**avbryte** failures:

* `avbryte+V+Inf` => `avbrye`
* `avbryte+V+Inf` => `avbrya`
* `avbryte` has no analyses either

**seksdoble** failures:

* `seksdoble+V+Inf` => `seksdobl`
* `seksdoble` has no analyses either

**denotere** failures:

* `denotere+V+Inf` => `denoter`
* `denotere` has no analyses either

**skamfare** failures:

* `skamfare+V+Inf` => `skamfaa`
* `skamfare+V+Inf` => `skamfae`
* `skamfare` has no analyses either

**taksere** failures:

* `taksere+V+Inf` => `takser`
* `taksere` has no analyses either

**blokkfryse** failures:

* `blokkfryse+V+Inf` => `blokkfrys`
* `blokkfryse` has no analyses either

**spesifisere** failures:

* `spesifisere+V+Inf` => `spesifiser`
* `spesifisere` has no analyses either

**trykkimpregnere** failures:

* `trykkimpregnere+V+Inf` => `trykkimpregner`
* `trykkimpregnere` has no analyses either

**skarre** failures:

* `skarre+V+Inf` => `skarr`
* `skarre` has no analyses either

**forsøple** failures:

* `forsøple+V+Inf` => `forsøpl`
* `forsøple` has no analyses either

**beklage** failures:

* `beklage+V+Inf` => `beklag`
* `beklage` has no analyses either

**forville** failures:

* `forville+V+Inf` does not generate!
* `forville` has no analyses either

**pensjonere** failures:

* `pensjonere+V+Inf` => `pensjoner`
* `pensjonere` has no analyses either

**garnisonere** failures:

* `garnisonere+V+Inf` => `garnisoner`
* `garnisonere` has no analyses either

**forlokke** failures:

* `forlokke+V+Inf` => `forlokk`
* `forlokke` has no analyses either

**forkorte** failures:

* `forkorte+V+Inf` => `forkort`
* `forkorte` has no analyses either

**urframføre** failures:

* `urframføre+V+Inf` => `urframfør`
* `urframføre` has no analyses either

**politisere** failures:

* `politisere+V+Inf` => `politiser`
* `politisere` has no analyses either

**stortrivast** failures:

* `stortrivast+V+Inf` => `stortriast`
* `stortrivast` has no analyses either

**førebyggje** failures:

* `førebyggje+V+Inf` does not generate!
* `førebyggje` has no analyses either

**slampe** failures:

* `slampe+V+Inf` => `slamp`
* `slampe` has no analyses either

**slike** failures:

* `slike+V+Inf` => `slik`
* `slike` has following analyses:
  * `slik+Det+Dem+Pl`

**hygge** failures:

* `hygge+V+Inf` does not generate!
* `hygge` has no analyses either

**grapse** failures:

* `grapse+V+Inf` => `graps`
* `grapse` has no analyses either

**eike** failures:

* `eike+V+Inf` => `eik`
* `eike` has no analyses either

**spjåke** failures:

* `spjåke+V+Inf` => `spjåk`
* `spjåke` has no analyses either

**eve** failures:

* `eve+V+Inf` => `ev`
* `eve` has no analyses either

**tromle** failures:

* `tromle+V+Inf` => `troml`
* `tromle` has no analyses either

**tvisteike** failures:

* `tvisteike+V+Inf` => `tvisteik`
* `tvisteike` has no analyses either

**bukke** failures:

* `bukke+V+Inf` => `bukk`
* `bukke` has no analyses either

**brygge** failures:

* `brygge+V+Inf` does not generate!
* `brygge` has no analyses either

**feime** failures:

* `feime+V+Inf` => `feim`
* `feime` has following analyses:
  * `feimen+A+Neu+Sg+Indef`

**gjordne** failures:

* `gjordne+V+Inf` => `gjordn`
* `gjordne` has no analyses either

**innskrenke** failures:

* `innskrenke+V+Inf` => `innskrenk`
* `innskrenke` has no analyses either

**nygge** failures:

* `nygge+V+Inf` => `nye`
* `nygge+V+Inf` => `nya`
* `nygge` has no analyses either

**liste** failures:

* `liste+V+Inf` => `list`
* `liste` has no analyses either

**pantsetje** failures:

* `pantsetje+V+Inf` does not generate!
* `pantsetje` has no analyses either

**sveltefore** failures:

* `sveltefore+V+Inf` => `sveltefor`
* `sveltefore` has no analyses either

**debattere** failures:

* `debattere+V+Inf` => `debatter`
* `debattere` has no analyses either

**tjuvtrene** failures:

* `tjuvtrene+V+Inf` => `tjuvtren`
* `tjuvtrene` has no analyses either

**verve** failures:

* `verve+V+Inf` => `verv`
* `verve` has no analyses either

**traspe** failures:

* `traspe+V+Inf` => `trasp`
* `traspe` has no analyses either

**kvamne** failures:

* `kvamne+V+Inf` => `kvamn`
* `kvamne` has no analyses either

**hoie** failures:

* `hoie+V+Inf` => `hoi`
* `hoie` has no analyses either

**lurke** failures:

* `lurke+V+Inf` => `lurk`
* `lurke` has no analyses either

**banepåleggje** failures:

* `banepåleggje+V+Inf` does not generate!
* `banepåleggje` has no analyses either

**yppast** failures:

* `yppast+V+Inf` => `yppastast`
* `yppast` has no analyses either

**snultre** failures:

* `snultre+V+Inf` => `snultr`
* `snultre` has no analyses either

**stanke** failures:

* `stanke+V+Inf` => `stank`
* `stanke` has no analyses either

**spire** failures:

* `spire+V+Inf` => `spir`
* `spire` has no analyses either

**drøse** failures:

* `drøse+V+Inf` => `drøs`
* `drøse` has no analyses either

**knysje** failures:

* `knysje+V+Inf` does not generate!
* `knysje` has no analyses either

**demre** failures:

* `demre+V+Inf` => `demr`
* `demre` has no analyses either

**åtnøye** failures:

* `åtnøye+V+Inf` does not generate!
* `åtnøye` has no analyses either

**trælke** failures:

* `trælke+V+Inf` => `trælk`
* `trælke` has no analyses either

**sprene** failures:

* `sprene+V+Inf` does not generate!
* `sprene` has no analyses either

**animere** failures:

* `animere+V+Inf` => `animer`
* `animere` has no analyses either

**strøye** failures:

* `strøye+V+Inf` does not generate!
* `strøye` has no analyses either

**gnaldre** failures:

* `gnaldre+V+Inf` => `gnaldr`
* `gnaldre` has no analyses either

**konsulere** failures:

* `konsulere+V+Inf` => `konsuler`
* `konsulere` has no analyses either

**kappsigle** failures:

* `kappsigle+V+Inf` does not generate!
* `kappsigle` has no analyses either

**vrenge** failures:

* `vrenge+V+Inf` => `vreng`
* `vrenge` has no analyses either

**skalke** failures:

* `skalke+V+Inf` => `skalk`
* `skalke` has no analyses either

**skårjage** failures:

* `skårjage+V+Inf` => `skårjag`
* `skårjage` has no analyses either

**fòre** failures:

* `fòre+V+Inf` => `fòr`
* `fòre` has no analyses either

**posere** failures:

* `posere+V+Inf` => `poser`
* `posere` has no analyses either

**lurtenke** failures:

* `lurtenke+V+Inf` => `lurtenk`
* `lurtenke` has no analyses either

**orminnast** failures:

* `orminnast+V+Inf` => `orminast`
* `orminnast` has no analyses either

**glideflyge** failures:

* `glideflyge+V+Inf` => `glideflye`
* `glideflyge+V+Inf` => `glideflya`
* `glideflyge` has no analyses either

**tusjere** failures:

* `tusjere+V+Inf` => `tusjer`
* `tusjere` has no analyses either

**disiplinere** failures:

* `disiplinere+V+Inf` => `disipliner`
* `disiplinere` has no analyses either

**inflammere** failures:

* `inflammere+V+Inf` => `inflammer`
* `inflammere` has no analyses either

**invadere** failures:

* `invadere+V+Inf` => `invader`
* `invadere` has no analyses either

**forsnevre** failures:

* `forsnevre+V+Inf` => `forsnevr`
* `forsnevre` has no analyses either

**knuse** failures:

* `knuse+V+Inf` => `knus`
* `knuse` has no analyses either

**skjemmast** failures:

* `skjemmast+V+Inf` => `skjemast`
* `skjemmast` has no analyses either

**fjukte** failures:

* `fjukte+V+Inf` => `fjukt`
* `fjukte` has no analyses either

**grillsteike** failures:

* `grillsteike+V+Inf` => `grillsteik`
* `grillsteike` has no analyses either

**militarisere** failures:

* `militarisere+V+Inf` => `militariser`
* `militarisere` has no analyses either

**vigge** failures:

* `vigge+V+Inf` => `vigg`
* `vigge` has no analyses either

**avlegge** failures:

* `avlegge+V+Inf` does not generate!
* `avlegge` has no analyses either

**avmåte** failures:

* `avmåte+V+Inf` => `avmåt`
* `avmåte` has no analyses either

**selebrere** failures:

* `selebrere+V+Inf` => `selebrer`
* `selebrere` has no analyses either

**lùte** failures:

* `lùte+V+Inf` => `lùt`
* `lùte` has no analyses either

**råsteikje** failures:

* `råsteikje+V+Inf` does not generate!
* `råsteikje` has no analyses either

**hylle** failures:

* `hylle+V+Inf` => `hyll`
* `hylle` has no analyses either

**valne** failures:

* `valne+V+Inf` => `valn`
* `valne` has following analyses:
  * `valen+A+Sg+Def`
  * `valen+A+Pl+Indef`

**frigje** failures:

* `frigje+V+Inf` => `friggje`
* `frigje` has no analyses either

**rente** failures:

* `rente+V+Inf` => `rent`
* `rente` has no analyses either

**bønnfalle** failures:

* `bønnfalle+V+Inf` => `bønnfaa`
* `bønnfalle+V+Inf` => `bønnfae`
* `bønnfalle` has no analyses either

**file** failures:

* `file+V+Inf` does not generate!
* `file` has following analyses:
  * `fillen+A+Neu+Sg+Indef`

**jarve** failures:

* `jarve+V+Inf` => `jarv`
* `jarve` has no analyses either

**skalpere** failures:

* `skalpere+V+Inf` => `skalper`
* `skalpere` has no analyses either

**lugge** failures:

* `lugge+V+Inf` => `lugg`
* `lugge` has no analyses either

**løye** failures:

* `løye+V+Inf` => `løy`
* `løye` has following analyses:
  * `løy+A+Sg+Def`
  * `løy+A+Pl+Indef`

**fnyse** failures:

* `fnyse+V+Inf` => `fnys`
* `fnyse` has no analyses either

**håpe** failures:

* `håpe+V+Inf` does not generate!
* `håpe` has no analyses either

**flette** failures:

* `flette+V+Inf` => `flett`
* `flette` has no analyses either

**filtre** failures:

* `filtre+V+Inf` => `filtr`
* `filtre` has no analyses either

**fingradere** failures:

* `fingradere+V+Inf` => `fingrader`
* `fingradere` has no analyses either

**fosskoke** failures:

* `fosskoke+V+Inf` does not generate!
* `fosskoke` has no analyses either

**interagere** failures:

* `interagere+V+Inf` => `interager`
* `interagere` has no analyses either

**høve** failures:

* `høve+V+Inf` does not generate!
* `høve` has following analyses:
  * `høv+A+Pl+Indef`
  * `høvd+A+A+Pl+Def`
  * `høvd+A+A+Pl+Indef`
  * `høvd+A+A+Sg+Def`
  * `høv+A+Sg+Def`

**tillegge** failures:

* `tillegge+V+Inf` does not generate!
* `tillegge` has no analyses either

**kalsinere** failures:

* `kalsinere+V+Inf` => `kalsiner`
* `kalsinere` has no analyses either

**brotsje** failures:

* `brotsje+V+Inf` => `brotsj`
* `brotsje` has no analyses either

**hive** failures:

* `hive+V+Inf` does not generate!
* `hive` has no analyses either

**forkalke** failures:

* `forkalke+V+Inf` => `forkalk`
* `forkalke` has no analyses either

**byggje** failures:

* `byggje+V+Inf` does not generate!
* `byggje` has no analyses either

**celebrere** failures:

* `celebrere+V+Inf` => `celebrer`
* `celebrere` has no analyses either

**hekle** failures:

* `hekle+V+Inf` => `hekl`
* `hekle` has no analyses either

**varmgjære** failures:

* `varmgjære+V+Inf` => `varmgjær`
* `varmgjære` has no analyses either

**sjørøverskute** failures:

* `sjørøverskute+V+Inf` => `sjørøverskut`
* `sjørøverskute` has no analyses either

**punche** failures:

* `punche+V+Inf` => `punch`
* `punche` has no analyses either

**maskulinisere** failures:

* `maskulinisere+V+Inf` => `maskuliniser`
* `maskulinisere` has no analyses either

**dagdrøyme** failures:

* `dagdrøyme+V+Inf` does not generate!
* `dagdrøyme` has no analyses either

**forvrengje** failures:

* `forvrengje+V+Inf` does not generate!
* `forvrengje` has no analyses either

**spunse** failures:

* `spunse+V+Inf` => `spuns`
* `spunse` has no analyses either

**arrangere** failures:

* `arrangere+V+Inf` => `arranger`
* `arrangere` has no analyses either

**tvislast** failures:

* `tvislast+V+Inf` => `tvislastast`
* `tvislast` has no analyses either

**ålmenngjere** failures:

* `ålmenngjere+V+Inf` does not generate!
* `ålmenngjere` has no analyses either

**finjustere** failures:

* `finjustere+V+Inf` => `finjuster`
* `finjustere` has no analyses either

**forvede** failures:

* `forvede+V+Inf` => `forved`
* `forvede` has no analyses either

**mildne** failures:

* `mildne+V+Inf` => `mildn`
* `mildne` has no analyses either

**renne** failures:

* `renne+V+Inf` does not generate!
* `renne` has no analyses either

**fascinere** failures:

* `fascinere+V+Inf` => `fasciner`
* `fascinere` has no analyses either

**blengje** failures:

* `blengje+V+Inf` does not generate!
* `blengje` has no analyses either

**plogbremse** failures:

* `plogbremse+V+Inf` => `plogbrems`
* `plogbremse` has no analyses either

**ugrashorve** failures:

* `ugrashorve+V+Inf` => `ugrashorv`
* `ugrashorve` has no analyses either

**amle** failures:

* `amle+V+Inf` => `aml`
* `amle` has no analyses either

**godta** failures:

* `godta+V+Inf` => `godtta`
* `godta` has following analyses:
  * `godte+V+Ind+Prt`
  * `godt+N+N+Neu+Pl+Def`
  * `godte+V+PrfPrc`

**talme** failures:

* `talme+V+Inf` => `talm`
* `talme` has no analyses either

**gurme** failures:

* `gurme+V+Inf` => `gurm`
* `gurme` has no analyses either

**ballottere** failures:

* `ballottere+V+Inf` => `ballotter`
* `ballottere` has no analyses either

**snuble** failures:

* `snuble+V+Inf` => `snubl`
* `snuble` has following analyses:
  * `snublen+A+Neu+Sg+Indef`

**lemmatisere** failures:

* `lemmatisere+V+Inf` => `lemmatiser`
* `lemmatisere` has no analyses either

**avstresse** failures:

* `avstresse+V+Inf` => `avstress`
* `avstresse` has no analyses either

**oppgi** failures:

* `oppgi+V+Inf` => `oppggi`
* `oppgi` has no analyses either

**makte** failures:

* `makte+V+Inf` => `makt`
* `makte` has no analyses either

**tvangsfore** failures:

* `tvangsfore+V+Inf` => `tvangsfor`
* `tvangsfore` has no analyses either

**konsumere** failures:

* `konsumere+V+Inf` => `konsumer`
* `konsumere` has no analyses either

**varmhevje** failures:

* `varmhevje+V+Inf` does not generate!
* `varmhevje` has no analyses either

**hóle** failures:

* `hóle+V+Inf` => `hól`
* `hóle` has no analyses either

**dytte** failures:

* `dytte+V+Inf` => `dytt`
* `dytte` has no analyses either

**jatte** failures:

* `jatte+V+Inf` => `jatt`
* `jatte` has no analyses either

**særmerke** failures:

* `særmerke+V+Inf` => `særmerk`
* `særmerke` has no analyses either

**kumulere** failures:

* `kumulere+V+Inf` => `kumuler`
* `kumulere` has no analyses either

**kjese** failures:

* `kjese+V+Inf` => `kjes`
* `kjese` has no analyses either

**yksne** failures:

* `yksne+V+Inf` => `yksn`
* `yksne` has following analyses:
  * `yksen+A+Sg+Def`
  * `yksen+A+Pl+Indef`

**trøyte** failures:

* `trøyte+V+Inf` => `trøyt`
* `trøyte` has following analyses:
  * `trøyten+A+Neu+Sg+Indef`

**ugjere** failures:

* `ugjere+V+Inf` does not generate!
* `ugjere` has no analyses either

**polemisere** failures:

* `polemisere+V+Inf` => `polemiser`
* `polemisere` has no analyses either

**drivle** failures:

* `drivle+V+Inf` => `drivl`
* `drivle` has no analyses either

**tasse** failures:

* `tasse+V+Inf` => `tass`
* `tasse` has no analyses either

**manøvrere** failures:

* `manøvrere+V+Inf` => `manøvrer`
* `manøvrere` has no analyses either

**adaptere** failures:

* `adaptere+V+Inf` => `adapter`
* `adaptere` has no analyses either

**kante** failures:

* `kante+V+Inf` => `kant`
* `kante` has following analyses:
  * `kant+A+A+Sg+Def`
  * `kant+A+A+Pl+Def`
  * `kant+A+A+Pl+Indef`

**råflire** failures:

* `råflire+V+Inf` does not generate!
* `råflire` has no analyses either

**fegnast** failures:

* `fegnast+V+Inf` => `fegnastast`
* `fegnast` has following analyses:
  * `fegen+A+Superl+Indef`

**remse** failures:

* `remse+V+Inf` => `rems`
* `remse` has no analyses either

**gaide** failures:

* `gaide+V+Inf` => `gaid`
* `gaide` has no analyses either

**bestøve** failures:

* `bestøve+V+Inf` => `bestøv`
* `bestøve` has no analyses either

**haugtake** failures:

* `haugtake+V+Inf` => `haugttaka`
* `haugtake+V+Inf` => `haugttake`
* `haugtake` has no analyses either

**tuffe** failures:

* `tuffe+V+Inf` => `tuff`
* `tuffe` has no analyses either

**doble** failures:

* `doble+V+Inf` => `dobl`
* `doble` has no analyses either

**orme** failures:

* `orme+V+Inf` => `orm`
* `orme` has no analyses either

**fatle** failures:

* `fatle+V+Inf` => `fatl`
* `fatle` has no analyses either

**kute** failures:

* `kute+V+Inf` does not generate!
* `kute` has no analyses either

**forhøyre** failures:

* `forhøyre+V+Inf` does not generate!
* `forhøyre` has no analyses either

**oppheve** failures:

* `oppheve+V+Inf` => `opphev`
* `oppheve` has no analyses either

**antirustbehandle** failures:

* `antirustbehandle+V+Inf` => `antirustbehandl`
* `antirustbehandle` has no analyses either

**europeisere** failures:

* `europeisere+V+Inf` => `europeiser`
* `europeisere` has no analyses either

**parallellflytte** failures:

* `parallellflytte+V+Inf` does not generate!
* `parallellflytte` has no analyses either

**vekke** failures:

* `vekke+V+Inf` does not generate!
* `vekke` has following analyses:
  * `vekke+N+N+Fem+Sg+Indef`

**bønnhøyre** failures:

* `bønnhøyre+V+Inf` does not generate!
* `bønnhøyre` has no analyses either

**kake** failures:

* `kake+V+Inf` => `kak`
* `kake` has no analyses either

**tørrleggje** failures:

* `tørrleggje+V+Inf` does not generate!
* `tørrleggje` has no analyses either

**skirne** failures:

* `skirne+V+Inf` => `skirn`
* `skirne` has following analyses:
  * `skirne+N`

**finstille** failures:

* `finstille+V+Inf` does not generate!
* `finstille` has no analyses either

**islamisere** failures:

* `islamisere+V+Inf` => `islamiser`
* `islamisere` has no analyses either

**forgrove** failures:

* `forgrove+V+Inf` => `forgrov`
* `forgrove` has no analyses either

**oppklare** failures:

* `oppklare+V+Inf` => `oppklar`
* `oppklare` has no analyses either

**boltre** failures:

* `boltre+V+Inf` => `boltr`
* `boltre` has no analyses either

**rubbe** failures:

* `rubbe+V+Inf` => `rubb`
* `rubbe` has no analyses either

**trame** failures:

* `trame+V+Inf` => `tram`
* `trame` has no analyses either

**forbeine** failures:

* `forbeine+V+Inf` => `forbein`
* `forbeine` has no analyses either

**innrette** failures:

* `innrette+V+Inf` => `innrett`
* `innrette` has no analyses either

**affektere** failures:

* `affektere+V+Inf` => `affekter`
* `affektere` has no analyses either

**romstere** failures:

* `romstere+V+Inf` => `romster`
* `romstere` has no analyses either

**handikappe** failures:

* `handikappe+V+Inf` => `handikapp`
* `handikappe` has no analyses either

**flagre** failures:

* `flagre+V+Inf` => `flagr`
* `flagre` has no analyses either

**praktisere** failures:

* `praktisere+V+Inf` => `praktiser`
* `praktisere` has no analyses either

**lyssette** failures:

* `lyssette+V+Inf` does not generate!
* `lyssette` has no analyses either

**x-e** failures:

* `x-e+V+Inf` => `x-`
* `x-e` has no analyses either

**monopolisere** failures:

* `monopolisere+V+Inf` => `monopoliser`
* `monopolisere` has no analyses either

**forsømme** failures:

* `forsømme+V+Inf` does not generate!
* `forsømme` has no analyses either

**gyve** failures:

* `gyve+V+Inf` => `gya`
* `gyve+V+Inf` => `gye`
* `gyve` has no analyses either

**løyne** failures:

* `løyne+V+Inf` does not generate!
* `løyne` has no analyses either

**menstruere** failures:

* `menstruere+V+Inf` => `menstruer`
* `menstruere` has no analyses either

**interferere** failures:

* `interferere+V+Inf` => `interferer`
* `interferere` has no analyses either

**ròtne** failures:

* `ròtne+V+Inf` => `ròtn`
* `ròtne` has following analyses:
  * `ròten+A+Pl+Indef`
  * `ròten+A+Sg+Def`

**slufse** failures:

* `slufse+V+Inf` => `slufs`
* `slufse` has no analyses either

**potte** failures:

* `potte+V+Inf` => `pott`
* `potte` has no analyses either

**gjennomstrøyme** failures:

* `gjennomstrøyme+V+Inf` does not generate!
* `gjennomstrøyme` has no analyses either

**syrgje** failures:

* `syrgje+V+Inf` does not generate!
* `syrgje` has no analyses either

**grille** failures:

* `grille+V+Inf` => `grill`
* `grille` has no analyses either

**brøyte** failures:

* `brøyte+V+Inf` => `brøyt`
* `brøyte` has no analyses either

**mumpe** failures:

* `mumpe+V+Inf` => `mump`
* `mumpe` has no analyses either

**kvakke** failures:

* `kvakke+V+Inf` => `kvakk`
* `kvakke` has no analyses either

**torpedere** failures:

* `torpedere+V+Inf` => `torpeder`
* `torpedere` has no analyses either

**vitse** failures:

* `vitse+V+Inf` => `vits`
* `vitse` has no analyses either

**klyngje** failures:

* `klyngje+V+Inf` => `klyng`
* `klyngje` has no analyses either

**utyrste** failures:

* `utyrste+V+Inf` => `utyrst`
* `utyrste` has following analyses:
  * `utyrst+A+A+Pl+Def`
  * `utyrst+A+A+Pl+Indef`
  * `utyrst+A+A+Sg+Def`

**deprimere** failures:

* `deprimere+V+Inf` => `deprimer`
* `deprimere` has no analyses either

**støve** failures:

* `støve+V+Inf` => `støv`
* `støve` has no analyses either

**variere** failures:

* `variere+V+Inf` => `varier`
* `variere` has no analyses either

**føyrne** failures:

* `føyrne+V+Inf` => `føyrn`
* `føyrne` has following analyses:
  * `føyren+A+Pl+Indef`
  * `føyren+A+Sg+Def`

**ire** failures:

* `ire+V+Inf` does not generate!
* `ire` has no analyses either

**infiltrere** failures:

* `infiltrere+V+Inf` => `infiltrer`
* `infiltrere` has no analyses either

**plystre** failures:

* `plystre+V+Inf` => `plystr`
* `plystre` has no analyses either

**naudstire** failures:

* `naudstire+V+Inf` does not generate!
* `naudstire` has no analyses either

**infisere** failures:

* `infisere+V+Inf` => `infiser`
* `infisere` has no analyses either

**smelte** failures:

* `smelte+V+Inf` does not generate!
* `smelte` has no analyses either

**sjekke** failures:

* `sjekke+V+Inf` => `sjekk`
* `sjekke` has no analyses either

**betone** failures:

* `betone+V+Inf` => `beton`
* `betone` has following analyses:
  * `bete+N+N+Fem+Pl+Def`

**snaldre** failures:

* `snaldre+V+Inf` => `snaldr`
* `snaldre` has no analyses either

**gilde** failures:

* `gilde+V+Inf` => `gild`
* `gilde` has following analyses:
  * `gild+A+Pl+Indef`
  * `gild+A+Sg+Def`

**bleigje** failures:

* `bleigje+V+Inf` does not generate!
* `bleigje` has no analyses either

**tirle** failures:

* `tirle+V+Inf` => `tirl`
* `tirle` has no analyses either

**ståke** failures:

* `ståke+V+Inf` => `ståk`
* `ståke` has no analyses either

**skvitre** failures:

* `skvitre+V+Inf` => `skvitr`
* `skvitre` has no analyses either

**byrje** failures:

* `byrje+V+Inf` => `byrj`
* `byrje` has no analyses either

**tråve** failures:

* `tråve+V+Inf` => `tråv`
* `tråve` has no analyses either

**raudne** failures:

* `raudne+V+Inf` => `raudn`
* `raudne` has no analyses either

**klede** failures:

* `klede+V+Inf` does not generate!
* `klede` has no analyses either

**attertrygde** failures:

* `attertrygde+V+Inf` => `attertrygd`
* `attertrygde` has no analyses either

**rangstrikke** failures:

* `rangstrikke+V+Inf` => `rangstrikk`
* `rangstrikke` has no analyses either

**sandstrøye** failures:

* `sandstrøye+V+Inf` does not generate!
* `sandstrøye` has no analyses either

**formørke** failures:

* `formørke+V+Inf` => `formørk`
* `formørke` has no analyses either

**utkrystallisere** failures:

* `utkrystallisere+V+Inf` => `utkrystalliser`
* `utkrystallisere` has no analyses either

**fiffe** failures:

* `fiffe+V+Inf` => `fiff`
* `fiffe` has no analyses either

**sjudoble** failures:

* `sjudoble+V+Inf` => `sjudobl`
* `sjudoble` has no analyses either

**duvle** failures:

* `duvle+V+Inf` => `duvl`
* `duvle` has no analyses either

**utrydje** failures:

* `utrydje+V+Inf` does not generate!
* `utrydje` has no analyses either

**ròse** failures:

* `ròse+V+Inf` => `ròs`
* `ròse` has no analyses either

**bøteleggje** failures:

* `bøteleggje+V+Inf` does not generate!
* `bøteleggje` has no analyses either

**raspe** failures:

* `raspe+V+Inf` => `rasp`
* `raspe` has no analyses either

**skjematisere** failures:

* `skjematisere+V+Inf` => `skjematiser`
* `skjematisere` has no analyses either

**råkast** failures:

* `råkast+V+Inf` => `råkastast`
* `råkast` has no analyses either

**mellomlande** failures:

* `mellomlande+V+Inf` => `mellomland`
* `mellomlande` has no analyses either

**gnurke** failures:

* `gnurke+V+Inf` => `gnurk`
* `gnurke` has no analyses either

**tidsnormere** failures:

* `tidsnormere+V+Inf` => `tidsnormer`
* `tidsnormere` has no analyses either

**spane** failures:

* `spane+V+Inf` => `span`
* `spane` has no analyses either

**riple** failures:

* `riple+V+Inf` => `ripl`
* `riple` has no analyses either

**andgjeglast** failures:

* `andgjeglast+V+Inf` => `andgjeglastast`
* `andgjeglast` has no analyses either

**svømme** failures:

* `svømme+V+Inf` => `svøme`
* `svømme+V+Inf` => `svøma`
* `svømme` has no analyses either

**finhøvle** failures:

* `finhøvle+V+Inf` => `finhøvl`
* `finhøvle` has no analyses either

**tovne** failures:

* `tovne+V+Inf` => `tovn`
* `tovne` has following analyses:
  * `toven+A+Sg+Def`
  * `toven+A+Pl+Indef`

**bonge** failures:

* `bonge+V+Inf` => `bong`
* `bonge` has no analyses either

**strengje** failures:

* `strengje+V+Inf` => `streng`
* `strengje` has no analyses either

**derte** failures:

* `derte+V+Inf` => `dert`
* `derte` has following analyses:
  * `derten+A+Neu+Sg+Indef`

**flekkje** failures:

* `flekkje+V+Inf` does not generate!
* `flekkje` has no analyses either

**kose** failures:

* `kose+V+Inf` does not generate!
* `kose` has following analyses:
  * `kos+A+A+Pl+Indef`
  * `kos+A+A+Pl+Def`
  * `kos+A+A+Sg+Def`

**bruke** failures:

* `bruke+V+Inf` does not generate!
* `bruke` has no analyses either

**bure** failures:

* `bure+V+Inf` => `bur`
* `bure` has no analyses either

**slange** failures:

* `slange+V+Inf` => `slang`
* `slange` has no analyses either

**proponere** failures:

* `proponere+V+Inf` => `proponer`
* `proponere` has no analyses either

**muse** failures:

* `muse+V+Inf` => `mus`
* `muse` has following analyses:
  * `museum+N`

**buste** failures:

* `buste+V+Inf` => `bust`
* `buste` has no analyses either

**høse** failures:

* `høse+V+Inf` => `høs`
* `høse` has no analyses either

**bjølle** failures:

* `bjølle+V+Inf` => `bjøll`
* `bjølle` has no analyses either

**reforhandle** failures:

* `reforhandle+V+Inf` => `reforhandl`
* `reforhandle` has no analyses either

**kryssforhøyre** failures:

* `kryssforhøyre+V+Inf` does not generate!
* `kryssforhøyre` has no analyses either

**haugleggje** failures:

* `haugleggje+V+Inf` does not generate!
* `haugleggje` has no analyses either

**hugtake** failures:

* `hugtake+V+Inf` => `hugttaka`
* `hugtake+V+Inf` => `hugttake`
* `hugtake` has no analyses either

**prestere** failures:

* `prestere+V+Inf` => `prester`
* `prestere` has no analyses either

**tette** failures:

* `tette+V+Inf` => `tett`
* `tette` has following analyses:
  * `tett+A+A+Sg+Def`
  * `tett+A+A+Pl+Def`
  * `tett+A+A+Pl+Indef`

**hudflette** failures:

* `hudflette+V+Inf` does not generate!
* `hudflette` has no analyses either

**syde** failures:

* `syde+V+Inf` => `sye`
* `syde+V+Inf` => `sya`
* `syde` has no analyses either

**arrondere** failures:

* `arrondere+V+Inf` => `arronder`
* `arrondere` has no analyses either

**hope** failures:

* `hope+V+Inf` => `hop`
* `hope` has no analyses either

**horse** failures:

* `horse+V+Inf` => `hors`
* `horse` has no analyses either

**skrifte** failures:

* `skrifte+V+Inf` => `skrift`
* `skrifte` has no analyses either

**labbe** failures:

* `labbe+V+Inf` => `labb`
* `labbe` has no analyses either

**reinse** failures:

* `reinse+V+Inf` => `reins`
* `reinse` has no analyses either

**speede** failures:

* `speede+V+Inf` => `speed`
* `speede` has no analyses either

**basunere** failures:

* `basunere+V+Inf` => `basuner`
* `basunere` has no analyses either

**hegne** failures:

* `hegne+V+Inf` => `hegn`
* `hegne` has no analyses either

**vidareutdanne** failures:

* `vidareutdanne+V+Inf` => `vidareutdann`
* `vidareutdanne` has no analyses either

**nedre** failures:

* `nedre+V+Inf` => `nedr`
* `nedre` has following analyses:
  * `nedre+A+Comp`

**punktere** failures:

* `punktere+V+Inf` => `punkter`
* `punktere` has no analyses either

**lægre** failures:

* `lægre+V+Inf` => `lægr`
* `lægre` has no analyses either

**avrette** failures:

* `avrette+V+Inf` => `avrett`
* `avrette` has no analyses either

**ramsalte** failures:

* `ramsalte+V+Inf` => `ramsalt`
* `ramsalte` has following analyses:
  * `ramsalt+A+A+Pl+Indef`
  * `ramsalt+A+A+Sg+Def`
  * `ramsalt+A+A+Pl+Def`

**endefare** failures:

* `endefare+V+Inf` => `endefae`
* `endefare+V+Inf` => `endefaa`
* `endefare` has no analyses either

**skvisle** failures:

* `skvisle+V+Inf` => `skvisl`
* `skvisle` has no analyses either

**fortyske** failures:

* `fortyske+V+Inf` => `fortysk`
* `fortyske` has no analyses either

**abbreviere** failures:

* `abbreviere+V+Inf` => `abbrevier`
* `abbreviere` has no analyses either

**glattkjemme** failures:

* `glattkjemme+V+Inf` does not generate!
* `glattkjemme` has no analyses either

**bresere** failures:

* `bresere+V+Inf` => `breser`
* `bresere` has no analyses either

**anlaupe** failures:

* `anlaupe+V+Inf` => `anlaup`
* `anlaupe` has no analyses either

**grymte** failures:

* `grymte+V+Inf` => `grymt`
* `grymte` has no analyses either

**slinke** failures:

* `slinke+V+Inf` => `slink`
* `slinke` has no analyses either

**inspirere** failures:

* `inspirere+V+Inf` => `inspirer`
* `inspirere` has no analyses either

**gisse** failures:

* `gisse+V+Inf` => `giss`
* `gisse` has no analyses either

**bisetje** failures:

* `bisetje+V+Inf` does not generate!
* `bisetje` has no analyses either

**unnfange** failures:

* `unnfange+V+Inf` => `unnfang`
* `unnfange` has no analyses either

**fredlyse** failures:

* `fredlyse+V+Inf` => `fredlys`
* `fredlyse` has no analyses either

**kallast** failures:

* `kallast+V+Inf` => `kallastast`
* `kallast` has no analyses either

**oppmuntre** failures:

* `oppmuntre+V+Inf` => `oppmuntr`
* `oppmuntre` has no analyses either

**overliste** failures:

* `overliste+V+Inf` => `overlist`
* `overliste` has no analyses either

**kive** failures:

* `kive+V+Inf` => `kiv`
* `kive` has following analyses:
  * `kiven+A+Neu+Sg+Indef`

**lepje** failures:

* `lepje+V+Inf` => `lepj`
* `lepje` has no analyses either

**møde** failures:

* `møde+V+Inf` does not generate!
* `møde` has no analyses either

**nivellere** failures:

* `nivellere+V+Inf` => `niveller`
* `nivellere` has no analyses either

**avlive** failures:

* `avlive+V+Inf` => `avliv`
* `avlive` has no analyses either

**kartere** failures:

* `kartere+V+Inf` => `karter`
* `kartere` has no analyses either

**basere** failures:

* `basere+V+Inf` => `baser`
* `basere` has no analyses either

**hjernevaske** failures:

* `hjernevaske+V+Inf` => `hjernevask`
* `hjernevaske` has no analyses either

**resistere** failures:

* `resistere+V+Inf` => `resister`
* `resistere` has no analyses either

**residivere** failures:

* `residivere+V+Inf` => `residiver`
* `residivere` has no analyses either

**gjelde** failures:

* `gjelde+V+Inf` does not generate!
* `gjelde` has following analyses:
  * `gjeld+A+Sg+Def`
  * `gjeld+A+Pl+Indef`

**oppsøkje** failures:

* `oppsøkje+V+Inf` does not generate!
* `oppsøkje` has no analyses either

**skingre** failures:

* `skingre+V+Inf` => `skingr`
* `skingre` has no analyses either

**gjerast** failures:

* `gjerast+V+Inf` => `gjeast`
* `gjerast` has following analyses:
  * `gjer+A+Superl+Indef`

**elle** failures:

* `elle+V+Inf` => `ell`
* `elle` has no analyses either

**sjablongere** failures:

* `sjablongere+V+Inf` => `sjablonger`
* `sjablongere` has no analyses either

**argumentere** failures:

* `argumentere+V+Inf` => `argumenter`
* `argumentere` has no analyses either

**kollkaste** failures:

* `kollkaste+V+Inf` => `kollkast`
* `kollkaste` has no analyses either

**flindre** failures:

* `flindre+V+Inf` => `flindr`
* `flindre` has no analyses either

**drype** failures:

* `drype+V+Inf` => `dr`
* `drype` has no analyses either

**dore** failures:

* `dore+V+Inf` => `dor`
* `dore` has no analyses either

**tviste** failures:

* `tviste+V+Inf` => `tvist`
* `tviste` has no analyses either

**luse** failures:

* `luse+V+Inf` => `lus`
* `luse` has following analyses:
  * `lusen+A+Neu+Sg+Indef`

**fordundre** failures:

* `fordundre+V+Inf` => `fordundr`
* `fordundre` has no analyses either

**benåde** failures:

* `benåde+V+Inf` => `benåd`
* `benåde` has no analyses either

**rue** failures:

* `rue+V+Inf` => `ru`
* `rue` has following analyses:
  * `ru+A+Pl+Indef`
  * `ru+A+Sg+Def`

**rikosjettere** failures:

* `rikosjettere+V+Inf` => `rikosjetter`
* `rikosjettere` has no analyses either

**klakne** failures:

* `klakne+V+Inf` => `klakn`
* `klakne` has following analyses:
  * `klaken+A+Pl+Indef`
  * `klaken+A+Sg+Def`

**støvsuge** failures:

* `støvsuge+V+Inf` does not generate!
* `støvsuge` has no analyses either

**børste** failures:

* `børste+V+Inf` => `børst`
* `børste` has following analyses:
  * `børsten+A+Neu+Sg+Indef`

**mystifisere** failures:

* `mystifisere+V+Inf` => `mystifiser`
* `mystifisere` has no analyses either

**faksimilere** failures:

* `faksimilere+V+Inf` => `faksimiler`
* `faksimilere` has no analyses either

**klistre** failures:

* `klistre+V+Inf` => `klistr`
* `klistre` has no analyses either

**tjuvlåne** failures:

* `tjuvlåne+V+Inf` => `tjuvlån`
* `tjuvlåne` has no analyses either

**teknifisere** failures:

* `teknifisere+V+Inf` => `teknifiser`
* `teknifisere` has no analyses either

**sabbe** failures:

* `sabbe+V+Inf` => `sabb`
* `sabbe` has no analyses either

**kåve** failures:

* `kåve+V+Inf` => `kåv`
* `kåve` has no analyses either

**omklassifisere** failures:

* `omklassifisere+V+Inf` => `omklassifiser`
* `omklassifisere` has no analyses either

**slekte** failures:

* `slekte+V+Inf` => `slekt`
* `slekte` has no analyses either

**åmåle** failures:

* `åmåle+V+Inf` => `åmål`
* `åmåle` has no analyses either

**prege** failures:

* `prege+V+Inf` => `preg`
* `prege` has no analyses either

**mogleggjere** failures:

* `mogleggjere+V+Inf` does not generate!
* `mogleggjere` has no analyses either

**mige** failures:

* `mige+V+Inf` => `mie`
* `mige+V+Inf` => `mia`
* `mige` has no analyses either

**tugge** failures:

* `tugge+V+Inf` => `tugg`
* `tugge` has no analyses either

**tørste** failures:

* `tørste+V+Inf` does not generate!
* `tørste` has following analyses:
  * `tørst+A+A+Pl+Def`
  * `tørst+A+A+Pl+Indef`
  * `tørst+A+A+Sg+Def`

**fliseleggje** failures:

* `fliseleggje+V+Inf` does not generate!
* `fliseleggje` has no analyses either

**stagge** failures:

* `stagge+V+Inf` => `stagg`
* `stagge` has no analyses either

**luftturke** failures:

* `luftturke+V+Inf` => `luftturk`
* `luftturke` has no analyses either

**nækje** failures:

* `nækje+V+Inf` does not generate!
* `nækje` has no analyses either

**tørrtenke** failures:

* `tørrtenke+V+Inf` => `tørrtenk`
* `tørrtenke` has no analyses either

**botnleggje** failures:

* `botnleggje+V+Inf` does not generate!
* `botnleggje` has no analyses either

**fiste** failures:

* `fiste+V+Inf` => `fist`
* `fiste` has no analyses either

**reinvaske** failures:

* `reinvaske+V+Inf` => `reinvask`
* `reinvaske` has no analyses either

**strenge** failures:

* `strenge+V+Inf` => `streng`
* `strenge` has following analyses:
  * `streng+A+Sg+Def`
  * `streng+A+Pl+Indef`

**jorde** failures:

* `jorde+V+Inf` => `jord`
* `jorde` has no analyses either

**ideologisere** failures:

* `ideologisere+V+Inf` => `ideologiser`
* `ideologisere` has no analyses either

**ynde** failures:

* `ynde+V+Inf` => `ynd`
* `ynde` has no analyses either

**halle** failures:

* `halle+V+Inf` => `hall`
* `halle` has following analyses:
  * `hall+A+Pl+Indef`
  * `hall+A+Sg+Def`

**omlægre** failures:

* `omlægre+V+Inf` => `omlægr`
* `omlægre` has no analyses either

**sigge** failures:

* `sigge+V+Inf` => `sigg`
* `sigge` has no analyses either

**etterspore** failures:

* `etterspore+V+Inf` => `etterspor`
* `etterspore` has no analyses either

**klynke** failures:

* `klynke+V+Inf` => `klynk`
* `klynke` has no analyses either

**forlange** failures:

* `forlange+V+Inf` => `forlang`
* `forlange` has no analyses either

**portrettere** failures:

* `portrettere+V+Inf` => `portretter`
* `portrettere` has no analyses either

**røle** failures:

* `røle+V+Inf` => `røl`
* `røle` has no analyses either

**kostnadsrekne** failures:

* `kostnadsrekne+V+Inf` => `kostnadsrekn`
* `kostnadsrekne` has no analyses either

**opparbeide** failures:

* `opparbeide+V+Inf` does not generate!
* `opparbeide` has no analyses either

**gjeve** failures:

* `gjeve+V+Inf` => `ggjeve`
* `gjeve+V+Inf` => `ggjeva`
* `gjeve` has following analyses:
  * `gjeven+A+Neu+Sg+Indef`
  * `gje+V+PrfPrc`

**hespe** failures:

* `hespe+V+Inf` => `hesp`
* `hespe` has no analyses either

**changere** failures:

* `changere+V+Inf` => `changer`
* `changere` has no analyses either

**huse** failures:

* `huse+V+Inf` does not generate!
* `huse` has no analyses either

**brage** failures:

* `brage+V+Inf` => `brag`
* `brage` has no analyses either

**småsnakke** failures:

* `småsnakke+V+Inf` => `småsnakk`
* `småsnakke` has no analyses either

**heilsteikje** failures:

* `heilsteikje+V+Inf` does not generate!
* `heilsteikje` has no analyses either

**presidere** failures:

* `presidere+V+Inf` => `presider`
* `presidere` has no analyses either

**busme** failures:

* `busme+V+Inf` => `busm`
* `busme` has no analyses either

**ulyste** failures:

* `ulyste+V+Inf` => `ulyst`
* `ulyste` has following analyses:
  * `ulyst+A+A+Sg+Def`
  * `ulyst+A+A+Pl+Indef`
  * `ulyst+A+A+Pl+Def`
  * `ulysten+A+Neu+Sg+Indef`

**strippe** failures:

* `strippe+V+Inf` => `stripp`
* `strippe` has no analyses either

**overgje** failures:

* `overgje+V+Inf` => `overggje`
* `overgje` has no analyses either

**dissosiere** failures:

* `dissosiere+V+Inf` => `dissosier`
* `dissosiere` has no analyses either

**rettkjenne** failures:

* `rettkjenne+V+Inf` does not generate!
* `rettkjenne` has no analyses either

**mjuklande** failures:

* `mjuklande+V+Inf` => `mjukland`
* `mjuklande` has no analyses either

**kurere** failures:

* `kurere+V+Inf` => `kurer`
* `kurere` has no analyses either

**overvinne** failures:

* `overvinne+V+Inf` => `overvie`
* `overvinne+V+Inf` => `overvia`
* `overvinne` has no analyses either

**vedrøre** failures:

* `vedrøre+V+Inf` => `vedrør`
* `vedrøre` has no analyses either

**dolle** failures:

* `dolle+V+Inf` => `doll`
* `dolle` has no analyses either

**syvje** failures:

* `syvje+V+Inf` => `syvj`
* `syvje` has no analyses either

**spritle** failures:

* `spritle+V+Inf` => `spritl`
* `spritle` has no analyses either

**kjølsprengje** failures:

* `kjølsprengje+V+Inf` does not generate!
* `kjølsprengje` has no analyses either

**late** failures:

* `late+V+Inf` => `l`
* `late` has following analyses:
  * `lat+A+Pl+Indef`
  * `lat+A+Sg+Def`

**rikle** failures:

* `rikle+V+Inf` => `rikl`
* `rikle` has no analyses either

**mole** failures:

* `mole+V+Inf` => `mol`
* `mole` has no analyses either

**smuldre** failures:

* `smuldre+V+Inf` => `smuldr`
* `smuldre` has no analyses either

**skysse** failures:

* `skysse+V+Inf` => `skyss`
* `skysse` has no analyses either

**knatte** failures:

* `knatte+V+Inf` => `knatt`
* `knatte` has no analyses either

**banne** failures:

* `banne+V+Inf` does not generate!
* `banne` has no analyses either

**gofse** failures:

* `gofse+V+Inf` => `gofs`
* `gofse` has no analyses either

**kulke** failures:

* `kulke+V+Inf` => `kulk`
* `kulke` has no analyses either

**knuffe** failures:

* `knuffe+V+Inf` => `knuff`
* `knuffe` has no analyses either

**shoppe** failures:

* `shoppe+V+Inf` => `shopp`
* `shoppe` has no analyses either

**makulere** failures:

* `makulere+V+Inf` => `makuler`
* `makulere` has no analyses either

**forsimple** failures:

* `forsimple+V+Inf` => `forsimpl`
* `forsimple` has no analyses either

**kompromittere** failures:

* `kompromittere+V+Inf` => `kompromitter`
* `kompromittere` has no analyses either

**akseptere** failures:

* `akseptere+V+Inf` => `aksepter`
* `akseptere` has no analyses either

**buke** failures:

* `buke+V+Inf` => `buk`
* `buke` has following analyses:
  * `buke+N+N+Msc+Sg+Indef`

**innfordre** failures:

* `innfordre+V+Inf` => `innfordr`
* `innfordre` has no analyses either

**tjøre** failures:

* `tjøre+V+Inf` => `tjør`
* `tjøre` has no analyses either

**tisse** failures:

* `tisse+V+Inf` => `tiss`
* `tisse` has no analyses either

**arde** failures:

* `arde+V+Inf` => `ard`
* `arde` has no analyses either

**nebbe** failures:

* `nebbe+V+Inf` => `nebb`
* `nebbe` has no analyses either

**verifisere** failures:

* `verifisere+V+Inf` => `verifiser`
* `verifisere` has no analyses either

**stunde** failures:

* `stunde+V+Inf` => `stund`
* `stunde` has no analyses either

**privatkøyre** failures:

* `privatkøyre+V+Inf` does not generate!
* `privatkøyre` has no analyses either

**prøvefilme** failures:

* `prøvefilme+V+Inf` => `prøvefilm`
* `prøvefilme` has no analyses either

**tufle** failures:

* `tufle+V+Inf` => `tufl`
* `tufle` has no analyses either

**nummerere** failures:

* `nummerere+V+Inf` => `nummerer`
* `nummerere` has no analyses either

**hekse** failures:

* `hekse+V+Inf` => `heks`
* `hekse` has no analyses either

**gjønstre** failures:

* `gjønstre+V+Inf` => `gjønstr`
* `gjønstre` has no analyses either

**steinsetje** failures:

* `steinsetje+V+Inf` does not generate!
* `steinsetje` has no analyses either

**møre** failures:

* `møre+V+Inf` => `mør`
* `møre` has following analyses:
  * `mør+A+Sg+Def`
  * `mør+A+Pl+Indef`

**fjamse** failures:

* `fjamse+V+Inf` => `fjams`
* `fjamse` has no analyses either

**skumtygge** failures:

* `skumtygge+V+Inf` => `skumtye`
* `skumtygge+V+Inf` => `skumtya`
* `skumtygge` has no analyses either

**tvikøyre** failures:

* `tvikøyre+V+Inf` does not generate!
* `tvikøyre` has no analyses either

**fakke** failures:

* `fakke+V+Inf` => `fakk`
* `fakke` has no analyses either

**visere** failures:

* `visere+V+Inf` => `viser`
* `visere` has no analyses either

**hånflire** failures:

* `hånflire+V+Inf` does not generate!
* `hånflire` has no analyses either

**kvantitere** failures:

* `kvantitere+V+Inf` => `kvantiter`
* `kvantitere` has no analyses either

**krysstille** failures:

* `krysstille+V+Inf` does not generate!
* `krysstille` has no analyses either

**undersøkje** failures:

* `undersøkje+V+Inf` does not generate!
* `undersøkje` has no analyses either

**buldre** failures:

* `buldre+V+Inf` => `buldr`
* `buldre` has no analyses either

**gjenreise** failures:

* `gjenreise+V+Inf` => `gjenreis`
* `gjenreise` has no analyses either

**flippe** failures:

* `flippe+V+Inf` => `flipp`
* `flippe` has no analyses either

**skugge** failures:

* `skugge+V+Inf` => `skugg`
* `skugge` has no analyses either

**ose** failures:

* `ose+V+Inf` does not generate!
* `ose` has no analyses either

**instituere** failures:

* `instituere+V+Inf` => `instituer`
* `instituere` has no analyses either

**småkoke** failures:

* `småkoke+V+Inf` does not generate!
* `småkoke` has no analyses either

**sylvlegge** failures:

* `sylvlegge+V+Inf` does not generate!
* `sylvlegge` has no analyses either

**handtakke** failures:

* `handtakke+V+Inf` => `handtakk`
* `handtakke` has no analyses either

**skusle** failures:

* `skusle+V+Inf` => `skusl`
* `skusle` has no analyses either

**djupe** failures:

* `djupe+V+Inf` => `djup`
* `djupe` has following analyses:
  * `djup+A+Sg+Def`
  * `djup+A+Pl+Indef`

**setne** failures:

* `setne+V+Inf` => `setn`
* `setne` has following analyses:
  * `sitte+V+Adj+Fl`
  * `sitje+V+Adj+Bu+Eint`
  * `sitje+V+Adj+Fl`
  * `sitte+V+Adj+Bu+Eint`

**posne** failures:

* `posne+V+Inf` => `posn`
* `posne` has following analyses:
  * `posen+A+Pl+Indef`
  * `posen+A+Sg+Def`

**prente** failures:

* `prente+V+Inf` => `prent`
* `prente` has no analyses either

**synkronisere** failures:

* `synkronisere+V+Inf` => `synkroniser`
* `synkronisere` has no analyses either

**synde** failures:

* `synde+V+Inf` => `synd`
* `synde` has no analyses either

**begripe** failures:

* `begripe+V+Inf` => `begrie`
* `begripe+V+Inf` => `begria`
* `begripe` has no analyses either

**andgjegle** failures:

* `andgjegle+V+Inf` => `andgjegl`
* `andgjegle` has no analyses either

**tollklarere** failures:

* `tollklarere+V+Inf` => `tollklarer`
* `tollklarere` has no analyses either

**forhauste** failures:

* `forhauste+V+Inf` => `forhaust`
* `forhauste` has no analyses either

**vande** failures:

* `vande+V+Inf` => `vand`
* `vande` has following analyses:
  * `vand+A+Sg+Def`
  * `vand+A+Pl+Indef`
  * `vike+V+PrsPrc`

**hælsparke** failures:

* `hælsparke+V+Inf` => `hælspark`
* `hælsparke` has no analyses either

**midle** failures:

* `midle+V+Inf` => `midl`
* `midle` has no analyses either

**opphalde** failures:

* `opphalde+V+Inf` => `opphae`
* `opphalde+V+Inf` => `opphaa`
* `opphalde` has no analyses either

**lufte** failures:

* `lufte+V+Inf` => `luft`
* `lufte` has no analyses either

**okulere** failures:

* `okulere+V+Inf` => `okuler`
* `okulere` has no analyses either

**psykoanalysere** failures:

* `psykoanalysere+V+Inf` => `psykoanalyser`
* `psykoanalysere` has no analyses either

**innkvartere** failures:

* `innkvartere+V+Inf` => `innkvarter`
* `innkvartere` has no analyses either

**skrynje** failures:

* `skrynje+V+Inf` does not generate!
* `skrynje` has no analyses either

**falbyde** failures:

* `falbyde+V+Inf` does not generate!
* `falbyde` has no analyses either

**brevveksle** failures:

* `brevveksle+V+Inf` => `brevveksl`
* `brevveksle` has no analyses either

**iscenesetje** failures:

* `iscenesetje+V+Inf` does not generate!
* `iscenesetje` has no analyses either

**robustisere** failures:

* `robustisere+V+Inf` => `robustiser`
* `robustisere` has no analyses either

**cruise** failures:

* `cruise+V+Inf` => `cruis`
* `cruise` has no analyses either

**peise** failures:

* `peise+V+Inf` does not generate!
* `peise` has no analyses either

**dagast** failures:

* `dagast+V+Inf` => `dagastast`
* `dagast` has no analyses either

**mæle** failures:

* `mæle+V+Inf` => `mæl`
* `mæle` has no analyses either

**somle** failures:

* `somle+V+Inf` => `soml`
* `somle` has no analyses either

**fekte** failures:

* `fekte+V+Inf` => `fekt`
* `fekte` has no analyses either

**fyse** failures:

* `fyse+V+Inf` => `fys`
* `fyse` has following analyses:
  * `fys+A+Sg+Def`
  * `fys+A+Pl+Indef`

**avsondre** failures:

* `avsondre+V+Inf` => `avsondr`
* `avsondre` has no analyses either

**etterberekne** failures:

* `etterberekne+V+Inf` => `etterberekn`
* `etterberekne` has no analyses either

**preservere** failures:

* `preservere+V+Inf` => `preserver`
* `preservere` has no analyses either

**skurdtreskje** failures:

* `skurdtreskje+V+Inf` does not generate!
* `skurdtreskje` has no analyses either

**datle** failures:

* `datle+V+Inf` => `datl`
* `datle` has no analyses either

**stiltre** failures:

* `stiltre+V+Inf` => `stiltr`
* `stiltre` has no analyses either

**kantskjere** failures:

* `kantskjere+V+Inf` => `kantskjee`
* `kantskjere+V+Inf` => `kantskjea`
* `kantskjere` has no analyses either

**strøype** failures:

* `strøype+V+Inf` => `strøyp`
* `strøype` has no analyses either

**solle** failures:

* `solle+V+Inf` => `soll`
* `solle` has no analyses either

**stigmatisere** failures:

* `stigmatisere+V+Inf` => `stigmatiser`
* `stigmatisere` has no analyses either

**forglagge** failures:

* `forglagge+V+Inf` => `forglagg`
* `forglagge` has no analyses either

**låntake** failures:

* `låntake+V+Inf` => `lånttake`
* `låntake+V+Inf` => `lånttaka`
* `låntake` has no analyses either

**domfelle** failures:

* `domfelle+V+Inf` does not generate!
* `domfelle` has no analyses either

**kakke** failures:

* `kakke+V+Inf` => `kakk`
* `kakke` has no analyses either

**tilvise** failures:

* `tilvise+V+Inf` => `tilvis`
* `tilvise` has no analyses either

**plusse** failures:

* `plusse+V+Inf` => `pluss`
* `plusse` has no analyses either

**smekte** failures:

* `smekte+V+Inf` => `smekt`
* `smekte` has no analyses either

**nåtle** failures:

* `nåtle+V+Inf` => `nåtl`
* `nåtle` has no analyses either

**leve** failures:

* `leve+V+Inf` does not generate!
* `leve` has following analyses:
  * `leve+N+N+Neu+Sg+Indef`

**proskribere** failures:

* `proskribere+V+Inf` => `proskriber`
* `proskribere` has no analyses either

**oversvømme** failures:

* `oversvømme+V+Inf` => `oversvøme`
* `oversvømme+V+Inf` => `oversvøma`
* `oversvømme` has no analyses either

**skire** failures:

* `skire+V+Inf` => `skir`
* `skire` has following analyses:
  * `skir+A+Sg+Def`
  * `skir+A+Pl+Indef`

**drysse** failures:

* `drysse+V+Inf` does not generate!
* `drysse` has no analyses either

**audmjuke** failures:

* `audmjuke+V+Inf` => `audmjuk`
* `audmjuke` has following analyses:
  * `audmjuk+A+A+Pl+Indef`
  * `audmjuk+A+A+Sg+Def`
  * `audmjuk+A+A+Pl+Def`

**hardne** failures:

* `hardne+V+Inf` => `hardn`
* `hardne` has no analyses either

**tegle** failures:

* `tegle+V+Inf` => `tegl`
* `tegle` has no analyses either

**giljotinere** failures:

* `giljotinere+V+Inf` => `giljotiner`
* `giljotinere` has no analyses either

**sulke** failures:

* `sulke+V+Inf` => `sulk`
* `sulke` has no analyses either

**forstuve** failures:

* `forstuve+V+Inf` => `forstuv`
* `forstuve` has no analyses either

**forfremje** failures:

* `forfremje+V+Inf` => `forfremj`
* `forfremje` has no analyses either

**kræsje** failures:

* `kræsje+V+Inf` => `kræsj`
* `kræsje` has no analyses either

**rugle** failures:

* `rugle+V+Inf` => `rugl`
* `rugle` has no analyses either

**gnaure** failures:

* `gnaure+V+Inf` => `gnaur`
* `gnaure` has no analyses either

**ringmerke** failures:

* `ringmerke+V+Inf` => `ringmerk`
* `ringmerke` has no analyses either

**ergre** failures:

* `ergre+V+Inf` => `ergr`
* `ergre` has no analyses either

**nedfelle** failures:

* `nedfelle+V+Inf` does not generate!
* `nedfelle` has no analyses either

**åndeleggjere** failures:

* `åndeleggjere+V+Inf` does not generate!
* `åndeleggjere` has no analyses either

**ferske** failures:

* `ferske+V+Inf` => `fersk`
* `ferske` has following analyses:
  * `fersken+A+Neu+Sg+Indef`
  * `fersk+A+A+Pl+Indef`
  * `fersk+A+A+Sg+Def`
  * `fersk+A+A+Pl+Def`

**hugta** failures:

* `hugta+V+Inf` => `hugtta`
* `hugta` has no analyses either

**forulempe** failures:

* `forulempe+V+Inf` => `forulemp`
* `forulempe` has no analyses either

**overskyggje** failures:

* `overskyggje+V+Inf` does not generate!
* `overskyggje` has no analyses either

**segregere** failures:

* `segregere+V+Inf` => `segreger`
* `segregere` has no analyses either

**overlaste** failures:

* `overlaste+V+Inf` => `overlast`
* `overlaste` has no analyses either

**dove** failures:

* `dove+V+Inf` => `dov`
* `dove` has following analyses:
  * `dov+A+A+Pl+Def`
  * `dov+A+A+Pl+Indef`
  * `doven+A+Neu+Sg+Indef`
  * `dov+A+A+Sg+Def`

**stemme** failures:

* `stemme+V+Inf` does not generate!
* `stemme` has no analyses either

**uniformere** failures:

* `uniformere+V+Inf` => `uniformer`
* `uniformere` has no analyses either

**forlesse** failures:

* `forlesse+V+Inf` => `forles`
* `forlesse` has no analyses either

**snøgge** failures:

* `snøgge+V+Inf` => `snøgg`
* `snøgge` has no analyses either

**kome** failures:

* `kome+V+Inf` => `kkoma`
* `kome+V+Inf` => `kkome`
* `kome` has following analyses:
  * `kommen+A+Neu+Sg+Indef`
  * `komen+A+Neu+Sg+Indef`

**-fisere** failures:

* `-fisere+V+Inf` => `-fiser`
* `-fisere` has no analyses either

**menesere** failures:

* `menesere+V+Inf` => `meneser`
* `menesere` has no analyses either

**gørre** failures:

* `gørre+V+Inf` => `gørr`
* `gørre` has following analyses:
  * `gørr+A+A+Pl+Indef`
  * `gørr+A+A+Sg+Def`
  * `gørr+A+A+Pl+Def`

**transportere** failures:

* `transportere+V+Inf` => `transporter`
* `transportere` has no analyses either

**for** failures:

* `for+V+Inf` does not generate!
* `for` has following analyses:
  * `for+CC`
  * `fore+N+N+Fem+Sg+Indef`
  * `fore+V+Imprt`
  * `for+N+N+Neu+Pl+Indef`
  * `for+N+N+Fem+Sg+Indef`
  * `for+A+Msc+Sg+Indef`
  * `for+A+Fem+Sg+Indef`
  * `for+Pr`
  * `for+N+N+Neu+Sg+Indef`
  * `fore+V+Inf`

**liskre** failures:

* `liskre+V+Inf` => `liskr`
* `liskre` has no analyses either

**speike** failures:

* `speike+V+Inf` => `speik`
* `speike` has no analyses either

**økse** failures:

* `økse+V+Inf` => `øks`
* `økse` has no analyses either

**mykje** failures:

* `mykje+V+Inf` does not generate!
* `mykje` has following analyses:
  * `mykjen+A+Neu+Sg+Indef`

**føreskrive** failures:

* `føreskrive+V+Inf` => `føreskrie`
* `føreskrive+V+Inf` => `føreskria`
* `føreskrive` has no analyses either

**grise** failures:

* `grise+V+Inf` => `gris`
* `grise` has following analyses:
  * `grisen+A+Neu+Sg+Indef`
  * `gris+A+Sg+Def`
  * `grissen+A+Neu+Sg+Indef`
  * `gris+A+Pl+Indef`

**dikte** failures:

* `dikte+V+Inf` => `dikt`
* `dikte` has no analyses either

**streame** failures:

* `streame+V+Inf` => `stream`
* `streame` has no analyses either

**investere** failures:

* `investere+V+Inf` => `invester`
* `investere` has no analyses either

**lalle** failures:

* `lalle+V+Inf` => `lall`
* `lalle` has no analyses either

**nye** failures:

* `nye+V+Inf` => `ny`
* `nye` has following analyses:
  * `nype+V+Inf`
  * `nyte+V+Inf`
  * `ny+A+Sg+Def`
  * `nygge+V+Inf`
  * `nyse+V+Inf`
  * `ny+A+Pl+Indef`

**levandegjere** failures:

* `levandegjere+V+Inf` does not generate!
* `levandegjere` has no analyses either

**skritte** failures:

* `skritte+V+Inf` => `skritt`
* `skritte` has no analyses either

**skylde** failures:

* `skylde+V+Inf` does not generate!
* `skylde` has following analyses:
  * `skylje+V+Ind+Prt`
  * `skyld+A+A+Pl+Def`
  * `skyld+A+A+Sg+Def`
  * `skyld+A+A+Pl+Indef`

**artikulere** failures:

* `artikulere+V+Inf` => `artikuler`
* `artikulere` has no analyses either

**avise** failures:

* `avise+V+Inf` does not generate!
* `avise` has no analyses either

**brevbyte** failures:

* `brevbyte+V+Inf` => `brevbyt`
* `brevbyte` has no analyses either

**aske** failures:

* `aske+V+Inf` => `ask`
* `aske` has no analyses either

**behalde** failures:

* `behalde+V+Inf` => `behaa`
* `behalde+V+Inf` => `behae`
* `behalde` has no analyses either

**blottstille** failures:

* `blottstille+V+Inf` does not generate!
* `blottstille` has no analyses either

**kringsetje** failures:

* `kringsetje+V+Inf` does not generate!
* `kringsetje` has no analyses either

**halshogge** failures:

* `halshogge+V+Inf` does not generate!
* `halshogge` has no analyses either

**reie** failures:

* `reie+V+Inf` does not generate!
* `reie` has following analyses:
  * `reie+A+A+Fem+Sg+Indef`
  * `reie+A+A+Msc+Sg+Indef`
  * `reie+A+A+Neu+Sg+Indef`

**fjernsyne** failures:

* `fjernsyne+V+Inf` => `fjernsyn`
* `fjernsyne` has no analyses either

**svivyrde** failures:

* `svivyrde+V+Inf` does not generate!
* `svivyrde` has no analyses either

**perle** failures:

* `perle+V+Inf` => `perl`
* `perle` has no analyses either

**oppgje** failures:

* `oppgje+V+Inf` => `oppggje`
* `oppgje` has no analyses either

**ende** failures:

* `ende+V+Inf` => `end`
* `ende` has following analyses:
  * `ende+A+A+Fem+Sg+Indef`
  * `ende+A+A+Msc+Sg+Indef`
  * `ende+A+A+Neu+Sg+Indef`

**byte** failures:

* `byte+V+Inf` => `byt`
* `byte` has no analyses either

**smiske** failures:

* `smiske+V+Inf` => `smisk`
* `smiske` has no analyses either

**stamme** failures:

* `stamme+V+Inf` => `stam`
* `stamme` has no analyses either

**brenne** failures:

* `brenne+V+Inf` does not generate!
* `brenne` has no analyses either

**fare** failures:

* `fare+V+Inf` => `fae`
* `fare+V+Inf` => `faa`
* `fare` has following analyses:
  * `faren+A+Neu+Sg+Indef`

**impregnere** failures:

* `impregnere+V+Inf` => `impregner`
* `impregnere` has no analyses either

**utstyre** failures:

* `utstyre+V+Inf` => `utstyr`
* `utstyre` has no analyses either

**mistvile** failures:

* `mistvile+V+Inf` does not generate!
* `mistvile` has no analyses either

**tørrdestillere** failures:

* `tørrdestillere+V+Inf` => `tørrdestiller`
* `tørrdestillere` has no analyses either

**hykje** failures:

* `hykje+V+Inf` does not generate!
* `hykje` has no analyses either

**omsetje** failures:

* `omsetje+V+Inf` does not generate!
* `omsetje` has no analyses either

**hage** failures:

* `hage+V+Inf` => `hag`
* `hage` has following analyses:
  * `hag+A+Pl+Indef`
  * `hag+A+Sg+Def`

**åmoge** failures:

* `åmoge+V+Inf` => `åmog`
* `åmoge` has no analyses either

**grøype** failures:

* `grøype+V+Inf` => `grøyp`
* `grøype` has no analyses either

**blenke** failures:

* `blenke+V+Inf` => `blenk`
* `blenke` has no analyses either

**skvatre** failures:

* `skvatre+V+Inf` => `skvatr`
* `skvatre` has no analyses either

**derivere** failures:

* `derivere+V+Inf` => `deriver`
* `derivere` has no analyses either

**trappe** failures:

* `trappe+V+Inf` => `trapp`
* `trappe` has no analyses either

**fristille** failures:

* `fristille+V+Inf` => `fristile`
* `fristille+V+Inf` => `fristila`
* `fristille` has no analyses either

**bølje** failures:

* `bølje+V+Inf` => `bølj`
* `bølje` has no analyses either

**dadle** failures:

* `dadle+V+Inf` => `dadl`
* `dadle` has no analyses either

**hacke** failures:

* `hacke+V+Inf` => `hack`
* `hacke` has no analyses either

**buge** failures:

* `buge+V+Inf` => `bug`
* `buge` has no analyses either

**bleikne** failures:

* `bleikne+V+Inf` => `bleikn`
* `bleikne` has no analyses either

**munse** failures:

* `munse+V+Inf` => `muns`
* `munse` has no analyses either

**duppe** failures:

* `duppe+V+Inf` => `dupp`
* `duppe` has no analyses either

**tilte** failures:

* `tilte+V+Inf` => `tilt`
* `tilte` has no analyses either

**stropne** failures:

* `stropne+V+Inf` => `stropn`
* `stropne` has following analyses:
  * `stropen+A+Pl+Indef`
  * `stropen+A+Sg+Def`

**kløyve** failures:

* `kløyve+V+Inf` does not generate!
* `kløyve` has following analyses:
  * `kløyv+A+Sg+Def`
  * `kløyv+A+Pl+Indef`

**frustrere** failures:

* `frustrere+V+Inf` => `frustrer`
* `frustrere` has no analyses either

**etterkome** failures:

* `etterkome+V+Inf` => `etterkkome`
* `etterkome+V+Inf` => `etterkkoma`
* `etterkome` has no analyses either

**restaurere** failures:

* `restaurere+V+Inf` => `restaurer`
* `restaurere` has no analyses either

**ryskje** failures:

* `ryskje+V+Inf` does not generate!
* `ryskje` has no analyses either

**lyne** failures:

* `lyne+V+Inf` => `lyn`
* `lyne` has no analyses either

**heke** failures:

* `heke+V+Inf` => `hek`
* `heke` has no analyses either

**korsmerkje** failures:

* `korsmerkje+V+Inf` does not generate!
* `korsmerkje` has no analyses either

**unnskylde** failures:

* `unnskylde+V+Inf` does not generate!
* `unnskylde` has no analyses either

**avdagast** failures:

* `avdagast+V+Inf` => `avdagastast`
* `avdagast` has no analyses either

**skymre** failures:

* `skymre+V+Inf` => `skymr`
* `skymre` has no analyses either

**demotivere** failures:

* `demotivere+V+Inf` => `demotiver`
* `demotivere` has no analyses either

**snaske** failures:

* `snaske+V+Inf` => `snask`
* `snaske` has no analyses either

**leie** failures:

* `leie+V+Inf` does not generate!
* `leie` has following analyses:
  * `lei+A+Sg+Def`
  * `lei+A+Pl+Indef`

**dregge** failures:

* `dregge+V+Inf` => `dregg`
* `dregge` has no analyses either

**spøkje** failures:

* `spøkje+V+Inf` does not generate!
* `spøkje` has no analyses either

**frigjeve** failures:

* `frigjeve+V+Inf` => `friggjeva`
* `frigjeve+V+Inf` => `friggjeve`
* `frigjeve` has no analyses either

**pleie** failures:

* `pleie+V+Inf` does not generate!
* `pleie` has no analyses either

**sesongdebutere** failures:

* `sesongdebutere+V+Inf` => `sesongdebuter`
* `sesongdebutere` has no analyses either

**oppsøke** failures:

* `oppsøke+V+Inf` => `oppsøk`
* `oppsøke` has no analyses either

**sprade** failures:

* `sprade+V+Inf` => `sprad`
* `sprade` has no analyses either

**fangste** failures:

* `fangste+V+Inf` => `fangst`
* `fangste` has no analyses either

**garantere** failures:

* `garantere+V+Inf` => `garanter`
* `garantere` has no analyses either

**domestisere** failures:

* `domestisere+V+Inf` => `domestiser`
* `domestisere` has no analyses either

**residere** failures:

* `residere+V+Inf` => `resider`
* `residere` has no analyses either

**pikke** failures:

* `pikke+V+Inf` => `pikk`
* `pikke` has no analyses either

**robbe** failures:

* `robbe+V+Inf` => `robb`
* `robbe` has no analyses either

**grønke** failures:

* `grønke+V+Inf` => `grønk`
* `grønke` has no analyses either

**tråkke** failures:

* `tråkke+V+Inf` => `tråkk`
* `tråkke` has no analyses either

**tvike** failures:

* `tvike+V+Inf` => `tvik`
* `tvike` has no analyses either

**flugse** failures:

* `flugse+V+Inf` => `flugs`
* `flugse` has no analyses either

**frikjenne** failures:

* `frikjenne+V+Inf` does not generate!
* `frikjenne` has no analyses either

**presumere** failures:

* `presumere+V+Inf` => `presumer`
* `presumere` has no analyses either

**strunte** failures:

* `strunte+V+Inf` => `strunt`
* `strunte` has no analyses either

**stålsetje** failures:

* `stålsetje+V+Inf` does not generate!
* `stålsetje` has no analyses either

**plaffe** failures:

* `plaffe+V+Inf` => `plaff`
* `plaffe` has no analyses either

**armere** failures:

* `armere+V+Inf` => `armer`
* `armere` has no analyses either

**skjønne** failures:

* `skjønne+V+Inf` does not generate!
* `skjønne` has no analyses either

**søple** failures:

* `søple+V+Inf` => `søpl`
* `søple` has no analyses either

**rafte** failures:

* `rafte+V+Inf` => `raft`
* `rafte` has no analyses either

**anduge** failures:

* `anduge+V+Inf` => `andug`
* `anduge` has no analyses either

**porsjonere** failures:

* `porsjonere+V+Inf` => `porsjoner`
* `porsjonere` has no analyses either

**pakke** failures:

* `pakke+V+Inf` => `pakk`
* `pakke` has no analyses either

**aule** failures:

* `aule+V+Inf` => `aul`
* `aule` has no analyses either

**halloie** failures:

* `halloie+V+Inf` => `halloi`
* `halloie` has no analyses either

**syrne** failures:

* `syrne+V+Inf` => `syrn`
* `syrne` has no analyses either

**herje** failures:

* `herje+V+Inf` => `herj`
* `herje` has no analyses either

**traumatisere** failures:

* `traumatisere+V+Inf` => `traumatiser`
* `traumatisere` has no analyses either

**frankere** failures:

* `frankere+V+Inf` => `franker`
* `frankere` has no analyses either

**tøyme** failures:

* `tøyme+V+Inf` does not generate!
* `tøyme` has no analyses either

**toske** failures:

* `toske+V+Inf` => `tosk`
* `toske` has no analyses either

**transittere** failures:

* `transittere+V+Inf` => `transitter`
* `transittere` has no analyses either

**derangere** failures:

* `derangere+V+Inf` => `deranger`
* `derangere` has no analyses either

**gysje** failures:

* `gysje+V+Inf` => `gysj`
* `gysje` has no analyses either

**avheimle** failures:

* `avheimle+V+Inf` => `avheiml`
* `avheimle` has no analyses either

**ekskludere** failures:

* `ekskludere+V+Inf` => `ekskluder`
* `ekskludere` has no analyses either

**byrte** failures:

* `byrte+V+Inf` does not generate!
* `byrte` has no analyses either

**sodde** failures:

* `sodde+V+Inf` => `sodd`
* `sodde` has no analyses either

**sause** failures:

* `sause+V+Inf` => `saus`
* `sause` has no analyses either

**digne** failures:

* `digne+V+Inf` => `dign`
* `digne` has no analyses either

**insistere** failures:

* `insistere+V+Inf` => `insister`
* `insistere` has no analyses either

**rage** failures:

* `rage+V+Inf` => `rag`
* `rage` has following analyses:
  * `ragen+A+Neu+Sg+Indef`

**ruffe** failures:

* `ruffe+V+Inf` => `ruff`
* `ruffe` has no analyses either

**forelske** failures:

* `forelske+V+Inf` => `forelsk`
* `forelske` has no analyses either

**stridgråte** failures:

* `stridgråte+V+Inf` => `stridgråe`
* `stridgråte+V+Inf` => `stridgråa`
* `stridgråte` has no analyses either

**aldrast** failures:

* `aldrast+V+Inf` => `aldrastast`
* `aldrast` has no analyses either

**forsnakke** failures:

* `forsnakke+V+Inf` => `forsnakk`
* `forsnakke` has no analyses either

**tilskrive** failures:

* `tilskrive+V+Inf` => `tilskrie`
* `tilskrive+V+Inf` => `tilskria`
* `tilskrive` has no analyses either

**svaie** failures:

* `svaie+V+Inf` => `svai`
* `svaie` has following analyses:
  * `svai+A+Pl+Indef`
  * `svai+A+Sg+Def`

**tøye** failures:

* `tøye+V+Inf` => `tøy`
* `tøye` has no analyses either

**eksellere** failures:

* `eksellere+V+Inf` => `ekseller`
* `eksellere` has no analyses either

**hypnotisere** failures:

* `hypnotisere+V+Inf` => `hypnotiser`
* `hypnotisere` has no analyses either

**utrydde** failures:

* `utrydde+V+Inf` => `utrydd`
* `utrydde` has no analyses either

**dampe** failures:

* `dampe+V+Inf` => `damp`
* `dampe` has no analyses either

**irettesetje** failures:

* `irettesetje+V+Inf` does not generate!
* `irettesetje` has no analyses either

**vinne** failures:

* `vinne+V+Inf` => `vinn`
* `vinne` has no analyses either

**skjekke** failures:

* `skjekke+V+Inf` does not generate!
* `skjekke` has no analyses either

**fleske** failures:

* `fleske+V+Inf` => `flesk`
* `fleske` has no analyses either

**eksaminere** failures:

* `eksaminere+V+Inf` => `eksaminer`
* `eksaminere` has no analyses either

**ionisere** failures:

* `ionisere+V+Inf` => `ioniser`
* `ionisere` has no analyses either

**utføre** failures:

* `utføre+V+Inf` does not generate!
* `utføre` has no analyses either

**augne** failures:

* `augne+V+Inf` => `augn`
* `augne` has no analyses either

**skjolde** failures:

* `skjolde+V+Inf` => `skjold`
* `skjolde` has no analyses either

**skyme** failures:

* `skyme+V+Inf` does not generate!
* `skyme` has following analyses:
  * `skym+A+Sg+Def`
  * `skym+A+Pl+Indef`

**skvæle** failures:

* `skvæle+V+Inf` => `skvæl`
* `skvæle` has no analyses either

**eikje** failures:

* `eikje+V+Inf` => `eik`
* `eikje` has no analyses either

**denaturalisere** failures:

* `denaturalisere+V+Inf` => `denaturaliser`
* `denaturalisere` has no analyses either

**dilke** failures:

* `dilke+V+Inf` => `dilk`
* `dilke` has no analyses either

**smuske** failures:

* `smuske+V+Inf` => `smusk`
* `smuske` has no analyses either

**gjorde** failures:

* `gjorde+V+Inf` => `gjord`
* `gjorde` has following analyses:
  * `gjord+A+Sg+Def`
  * `gjord+A+Pl+Indef`
  * `gjøre+V+Ind+Prt`
  * `gjere+V+Ind+Prt`

**orientere** failures:

* `orientere+V+Inf` => `orienter`
* `orientere` has no analyses either

**regalere** failures:

* `regalere+V+Inf` => `regaler`
* `regalere` has no analyses either

**title** failures:

* `title+V+Inf` => `titl`
* `title` has no analyses either

**promulgere** failures:

* `promulgere+V+Inf` => `promulger`
* `promulgere` has no analyses either

**grope** failures:

* `grope+V+Inf` => `grop`
* `grope` has following analyses:
  * `gropen+A+Neu+Sg+Indef`

**isne** failures:

* `isne+V+Inf` => `isn`
* `isne` has no analyses either

**kvisje** failures:

* `kvisje+V+Inf` => `kvisj`
* `kvisje` has no analyses either

**leiste** failures:

* `leiste+V+Inf` => `leist`
* `leiste` has no analyses either

**belle** failures:

* `belle+V+Inf` does not generate!
* `belle` has no analyses either

**overrekkje** failures:

* `overrekkje+V+Inf` does not generate!
* `overrekkje` has no analyses either

**teksturere** failures:

* `teksturere+V+Inf` => `teksturer`
* `teksturere` has no analyses either

**gramme** failures:

* `gramme+V+Inf` => `gram`
* `gramme` has no analyses either

**demolere** failures:

* `demolere+V+Inf` => `demoler`
* `demolere` has no analyses either

**ande** failures:

* `ande+V+Inf` => `and`
* `ande` has no analyses either

**ajourføre** failures:

* `ajourføre+V+Inf` does not generate!
* `ajourføre` has no analyses either

**homme** failures:

* `homme+V+Inf` => `hom`
* `homme` has no analyses either

**klarleggje** failures:

* `klarleggje+V+Inf` does not generate!
* `klarleggje` has no analyses either

**vulgarisere** failures:

* `vulgarisere+V+Inf` => `vulgariser`
* `vulgarisere` has no analyses either

**skrinlegge** failures:

* `skrinlegge+V+Inf` does not generate!
* `skrinlegge` has no analyses either

**kollsigle** failures:

* `kollsigle+V+Inf` does not generate!
* `kollsigle` has no analyses either

**olle** failures:

* `olle+V+Inf` => `oll`
* `olle` has no analyses either

**line** failures:

* `line+V+Inf` => `lin`
* `line` has following analyses:
  * `linn+A+A+Pl+Def`
  * `linn+A+A+Pl+Indef`
  * `lin+A+Sg+Def`
  * `linn+A+A+Sg+Def`
  * `lin+A+Pl+Indef`
  * `lite+V+Adj+Fl`
  * `lite+V+Adj+Bu+Eint`

**fintelje** failures:

* `fintelje+V+Inf` does not generate!
* `fintelje` has no analyses either

**pelletisere** failures:

* `pelletisere+V+Inf` => `pelletiser`
* `pelletisere` has no analyses either

**invalidisere** failures:

* `invalidisere+V+Inf` => `invalidiser`
* `invalidisere` has no analyses either

**feite** failures:

* `feite+V+Inf` => `feit`
* `feite` has following analyses:
  * `feit+A+Sg+Def`
  * `feit+A+Pl+Indef`

**haike** failures:

* `haike+V+Inf` => `haik`
* `haike` has no analyses either

**léte** failures:

* `léte+V+Inf` => `lét`
* `léte` has no analyses either

**skrave** failures:

* `skrave+V+Inf` => `skrav`
* `skrave` has no analyses either

**tvidele** failures:

* `tvidele+V+Inf` => `tvidel`
* `tvidele` has no analyses either

**glatte** failures:

* `glatte+V+Inf` => `glatt`
* `glatte` has following analyses:
  * `glatt+A+A+Pl+Indef`
  * `glatt+A+A+Sg+Def`
  * `glatt+A+A+Pl+Def`

**steine** failures:

* `steine+V+Inf` => `stein`
* `steine` has following analyses:
  * `stein+A+A+Pl+Def`
  * `stein+A+A+Sg+Def`
  * `stein+A+A+Pl+Indef`

**avstytte** failures:

* `avstytte+V+Inf` does not generate!
* `avstytte` has no analyses either

**sælde** failures:

* `sælde+V+Inf` does not generate!
* `sælde` has no analyses either

**taktere** failures:

* `taktere+V+Inf` => `takter`
* `taktere` has no analyses either

**personifisere** failures:

* `personifisere+V+Inf` => `personifiser`
* `personifisere` has no analyses either

**tagne** failures:

* `tagne+V+Inf` => `tagn`
* `tagne` has no analyses either

**smake** failures:

* `smake+V+Inf` does not generate!
* `smake` has no analyses either

**hanske** failures:

* `hanske+V+Inf` => `hansk`
* `hanske` has no analyses either

**sjøsette** failures:

* `sjøsette+V+Inf` does not generate!
* `sjøsette` has no analyses either

**bestråle** failures:

* `bestråle+V+Inf` does not generate!
* `bestråle` has no analyses either

**palme** failures:

* `palme+V+Inf` => `palm`
* `palme` has no analyses either

**snare** failures:

* `snare+V+Inf` => `snar`
* `snare` has following analyses:
  * `snar+A+Pl+Indef`
  * `snar+A+Sg+Def`

**branntrygde** failures:

* `branntrygde+V+Inf` => `branntrygd`
* `branntrygde` has no analyses either

**plente** failures:

* `plente+V+Inf` => `plent`
* `plente` has following analyses:
  * `plent+A+A+Pl+Indef`
  * `plent+A+A+Sg+Def`
  * `plent+A+A+Pl+Def`

**dokumentere** failures:

* `dokumentere+V+Inf` => `dokumenter`
* `dokumentere` has no analyses either

**mumifisere** failures:

* `mumifisere+V+Inf` => `mumifiser`
* `mumifisere` has no analyses either

**ridle** failures:

* `ridle+V+Inf` => `ridl`
* `ridle` has no analyses either

**tufse** failures:

* `tufse+V+Inf` => `tufs`
* `tufse` has following analyses:
  * `tufs+A+A+Sg+Def`
  * `tufs+A+A+Pl+Def`
  * `tufs+A+A+Pl+Indef`
  * `tufsen+A+Neu+Sg+Indef`

**annotere** failures:

* `annotere+V+Inf` => `annoter`
* `annotere` has no analyses either

**resonnere** failures:

* `resonnere+V+Inf` => `resonner`
* `resonnere` has no analyses either

**atterskape** failures:

* `atterskape+V+Inf` => `atterskap`
* `atterskape` has no analyses either

**konte** failures:

* `konte+V+Inf` => `kont`
* `konte` has no analyses either

**ætte** failures:

* `ætte+V+Inf` => `ætt`
* `ætte` has no analyses either

**lovere** failures:

* `lovere+V+Inf` => `lover`
* `lovere` has no analyses either

**skalere** failures:

* `skalere+V+Inf` => `skaler`
* `skalere` has no analyses either

**infantilisere** failures:

* `infantilisere+V+Inf` => `infantiliser`
* `infantilisere` has no analyses either

**komplettere** failures:

* `komplettere+V+Inf` => `kompletter`
* `komplettere` has no analyses either

**knarte** failures:

* `knarte+V+Inf` => `knart`
* `knarte` has no analyses either

**anløpe** failures:

* `anløpe+V+Inf` => `anløp`
* `anløpe` has no analyses either

**tjuve** failures:

* `tjuve+V+Inf` => `tjuv`
* `tjuve` has no analyses either

**tøyse** failures:

* `tøyse+V+Inf` does not generate!
* `tøyse` has no analyses either

**blade** failures:

* `blade+V+Inf` does not generate!
* `blade` has no analyses either

**clutche** failures:

* `clutche+V+Inf` => `clutch`
* `clutche` has no analyses either

**korrespondere** failures:

* `korrespondere+V+Inf` => `korresponder`
* `korrespondere` has no analyses either

**dundre** failures:

* `dundre+V+Inf` => `dundr`
* `dundre` has no analyses either

**ureine** failures:

* `ureine+V+Inf` => `urein`
* `ureine` has following analyses:
  * `urein+A+Pl+Indef`
  * `urein+A+Sg+Def`

**jordfeste** failures:

* `jordfeste+V+Inf` does not generate!
* `jordfeste` has no analyses either

**desinformere** failures:

* `desinformere+V+Inf` => `desinformer`
* `desinformere` has no analyses either

**klase** failures:

* `klase+V+Inf` => `klas`
* `klase` has no analyses either

**lauve** failures:

* `lauve+V+Inf` => `lauv`
* `lauve` has no analyses either

**samtale** failures:

* `samtale+V+Inf` does not generate!
* `samtale` has no analyses either

**dvele** failures:

* `dvele+V+Inf` => `dvel`
* `dvele` has no analyses either

**forverre** failures:

* `forverre+V+Inf` => `forverr`
* `forverre` has no analyses either

**irrigere** failures:

* `irrigere+V+Inf` => `irriger`
* `irrigere` has no analyses either

**akkedere** failures:

* `akkedere+V+Inf` => `akkeder`
* `akkedere` has no analyses either

**kjederøykje** failures:

* `kjederøykje+V+Inf` does not generate!
* `kjederøykje` has no analyses either

**flæse** failures:

* `flæse+V+Inf` => `flæs`
* `flæse` has no analyses either

**konstituere** failures:

* `konstituere+V+Inf` => `konstituer`
* `konstituere` has no analyses either

**krysse** failures:

* `krysse+V+Inf` => `kryss`
* `krysse` has no analyses either

**skåne** failures:

* `skåne+V+Inf` => `skån`
* `skåne` has no analyses either

**fangta** failures:

* `fangta+V+Inf` => `fangtta`
* `fangta` has no analyses either

**nedprioritere** failures:

* `nedprioritere+V+Inf` => `nedprioriter`
* `nedprioritere` has no analyses either

**flikje** failures:

* `flikje+V+Inf` does not generate!
* `flikje` has no analyses either

**ornamentere** failures:

* `ornamentere+V+Inf` => `ornamenter`
* `ornamentere` has no analyses either

**staile** failures:

* `staile+V+Inf` => `stail`
* `staile` has no analyses either

**evle** failures:

* `evle+V+Inf` => `evl`
* `evle` has no analyses either

**fjone** failures:

* `fjone+V+Inf` => `fjon`
* `fjone` has no analyses either

**føykje** failures:

* `føykje+V+Inf` does not generate!
* `føykje` has no analyses either

**geispe** failures:

* `geispe+V+Inf` => `geisp`
* `geispe` has following analyses:
  * `geispen+A+Neu+Sg+Indef`

**fåne** failures:

* `fåne+V+Inf` => `fån`
* `fåne` has following analyses:
  * `fåen+A+Pl+Indef`
  * `fåen+A+Sg+Def`

**rale** failures:

* `rale+V+Inf` => `ral`
* `rale` has no analyses either

**tverr-rykke** failures:

* `tverr-rykke+V+Inf` does not generate!
* `tverr-rykke` has no analyses either

**resosialisere** failures:

* `resosialisere+V+Inf` => `resosialiser`
* `resosialisere` has no analyses either

**landsetje** failures:

* `landsetje+V+Inf` does not generate!
* `landsetje` has no analyses either

**kastehandle** failures:

* `kastehandle+V+Inf` => `kastehandl`
* `kastehandle` has no analyses either

**graute** failures:

* `graute+V+Inf` => `graut`
* `graute` has following analyses:
  * `grauten+A+Neu+Sg+Indef`

**auskultere** failures:

* `auskultere+V+Inf` => `auskulter`
* `auskultere` has no analyses either

**inntreffe** failures:

* `inntreffe+V+Inf` => `inntrefe`
* `inntreffe+V+Inf` => `inntrefa`
* `inntreffe` has no analyses either

**stive** failures:

* `stive+V+Inf` => `stiv`
* `stive` has following analyses:
  * `stiv+A+Sg+Def`
  * `stiv+A+Pl+Indef`

**polarisere** failures:

* `polarisere+V+Inf` => `polariser`
* `polarisere` has no analyses either

**verbalisere** failures:

* `verbalisere+V+Inf` => `verbaliser`
* `verbalisere` has no analyses either

**dryge** failures:

* `dryge+V+Inf` does not generate!
* `dryge` has following analyses:
  * `dryg+A+Pl+Indef`
  * `dryg+A+Sg+Def`

**vippe** failures:

* `vippe+V+Inf` => `vipp`
* `vippe` has no analyses either

**autorisere** failures:

* `autorisere+V+Inf` => `autoriser`
* `autorisere` has no analyses either

**hulkegråte** failures:

* `hulkegråte+V+Inf` => `hulkegråa`
* `hulkegråte+V+Inf` => `hulkegråe`
* `hulkegråte` has no analyses either

**tiltrekkje** failures:

* `tiltrekkje+V+Inf` does not generate!
* `tiltrekkje` has no analyses either

**kreditere** failures:

* `kreditere+V+Inf` => `krediter`
* `kreditere` has no analyses either

**nomadisere** failures:

* `nomadisere+V+Inf` => `nomadiser`
* `nomadisere` has no analyses either

**fergje** failures:

* `fergje+V+Inf` does not generate!
* `fergje` has no analyses either

**slime** failures:

* `slime+V+Inf` => `slim`
* `slime` has no analyses either

**etymologisere** failures:

* `etymologisere+V+Inf` => `etymologiser`
* `etymologisere` has no analyses either

**formørkje** failures:

* `formørkje+V+Inf` does not generate!
* `formørkje` has no analyses either

**attgi** failures:

* `attgi+V+Inf` => `attggi`
* `attgi` has no analyses either

**berje** failures:

* `berje+V+Inf` does not generate!
* `berje` has no analyses either

**drifte** failures:

* `drifte+V+Inf` => `drift`
* `drifte` has no analyses either

**forsyrgje** failures:

* `forsyrgje+V+Inf` does not generate!
* `forsyrgje` has no analyses either

**fingre** failures:

* `fingre+V+Inf` => `fingr`
* `fingre` has no analyses either

**overfuse** failures:

* `overfuse+V+Inf` => `overfus`
* `overfuse` has no analyses either

**utforske** failures:

* `utforske+V+Inf` => `utforsk`
* `utforske` has no analyses either

**spraye** failures:

* `spraye+V+Inf` => `spray`
* `spraye` has no analyses either

**oscillere** failures:

* `oscillere+V+Inf` => `osciller`
* `oscillere` has no analyses either

**spøte** failures:

* `spøte+V+Inf` => `spøt`
* `spøte` has no analyses either

**radle** failures:

* `radle+V+Inf` => `radl`
* `radle` has no analyses either

**snerre** failures:

* `snerre+V+Inf` => `snerr`
* `snerre` has no analyses either

**trafikkere** failures:

* `trafikkere+V+Inf` => `trafikker`
* `trafikkere` has no analyses either

**kvetje** failures:

* `kvetje+V+Inf` does not generate!
* `kvetje` has no analyses either

**gerere** failures:

* `gerere+V+Inf` => `gerer`
* `gerere` has no analyses either

**deklarere** failures:

* `deklarere+V+Inf` => `deklarer`
* `deklarere` has no analyses either

**pepre** failures:

* `pepre+V+Inf` => `pepr`
* `pepre` has no analyses either

**disputere** failures:

* `disputere+V+Inf` => `disputer`
* `disputere` has no analyses either

**ubbe** failures:

* `ubbe+V+Inf` => `ubb`
* `ubbe` has no analyses either

**gnise** failures:

* `gnise+V+Inf` does not generate!
* `gnise` has no analyses either

**sprøyte** failures:

* `sprøyte+V+Inf` does not generate!
* `sprøyte` has no analyses either

**undervise** failures:

* `undervise+V+Inf` => `undervis`
* `undervise` has no analyses either

**spekje** failures:

* `spekje+V+Inf` does not generate!
* `spekje` has no analyses either

**nyste** failures:

* `nyste+V+Inf` => `nyst`
* `nyste` has no analyses either

**kultivere** failures:

* `kultivere+V+Inf` => `kultiver`
* `kultivere` has no analyses either

**skadeskyte** failures:

* `skadeskyte+V+Inf` => `skadeskya`
* `skadeskyte+V+Inf` => `skadeskye`
* `skadeskyte` has no analyses either

**avmerkje** failures:

* `avmerkje+V+Inf` does not generate!
* `avmerkje` has no analyses either

**rocke** failures:

* `rocke+V+Inf` => `rock`
* `rocke` has no analyses either

**kjene** failures:

* `kjene+V+Inf` => `kjen`
* `kjene` has no analyses either

**kollidere** failures:

* `kollidere+V+Inf` => `kollider`
* `kollidere` has no analyses either

**gjengjelde** failures:

* `gjengjelde+V+Inf` does not generate!
* `gjengjelde` has no analyses either

**konnotere** failures:

* `konnotere+V+Inf` => `konnoter`
* `konnotere` has no analyses either

**gnyste** failures:

* `gnyste+V+Inf` => `gnyst`
* `gnyste` has no analyses either

**nugge** failures:

* `nugge+V+Inf` => `nugg`
* `nugge` has no analyses either

**sutte** failures:

* `sutte+V+Inf` => `sutt`
* `sutte` has no analyses either

**losne** failures:

* `losne+V+Inf` => `losn`
* `losne` has no analyses either

**omstøyte** failures:

* `omstøyte+V+Inf` => `omstøyt`
* `omstøyte` has no analyses either

**overraske** failures:

* `overraske+V+Inf` => `overrask`
* `overraske` has no analyses either

**forrøyne** failures:

* `forrøyne+V+Inf` does not generate!
* `forrøyne` has no analyses either

**maie** failures:

* `maie+V+Inf` => `mai`
* `maie` has no analyses either

**innebere** failures:

* `innebere+V+Inf` => `innebee`
* `innebere+V+Inf` => `innebea`
* `innebere` has no analyses either

**forlenge** failures:

* `forlenge+V+Inf` does not generate!
* `forlenge` has no analyses either

**avverje** failures:

* `avverje+V+Inf` => `avverj`
* `avverje` has no analyses either

**avansere** failures:

* `avansere+V+Inf` => `avanser`
* `avansere` has no analyses either

**kvalitetssikre** failures:

* `kvalitetssikre+V+Inf` => `kvalitetssikr`
* `kvalitetssikre` has no analyses either

**koppe** failures:

* `koppe+V+Inf` => `kopp`
* `koppe` has no analyses either

**snåpe** failures:

* `snåpe+V+Inf` => `snåp`
* `snåpe` has following analyses:
  * `snåp+A+Pl+Indef`
  * `snåp+A+Sg+Def`

**grunngje** failures:

* `grunngje+V+Inf` => `grunnggje`
* `grunngje` has no analyses either

**spørje** failures:

* `spørje+V+Inf` does not generate!
* `spørje` has no analyses either

**konservere** failures:

* `konservere+V+Inf` => `konserver`
* `konservere` has no analyses either

**kryptifisere** failures:

* `kryptifisere+V+Inf` => `kryptifiser`
* `kryptifisere` has no analyses either

**krengje** failures:

* `krengje+V+Inf` does not generate!
* `krengje` has no analyses either

**hire** failures:

* `hire+V+Inf` => `hir`
* `hire` has following analyses:
  * `hiren+A+Neu+Sg+Indef`

**bratne** failures:

* `bratne+V+Inf` => `bratn`
* `bratne` has no analyses either

**snikfotografere** failures:

* `snikfotografere+V+Inf` => `snikfotografer`
* `snikfotografere` has no analyses either

**rappellere** failures:

* `rappellere+V+Inf` => `rappeller`
* `rappellere` has no analyses either

**mudre** failures:

* `mudre+V+Inf` => `mudr`
* `mudre` has no analyses either

**skatte** failures:

* `skatte+V+Inf` => `skatt`
* `skatte` has no analyses either

**interpolere** failures:

* `interpolere+V+Inf` => `interpoler`
* `interpolere` has no analyses either

**kolne** failures:

* `kolne+V+Inf` => `koln`
* `kolne` has no analyses either

**kuste** failures:

* `kuste+V+Inf` => `kust`
* `kuste` has no analyses either

**hengje** failures:

* `hengje+V+Inf` does not generate!
* `hengje` has no analyses either

**dosere** failures:

* `dosere+V+Inf` => `doser`
* `dosere` has no analyses either

**ryske** failures:

* `ryske+V+Inf` => `rysk`
* `ryske` has no analyses either

**kvingle** failures:

* `kvingle+V+Inf` => `kvingl`
* `kvingle` has no analyses either

**uvite** failures:

* `uvite+V+Inf` => `uvit`
* `uvite` has no analyses either

**eskalere** failures:

* `eskalere+V+Inf` => `eskaler`
* `eskalere` has no analyses either

**gruslegge** failures:

* `gruslegge+V+Inf` does not generate!
* `gruslegge` has no analyses either

**bråne** failures:

* `bråne+V+Inf` => `brån`
* `bråne` has no analyses either

**himle** failures:

* `himle+V+Inf` => `himl`
* `himle` has no analyses either

**fortvile** failures:

* `fortvile+V+Inf` does not generate!
* `fortvile` has no analyses either

**parallellforskuve** failures:

* `parallellforskuve+V+Inf` => `parallellforskuv`
* `parallellforskuve` has no analyses either

**skræne** failures:

* `skræne+V+Inf` => `skræn`
* `skræne` has no analyses either

**mishage** failures:

* `mishage+V+Inf` => `mishag`
* `mishage` has no analyses either

**gjøgle** failures:

* `gjøgle+V+Inf` => `gjøgl`
* `gjøgle` has no analyses either

**linjere** failures:

* `linjere+V+Inf` => `linjer`
* `linjere` has no analyses either

**magasinere** failures:

* `magasinere+V+Inf` => `magasiner`
* `magasinere` has no analyses either

**førebere** failures:

* `førebere+V+Inf` => `førebee`
* `førebere+V+Inf` => `førebea`
* `førebere` has no analyses either

**foryngje** failures:

* `foryngje+V+Inf` does not generate!
* `foryngje` has no analyses either

**persipere** failures:

* `persipere+V+Inf` => `persiper`
* `persipere` has no analyses either

**gryle** failures:

* `gryle+V+Inf` => `gryl`
* `gryle` has no analyses either

**krøple** failures:

* `krøple+V+Inf` => `krøpl`
* `krøple` has no analyses either

**rikse** failures:

* `rikse+V+Inf` => `riks`
* `rikse` has no analyses either

**penne** failures:

* `penne+V+Inf` => `penn`
* `penne` has no analyses either

**transponere** failures:

* `transponere+V+Inf` => `transponer`
* `transponere` has no analyses either

**rakle** failures:

* `rakle+V+Inf` => `rakl`
* `rakle` has no analyses either

**motivere** failures:

* `motivere+V+Inf` => `motiver`
* `motivere` has no analyses either

**nidoble** failures:

* `nidoble+V+Inf` => `nidobl`
* `nidoble` has no analyses either

**gugle** failures:

* `gugle+V+Inf` => `gugl`
* `gugle` has no analyses either

**dryle** failures:

* `dryle+V+Inf` => `dryl`
* `dryle` has no analyses either

**prefabrikkere** failures:

* `prefabrikkere+V+Inf` => `prefabrikker`
* `prefabrikkere` has no analyses either

**snære** failures:

* `snære+V+Inf` does not generate!
* `snære` has no analyses either

**titre** failures:

* `titre+V+Inf` => `titr`
* `titre` has no analyses either

**avklare** failures:

* `avklare+V+Inf` => `avklar`
* `avklare` has no analyses either

**grugge** failures:

* `grugge+V+Inf` => `grugg`
* `grugge` has no analyses either

**rydde** failures:

* `rydde+V+Inf` => `rydd`
* `rydde` has no analyses either

**klårleggje** failures:

* `klårleggje+V+Inf` does not generate!
* `klårleggje` has no analyses either

**puge** failures:

* `puge+V+Inf` => `pug`
* `puge` has no analyses either

**frøse** failures:

* `frøse+V+Inf` => `frøs`
* `frøse` has no analyses either

**ule** failures:

* `ule+V+Inf` does not generate!
* `ule` has following analyses:
  * `ul+A+Pl+Indef`
  * `ul+A+Sg+Def`
  * `ullen+A+Neu+Sg+Indef`

**ektevie** failures:

* `ektevie+V+Inf` does not generate!
* `ektevie` has no analyses either

**nyttiggjere** failures:

* `nyttiggjere+V+Inf` does not generate!
* `nyttiggjere` has no analyses either

**fumpe** failures:

* `fumpe+V+Inf` => `fump`
* `fumpe` has no analyses either

**hermetisere** failures:

* `hermetisere+V+Inf` => `hermetiser`
* `hermetisere` has no analyses either

**slurpe** failures:

* `slurpe+V+Inf` => `slurp`
* `slurpe` has no analyses either

**reise** failures:

* `reise+V+Inf` => `reis`
* `reise` has no analyses either

**synge** failures:

* `synge+V+Inf` => `sye`
* `synge+V+Inf` => `sya`
* `synge` has no analyses either

**plenarbehandle** failures:

* `plenarbehandle+V+Inf` => `plenarbehandl`
* `plenarbehandle` has no analyses either

**gjentake** failures:

* `gjentake+V+Inf` => `gjenttake`
* `gjentake+V+Inf` => `gjenttaka`
* `gjentake` has no analyses either

**innskrive** failures:

* `innskrive+V+Inf` => `innskrie`
* `innskrive+V+Inf` => `innskria`
* `innskrive` has no analyses either

**yngje** failures:

* `yngje+V+Inf` does not generate!
* `yngje` has no analyses either

**skrukne** failures:

* `skrukne+V+Inf` => `skrukn`
* `skrukne` has following analyses:
  * `skrukken+A+Pl+Indef`
  * `skrukken+A+Sg+Def`

**pilte** failures:

* `pilte+V+Inf` => `pilt`
* `pilte` has no analyses either

**trasere** failures:

* `trasere+V+Inf` => `traser`
* `trasere` has no analyses either

**fise** failures:

* `fise+V+Inf` => `fia`
* `fise+V+Inf` => `fie`
* `fise` has following analyses:
  * `fisen+A+Neu+Sg+Indef`

**bleike** failures:

* `bleike+V+Inf` => `bleik`
* `bleike` has following analyses:
  * `bleik+A+Pl+Indef`
  * `bleike+N+N+Fem+Sg+Indef`
  * `bleik+A+Sg+Def`

**utsondre** failures:

* `utsondre+V+Inf` => `utsondr`
* `utsondre` has no analyses either

**landdra** failures:

* `landdra+V+Inf` => `landdrdra`
* `landdra` has no analyses either

**slakke** failures:

* `slakke+V+Inf` => `slakk`
* `slakke` has no analyses either

**berte** failures:

* `berte+V+Inf` => `bert`
* `berte` has no analyses either

**raske** failures:

* `raske+V+Inf` => `rask`
* `raske` has following analyses:
  * `rask+A+A+Sg+Def`
  * `rask+A+A+Pl+Indef`
  * `rask+A+A+Pl+Def`

**bøykje** failures:

* `bøykje+V+Inf` does not generate!
* `bøykje` has no analyses either

**trøtne** failures:

* `trøtne+V+Inf` => `trøtn`
* `trøtne` has no analyses either

**auke** failures:

* `auke+V+Inf` => `auk`
* `auke` has no analyses either

**konvergere** failures:

* `konvergere+V+Inf` => `konverger`
* `konvergere` has no analyses either

**blotne** failures:

* `blotne+V+Inf` => `blotn`
* `blotne` has no analyses either

**sjage** failures:

* `sjage+V+Inf` => `sjag`
* `sjage` has no analyses either

**tole** failures:

* `tole+V+Inf` does not generate!
* `tole` has no analyses either

**fetere** failures:

* `fetere+V+Inf` => `feter`
* `fetere` has no analyses either

**knite** failures:

* `knite+V+Inf` => `knit`
* `knite` has no analyses either

**allmenngjere** failures:

* `allmenngjere+V+Inf` does not generate!
* `allmenngjere` has no analyses either

**totte** failures:

* `totte+V+Inf` => `tott`
* `totte` has no analyses either

**åhende** failures:

* `åhende+V+Inf` does not generate!
* `åhende` has no analyses either

**dune** failures:

* `dune+V+Inf` => `dun`
* `dune` has no analyses either

**gjeste** failures:

* `gjeste+V+Inf` => `gjest`
* `gjeste` has no analyses either

**bægje** failures:

* `bægje+V+Inf` does not generate!
* `bægje` has no analyses either

**avfeie** failures:

* `avfeie+V+Inf` => `avfei`
* `avfeie` has no analyses either

**anse** failures:

* `anse+V+Inf` => `ans`
* `anse` has no analyses either

**brigde** failures:

* `brigde+V+Inf` => `brigd`
* `brigde` has no analyses either

**tvagle** failures:

* `tvagle+V+Inf` => `tvagl`
* `tvagle` has no analyses either

**aksle** failures:

* `aksle+V+Inf` => `aksl`
* `aksle` has no analyses either

**rade** failures:

* `rade+V+Inf` => `rad`
* `rade` has following analyses:
  * `rad+A+Sg+Def`
  * `rad+A+Pl+Indef`

**amerikanisere** failures:

* `amerikanisere+V+Inf` => `amerikaniser`
* `amerikanisere` has no analyses either

**funke** failures:

* `funke+V+Inf` => `funk`
* `funke` has no analyses either

**idke** failures:

* `idke+V+Inf` => `idk`
* `idke` has no analyses either

**fjøre** failures:

* `fjøre+V+Inf` => `fjør`
* `fjøre` has no analyses either

**rejisere** failures:

* `rejisere+V+Inf` => `rejiser`
* `rejisere` has no analyses either

**ferdigbehandle** failures:

* `ferdigbehandle+V+Inf` => `ferdigbehandl`
* `ferdigbehandle` has no analyses either

**voke** failures:

* `voke+V+Inf` => `vok`
* `voke` has no analyses either

**restrukturere** failures:

* `restrukturere+V+Inf` => `restrukturer`
* `restrukturere` has no analyses either

**ættast** failures:

* `ættast+V+Inf` => `ættastast`
* `ættast` has no analyses either

**lemme** failures:

* `lemme+V+Inf` => `lem`
* `lemme` has no analyses either

**knekke** failures:

* `knekke+V+Inf` does not generate!
* `knekke` has no analyses either

**engasjere** failures:

* `engasjere+V+Inf` => `engasjer`
* `engasjere` has no analyses either

**kidnappe** failures:

* `kidnappe+V+Inf` => `kidnapp`
* `kidnappe` has no analyses either

**kondensere** failures:

* `kondensere+V+Inf` => `kondenser`
* `kondensere` has no analyses either

**påminne** failures:

* `påminne+V+Inf` => `påmine`
* `påminne+V+Inf` => `påmina`
* `påminne` has no analyses either

**hame** failures:

* `hame+V+Inf` => `ham`
* `hame` has no analyses either

**referere** failures:

* `referere+V+Inf` => `referer`
* `referere` has no analyses either

**bytne** failures:

* `bytne+V+Inf` => `bytn`
* `bytne` has no analyses either

**stilne** failures:

* `stilne+V+Inf` => `stiln`
* `stilne` has no analyses either

**slurke** failures:

* `slurke+V+Inf` => `slurk`
* `slurke` has no analyses either

**kokre** failures:

* `kokre+V+Inf` => `kokr`
* `kokre` has following analyses:
  * `kokren+A+Neu+Sg+Indef`

**pretendere** failures:

* `pretendere+V+Inf` => `pretender`
* `pretendere` has no analyses either

**siple** failures:

* `siple+V+Inf` => `sipl`
* `siple` has no analyses either

**helle** failures:

* `helle+V+Inf` does not generate!
* `helle` has no analyses either

**dalke** failures:

* `dalke+V+Inf` => `dalk`
* `dalke` has no analyses either

**vrante** failures:

* `vrante+V+Inf` => `vrant`
* `vrante` has no analyses either

**småne** failures:

* `småne+V+Inf` => `smån`
* `småne` has no analyses either

**filmatisere** failures:

* `filmatisere+V+Inf` => `filmatiser`
* `filmatisere` has no analyses either

**kukelure** failures:

* `kukelure+V+Inf` does not generate!
* `kukelure` has no analyses either

**feile** failures:

* `feile+V+Inf` does not generate!
* `feile` has following analyses:
  * `feil+A+A+Sg+Def`
  * `feil+A+A+Pl+Def`
  * `feil+A+A+Pl+Indef`

**tenkje** failures:

* `tenkje+V+Inf` does not generate!
* `tenkje` has no analyses either

**duellere** failures:

* `duellere+V+Inf` => `dueller`
* `duellere` has no analyses either

**lykkønskje** failures:

* `lykkønskje+V+Inf` does not generate!
* `lykkønskje` has no analyses either

**ruske** failures:

* `ruske+V+Inf` => `rusk`
* `ruske` has following analyses:
  * `rusk+A+A+Sg+Def`
  * `rusken+A+Neu+Sg+Indef`
  * `rusk+A+A+Pl+Def`
  * `rusk+A+A+Pl+Indef`

**lufttørke** failures:

* `lufttørke+V+Inf` => `lufttørk`
* `lufttørke` has no analyses either

**rekapitulere** failures:

* `rekapitulere+V+Inf` => `rekapituler`
* `rekapitulere` has no analyses either

**vrake** failures:

* `vrake+V+Inf` => `vrak`
* `vrake` has following analyses:
  * `vrak+A+A+Pl+Def`
  * `vrak+A+A+Sg+Def`
  * `vrak+A+A+Pl+Indef`

**trette** failures:

* `trette+V+Inf` does not generate!
* `trette` has no analyses either

**tenke** failures:

* `tenke+V+Inf` => `tenk`
* `tenke` has no analyses either

**objektivisere** failures:

* `objektivisere+V+Inf` => `objektiviser`
* `objektivisere` has no analyses either

**tetne** failures:

* `tetne+V+Inf` => `tetn`
* `tetne` has no analyses either

**lyge** failures:

* `lyge+V+Inf` => `lya`
* `lyge+V+Inf` => `lye`
* `lyge` has following analyses:
  * `lygen+A+Neu+Sg+Indef`

**glase** failures:

* `glase+V+Inf` => `glas`
* `glase` has no analyses either

**kjevle** failures:

* `kjevle+V+Inf` => `kjevl`
* `kjevle` has no analyses either

**vekte** failures:

* `vekte+V+Inf` => `vekt`
* `vekte` has no analyses either

**skjekkje** failures:

* `skjekkje+V+Inf` does not generate!
* `skjekkje` has no analyses either

**stemne** failures:

* `stemne+V+Inf` does not generate!
* `stemne` has no analyses either

**handhelse** failures:

* `handhelse+V+Inf` does not generate!
* `handhelse` has no analyses either

**formgi** failures:

* `formgi+V+Inf` => `formggi`
* `formgi` has no analyses either

**siregne** failures:

* `siregne+V+Inf` does not generate!
* `siregne` has no analyses either

**skuve** failures:

* `skuve+V+Inf` => `skuv`
* `skuve` has no analyses either

**gremjast** failures:

* `gremjast+V+Inf` => `grejast`
* `gremjast` has no analyses either

**flørte** failures:

* `flørte+V+Inf` => `flørt`
* `flørte` has no analyses either

**platte** failures:

* `platte+V+Inf` => `platt`
* `platte` has following analyses:
  * `platt+A+A+Pl+Indef`
  * `platt+A+A+Sg+Def`
  * `platt+A+A+Pl+Def`

**plombere** failures:

* `plombere+V+Inf` => `plomber`
* `plombere` has no analyses either

**postulere** failures:

* `postulere+V+Inf` => `postuler`
* `postulere` has no analyses either

**skinne** failures:

* `skinne+V+Inf` => `skinn`
* `skinne` has no analyses either

**utslette** failures:

* `utslette+V+Inf` => `utslett`
* `utslette` has no analyses either

**ragle** failures:

* `ragle+V+Inf` => `ragl`
* `ragle` has no analyses either

**snuppe** failures:

* `snuppe+V+Inf` => `snupp`
* `snuppe` has no analyses either

**vederfarast** failures:

* `vederfarast+V+Inf` => `vederfaast`
* `vederfarast` has no analyses either

**mote** failures:

* `mote+V+Inf` => `mot`
* `mote` has no analyses either

**kjederøyke** failures:

* `kjederøyke+V+Inf` => `kjederøyk`
* `kjederøyke` has no analyses either

**formaste** failures:

* `formaste+V+Inf` => `formast`
* `formaste` has no analyses either

**klinkbyggje** failures:

* `klinkbyggje+V+Inf` does not generate!
* `klinkbyggje` has no analyses either

**meiske** failures:

* `meiske+V+Inf` => `meisk`
* `meiske` has no analyses either

**storregne** failures:

* `storregne+V+Inf` does not generate!
* `storregne` has no analyses either

**utarte** failures:

* `utarte+V+Inf` => `utart`
* `utarte` has no analyses either

**skamby** failures:

* `skamby+V+Inf` does not generate!
* `skamby` has no analyses either

**maskinskrive** failures:

* `maskinskrive+V+Inf` => `maskinskria`
* `maskinskrive+V+Inf` => `maskinskrie`
* `maskinskrive` has no analyses either

**darre** failures:

* `darre+V+Inf` => `darr`
* `darre` has no analyses either

**faste** failures:

* `faste+V+Inf` => `fast`
* `faste` has following analyses:
  * `fast+A+A+Pl+Def`
  * `fast+A+A+Sg+Def`
  * `fast+A+A+Pl+Indef`

**seiast** failures:

* `seiast+V+Inf` => `sseiast`
* `seiast` has following analyses:
  * `seie+V+Inf+Pass`

**dekatere** failures:

* `dekatere+V+Inf` => `dekater`
* `dekatere` has no analyses either

**medgi** failures:

* `medgi+V+Inf` => `medggi`
* `medgi` has no analyses either

**såre** failures:

* `såre+V+Inf` => `sår`
* `såre` has following analyses:
  * `sår+A+Pl+Indef`
  * `sår+A+Sg+Def`

**palmere** failures:

* `palmere+V+Inf` => `palmer`
* `palmere` has no analyses either

**mæte** failures:

* `mæte+V+Inf` => `mæt`
* `mæte` has following analyses:
  * `mæt+A+Sg+Def`
  * `mæt+A+Pl+Indef`

**hime** failures:

* `hime+V+Inf` => `him`
* `hime` has no analyses either

**trykke** failures:

* `trykke+V+Inf` does not generate!
* `trykke` has no analyses either

**nytte** failures:

* `nytte+V+Inf` => `nytt`
* `nytte` has no analyses either

**balere** failures:

* `balere+V+Inf` => `baler`
* `balere` has no analyses either

**rettferdiggjere** failures:

* `rettferdiggjere+V+Inf` does not generate!
* `rettferdiggjere` has no analyses either

**tentere** failures:

* `tentere+V+Inf` => `tenter`
* `tentere` has no analyses either

**skalle** failures:

* `skalle+V+Inf` => `skall`
* `skalle` has no analyses either

**overtyde** failures:

* `overtyde+V+Inf` does not generate!
* `overtyde` has no analyses either

**implantere** failures:

* `implantere+V+Inf` => `implanter`
* `implantere` has no analyses either

**komme** failures:

* `komme+V+Inf` => `kkomma`
* `komme+V+Inf` => `kkomme`
* `komme` has no analyses either

**gjennomsøkje** failures:

* `gjennomsøkje+V+Inf` does not generate!
* `gjennomsøkje` has no analyses either

**reive** failures:

* `reive+V+Inf` => `reiv`
* `reive` has no analyses either

**utspionere** failures:

* `utspionere+V+Inf` => `utspioner`
* `utspionere` has no analyses either

**dyngje** failures:

* `dyngje+V+Inf` does not generate!
* `dyngje` has no analyses either

**hålkast** failures:

* `hålkast+V+Inf` => `hålkastast`
* `hålkast` has no analyses either

**duge** failures:

* `duge+V+Inf` does not generate!
* `duge` has no analyses either

**sprøytelakkere** failures:

* `sprøytelakkere+V+Inf` => `sprøytelakker`
* `sprøytelakkere` has no analyses either

**etterkomme** failures:

* `etterkomme+V+Inf` => `etterkkomme`
* `etterkomme+V+Inf` => `etterkkomma`
* `etterkomme` has no analyses either

**saktne** failures:

* `saktne+V+Inf` => `saktn`
* `saktne` has no analyses either

**stifte** failures:

* `stifte+V+Inf` => `stift`
* `stifte` has no analyses either

**sosialisere** failures:

* `sosialisere+V+Inf` => `sosialiser`
* `sosialisere` has no analyses either

**stòte** failures:

* `stòte+V+Inf` => `stòt`
* `stòte` has no analyses either

**tvangsflytte** failures:

* `tvangsflytte+V+Inf` does not generate!
* `tvangsflytte` has no analyses either

**kyrke** failures:

* `kyrke+V+Inf` => `kyrk`
* `kyrke` has no analyses either

**sleggje** failures:

* `sleggje+V+Inf` => `slegg`
* `sleggje` has no analyses either

**rustne** failures:

* `rustne+V+Inf` => `rustn`
* `rustne` has following analyses:
  * `rusten+A+Pl+Indef`
  * `rusten+A+Sg+Def`

**hisse** failures:

* `hisse+V+Inf` => `hiss`
* `hisse` has no analyses either

**lorgnettere** failures:

* `lorgnettere+V+Inf` => `lorgnetter`
* `lorgnettere` has no analyses either

**blote** failures:

* `blote+V+Inf` => `blot`
* `blote` has no analyses either

**fjelgne** failures:

* `fjelgne+V+Inf` => `fjelgn`
* `fjelgne` has no analyses either

**muldre** failures:

* `muldre+V+Inf` => `muldr`
* `muldre` has no analyses either

**prunke** failures:

* `prunke+V+Inf` => `prunk`
* `prunke` has no analyses either

**slurve** failures:

* `slurve+V+Inf` => `slurv`
* `slurve` has no analyses either

**skjekle** failures:

* `skjekle+V+Inf` => `skjekl`
* `skjekle` has no analyses either

**lansere** failures:

* `lansere+V+Inf` => `lanser`
* `lansere` has no analyses either

**flike** failures:

* `flike+V+Inf` => `flik`
* `flike` has no analyses either

**andøve** failures:

* `andøve+V+Inf` does not generate!
* `andøve` has no analyses either

**sikle** failures:

* `sikle+V+Inf` => `sikl`
* `sikle` has following analyses:
  * `siklen+A+Neu+Sg+Indef`

**knipse** failures:

* `knipse+V+Inf` => `knips`
* `knipse` has no analyses either

**matstelle** failures:

* `matstelle+V+Inf` does not generate!
* `matstelle` has no analyses either

**økonomisere** failures:

* `økonomisere+V+Inf` => `økonomiser`
* `økonomisere` has no analyses either

**deppe** failures:

* `deppe+V+Inf` => `depp`
* `deppe` has no analyses either

**lene** failures:

* `lene+V+Inf` does not generate!
* `lene` has following analyses:
  * `lus+N+N+Fem+Pl+Def`
  * `lese+V+Adj+Fl`
  * `lese+V+Adj+Bu+Eint`

**fjolle** failures:

* `fjolle+V+Inf` => `fjoll`
* `fjolle` has no analyses either

**gamble** failures:

* `gamble+V+Inf` => `gambl`
* `gamble` has no analyses either

**naturalisere** failures:

* `naturalisere+V+Inf` => `naturaliser`
* `naturalisere` has no analyses either

**sparkle** failures:

* `sparkle+V+Inf` => `sparkl`
* `sparkle` has no analyses either

**slenge** failures:

* `slenge+V+Inf` => `sl`
* `slenge` has following analyses:
  * `slengen+A+Neu+Sg+Indef`

**svane** failures:

* `svane+V+Inf` => `svan`
* `svane` has no analyses either

**tårast** failures:

* `tårast+V+Inf` => `tårastast`
* `tårast` has no analyses either

**motsetje** failures:

* `motsetje+V+Inf` does not generate!
* `motsetje` has no analyses either

**hardtrampe** failures:

* `hardtrampe+V+Inf` => `hardtramp`
* `hardtrampe` has no analyses either

**eksportere** failures:

* `eksportere+V+Inf` => `eksporter`
* `eksportere` has no analyses either

**gipse** failures:

* `gipse+V+Inf` => `gips`
* `gipse` has no analyses either

**rekognosere** failures:

* `rekognosere+V+Inf` => `rekognoser`
* `rekognosere` has no analyses either

**baisse** failures:

* `baisse+V+Inf` => `baiss`
* `baisse` has no analyses either

**lukkynske** failures:

* `lukkynske+V+Inf` => `lukkynsk`
* `lukkynske` has no analyses either

**slote** failures:

* `slote+V+Inf` => `slot`
* `slote` has no analyses either

**dandere** failures:

* `dandere+V+Inf` => `dander`
* `dandere` has no analyses either

**skoggerle** failures:

* `skoggerle+V+Inf` => `skoggerlle`
* `skoggerle` has no analyses either

**braute** failures:

* `braute+V+Inf` => `braut`
* `braute` has no analyses either

**knippe** failures:

* `knippe+V+Inf` => `knipp`
* `knippe` has no analyses either

**senke** failures:

* `senke+V+Inf` => `senk`
* `senke` has no analyses either

**raste** failures:

* `raste+V+Inf` => `rast`
* `raste` has no analyses either

**stønne** failures:

* `stønne+V+Inf` does not generate!
* `stønne` has no analyses either

**beisle** failures:

* `beisle+V+Inf` => `beisl`
* `beisle` has no analyses either

**barne** failures:

* `barne+V+Inf` => `barn`
* `barne` has no analyses either

**botnfelle** failures:

* `botnfelle+V+Inf` does not generate!
* `botnfelle` has no analyses either

**optimere** failures:

* `optimere+V+Inf` => `optimer`
* `optimere` has no analyses either

**skravle** failures:

* `skravle+V+Inf` => `skravl`
* `skravle` has no analyses either

**ankre** failures:

* `ankre+V+Inf` => `ankr`
* `ankre` has no analyses either

**avlytte** failures:

* `avlytte+V+Inf` => `avlytt`
* `avlytte` has no analyses either

**oppfatte** failures:

* `oppfatte+V+Inf` => `oppfatt`
* `oppfatte` has no analyses either

**audmykje** failures:

* `audmykje+V+Inf` does not generate!
* `audmykje` has no analyses either

**kristne** failures:

* `kristne+V+Inf` => `kristn`
* `kristne` has following analyses:
  * `kristen+A+Sg+Def`
  * `kristen+A+Pl+Indef`

**skrumle** failures:

* `skrumle+V+Inf` => `skruml`
* `skrumle` has no analyses either

**antre** failures:

* `antre+V+Inf` => `antr`
* `antre` has no analyses either

**rodne** failures:

* `rodne+V+Inf` => `rodn`
* `rodne` has no analyses either

**krøke** failures:

* `krøke+V+Inf` => `krøk`
* `krøke` has no analyses either

**rømmast** failures:

* `rømmast+V+Inf` => `rømast`
* `rømmast` has no analyses either

**transfundere** failures:

* `transfundere+V+Inf` => `transfunder`
* `transfundere` has no analyses either

**skamraske** failures:

* `skamraske+V+Inf` => `skamrask`
* `skamraske` has no analyses either

**godsvalle** failures:

* `godsvalle+V+Inf` => `godsvall`
* `godsvalle` has no analyses either

**ilskne** failures:

* `ilskne+V+Inf` => `ilskn`
* `ilskne` has following analyses:
  * `ilsken+A+Pl+Indef`
  * `ilsken+A+Sg+Def`

**blive** failures:

* `blive+V+Inf` => `blia`
* `blive+V+Inf` => `blie`
* `blive` has no analyses either

**treske** failures:

* `treske+V+Inf` => `tresk`
* `treske` has no analyses either

**dåfurdast** failures:

* `dåfurdast+V+Inf` => `dåfurdastast`
* `dåfurdast` has no analyses either

**mosjonere** failures:

* `mosjonere+V+Inf` => `mosjoner`
* `mosjonere` has no analyses either

**forpeste** failures:

* `forpeste+V+Inf` => `forpest`
* `forpeste` has no analyses either

**pinse** failures:

* `pinse+V+Inf` => `pins`
* `pinse` has no analyses either

**reindyrke** failures:

* `reindyrke+V+Inf` => `reindyrk`
* `reindyrke` has no analyses either

**tyne** failures:

* `tyne+V+Inf` => `tyn`
* `tyne` has following analyses:
  * `tynn+A+A+Pl+Def`
  * `tynn+A+A+Pl+Indef`
  * `tynn+A+A+Sg+Def`

**oversymje** failures:

* `oversymje+V+Inf` => `oversyja`
* `oversymje+V+Inf` => `oversyje`
* `oversymje` has no analyses either

**avfolke** failures:

* `avfolke+V+Inf` => `avfolk`
* `avfolke` has no analyses either

**annonsere** failures:

* `annonsere+V+Inf` => `annonser`
* `annonsere` has no analyses either

**tørrtenkje** failures:

* `tørrtenkje+V+Inf` does not generate!
* `tørrtenkje` has no analyses either

**stunte** failures:

* `stunte+V+Inf` => `stunt`
* `stunte` has no analyses either

**tvidoble** failures:

* `tvidoble+V+Inf` => `tvidobl`
* `tvidoble` has no analyses either

**furde** failures:

* `furde+V+Inf` => `furd`
* `furde` has no analyses either

**sjangle** failures:

* `sjangle+V+Inf` => `sjangl`
* `sjangle` has no analyses either

**drøle** failures:

* `drøle+V+Inf` => `drøl`
* `drøle` has no analyses either

**love** failures:

* `love+V+Inf` => `lov`
* `love` has following analyses:
  * `love+N+N+Sg`

**fossilisere** failures:

* `fossilisere+V+Inf` => `fossiliser`
* `fossilisere` has no analyses either

**påskjøne** failures:

* `påskjøne+V+Inf` does not generate!
* `påskjøne` has no analyses either

**undervurdere** failures:

* `undervurdere+V+Inf` => `undervurder`
* `undervurdere` has no analyses either

**etterfylgje** failures:

* `etterfylgje+V+Inf` does not generate!
* `etterfylgje` has no analyses either

**kube** failures:

* `kube+V+Inf` => `kub`
* `kube` has following analyses:
  * `kubben+A+Neu+Sg+Indef`

**vekse** failures:

* `vekse+V+Inf` => `vea`
* `vekse+V+Inf` => `vee`
* `vekse` has no analyses either

**smitte** failures:

* `smitte+V+Inf` => `smitt`
* `smitte` has no analyses either

**desentralisere** failures:

* `desentralisere+V+Inf` => `desentraliser`
* `desentralisere` has no analyses either

**smågråte** failures:

* `smågråte+V+Inf` => `smågråe`
* `smågråte+V+Inf` => `smågråa`
* `smågråte` has no analyses either

**turrleggje** failures:

* `turrleggje+V+Inf` does not generate!
* `turrleggje` has no analyses either

**våle** failures:

* `våle+V+Inf` => `vål`
* `våle` has no analyses either

**akkordere** failures:

* `akkordere+V+Inf` => `akkorder`
* `akkordere` has no analyses either

**stuke** failures:

* `stuke+V+Inf` => `stuk`
* `stuke` has no analyses either

**ormekte** failures:

* `ormekte+V+Inf` => `ormekt`
* `ormekte` has no analyses either

**glikke** failures:

* `glikke+V+Inf` => `glikk`
* `glikke` has no analyses either

**perse** failures:

* `perse+V+Inf` => `pers`
* `perse` has no analyses either

**ramse** failures:

* `ramse+V+Inf` => `rams`
* `ramse` has no analyses either

**ekspirere** failures:

* `ekspirere+V+Inf` => `ekspirer`
* `ekspirere` has no analyses either

**tjuvskyte** failures:

* `tjuvskyte+V+Inf` => `tjuvskye`
* `tjuvskyte+V+Inf` => `tjuvskya`
* `tjuvskyte` has no analyses either

**sylvleggje** failures:

* `sylvleggje+V+Inf` does not generate!
* `sylvleggje` has no analyses either

**urydje** failures:

* `urydje+V+Inf` does not generate!
* `urydje` has no analyses either

**vigilere** failures:

* `vigilere+V+Inf` => `vigiler`
* `vigilere` has no analyses either

**lyre** failures:

* `lyre+V+Inf` => `lyr`
* `lyre` has no analyses either

**misse** failures:

* `misse+V+Inf` => `mis`
* `misse` has no analyses either

**gniste** failures:

* `gniste+V+Inf` => `gnist`
* `gniste` has no analyses either

**deregulere** failures:

* `deregulere+V+Inf` => `dereguler`
* `deregulere` has no analyses either

**staure** failures:

* `staure+V+Inf` => `staur`
* `staure` has no analyses either

**stote** failures:

* `stote+V+Inf` => `stot`
* `stote` has no analyses either

**tulte** failures:

* `tulte+V+Inf` => `tult`
* `tulte` has no analyses either

**kabbe** failures:

* `kabbe+V+Inf` => `kabb`
* `kabbe` has no analyses either

**samkøyre** failures:

* `samkøyre+V+Inf` does not generate!
* `samkøyre` has no analyses either

**sirre** failures:

* `sirre+V+Inf` => `sirr`
* `sirre` has no analyses either

**tevle** failures:

* `tevle+V+Inf` => `tevl`
* `tevle` has no analyses either

**katekisere** failures:

* `katekisere+V+Inf` => `katekiser`
* `katekisere` has no analyses either

**stryke** failures:

* `stryke+V+Inf` => `strya`
* `stryke+V+Inf` => `strye`
* `stryke` has no analyses either

**forgåast** failures:

* `forgåast+V+Inf` => `forggåast`
* `forgåast` has following analyses:
  * `forgå+V+Inf+Pass`

**oppsummere** failures:

* `oppsummere+V+Inf` => `oppsummer`
* `oppsummere` has no analyses either

**øyre** failures:

* `øyre+V+Inf` does not generate!
* `øyre` has no analyses either

**strutte** failures:

* `strutte+V+Inf` => `strutt`
* `strutte` has no analyses either

**rispe** failures:

* `rispe+V+Inf` => `risp`
* `rispe` has no analyses either

**avlage** failures:

* `avlage+V+Inf` => `avlag`
* `avlage` has no analyses either

**labialisere** failures:

* `labialisere+V+Inf` => `labialiser`
* `labialisere` has no analyses either

**tikke** failures:

* `tikke+V+Inf` => `tikk`
* `tikke` has no analyses either

**briljere** failures:

* `briljere+V+Inf` => `briljer`
* `briljere` has no analyses either

**geigre** failures:

* `geigre+V+Inf` => `geigr`
* `geigre` has no analyses either

**danne** failures:

* `danne+V+Inf` => `dann`
* `danne` has following analyses:
  * `danne+A+A+Neu+Sg+Indef`
  * `danne+A+A+Fem+Sg+Indef`
  * `danne+A+A+Msc+Sg+Indef`

**økte** failures:

* `økte+V+Inf` => `økt`
* `økte` has no analyses either

**datomerkje** failures:

* `datomerkje+V+Inf` does not generate!
* `datomerkje` has no analyses either

**kontere** failures:

* `kontere+V+Inf` => `konter`
* `kontere` has no analyses either

**køye** failures:

* `køye+V+Inf` => `køy`
* `køye` has no analyses either

**bunge** failures:

* `bunge+V+Inf` => `bung`
* `bunge` has no analyses either

**sekularisere** failures:

* `sekularisere+V+Inf` => `sekulariser`
* `sekularisere` has no analyses either

**grovreinse** failures:

* `grovreinse+V+Inf` => `grovreins`
* `grovreinse` has no analyses either

**implementere** failures:

* `implementere+V+Inf` => `implementer`
* `implementere` has no analyses either

**jadre** failures:

* `jadre+V+Inf` => `jadr`
* `jadre` has no analyses either

**snurpe** failures:

* `snurpe+V+Inf` => `snurp`
* `snurpe` has following analyses:
  * `snurp+A+Sg+Def`
  * `snurp+A+Pl+Indef`

**sytre** failures:

* `sytre+V+Inf` => `sytr`
* `sytre` has no analyses either

**snappe** failures:

* `snappe+V+Inf` => `snapp`
* `snappe` has no analyses either

**pòte** failures:

* `pòte+V+Inf` => `pòt`
* `pòte` has no analyses either

**mobilisere** failures:

* `mobilisere+V+Inf` => `mobiliser`
* `mobilisere` has no analyses either

**rekle** failures:

* `rekle+V+Inf` => `rekl`
* `rekle` has no analyses either

**strype** failures:

* `strype+V+Inf` => `stryp`
* `strype` has no analyses either

**maule** failures:

* `maule+V+Inf` => `maul`
* `maule` has no analyses either

**desinfisere** failures:

* `desinfisere+V+Inf` => `desinfiser`
* `desinfisere` has no analyses either

**universalisere** failures:

* `universalisere+V+Inf` => `universaliser`
* `universalisere` has no analyses either

**heimfalle** failures:

* `heimfalle+V+Inf` => `heimfae`
* `heimfalle+V+Inf` => `heimfaa`
* `heimfalle` has no analyses either

**plukke** failures:

* `plukke+V+Inf` => `plukk`
* `plukke` has no analyses either

**vipse** failures:

* `vipse+V+Inf` => `vips`
* `vipse` has no analyses either

**ynskje** failures:

* `ynskje+V+Inf` does not generate!
* `ynskje` has no analyses either

**oversjå** failures:

* `oversjå+V+Inf` => `overssjå`
* `oversjå` has no analyses either

**berekne** failures:

* `berekne+V+Inf` => `berekn`
* `berekne` has no analyses either

**drypptørke** failures:

* `drypptørke+V+Inf` => `drypptørk`
* `drypptørke` has no analyses either

**båse** failures:

* `båse+V+Inf` => `bås`
* `båse` has no analyses either

**ordleggje** failures:

* `ordleggje+V+Inf` does not generate!
* `ordleggje` has no analyses either

**feste** failures:

* `feste+V+Inf` => `fest`
* `feste` has no analyses either

**pante** failures:

* `pante+V+Inf` => `pant`
* `pante` has no analyses either

**svitsje** failures:

* `svitsje+V+Inf` => `svitsj`
* `svitsje` has no analyses either

**baldre** failures:

* `baldre+V+Inf` => `baldr`
* `baldre` has no analyses either

**eime** failures:

* `eime+V+Inf` => `eim`
* `eime` has following analyses:
  * `eim+A+Pl+Indef`
  * `eim+A+Sg+Def`

**betene** failures:

* `betene+V+Inf` => `beten`
* `betene` has following analyses:
  * `bete+N+N+Fem+Pl+Def`

**integrere** failures:

* `integrere+V+Inf` => `integrer`
* `integrere` has no analyses either

**anskaffe** failures:

* `anskaffe+V+Inf` => `anskaff`
* `anskaffe` has no analyses either

**leggje** failures:

* `leggje+V+Inf` does not generate!
* `leggje` has no analyses either

**gnubbe** failures:

* `gnubbe+V+Inf` => `gnubb`
* `gnubbe` has no analyses either

**skjaltre** failures:

* `skjaltre+V+Inf` => `skjaltr`
* `skjaltre` has no analyses either

**balansere** failures:

* `balansere+V+Inf` => `balanser`
* `balansere` has no analyses either

**nase** failures:

* `nase+V+Inf` => `nas`
* `nase` has no analyses either

**håne** failures:

* `håne+V+Inf` => `hån`
* `håne` has no analyses either

**tolerere** failures:

* `tolerere+V+Inf` => `tolerer`
* `tolerere` has no analyses either

**klaske** failures:

* `klaske+V+Inf` => `klask`
* `klaske` has no analyses either

**gryne** failures:

* `gryne+V+Inf` => `gryn`
* `gryne` has no analyses either

**avhovde** failures:

* `avhovde+V+Inf` => `avhovd`
* `avhovde` has no analyses either

**kippe** failures:

* `kippe+V+Inf` does not generate!
* `kippe` has no analyses either

**vassbeise** failures:

* `vassbeise+V+Inf` => `vassbeis`
* `vassbeise` has no analyses either

**regjere** failures:

* `regjere+V+Inf` => `regjer`
* `regjere` has no analyses either

**rasle** failures:

* `rasle+V+Inf` => `rasl`
* `rasle` has no analyses either

**folkesette** failures:

* `folkesette+V+Inf` does not generate!
* `folkesette` has no analyses either

**retardere** failures:

* `retardere+V+Inf` => `retarder`
* `retardere` has no analyses either

**desavuere** failures:

* `desavuere+V+Inf` => `desavuer`
* `desavuere` has no analyses either

**rolse** failures:

* `rolse+V+Inf` => `rols`
* `rolse` has no analyses either

**putle** failures:

* `putle+V+Inf` => `putl`
* `putle` has no analyses either

**rave** failures:

* `rave+V+Inf` => `rav`
* `rave` has no analyses either

**fortrykkje** failures:

* `fortrykkje+V+Inf` does not generate!
* `fortrykkje` has no analyses either

**stolpre** failures:

* `stolpre+V+Inf` => `stolpr`
* `stolpre` has no analyses either

**klemte** failures:

* `klemte+V+Inf` => `klemt`
* `klemte` has no analyses either

**vanke** failures:

* `vanke+V+Inf` => `vank`
* `vanke` has following analyses:
  * `vank+A+Sg+Def`
  * `vank+A+Pl+Indef`

**korskaste** failures:

* `korskaste+V+Inf` => `korskast`
* `korskaste` has no analyses either

**torturere** failures:

* `torturere+V+Inf` => `torturer`
* `torturere` has no analyses either

**veksle** failures:

* `veksle+V+Inf` => `veksl`
* `veksle` has no analyses either

**vaie** failures:

* `vaie+V+Inf` => `vai`
* `vaie` has no analyses either

**yste** failures:

* `yste+V+Inf` does not generate!
* `yste` has no analyses either

**forskrekke** failures:

* `forskrekke+V+Inf` => `forskrekk`
* `forskrekke` has no analyses either

**fote** failures:

* `fote+V+Inf` => `fot`
* `fote` has no analyses either

**kartlegge** failures:

* `kartlegge+V+Inf` does not generate!
* `kartlegge` has no analyses either

**overleve** failures:

* `overleve+V+Inf` does not generate!
* `overleve` has no analyses either

**duve** failures:

* `duve+V+Inf` => `duv`
* `duve` has no analyses either

**forlove** failures:

* `forlove+V+Inf` => `forlov`
* `forlove` has no analyses either

**skjerme** failures:

* `skjerme+V+Inf` => `skjerm`
* `skjerme` has no analyses either

**tilbakevise** failures:

* `tilbakevise+V+Inf` => `tilbakevis`
* `tilbakevise` has no analyses either

**dilte** failures:

* `dilte+V+Inf` => `dilt`
* `dilte` has no analyses either

**flåse** failures:

* `flåse+V+Inf` => `flås`
* `flåse` has no analyses either

**fylje** failures:

* `fylje+V+Inf` => `fylj`
* `fylje` has no analyses either

**jubilere** failures:

* `jubilere+V+Inf` => `jubiler`
* `jubilere` has no analyses either

**søgje** failures:

* `søgje+V+Inf` does not generate!
* `søgje` has no analyses either

**forureine** failures:

* `forureine+V+Inf` => `forurein`
* `forureine` has no analyses either

**skjale** failures:

* `skjale+V+Inf` => `skjal`
* `skjale` has no analyses either

**tyde** failures:

* `tyde+V+Inf` does not generate!
* `tyde` has following analyses:
  * `tyd+A+Pl+Indef`
  * `tyd+A+Sg+Def`

**forsterke** failures:

* `forsterke+V+Inf` => `forsterk`
* `forsterke` has no analyses either

**panele** failures:

* `panele+V+Inf` => `panel`
* `panele` has no analyses either

**gurpe** failures:

* `gurpe+V+Inf` => `gurp`
* `gurpe` has no analyses either

**holast** failures:

* `holast+V+Inf` => `holastast`
* `holast` has following analyses:
  * `hol+A+Superl+Indef`

**profilere** failures:

* `profilere+V+Inf` => `profiler`
* `profilere` has no analyses either

**rundkysse** failures:

* `rundkysse+V+Inf` does not generate!
* `rundkysse` has no analyses either

**hafse** failures:

* `hafse+V+Inf` => `hafs`
* `hafse` has no analyses either

**vanstelle** failures:

* `vanstelle+V+Inf` does not generate!
* `vanstelle` has no analyses either

**indikere** failures:

* `indikere+V+Inf` => `indiker`
* `indikere` has no analyses either

**huglegge** failures:

* `huglegge+V+Inf` does not generate!
* `huglegge` has no analyses either

**lire** failures:

* `lire+V+Inf` does not generate!
* `lire` has no analyses either

**rifle** failures:

* `rifle+V+Inf` => `rifl`
* `rifle` has no analyses either

**handgi** failures:

* `handgi+V+Inf` => `handggi`
* `handgi` has no analyses either

**stipulere** failures:

* `stipulere+V+Inf` => `stipuler`
* `stipulere` has no analyses either

**brekke** failures:

* `brekke+V+Inf` does not generate!
* `brekke` has no analyses either

**trongsprøve** failures:

* `trongsprøve+V+Inf` does not generate!
* `trongsprøve` has no analyses either

**turrbanke** failures:

* `turrbanke+V+Inf` => `turrbank`
* `turrbanke` has no analyses either

**overmanne** failures:

* `overmanne+V+Inf` => `overmann`
* `overmanne` has no analyses either

**forarme** failures:

* `forarme+V+Inf` => `forarm`
* `forarme` has no analyses either

**tinde** failures:

* `tinde+V+Inf` => `tind`
* `tinde` has no analyses either

**trumpe** failures:

* `trumpe+V+Inf` => `trump`
* `trumpe` has no analyses either

**temje** failures:

* `temje+V+Inf` does not generate!
* `temje` has no analyses either

**kanalisere** failures:

* `kanalisere+V+Inf` => `kanaliser`
* `kanalisere` has no analyses either

**vedkome** failures:

* `vedkome+V+Inf` => `vedkkome`
* `vedkome+V+Inf` => `vedkkoma`
* `vedkome` has no analyses either

**gjempe** failures:

* `gjempe+V+Inf` => `gjemp`
* `gjempe` has no analyses either

**snåle** failures:

* `snåle+V+Inf` => `snål`
* `snåle` has following analyses:
  * `snål+A+A+Sg+Def`
  * `snål+A+A+Pl+Indef`
  * `snål+A+A+Pl+Def`

**beskytte** failures:

* `beskytte+V+Inf` => `beskytt`
* `beskytte` has no analyses either

**nusse** failures:

* `nusse+V+Inf` => `nuss`
* `nusse` has no analyses either

**flyge** failures:

* `flyge+V+Inf` => `flye`
* `flyge+V+Inf` => `flya`
* `flyge` has no analyses either

**vage** failures:

* `vage+V+Inf` => `vag`
* `vage` has following analyses:
  * `vag+A+Pl+Indef`
  * `vag+A+Sg+Def`

**leike** failures:

* `leike+V+Inf` does not generate!
* `leike` has no analyses either

**grovsikte** failures:

* `grovsikte+V+Inf` => `grovsikt`
* `grovsikte` has no analyses either

**forsone** failures:

* `forsone+V+Inf` => `forson`
* `forsone` has following analyses:
  * `forsove+V+Adj+Bu+Eint`
  * `forsove+V+Adj+Fl`

**representere** failures:

* `representere+V+Inf` => `representer`
* `representere` has no analyses either

**forvende** failures:

* `forvende+V+Inf` does not generate!
* `forvende` has no analyses either

**frygde** failures:

* `frygde+V+Inf` => `frygd`
* `frygde` has no analyses either

**torve** failures:

* `torve+V+Inf` => `torv`
* `torve` has no analyses either

**protokollere** failures:

* `protokollere+V+Inf` => `protokoller`
* `protokollere` has no analyses either

**adsorbere** failures:

* `adsorbere+V+Inf` => `adsorber`
* `adsorbere` has no analyses either

**korne** failures:

* `korne+V+Inf` => `korn`
* `korne` has no analyses either

**ordne** failures:

* `ordne+V+Inf` => `ordn`
* `ordne` has no analyses either

**tilbede** failures:

* `tilbede+V+Inf` => `tilbea`
* `tilbede+V+Inf` => `tilbee`
* `tilbede` has following analyses:
  * `tilbe+V+PrfPrc`

**begeistre** failures:

* `begeistre+V+Inf` => `begeistr`
* `begeistre` has no analyses either

**samansverje** failures:

* `samansverje+V+Inf` => `samansveje`
* `samansverje+V+Inf` => `samansveja`
* `samansverje` has no analyses either

**vatre** failures:

* `vatre+V+Inf` => `vatr`
* `vatre` has no analyses either

**maksimalisere** failures:

* `maksimalisere+V+Inf` => `maksimaliser`
* `maksimalisere` has no analyses either

**regulere** failures:

* `regulere+V+Inf` => `reguler`
* `regulere` has no analyses either

**glyre** failures:

* `glyre+V+Inf` does not generate!
* `glyre` has no analyses either

**clinche** failures:

* `clinche+V+Inf` => `clinch`
* `clinche` has no analyses either

**fordunste** failures:

* `fordunste+V+Inf` => `fordunst`
* `fordunste` has no analyses either

**rangle** failures:

* `rangle+V+Inf` => `rangl`
* `rangle` has no analyses either

**småminke** failures:

* `småminke+V+Inf` => `småmink`
* `småminke` has no analyses either

**til** failures:

* `til+V+Inf` does not generate!
* `til` has following analyses:
  * `til+Pr`
  * `tile+N`

**palpere** failures:

* `palpere+V+Inf` => `palper`
* `palpere` has no analyses either

**sprøsteike** failures:

* `sprøsteike+V+Inf` => `sprøsteik`
* `sprøsteike` has no analyses either

**dibbe** failures:

* `dibbe+V+Inf` does not generate!
* `dibbe` has no analyses either

**legitimere** failures:

* `legitimere+V+Inf` => `legitimer`
* `legitimere` has no analyses either

**nubbe** failures:

* `nubbe+V+Inf` => `nubb`
* `nubbe` has no analyses either

**kurtisere** failures:

* `kurtisere+V+Inf` => `kurtiser`
* `kurtisere` has no analyses either

**lenkje** failures:

* `lenkje+V+Inf` => `lenk`
* `lenkje` has no analyses either

**kalle** failures:

* `kalle+V+Inf` => `kall`
* `kalle` has no analyses either

**veike** failures:

* `veike+V+Inf` => `veik`
* `veike` has following analyses:
  * `veik+A+Pl+Indef`
  * `veik+A+Sg+Def`

**klynge** failures:

* `klynge+V+Inf` => `klyng`
* `klynge` has no analyses either

**krible** failures:

* `krible+V+Inf` => `kribl`
* `krible` has no analyses either

**tele** failures:

* `tele+V+Inf` => `tel`
* `tele` has no analyses either

**mislike** failures:

* `mislike+V+Inf` does not generate!
* `mislike` has no analyses either

**vaske** failures:

* `vaske+V+Inf` => `vask`
* `vaske` has no analyses either

**maksle** failures:

* `maksle+V+Inf` => `maksl`
* `maksle` has no analyses either

**stensilere** failures:

* `stensilere+V+Inf` => `stensiler`
* `stensilere` has no analyses either

**tabellarisere** failures:

* `tabellarisere+V+Inf` => `tabellariser`
* `tabellarisere` has no analyses either

**blende** failures:

* `blende+V+Inf` => `blend`
* `blende` has no analyses either

**årøyte** failures:

* `årøyte+V+Inf` => `årøyt`
* `årøyte` has no analyses either

**smule** failures:

* `smule+V+Inf` => `smul`
* `smule` has following analyses:
  * `smul+A+A+Pl+Def`
  * `smul+A+A+Pl+Indef`
  * `smul+A+A+Sg+Def`

**konkurranseutsetje** failures:

* `konkurranseutsetje+V+Inf` does not generate!
* `konkurranseutsetje` has no analyses either

**stage** failures:

* `stage+V+Inf` => `stag`
* `stage` has no analyses either

**spanke** failures:

* `spanke+V+Inf` => `spank`
* `spanke` has no analyses either

**næle** failures:

* `næle+V+Inf` => `næl`
* `næle` has no analyses either

**mistenkje** failures:

* `mistenkje+V+Inf` does not generate!
* `mistenkje` has no analyses either

**fordreie** failures:

* `fordreie+V+Inf` => `fordrei`
* `fordreie` has no analyses either

**problematisere** failures:

* `problematisere+V+Inf` => `problematiser`
* `problematisere` has no analyses either

**postlegge** failures:

* `postlegge+V+Inf` does not generate!
* `postlegge` has no analyses either

**forminske** failures:

* `forminske+V+Inf` => `forminsk`
* `forminske` has no analyses either

**tåtte** failures:

* `tåtte+V+Inf` => `tått`
* `tåtte` has no analyses either

**fraksjonere** failures:

* `fraksjonere+V+Inf` => `fraksjoner`
* `fraksjonere` has no analyses either

**supplikkere** failures:

* `supplikkere+V+Inf` => `supplikker`
* `supplikkere` has no analyses either

**ekvivalere** failures:

* `ekvivalere+V+Inf` => `ekvivaler`
* `ekvivalere` has no analyses either

**kvare** failures:

* `kvare+V+Inf` => `kvar`
* `kvare` has following analyses:
  * `kvar+A+Sg+Def`
  * `kvar+A+Pl+Indef`

**prognostisere** failures:

* `prognostisere+V+Inf` => `prognostiser`
* `prognostisere` has no analyses either

**laupe** failures:

* `laupe+V+Inf` => `laup`
* `laupe` has no analyses either

**gleivre** failures:

* `gleivre+V+Inf` => `gleivr`
* `gleivre` has no analyses either

**kjæle** failures:

* `kjæle+V+Inf` => `kjæl`
* `kjæle` has no analyses either

**forfjase** failures:

* `forfjase+V+Inf` => `forfjas`
* `forfjase` has no analyses either

**abortere** failures:

* `abortere+V+Inf` => `aborter`
* `abortere` has no analyses either

**gardere** failures:

* `gardere+V+Inf` => `garder`
* `gardere` has no analyses either

**skjennast** failures:

* `skjennast+V+Inf` => `skjenast`
* `skjennast` has no analyses either

**mune** failures:

* `mune+V+Inf` => `mun`
* `mune` has no analyses either

**kroke** failures:

* `kroke+V+Inf` => `krok`
* `kroke` has no analyses either

**jamse** failures:

* `jamse+V+Inf` => `jams`
* `jamse` has no analyses either

**mute** failures:

* `mute+V+Inf` => `mut`
* `mute` has no analyses either

**juske** failures:

* `juske+V+Inf` => `jusk`
* `juske` has no analyses either

**dugeleggjere** failures:

* `dugeleggjere+V+Inf` does not generate!
* `dugeleggjere` has no analyses either

**sjøe** failures:

* `sjøe+V+Inf` => `sjø`
* `sjøe` has no analyses either

**skoddeleggje** failures:

* `skoddeleggje+V+Inf` does not generate!
* `skoddeleggje` has no analyses either

**saluttere** failures:

* `saluttere+V+Inf` => `salutter`
* `saluttere` has no analyses either

**beskrive** failures:

* `beskrive+V+Inf` => `beskrie`
* `beskrive+V+Inf` => `beskria`
* `beskrive` has no analyses either

**forsvinne** failures:

* `forsvinne+V+Inf` => `forsvia`
* `forsvinne+V+Inf` => `forsvie`
* `forsvinne` has no analyses either

**tynge** failures:

* `tynge+V+Inf` => `tyng`
* `tynge` has no analyses either

**gose** failures:

* `gose+V+Inf` => `gos`
* `gose` has no analyses either

**tyngne** failures:

* `tyngne+V+Inf` => `tyngn`
* `tyngne` has no analyses either

**vasse** failures:

* `vasse+V+Inf` => `vass`
* `vasse` has no analyses either

**fotografere** failures:

* `fotografere+V+Inf` => `fotografer`
* `fotografere` has no analyses either

**heilbrigde** failures:

* `heilbrigde+V+Inf` => `heilbrigd`
* `heilbrigde` has following analyses:
  * `heilbrigd+A+Sg+Def`
  * `heilbrigd+A+Pl+Indef`

**skriple** failures:

* `skriple+V+Inf` => `skripl`
* `skriple` has no analyses either

**persiflere** failures:

* `persiflere+V+Inf` => `persifler`
* `persiflere` has no analyses either

**evakuere** failures:

* `evakuere+V+Inf` => `evakuer`
* `evakuere` has no analyses either

**bryske** failures:

* `bryske+V+Inf` => `brysk`
* `bryske` has following analyses:
  * `brysk+A+A+Sg+Def`
  * `brysk+A+A+Pl+Indef`
  * `brysk+A+A+Pl+Def`

**misfare** failures:

* `misfare+V+Inf` => `misfaa`
* `misfare+V+Inf` => `misfae`
* `misfare` has no analyses either

**skålde** failures:

* `skålde+V+Inf` => `skåld`
* `skålde` has no analyses either

**sælke** failures:

* `sælke+V+Inf` => `sælk`
* `sælke` has no analyses either

**klare** failures:

* `klare+V+Inf` => `klar`
* `klare` has following analyses:
  * `klar+A+Pl+Indef`
  * `klar+A+Sg+Def`

**talfeste** failures:

* `talfeste+V+Inf` does not generate!
* `talfeste` has no analyses either

**miskunne** failures:

* `miskunne+V+Inf` => `miskunn`
* `miskunne` has no analyses either

**dagverde** failures:

* `dagverde+V+Inf` => `dagverd`
* `dagverde` has no analyses either

**versifisere** failures:

* `versifisere+V+Inf` => `versifiser`
* `versifisere` has no analyses either

**yfse** failures:

* `yfse+V+Inf` => `yfs`
* `yfse` has no analyses either

**talle** failures:

* `talle+V+Inf` => `tall`
* `talle` has no analyses either

**fnise** failures:

* `fnise+V+Inf` => `fnis`
* `fnise` has no analyses either

**sprekke** failures:

* `sprekke+V+Inf` does not generate!
* `sprekke` has no analyses either

**hyste** failures:

* `hyste+V+Inf` => `hyst`
* `hyste` has following analyses:
  * `hyse+V+PrfPrc`

**avtelje** failures:

* `avtelje+V+Inf` => `avteja`
* `avtelje+V+Inf` => `avteje`
* `avtelje` has no analyses either

**harke** failures:

* `harke+V+Inf` => `hark`
* `harke` has following analyses:
  * `harken+A+Neu+Sg+Indef`

**formilde** failures:

* `formilde+V+Inf` => `formild`
* `formilde` has no analyses either

**busette** failures:

* `busette+V+Inf` does not generate!
* `busette` has following analyses:
  * `busett+A+A+Pl+Indef`
  * `busett+A+A+Sg+Def`
  * `busett+A+A+Pl+Def`

**mørke** failures:

* `mørke+V+Inf` => `mørk`
* `mørke` has following analyses:
  * `mørk+A+Sg+Def`
  * `mørk+A+Pl+Indef`

**plaske** failures:

* `plaske+V+Inf` => `plask`
* `plaske` has no analyses either

**syntetisere** failures:

* `syntetisere+V+Inf` => `syntetiser`
* `syntetisere` has no analyses either

**epilere** failures:

* `epilere+V+Inf` => `epiler`
* `epilere` has no analyses either

**tjørebrede** failures:

* `tjørebrede+V+Inf` does not generate!
* `tjørebrede` has no analyses either

**mislukkast** failures:

* `mislukkast+V+Inf` => `mislukkastast`
* `mislukkast` has no analyses either

**øygne** failures:

* `øygne+V+Inf` => `øygn`
* `øygne` has no analyses either

**beordre** failures:

* `beordre+V+Inf` => `beordr`
* `beordre` has no analyses either

**knakke** failures:

* `knakke+V+Inf` => `knakk`
* `knakke` has no analyses either

**heimle** failures:

* `heimle+V+Inf` => `heiml`
* `heimle` has no analyses either

**tilbrigde** failures:

* `tilbrigde+V+Inf` => `tilbrigd`
* `tilbrigde` has following analyses:
  * `tilbrigde+N+N+Fem+Sg+Indef`

**ymte** failures:

* `ymte+V+Inf` => `ymt`
* `ymte` has no analyses either

**rasse** failures:

* `rasse+V+Inf` => `rass`
* `rasse` has no analyses either

**tøle** failures:

* `tøle+V+Inf` does not generate!
* `tøle` has no analyses either

**pansre** failures:

* `pansre+V+Inf` => `pansr`
* `pansre` has no analyses either

**komplimentere** failures:

* `komplimentere+V+Inf` => `komplimenter`
* `komplimentere` has no analyses either

**herde** failures:

* `herde+V+Inf` does not generate!
* `herde` has no analyses either

**storle** failures:

* `storle+V+Inf` => `storlle`
* `storle` has no analyses either

**trepanere** failures:

* `trepanere+V+Inf` => `trepaner`
* `trepanere` has no analyses either

**forskottere** failures:

* `forskottere+V+Inf` => `forskotter`
* `forskottere` has no analyses either

**beksle** failures:

* `beksle+V+Inf` => `beksl`
* `beksle` has no analyses either

**overtelje** failures:

* `overtelje+V+Inf` does not generate!
* `overtelje` has no analyses either

**smusse** failures:

* `smusse+V+Inf` => `smuss`
* `smusse` has no analyses either

**attribuere** failures:

* `attribuere+V+Inf` => `attribuer`
* `attribuere` has no analyses either

**irisere** failures:

* `irisere+V+Inf` => `iriser`
* `irisere` has no analyses either

**apostrofere** failures:

* `apostrofere+V+Inf` => `apostrofer`
* `apostrofere` has no analyses either

**paginere** failures:

* `paginere+V+Inf` => `paginer`
* `paginere` has no analyses either

**sirle** failures:

* `sirle+V+Inf` => `sirl`
* `sirle` has no analyses either

**dukke** failures:

* `dukke+V+Inf` => `dukk`
* `dukke` has no analyses either

**fattast** failures:

* `fattast+V+Inf` => `fattastast`
* `fattast` has following analyses:
  * `fatt+A+A+Superl+Indef`

**avsjå** failures:

* `avsjå+V+Inf` => `avssjå`
* `avsjå` has following analyses:
  * `avse+V+Inf`
  * `avse+V+Imprt`

**dvelje** failures:

* `dvelje+V+Inf` does not generate!
* `dvelje` has no analyses either

**samverke** failures:

* `samverke+V+Inf` => `samverk`
* `samverke` has no analyses either

**pakse** failures:

* `pakse+V+Inf` => `paks`
* `pakse` has no analyses either

**jagle** failures:

* `jagle+V+Inf` => `jagl`
* `jagle` has no analyses either

**knege** failures:

* `knege+V+Inf` => `kneg`
* `knege` has following analyses:
  * `knegen+A+Neu+Sg+Indef`

**tenje** failures:

* `tenje+V+Inf` does not generate!
* `tenje` has no analyses either

**prate** failures:

* `prate+V+Inf` => `prat`
* `prate` has no analyses either

**avalkoholisere** failures:

* `avalkoholisere+V+Inf` => `avalkoholiser`
* `avalkoholisere` has no analyses either

**brengle** failures:

* `brengle+V+Inf` => `brengl`
* `brengle` has no analyses either

**ileggje** failures:

* `ileggje+V+Inf` does not generate!
* `ileggje` has no analyses either

**signalere** failures:

* `signalere+V+Inf` => `signaler`
* `signalere` has no analyses either

**slitne** failures:

* `slitne+V+Inf` => `slitn`
* `slitne` has following analyses:
  * `sliten+A+Sg+Def`
  * `sliten+A+Pl+Indef`

**ake** failures:

* `ake+V+Inf` does not generate!
* `ake` has following analyses:
  * `ake+V`

**porre** failures:

* `porre+V+Inf` => `porr`
* `porre` has no analyses either

**kappspringe** failures:

* `kappspringe+V+Inf` => `kappspria`
* `kappspringe+V+Inf` => `kappsprie`
* `kappspringe` has no analyses either

**magre** failures:

* `magre+V+Inf` => `magr`
* `magre` has no analyses either

**gnisle** failures:

* `gnisle+V+Inf` => `gnisl`
* `gnisle` has no analyses either

**ybbe** failures:

* `ybbe+V+Inf` => `ybb`
* `ybbe` has no analyses either

**førekome** failures:

* `førekome+V+Inf` => `førekkoma`
* `førekome+V+Inf` => `førekkome`
* `førekome` has no analyses either

**svibrenne** failures:

* `svibrenne+V+Inf` => `svibrene`
* `svibrenne+V+Inf` => `svibrena`
* `svibrenne` has no analyses either

**slynge** failures:

* `slynge+V+Inf` => `slyng`
* `slynge` has no analyses either

**tildele** failures:

* `tildele+V+Inf` => `tildel`
* `tildele` has no analyses either

**støle** failures:

* `støle+V+Inf` => `støl`
* `støle` has following analyses:
  * `støl+A+A+Pl+Def`
  * `støl+A+A+Sg+Def`
  * `støl+A+A+Pl+Indef`

**gjennomdrøfte** failures:

* `gjennomdrøfte+V+Inf` does not generate!
* `gjennomdrøfte` has no analyses either

**overgi** failures:

* `overgi+V+Inf` => `overggi`
* `overgi` has no analyses either

**fortøye** failures:

* `fortøye+V+Inf` does not generate!
* `fortøye` has no analyses either

**klenge** failures:

* `klenge+V+Inf` => `kleng`
* `klenge` has following analyses:
  * `klengen+A+Neu+Sg+Indef`

**veide** failures:

* `veide+V+Inf` does not generate!
* `veide` has no analyses either

**ørske** failures:

* `ørske+V+Inf` => `ørsk`
* `ørske` has following analyses:
  * `ørsken+A+Neu+Sg+Indef`

**smalke** failures:

* `smalke+V+Inf` => `smalk`
* `smalke` has no analyses either

**done** failures:

* `done+V+Inf` => `don`
* `done` has following analyses:
  * `dette+V+Adj+Fl`
  * `dette+V+Adj+Bu+Eint`

**strekkje** failures:

* `strekkje+V+Inf` does not generate!
* `strekkje` has no analyses either

**lausne** failures:

* `lausne+V+Inf` => `lausn`
* `lausne` has no analyses either

**lemleste** failures:

* `lemleste+V+Inf` => `lemlest`
* `lemleste` has no analyses either

**tutle** failures:

* `tutle+V+Inf` => `tutl`
* `tutle` has following analyses:
  * `tutlen+A+Neu+Sg+Indef`

**merkje** failures:

* `merkje+V+Inf` does not generate!
* `merkje` has no analyses either

**tråkinne** failures:

* `tråkinne+V+Inf` => `tråkinn`
* `tråkinne` has no analyses either

**villeie** failures:

* `villeie+V+Inf` does not generate!
* `villeie` has no analyses either

**kauterisere** failures:

* `kauterisere+V+Inf` => `kauteriser`
* `kauterisere` has no analyses either

**daldre** failures:

* `daldre+V+Inf` => `daldr`
* `daldre` has no analyses either

**graduere** failures:

* `graduere+V+Inf` => `graduer`
* `graduere` has no analyses either

**bufre** failures:

* `bufre+V+Inf` => `bufr`
* `bufre` has no analyses either

**guffe** failures:

* `guffe+V+Inf` => `guff`
* `guffe` has following analyses:
  * `guffe+N+N+Sg`

**bylte** failures:

* `bylte+V+Inf` => `bylt`
* `bylte` has no analyses either

**fisle** failures:

* `fisle+V+Inf` => `fisl`
* `fisle` has no analyses either

**seglbu** failures:

* `seglbu+V+Inf` does not generate!
* `seglbu` has following analyses:
  * `seglbu+V`

**hose** failures:

* `hose+V+Inf` => `hos`
* `hose` has following analyses:
  * `hosen+A+Neu+Sg+Indef`

**tirre** failures:

* `tirre+V+Inf` => `tirr`
* `tirre` has no analyses either

**tville** failures:

* `tville+V+Inf` => `tvill`
* `tville` has no analyses either

**alienere** failures:

* `alienere+V+Inf` => `aliener`
* `alienere` has no analyses either

**forsette** failures:

* `forsette+V+Inf` does not generate!
* `forsette` has following analyses:
  * `forsett+A+A+Sg+Def`
  * `forsett+A+A+Pl+Def`
  * `forsett+A+A+Pl+Indef`

**kvape** failures:

* `kvape+V+Inf` => `kvap`
* `kvape` has following analyses:
  * `kvapen+A+Neu+Sg+Indef`

**røve** failures:

* `røve+V+Inf` => `røv`
* `røve` has no analyses either

**betre** failures:

* `betre+V+Inf` => `betr`
* `betre` has following analyses:
  * `betre+A+A+Neu+Sg+Indef`
  * `betre+A+A+Fem+Sg+Indef`
  * `betre+A+A+Msc+Sg+Indef`
  * `god+A+Comp`

**skovle** failures:

* `skovle+V+Inf` => `skovl`
* `skovle` has no analyses either

**avpasse** failures:

* `avpasse+V+Inf` => `avpass`
* `avpasse` has no analyses either

**bue** failures:

* `bue+V+Inf` => `bu`
* `bue` has following analyses:
  * `budd+A+A+Sg+Def`
  * `budd+A+A+Pl+Def`
  * `budd+A+A+Pl+Indef`
  * `buen+A+Neu+Sg+Indef`

**bunkre** failures:

* `bunkre+V+Inf` => `bunkr`
* `bunkre` has no analyses either

**krokkere** failures:

* `krokkere+V+Inf` => `krokker`
* `krokkere` has no analyses either

**diftongere** failures:

* `diftongere+V+Inf` => `diftonger`
* `diftongere` has no analyses either

**hovudrunde** failures:

* `hovudrunde+V+Inf` => `hovudrund`
* `hovudrunde` has no analyses either

**avmytologisere** failures:

* `avmytologisere+V+Inf` => `avmytologiser`
* `avmytologisere` has no analyses either

**høyre** failures:

* `høyre+V+Inf` does not generate!
* `høyre` has no analyses either

**strigle** failures:

* `strigle+V+Inf` => `strigl`
* `strigle` has no analyses either

**propagandere** failures:

* `propagandere+V+Inf` => `propagander`
* `propagandere` has no analyses either

**fortrykke** failures:

* `fortrykke+V+Inf` does not generate!
* `fortrykke` has no analyses either

**skjærne** failures:

* `skjærne+V+Inf` => `skjærn`
* `skjærne` has no analyses either

**varre** failures:

* `varre+V+Inf` => `varr`
* `varre` has no analyses either

**blære** failures:

* `blære+V+Inf` => `blær`
* `blære` has no analyses either

**lamme** failures:

* `lamme+V+Inf` => `lam`
* `lamme` has no analyses either

**datomerke** failures:

* `datomerke+V+Inf` => `datomerk`
* `datomerke` has no analyses either

**programmere** failures:

* `programmere+V+Inf` => `programmer`
* `programmere` has no analyses either

**tiske** failures:

* `tiske+V+Inf` => `tisk`
* `tiske` has no analyses either

**græle** failures:

* `græle+V+Inf` => `græl`
* `græle` has no analyses either

**støtte** failures:

* `støtte+V+Inf` => `støtt`
* `støtte` has no analyses either

**overteikne** failures:

* `overteikne+V+Inf` => `overteikn`
* `overteikne` has no analyses either

**blusse** failures:

* `blusse+V+Inf` => `bluss`
* `blusse` has no analyses either

**klemme** failures:

* `klemme+V+Inf` does not generate!
* `klemme` has no analyses either

**kople** failures:

* `kople+V+Inf` => `kopl`
* `kople` has no analyses either

**spinne** failures:

* `spinne+V+Inf` => `spie`
* `spinne+V+Inf` => `spia`
* `spinne` has no analyses either

**svinte** failures:

* `svinte+V+Inf` => `svint`
* `svinte` has following analyses:
  * `svint+A+A+Pl+Indef`
  * `svint+A+A+Pl+Def`
  * `svint+A+A+Sg+Def`

**kvinne** failures:

* `kvinne+V+Inf` => `kvinn`
* `kvinne` has no analyses either

**belje** failures:

* `belje+V+Inf` => `belj`
* `belje` has no analyses either

**sikke** failures:

* `sikke+V+Inf` => `sikk`
* `sikke` has no analyses either

**honorere** failures:

* `honorere+V+Inf` => `honorer`
* `honorere` has no analyses either

**forsyne** failures:

* `forsyne+V+Inf` => `forsyn`
* `forsyne` has no analyses either

**glope** failures:

* `glope+V+Inf` => `glop`
* `glope` has no analyses either

**utparsellere** failures:

* `utparsellere+V+Inf` => `utparseller`
* `utparsellere` has no analyses either

**knøde** failures:

* `knøde+V+Inf` does not generate!
* `knøde` has no analyses either

**berrleggje** failures:

* `berrleggje+V+Inf` does not generate!
* `berrleggje` has no analyses either

**lee** failures:

* `lee+V+Inf` => `le`
* `lee` has following analyses:
  * `le+A+A+Sg+Def`
  * `le+A+A+Pl+Def`
  * `lese+V+Inf`
  * `lese+V+Adj+Nøyt+Ub+Eint`
  * `le+A+A+Pl+Indef`
  * `lese+V+PrfPrc`

**vokalisere** failures:

* `vokalisere+V+Inf` => `vokaliser`
* `vokalisere` has no analyses either

**ønskje** failures:

* `ønskje+V+Inf` does not generate!
* `ønskje` has no analyses either

**anvise** failures:

* `anvise+V+Inf` => `anvis`
* `anvise` has no analyses either

**ejakulere** failures:

* `ejakulere+V+Inf` => `ejakuler`
* `ejakulere` has no analyses either

**torpe** failures:

* `torpe+V+Inf` => `torp`
* `torpe` has no analyses either

**loope** failures:

* `loope+V+Inf` => `loop`
* `loope` has no analyses either

**rådlegge** failures:

* `rådlegge+V+Inf` does not generate!
* `rådlegge` has no analyses either

**verdsetje** failures:

* `verdsetje+V+Inf` does not generate!
* `verdsetje` has no analyses either

**tjære** failures:

* `tjære+V+Inf` => `tjær`
* `tjære` has no analyses either

**hatre** failures:

* `hatre+V+Inf` => `hatr`
* `hatre` has no analyses either

**sjarmere** failures:

* `sjarmere+V+Inf` => `sjarmer`
* `sjarmere` has no analyses either

**dekonstruere** failures:

* `dekonstruere+V+Inf` => `dekonstruer`
* `dekonstruere` has no analyses either

**bøtlegge** failures:

* `bøtlegge+V+Inf` does not generate!
* `bøtlegge` has no analyses either

**konkurrere** failures:

* `konkurrere+V+Inf` => `konkurrer`
* `konkurrere` has no analyses either

**taue** failures:

* `taue+V+Inf` => `tau`
* `taue` has no analyses either

**dåme** failures:

* `dåme+V+Inf` => `dåm`
* `dåme` has no analyses either

**gremmast** failures:

* `gremmast+V+Inf` => `gremast`
* `gremmast` has no analyses either

**emballere** failures:

* `emballere+V+Inf` => `emballer`
* `emballere` has no analyses either

**slipe** failures:

* `slipe+V+Inf` does not generate!
* `slipe` has following analyses:
  * `slipen+A+Neu+Sg+Indef`

**svakke** failures:

* `svakke+V+Inf` => `svakk`
* `svakke` has no analyses either

**rennast** failures:

* `rennast+V+Inf` => `reast`
* `rennast` has no analyses either

**inndrive** failures:

* `inndrive+V+Inf` => `inndria`
* `inndrive+V+Inf` => `inndrie`
* `inndrive` has no analyses either

**snorksove** failures:

* `snorksove+V+Inf` => `snorksoe`
* `snorksove+V+Inf` => `snorksoa`
* `snorksove` has no analyses either

**beatboxe** failures:

* `beatboxe+V+Inf` => `beatbox`
* `beatboxe` has no analyses either

**sede** failures:

* `sede+V+Inf` => `sed`
* `sede` has no analyses either

**smugle** failures:

* `smugle+V+Inf` => `smugl`
* `smugle` has following analyses:
  * `smuglese+V+Ind+Prs`
  * `smuglese+V+Imprt`

**omrøde** failures:

* `omrøde+V+Inf` does not generate!
* `omrøde` has no analyses either

**låre** failures:

* `låre+V+Inf` => `lår`
* `låre` has no analyses either

**juble** failures:

* `juble+V+Inf` => `jubl`
* `juble` has no analyses either

**tusne** failures:

* `tusne+V+Inf` => `tusn`
* `tusne` has no analyses either

**fate** failures:

* `fate+V+Inf` => `fat`
* `fate` has no analyses either

**skuldgi** failures:

* `skuldgi+V+Inf` => `skuldggi`
* `skuldgi` has no analyses either

**fryde** failures:

* `fryde+V+Inf` => `fryd`
* `fryde` has no analyses either

**handtere** failures:

* `handtere+V+Inf` => `handter`
* `handtere` has no analyses either

**sterkne** failures:

* `sterkne+V+Inf` => `sterkn`
* `sterkne` has no analyses either

**handfare** failures:

* `handfare+V+Inf` => `handfae`
* `handfare+V+Inf` => `handfaa`
* `handfare` has no analyses either

**harme** failures:

* `harme+V+Inf` => `harm`
* `harme` has following analyses:
  * `harm+A+Pl+Indef`
  * `harm+A+Sg+Def`

**humanisere** failures:

* `humanisere+V+Inf` => `humaniser`
* `humanisere` has no analyses either

**turrmaule** failures:

* `turrmaule+V+Inf` => `turrmaul`
* `turrmaule` has no analyses either

**nenne** failures:

* `nenne+V+Inf` does not generate!
* `nenne` has no analyses either

**smale** failures:

* `smale+V+Inf` => `smal`
* `smale` has following analyses:
  * `smal+A+A+Pl+Def`
  * `smal+A+A+Pl+Indef`
  * `smal+A+A+Sg+Def`

**fye** failures:

* `fye+V+Inf` => `fy`
* `fye` has following analyses:
  * `fyke+V+Inf`

**avdekke** failures:

* `avdekke+V+Inf` does not generate!
* `avdekke` has no analyses either

**verkeleggjere** failures:

* `verkeleggjere+V+Inf` does not generate!
* `verkeleggjere` has no analyses either

**slitre** failures:

* `slitre+V+Inf` => `slitr`
* `slitre` has no analyses either

**sondere** failures:

* `sondere+V+Inf` => `sonder`
* `sondere` has no analyses either

**kvervle** failures:

* `kvervle+V+Inf` => `kvervl`
* `kvervle` has no analyses either

**innby** failures:

* `innby+V+Inf` does not generate!
* `innby` has no analyses either

**innstudere** failures:

* `innstudere+V+Inf` => `innstuder`
* `innstudere` has no analyses either

**kurve** failures:

* `kurve+V+Inf` => `kurv`
* `kurve` has no analyses either

**skamfryse** failures:

* `skamfryse+V+Inf` => `skamfrye`
* `skamfryse+V+Inf` => `skamfrya`
* `skamfryse` has no analyses either

**sanke** failures:

* `sanke+V+Inf` => `sank`
* `sanke` has no analyses either

**delfinansiere** failures:

* `delfinansiere+V+Inf` => `delfinansier`
* `delfinansiere` has no analyses either

**rake** failures:

* `rake+V+Inf` => `rak`
* `rake` has following analyses:
  * `rake+N+N+Sg`
  * `rake+N+N+Fem+Sg+Indef`
  * `rak+A+Sg+Def`
  * `rak+A+Pl+Indef`

**tyte** failures:

* `tyte+V+Inf` => `t`
* `tyte` has no analyses either

**pese** failures:

* `pese+V+Inf` => `pes`
* `pese` has no analyses either

**fortsette** failures:

* `fortsette+V+Inf` => `fortseta`
* `fortsette+V+Inf` => `fortsete`
* `fortsette` has following analyses:
  * `fortsette+V+Ind+Prt`
  * `fortsette+V+Adj+Fl`
  * `fortsette+V+Adj+Bu+Eint`

**eutrofiere** failures:

* `eutrofiere+V+Inf` => `eutrofier`
* `eutrofiere` has no analyses either

**fondere** failures:

* `fondere+V+Inf` => `fonder`
* `fondere` has no analyses either

**augnefeste** failures:

* `augnefeste+V+Inf` does not generate!
* `augnefeste` has no analyses either

**tystne** failures:

* `tystne+V+Inf` => `tystn`
* `tystne` has no analyses either

**vedkomme** failures:

* `vedkomme+V+Inf` => `vedkkomma`
* `vedkomme+V+Inf` => `vedkkomme`
* `vedkomme` has no analyses either

**gamse** failures:

* `gamse+V+Inf` => `gams`
* `gamse` has no analyses either

**mugne** failures:

* `mugne+V+Inf` => `mugn`
* `mugne` has following analyses:
  * `muggen+A+Pl+Indef`
  * `muggen+A+Sg+Def`

**tamburere** failures:

* `tamburere+V+Inf` => `tamburer`
* `tamburere` has no analyses either

**smuglese** failures:

* `smuglese+V+Inf` => `smuglee`
* `smuglese+V+Inf` => `smuglea`
* `smuglese` has no analyses either

**mage** failures:

* `mage+V+Inf` => `mag`
* `mage` has following analyses:
  * `mager+A+A+Pl+Def`
  * `mager+A+A+Sg+Def`
  * `mager+A+A+Pl+Indef`

**vole** failures:

* `vole+V+Inf` => `vol`
* `vole` has no analyses either

**halve** failures:

* `halve+V+Inf` => `halv`
* `halve` has following analyses:
  * `halv+A+Sg+Def`
  * `halv+A+Pl+Indef`

**sleppe** failures:

* `sleppe+V+Inf` => `sl`
* `sleppe` has no analyses either

**tjuvstarte** failures:

* `tjuvstarte+V+Inf` => `tjuvstart`
* `tjuvstarte` has no analyses either

**supere** failures:

* `supere+V+Inf` => `super`
* `supere` has no analyses either

**virre** failures:

* `virre+V+Inf` => `virr`
* `virre` has no analyses either

**driste** failures:

* `driste+V+Inf` => `drist`
* `driste` has no analyses either

**glede** failures:

* `glede+V+Inf` does not generate!
* `glede` has no analyses either

**sakne** failures:

* `sakne+V+Inf` => `sakn`
* `sakne` has no analyses either

**wappe** failures:

* `wappe+V+Inf` => `wapp`
* `wappe` has no analyses either

**avdramatisere** failures:

* `avdramatisere+V+Inf` => `avdramatiser`
* `avdramatisere` has no analyses either

**drubbe** failures:

* `drubbe+V+Inf` => `drubb`
* `drubbe` has no analyses either

**moralisere** failures:

* `moralisere+V+Inf` => `moraliser`
* `moralisere` has no analyses either

**voltere** failures:

* `voltere+V+Inf` => `volter`
* `voltere` has no analyses either

**ørmekte** failures:

* `ørmekte+V+Inf` => `ørmekt`
* `ørmekte` has no analyses either

**musisere** failures:

* `musisere+V+Inf` => `musiser`
* `musisere` has no analyses either

**orsake** failures:

* `orsake+V+Inf` => `orsak`
* `orsake` has no analyses either

**valfarte** failures:

* `valfarte+V+Inf` => `valfart`
* `valfarte` has no analyses either

**kvikne** failures:

* `kvikne+V+Inf` => `kvikn`
* `kvikne` has no analyses either

**svippe** failures:

* `svippe+V+Inf` => `svipp`
* `svippe` has no analyses either

**spratle** failures:

* `spratle+V+Inf` => `spratl`
* `spratle` has no analyses either

**forfjamse** failures:

* `forfjamse+V+Inf` => `forfjams`
* `forfjamse` has no analyses either

**skove** failures:

* `skove+V+Inf` => `skov`
* `skove` has no analyses either

**kåle** failures:

* `kåle+V+Inf` => `kål`
* `kåle` has no analyses either

**drepe** failures:

* `drepe+V+Inf` => `dree`
* `drepe+V+Inf` => `drea`
* `drepe` has no analyses either

**applaudere** failures:

* `applaudere+V+Inf` => `applauder`
* `applaudere` has no analyses either

**feitte** failures:

* `feitte+V+Inf` => `feitt`
* `feitte` has no analyses either

**gumpe** failures:

* `gumpe+V+Inf` => `gump`
* `gumpe` has no analyses either

**hikstele** failures:

* `hikstele+V+Inf` => `hikstelle`
* `hikstele` has no analyses either

**vavle** failures:

* `vavle+V+Inf` => `vavl`
* `vavle` has no analyses either

**vavre** failures:

* `vavre+V+Inf` => `vavr`
* `vavre` has no analyses either

**femdoble** failures:

* `femdoble+V+Inf` => `femdobl`
* `femdoble` has no analyses either

**gremme** failures:

* `gremme+V+Inf` does not generate!
* `gremme` has no analyses either

**klattløyve** failures:

* `klattløyve+V+Inf` does not generate!
* `klattløyve` has no analyses either

**haugta** failures:

* `haugta+V+Inf` => `haugtta`
* `haugta` has no analyses either

**pense** failures:

* `pense+V+Inf` => `pens`
* `pense` has no analyses either

**bose** failures:

* `bose+V+Inf` => `bos`
* `bose` has no analyses either

**blunke** failures:

* `blunke+V+Inf` => `blunk`
* `blunke` has no analyses either

**ilegge** failures:

* `ilegge+V+Inf` does not generate!
* `ilegge` has no analyses either

**rådspørje** failures:

* `rådspørje+V+Inf` does not generate!
* `rådspørje` has no analyses either

**pålegge** failures:

* `pålegge+V+Inf` does not generate!
* `pålegge` has no analyses either

**omkomme** failures:

* `omkomme+V+Inf` => `omkkomma`
* `omkomme+V+Inf` => `omkkomme`
* `omkomme` has no analyses either

**gjennomgløde** failures:

* `gjennomgløde+V+Inf` does not generate!
* `gjennomgløde` has no analyses either

**hoppe** failures:

* `hoppe+V+Inf` => `hopp`
* `hoppe` has no analyses either

**kløvje** failures:

* `kløvje+V+Inf` => `kløvj`
* `kløvje` has no analyses either

**sanforisere** failures:

* `sanforisere+V+Inf` => `sanforiser`
* `sanforisere` has no analyses either

**stjernemerkje** failures:

* `stjernemerkje+V+Inf` does not generate!
* `stjernemerkje` has no analyses either

**etterbetale** failures:

* `etterbetale+V+Inf` does not generate!
* `etterbetale` has no analyses either

**resette** failures:

* `resette+V+Inf` does not generate!
* `resette` has no analyses either

**drupe** failures:

* `drupe+V+Inf` => `drup`
* `drupe` has no analyses either

**losse** failures:

* `losse+V+Inf` => `loss`
* `losse` has following analyses:
  * `loss+A+A+Pl+Indef`
  * `loss+A+A+Sg+Def`
  * `loss+A+A+Pl+Def`

**pirast** failures:

* `pirast+V+Inf` => `pirastast`
* `pirast` has no analyses either

**skåre** failures:

* `skåre+V+Inf` => `skår`
* `skåre` has no analyses either

**nette** failures:

* `nette+V+Inf` => `nett`
* `nette` has following analyses:
  * `nett+A+A+Pl+Indef`
  * `nett+A+A+Sg+Def`
  * `nett+A+A+Pl+Def`

**tyngje** failures:

* `tyngje+V+Inf` does not generate!
* `tyngje` has no analyses either

**kortslutte** failures:

* `kortslutte+V+Inf` => `kortslutt`
* `kortslutte` has no analyses either

**dissimilere** failures:

* `dissimilere+V+Inf` => `dissimiler`
* `dissimilere` has no analyses either

**nikse** failures:

* `nikse+V+Inf` => `niks`
* `nikse` has following analyses:
  * `niks+A+A+Sg+Def`
  * `niks+A+A+Pl+Def`
  * `niks+A+A+Pl+Indef`

**kaskotrygde** failures:

* `kaskotrygde+V+Inf` => `kaskotrygd`
* `kaskotrygde` has no analyses either

**liggje** failures:

* `liggje+V+Inf` => `lliggja`
* `liggje+V+Inf` => `lliggje`
* `liggje` has no analyses either

**fortjukke** failures:

* `fortjukke+V+Inf` => `fortjukk`
* `fortjukke` has no analyses either

**sleine** failures:

* `sleine+V+Inf` => `slein`
* `sleine` has no analyses either

**kveste** failures:

* `kveste+V+Inf` => `kvest`
* `kveste` has following analyses:
  * `kvese+V+PrfPrc`

**gyfte** failures:

* `gyfte+V+Inf` does not generate!
* `gyfte` has no analyses either

**hundfare** failures:

* `hundfare+V+Inf` => `hundfae`
* `hundfare+V+Inf` => `hundfaa`
* `hundfare` has no analyses either

**skjemte** failures:

* `skjemte+V+Inf` => `skjemt`
* `skjemte` has following analyses:
  * `skjemt+A+A+Pl+Indef`
  * `skjemt+A+A+Pl+Def`
  * `skjemt+A+A+Sg+Def`

**skamklippe** failures:

* `skamklippe+V+Inf` does not generate!
* `skamklippe` has no analyses either

**valutere** failures:

* `valutere+V+Inf` => `valuter`
* `valutere` has no analyses either

**barbere** failures:

* `barbere+V+Inf` => `barber`
* `barbere` has no analyses either

**draule** failures:

* `draule+V+Inf` => `draul`
* `draule` has following analyses:
  * `draulen+A+Neu+Sg+Indef`

**busse** failures:

* `busse+V+Inf` => `buss`
* `busse` has no analyses either

**fine** failures:

* `fine+V+Inf` => `fin`
* `fine` has following analyses:
  * `fin+A+A+Pl+Def`
  * `fise+V+Adj+Fl`
  * `fin+A+A+Sg+Def`
  * `fin+A+A+Pl+Indef`
  * `fise+V+Adj+Bu+Eint`

**avkjønne** failures:

* `avkjønne+V+Inf` => `avkjønn`
* `avkjønne` has no analyses either

**dyrkast** failures:

* `dyrkast+V+Inf` => `dyrkastast`
* `dyrkast` has no analyses either

**tjore** failures:

* `tjore+V+Inf` => `tjor`
* `tjore` has no analyses either

**stogge** failures:

* `stogge+V+Inf` => `stogg`
* `stogge` has no analyses either

**kvelle** failures:

* `kvelle+V+Inf` => `kvea`
* `kvelle+V+Inf` => `kvee`
* `kvelle` has no analyses either

**predeterminere** failures:

* `predeterminere+V+Inf` => `predeterminer`
* `predeterminere` has no analyses either

**hue** failures:

* `hue+V+Inf` => `hu`
* `hue` has no analyses either

**lòse** failures:

* `lòse+V+Inf` does not generate!
* `lòse` has no analyses either

**tenle** failures:

* `tenle+V+Inf` => `tenl`
* `tenle` has no analyses either

**stivfryse** failures:

* `stivfryse+V+Inf` => `stivfrya`
* `stivfryse+V+Inf` => `stivfrye`
* `stivfryse` has no analyses either

**klasse** failures:

* `klasse+V+Inf` => `klass`
* `klasse` has no analyses either

**drusle** failures:

* `drusle+V+Inf` => `drusl`
* `drusle` has no analyses either

**konseptualisere** failures:

* `konseptualisere+V+Inf` => `konseptualiser`
* `konseptualisere` has no analyses either

**lakke** failures:

* `lakke+V+Inf` => `lakk`
* `lakke` has no analyses either

**putte** failures:

* `putte+V+Inf` => `putt`
* `putte` has following analyses:
  * `putt+A+A+Pl+Def`
  * `putt+A+A+Pl+Indef`
  * `putt+A+A+Sg+Def`

**tekste** failures:

* `tekste+V+Inf` => `tekst`
* `tekste` has no analyses either

**mussere** failures:

* `mussere+V+Inf` => `musser`
* `mussere` has no analyses either

**incitere** failures:

* `incitere+V+Inf` => `inciter`
* `incitere` has no analyses either

**tøvle** failures:

* `tøvle+V+Inf` => `tøvl`
* `tøvle` has no analyses either

**kovne** failures:

* `kovne+V+Inf` => `kovn`
* `kovne` has following analyses:
  * `koven+A+Sg+Def`
  * `koven+A+Pl+Indef`

**antake** failures:

* `antake+V+Inf` => `anttake`
* `antake+V+Inf` => `anttaka`
* `antake` has no analyses either

**eskapere** failures:

* `eskapere+V+Inf` => `eskaper`
* `eskapere` has no analyses either

**hakke** failures:

* `hakke+V+Inf` => `hakk`
* `hakke` has no analyses either

**vondle** failures:

* `vondle+V+Inf` => `vondl`
* `vondle` has no analyses either

**trasle** failures:

* `trasle+V+Inf` => `trasl`
* `trasle` has no analyses either

**implodere** failures:

* `implodere+V+Inf` => `imploder`
* `implodere` has no analyses either

**førebygge** failures:

* `førebygge+V+Inf` does not generate!
* `førebygge` has no analyses either

**kryss-sjekke** failures:

* `kryss-sjekke+V+Inf` => `kryss-sjekk`
* `kryss-sjekke` has no analyses either

**neve** failures:

* `neve+V+Inf` => `nev`
* `neve` has no analyses either

**brutalisere** failures:

* `brutalisere+V+Inf` => `brutaliser`
* `brutalisere` has no analyses either

**opalescere** failures:

* `opalescere+V+Inf` => `opalescer`
* `opalescere` has no analyses either

**kvotere** failures:

* `kvotere+V+Inf` => `kvoter`
* `kvotere` has no analyses either

**skrikke** failures:

* `skrikke+V+Inf` => `skrikk`
* `skrikke` has no analyses either

**tukke** failures:

* `tukke+V+Inf` => `tukk`
* `tukke` has no analyses either

**bemle** failures:

* `bemle+V+Inf` => `beml`
* `bemle` has no analyses either

**fegne** failures:

* `fegne+V+Inf` => `fegn`
* `fegne` has following analyses:
  * `fegen+A+Pl+Indef`
  * `fegen+A+Sg+Def`

**vulke** failures:

* `vulke+V+Inf` => `vulk`
* `vulke` has no analyses either

**kludre** failures:

* `kludre+V+Inf` => `kludr`
* `kludre` has no analyses either

**avleive** failures:

* `avleive+V+Inf` does not generate!
* `avleive` has no analyses either

**gjennomføre** failures:

* `gjennomføre+V+Inf` => `gjennomfør`
* `gjennomføre` has no analyses either

**regle** failures:

* `regle+V+Inf` => `regl`
* `regle` has no analyses either

**svirte** failures:

* `svirte+V+Inf` => `svirt`
* `svirte` has no analyses either

**skumlese** failures:

* `skumlese+V+Inf` => `skumlee`
* `skumlese+V+Inf` => `skumlea`
* `skumlese` has no analyses either

**tæpe** failures:

* `tæpe+V+Inf` => `tæp`
* `tæpe` has no analyses either

**bestikke** failures:

* `bestikke+V+Inf` => `bestia`
* `bestikke+V+Inf` => `bestie`
* `bestikke` has no analyses either

**strakne** failures:

* `strakne+V+Inf` => `strakn`
* `strakne` has no analyses either

**snitte** failures:

* `snitte+V+Inf` => `snitt`
* `snitte` has no analyses either

**tene** failures:

* `tene+V+Inf` => `ten`
* `tene` has following analyses:
  * `to+N+N+Fem+Pl+Def`
  * `tå+N+N+Fem+Pl+Def`
  * `tåg+N+N+Fem+Pl+Def`

**embarkere** failures:

* `embarkere+V+Inf` => `embarker`
* `embarkere` has no analyses either

**sæde** failures:

* `sæde+V+Inf` => `sæd`
* `sæde` has no analyses either

**okkupere** failures:

* `okkupere+V+Inf` => `okkuper`
* `okkupere` has no analyses either

**kildre** failures:

* `kildre+V+Inf` => `kildr`
* `kildre` has no analyses either

**bladre** failures:

* `bladre+V+Inf` => `bladr`
* `bladre` has no analyses either

**polstre** failures:

* `polstre+V+Inf` => `polstr`
* `polstre` has no analyses either

**telte** failures:

* `telte+V+Inf` => `telt`
* `telte` has no analyses either

**dissentere** failures:

* `dissentere+V+Inf` => `dissenter`
* `dissentere` has no analyses either

**unnvere** failures:

* `unnvere+V+Inf` => `unnvee`
* `unnvere+V+Inf` => `unnvea`
* `unnvere` has no analyses either

**forbryte** failures:

* `forbryte+V+Inf` => `forbrye`
* `forbryte+V+Inf` => `forbrya`
* `forbryte` has no analyses either

**einsrette** failures:

* `einsrette+V+Inf` => `einsrett`
* `einsrette` has no analyses either

**klårgjere** failures:

* `klårgjere+V+Inf` does not generate!
* `klårgjere` has no analyses either

**floge** failures:

* `floge+V+Inf` => `flog`
* `floge` has following analyses:
  * `flogen+A+Neu+Sg+Indef`

**ymje** failures:

* `ymje+V+Inf` does not generate!
* `ymje` has following analyses:
  * `ymje+V`

**eigne** failures:

* `eigne+V+Inf` => `eign`
* `eigne` has no analyses either

**elevere** failures:

* `elevere+V+Inf` => `elever`
* `elevere` has no analyses either

**utheve** failures:

* `utheve+V+Inf` => `uthev`
* `utheve` has no analyses either

**hanse** failures:

* `hanse+V+Inf` => `hans`
* `hanse` has no analyses either

**korrodere** failures:

* `korrodere+V+Inf` => `korroder`
* `korrodere` has no analyses either

**påreknast** failures:

* `påreknast+V+Inf` => `påreknastast`
* `påreknast` has no analyses either

**svenske** failures:

* `svenske+V+Inf` => `svensk`
* `svenske` has following analyses:
  * `svensk+A+A+Pl+Def`
  * `svensk+A+A+Sg+Def`
  * `svensk+A+A+Pl+Indef`

**proporsjonere** failures:

* `proporsjonere+V+Inf` => `proporsjoner`
* `proporsjonere` has no analyses either

**tårne** failures:

* `tårne+V+Inf` => `tårn`
* `tårne` has no analyses either

**dirigere** failures:

* `dirigere+V+Inf` => `diriger`
* `dirigere` has no analyses either

**hike** failures:

* `hike+V+Inf` => `hik`
* `hike` has no analyses either

**kategorisere** failures:

* `kategorisere+V+Inf` => `kategoriser`
* `kategorisere` has no analyses either

**klatte** failures:

* `klatte+V+Inf` => `klatt`
* `klatte` has no analyses either

**ilikne** failures:

* `ilikne+V+Inf` => `ilikn`
* `ilikne` has no analyses either

**lobbe** failures:

* `lobbe+V+Inf` => `lobb`
* `lobbe` has no analyses either

**styre** failures:

* `styre+V+Inf` does not generate!
* `styre` has no analyses either

**måle** failures:

* `måle+V+Inf` => `mål`
* `måle` has no analyses either

**fylle** failures:

* `fylle+V+Inf` => `fyl`
* `fylle` has no analyses either

**quilte** failures:

* `quilte+V+Inf` => `quilt`
* `quilte` has no analyses either

**spjarre** failures:

* `spjarre+V+Inf` => `spjarr`
* `spjarre` has no analyses either

**sysle** failures:

* `sysle+V+Inf` => `sysl`
* `sysle` has no analyses either

**forgasse** failures:

* `forgasse+V+Inf` => `forgass`
* `forgasse` has no analyses either

**forføtte** failures:

* `forføtte+V+Inf` => `forføtt`
* `forføtte` has no analyses either

**keie** failures:

* `keie+V+Inf` => `kei`
* `keie` has following analyses:
  * `kei+A+Pl+Indef`
  * `kei+A+Sg+Def`

**falle** failures:

* `falle+V+Inf` => `fae`
* `falle+V+Inf` => `faa`
* `falle` has no analyses either

**nominere** failures:

* `nominere+V+Inf` => `nominer`
* `nominere` has no analyses either

**enkje** failures:

* `enkje+V+Inf` => `enk`
* `enkje` has no analyses either

**sluffe** failures:

* `sluffe+V+Inf` => `sluff`
* `sluffe` has no analyses either

**hære** failures:

* `hære+V+Inf` => `hær`
* `hære` has no analyses either

**masturbere** failures:

* `masturbere+V+Inf` => `masturber`
* `masturbere` has no analyses either

**overkompensere** failures:

* `overkompensere+V+Inf` => `overkompenser`
* `overkompensere` has no analyses either

**vise** failures:

* `vise+V+Inf` => `vis`
* `vise` has following analyses:
  * `vissen+A+Neu+Sg+Indef`
  * `vis+A+Pl+Indef`
  * `visen+A+Neu+Sg+Indef`
  * `vis+A+Sg+Def`

**snakkast** failures:

* `snakkast+V+Inf` => `snakkastast`
* `snakkast` has no analyses either

**bevise** failures:

* `bevise+V+Inf` => `bevis`
* `bevise` has no analyses either

**arkaisere** failures:

* `arkaisere+V+Inf` => `arkaiser`
* `arkaisere` has no analyses either

**blóte** failures:

* `blóte+V+Inf` => `blót`
* `blóte` has no analyses either

**immigrere** failures:

* `immigrere+V+Inf` => `immigrer`
* `immigrere` has no analyses either

**fomle** failures:

* `fomle+V+Inf` => `foml`
* `fomle` has no analyses either

**poengtere** failures:

* `poengtere+V+Inf` => `poengter`
* `poengtere` has no analyses either

**krile** failures:

* `krile+V+Inf` => `kril`
* `krile` has no analyses either

**skattleggje** failures:

* `skattleggje+V+Inf` does not generate!
* `skattleggje` has no analyses either

**irre** failures:

* `irre+V+Inf` => `irr`
* `irre` has no analyses either

**dusje** failures:

* `dusje+V+Inf` => `dusj`
* `dusje` has no analyses either

**flamme** failures:

* `flamme+V+Inf` => `flam`
* `flamme` has no analyses either

**svage** failures:

* `svage+V+Inf` => `svag`
* `svage` has no analyses either

**susle** failures:

* `susle+V+Inf` => `susl`
* `susle` has no analyses either

**baute** failures:

* `baute+V+Inf` => `baut`
* `baute` has no analyses either

**triumfere** failures:

* `triumfere+V+Inf` => `triumfer`
* `triumfere` has no analyses either

**ballaste** failures:

* `ballaste+V+Inf` => `ballast`
* `ballaste` has no analyses either

**lakkere** failures:

* `lakkere+V+Inf` => `lakker`
* `lakkere` has no analyses either

**snikre** failures:

* `snikre+V+Inf` => `snikr`
* `snikre` has no analyses either

**kaldstarte** failures:

* `kaldstarte+V+Inf` => `kaldstart`
* `kaldstarte` has no analyses either

**knepre** failures:

* `knepre+V+Inf` => `knepr`
* `knepre` has no analyses either

**hutte** failures:

* `hutte+V+Inf` => `hutt`
* `hutte` has no analyses either

**starte** failures:

* `starte+V+Inf` => `start`
* `starte` has no analyses either

**le** failures:

* `le+V+Inf` => `lle`
* `le` has following analyses:
  * `le+A+A+Fem+Sg+Indef`
  * `le+A+A+Neu+Sg+Indef`
  * `lese+V+Ind+Prs`
  * `låg+A+Pl+Indef`
  * `lee+V+Imprt`
  * `låg+A+Sg+Def`
  * `le+A+A+Msc+Sg+Indef`
  * `lee+V+Inf`
  * `lese+V+Imprt`

**besøke** failures:

* `besøke+V+Inf` => `besøk`
* `besøke` has no analyses either

**skjere** failures:

* `skjere+V+Inf` => `skjea`
* `skjere+V+Inf` => `skjee`
* `skjere` has following analyses:
  * `skjerr+A+A+Sg+Def`
  * `skjerr+A+A+Pl+Indef`
  * `skjerr+A+A+Pl+Def`

**utradere** failures:

* `utradere+V+Inf` => `utrader`
* `utradere` has no analyses either

**skjerre** failures:

* `skjerre+V+Inf` => `skjerr`
* `skjerre` has no analyses either

**kalkere** failures:

* `kalkere+V+Inf` => `kalker`
* `kalkere` has no analyses either

**samstemme** failures:

* `samstemme+V+Inf` does not generate!
* `samstemme` has no analyses either

**husse** failures:

* `husse+V+Inf` => `huss`
* `husse` has no analyses either

**sølvlegge** failures:

* `sølvlegge+V+Inf` does not generate!
* `sølvlegge` has no analyses either

**overføre** failures:

* `overføre+V+Inf` => `overfør`
* `overføre` has no analyses either

**forte** failures:

* `forte+V+Inf` => `fort`
* `forte` has following analyses:
  * `fort+A+A+Pl+Def`
  * `fort+A+A+Sg+Def`
  * `fort+A+A+Pl+Indef`

**terrassere** failures:

* `terrassere+V+Inf` => `terrasser`
* `terrassere` has no analyses either

**skrelle** failures:

* `skrelle+V+Inf` does not generate!
* `skrelle` has no analyses either

**sveitte** failures:

* `sveitte+V+Inf` => `sveitt`
* `sveitte` has following analyses:
  * `sveitt+A+A+Pl+Indef`
  * `sveitt+A+A+Sg+Def`
  * `sveitt+A+A+Pl+Def`

**kjelve** failures:

* `kjelve+V+Inf` => `kjelv`
* `kjelve` has no analyses either

**ure** failures:

* `ure+V+Inf` => `ur`
* `ure` has no analyses either

**resorbere** failures:

* `resorbere+V+Inf` => `resorber`
* `resorbere` has no analyses either

**urde** failures:

* `urde+V+Inf` => `urd`
* `urde` has no analyses either

**forrente** failures:

* `forrente+V+Inf` => `forrent`
* `forrente` has no analyses either

**tjatte** failures:

* `tjatte+V+Inf` => `tjatt`
* `tjatte` has no analyses either

**hardstyre** failures:

* `hardstyre+V+Inf` does not generate!
* `hardstyre` has no analyses either

**raute** failures:

* `raute+V+Inf` => `raut`
* `raute` has no analyses either

**sniffe** failures:

* `sniffe+V+Inf` => `sniff`
* `sniffe` has no analyses either

**stikke** failures:

* `stikke+V+Inf` => `stia`
* `stikke+V+Inf` => `stie`
* `stikke` has no analyses either

**forkleine** failures:

* `forkleine+V+Inf` => `forklein`
* `forkleine` has no analyses either

**klårne** failures:

* `klårne+V+Inf` => `klårn`
* `klårne` has no analyses either

**gjendikte** failures:

* `gjendikte+V+Inf` => `gjendikt`
* `gjendikte` has no analyses either

**ljossette** failures:

* `ljossette+V+Inf` does not generate!
* `ljossette` has no analyses either

**immatrikulere** failures:

* `immatrikulere+V+Inf` => `immatrikuler`
* `immatrikulere` has no analyses either

**frese** failures:

* `frese+V+Inf` => `fres`
* `frese` has no analyses either

**produsere** failures:

* `produsere+V+Inf` => `produser`
* `produsere` has no analyses either

**isskyte** failures:

* `isskyte+V+Inf` => `isskye`
* `isskyte+V+Inf` => `isskya`
* `isskyte` has no analyses either

**vitskremme** failures:

* `vitskremme+V+Inf` does not generate!
* `vitskremme` has no analyses either

**lure** failures:

* `lure+V+Inf` => `lur`
* `lure` has following analyses:
  * `lur+A+Pl+Indef`
  * `luren+A+Neu+Sg+Indef`
  * `lur+A+Sg+Def`

**tvitre** failures:

* `tvitre+V+Inf` => `tvitr`
* `tvitre` has no analyses either

**rekne** failures:

* `rekne+V+Inf` => `rekn`
* `rekne` has no analyses either

**uttale** failures:

* `uttale+V+Inf` does not generate!
* `uttale` has no analyses either

**forskyve** failures:

* `forskyve+V+Inf` => `forske`
* `forskyve+V+Inf` => `forska`
* `forskyve` has no analyses either

**platinere** failures:

* `platinere+V+Inf` => `platiner`
* `platinere` has no analyses either

**duplisere** failures:

* `duplisere+V+Inf` => `dupliser`
* `duplisere` has no analyses either

**kollaborere** failures:

* `kollaborere+V+Inf` => `kollaborer`
* `kollaborere` has no analyses either

**kvadrere** failures:

* `kvadrere+V+Inf` => `kvadrer`
* `kvadrere` has no analyses either

**børsnotere** failures:

* `børsnotere+V+Inf` => `børsnoter`
* `børsnotere` has no analyses either

**tinge** failures:

* `tinge+V+Inf` => `ting`
* `tinge` has no analyses either

**dragast** failures:

* `dragast+V+Inf` => `draast`
* `dragast` has following analyses:
  * `drage+V+Inf+Pass`

**formatere** failures:

* `formatere+V+Inf` => `formater`
* `formatere` has no analyses either

**bandasjere** failures:

* `bandasjere+V+Inf` => `bandasjer`
* `bandasjere` has no analyses either

**spyle** failures:

* `spyle+V+Inf` => `spyl`
* `spyle` has no analyses either

**såpe** failures:

* `såpe+V+Inf` => `såp`
* `såpe` has no analyses either

**spreie** failures:

* `spreie+V+Inf` does not generate!
* `spreie` has no analyses either

**gårast** failures:

* `gårast+V+Inf` => `gårastast`
* `gårast` has no analyses either

**glåpe** failures:

* `glåpe+V+Inf` => `glåp`
* `glåpe` has no analyses either

**vasskjemme** failures:

* `vasskjemme+V+Inf` does not generate!
* `vasskjemme` has no analyses either

**fløte** failures:

* `fløte+V+Inf` => `fløt`
* `fløte` has following analyses:
  * `fløt+A+Pl+Indef`
  * `fløt+A+Sg+Def`

**neie** failures:

* `neie+V+Inf` => `nei`
* `neie` has no analyses either

**loffe** failures:

* `loffe+V+Inf` => `loff`
* `loffe` has no analyses either

**motarbeide** failures:

* `motarbeide+V+Inf` does not generate!
* `motarbeide` has no analyses either

**heme** failures:

* `heme+V+Inf` => `hem`
* `heme` has no analyses either

**kare** failures:

* `kare+V+Inf` => `kar`
* `kare` has no analyses either

**blaffe** failures:

* `blaffe+V+Inf` => `blaff`
* `blaffe` has no analyses either

**rysje** failures:

* `rysje+V+Inf` does not generate!
* `rysje` has no analyses either

**innkalle** failures:

* `innkalle+V+Inf` => `innkall`
* `innkalle` has no analyses either

**memorere** failures:

* `memorere+V+Inf` => `memorer`
* `memorere` has no analyses either

**forfekte** failures:

* `forfekte+V+Inf` => `forfekt`
* `forfekte` has no analyses either

**krevjast** failures:

* `krevjast+V+Inf` => `krejast`
* `krevjast` has no analyses either

**gafle** failures:

* `gafle+V+Inf` => `gafl`
* `gafle` has no analyses either

**konstruere** failures:

* `konstruere+V+Inf` => `konstruer`
* `konstruere` has no analyses either

**tapetsere** failures:

* `tapetsere+V+Inf` => `tapetser`
* `tapetsere` has no analyses either

**krukse** failures:

* `krukse+V+Inf` => `kruks`
* `krukse` has no analyses either

**turrtenkje** failures:

* `turrtenkje+V+Inf` does not generate!
* `turrtenkje` has no analyses either

**gløde** failures:

* `gløde+V+Inf` does not generate!
* `gløde` has no analyses either

**budsjettere** failures:

* `budsjettere+V+Inf` => `budsjetter`
* `budsjettere` has no analyses either

**sudde** failures:

* `sudde+V+Inf` => `sudd`
* `sudde` has no analyses either

**frappere** failures:

* `frappere+V+Inf` => `frapper`
* `frappere` has no analyses either

**lekkje** failures:

* `lekkje+V+Inf` does not generate!
* `lekkje` has no analyses either

**krystallisere** failures:

* `krystallisere+V+Inf` => `krystalliser`
* `krystallisere` has no analyses either

**pirke** failures:

* `pirke+V+Inf` => `pirk`
* `pirke` has following analyses:
  * `pirken+A+Neu+Sg+Indef`

**larve** failures:

* `larve+V+Inf` => `larv`
* `larve` has following analyses:
  * `larven+A+Neu+Sg+Indef`

**sabotere** failures:

* `sabotere+V+Inf` => `saboter`
* `sabotere` has no analyses either

**geipe** failures:

* `geipe+V+Inf` does not generate!
* `geipe` has no analyses either

**mottake** failures:

* `mottake+V+Inf` => `motttaka`
* `mottake+V+Inf` => `motttake`
* `mottake` has no analyses either

**swinge** failures:

* `swinge+V+Inf` => `swing`
* `swinge` has no analyses either

**inntake** failures:

* `inntake+V+Inf` => `innttaka`
* `inntake+V+Inf` => `innttake`
* `inntake` has no analyses either

**restituere** failures:

* `restituere+V+Inf` => `restituer`
* `restituere` has no analyses either

**smeike** failures:

* `smeike+V+Inf` => `smeik`
* `smeike` has following analyses:
  * `smeiken+A+Neu+Sg+Indef`

**frege** failures:

* `frege+V+Inf` => `freg`
* `frege` has no analyses either

**smalne** failures:

* `smalne+V+Inf` => `smaln`
* `smalne` has no analyses either

**røyte** failures:

* `røyte+V+Inf` => `røyt`
* `røyte` has no analyses either

**kangle** failures:

* `kangle+V+Inf` => `kangl`
* `kangle` has no analyses either

**avkorte** failures:

* `avkorte+V+Inf` => `avkort`
* `avkorte` has no analyses either

**bygsle** failures:

* `bygsle+V+Inf` => `bygsl`
* `bygsle` has no analyses either

**avbestille** failures:

* `avbestille+V+Inf` does not generate!
* `avbestille` has no analyses either

**gøyve** failures:

* `gøyve+V+Inf` does not generate!
* `gøyve` has no analyses either

**perte** failures:

* `perte+V+Inf` => `pert`
* `perte` has no analyses either

**ruvle** failures:

* `ruvle+V+Inf` => `ruvl`
* `ruvle` has no analyses either

**skri** failures:

* `skri+V+Inf` does not generate!
* `skri` has following analyses:
  * `skrive+V+Imprt`
  * `skrive+V+Ind+Prs`
  * `skrå+N+N+Neu+Pl+Def`
  * `skrå+N+N+Fem+Sg+Def`

**dynje** failures:

* `dynje+V+Inf` does not generate!
* `dynje` has no analyses either

**sukre** failures:

* `sukre+V+Inf` => `sukr`
* `sukre` has no analyses either

**snadre** failures:

* `snadre+V+Inf` => `snadr`
* `snadre` has no analyses either

**tillempe** failures:

* `tillempe+V+Inf` => `tillemp`
* `tillempe` has no analyses either

**signere** failures:

* `signere+V+Inf` => `signer`
* `signere` has no analyses either

**trakke** failures:

* `trakke+V+Inf` => `trakk`
* `trakke` has no analyses either

**skòle** failures:

* `skòle+V+Inf` => `skòl`
* `skòle` has no analyses either

**prøvehoppe** failures:

* `prøvehoppe+V+Inf` => `prøvehopp`
* `prøvehoppe` has no analyses either

**stiple** failures:

* `stiple+V+Inf` => `stipl`
* `stiple` has no analyses either

**separere** failures:

* `separere+V+Inf` => `separer`
* `separere` has no analyses either

**pinke** failures:

* `pinke+V+Inf` => `pink`
* `pinke` has no analyses either

**innsjå** failures:

* `innsjå+V+Inf` => `innssjå`
* `innsjå` has following analyses:
  * `innse+V+Imprt`
  * `innse+V+Inf`

**tvinte** failures:

* `tvinte+V+Inf` => `tvint`
* `tvinte` has no analyses either

**formeinke** failures:

* `formeinke+V+Inf` => `formeink`
* `formeinke` has no analyses either

**forskanse** failures:

* `forskanse+V+Inf` => `forskans`
* `forskanse` has no analyses either

**smie** failures:

* `smie+V+Inf` does not generate!
* `smie` has no analyses either

**krabbe** failures:

* `krabbe+V+Inf` => `krabb`
* `krabbe` has no analyses either

**twiste** failures:

* `twiste+V+Inf` => `twist`
* `twiste` has no analyses either

**ille** failures:

* `ille+V+Inf` => `ill`
* `ille` has no analyses either

**grymje** failures:

* `grymje+V+Inf` does not generate!
* `grymje` has no analyses either

**registrere** failures:

* `registrere+V+Inf` => `registrer`
* `registrere` has no analyses either

**promotere** failures:

* `promotere+V+Inf` => `promoter`
* `promotere` has no analyses either

**unndra** failures:

* `unndra+V+Inf` => `unndrdra`
* `unndra` has no analyses either

**odle** failures:

* `odle+V+Inf` => `odl`
* `odle` has no analyses either

**legge** failures:

* `legge+V+Inf` does not generate!
* `legge` has no analyses either

**slettbygge** failures:

* `slettbygge+V+Inf` does not generate!
* `slettbygge` has no analyses either

**avvike** failures:

* `avvike+V+Inf` => `avvie`
* `avvike+V+Inf` => `avvia`
* `avvike` has no analyses either

**høgvyrde** failures:

* `høgvyrde+V+Inf` does not generate!
* `høgvyrde` has following analyses:
  * `høgvyrd+A+A+Pl+Indef`
  * `høgvyrd+A+A+Sg+Def`
  * `høgvyrd+A+A+Pl+Def`

**måpe** failures:

* `måpe+V+Inf` does not generate!
* `måpe` has no analyses either

**skeike** failures:

* `skeike+V+Inf` => `skeik`
* `skeike` has no analyses either

**immunisere** failures:

* `immunisere+V+Inf` => `immuniser`
* `immunisere` has no analyses either

**traustne** failures:

* `traustne+V+Inf` => `traustn`
* `traustne` has no analyses either

**mismæte** failures:

* `mismæte+V+Inf` => `mismæt`
* `mismæte` has no analyses either

**forvelle** failures:

* `forvelle+V+Inf` does not generate!
* `forvelle` has no analyses either

**meske** failures:

* `meske+V+Inf` => `mesk`
* `meske` has no analyses either

**overdra** failures:

* `overdra+V+Inf` => `overdrdra`
* `overdra` has no analyses either

**hudflengje** failures:

* `hudflengje+V+Inf` does not generate!
* `hudflengje` has no analyses either

**vemle** failures:

* `vemle+V+Inf` => `veml`
* `vemle` has following analyses:
  * `vemlen+A+Neu+Sg+Indef`

**skrøyte** failures:

* `skrøyte+V+Inf` => `skrøyt`
* `skrøyte` has following analyses:
  * `skrøyten+A+Neu+Sg+Indef`

**søtne** failures:

* `søtne+V+Inf` => `søtn`
* `søtne` has no analyses either

**varetake** failures:

* `varetake+V+Inf` => `varettake`
* `varetake+V+Inf` => `varettaka`
* `varetake` has no analyses either

**vinterisolere** failures:

* `vinterisolere+V+Inf` => `vinterisoler`
* `vinterisolere` has no analyses either

**delprivatisere** failures:

* `delprivatisere+V+Inf` => `delprivatiser`
* `delprivatisere` has no analyses either

**overlevere** failures:

* `overlevere+V+Inf` => `overlever`
* `overlevere` has no analyses either

**scripte** failures:

* `scripte+V+Inf` => `script`
* `scripte` has no analyses either

**forsøkje** failures:

* `forsøkje+V+Inf` does not generate!
* `forsøkje` has no analyses either

**ugrasharve** failures:

* `ugrasharve+V+Inf` => `ugrasharv`
* `ugrasharve` has no analyses either

**foliere** failures:

* `foliere+V+Inf` => `folier`
* `foliere` has no analyses either

**krinse** failures:

* `krinse+V+Inf` => `krins`
* `krinse` has no analyses either

**privilegere** failures:

* `privilegere+V+Inf` => `privileger`
* `privilegere` has no analyses either

**kjøgle** failures:

* `kjøgle+V+Inf` => `kjøgl`
* `kjøgle` has no analyses either

**gnafse** failures:

* `gnafse+V+Inf` => `gnafs`
* `gnafse` has no analyses either

**hylskrike** failures:

* `hylskrike+V+Inf` => `hylskria`
* `hylskrike+V+Inf` => `hylskrie`
* `hylskrike` has no analyses either

**drjose** failures:

* `drjose+V+Inf` => `drjoa`
* `drjose+V+Inf` => `drjoe`
* `drjose` has no analyses either

**rose** failures:

* `rose+V+Inf` does not generate!
* `rose` has no analyses either

**ense** failures:

* `ense+V+Inf` does not generate!
* `ense` has no analyses either

**turrtrene** failures:

* `turrtrene+V+Inf` => `turrtren`
* `turrtrene` has no analyses either

**illtrivast** failures:

* `illtrivast+V+Inf` => `illtriast`
* `illtrivast` has no analyses either

**hyrde** failures:

* `hyrde+V+Inf` => `hyrd`
* `hyrde` has no analyses either

**ulme** failures:

* `ulme+V+Inf` => `ulm`
* `ulme` has no analyses either

**skulde** failures:

* `skulde+V+Inf` => `skuld`
* `skulde` has no analyses either

**flambere** failures:

* `flambere+V+Inf` => `flamber`
* `flambere` has no analyses either

**lygast** failures:

* `lygast+V+Inf` => `lyast`
* `lygast` has no analyses either

**påmønstre** failures:

* `påmønstre+V+Inf` => `påmønstr`
* `påmønstre` has no analyses either

**plassere** failures:

* `plassere+V+Inf` => `plasser`
* `plassere` has no analyses either

**varmrøykje** failures:

* `varmrøykje+V+Inf` does not generate!
* `varmrøykje` has no analyses either

**klake** failures:

* `klake+V+Inf` => `klak`
* `klake` has no analyses either

**tave** failures:

* `tave+V+Inf` => `tav`
* `tave` has no analyses either

**kakstryke** failures:

* `kakstryke+V+Inf` => `kakstrye`
* `kakstryke+V+Inf` => `kakstrya`
* `kakstryke` has no analyses either

**sklide** failures:

* `sklide+V+Inf` does not generate!
* `sklide` has no analyses either

**sjanse** failures:

* `sjanse+V+Inf` => `sjans`
* `sjanse` has no analyses either

**proklamere** failures:

* `proklamere+V+Inf` => `proklamer`
* `proklamere` has no analyses either

**hærsette** failures:

* `hærsette+V+Inf` does not generate!
* `hærsette` has no analyses either

**tremulere** failures:

* `tremulere+V+Inf` => `tremuler`
* `tremulere` has no analyses either

**rynke** failures:

* `rynke+V+Inf` => `rynk`
* `rynke` has no analyses either

**sølvleggje** failures:

* `sølvleggje+V+Inf` does not generate!
* `sølvleggje` has no analyses either

**eksperimentere** failures:

* `eksperimentere+V+Inf` => `eksperimenter`
* `eksperimentere` has no analyses either

**inventere** failures:

* `inventere+V+Inf` => `inventer`
* `inventere` has no analyses either

**sementere** failures:

* `sementere+V+Inf` => `sementer`
* `sementere` has no analyses either

**sentralisere** failures:

* `sentralisere+V+Inf` => `sentraliser`
* `sentralisere` has no analyses either

**dreie** failures:

* `dreie+V+Inf` does not generate!
* `dreie` has no analyses either

**guide** failures:

* `guide+V+Inf` => `guid`
* `guide` has no analyses either

**estimere** failures:

* `estimere+V+Inf` => `estimer`
* `estimere` has no analyses either

**fortenke** failures:

* `fortenke+V+Inf` => `fortenk`
* `fortenke` has no analyses either

**skyrpe** failures:

* `skyrpe+V+Inf` => `skyrp`
* `skyrpe` has no analyses either

**meine** failures:

* `meine+V+Inf` => `mein`
* `meine` has following analyses:
  * `mein+A+Sg+Def`
  * `mein+A+Pl+Indef`

**speide** failures:

* `speide+V+Inf` => `speid`
* `speide` has no analyses either

**søkkje** failures:

* `søkkje+V+Inf` does not generate!
* `søkkje` has no analyses either

**pukkleggje** failures:

* `pukkleggje+V+Inf` does not generate!
* `pukkleggje` has no analyses either

**kvakle** failures:

* `kvakle+V+Inf` => `kvakl`
* `kvakle` has no analyses either

**fuge** failures:

* `fuge+V+Inf` => `fug`
* `fuge` has no analyses either

**smeikje** failures:

* `smeikje+V+Inf` does not generate!
* `smeikje` has no analyses either

**lende** failures:

* `lende+V+Inf` => `lend`
* `lende` has no analyses either

**kisle** failures:

* `kisle+V+Inf` => `kisl`
* `kisle` has no analyses either

**knase** failures:

* `knase+V+Inf` => `knas`
* `knase` has following analyses:
  * `knas+A+Sg+Def`
  * `knas+A+Pl+Indef`

**skolle** failures:

* `skolle+V+Inf` does not generate!
* `skolle` has no analyses either

**drapere** failures:

* `drapere+V+Inf` => `draper`
* `drapere` has no analyses either

**gine** failures:

* `gine+V+Inf` does not generate!
* `gine` has no analyses either

**skòre** failures:

* `skòre+V+Inf` => `skòr`
* `skòre` has no analyses either

**trivast** failures:

* `trivast+V+Inf` => `triast`
* `trivast` has no analyses either

**blekte** failures:

* `blekte+V+Inf` => `blekt`
* `blekte` has no analyses either

**gneste** failures:

* `gneste+V+Inf` => `gnee`
* `gneste+V+Inf` => `gnea`
* `gneste` has no analyses either

**undereksponere** failures:

* `undereksponere+V+Inf` => `undereksponer`
* `undereksponere` has no analyses either

**beiskne** failures:

* `beiskne+V+Inf` => `beiskn`
* `beiskne` has no analyses either

**veikne** failures:

* `veikne+V+Inf` => `veikn`
* `veikne` has no analyses either

**bendsle** failures:

* `bendsle+V+Inf` => `bendsl`
* `bendsle` has no analyses either

**sippe** failures:

* `sippe+V+Inf` => `sipp`
* `sippe` has no analyses either

**nitrere** failures:

* `nitrere+V+Inf` => `nitrer`
* `nitrere` has no analyses either

**sylte** failures:

* `sylte+V+Inf` => `sylt`
* `sylte` has no analyses either

**prange** failures:

* `prange+V+Inf` => `prang`
* `prange` has no analyses either

**klappe** failures:

* `klappe+V+Inf` => `klapp`
* `klappe` has no analyses either

**vidareutvikle** failures:

* `vidareutvikle+V+Inf` => `vidareutvikl`
* `vidareutvikle` has no analyses either

**rehabilitere** failures:

* `rehabilitere+V+Inf` => `rehabiliter`
* `rehabilitere` has no analyses either

**fosforescere** failures:

* `fosforescere+V+Inf` => `fosforescer`
* `fosforescere` has no analyses either

**fastlegge** failures:

* `fastlegge+V+Inf` does not generate!
* `fastlegge` has no analyses either

**stime** failures:

* `stime+V+Inf` => `stim`
* `stime` has no analyses either

**glyggje** failures:

* `glyggje+V+Inf` does not generate!
* `glyggje` has no analyses either

**innføre** failures:

* `innføre+V+Inf` => `innfør`
* `innføre` has no analyses either

**davre** failures:

* `davre+V+Inf` => `davr`
* `davre` has no analyses either

**ulagast** failures:

* `ulagast+V+Inf` => `ulagastast`
* `ulagast` has no analyses either

**kvitre** failures:

* `kvitre+V+Inf` => `kvitr`
* `kvitre` has no analyses either

**moderere** failures:

* `moderere+V+Inf` => `moderer`
* `moderere` has no analyses either

**nafse** failures:

* `nafse+V+Inf` => `nafs`
* `nafse` has no analyses either

**nedætte** failures:

* `nedætte+V+Inf` => `nedætt`
* `nedætte` has no analyses either

**omplassere** failures:

* `omplassere+V+Inf` => `omplasser`
* `omplassere` has no analyses either

**bøye** failures:

* `bøye+V+Inf` does not generate!
* `bøye` has no analyses either

**visle** failures:

* `visle+V+Inf` => `visl`
* `visle` has no analyses either

**svovle** failures:

* `svovle+V+Inf` => `svovl`
* `svovle` has no analyses either

**kysse** failures:

* `kysse+V+Inf` does not generate!
* `kysse` has no analyses either

**mitigere** failures:

* `mitigere+V+Inf` => `mitiger`
* `mitigere` has no analyses either

**reagere** failures:

* `reagere+V+Inf` => `reager`
* `reagere` has no analyses either

**kryss-stille** failures:

* `kryss-stille+V+Inf` does not generate!
* `kryss-stille` has no analyses either

**tøe** failures:

* `tøe+V+Inf` => `tø`
* `tøe` has no analyses either

**stusse** failures:

* `stusse+V+Inf` => `stuss`
* `stusse` has no analyses either

**sine** failures:

* `sine+V+Inf` => `sin`
* `sine` has following analyses:
  * `sine+Det+Poss+Pl`
  * `sige+V+Adj+Bu+Eint`
  * `sige+V+Adj+Fl`

**utbetre** failures:

* `utbetre+V+Inf` => `utbetr`
* `utbetre` has no analyses either

**flimre** failures:

* `flimre+V+Inf` => `flimr`
* `flimre` has no analyses either

**kable** failures:

* `kable+V+Inf` => `kabl`
* `kable` has no analyses either

**halde** failures:

* `halde+V+Inf` => `haa`
* `halde+V+Inf` => `hae`
* `halde` has following analyses:
  * `halden+A+Neu+Sg+Indef`

**martre** failures:

* `martre+V+Inf` => `martr`
* `martre` has no analyses either

**motsette** failures:

* `motsette+V+Inf` does not generate!
* `motsette` has following analyses:
  * `motsett+A+A+Sg+Def`
  * `motsett+A+A+Pl+Def`
  * `motsett+A+A+Pl+Indef`

**skurdtreske** failures:

* `skurdtreske+V+Inf` => `skurdtresk`
* `skurdtreske` has no analyses either

**skru** failures:

* `skru+V+Inf` does not generate!
* `skru` has following analyses:
  * `skru+N`
  * `skrue+V`
  * `skrue+N+N+Msc+Sg+Indef`
  * `skru+N+N+Msc+Sg+Indef`
  * `skru+V`

**forgreine** failures:

* `forgreine+V+Inf` => `forgrein`
* `forgreine` has no analyses either

**myrde** failures:

* `myrde+V+Inf` => `myrd`
* `myrde` has no analyses either

**herpesere** failures:

* `herpesere+V+Inf` => `herpeser`
* `herpesere` has no analyses either

**avduke** failures:

* `avduke+V+Inf` => `avduk`
* `avduke` has no analyses either

**frifinne** failures:

* `frifinne+V+Inf` => `frifie`
* `frifinne+V+Inf` => `frifia`
* `frifinne` has no analyses either

**gravfeste** failures:

* `gravfeste+V+Inf` does not generate!
* `gravfeste` has no analyses either

**reste** failures:

* `reste+V+Inf` => `rest`
* `reste` has no analyses either

**svalke** failures:

* `svalke+V+Inf` => `svalk`
* `svalke` has no analyses either

**eggje** failures:

* `eggje+V+Inf` => `egg`
* `eggje` has no analyses either

**gravere** failures:

* `gravere+V+Inf` => `graver`
* `gravere` has no analyses either

**klandre** failures:

* `klandre+V+Inf` => `klandr`
* `klandre` has no analyses either

**natte** failures:

* `natte+V+Inf` => `natt`
* `natte` has no analyses either

**forbløde** failures:

* `forbløde+V+Inf` does not generate!
* `forbløde` has no analyses either

**findele** failures:

* `findele+V+Inf` => `findel`
* `findele` has no analyses either

**reassumere** failures:

* `reassumere+V+Inf` => `reassumer`
* `reassumere` has no analyses either

**falne** failures:

* `falne+V+Inf` => `faln`
* `falne` has following analyses:
  * `fallen+A+Sg+Def`
  * `fallen+A+Pl+Indef`

**kvese** failures:

* `kvese+V+Inf` => `kves`
* `kvese` has no analyses either

**gråte** failures:

* `gråte+V+Inf` => `gråe`
* `gråte+V+Inf` => `gråa`
* `gråte` has no analyses either

**forstille** failures:

* `forstille+V+Inf` => `forstile`
* `forstille+V+Inf` => `forstila`
* `forstille` has no analyses either

**fredsmøte** failures:

* `fredsmøte+V+Inf` => `fredsmøt`
* `fredsmøte` has no analyses either

**tynsle** failures:

* `tynsle+V+Inf` => `tynsl`
* `tynsle` has no analyses either

**avvæpne** failures:

* `avvæpne+V+Inf` => `avvæpn`
* `avvæpne` has no analyses either

**spikre** failures:

* `spikre+V+Inf` => `spikr`
* `spikre` has no analyses either

**skurre** failures:

* `skurre+V+Inf` => `skurr`
* `skurre` has no analyses either

**uttrykkje** failures:

* `uttrykkje+V+Inf` does not generate!
* `uttrykkje` has no analyses either

**slamse** failures:

* `slamse+V+Inf` => `slams`
* `slamse` has no analyses either

**depravere** failures:

* `depravere+V+Inf` => `depraver`
* `depravere` has no analyses either

**stridregne** failures:

* `stridregne+V+Inf` does not generate!
* `stridregne` has no analyses either

**gneistre** failures:

* `gneistre+V+Inf` => `gneistr`
* `gneistre` has no analyses either

**nære** failures:

* `nære+V+Inf` does not generate!
* `nære` has following analyses:
  * `nær+A+Pl+Indef`
  * `nær+A+Sg+Def`

**grimeleie** failures:

* `grimeleie+V+Inf` does not generate!
* `grimeleie` has no analyses either

**seile** failures:

* `seile+V+Inf` => `seil`
* `seile` has no analyses either

**veivle** failures:

* `veivle+V+Inf` => `veivl`
* `veivle` has no analyses either

**autoklavere** failures:

* `autoklavere+V+Inf` => `autoklaver`
* `autoklavere` has no analyses either

**handheve** failures:

* `handheve+V+Inf` => `handhev`
* `handheve` has no analyses either

**skitne** failures:

* `skitne+V+Inf` => `skitn`
* `skitne` has following analyses:
  * `skiten+A+Pl+Indef`
  * `skitten+A+Pl+Indef`
  * `skiten+A+Sg+Def`
  * `skitten+A+Sg+Def`

**fråtse** failures:

* `fråtse+V+Inf` => `fråts`
* `fråtse` has no analyses either

**skyfle** failures:

* `skyfle+V+Inf` => `skyfl`
* `skyfle` has no analyses either

**utferde** failures:

* `utferde+V+Inf` => `utferd`
* `utferde` has no analyses either

**tevje** failures:

* `tevje+V+Inf` => `tevj`
* `tevje` has no analyses either

**krikle** failures:

* `krikle+V+Inf` => `krikl`
* `krikle` has no analyses either

**prioritere** failures:

* `prioritere+V+Inf` => `prioriter`
* `prioritere` has no analyses either

**flangse** failures:

* `flangse+V+Inf` => `flangs`
* `flangse` has no analyses either

**førehandstinge** failures:

* `førehandstinge+V+Inf` => `førehandsting`
* `førehandstinge` has no analyses either

**raffinere** failures:

* `raffinere+V+Inf` => `raffiner`
* `raffinere` has no analyses either

**sløvne** failures:

* `sløvne+V+Inf` => `sløvn`
* `sløvne` has no analyses either

**rydje** failures:

* `rydje+V+Inf` does not generate!
* `rydje` has no analyses either

**røyne** failures:

* `røyne+V+Inf` does not generate!
* `røyne` has no analyses either

**drasjere** failures:

* `drasjere+V+Inf` => `drasjer`
* `drasjere` has no analyses either

**smette** failures:

* `smette+V+Inf` does not generate!
* `smette` has no analyses either

**svelle** failures:

* `svelle+V+Inf` => `svea`
* `svelle+V+Inf` => `svee`
* `svelle` has no analyses either

**renge** failures:

* `renge+V+Inf` => `reng`
* `renge` has no analyses either

**heie** failures:

* `heie+V+Inf` => `hei`
* `heie` has following analyses:
  * `hei+A+Sg+Def`
  * `hei+A+Pl+Indef`

**orvonast** failures:

* `orvonast+V+Inf` => `orvonastast`
* `orvonast` has no analyses either

**gynge** failures:

* `gynge+V+Inf` => `gyng`
* `gynge` has no analyses either

**vikke** failures:

* `vikke+V+Inf` => `vikk`
* `vikke` has no analyses either

**dolke** failures:

* `dolke+V+Inf` => `dolk`
* `dolke` has no analyses either

**dabbe** failures:

* `dabbe+V+Inf` => `dabb`
* `dabbe` has no analyses either

**mobbe** failures:

* `mobbe+V+Inf` => `mobb`
* `mobbe` has no analyses either

**geime** failures:

* `geime+V+Inf` => `geim`
* `geime` has no analyses either

**stekkje** failures:

* `stekkje+V+Inf` does not generate!
* `stekkje` has no analyses either

**stadfeste** failures:

* `stadfeste+V+Inf` does not generate!
* `stadfeste` has no analyses either

**påby** failures:

* `påby+V+Inf` does not generate!
* `påby` has no analyses either

**habilitere** failures:

* `habilitere+V+Inf` => `habiliter`
* `habilitere` has no analyses either

**paragrafere** failures:

* `paragrafere+V+Inf` => `paragrafer`
* `paragrafere` has no analyses either

**skildre** failures:

* `skildre+V+Inf` => `skildr`
* `skildre` has no analyses either

**skvipe** failures:

* `skvipe+V+Inf` => `skvip`
* `skvipe` has no analyses either

**tøde** failures:

* `tøde+V+Inf` => `tød`
* `tøde` has no analyses either

**gråle** failures:

* `gråle+V+Inf` => `grål`
* `gråle` has no analyses either

**privatisere** failures:

* `privatisere+V+Inf` => `privatiser`
* `privatisere` has no analyses either

**rutsje** failures:

* `rutsje+V+Inf` => `rutsj`
* `rutsje` has no analyses either

**forlyfte** failures:

* `forlyfte+V+Inf` does not generate!
* `forlyfte` has no analyses either

**rusthandsame** failures:

* `rusthandsame+V+Inf` => `rusthandsam`
* `rusthandsame` has no analyses either

**skare** failures:

* `skare+V+Inf` => `skar`
* `skare` has no analyses either

**overrekke** failures:

* `overrekke+V+Inf` does not generate!
* `overrekke` has no analyses either

**samhandle** failures:

* `samhandle+V+Inf` => `samhandl`
* `samhandle` has no analyses either

**klatre** failures:

* `klatre+V+Inf` => `klatr`
* `klatre` has no analyses either

**famnta** failures:

* `famnta+V+Inf` => `famntta`
* `famnta` has no analyses either

**lafte** failures:

* `lafte+V+Inf` => `laft`
* `lafte` has no analyses either

**kjemme** failures:

* `kjemme+V+Inf` does not generate!
* `kjemme` has no analyses either

**slapse** failures:

* `slapse+V+Inf` => `slaps`
* `slapse` has no analyses either

**bæsje** failures:

* `bæsje+V+Inf` => `bæsj`
* `bæsje` has no analyses either

**fjampe** failures:

* `fjampe+V+Inf` => `fjamp`
* `fjampe` has following analyses:
  * `fjampen+A+Neu+Sg+Indef`

**revansjere** failures:

* `revansjere+V+Inf` => `revansjer`
* `revansjere` has no analyses either

**skunde** failures:

* `skunde+V+Inf` => `skund`
* `skunde` has no analyses either

**øyde** failures:

* `øyde+V+Inf` does not generate!
* `øyde` has following analyses:
  * `øyden+A+Neu+Sg+Indef`

**underdrive** failures:

* `underdrive+V+Inf` => `underdria`
* `underdrive+V+Inf` => `underdrie`
* `underdrive` has no analyses either

**angjeve** failures:

* `angjeve+V+Inf` => `anggjeva`
* `angjeve+V+Inf` => `anggjeve`
* `angjeve` has no analyses either

**handfakte** failures:

* `handfakte+V+Inf` => `handfakt`
* `handfakte` has no analyses either

**bate** failures:

* `bate+V+Inf` => `bat`
* `bate` has no analyses either

**tuske** failures:

* `tuske+V+Inf` => `tusk`
* `tuske` has following analyses:
  * `tusk+A+Sg+Def`
  * `tusk+A+Pl+Indef`

**svirre** failures:

* `svirre+V+Inf` => `svirr`
* `svirre` has no analyses either

**bynje** failures:

* `bynje+V+Inf` does not generate!
* `bynje` has no analyses either

**fikse** failures:

* `fikse+V+Inf` => `fiks`
* `fikse` has following analyses:
  * `fiks+A+Sg+Def`
  * `fiks+A+Pl+Indef`

**grade** failures:

* `grade+V+Inf` => `grad`
* `grade` has no analyses either

**faite** failures:

* `faite+V+Inf` => `fait`
* `faite` has no analyses either

**insinuere** failures:

* `insinuere+V+Inf` => `insinuer`
* `insinuere` has no analyses either

**ulukketrygde** failures:

* `ulukketrygde+V+Inf` => `ulukketrygd`
* `ulukketrygde` has no analyses either

**døyve** failures:

* `døyve+V+Inf` does not generate!
* `døyve` has no analyses either

**sålast** failures:

* `sålast+V+Inf` => `sålastast`
* `sålast` has no analyses either

**vrengle** failures:

* `vrengle+V+Inf` => `vrengl`
* `vrengle` has no analyses either

**gravitere** failures:

* `gravitere+V+Inf` => `graviter`
* `gravitere` has no analyses either

**spalte** failures:

* `spalte+V+Inf` => `spalt`
* `spalte` has no analyses either

**godtale** failures:

* `godtale+V+Inf` does not generate!
* `godtale` has no analyses either

**trysje** failures:

* `trysje+V+Inf` does not generate!
* `trysje` has no analyses either

**dangle** failures:

* `dangle+V+Inf` => `dangl`
* `dangle` has no analyses either

**resolvere** failures:

* `resolvere+V+Inf` => `resolver`
* `resolvere` has no analyses either

**forete** failures:

* `forete+V+Inf` => `foree`
* `forete+V+Inf` => `forea`
* `forete` has following analyses:
  * `foreten+A+Neu+Sg+Indef`

**karve** failures:

* `karve+V+Inf` => `karv`
* `karve` has no analyses either

**håke** failures:

* `håke+V+Inf` => `håk`
* `håke` has no analyses either

**honke** failures:

* `honke+V+Inf` => `honk`
* `honke` has no analyses either

**turrete** failures:

* `turrete+V+Inf` => `turrea`
* `turrete+V+Inf` => `turree`
* `turrete` has no analyses either

**forskjere** failures:

* `forskjere+V+Inf` => `forskjea`
* `forskjere+V+Inf` => `forskjee`
* `forskjere` has no analyses either

**vegetere** failures:

* `vegetere+V+Inf` => `vegeter`
* `vegetere` has no analyses either

**ladde** failures:

* `ladde+V+Inf` => `ladd`
* `ladde` has following analyses:
  * `la+V+Ind+Prt`

**avkolonisere** failures:

* `avkolonisere+V+Inf` => `avkoloniser`
* `avkolonisere` has no analyses either

**sjokkhandsame** failures:

* `sjokkhandsame+V+Inf` => `sjokkhandsam`
* `sjokkhandsame` has no analyses either

**slanke** failures:

* `slanke+V+Inf` => `slank`
* `slanke` has following analyses:
  * `slank+A+Pl+Indef`
  * `slank+A+Sg+Def`

**fakte** failures:

* `fakte+V+Inf` => `fakt`
* `fakte` has no analyses either

**realitetsdrøfte** failures:

* `realitetsdrøfte+V+Inf` does not generate!
* `realitetsdrøfte` has no analyses either

**svinge** failures:

* `svinge+V+Inf` => `sving`
* `svinge` has no analyses either

**kvaste** failures:

* `kvaste+V+Inf` => `kvast`
* `kvaste` has no analyses either

**tvangsinnleggje** failures:

* `tvangsinnleggje+V+Inf` does not generate!
* `tvangsinnleggje` has no analyses either

**bedømme** failures:

* `bedømme+V+Inf` does not generate!
* `bedømme` has no analyses either

**vrine** failures:

* `vrine+V+Inf` => `vrie`
* `vrine+V+Inf` => `vria`
* `vrine` has following analyses:
  * `vrine+V+Adj+Fl`
  * `vrien+A+Sg+Def`
  * `vrien+A+Pl+Indef`
  * `vrine+V+Adj+Bu+Eint`

**sympatistreike** failures:

* `sympatistreike+V+Inf` => `sympatistreik`
* `sympatistreike` has no analyses either

**hangle** failures:

* `hangle+V+Inf` => `hangl`
* `hangle` has no analyses either

**halme** failures:

* `halme+V+Inf` => `halm`
* `halme` has no analyses either

**dulme** failures:

* `dulme+V+Inf` => `dulm`
* `dulme` has following analyses:
  * `dulmen+A+Neu+Sg+Indef`

**barsle** failures:

* `barsle+V+Inf` => `barsl`
* `barsle` has no analyses either

**blegge** failures:

* `blegge+V+Inf` does not generate!
* `blegge` has no analyses either

**illudere** failures:

* `illudere+V+Inf` => `illuder`
* `illudere` has no analyses either

**brote** failures:

* `brote+V+Inf` => `brot`
* `brote` has following analyses:
  * `broten+A+Neu+Sg+Indef`

**fjerne** failures:

* `fjerne+V+Inf` => `fjern`
* `fjerne` has following analyses:
  * `fjern+A+Sg+Def`
  * `fjern+A+Pl+Indef`

**forsure** failures:

* `forsure+V+Inf` => `forsur`
* `forsure` has no analyses either

**pantsette** failures:

* `pantsette+V+Inf` does not generate!
* `pantsette` has no analyses either

**røpe** failures:

* `røpe+V+Inf` => `røp`
* `røpe` has no analyses either

**dalte** failures:

* `dalte+V+Inf` => `dalt`
* `dalte` has no analyses either

**gnarre** failures:

* `gnarre+V+Inf` => `gnarr`
* `gnarre` has no analyses either

**forkjøpe** failures:

* `forkjøpe+V+Inf` => `forkjøp`
* `forkjøpe` has no analyses either

**benkje** failures:

* `benkje+V+Inf` => `benk`
* `benkje` has no analyses either

**dratle** failures:

* `dratle+V+Inf` => `dratl`
* `dratle` has no analyses either

**slokne** failures:

* `slokne+V+Inf` => `slokn`
* `slokne` has no analyses either

**forvildre** failures:

* `forvildre+V+Inf` => `forvildr`
* `forvildre` has no analyses either

**vinterfôre** failures:

* `vinterfôre+V+Inf` => `vinterfôr`
* `vinterfôre` has no analyses either

**bolne** failures:

* `bolne+V+Inf` => `boln`
* `bolne` has following analyses:
  * `bolen+A+Pl+Indef`
  * `bolen+A+Sg+Def`

**spolere** failures:

* `spolere+V+Inf` => `spoler`
* `spolere` has no analyses either

**utlikne** failures:

* `utlikne+V+Inf` => `utlikn`
* `utlikne` has no analyses either

**sjartre** failures:

* `sjartre+V+Inf` => `sjartr`
* `sjartre` has no analyses either

**kvitte** failures:

* `kvitte+V+Inf` => `kvitt`
* `kvitte` has following analyses:
  * `kvitt+A+A+Pl+Indef`
  * `kvitt+A+A+Sg+Def`
  * `kvitt+A+A+Pl+Def`

**dulpe** failures:

* `dulpe+V+Inf` => `dulp`
* `dulpe` has no analyses either

**sjakle** failures:

* `sjakle+V+Inf` => `sjakl`
* `sjakle` has no analyses either

**mulktere** failures:

* `mulktere+V+Inf` => `mulkter`
* `mulktere` has no analyses either

**mistrivast** failures:

* `mistrivast+V+Inf` => `mistriast`
* `mistrivast` has no analyses either

**surre** failures:

* `surre+V+Inf` => `surr`
* `surre` has no analyses either

**ettervise** failures:

* `ettervise+V+Inf` => `ettervis`
* `ettervise` has no analyses either

**desimere** failures:

* `desimere+V+Inf` => `desimer`
* `desimere` has no analyses either

**berkje** failures:

* `berkje+V+Inf` does not generate!
* `berkje` has no analyses either

**kreiste** failures:

* `kreiste+V+Inf` => `kreist`
* `kreiste` has no analyses either

**tjatre** failures:

* `tjatre+V+Inf` => `tjatr`
* `tjatre` has no analyses either

**følje** failures:

* `følje+V+Inf` => `følj`
* `følje` has no analyses either

**subtrahere** failures:

* `subtrahere+V+Inf` => `subtraher`
* `subtrahere` has no analyses either

**skjelle** failures:

* `skjelle+V+Inf` => `sk`
* `skjelle` has no analyses either

**forandre** failures:

* `forandre+V+Inf` => `forandr`
* `forandre` has no analyses either

**tåkeleggje** failures:

* `tåkeleggje+V+Inf` does not generate!
* `tåkeleggje` has no analyses either

**fortjone** failures:

* `fortjone+V+Inf` => `fortjon`
* `fortjone` has no analyses either

**herkje** failures:

* `herkje+V+Inf` does not generate!
* `herkje` has no analyses either

**finlese** failures:

* `finlese+V+Inf` => `finlea`
* `finlese+V+Inf` => `finlee`
* `finlese` has no analyses either

**remje** failures:

* `remje+V+Inf` => `remj`
* `remje` has no analyses either

**tipse** failures:

* `tipse+V+Inf` => `tips`
* `tipse` has no analyses either

**debarkere** failures:

* `debarkere+V+Inf` => `debarker`
* `debarkere` has no analyses either

**kremte** failures:

* `kremte+V+Inf` => `kremt`
* `kremte` has no analyses either

**skråve** failures:

* `skråve+V+Inf` => `skråv`
* `skråve` has no analyses either

**sidke** failures:

* `sidke+V+Inf` => `sidk`
* `sidke` has no analyses either

**framleige** failures:

* `framleige+V+Inf` does not generate!
* `framleige` has following analyses:
  * `framleige+N+Fem+Sg+Indef`

**flattere** failures:

* `flattere+V+Inf` => `flatter`
* `flattere` has no analyses either

**røynleggjere** failures:

* `røynleggjere+V+Inf` does not generate!
* `røynleggjere` has no analyses either

**andvarpe** failures:

* `andvarpe+V+Inf` => `andvarp`
* `andvarpe` has no analyses either

**neise** failures:

* `neise+V+Inf` => `neis`
* `neise` has no analyses either

**dryppturke** failures:

* `dryppturke+V+Inf` => `dryppturk`
* `dryppturke` has no analyses either

**mule** failures:

* `mule+V+Inf` => `mul`
* `mule` has no analyses either

**veime** failures:

* `veime+V+Inf` => `veim`
* `veime` has no analyses either

**flukte** failures:

* `flukte+V+Inf` => `flukt`
* `flukte` has no analyses either

**krone** failures:

* `krone+V+Inf` => `kron`
* `krone` has following analyses:
  * `krype+V+Adj+Fl`
  * `krype+V+Adj+Bu+Eint`

**skjene** failures:

* `skjene+V+Inf` => `skjen`
* `skjene` has no analyses either

**overtrekke** failures:

* `overtrekke+V+Inf` does not generate!
* `overtrekke` has no analyses either

**leske** failures:

* `leske+V+Inf` => `lesk`
* `leske` has no analyses either

**systematisere** failures:

* `systematisere+V+Inf` => `systematiser`
* `systematisere` has no analyses either

**vete** failures:

* `vete+V+Inf` => `vveta`
* `vete+V+Inf` => `vvete`
* `vete` has no analyses either

**fraternisere** failures:

* `fraternisere+V+Inf` => `fraterniser`
* `fraternisere` has no analyses either

**snurle** failures:

* `snurle+V+Inf` => `snurl`
* `snurle` has no analyses either

**dresse** failures:

* `dresse+V+Inf` => `dress`
* `dresse` has no analyses either

**rukke** failures:

* `rukke+V+Inf` => `rukk`
* `rukke` has no analyses either

**krage** failures:

* `krage+V+Inf` => `krag`
* `krage` has no analyses either

**krige** failures:

* `krige+V+Inf` => `krig`
* `krige` has no analyses either

**kapunere** failures:

* `kapunere+V+Inf` => `kapuner`
* `kapunere` has no analyses either

**sprengje** failures:

* `sprengje+V+Inf` does not generate!
* `sprengje` has no analyses either

**nedgradere** failures:

* `nedgradere+V+Inf` => `nedgrader`
* `nedgradere` has no analyses either

**skrante** failures:

* `skrante+V+Inf` => `skrant`
* `skrante` has following analyses:
  * `skranten+A+Neu+Sg+Indef`

**dalge** failures:

* `dalge+V+Inf` => `dalg`
* `dalge` has no analyses either

**spante** failures:

* `spante+V+Inf` => `spant`
* `spante` has no analyses either

**bleikje** failures:

* `bleikje+V+Inf` does not generate!
* `bleikje` has no analyses either

**jåle** failures:

* `jåle+V+Inf` does not generate!
* `jåle` has no analyses either

**kreke** failures:

* `kreke+V+Inf` => `kree`
* `kreke+V+Inf` => `krea`
* `kreke` has no analyses either

**deggje** failures:

* `deggje+V+Inf` does not generate!
* `deggje` has no analyses either

**samse** failures:

* `samse+V+Inf` => `sams`
* `samse` has following analyses:
  * `sams+A+A+Pl+Def`
  * `sams+A+A+Pl+Indef`
  * `sams+A+A+Sg+Def`

**vøle** failures:

* `vøle+V+Inf` => `vøl`
* `vøle` has no analyses either

**aluminere** failures:

* `aluminere+V+Inf` => `aluminer`
* `aluminere` has no analyses either

**smyge** failures:

* `smyge+V+Inf` => `smya`
* `smyge+V+Inf` => `smye`
* `smyge` has no analyses either

**tose** failures:

* `tose+V+Inf` => `tos`
* `tose` has following analyses:
  * `tossen+A+Neu+Sg+Indef`
  * `tosen+A+Neu+Sg+Indef`

**skunkle** failures:

* `skunkle+V+Inf` => `skunkl`
* `skunkle` has no analyses either

**rundsnakke** failures:

* `rundsnakke+V+Inf` => `rundsnakk`
* `rundsnakke` has no analyses either

**ansvarstrygde** failures:

* `ansvarstrygde+V+Inf` => `ansvarstrygd`
* `ansvarstrygde` has no analyses either

**kitle** failures:

* `kitle+V+Inf` => `kitl`
* `kitle` has following analyses:
  * `kitlen+A+Neu+Sg+Indef`

**nøytralisere** failures:

* `nøytralisere+V+Inf` => `nøytraliser`
* `nøytralisere` has no analyses either

**pløse** failures:

* `pløse+V+Inf` => `pløs`
* `pløse` has following analyses:
  * `pløsen+A+Neu+Sg+Indef`

**manifestere** failures:

* `manifestere+V+Inf` => `manifester`
* `manifestere` has no analyses either

**fordyre** failures:

* `fordyre+V+Inf` => `fordyr`
* `fordyre` has no analyses either

**filme** failures:

* `filme+V+Inf` => `film`
* `filme` has no analyses either

**hardtrene** failures:

* `hardtrene+V+Inf` => `hardtren`
* `hardtrene` has no analyses either

**modnast** failures:

* `modnast+V+Inf` => `modnastast`
* `modnast` has following analyses:
  * `moden+A+Superl+Indef`

**slegge** failures:

* `slegge+V+Inf` => `slegg`
* `slegge` has no analyses either

**føne** failures:

* `føne+V+Inf` does not generate!
* `føne` has no analyses either

**duste** failures:

* `duste+V+Inf` => `dust`
* `duste` has following analyses:
  * `duse+V+PrfPrc`

**sverme** failures:

* `sverme+V+Inf` => `sverm`
* `sverme` has no analyses either

**stette** failures:

* `stette+V+Inf` => `stett`
* `stette` has no analyses either

**forsake** failures:

* `forsake+V+Inf` => `forsak`
* `forsake` has no analyses either

**dampkoke** failures:

* `dampkoke+V+Inf` => `dampkok`
* `dampkoke` has no analyses either

**nasjonalisere** failures:

* `nasjonalisere+V+Inf` => `nasjonaliser`
* `nasjonalisere` has no analyses either

**avdøme** failures:

* `avdøme+V+Inf` does not generate!
* `avdøme` has no analyses either

**kjøtte** failures:

* `kjøtte+V+Inf` does not generate!
* `kjøtte` has following analyses:
  * `kjøte+V+PrfPrc`

**skivje** failures:

* `skivje+V+Inf` => `skivj`
* `skivje` has no analyses either

**logre** failures:

* `logre+V+Inf` => `logr`
* `logre` has no analyses either

**snake** failures:

* `snake+V+Inf` => `snak`
* `snake` has following analyses:
  * `snak+A+Pl+Indef`
  * `snak+A+Sg+Def`

**parasittere** failures:

* `parasittere+V+Inf` => `parasitter`
* `parasittere` has no analyses either

**by** failures:

* `by+V+Inf` does not generate!
* `by` has following analyses:
  * `bye+V+Imprt`
  * `by+N+N+Msc+Sg+Indef`
  * `bye+N+N+Fem+Sg+Indef`
  * `bye+V+Inf`

**antikrympebehandle** failures:

* `antikrympebehandle+V+Inf` => `antikrympebehandl`
* `antikrympebehandle` has no analyses either

**bende** failures:

* `bende+V+Inf` does not generate!
* `bende` has following analyses:
  * `benden+A+Neu+Sg+Indef`

**herske** failures:

* `herske+V+Inf` => `hersk`
* `herske` has no analyses either

**skryte** failures:

* `skryte+V+Inf` => `skryt`
* `skryte` has following analyses:
  * `skryten+A+Neu+Sg+Indef`

**dekke** failures:

* `dekke+V+Inf` does not generate!
* `dekke` has no analyses either

**tvisteikje** failures:

* `tvisteikje+V+Inf` does not generate!
* `tvisteikje` has no analyses either

**prikke** failures:

* `prikke+V+Inf` => `prikk`
* `prikke` has no analyses either

**jukke** failures:

* `jukke+V+Inf` => `jukk`
* `jukke` has no analyses either

**blidgjere** failures:

* `blidgjere+V+Inf` does not generate!
* `blidgjere` has no analyses either

**auksjonere** failures:

* `auksjonere+V+Inf` => `auksjoner`
* `auksjonere` has no analyses either

**darte** failures:

* `darte+V+Inf` => `dart`
* `darte` has no analyses either

**drasse** failures:

* `drasse+V+Inf` => `drass`
* `drasse` has no analyses either

**tøvre** failures:

* `tøvre+V+Inf` => `tøvr`
* `tøvre` has no analyses either

**internasjonalisere** failures:

* `internasjonalisere+V+Inf` => `internasjonaliser`
* `internasjonalisere` has no analyses either

**lokalbedøve** failures:

* `lokalbedøve+V+Inf` does not generate!
* `lokalbedøve` has no analyses either

**gidde** failures:

* `gidde+V+Inf` => `gidd`
* `gidde` has following analyses:
  * `gidd+A+Pl+Indef`
  * `gidd+A+Sg+Def`

**rappe** failures:

* `rappe+V+Inf` => `rapp`
* `rappe` has no analyses either

**fjetre** failures:

* `fjetre+V+Inf` => `fjetr`
* `fjetre` has no analyses either

**attgjeve** failures:

* `attgjeve+V+Inf` => `attggjeve`
* `attgjeve+V+Inf` => `attggjeva`
* `attgjeve` has following analyses:
  * `attgjeven+A+Neu+Sg+Indef`

**gjøne** failures:

* `gjøne+V+Inf` => `gjøn`
* `gjøne` has no analyses either

**sangle** failures:

* `sangle+V+Inf` => `sangl`
* `sangle` has following analyses:
  * `sanglen+A+Neu+Sg+Indef`

**kriminalisere** failures:

* `kriminalisere+V+Inf` => `kriminaliser`
* `kriminalisere` has no analyses either

**suggerere** failures:

* `suggerere+V+Inf` => `suggerer`
* `suggerere` has no analyses either

**tolle** failures:

* `tolle+V+Inf` => `toll`
* `tolle` has no analyses either

**sprette** failures:

* `sprette+V+Inf` does not generate!
* `sprette` has no analyses either

**flakre** failures:

* `flakre+V+Inf` => `flakr`
* `flakre` has no analyses either

**klikke** failures:

* `klikke+V+Inf` => `klikk`
* `klikke` has no analyses either

**gøyme** failures:

* `gøyme+V+Inf` does not generate!
* `gøyme` has no analyses either

**akademisere** failures:

* `akademisere+V+Inf` => `akademiser`
* `akademisere` has no analyses either

**kinke** failures:

* `kinke+V+Inf` => `kink`
* `kinke` has no analyses either

**finpolere** failures:

* `finpolere+V+Inf` => `finpoler`
* `finpolere` has no analyses either

**stilisere** failures:

* `stilisere+V+Inf` => `stiliser`
* `stilisere` has no analyses either

**blodmelte** failures:

* `blodmelte+V+Inf` => `blodmelt`
* `blodmelte` has no analyses either

**kråne** failures:

* `kråne+V+Inf` => `krån`
* `kråne` has no analyses either

**fôre** failures:

* `fôre+V+Inf` => `fôr`
* `fôre` has no analyses either

**grevje** failures:

* `grevje+V+Inf` => `grevj`
* `grevje` has no analyses either

**vabbe** failures:

* `vabbe+V+Inf` => `vabb`
* `vabbe` has no analyses either

**serialisere** failures:

* `serialisere+V+Inf` => `serialiser`
* `serialisere` has no analyses either

**bombe** failures:

* `bombe+V+Inf` => `bomb`
* `bombe` has no analyses either

**teve** failures:

* `teve+V+Inf` => `tev`
* `teve` has no analyses either

**nysne** failures:

* `nysne+V+Inf` => `nysn`
* `nysne` has no analyses either

**hale** failures:

* `hale+V+Inf` does not generate!
* `hale` has following analyses:
  * `hallen+A+Neu+Sg+Indef`

**beslagleggje** failures:

* `beslagleggje+V+Inf` does not generate!
* `beslagleggje` has no analyses either

**bisette** failures:

* `bisette+V+Inf` does not generate!
* `bisette` has no analyses either

**flotne** failures:

* `flotne+V+Inf` => `flotn`
* `flotne` has following analyses:
  * `floten+A+Sg+Def`
  * `floten+A+Pl+Indef`

**kjennast** failures:

* `kjennast+V+Inf` => `kjenast`
* `kjennast` has no analyses either

**kløkke** failures:

* `kløkke+V+Inf` does not generate!
* `kløkke` has no analyses either

**adle** failures:

* `adle+V+Inf` => `adl`
* `adle` has no analyses either

**reke** failures:

* `reke+V+Inf` => `ree`
* `reke+V+Inf` => `rea`
* `reke` has no analyses either

**unngjelde** failures:

* `unngjelde+V+Inf` does not generate!
* `unngjelde` has no analyses either

**surle** failures:

* `surle+V+Inf` => `surl`
* `surle` has no analyses either

**nosse** failures:

* `nosse+V+Inf` => `noss`
* `nosse` has no analyses either

**grovtelje** failures:

* `grovtelje+V+Inf` does not generate!
* `grovtelje` has no analyses either

**tonesetje** failures:

* `tonesetje+V+Inf` does not generate!
* `tonesetje` has no analyses either

**folkesetje** failures:

* `folkesetje+V+Inf` does not generate!
* `folkesetje` has no analyses either

**fungere** failures:

* `fungere+V+Inf` => `funger`
* `fungere` has no analyses either

**vanslekte** failures:

* `vanslekte+V+Inf` => `vanslekt`
* `vanslekte` has no analyses either

**hersette** failures:

* `hersette+V+Inf` does not generate!
* `hersette` has no analyses either

**sulle** failures:

* `sulle+V+Inf` => `sull`
* `sulle` has no analyses either

**erigere** failures:

* `erigere+V+Inf` => `eriger`
* `erigere` has no analyses either

**fylkje** failures:

* `fylkje+V+Inf` does not generate!
* `fylkje` has no analyses either

**remplassere** failures:

* `remplassere+V+Inf` => `remplasser`
* `remplassere` has no analyses either

**utsmykke** failures:

* `utsmykke+V+Inf` => `utsmykk`
* `utsmykke` has no analyses either

**svartmåle** failures:

* `svartmåle+V+Inf` => `svartmål`
* `svartmåle` has no analyses either

**engle** failures:

* `engle+V+Inf` => `engl`
* `engle` has no analyses either

**svimre** failures:

* `svimre+V+Inf` => `svimr`
* `svimre` has no analyses either

**frøyse** failures:

* `frøyse+V+Inf` => `frøys`
* `frøyse` has no analyses either

**overvurdere** failures:

* `overvurdere+V+Inf` => `overvurder`
* `overvurdere` has no analyses either

**tvangsinnlegge** failures:

* `tvangsinnlegge+V+Inf` does not generate!
* `tvangsinnlegge` has no analyses either

**pøyse** failures:

* `pøyse+V+Inf` => `pøys`
* `pøyse` has no analyses either

**bryste** failures:

* `bryste+V+Inf` => `bryst`
* `bryste` has no analyses either

**forfryse** failures:

* `forfryse+V+Inf` => `forfrya`
* `forfryse+V+Inf` => `forfrye`
* `forfryse` has no analyses either

**slemme** failures:

* `slemme+V+Inf` => `slem`
* `slemme` has no analyses either

**forvirre** failures:

* `forvirre+V+Inf` => `forvirr`
* `forvirre` has no analyses either

**trøysteete** failures:

* `trøysteete+V+Inf` => `trøysteea`
* `trøysteete+V+Inf` => `trøysteee`
* `trøysteete` has no analyses either

**smugdrikke** failures:

* `smugdrikke+V+Inf` => `smugdria`
* `smugdrikke+V+Inf` => `smugdrie`
* `smugdrikke` has no analyses either

**røfle** failures:

* `røfle+V+Inf` => `røfl`
* `røfle` has no analyses either

**varetektsfengsle** failures:

* `varetektsfengsle+V+Inf` => `varetektsfengsl`
* `varetektsfengsle` has no analyses either

**disse** failures:

* `disse+V+Inf` => `diss`
* `disse` has no analyses either

**skvakke** failures:

* `skvakke+V+Inf` => `skvakk`
* `skvakke` has no analyses either

**steppe** failures:

* `steppe+V+Inf` => `stepp`
* `steppe` has no analyses either

**innarbeide** failures:

* `innarbeide+V+Inf` does not generate!
* `innarbeide` has no analyses either

**rasjonalisere** failures:

* `rasjonalisere+V+Inf` => `rasjonaliser`
* `rasjonalisere` has no analyses either

**spjerre** failures:

* `spjerre+V+Inf` => `spjerr`
* `spjerre` has no analyses either

**hemte** failures:

* `hemte+V+Inf` => `hemt`
* `hemte` has no analyses either

**alterere** failures:

* `alterere+V+Inf` => `alterer`
* `alterere` has no analyses either

**knulle** failures:

* `knulle+V+Inf` => `knull`
* `knulle` has no analyses either

**kneggje** failures:

* `kneggje+V+Inf` => `knegg`
* `kneggje` has no analyses either

**vektleggje** failures:

* `vektleggje+V+Inf` does not generate!
* `vektleggje` has no analyses either

**kime** failures:

* `kime+V+Inf` => `kim`
* `kime` has no analyses either

**skope** failures:

* `skope+V+Inf` => `skop`
* `skope` has no analyses either

**reorganisere** failures:

* `reorganisere+V+Inf` => `reorganiser`
* `reorganisere` has no analyses either

**hytte** failures:

* `hytte+V+Inf` => `hytt`
* `hytte` has no analyses either

**kovre** failures:

* `kovre+V+Inf` => `kovr`
* `kovre` has no analyses either

**skode** failures:

* `skode+V+Inf` => `skod`
* `skode` has no analyses either

**syndikere** failures:

* `syndikere+V+Inf` => `syndiker`
* `syndikere` has no analyses either

**donte** failures:

* `donte+V+Inf` => `dont`
* `donte` has no analyses either

**dekantere** failures:

* `dekantere+V+Inf` => `dekanter`
* `dekantere` has no analyses either

**falme** failures:

* `falme+V+Inf` => `falm`
* `falme` has no analyses either

**påle** failures:

* `påle+V+Inf` => `pål`
* `påle` has no analyses either

**ripe** failures:

* `ripe+V+Inf` => `rip`
* `ripe` has no analyses either

**xerografere** failures:

* `xerografere+V+Inf` => `xerografer`
* `xerografere` has no analyses either

**innpasse** failures:

* `innpasse+V+Inf` => `innpass`
* `innpasse` has no analyses either

**rengje** failures:

* `rengje+V+Inf` does not generate!
* `rengje` has no analyses either

**geige** failures:

* `geige+V+Inf` => `geig`
* `geige` has no analyses either

**anta** failures:

* `anta+V+Inf` => `antta`
* `anta` has following analyses:
  * `ante+V+Ind+Prt`
  * `ante+V+PrfPrc`

**uheppe** failures:

* `uheppe+V+Inf` => `uhepp`
* `uheppe` has no analyses either

**grøsse** failures:

* `grøsse+V+Inf` does not generate!
* `grøsse` has no analyses either

**strøyme** failures:

* `strøyme+V+Inf` does not generate!
* `strøyme` has no analyses either

**kvike** failures:

* `kvike+V+Inf` => `kvik`
* `kvike` has following analyses:
  * `kvikk+A+A+Sg+Def`
  * `kvikk+A+A+Pl+Def`
  * `kvik+A+Sg+Def`
  * `kvikk+A+A+Pl+Indef`
  * `kvik+A+Pl+Indef`

**bondske** failures:

* `bondske+V+Inf` => `bondsk`
* `bondske` has following analyses:
  * `bondsk+A+A+Pl+Def`
  * `bondsk+A+A+Sg+Def`
  * `bondsk+A+A+Pl+Indef`

**minimalisere** failures:

* `minimalisere+V+Inf` => `minimaliser`
* `minimalisere` has no analyses either

**perturbere** failures:

* `perturbere+V+Inf` => `perturber`
* `perturbere` has no analyses either

**nøyte** failures:

* `nøyte+V+Inf` => `nøyt`
* `nøyte` has following analyses:
  * `nøyten+A+Neu+Sg+Indef`

**klargjere** failures:

* `klargjere+V+Inf` does not generate!
* `klargjere` has no analyses either

**brosjere** failures:

* `brosjere+V+Inf` => `brosjer`
* `brosjere` has no analyses either

**utkjempe** failures:

* `utkjempe+V+Inf` => `utkjemp`
* `utkjempe` has no analyses either

**kolportere** failures:

* `kolportere+V+Inf` => `kolporter`
* `kolportere` has no analyses either

**landlyse** failures:

* `landlyse+V+Inf` => `landlys`
* `landlyse` has no analyses either

**svitte** failures:

* `svitte+V+Inf` => `svitt`
* `svitte` has no analyses either

**kvekke** failures:

* `kvekke+V+Inf` => `kv`
* `kvekke` has no analyses either

**møkje** failures:

* `møkje+V+Inf` => `møk`
* `møkje` has no analyses either

**vidareforedle** failures:

* `vidareforedle+V+Inf` => `vidareforedl`
* `vidareforedle` has no analyses either

**rumle** failures:

* `rumle+V+Inf` => `ruml`
* `rumle` has no analyses either

**skakke** failures:

* `skakke+V+Inf` => `skakk`
* `skakke` has no analyses either

**minelegge** failures:

* `minelegge+V+Inf` does not generate!
* `minelegge` has no analyses either

**glime** failures:

* `glime+V+Inf` does not generate!
* `glime` has no analyses either

**rute** failures:

* `rute+V+Inf` => `rut`
* `rute` has no analyses either

**kveile** failures:

* `kveile+V+Inf` => `kveil`
* `kveile` has no analyses either

**godgjere** failures:

* `godgjere+V+Inf` does not generate!
* `godgjere` has no analyses either

**vide** failures:

* `vide+V+Inf` => `vid`
* `vide` has following analyses:
  * `vid+A+Sg+Def`
  * `vid+A+Pl+Indef`

**fremje** failures:

* `fremje+V+Inf` => `fremj`
* `fremje` has no analyses either

**vulkanisere** failures:

* `vulkanisere+V+Inf` => `vulkaniser`
* `vulkanisere` has no analyses either

**nøle** failures:

* `nøle+V+Inf` => `nøl`
* `nøle` has no analyses either

**avpatruljere** failures:

* `avpatruljere+V+Inf` => `avpatruljer`
* `avpatruljere` has no analyses either

**anne** failures:

* `anne+V+Inf` => `ann`
* `anne` has no analyses either

**fleipe** failures:

* `fleipe+V+Inf` does not generate!
* `fleipe` has no analyses either

**forenkle** failures:

* `forenkle+V+Inf` => `forenkl`
* `forenkle` has no analyses either

**trive** failures:

* `trive+V+Inf` => `trie`
* `trive+V+Inf` => `tria`
* `trive` has following analyses:
  * `triven+A+Neu+Sg+Indef`

**flykte** failures:

* `flykte+V+Inf` => `flykt`
* `flykte` has no analyses either

**spamme** failures:

* `spamme+V+Inf` => `spam`
* `spamme` has no analyses either

**fable** failures:

* `fable+V+Inf` => `fabl`
* `fable` has no analyses either

**kokle** failures:

* `kokle+V+Inf` => `kokl`
* `kokle` has no analyses either

**presipitere** failures:

* `presipitere+V+Inf` => `presipiter`
* `presipitere` has no analyses either

**linke** failures:

* `linke+V+Inf` => `link`
* `linke` has no analyses either

**veve** failures:

* `veve+V+Inf` => `vea`
* `veve+V+Inf` => `vee`
* `veve` has following analyses:
  * `vever+A+A+Pl+Def`
  * `vever+A+A+Pl+Indef`
  * `vever+A+A+Sg+Def`

**koseprate** failures:

* `koseprate+V+Inf` => `koseprat`
* `koseprate` has no analyses either

**fljote** failures:

* `fljote+V+Inf` => `fljot`
* `fljote` has following analyses:
  * `fljot+A+Sg+Def`
  * `fljot+A+Pl+Indef`

**røste** failures:

* `røste+V+Inf` => `røst`
* `røste` has following analyses:
  * `røse+V+PrfPrc`

**nappe** failures:

* `nappe+V+Inf` => `napp`
* `nappe` has no analyses either

**supe** failures:

* `supe+V+Inf` => `s`
* `supe` has following analyses:
  * `super+A+A+Pl+Def`
  * `super+A+A+Sg+Def`
  * `super+A+A+Pl+Indef`
  * `supe+N+N+Msc+Sg+Indef`

**stambokføre** failures:

* `stambokføre+V+Inf` => `stambokfør`
* `stambokføre` has no analyses either

**skjefte** failures:

* `skjefte+V+Inf` does not generate!
* `skjefte` has no analyses either

**kommutere** failures:

* `kommutere+V+Inf` => `kommuter`
* `kommutere` has no analyses either

**fortene** failures:

* `fortene+V+Inf` => `forten`
* `fortene` has no analyses either

**velte** failures:

* `velte+V+Inf` does not generate!
* `velte` has following analyses:
  * `velten+A+Neu+Sg+Indef`

**forsøme** failures:

* `forsøme+V+Inf` does not generate!
* `forsøme` has no analyses either

**devlast** failures:

* `devlast+V+Inf` => `devlastast`
* `devlast` has no analyses either

**syre** failures:

* `syre+V+Inf` does not generate!
* `syre` has no analyses either

**myle** failures:

* `myle+V+Inf` => `myl`
* `myle` has no analyses either

**programfeste** failures:

* `programfeste+V+Inf` does not generate!
* `programfeste` has no analyses either

**påføre** failures:

* `påføre+V+Inf` => `påfør`
* `påføre` has no analyses either

**suspendere** failures:

* `suspendere+V+Inf` => `suspender`
* `suspendere` has no analyses either

**bestille** failures:

* `bestille+V+Inf` does not generate!
* `bestille` has no analyses either

**svortne** failures:

* `svortne+V+Inf` => `svortn`
* `svortne` has no analyses either

**innverke** failures:

* `innverke+V+Inf` => `innverk`
* `innverke` has no analyses either

**førestille** failures:

* `førestille+V+Inf` => `førestile`
* `førestille+V+Inf` => `førestila`
* `førestille` has no analyses either

**kvede** failures:

* `kvede+V+Inf` => `kvea`
* `kvede+V+Inf` => `kvee`
* `kvede` has no analyses either

**knitre** failures:

* `knitre+V+Inf` => `knitr`
* `knitre` has no analyses either

**kjøle** failures:

* `kjøle+V+Inf` => `kjøl`
* `kjøle` has no analyses either

**reflektere** failures:

* `reflektere+V+Inf` => `reflekter`
* `reflektere` has no analyses either

**klave** failures:

* `klave+V+Inf` => `klav`
* `klave` has no analyses either

**bevare** failures:

* `bevare+V+Inf` does not generate!
* `bevare` has no analyses either

**skredderere** failures:

* `skredderere+V+Inf` => `skredderer`
* `skredderere` has no analyses either

**merle** failures:

* `merle+V+Inf` => `merl`
* `merle` has no analyses either

**ratle** failures:

* `ratle+V+Inf` => `ratl`
* `ratle` has no analyses either

**styggjast** failures:

* `styggjast+V+Inf` => `stygjast`
* `styggjast` has no analyses either

**avmønstre** failures:

* `avmønstre+V+Inf` => `avmønstr`
* `avmønstre` has no analyses either

**klette** failures:

* `klette+V+Inf` => `klea`
* `klette+V+Inf` => `klee`
* `klette` has no analyses either

**simplifisere** failures:

* `simplifisere+V+Inf` => `simplifiser`
* `simplifisere` has no analyses either

**mylde** failures:

* `mylde+V+Inf` does not generate!
* `mylde` has no analyses either

**helme** failures:

* `helme+V+Inf` => `helm`
* `helme` has no analyses either

**sjue** failures:

* `sjue+V+Inf` => `sju`
* `sjue` has no analyses either

**stire** failures:

* `stire+V+Inf` does not generate!
* `stire` has no analyses either

**euforisere** failures:

* `euforisere+V+Inf` => `euforiser`
* `euforisere` has no analyses either

**besne** failures:

* `besne+V+Inf` => `besn`
* `besne` has no analyses either

**skrauve** failures:

* `skrauve+V+Inf` => `skrauv`
* `skrauve` has no analyses either

**gaue** failures:

* `gaue+V+Inf` => `gau`
* `gaue` has no analyses either

**vare** failures:

* `vare+V+Inf` => `var`
* `vare` has following analyses:
  * `var+A+Sg+Def`
  * `var+A+Pl+Indef`
  * `vond+A+Comp`

**yppe** failures:

* `yppe+V+Inf` => `ypp`
* `yppe` has no analyses either

**myte** failures:

* `myte+V+Inf` => `myt`
* `myte` has no analyses either

**emendere** failures:

* `emendere+V+Inf` => `emender`
* `emendere` has no analyses either

**mjøle** failures:

* `mjøle+V+Inf` => `mjøl`
* `mjøle` has following analyses:
  * `mjøll+A+A+Sg+Def`
  * `mjøll+A+A+Pl+Indef`
  * `mjøll+A+A+Pl+Def`

**popularisere** failures:

* `popularisere+V+Inf` => `populariser`
* `popularisere` has no analyses either

**godkjenne** failures:

* `godkjenne+V+Inf` does not generate!
* `godkjenne` has no analyses either

**tviskifte** failures:

* `tviskifte+V+Inf` does not generate!
* `tviskifte` has no analyses either

**utgjere** failures:

* `utgjere+V+Inf` does not generate!
* `utgjere` has no analyses either

**krepere** failures:

* `krepere+V+Inf` => `kreper`
* `krepere` has no analyses either

**namngje** failures:

* `namngje+V+Inf` => `namnggje`
* `namngje` has no analyses either

**knegge** failures:

* `knegge+V+Inf` => `knegg`
* `knegge` has no analyses either

**spadevende** failures:

* `spadevende+V+Inf` does not generate!
* `spadevende` has no analyses either

**skulere** failures:

* `skulere+V+Inf` => `skuler`
* `skulere` has no analyses either

**gove** failures:

* `gove+V+Inf` => `gov`
* `gove` has no analyses either

**gjeme** failures:

* `gjeme+V+Inf` => `gjem`
* `gjeme` has no analyses either

**fisjonere** failures:

* `fisjonere+V+Inf` => `fisjoner`
* `fisjonere` has no analyses either

**forstøve** failures:

* `forstøve+V+Inf` => `forstøv`
* `forstøve` has no analyses either

**banke** failures:

* `banke+V+Inf` => `bank`
* `banke` has no analyses either

**fastsette** failures:

* `fastsette+V+Inf` does not generate!
* `fastsette` has following analyses:
  * `fastsett+A+A+Pl+Indef`
  * `fastsett+A+A+Pl+Def`
  * `fastsett+A+A+Sg+Def`

**krake** failures:

* `krake+V+Inf` => `krak`
* `krake` has following analyses:
  * `kraken+A+Neu+Sg+Indef`

**ribbe** failures:

* `ribbe+V+Inf` => `ribb`
* `ribbe` has no analyses either

**skorsetje** failures:

* `skorsetje+V+Inf` does not generate!
* `skorsetje` has no analyses either

**ansvarleggjere** failures:

* `ansvarleggjere+V+Inf` does not generate!
* `ansvarleggjere` has no analyses either

**maile** failures:

* `maile+V+Inf` => `mail`
* `maile` has no analyses either

**kyte** failures:

* `kyte+V+Inf` => `kyt`
* `kyte` has following analyses:
  * `kyten+A+Neu+Sg+Indef`

**garrottere** failures:

* `garrottere+V+Inf` => `garrotter`
* `garrottere` has no analyses either

**grovmale** failures:

* `grovmale+V+Inf` => `grovmae`
* `grovmale+V+Inf` => `grovmaa`
* `grovmale` has no analyses either

**rippe** failures:

* `rippe+V+Inf` => `ripp`
* `rippe` has no analyses either

**røyve** failures:

* `røyve+V+Inf` does not generate!
* `røyve` has no analyses either

**mette** failures:

* `mette+V+Inf` => `mett`
* `mette` has following analyses:
  * `mett+A+A+Sg+Def`
  * `mett+A+A+Pl+Indef`
  * `mett+A+A+Pl+Def`

**flare** failures:

* `flare+V+Inf` => `flar`
* `flare` has no analyses either

**omgjelde** failures:

* `omgjelde+V+Inf` does not generate!
* `omgjelde` has no analyses either

**skeie** failures:

* `skeie+V+Inf` => `skei`
* `skeie` has no analyses either

**treffe** failures:

* `treffe+V+Inf` does not generate!
* `treffe` has no analyses either

**ettersende** failures:

* `ettersende+V+Inf` does not generate!
* `ettersende` has no analyses either

**personleggjere** failures:

* `personleggjere+V+Inf` does not generate!
* `personleggjere` has no analyses either

**kodifisere** failures:

* `kodifisere+V+Inf` => `kodifiser`
* `kodifisere` has no analyses either

**hustre** failures:

* `hustre+V+Inf` => `hustr`
* `hustre` has no analyses either

**journalføre** failures:

* `journalføre+V+Inf` => `journalfør`
* `journalføre` has no analyses either

**attrast** failures:

* `attrast+V+Inf` => `attrastast`
* `attrast` has no analyses either

**beleggje** failures:

* `beleggje+V+Inf` does not generate!
* `beleggje` has no analyses either

**murre** failures:

* `murre+V+Inf` => `murr`
* `murre` has no analyses either

**skalte** failures:

* `skalte+V+Inf` => `skalt`
* `skalte` has no analyses either

**vidaresende** failures:

* `vidaresende+V+Inf` does not generate!
* `vidaresende` has no analyses either

**skrømte** failures:

* `skrømte+V+Inf` => `skrømt`
* `skrømte` has no analyses either

**grefte** failures:

* `grefte+V+Inf` => `greft`
* `grefte` has no analyses either

**diffundere** failures:

* `diffundere+V+Inf` => `diffunder`
* `diffundere` has no analyses either

**kuvende** failures:

* `kuvende+V+Inf` does not generate!
* `kuvende` has no analyses either

**skvate** failures:

* `skvate+V+Inf` => `skvat`
* `skvate` has no analyses either

**interpretere** failures:

* `interpretere+V+Inf` => `interpreter`
* `interpretere` has no analyses either

**byrge** failures:

* `byrge+V+Inf` => `byrg`
* `byrge` has following analyses:
  * `byrg+A+Sg+Def`
  * `byrg+A+Pl+Indef`

**lete** failures:

* `lete+V+Inf` => `let`
* `lete` has following analyses:
  * `la+V+PrfPrc`

**grande** failures:

* `grande+V+Inf` => `grand`
* `grande` has following analyses:
  * `grand+A+A+Pl+Def`
  * `grand+A+A+Pl+Indef`
  * `grand+A+A+Sg+Def`

**hake** failures:

* `hake+V+Inf` => `hak`
* `hake` has no analyses either

**dokke** failures:

* `dokke+V+Inf` => `dokk`
* `dokke` has no analyses either

**forsørgje** failures:

* `forsørgje+V+Inf` does not generate!
* `forsørgje` has no analyses either

**mistale** failures:

* `mistale+V+Inf` does not generate!
* `mistale` has no analyses either

**regissere** failures:

* `regissere+V+Inf` => `regisser`
* `regissere` has no analyses either

**stute** failures:

* `stute+V+Inf` => `stut`
* `stute` has no analyses either

**straffedøme** failures:

* `straffedøme+V+Inf` does not generate!
* `straffedøme` has no analyses either

**invertere** failures:

* `invertere+V+Inf` => `inverter`
* `invertere` has no analyses either

**gnukke** failures:

* `gnukke+V+Inf` => `gnukk`
* `gnukke` has no analyses either

**hulke** failures:

* `hulke+V+Inf` => `hulk`
* `hulke` has no analyses either

**rådleggje** failures:

* `rådleggje+V+Inf` does not generate!
* `rådleggje` has no analyses either

**korse** failures:

* `korse+V+Inf` => `kors`
* `korse` has no analyses either

**halvsove** failures:

* `halvsove+V+Inf` => `halvsoe`
* `halvsove+V+Inf` => `halvsoa`
* `halvsove` has no analyses either

**stjoltre** failures:

* `stjoltre+V+Inf` => `stjoltr`
* `stjoltre` has no analyses either

**fade** failures:

* `fade+V+Inf` => `fad`
* `fade` has no analyses either

**tynne** failures:

* `tynne+V+Inf` does not generate!
* `tynne` has no analyses either

**tomme** failures:

* `tomme+V+Inf` => `tom`
* `tomme` has no analyses either

**seede** failures:

* `seede+V+Inf` => `seed`
* `seede` has no analyses either

**bylje** failures:

* `bylje+V+Inf` => `bylj`
* `bylje` has no analyses either

**døme** failures:

* `døme+V+Inf` does not generate!
* `døme` has no analyses either

**lykkynske** failures:

* `lykkynske+V+Inf` => `lykkynsk`
* `lykkynske` has no analyses either

**oppta** failures:

* `oppta+V+Inf` => `opptta`
* `oppta` has no analyses either

**æreskjemme** failures:

* `æreskjemme+V+Inf` does not generate!
* `æreskjemme` has no analyses either

**tantre** failures:

* `tantre+V+Inf` => `tantr`
* `tantre` has no analyses either

**samsnakke** failures:

* `samsnakke+V+Inf` => `samsnakk`
* `samsnakke` has no analyses either

**svindle** failures:

* `svindle+V+Inf` => `svindl`
* `svindle` has no analyses either

**snauhogge** failures:

* `snauhogge+V+Inf` does not generate!
* `snauhogge` has no analyses either

**førehandsdømme** failures:

* `førehandsdømme+V+Inf` does not generate!
* `førehandsdømme` has no analyses either

**spankulere** failures:

* `spankulere+V+Inf` => `spankuler`
* `spankulere` has no analyses either

**kassere** failures:

* `kassere+V+Inf` => `kasser`
* `kassere` has no analyses either

**forbruke** failures:

* `forbruke+V+Inf` does not generate!
* `forbruke` has no analyses either

**multiplisere** failures:

* `multiplisere+V+Inf` => `multipliser`
* `multiplisere` has no analyses either

**andfare** failures:

* `andfare+V+Inf` => `andfae`
* `andfare+V+Inf` => `andfaa`
* `andfare` has no analyses either

**bele** failures:

* `bele+V+Inf` => `bel`
* `bele` has no analyses either

**belegge** failures:

* `belegge+V+Inf` does not generate!
* `belegge` has no analyses either

**nulle** failures:

* `nulle+V+Inf` => `null`
* `nulle` has no analyses either

**redigere** failures:

* `redigere+V+Inf` => `rediger`
* `redigere` has no analyses either

**knee** failures:

* `knee+V+Inf` => `kne`
* `knee` has following analyses:
  * `kneste+V+Inf`
  * `knette+V+Inf`

**hevde** failures:

* `hevde+V+Inf` => `hevd`
* `hevde` has no analyses either

**sprone** failures:

* `sprone+V+Inf` => `spron`
* `sprone` has no analyses either

**nyreise** failures:

* `nyreise+V+Inf` => `nyreis`
* `nyreise` has no analyses either

**gjenopplive** failures:

* `gjenopplive+V+Inf` => `gjenoppliv`
* `gjenopplive` has no analyses either

**snikmyrde** failures:

* `snikmyrde+V+Inf` => `snikmyrd`
* `snikmyrde` has no analyses either

**spøne** failures:

* `spøne+V+Inf` => `spøn`
* `spøne` has no analyses either

**sele** failures:

* `sele+V+Inf` => `sel`
* `sele` has no analyses either

**overdimensjonere** failures:

* `overdimensjonere+V+Inf` => `overdimensjoner`
* `overdimensjonere` has no analyses either

**avskipe** failures:

* `avskipe+V+Inf` => `avskip`
* `avskipe` has no analyses either

**kjæte** failures:

* `kjæte+V+Inf` => `kjæt`
* `kjæte` has no analyses either

**kvime** failures:

* `kvime+V+Inf` => `kvim`
* `kvime` has following analyses:
  * `kvimen+A+Neu+Sg+Indef`

**tøffe** failures:

* `tøffe+V+Inf` => `tøff`
* `tøffe` has no analyses either

**trøble** failures:

* `trøble+V+Inf` => `trøbl`
* `trøble` has no analyses either

**herleggjere** failures:

* `herleggjere+V+Inf` does not generate!
* `herleggjere` has no analyses either

**nihalde** failures:

* `nihalde+V+Inf` => `nihae`
* `nihalde+V+Inf` => `nihaa`
* `nihalde` has no analyses either

**oppvarte** failures:

* `oppvarte+V+Inf` => `oppvart`
* `oppvarte` has no analyses either

**ruge** failures:

* `ruge+V+Inf` => `rug`
* `ruge` has no analyses either

**staffere** failures:

* `staffere+V+Inf` => `staffer`
* `staffere` has no analyses either

**knerke** failures:

* `knerke+V+Inf` => `knerk`
* `knerke` has no analyses either

**trutne** failures:

* `trutne+V+Inf` => `trutn`
* `trutne` has following analyses:
  * `truten+A+Sg+Def`
  * `truten+A+Pl+Indef`

**gnusse** failures:

* `gnusse+V+Inf` => `gnuss`
* `gnusse` has no analyses either

**fritere** failures:

* `fritere+V+Inf` => `friter`
* `fritere` has no analyses either

**drimse** failures:

* `drimse+V+Inf` => `drims`
* `drimse` has no analyses either

**furte** failures:

* `furte+V+Inf` => `furt`
* `furte` has no analyses either

**sjugse** failures:

* `sjugse+V+Inf` => `sjugs`
* `sjugse` has no analyses either

**kròke** failures:

* `kròke+V+Inf` => `kròk`
* `kròke` has following analyses:
  * `kròken+A+Neu+Sg+Indef`

**tåe** failures:

* `tåe+V+Inf` => `tå`
* `tåe` has following analyses:
  * `tåen+A+Neu+Sg+Indef`
  * `tå+A+Sg+Def`
  * `tå+A+Pl+Indef`

**tilføre** failures:

* `tilføre+V+Inf` does not generate!
* `tilføre` has no analyses either

**kolorere** failures:

* `kolorere+V+Inf` => `kolorer`
* `kolorere` has no analyses either

**reparere** failures:

* `reparere+V+Inf` => `reparer`
* `reparere` has no analyses either

**underteikne** failures:

* `underteikne+V+Inf` => `underteikn`
* `underteikne` has no analyses either

**skadebite** failures:

* `skadebite+V+Inf` => `skadebie`
* `skadebite+V+Inf` => `skadebia`
* `skadebite` has no analyses either

**bjukte** failures:

* `bjukte+V+Inf` => `bjukt`
* `bjukte` has no analyses either

**klirre** failures:

* `klirre+V+Inf` => `klirr`
* `klirre` has no analyses either

**sprudle** failures:

* `sprudle+V+Inf` => `sprudl`
* `sprudle` has no analyses either

**gryte** failures:

* `gryte+V+Inf` => `gryt`
* `gryte` has no analyses either

**klukkle** failures:

* `klukkle+V+Inf` => `klukklle`
* `klukkle` has no analyses either

**mishugse** failures:

* `mishugse+V+Inf` => `mishugs`
* `mishugse` has no analyses either

**huste** failures:

* `huste+V+Inf` => `hust`
* `huste` has no analyses either

**skøye** failures:

* `skøye+V+Inf` => `skøy`
* `skøye` has following analyses:
  * `skøyen+A+Neu+Sg+Indef`

**underbyggje** failures:

* `underbyggje+V+Inf` does not generate!
* `underbyggje` has no analyses either

**lengje** failures:

* `lengje+V+Inf` does not generate!
* `lengje` has no analyses either

**andstemne** failures:

* `andstemne+V+Inf` does not generate!
* `andstemne` has no analyses either

**storgråte** failures:

* `storgråte+V+Inf` => `storgråe`
* `storgråte+V+Inf` => `storgråa`
* `storgråte` has no analyses either

**skrabbe** failures:

* `skrabbe+V+Inf` => `skrabb`
* `skrabbe` has no analyses either

**hyre** failures:

* `hyre+V+Inf` => `hyr`
* `hyre` has following analyses:
  * `hyrren+A+Neu+Sg+Indef`

**finhogge** failures:

* `finhogge+V+Inf` does not generate!
* `finhogge` has no analyses either

**kollsegle** failures:

* `kollsegle+V+Inf` does not generate!
* `kollsegle` has no analyses either

**vefte** failures:

* `vefte+V+Inf` => `veft`
* `vefte` has no analyses either

**erte** failures:

* `erte+V+Inf` => `ert`
* `erte` has no analyses either

**varle** failures:

* `varle+V+Inf` => `varl`
* `varle` has no analyses either

**rækje** failures:

* `rækje+V+Inf` does not generate!
* `rækje` has no analyses either

**overlesse** failures:

* `overlesse+V+Inf` does not generate!
* `overlesse` has no analyses either

**formane** failures:

* `formane+V+Inf` does not generate!
* `formane` has no analyses either

**harmonere** failures:

* `harmonere+V+Inf` => `harmoner`
* `harmonere` has no analyses either

**fesle** failures:

* `fesle+V+Inf` => `fesl`
* `fesle` has no analyses either

**skvære** failures:

* `skvære+V+Inf` => `skvær`
* `skvære` has following analyses:
  * `skvær+A+Pl+Indef`
  * `skvær+A+Sg+Def`

**fjatle** failures:

* `fjatle+V+Inf` => `fjatl`
* `fjatle` has following analyses:
  * `fjatlen+A+Neu+Sg+Indef`

**styggje** failures:

* `styggje+V+Inf` does not generate!
* `styggje` has no analyses either

**are** failures:

* `are+V+Inf` => `ar`
* `are` has no analyses either

**innleme** failures:

* `innleme+V+Inf` => `innlem`
* `innleme` has no analyses either

**trenere** failures:

* `trenere+V+Inf` => `trener`
* `trenere` has no analyses either

**nevle** failures:

* `nevle+V+Inf` => `nevl`
* `nevle` has no analyses either

**timje** failures:

* `timje+V+Inf` => `tim`
* `timje` has no analyses either

**påleggje** failures:

* `påleggje+V+Inf` does not generate!
* `påleggje` has no analyses either

**utbrodere** failures:

* `utbrodere+V+Inf` => `utbroder`
* `utbrodere` has no analyses either

**gasterere** failures:

* `gasterere+V+Inf` => `gasterer`
* `gasterere` has no analyses either

**gleime** failures:

* `gleime+V+Inf` => `gleim`
* `gleime` has no analyses either

**fyne** failures:

* `fyne+V+Inf` => `fyn`
* `fyne` has no analyses either

**pirse** failures:

* `pirse+V+Inf` => `pirs`
* `pirse` has no analyses either

**blottleggje** failures:

* `blottleggje+V+Inf` does not generate!
* `blottleggje` has no analyses either

**dåfurde** failures:

* `dåfurde+V+Inf` => `dåfurd`
* `dåfurde` has no analyses either

**linne** failures:

* `linne+V+Inf` => `linn`
* `linne` has no analyses either

**slubbe** failures:

* `slubbe+V+Inf` => `slubb`
* `slubbe` has no analyses either

**grønske** failures:

* `grønske+V+Inf` => `grønsk`
* `grønske` has no analyses either

**ratifisere** failures:

* `ratifisere+V+Inf` => `ratifiser`
* `ratifisere` has no analyses either

**utkome** failures:

* `utkome+V+Inf` => `utkkoma`
* `utkome+V+Inf` => `utkkome`
* `utkome` has no analyses either

**slankre** failures:

* `slankre+V+Inf` => `slankr`
* `slankre` has no analyses either

**segne** failures:

* `segne+V+Inf` => `segn`
* `segne` has no analyses either

**kinne** failures:

* `kinne+V+Inf` => `kinn`
* `kinne` has no analyses either

**minske** failures:

* `minske+V+Inf` => `minsk`
* `minske` has no analyses either

**vorpe** failures:

* `vorpe+V+Inf` => `vorp`
* `vorpe` has no analyses either

**rette** failures:

* `rette+V+Inf` => `rett`
* `rette` has following analyses:
  * `rett+A+A+Sg+Def`
  * `rett+A+A+Pl+Indef`
  * `rett+A+A+Pl+Def`

**revaluere** failures:

* `revaluere+V+Inf` => `revaluer`
* `revaluere` has no analyses either

**booke** failures:

* `booke+V+Inf` => `book`
* `booke` has no analyses either

**kyngje** failures:

* `kyngje+V+Inf` does not generate!
* `kyngje` has no analyses either

**sjokke** failures:

* `sjokke+V+Inf` => `sjokk`
* `sjokke` has no analyses either

**fyllast** failures:

* `fyllast+V+Inf` => `fylast`
* `fyllast` has no analyses either

**terminfeste** failures:

* `terminfeste+V+Inf` does not generate!
* `terminfeste` has no analyses either

**transendere** failures:

* `transendere+V+Inf` => `transender`
* `transendere` has no analyses either

**anonymisere** failures:

* `anonymisere+V+Inf` => `anonymiser`
* `anonymisere` has no analyses either

**frynse** failures:

* `frynse+V+Inf` => `fryns`
* `frynse` has no analyses either

**smokke** failures:

* `smokke+V+Inf` => `smokk`
* `smokke` has no analyses either

**gjenta** failures:

* `gjenta+V+Inf` => `gjentta`
* `gjenta` has no analyses either

**handsette** failures:

* `handsette+V+Inf` does not generate!
* `handsette` has no analyses either

**omvurdere** failures:

* `omvurdere+V+Inf` => `omvurder`
* `omvurdere` has no analyses either

**prognosere** failures:

* `prognosere+V+Inf` => `prognoser`
* `prognosere` has no analyses either

**humse** failures:

* `humse+V+Inf` => `hums`
* `humse` has no analyses either

**dørgje** failures:

* `dørgje+V+Inf` does not generate!
* `dørgje` has no analyses either

**morske** failures:

* `morske+V+Inf` => `morsk`
* `morske` has following analyses:
  * `morsk+A+A+Pl+Def`
  * `morsk+A+A+Sg+Def`
  * `morsk+A+A+Pl+Indef`

**gasse** failures:

* `gasse+V+Inf` => `gass`
* `gasse` has no analyses either

**kaldprate** failures:

* `kaldprate+V+Inf` => `kaldprat`
* `kaldprate` has no analyses either

**bløkte** failures:

* `bløkte+V+Inf` => `bløkt`
* `bløkte` has no analyses either

**tørrmaule** failures:

* `tørrmaule+V+Inf` => `tørrmaul`
* `tørrmaule` has no analyses either

**skamrose** failures:

* `skamrose+V+Inf` does not generate!
* `skamrose` has no analyses either

**avtalefeste** failures:

* `avtalefeste+V+Inf` does not generate!
* `avtalefeste` has following analyses:
  * `avtalefest+A+A+Pl+Def`
  * `avtalefest+A+A+Sg+Def`
  * `avtalefest+A+A+Pl+Indef`

**undertvinge** failures:

* `undertvinge+V+Inf` => `undertving`
* `undertvinge` has no analyses either

**heppe** failures:

* `heppe+V+Inf` => `hepp`
* `heppe` has no analyses either

**takste** failures:

* `takste+V+Inf` => `takst`
* `takste` has no analyses either

**silbløde** failures:

* `silbløde+V+Inf` does not generate!
* `silbløde` has no analyses either

**flose** failures:

* `flose+V+Inf` => `flos`
* `flose` has no analyses either

**søve** failures:

* `søve+V+Inf` does not generate!
* `søve` has no analyses either

**barme** failures:

* `barme+V+Inf` => `barm`
* `barme` has no analyses either

**tvangsfôre** failures:

* `tvangsfôre+V+Inf` => `tvangsfôr`
* `tvangsfôre` has no analyses either

**slappe** failures:

* `slappe+V+Inf` => `slapp`
* `slappe` has no analyses either

**sensitivisere** failures:

* `sensitivisere+V+Inf` => `sensitiviser`
* `sensitivisere` has no analyses either

**rikje** failures:

* `rikje+V+Inf` does not generate!
* `rikje` has no analyses either

**eksplodere** failures:

* `eksplodere+V+Inf` => `eksploder`
* `eksplodere` has no analyses either

**sordinere** failures:

* `sordinere+V+Inf` => `sordiner`
* `sordinere` has no analyses either

**teikne** failures:

* `teikne+V+Inf` => `teikn`
* `teikne` has no analyses either

**sinne** failures:

* `sinne+V+Inf` => `sinn`
* `sinne` has no analyses either

**bekrefte** failures:

* `bekrefte+V+Inf` => `bekreft`
* `bekrefte` has no analyses either

**falde** failures:

* `falde+V+Inf` => `fald`
* `falde` has no analyses either

**fike** failures:

* `fike+V+Inf` => `fik`
* `fike` has no analyses either

**væle** failures:

* `væle+V+Inf` => `væl`
* `væle` has no analyses either

**gruppere** failures:

* `gruppere+V+Inf` => `grupper`
* `gruppere` has no analyses either

**jøye** failures:

* `jøye+V+Inf` => `jøy`
* `jøye` has no analyses either

**givast** failures:

* `givast+V+Inf` => `ggivast`
* `givast` has no analyses either

**fortorve** failures:

* `fortorve+V+Inf` => `fortorv`
* `fortorve` has no analyses either

**avvente** failures:

* `avvente+V+Inf` => `avvent`
* `avvente` has no analyses either

**lønne** failures:

* `lønne+V+Inf` does not generate!
* `lønne` has no analyses either

**mulle** failures:

* `mulle+V+Inf` => `mull`
* `mulle` has no analyses either

**grønskast** failures:

* `grønskast+V+Inf` => `grønskastast`
* `grønskast` has no analyses either

**fæle** failures:

* `fæle+V+Inf` => `fæl`
* `fæle` has following analyses:
  * `fæl+A+A+Sg+Def`
  * `fæl+A+A+Pl+Def`
  * `fæl+A+A+Pl+Indef`

**prejudisere** failures:

* `prejudisere+V+Inf` => `prejudiser`
* `prejudisere` has no analyses either

**striregne** failures:

* `striregne+V+Inf` does not generate!
* `striregne` has no analyses either

**tradere** failures:

* `tradere+V+Inf` => `trader`
* `tradere` has no analyses either

**mekle** failures:

* `mekle+V+Inf` => `mekl`
* `mekle` has no analyses either

**straffe** failures:

* `straffe+V+Inf` => `straff`
* `straffe` has no analyses either

**forbrenne** failures:

* `forbrenne+V+Inf` does not generate!
* `forbrenne` has no analyses either

**føreta** failures:

* `føreta+V+Inf` => `føretta`
* `føreta` has no analyses either

**snyrte** failures:

* `snyrte+V+Inf` => `snyrt`
* `snyrte` has no analyses either

**sløkkje** failures:

* `sløkkje+V+Inf` does not generate!
* `sløkkje` has no analyses either

**klåre** failures:

* `klåre+V+Inf` => `klår`
* `klåre` has following analyses:
  * `klår+A+Pl+Indef`
  * `klår+A+Sg+Def`

**transfigurere** failures:

* `transfigurere+V+Inf` => `transfigurer`
* `transfigurere` has no analyses either

**fronte** failures:

* `fronte+V+Inf` => `front`
* `fronte` has no analyses either

**vedkjenne** failures:

* `vedkjenne+V+Inf` does not generate!
* `vedkjenne` has no analyses either

**syte** failures:

* `syte+V+Inf` => `syt`
* `syte` has following analyses:
  * `syten+A+Neu+Sg+Indef`

**samsvare** failures:

* `samsvare+V+Inf` does not generate!
* `samsvare` has no analyses either

**kompostere** failures:

* `kompostere+V+Inf` => `komposter`
* `kompostere` has no analyses either

**drøyme** failures:

* `drøyme+V+Inf` does not generate!
* `drøyme` has following analyses:
  * `drøymen+A+Neu+Sg+Indef`

**orne** failures:

* `orne+V+Inf` => `orn`
* `orne` has no analyses either

**dæme** failures:

* `dæme+V+Inf` does not generate!
* `dæme` has no analyses either

**feitne** failures:

* `feitne+V+Inf` => `feitn`
* `feitne` has no analyses either

**vebbe** failures:

* `vebbe+V+Inf` => `vebb`
* `vebbe` has no analyses either

**turrhoste** failures:

* `turrhoste+V+Inf` => `turrhost`
* `turrhoste` has no analyses either

**ramme** failures:

* `ramme+V+Inf` => `ram`
* `ramme` has no analyses either

**hurkle** failures:

* `hurkle+V+Inf` => `hurkl`
* `hurkle` has no analyses either

**tatle** failures:

* `tatle+V+Inf` => `tatl`
* `tatle` has no analyses either

**låse** failures:

* `låse+V+Inf` => `lås`
* `låse` has no analyses either

**agitere** failures:

* `agitere+V+Inf` => `agiter`
* `agitere` has no analyses either

**tryte** failures:

* `tryte+V+Inf` => `tr`
* `tryte` has no analyses either

**kringkaste** failures:

* `kringkaste+V+Inf` => `kringkast`
* `kringkaste` has no analyses either

**addere** failures:

* `addere+V+Inf` => `adder`
* `addere` has no analyses either

**mergstele** failures:

* `mergstele+V+Inf` => `mergstee`
* `mergstele+V+Inf` => `mergstea`
* `mergstele` has no analyses either

**heilne** failures:

* `heilne+V+Inf` => `heiln`
* `heilne` has no analyses either

**gjestespele** failures:

* `gjestespele+V+Inf` does not generate!
* `gjestespele` has no analyses either

**rutte** failures:

* `rutte+V+Inf` => `rutt`
* `rutte` has no analyses either

**utdanne** failures:

* `utdanne+V+Inf` => `utdann`
* `utdanne` has no analyses either

**sikte** failures:

* `sikte+V+Inf` => `sikt`
* `sikte` has no analyses either

**knesetje** failures:

* `knesetje+V+Inf` does not generate!
* `knesetje` has no analyses either

**promillekøyre** failures:

* `promillekøyre+V+Inf` does not generate!
* `promillekøyre` has no analyses either

**aromatisere** failures:

* `aromatisere+V+Inf` => `aromatiser`
* `aromatisere` has no analyses either

**skjelettere** failures:

* `skjelettere+V+Inf` => `skjeletter`
* `skjelettere` has no analyses either

**avunde** failures:

* `avunde+V+Inf` => `avund`
* `avunde` has no analyses either

**penetrere** failures:

* `penetrere+V+Inf` => `penetrer`
* `penetrere` has no analyses either

**utbasunere** failures:

* `utbasunere+V+Inf` => `utbasuner`
* `utbasunere` has no analyses either

**skandere** failures:

* `skandere+V+Inf` => `skander`
* `skandere` has no analyses either

**skjenkje** failures:

* `skjenkje+V+Inf` does not generate!
* `skjenkje` has no analyses either

**klabbe** failures:

* `klabbe+V+Inf` => `klabb`
* `klabbe` has no analyses either

**etterforske** failures:

* `etterforske+V+Inf` => `etterforsk`
* `etterforske` has no analyses either

**avlaste** failures:

* `avlaste+V+Inf` => `avlast`
* `avlaste` has no analyses either

**utmanøvrere** failures:

* `utmanøvrere+V+Inf` => `utmanøvrer`
* `utmanøvrere` has no analyses either

**enje** failures:

* `enje+V+Inf` => `enj`
* `enje` has no analyses either

**kjeske** failures:

* `kjeske+V+Inf` => `kjesk`
* `kjeske` has following analyses:
  * `kjesken+A+Neu+Sg+Indef`

**zappe** failures:

* `zappe+V+Inf` => `zapp`
* `zappe` has no analyses either

**okke** failures:

* `okke+V+Inf` => `okk`
* `okke` has no analyses either

**daske** failures:

* `daske+V+Inf` => `dask`
* `daske` has no analyses either

**sprite** failures:

* `sprite+V+Inf` => `sprit`
* `sprite` has no analyses either

**bryne** failures:

* `bryne+V+Inf` => `bryn`
* `bryne` has no analyses either

**fendre** failures:

* `fendre+V+Inf` => `fendr`
* `fendre` has no analyses either

**rynje** failures:

* `rynje+V+Inf` does not generate!
* `rynje` has no analyses either

**forbanne** failures:

* `forbanne+V+Inf` does not generate!
* `forbanne` has no analyses either

**framføre** failures:

* `framføre+V+Inf` => `framfør`
* `framføre` has no analyses either

**refundere** failures:

* `refundere+V+Inf` => `refunder`
* `refundere` has no analyses either

**urinere** failures:

* `urinere+V+Inf` => `uriner`
* `urinere` has no analyses either

**ruve** failures:

* `ruve+V+Inf` does not generate!
* `ruve` has no analyses either

**denasjonalisere** failures:

* `denasjonalisere+V+Inf` => `denasjonaliser`
* `denasjonalisere` has no analyses either

**dynke** failures:

* `dynke+V+Inf` => `dynk`
* `dynke` has no analyses either

**ivaretake** failures:

* `ivaretake+V+Inf` => `ivarettaka`
* `ivaretake+V+Inf` => `ivarettake`
* `ivaretake` has no analyses either

**trenge** failures:

* `trenge+V+Inf` does not generate!
* `trenge` has no analyses either

**trimme** failures:

* `trimme+V+Inf` => `trim`
* `trimme` has no analyses either

**anbefale** failures:

* `anbefale+V+Inf` => `anbefal`
* `anbefale` has no analyses either

**utbytte** failures:

* `utbytte+V+Inf` does not generate!
* `utbytte` has following analyses:
  * `utbyte+V+PrfPrc`

**handle** failures:

* `handle+V+Inf` => `handl`
* `handle` has no analyses either

**brurvie** failures:

* `brurvie+V+Inf` does not generate!
* `brurvie` has no analyses either

**flume** failures:

* `flume+V+Inf` => `flum`
* `flume` has no analyses either

**homologere** failures:

* `homologere+V+Inf` => `homologer`
* `homologere` has no analyses either

**saume** failures:

* `saume+V+Inf` => `saum`
* `saume` has no analyses either

**kjangse** failures:

* `kjangse+V+Inf` => `kjangs`
* `kjangse` has no analyses either

**remontere** failures:

* `remontere+V+Inf` => `remonter`
* `remontere` has no analyses either

**rusle** failures:

* `rusle+V+Inf` => `rusl`
* `rusle` has no analyses either

**knesette** failures:

* `knesette+V+Inf` does not generate!
* `knesette` has no analyses either

**hyggje** failures:

* `hyggje+V+Inf` does not generate!
* `hyggje` has no analyses either

**risle** failures:

* `risle+V+Inf` => `risl`
* `risle` has no analyses either

**komponere** failures:

* `komponere+V+Inf` => `komponer`
* `komponere` has no analyses either

**fengsle** failures:

* `fengsle+V+Inf` => `fengsl`
* `fengsle` has no analyses either

**knòde** failures:

* `knòde+V+Inf` does not generate!
* `knòde` has no analyses either

**tvarle** failures:

* `tvarle+V+Inf` => `tvarl`
* `tvarle` has no analyses either

**dysje** failures:

* `dysje+V+Inf` does not generate!
* `dysje` has no analyses either

**tandre** failures:

* `tandre+V+Inf` => `tandr`
* `tandre` has no analyses either

**gjennomarbeide** failures:

* `gjennomarbeide+V+Inf` does not generate!
* `gjennomarbeide` has no analyses either

**jorte** failures:

* `jorte+V+Inf` => `jort`
* `jorte` has no analyses either

**jodle** failures:

* `jodle+V+Inf` => `jodl`
* `jodle` has no analyses either

**stolpe** failures:

* `stolpe+V+Inf` => `stolp`
* `stolpe` has no analyses either

**lagerføre** failures:

* `lagerføre+V+Inf` => `lagerfør`
* `lagerføre` has no analyses either

**fjante** failures:

* `fjante+V+Inf` => `fjant`
* `fjante` has no analyses either

**avmystifisere** failures:

* `avmystifisere+V+Inf` => `avmystifiser`
* `avmystifisere` has no analyses either

**ese** failures:

* `ese+V+Inf` => `es`
* `ese` has no analyses either

**beine** failures:

* `beine+V+Inf` => `bein`
* `beine` has following analyses:
  * `bein+A+Sg+Def`
  * `bein+A+Pl+Indef`

**møyke** failures:

* `møyke+V+Inf` => `møyk`
* `møyke` has no analyses either

**kompilere** failures:

* `kompilere+V+Inf` => `kompiler`
* `kompilere` has no analyses either

**sprute** failures:

* `sprute+V+Inf` does not generate!
* `sprute` has no analyses either

**dykke** failures:

* `dykke+V+Inf` => `dykk`
* `dykke` has no analyses either

**dogge** failures:

* `dogge+V+Inf` => `dogg`
* `dogge` has no analyses either

**vanske** failures:

* `vanske+V+Inf` => `vansk`
* `vanske` has no analyses either

**blogge** failures:

* `blogge+V+Inf` => `blogg`
* `blogge` has no analyses either

**óme** failures:

* `óme+V+Inf` => `óm`
* `óme` has no analyses either

**kursivere** failures:

* `kursivere+V+Inf` => `kursiver`
* `kursivere` has no analyses either

**avlyse** failures:

* `avlyse+V+Inf` => `avlys`
* `avlyse` has no analyses either

**kappsvømme** failures:

* `kappsvømme+V+Inf` => `kappsvøme`
* `kappsvømme+V+Inf` => `kappsvøma`
* `kappsvømme` has no analyses either

**ettermontere** failures:

* `ettermontere+V+Inf` => `ettermonter`
* `ettermontere` has no analyses either

**drulte** failures:

* `drulte+V+Inf` => `drult`
* `drulte` has following analyses:
  * `drulten+A+Neu+Sg+Indef`

**snoe** failures:

* `snoe+V+Inf` => `sno`
* `snoe` has following analyses:
  * `snype+V+PrfPrc`
  * `snype+V+Adj+Nøyt+Ub+Eint`

**samanfatte** failures:

* `samanfatte+V+Inf` => `samanfatt`
* `samanfatte` has no analyses either

**finte** failures:

* `finte+V+Inf` => `fint`
* `finte` has no analyses either

**spissformulere** failures:

* `spissformulere+V+Inf` => `spissformuler`
* `spissformulere` has no analyses either

**blåhalde** failures:

* `blåhalde+V+Inf` => `blåhae`
* `blåhalde+V+Inf` => `blåhaa`
* `blåhalde` has no analyses either

**resirkulere** failures:

* `resirkulere+V+Inf` => `resirkuler`
* `resirkulere` has no analyses either

**trace** failures:

* `trace+V+Inf` => `trac`
* `trace` has no analyses either

**typpe** failures:

* `typpe+V+Inf` does not generate!
* `typpe` has no analyses either

**score** failures:

* `score+V+Inf` => `scor`
* `score` has no analyses either

**slentre** failures:

* `slentre+V+Inf` => `slentr`
* `slentre` has no analyses either

**skeivdele** failures:

* `skeivdele+V+Inf` => `skeivdel`
* `skeivdele` has no analyses either

**skumme** failures:

* `skumme+V+Inf` => `skum`
* `skumme` has no analyses either

**tymte** failures:

* `tymte+V+Inf` => `tymt`
* `tymte` has no analyses either

**krenge** failures:

* `krenge+V+Inf` => `kreng`
* `krenge` has no analyses either

**ekshalere** failures:

* `ekshalere+V+Inf` => `ekshaler`
* `ekshalere` has no analyses either

**injisere** failures:

* `injisere+V+Inf` => `injiser`
* `injisere` has no analyses either

**håe** failures:

* `håe+V+Inf` => `hå`
* `håe` has no analyses either

**utnemne** failures:

* `utnemne+V+Inf` does not generate!
* `utnemne` has no analyses either

**høgde** failures:

* `høgde+V+Inf` => `høgd`
* `høgde` has no analyses either

**vitne** failures:

* `vitne+V+Inf` => `vitn`
* `vitne` has no analyses either

**flokke** failures:

* `flokke+V+Inf` => `flokk`
* `flokke` has no analyses either

**fullfølgje** failures:

* `fullfølgje+V+Inf` does not generate!
* `fullfølgje` has no analyses either

**elke** failures:

* `elke+V+Inf` => `elk`
* `elke` has no analyses either

**avstemme** failures:

* `avstemme+V+Inf` does not generate!
* `avstemme` has no analyses either

**hyme** failures:

* `hyme+V+Inf` does not generate!
* `hyme` has no analyses either

**irettesette** failures:

* `irettesette+V+Inf` does not generate!
* `irettesette` has no analyses either

**slite** failures:

* `slite+V+Inf` => `slia`
* `slite+V+Inf` => `slie`
* `slite` has no analyses either

**meite** failures:

* `meite+V+Inf` => `meit`
* `meite` has no analyses either

**lekamleggjere** failures:

* `lekamleggjere+V+Inf` does not generate!
* `lekamleggjere` has no analyses either

**svasse** failures:

* `svasse+V+Inf` => `svass`
* `svasse` has no analyses either

**forflate** failures:

* `forflate+V+Inf` => `forflat`
* `forflate` has no analyses either

**landsforvise** failures:

* `landsforvise+V+Inf` => `landsforvis`
* `landsforvise` has no analyses either

**fetle** failures:

* `fetle+V+Inf` => `fetl`
* `fetle` has no analyses either

**allokere** failures:

* `allokere+V+Inf` => `alloker`
* `allokere` has no analyses either

**stemple** failures:

* `stemple+V+Inf` => `stempl`
* `stemple` has no analyses either

**pæle** failures:

* `pæle+V+Inf` => `pæl`
* `pæle` has no analyses either

**ukvesse** failures:

* `ukvesse+V+Inf` => `ukvess`
* `ukvesse` has no analyses either

**reduplisere** failures:

* `reduplisere+V+Inf` => `redupliser`
* `reduplisere` has no analyses either

**kleime** failures:

* `kleime+V+Inf` does not generate!
* `kleime` has no analyses either

**mumle** failures:

* `mumle+V+Inf` => `muml`
* `mumle` has no analyses either

**kile** failures:

* `kile+V+Inf` does not generate!
* `kile` has following analyses:
  * `kilen+A+Neu+Sg+Indef`

**rabbe** failures:

* `rabbe+V+Inf` => `rabb`
* `rabbe` has no analyses either

**dobbelklikke** failures:

* `dobbelklikke+V+Inf` => `dobbelklikk`
* `dobbelklikke` has no analyses either

**dokksette** failures:

* `dokksette+V+Inf` does not generate!
* `dokksette` has no analyses either

**plettere** failures:

* `plettere+V+Inf` => `pletter`
* `plettere` has no analyses either

**flisleggje** failures:

* `flisleggje+V+Inf` does not generate!
* `flisleggje` has no analyses either

**fossregne** failures:

* `fossregne+V+Inf` does not generate!
* `fossregne` has no analyses either

**kåsere** failures:

* `kåsere+V+Inf` => `kåser`
* `kåsere` has no analyses either

**drøfte** failures:

* `drøfte+V+Inf` does not generate!
* `drøfte` has no analyses either

**dyrke** failures:

* `dyrke+V+Inf` => `dyrk`
* `dyrke` has no analyses either

**hausse** failures:

* `hausse+V+Inf` => `hauss`
* `hausse` has no analyses either

**smolle** failures:

* `smolle+V+Inf` => `smoll`
* `smolle` has no analyses either

**forfalske** failures:

* `forfalske+V+Inf` => `forfalsk`
* `forfalske` has no analyses either

**kvarde** failures:

* `kvarde+V+Inf` => `kvard`
* `kvarde` has no analyses either

**utsette** failures:

* `utsette+V+Inf` does not generate!
* `utsette` has following analyses:
  * `utsett+A+A+Sg+Def`
  * `utsett+A+A+Pl+Def`
  * `utsett+A+A+Pl+Indef`

**tykkast** failures:

* `tykkast+V+Inf` => `tykast`
* `tykkast` has no analyses either

**stereotypere** failures:

* `stereotypere+V+Inf` => `stereotyper`
* `stereotypere` has no analyses either

**valdre** failures:

* `valdre+V+Inf` => `valdr`
* `valdre` has no analyses either

**gnure** failures:

* `gnure+V+Inf` => `gnur`
* `gnure` has no analyses either

**kyrkje** failures:

* `kyrkje+V+Inf` does not generate!
* `kyrkje` has no analyses either

**innsegle** failures:

* `innsegle+V+Inf` => `innsegl`
* `innsegle` has no analyses either

**jage** failures:

* `jage+V+Inf` => `jag`
* `jage` has no analyses either

**tåne** failures:

* `tåne+V+Inf` => `tån`
* `tåne` has following analyses:
  * `tåen+A+Sg+Def`
  * `tåen+A+Pl+Indef`

**utklasse** failures:

* `utklasse+V+Inf` => `utklass`
* `utklasse` has no analyses either

**mankere** failures:

* `mankere+V+Inf` => `manker`
* `mankere` has no analyses either

**klekkje** failures:

* `klekkje+V+Inf` does not generate!
* `klekkje` has no analyses either

**vaslast** failures:

* `vaslast+V+Inf` => `vaslastast`
* `vaslast` has no analyses either

**brennemerkje** failures:

* `brennemerkje+V+Inf` does not generate!
* `brennemerkje` has no analyses either

**motsvare** failures:

* `motsvare+V+Inf` does not generate!
* `motsvare` has no analyses either

**karnefle** failures:

* `karnefle+V+Inf` => `karnefl`
* `karnefle` has no analyses either

**aksentuere** failures:

* `aksentuere+V+Inf` => `aksentuer`
* `aksentuere` has no analyses either

**deflorere** failures:

* `deflorere+V+Inf` => `deflorer`
* `deflorere` has no analyses either

**postere** failures:

* `postere+V+Inf` => `poster`
* `postere` has no analyses either

**peile** failures:

* `peile+V+Inf` => `peil`
* `peile` has no analyses either

**krasjlande** failures:

* `krasjlande+V+Inf` => `krasjland`
* `krasjlande` has no analyses either

**buskape** failures:

* `buskape+V+Inf` => `buskap`
* `buskape` has no analyses either

**strete** failures:

* `strete+V+Inf` => `stret`
* `strete` has no analyses either

**tokke** failures:

* `tokke+V+Inf` => `tokk`
* `tokke` has no analyses either

**treskje** failures:

* `treskje+V+Inf` does not generate!
* `treskje` has no analyses either

**fnøse** failures:

* `fnøse+V+Inf` => `fnøs`
* `fnøse` has no analyses either

**hesple** failures:

* `hesple+V+Inf` => `hespl`
* `hesple` has no analyses either

**normalisere** failures:

* `normalisere+V+Inf` => `normaliser`
* `normalisere` has no analyses either

**assosiere** failures:

* `assosiere+V+Inf` => `assosier`
* `assosiere` has no analyses either

**skanse** failures:

* `skanse+V+Inf` => `skans`
* `skanse` has no analyses either

**pile** failures:

* `pile+V+Inf` does not generate!
* `pile` has no analyses either

**atterføde** failures:

* `atterføde+V+Inf` does not generate!
* `atterføde` has no analyses either

**kjakse** failures:

* `kjakse+V+Inf` => `kjaks`
* `kjakse` has no analyses either

**roste** failures:

* `roste+V+Inf` => `rost`
* `roste` has no analyses either

**bore** failures:

* `bore+V+Inf` => `bor`
* `bore` has no analyses either

**illsvi** failures:

* `illsvi+V+Inf` does not generate!
* `illsvi` has no analyses either

**mimre** failures:

* `mimre+V+Inf` => `mimr`
* `mimre` has no analyses either

**absentere** failures:

* `absentere+V+Inf` => `absenter`
* `absentere` has no analyses either

**forrekne** failures:

* `forrekne+V+Inf` => `forrekn`
* `forrekne` has no analyses either

**slagge** failures:

* `slagge+V+Inf` => `slagg`
* `slagge` has no analyses either

**starve** failures:

* `starve+V+Inf` => `starv`
* `starve` has no analyses either

**desorientere** failures:

* `desorientere+V+Inf` => `desorienter`
* `desorientere` has no analyses either

**snike** failures:

* `snike+V+Inf` => `sn`
* `snike` has following analyses:
  * `sniken+A+Neu+Sg+Indef`

**bryggje** failures:

* `bryggje+V+Inf` does not generate!
* `bryggje` has no analyses either

**leke** failures:

* `leke+V+Inf` => `l`
* `leke` has following analyses:
  * `lekker+A+A+Pl+Indef`
  * `lek+A+Sg+Def`
  * `lekker+A+A+Sg+Def`
  * `lekk+A+A+Sg+Def`
  * `lekk+A+A+Pl+Def`
  * `lekk+A+A+Pl+Indef`
  * `lekker+A+A+Pl+Def`
  * `lek+A+Pl+Indef`

**dissonere** failures:

* `dissonere+V+Inf` => `dissoner`
* `dissonere` has no analyses either

**ryke** failures:

* `ryke+V+Inf` => `rya`
* `ryke+V+Inf` => `rye`
* `ryke` has no analyses either

**slitast** failures:

* `slitast+V+Inf` => `sliast`
* `slitast` has no analyses either

**grundere** failures:

* `grundere+V+Inf` => `grunder`
* `grundere` has no analyses either

**lurve** failures:

* `lurve+V+Inf` => `lurv`
* `lurve` has no analyses either

**ante** failures:

* `ante+V+Inf` => `ant`
* `ante` has no analyses either

**sprekkje** failures:

* `sprekkje+V+Inf` does not generate!
* `sprekkje` has no analyses either

**øve** failures:

* `øve+V+Inf` does not generate!
* `øve` has following analyses:
  * `øvd+A+A+Sg+Def`
  * `øvd+A+A+Pl+Indef`
  * `øvd+A+A+Pl+Def`

**applikere** failures:

* `applikere+V+Inf` => `appliker`
* `applikere` has no analyses either

**hogge** failures:

* `hogge+V+Inf` does not generate!
* `hogge` has no analyses either

**hovne** failures:

* `hovne+V+Inf` => `hovn`
* `hovne` has following analyses:
  * `hoven+A+Pl+Indef`
  * `hoven+A+Sg+Def`

**lyfte** failures:

* `lyfte+V+Inf` does not generate!
* `lyfte` has no analyses either

**redde** failures:

* `redde+V+Inf` => `redd`
* `redde` has following analyses:
  * `redd+A+A+Sg+Def`
  * `redd+A+A+Pl+Def`
  * `redd+A+A+Pl+Indef`

**grasbinde** failures:

* `grasbinde+V+Inf` does not generate!
* `grasbinde` has no analyses either

**dote** failures:

* `dote+V+Inf` => `dot`
* `dote` has no analyses either

**døre** failures:

* `døre+V+Inf` => `dør`
* `døre` has no analyses either

**drøype** failures:

* `drøype+V+Inf` => `drøyp`
* `drøype` has no analyses either

**undertrykkje** failures:

* `undertrykkje+V+Inf` does not generate!
* `undertrykkje` has no analyses either

**kynge** failures:

* `kynge+V+Inf` does not generate!
* `kynge` has no analyses either

**yrke** failures:

* `yrke+V+Inf` => `yrk`
* `yrke` has following analyses:
  * `yrk+A+Pl+Indef`
  * `yrk+A+Sg+Def`

**slafse** failures:

* `slafse+V+Inf` => `slafs`
* `slafse` has following analyses:
  * `slafsen+A+Neu+Sg+Indef`

**modulere** failures:

* `modulere+V+Inf` => `moduler`
* `modulere` has no analyses either

**tandle** failures:

* `tandle+V+Inf` => `tandl`
* `tandle` has no analyses either

**preludere** failures:

* `preludere+V+Inf` => `preluder`
* `preludere` has no analyses either

**mønstre** failures:

* `mønstre+V+Inf` => `mønstr`
* `mønstre` has no analyses either

**brunsteike** failures:

* `brunsteike+V+Inf` => `brunsteik`
* `brunsteike` has no analyses either

**antireflekshandsame** failures:

* `antireflekshandsame+V+Inf` => `antireflekshandsam`
* `antireflekshandsame` has no analyses either

**sigle** failures:

* `sigle+V+Inf` does not generate!
* `sigle` has no analyses either

**trakte** failures:

* `trakte+V+Inf` => `trakt`
* `trakte` has no analyses either

**todele** failures:

* `todele+V+Inf` => `todel`
* `todele` has no analyses either

**skule** failures:

* `skule+V+Inf` => `skul`
* `skule` has following analyses:
  * `skuler+N+N+Pl`

**lyssetje** failures:

* `lyssetje+V+Inf` does not generate!
* `lyssetje` has no analyses either

**forgjeve** failures:

* `forgjeve+V+Inf` => `forggjeva`
* `forgjeve+V+Inf` => `forggjeve`
* `forgjeve` has following analyses:
  * `forgjeven+A+Neu+Sg+Indef`

**verne** failures:

* `verne+V+Inf` => `vern`
* `verne` has no analyses either

**vante** failures:

* `vante+V+Inf` => `vant`
* `vante` has no analyses either

**utklarere** failures:

* `utklarere+V+Inf` => `utklarer`
* `utklarere` has no analyses either

**brennemerke** failures:

* `brennemerke+V+Inf` => `brennemerk`
* `brennemerke` has following analyses:
  * `brennemerke+N`

**pukke** failures:

* `pukke+V+Inf` => `pukk`
* `pukke` has no analyses either

**bodsende** failures:

* `bodsende+V+Inf` does not generate!
* `bodsende` has no analyses either

**føretake** failures:

* `føretake+V+Inf` => `førettake`
* `føretake+V+Inf` => `førettaka`
* `føretake` has no analyses either

**grisetakle** failures:

* `grisetakle+V+Inf` => `grisetakl`
* `grisetakle` has no analyses either

**atrofiere** failures:

* `atrofiere+V+Inf` => `atrofier`
* `atrofiere` has no analyses either

**overbevise** failures:

* `overbevise+V+Inf` => `overbevis`
* `overbevise` has no analyses either

**skumle** failures:

* `skumle+V+Inf` => `skuml`
* `skumle` has following analyses:
  * `skumlese+V+Imprt`
  * `skumlese+V+Ind+Prs`

**fastleggje** failures:

* `fastleggje+V+Inf` does not generate!
* `fastleggje` has no analyses either

**akselerere** failures:

* `akselerere+V+Inf` => `akselerer`
* `akselerere` has no analyses either

**åkalle** failures:

* `åkalle+V+Inf` => `åkall`
* `åkalle` has no analyses either

**landsette** failures:

* `landsette+V+Inf` does not generate!
* `landsette` has no analyses either

**kjeltre** failures:

* `kjeltre+V+Inf` => `kjeltr`
* `kjeltre` has no analyses either

**brysje** failures:

* `brysje+V+Inf` => `brysj`
* `brysje` has no analyses either

**føte** failures:

* `føte+V+Inf` => `føt`
* `føte` has no analyses either

**stinke** failures:

* `stinke+V+Inf` => `stink`
* `stinke` has no analyses either

**sveipe** failures:

* `sveipe+V+Inf` => `sveip`
* `sveipe` has no analyses either

**skrolle** failures:

* `skrolle+V+Inf` => `skroll`
* `skrolle` has no analyses either

**kvantifisere** failures:

* `kvantifisere+V+Inf` => `kvantifiser`
* `kvantifisere` has no analyses either

**lóse** failures:

* `lóse+V+Inf` => `lós`
* `lóse` has no analyses either

**dimensjonere** failures:

* `dimensjonere+V+Inf` => `dimensjoner`
* `dimensjonere` has no analyses either

**sintre** failures:

* `sintre+V+Inf` => `sintr`
* `sintre` has no analyses either

**fordryge** failures:

* `fordryge+V+Inf` does not generate!
* `fordryge` has no analyses either

**flogse** failures:

* `flogse+V+Inf` => `flogs`
* `flogse` has no analyses either

**skyldsette** failures:

* `skyldsette+V+Inf` does not generate!
* `skyldsette` has no analyses either

**drønne** failures:

* `drønne+V+Inf` does not generate!
* `drønne` has no analyses either

**tjuvkjenne** failures:

* `tjuvkjenne+V+Inf` does not generate!
* `tjuvkjenne` has no analyses either

**tverrhalde** failures:

* `tverrhalde+V+Inf` => `tverrhae`
* `tverrhalde+V+Inf` => `tverrhaa`
* `tverrhalde` has no analyses either

**dugurde** failures:

* `dugurde+V+Inf` => `dugurd`
* `dugurde` has no analyses either

**sluke** failures:

* `sluke+V+Inf` => `sluk`
* `sluke` has following analyses:
  * `sluken+A+Neu+Sg+Indef`

**skrate** failures:

* `skrate+V+Inf` => `skrat`
* `skrate` has no analyses either

**stramme** failures:

* `stramme+V+Inf` => `stram`
* `stramme` has no analyses either

**samlage** failures:

* `samlage+V+Inf` => `samlag`
* `samlage` has no analyses either

**dave** failures:

* `dave+V+Inf` => `dav`
* `dave` has no analyses either

**slåst** failures:

* `slåst+V+Inf` => `slslåst`
* `slåst` has no analyses either

**alast** failures:

* `alast+V+Inf` => `alastaast`
* `alast` has no analyses either

**sautre** failures:

* `sautre+V+Inf` => `sautr`
* `sautre` has no analyses either

**attakkere** failures:

* `attakkere+V+Inf` => `attakker`
* `attakkere` has no analyses either

**ljoske** failures:

* `ljoske+V+Inf` => `ljosk`
* `ljoske` has no analyses either

**knake** failures:

* `knake+V+Inf` => `knak`
* `knake` has no analyses either

**kjefte** failures:

* `kjefte+V+Inf` => `kjeft`
* `kjefte` has no analyses either

**jekke** failures:

* `jekke+V+Inf` => `jekk`
* `jekke` has no analyses either

**lake** failures:

* `lake+V+Inf` => `lak`
* `lake` has no analyses either

**blitse** failures:

* `blitse+V+Inf` => `blits`
* `blitse` has no analyses either

**svimerkje** failures:

* `svimerkje+V+Inf` does not generate!
* `svimerkje` has no analyses either

**frasere** failures:

* `frasere+V+Inf` => `fraser`
* `frasere` has no analyses either

**skvape** failures:

* `skvape+V+Inf` => `skvap`
* `skvape` has no analyses either

**poke** failures:

* `poke+V+Inf` => `pok`
* `poke` has no analyses either

**stele** failures:

* `stele+V+Inf` => `stee`
* `stele+V+Inf` => `stea`
* `stele` has no analyses either

**kolle** failures:

* `kolle+V+Inf` => `koll`
* `kolle` has no analyses either

**avsette** failures:

* `avsette+V+Inf` does not generate!
* `avsette` has no analyses either

**sveime** failures:

* `sveime+V+Inf` => `sveim`
* `sveime` has no analyses either

**kibbe** failures:

* `kibbe+V+Inf` => `kibb`
* `kibbe` has no analyses either

**skrubbe** failures:

* `skrubbe+V+Inf` => `skrubb`
* `skrubbe` has no analyses either

**fluoridere** failures:

* `fluoridere+V+Inf` => `fluorider`
* `fluoridere` has no analyses either

**omgjeve** failures:

* `omgjeve+V+Inf` => `omggjeva`
* `omgjeve+V+Inf` => `omggjeve`
* `omgjeve` has no analyses either

**omskole** failures:

* `omskole+V+Inf` => `omskol`
* `omskole` has no analyses either

**skjerpe** failures:

* `skjerpe+V+Inf` does not generate!
* `skjerpe` has no analyses either

**fiksjonalisere** failures:

* `fiksjonalisere+V+Inf` => `fiksjonaliser`
* `fiksjonalisere` has no analyses either

**skiplast** failures:

* `skiplast+V+Inf` => `skiplastast`
* `skiplast` has no analyses either

**skote** failures:

* `skote+V+Inf` => `skot`
* `skote` has following analyses:
  * `skoten+A+Neu+Sg+Indef`

**fløyme** failures:

* `fløyme+V+Inf` does not generate!
* `fløyme` has no analyses either

**forprosjektere** failures:

* `forprosjektere+V+Inf` => `forprosjekter`
* `forprosjektere` has no analyses either

**sjukne** failures:

* `sjukne+V+Inf` => `sjukn`
* `sjukne` has no analyses either

**dekretere** failures:

* `dekretere+V+Inf` => `dekreter`
* `dekretere` has no analyses either

**krasse** failures:

* `krasse+V+Inf` => `krass`
* `krasse` has no analyses either

**skvampe** failures:

* `skvampe+V+Inf` => `skvamp`
* `skvampe` has no analyses either

**hektografere** failures:

* `hektografere+V+Inf` => `hektografer`
* `hektografere` has no analyses either

**base** failures:

* `base+V+Inf` => `bas`
* `base` has no analyses either

**avbalansere** failures:

* `avbalansere+V+Inf` => `avbalanser`
* `avbalansere` has no analyses either

**kickstarte** failures:

* `kickstarte+V+Inf` => `kickstart`
* `kickstarte` has no analyses either

**revolusjonere** failures:

* `revolusjonere+V+Inf` => `revolusjoner`
* `revolusjonere` has no analyses either

**skvaldre** failures:

* `skvaldre+V+Inf` => `skvaldr`
* `skvaldre` has no analyses either

**emittere** failures:

* `emittere+V+Inf` => `emitter`
* `emittere` has no analyses either

**gnugge** failures:

* `gnugge+V+Inf` => `gnugg`
* `gnugge` has no analyses either

**dirke** failures:

* `dirke+V+Inf` => `dirk`
* `dirke` has no analyses either

**gjenspegle** failures:

* `gjenspegle+V+Inf` => `gjenspegl`
* `gjenspegle` has no analyses either

**bølge** failures:

* `bølge+V+Inf` => `bølg`
* `bølge` has no analyses either

**gjøde** failures:

* `gjøde+V+Inf` does not generate!
* `gjøde` has following analyses:
  * `gjødd+A+A+Pl+Def`
  * `gjødd+A+A+Sg+Def`
  * `gjødd+A+A+Pl+Indef`

**krenke** failures:

* `krenke+V+Inf` => `krenk`
* `krenke` has no analyses either

**møke** failures:

* `møke+V+Inf` => `møk`
* `møke` has no analyses either

**utrette** failures:

* `utrette+V+Inf` => `utrett`
* `utrette` has following analyses:
  * `utrett+A+A+Sg+Def`
  * `utrett+A+A+Pl+Indef`
  * `utrett+A+A+Pl+Def`

**mangle** failures:

* `mangle+V+Inf` => `mangl`
* `mangle` has no analyses either

**kjøve** failures:

* `kjøve+V+Inf` does not generate!
* `kjøve` has no analyses either

**perforere** failures:

* `perforere+V+Inf` => `perforer`
* `perforere` has no analyses either

**låst** failures:

* `låst+V+Inf` => `llåst`
* `låst` has following analyses:
  * `låse+V+Ind+Prt`

**plapre** failures:

* `plapre+V+Inf` => `plapr`
* `plapre` has no analyses either

**sukke** failures:

* `sukke+V+Inf` => `sukk`
* `sukke` has no analyses either

**flyte** failures:

* `flyte+V+Inf` => `fl`
* `flyte` has no analyses either

**settle** failures:

* `settle+V+Inf` => `settl`
* `settle` has no analyses either

**buklande** failures:

* `buklande+V+Inf` => `bukland`
* `buklande` has following analyses:
  * `bukle+V+PrsPrc`

**halloe** failures:

* `halloe+V+Inf` => `hallo`
* `halloe` has no analyses either

**latinisere** failures:

* `latinisere+V+Inf` => `latiniser`
* `latinisere` has no analyses either

**andast** failures:

* `andast+V+Inf` => `andastast`
* `andast` has no analyses either

**kauke** failures:

* `kauke+V+Inf` => `kauk`
* `kauke` has no analyses either

**vase** failures:

* `vase+V+Inf` => `vas`
* `vase` has following analyses:
  * `vassen+A+Neu+Sg+Indef`

**nagle** failures:

* `nagle+V+Inf` => `nagl`
* `nagle` has no analyses either

**tvoge** failures:

* `tvoge+V+Inf` => `tvog`
* `tvoge` has no analyses either

**eidfeste** failures:

* `eidfeste+V+Inf` does not generate!
* `eidfeste` has no analyses either

**vakuumere** failures:

* `vakuumere+V+Inf` => `vakuumer`
* `vakuumere` has no analyses either

**reinskrive** failures:

* `reinskrive+V+Inf` => `reinskrie`
* `reinskrive+V+Inf` => `reinskria`
* `reinskrive` has no analyses either

**utpeike** failures:

* `utpeike+V+Inf` => `utpeik`
* `utpeike` has no analyses either

**ettersløkkje** failures:

* `ettersløkkje+V+Inf` does not generate!
* `ettersløkkje` has no analyses either

**slutte** failures:

* `slutte+V+Inf` => `slutt`
* `slutte` has following analyses:
  * `slutt+A+A+Pl+Def`
  * `slutt+A+A+Pl+Indef`
  * `slutt+A+A+Sg+Def`

**uteksaminere** failures:

* `uteksaminere+V+Inf` => `uteksaminer`
* `uteksaminere` has no analyses either

**gnuffe** failures:

* `gnuffe+V+Inf` => `gnuff`
* `gnuffe` has no analyses either

**hækje** failures:

* `hækje+V+Inf` does not generate!
* `hækje` has no analyses either

**skave** failures:

* `skave+V+Inf` does not generate!
* `skave` has no analyses either

**knustre** failures:

* `knustre+V+Inf` => `knustr`
* `knustre` has no analyses either

**fryne** failures:

* `fryne+V+Inf` => `fryn`
* `fryne` has no analyses either

**ljosne** failures:

* `ljosne+V+Inf` => `ljosn`
* `ljosne` has no analyses either

**hydrogenere** failures:

* `hydrogenere+V+Inf` => `hydrogener`
* `hydrogenere` has no analyses either

**namnkjenne** failures:

* `namnkjenne+V+Inf` does not generate!
* `namnkjenne` has no analyses either

**ettersløkke** failures:

* `ettersløkke+V+Inf` does not generate!
* `ettersløkke` has no analyses either

**skòlregne** failures:

* `skòlregne+V+Inf` => `skòlregn`
* `skòlregne` has no analyses either

**skyle** failures:

* `skyle+V+Inf` => `skyl`
* `skyle` has no analyses either

**tredoble** failures:

* `tredoble+V+Inf` => `tredobl`
* `tredoble` has no analyses either

**innfinne** failures:

* `innfinne+V+Inf` => `innfia`
* `innfinne+V+Inf` => `innfie`
* `innfinne` has no analyses either

**nudde** failures:

* `nudde+V+Inf` => `nudd`
* `nudde` has no analyses either

**forgripe** failures:

* `forgripe+V+Inf` => `forgria`
* `forgripe+V+Inf` => `forgrie`
* `forgripe` has no analyses either

**avbetale** failures:

* `avbetale+V+Inf` does not generate!
* `avbetale` has no analyses either

**kadmiere** failures:

* `kadmiere+V+Inf` => `kadmier`
* `kadmiere` has no analyses either

**gytte** failures:

* `gytte+V+Inf` => `gytt`
* `gytte` has no analyses either

**rumpeake** failures:

* `rumpeake+V+Inf` => `rumpe`
* `rumpeake` has no analyses either

**heimsøke** failures:

* `heimsøke+V+Inf` => `heimsøk`
* `heimsøke` has no analyses either

**parallellføre** failures:

* `parallellføre+V+Inf` => `parallellfør`
* `parallellføre` has no analyses either

**koltne** failures:

* `koltne+V+Inf` => `koltn`
* `koltne` has following analyses:
  * `kolten+A+Sg+Def`
  * `kolten+A+Pl+Indef`

**ektevigsle** failures:

* `ektevigsle+V+Inf` => `ektevigsl`
* `ektevigsle` has no analyses either

**kollasjonere** failures:

* `kollasjonere+V+Inf` => `kollasjoner`
* `kollasjonere` has no analyses either

**opptake** failures:

* `opptake+V+Inf` => `oppttaka`
* `opptake+V+Inf` => `oppttake`
* `opptake` has no analyses either

**effeminere** failures:

* `effeminere+V+Inf` => `effeminer`
* `effeminere` has no analyses either

**steinsette** failures:

* `steinsette+V+Inf` does not generate!
* `steinsette` has no analyses either

**aktivere** failures:

* `aktivere+V+Inf` => `aktiver`
* `aktivere` has no analyses either

**nyskipe** failures:

* `nyskipe+V+Inf` => `nyskip`
* `nyskipe` has no analyses either

**sutenere** failures:

* `sutenere+V+Inf` => `sutener`
* `sutenere` has no analyses either

**tempere** failures:

* `tempere+V+Inf` => `temper`
* `tempere` has no analyses either

**glipe** failures:

* `glipe+V+Inf` => `glip`
* `glipe` has no analyses either

**lue** failures:

* `lue+V+Inf` => `lu`
* `lue` has no analyses either

**akkviescere** failures:

* `akkviescere+V+Inf` => `akkviescer`
* `akkviescere` has no analyses either

**overvake** failures:

* `overvake+V+Inf` => `overvak`
* `overvake` has no analyses either

**trøyste** failures:

* `trøyste+V+Inf` => `trøyst`
* `trøyste` has no analyses either

**barske** failures:

* `barske+V+Inf` => `barsk`
* `barske` has following analyses:
  * `barsk+A+A+Sg+Def`
  * `barsk+A+A+Pl+Indef`
  * `barsk+A+A+Pl+Def`

**prise** failures:

* `prise+V+Inf` => `pris`
* `prise` has no analyses either

**båre** failures:

* `båre+V+Inf` => `bår`
* `båre` has no analyses either

**bråbremse** failures:

* `bråbremse+V+Inf` => `bråbrems`
* `bråbremse` has no analyses either

**sale** failures:

* `sale+V+Inf` => `sal`
* `sale` has no analyses either

**fløkje** failures:

* `fløkje+V+Inf` does not generate!
* `fløkje` has no analyses either

**formgje** failures:

* `formgje+V+Inf` => `formggje`
* `formgje` has no analyses either

**relatere** failures:

* `relatere+V+Inf` => `relater`
* `relatere` has no analyses either

**fordømme** failures:

* `fordømme+V+Inf` does not generate!
* `fordømme` has no analyses either

**surke** failures:

* `surke+V+Inf` => `surk`
* `surke` has no analyses either

**sløkke** failures:

* `sløkke+V+Inf` does not generate!
* `sløkke` has no analyses either

**relegere** failures:

* `relegere+V+Inf` => `releger`
* `relegere` has no analyses either

**deklassere** failures:

* `deklassere+V+Inf` => `deklasser`
* `deklassere` has no analyses either

**skjerve** failures:

* `skjerve+V+Inf` => `skjerv`
* `skjerve` has no analyses either

**anodisere** failures:

* `anodisere+V+Inf` => `anodiser`
* `anodisere` has no analyses either

**arve** failures:

* `arve+V+Inf` => `arv`
* `arve` has no analyses either

**sandmale** failures:

* `sandmale+V+Inf` => `sandmae`
* `sandmale+V+Inf` => `sandmaa`
* `sandmale` has no analyses either

**hikke** failures:

* `hikke+V+Inf` => `hikk`
* `hikke` has no analyses either

**orke** failures:

* `orke+V+Inf` => `ork`
* `orke` has no analyses either

**bregde** failures:

* `bregde+V+Inf` does not generate!
* `bregde` has no analyses either

**kausjonere** failures:

* `kausjonere+V+Inf` => `kausjoner`
* `kausjonere` has no analyses either

**berrlegge** failures:

* `berrlegge+V+Inf` does not generate!
* `berrlegge` has no analyses either

**nyse** failures:

* `nyse+V+Inf` => `nya`
* `nyse+V+Inf` => `nye`
* `nyse` has no analyses either

**onanere** failures:

* `onanere+V+Inf` => `onaner`
* `onanere` has no analyses either

**påpeike** failures:

* `påpeike+V+Inf` does not generate!
* `påpeike` has no analyses either

**usurpere** failures:

* `usurpere+V+Inf` => `usurper`
* `usurpere` has no analyses either

**hauste** failures:

* `hauste+V+Inf` => `haust`
* `hauste` has no analyses either

**kvelve** failures:

* `kvelve+V+Inf` does not generate!
* `kvelve` has following analyses:
  * `kvelven+A+Neu+Sg+Indef`

**levitere** failures:

* `levitere+V+Inf` => `leviter`
* `levitere` has no analyses either

**råle** failures:

* `råle+V+Inf` => `rål`
* `råle` has no analyses either

**snækje** failures:

* `snækje+V+Inf` does not generate!
* `snækje` has no analyses either

**krele** failures:

* `krele+V+Inf` => `krel`
* `krele` has no analyses either

**fortolke** failures:

* `fortolke+V+Inf` => `fortolk`
* `fortolke` has no analyses either

**lingse** failures:

* `lingse+V+Inf` => `lings`
* `lingse` has no analyses either

**propagere** failures:

* `propagere+V+Inf` => `propager`
* `propagere` has no analyses either

**tangere** failures:

* `tangere+V+Inf` => `tanger`
* `tangere` has no analyses either

**marinere** failures:

* `marinere+V+Inf` => `mariner`
* `marinere` has no analyses either

**ræse** failures:

* `ræse+V+Inf` => `ræs`
* `ræse` has no analyses either

**tilkalle** failures:

* `tilkalle+V+Inf` => `tilkall`
* `tilkalle` has no analyses either

**frode** failures:

* `frode+V+Inf` => `frod`
* `frode` has following analyses:
  * `frod+A+A+Sg+Def`
  * `froden+A+Neu+Sg+Indef`
  * `frod+A+A+Pl+Def`
  * `frod+A+A+Pl+Indef`

**tynske** failures:

* `tynske+V+Inf` => `tynsk`
* `tynske` has no analyses either

**kooperere** failures:

* `kooperere+V+Inf` => `kooperer`
* `kooperere` has no analyses either

**lenke** failures:

* `lenke+V+Inf` => `lenk`
* `lenke` has no analyses either

**frøyne** failures:

* `frøyne+V+Inf` => `frøyn`
* `frøyne` has following analyses:
  * `frøyen+A+Sg+Def`
  * `frøyen+A+Pl+Indef`
  * `frøyn+A+Sg+Def`
  * `frøyn+A+Pl+Indef`

**antikisere** failures:

* `antikisere+V+Inf` => `antikiser`
* `antikisere` has no analyses either

**grevsetje** failures:

* `grevsetje+V+Inf` does not generate!
* `grevsetje` has no analyses either

**vakuumpakke** failures:

* `vakuumpakke+V+Inf` => `vakuumpakk`
* `vakuumpakke` has no analyses either

**tilsløre** failures:

* `tilsløre+V+Inf` does not generate!
* `tilsløre` has no analyses either

**vanhelge** failures:

* `vanhelge+V+Inf` => `vanhelg`
* `vanhelge` has no analyses either

**fortrengje** failures:

* `fortrengje+V+Inf` does not generate!
* `fortrengje` has no analyses either

**emulgere** failures:

* `emulgere+V+Inf` => `emulger`
* `emulgere` has no analyses either

**behovsprøve** failures:

* `behovsprøve+V+Inf` does not generate!
* `behovsprøve` has following analyses:
  * `behovsprøvd+A+A+Sg+Def`
  * `behovsprøvd+A+A+Pl+Def`
  * `behovsprøvd+A+A+Pl+Indef`

**røyste** failures:

* `røyste+V+Inf` => `røyst`
* `røyste` has no analyses either

**hile** failures:

* `hile+V+Inf` => `hil`
* `hile` has no analyses either

**kuppe** failures:

* `kuppe+V+Inf` => `kupp`
* `kuppe` has no analyses either

**byrgsle** failures:

* `byrgsle+V+Inf` => `byrgsl`
* `byrgsle` has no analyses either

**glape** failures:

* `glape+V+Inf` => `glap`
* `glape` has no analyses either

**krossfeste** failures:

* `krossfeste+V+Inf` does not generate!
* `krossfeste` has no analyses either

**transmittere** failures:

* `transmittere+V+Inf` => `transmitter`
* `transmittere` has no analyses either

**flokkast** failures:

* `flokkast+V+Inf` => `flokkastast`
* `flokkast` has no analyses either

**fordele** failures:

* `fordele+V+Inf` => `fordel`
* `fordele` has no analyses either

**sinke** failures:

* `sinke+V+Inf` => `sink`
* `sinke` has following analyses:
  * `sinken+A+Neu+Sg+Indef`

**kokettere** failures:

* `kokettere+V+Inf` => `koketter`
* `kokettere` has no analyses either

**gapflire** failures:

* `gapflire+V+Inf` does not generate!
* `gapflire` has no analyses either

**overtrekkje** failures:

* `overtrekkje+V+Inf` does not generate!
* `overtrekkje` has no analyses either

**antirusthandsame** failures:

* `antirusthandsame+V+Inf` => `antirusthandsam`
* `antirusthandsame` has no analyses either

**shunte** failures:

* `shunte+V+Inf` => `shunt`
* `shunte` has no analyses either

**dødse** failures:

* `dødse+V+Inf` => `døds`
* `dødse` has no analyses either

**torne** failures:

* `torne+V+Inf` => `torn`
* `torne` has no analyses either

**heve** failures:

* `heve+V+Inf` does not generate!
* `heve` has following analyses:
  * `hever+A+A+Sg+Def`
  * `hever+A+A+Pl+Def`
  * `hever+A+A+Pl+Indef`

**hemne** failures:

* `hemne+V+Inf` does not generate!
* `hemne` has no analyses either

**vagle** failures:

* `vagle+V+Inf` => `vagl`
* `vagle` has no analyses either

**fornærme** failures:

* `fornærme+V+Inf` => `fornærm`
* `fornærme` has no analyses either

**delegere** failures:

* `delegere+V+Inf` => `deleger`
* `delegere` has no analyses either

**hjelle** failures:

* `hjelle+V+Inf` => `hjell`
* `hjelle` has no analyses either

**rettleie** failures:

* `rettleie+V+Inf` does not generate!
* `rettleie` has no analyses either

**borde** failures:

* `borde+V+Inf` => `bord`
* `borde` has no analyses either

**nuve** failures:

* `nuve+V+Inf` => `nuv`
* `nuve` has following analyses:
  * `nuven+A+Neu+Sg+Indef`
  * `nuv+A+Sg+Def`
  * `nuv+A+Pl+Indef`

**kritle** failures:

* `kritle+V+Inf` => `kritl`
* `kritle` has no analyses either

**e-poste** failures:

* `e-poste+V+Inf` => `e-post`
* `e-poste` has no analyses either

**flikre** failures:

* `flikre+V+Inf` => `flikr`
* `flikre` has no analyses either

**støpsle** failures:

* `støpsle+V+Inf` => `støpsl`
* `støpsle` has no analyses either

**tømre** failures:

* `tømre+V+Inf` => `tømr`
* `tømre` has no analyses either

**kverve** failures:

* `kverve+V+Inf` does not generate!
* `kverve` has following analyses:
  * `kverv+A+Sg+Def`
  * `kverven+A+Neu+Sg+Indef`
  * `kverv+A+Pl+Indef`

**skratte** failures:

* `skratte+V+Inf` => `skratt`
* `skratte` has following analyses:
  * `skratt+A+A+Sg+Def`
  * `skratt+A+A+Pl+Indef`
  * `skratt+A+A+Pl+Def`

**translitterere** failures:

* `translitterere+V+Inf` => `translitterer`
* `translitterere` has no analyses either

**vidne** failures:

* `vidne+V+Inf` => `vidn`
* `vidne` has no analyses either

**staple** failures:

* `staple+V+Inf` => `stapl`
* `staple` has no analyses either

**skore** failures:

* `skore+V+Inf` => `skor`
* `skore` has following analyses:
  * `skoren+A+Neu+Sg+Indef`

**spurte** failures:

* `spurte+V+Inf` => `spurt`
* `spurte` has no analyses either

**stoppe** failures:

* `stoppe+V+Inf` => `stopp`
* `stoppe` has no analyses either

**alludere** failures:

* `alludere+V+Inf` => `alluder`
* `alludere` has no analyses either

**uke** failures:

* `uke+V+Inf` => `uk`
* `uke` has no analyses either

**delirere** failures:

* `delirere+V+Inf` => `delirer`
* `delirere` has no analyses either

**profesjonalisere** failures:

* `profesjonalisere+V+Inf` => `profesjonaliser`
* `profesjonalisere` has no analyses either

**ide** failures:

* `ide+V+Inf` => `id`
* `ide` has no analyses either

**blottlegge** failures:

* `blottlegge+V+Inf` does not generate!
* `blottlegge` has no analyses either

**stabilisere** failures:

* `stabilisere+V+Inf` => `stabiliser`
* `stabilisere` has no analyses either

**studere** failures:

* `studere+V+Inf` => `studer`
* `studere` has no analyses either

**forarge** failures:

* `forarge+V+Inf` => `forarg`
* `forarge` has no analyses either

**galnast** failures:

* `galnast+V+Inf` => `galnastast`
* `galnast` has following analyses:
  * `galen+A+Superl+Indef`

**fippe** failures:

* `fippe+V+Inf` => `fipp`
* `fippe` has no analyses either

**garve** failures:

* `garve+V+Inf` => `garv`
* `garve` has no analyses either

**forårsake** failures:

* `forårsake+V+Inf` => `forårsak`
* `forårsake` has no analyses either

**samtykkje** failures:

* `samtykkje+V+Inf` does not generate!
* `samtykkje` has no analyses either

**livnære** failures:

* `livnære+V+Inf` => `livnær`
* `livnære` has no analyses either

**bolte** failures:

* `bolte+V+Inf` => `bolt`
* `bolte` has no analyses either

**sadde** failures:

* `sadde+V+Inf` => `sadd`
* `sadde` has no analyses either

**ange** failures:

* `ange+V+Inf` => `ang`
* `ange` has no analyses either

**jåsse** failures:

* `jåsse+V+Inf` => `jåss`
* `jåsse` has no analyses either

**vyrde** failures:

* `vyrde+V+Inf` does not generate!
* `vyrde` has following analyses:
  * `vyrden+A+Neu+Sg+Indef`

**holde** failures:

* `holde+V+Inf` => `hold`
* `holde` has no analyses either

**fortne** failures:

* `fortne+V+Inf` => `fortn`
* `fortne` has no analyses either

**skavle** failures:

* `skavle+V+Inf` => `skavl`
* `skavle` has no analyses either

**vitrast** failures:

* `vitrast+V+Inf` => `vitrastast`
* `vitrast` has no analyses either

**svare** failures:

* `svare+V+Inf` does not generate!
* `svare` has following analyses:
  * `svare+A+Pl+Indef`
  * `svare+A+Fem+Sg+Indef`
  * `svare+A+Sg+Def`
  * `svare+A+Neu+Sg+Indef`
  * `svare+A+Msc+Sg+Indef`

**lækje** failures:

* `lækje+V+Inf` does not generate!
* `lækje` has no analyses either

**appropriere** failures:

* `appropriere+V+Inf` => `approprier`
* `appropriere` has no analyses either

**institusjonalisere** failures:

* `institusjonalisere+V+Inf` => `institusjonaliser`
* `institusjonalisere` has no analyses either

**trolle** failures:

* `trolle+V+Inf` => `troll`
* `trolle` has no analyses either

**hinke** failures:

* `hinke+V+Inf` => `hink`
* `hinke` has no analyses either

**kramse** failures:

* `kramse+V+Inf` => `krams`
* `kramse` has no analyses either

**avsanne** failures:

* `avsanne+V+Inf` => `avsann`
* `avsanne` has no analyses either

**handsame** failures:

* `handsame+V+Inf` => `handsam`
* `handsame` has no analyses either

**fremme** failures:

* `fremme+V+Inf` does not generate!
* `fremme` has no analyses either

**forsetje** failures:

* `forsetje+V+Inf` does not generate!
* `forsetje` has no analyses either

**frottere** failures:

* `frottere+V+Inf` => `frotter`
* `frottere` has no analyses either

**jakse** failures:

* `jakse+V+Inf` => `jaks`
* `jakse` has no analyses either

**sleive** failures:

* `sleive+V+Inf` => `sleiv`
* `sleive` has no analyses either

**kjøte** failures:

* `kjøte+V+Inf` => `kjøt`
* `kjøte` has no analyses either

**sjaske** failures:

* `sjaske+V+Inf` => `sjask`
* `sjaske` has no analyses either

**heise** failures:

* `heise+V+Inf` does not generate!
* `heise` has following analyses:
  * `heisen+A+Neu+Sg+Indef`

**kyle** failures:

* `kyle+V+Inf` does not generate!
* `kyle` has no analyses either

**klengje** failures:

* `klengje+V+Inf` does not generate!
* `klengje` has no analyses either

**kukke** failures:

* `kukke+V+Inf` => `kukk`
* `kukke` has no analyses either

**moke** failures:

* `moke+V+Inf` => `mok`
* `moke` has no analyses either

**blåpugge** failures:

* `blåpugge+V+Inf` => `blåpugg`
* `blåpugge` has no analyses either

**kulske** failures:

* `kulske+V+Inf` => `kulsk`
* `kulske` has no analyses either

**nakke** failures:

* `nakke+V+Inf` => `nakk`
* `nakke` has no analyses either

**turne** failures:

* `turne+V+Inf` => `turn`
* `turne` has following analyses:
  * `turne+N+N+Msc+Sg+Indef`

**slumre** failures:

* `slumre+V+Inf` => `slumr`
* `slumre` has no analyses either

**forkjæle** failures:

* `forkjæle+V+Inf` => `forkjæl`
* `forkjæle` has no analyses either

**drangle** failures:

* `drangle+V+Inf` => `drangl`
* `drangle` has no analyses either

**kanonisere** failures:

* `kanonisere+V+Inf` => `kanoniser`
* `kanonisere` has no analyses either

**dramatisere** failures:

* `dramatisere+V+Inf` => `dramatiser`
* `dramatisere` has no analyses either

**degge** failures:

* `degge+V+Inf` => `degg`
* `degge` has no analyses either

**protestere** failures:

* `protestere+V+Inf` => `protester`
* `protestere` has no analyses either

**sable** failures:

* `sable+V+Inf` => `sabl`
* `sable` has no analyses either

**forvenne** failures:

* `forvenne+V+Inf` does not generate!
* `forvenne` has no analyses either

**konsultere** failures:

* `konsultere+V+Inf` => `konsulter`
* `konsultere` has no analyses either

**sugle** failures:

* `sugle+V+Inf` => `sugl`
* `sugle` has no analyses either

**eine** failures:

* `eine+V+Inf` does not generate!
* `eine` has no analyses either

**ulukkesforsikre** failures:

* `ulukkesforsikre+V+Inf` => `ulukkesforsikr`
* `ulukkesforsikre` has no analyses either

**teoretisere** failures:

* `teoretisere+V+Inf` => `teoretiser`
* `teoretisere` has no analyses either

**bergta** failures:

* `bergta+V+Inf` => `bergtta`
* `bergta` has no analyses either

**varme** failures:

* `varme+V+Inf` => `varm`
* `varme` has following analyses:
  * `varm+A+Pl+Indef`
  * `varm+A+Sg+Def`

**totalfrede** failures:

* `totalfrede+V+Inf` => `totalfred`
* `totalfrede` has no analyses either

**mistenkjeleggjere** failures:

* `mistenkjeleggjere+V+Inf` does not generate!
* `mistenkjeleggjere` has no analyses either

**gestikulere** failures:

* `gestikulere+V+Inf` => `gestikuler`
* `gestikulere` has no analyses either

**langpine** failures:

* `langpine+V+Inf` => `langpin`
* `langpine` has no analyses either

**lovsynge** failures:

* `lovsynge+V+Inf` => `lovsye`
* `lovsynge+V+Inf` => `lovsya`
* `lovsynge` has no analyses either

**samstille** failures:

* `samstille+V+Inf` does not generate!
* `samstille` has no analyses either

**skrumpe** failures:

* `skrumpe+V+Inf` => `skrump`
* `skrumpe` has no analyses either

**tiltrekke** failures:

* `tiltrekke+V+Inf` => `tiltreke`
* `tiltrekke+V+Inf` => `tiltreka`
* `tiltrekke` has no analyses either

**huie** failures:

* `huie+V+Inf` => `hui`
* `huie` has no analyses either

**forgjere** failures:

* `forgjere+V+Inf` does not generate!
* `forgjere` has no analyses either

**ovundre** failures:

* `ovundre+V+Inf` => `ovundr`
* `ovundre` has no analyses either

**prette** failures:

* `prette+V+Inf` => `prett`
* `prette` has no analyses either

**sense** failures:

* `sense+V+Inf` => `sens`
* `sense` has no analyses either

**reassurere** failures:

* `reassurere+V+Inf` => `reassurer`
* `reassurere` has no analyses either

**parere** failures:

* `parere+V+Inf` => `parer`
* `parere` has no analyses either

**dissekere** failures:

* `dissekere+V+Inf` => `disseker`
* `dissekere` has no analyses either

**jobbe** failures:

* `jobbe+V+Inf` => `jobb`
* `jobbe` has no analyses either

**kvelpe** failures:

* `kvelpe+V+Inf` => `kvelp`
* `kvelpe` has no analyses either

**rømme** failures:

* `rømme+V+Inf` does not generate!
* `rømme` has no analyses either

**matrikulere** failures:

* `matrikulere+V+Inf` => `matrikuler`
* `matrikulere` has no analyses either

**showe** failures:

* `showe+V+Inf` => `show`
* `showe` has no analyses either

**forme** failures:

* `forme+V+Inf` => `form`
* `forme` has no analyses either

**tverrykkje** failures:

* `tverrykkje+V+Inf` does not generate!
* `tverrykkje` has no analyses either

**gispe** failures:

* `gispe+V+Inf` => `gisp`
* `gispe` has no analyses either

**sirkle** failures:

* `sirkle+V+Inf` => `sirkl`
* `sirkle` has no analyses either

**sletne** failures:

* `sletne+V+Inf` => `sletn`
* `sletne` has no analyses either

**gratulere** failures:

* `gratulere+V+Inf` => `gratuler`
* `gratulere` has no analyses either

**fortsetje** failures:

* `fortsetje+V+Inf` does not generate!
* `fortsetje` has no analyses either

**buse** failures:

* `buse+V+Inf` does not generate!
* `buse` has following analyses:
  * `bussen+A+Neu+Sg+Indef`
  * `bus+A+Pl+Indef`
  * `bus+A+Sg+Def`

**listeføre** failures:

* `listeføre+V+Inf` => `listefør`
* `listeføre` has no analyses either

**klampe** failures:

* `klampe+V+Inf` => `klamp`
* `klampe` has no analyses either

**fake** failures:

* `fake+V+Inf` => `fak`
* `fake` has following analyses:
  * `fake+A+Msc+Sg+Indef`
  * `fake+A+Sg+Def`
  * `fake+A+Fem+Sg+Indef`
  * `fake+A+Pl+Indef`
  * `fake+A+Neu+Sg+Indef`

**mose** failures:

* `mose+V+Inf` => `mos`
* `mose` has following analyses:
  * `mosen+A+Neu+Sg+Indef`

**kamuflere** failures:

* `kamuflere+V+Inf` => `kamufler`
* `kamuflere` has no analyses either

**annektere** failures:

* `annektere+V+Inf` => `annekter`
* `annektere` has no analyses either

**kulminere** failures:

* `kulminere+V+Inf` => `kulminer`
* `kulminere` has no analyses either

**narkotisere** failures:

* `narkotisere+V+Inf` => `narkotiser`
* `narkotisere` has no analyses either

**overhale** failures:

* `overhale+V+Inf` does not generate!
* `overhale` has no analyses either

**bese** failures:

* `bese+V+Inf` => `bes`
* `bese+V+Inf` => `besjå`
* `bese` has no analyses either

**skrive** failures:

* `skrive+V+Inf` => `skrie`
* `skrive+V+Inf` => `skria`
* `skrive` has following analyses:
  * `skriven+A+Neu+Sg+Indef`

**hekte** failures:

* `hekte+V+Inf` => `hekt`
* `hekte` has no analyses either

**flakke** failures:

* `flakke+V+Inf` => `flakk`
* `flakke` has no analyses either

**smaske** failures:

* `smaske+V+Inf` => `smask`
* `smaske` has no analyses either

**botnskrape** failures:

* `botnskrape+V+Inf` => `botnskrap`
* `botnskrape` has no analyses either

**assistere** failures:

* `assistere+V+Inf` => `assister`
* `assistere` has no analyses either

**oppgradere** failures:

* `oppgradere+V+Inf` => `oppgrader`
* `oppgradere` has no analyses either

**tviauke** failures:

* `tviauke+V+Inf` => `tviauk`
* `tviauke` has no analyses either

**baste** failures:

* `baste+V+Inf` => `bast`
* `baste` has no analyses either

**attachere** failures:

* `attachere+V+Inf` => `attacher`
* `attachere` has no analyses either

**lovsyngje** failures:

* `lovsyngje+V+Inf` => `lovsyje`
* `lovsyngje+V+Inf` => `lovsyja`
* `lovsyngje` has no analyses either

**forherde** failures:

* `forherde+V+Inf` => `forherd`
* `forherde` has following analyses:
  * `forherd+A+A+Pl+Def`
  * `forherd+A+A+Pl+Indef`
  * `forherd+A+A+Sg+Def`

**røyklegge** failures:

* `røyklegge+V+Inf` does not generate!
* `røyklegge` has no analyses either

**drøyle** failures:

* `drøyle+V+Inf` => `drøyl`
* `drøyle` has following analyses:
  * `drøylen+A+Neu+Sg+Indef`

**uskadeleggjere** failures:

* `uskadeleggjere+V+Inf` does not generate!
* `uskadeleggjere` has no analyses either

**prisgjeve** failures:

* `prisgjeve+V+Inf` => `prisggjeve`
* `prisgjeve+V+Inf` => `prisggjeva`
* `prisgjeve` has no analyses either

**undersøke** failures:

* `undersøke+V+Inf` => `undersøk`
* `undersøke` has no analyses either

**forsølve** failures:

* `forsølve+V+Inf` => `forsølv`
* `forsølve` has no analyses either

**sjonglere** failures:

* `sjonglere+V+Inf` => `sjongler`
* `sjonglere` has no analyses either

**tilsetje** failures:

* `tilsetje+V+Inf` does not generate!
* `tilsetje` has no analyses either

**tåme** failures:

* `tåme+V+Inf` => `tåm`
* `tåme` has no analyses either

**sveige** failures:

* `sveige+V+Inf` => `sveig`
* `sveige` has following analyses:
  * `sveig+A+Pl+Indef`
  * `sveig+A+Sg+Def`

**omfatte** failures:

* `omfatte+V+Inf` => `omfatt`
* `omfatte` has no analyses either

**eksplikere** failures:

* `eksplikere+V+Inf` => `ekspliker`
* `eksplikere` has no analyses either

**drople** failures:

* `drople+V+Inf` => `dropl`
* `drople` has no analyses either

**triangulere** failures:

* `triangulere+V+Inf` => `trianguler`
* `triangulere` has no analyses either

**analysere** failures:

* `analysere+V+Inf` => `analyser`
* `analysere` has no analyses either

**bryte** failures:

* `bryte+V+Inf` => `brya`
* `bryte+V+Inf` => `brye`
* `bryte` has no analyses either

**tredele** failures:

* `tredele+V+Inf` => `tredel`
* `tredele` has no analyses either

**regummiere** failures:

* `regummiere+V+Inf` => `regummier`
* `regummiere` has no analyses either

**annamme** failures:

* `annamme+V+Inf` => `annam`
* `annamme` has no analyses either

**hase** failures:

* `hase+V+Inf` => `has`
* `hase` has no analyses either

**avbyråkratisere** failures:

* `avbyråkratisere+V+Inf` => `avbyråkratiser`
* `avbyråkratisere` has no analyses either

**bomme** failures:

* `bomme+V+Inf` => `bom`
* `bomme` has no analyses either

**knuspe** failures:

* `knuspe+V+Inf` => `knusp`
* `knuspe` has no analyses either

**proppe** failures:

* `proppe+V+Inf` => `propp`
* `proppe` has no analyses either

**punktsveise** failures:

* `punktsveise+V+Inf` does not generate!
* `punktsveise` has no analyses either

**sagge** failures:

* `sagge+V+Inf` => `sagg`
* `sagge` has no analyses either

**forhale** failures:

* `forhale+V+Inf` does not generate!
* `forhale` has no analyses either

**deilde** failures:

* `deilde+V+Inf` => `deild`
* `deilde` has no analyses either

**stinte** failures:

* `stinte+V+Inf` => `stint`
* `stinte` has no analyses either

**utbyte** failures:

* `utbyte+V+Inf` => `utbyt`
* `utbyte` has no analyses either

**utgiftsføre** failures:

* `utgiftsføre+V+Inf` => `utgiftsfør`
* `utgiftsføre` has no analyses either

**kringe** failures:

* `kringe+V+Inf` => `kring`
* `kringe` has following analyses:
  * `kring+A+Sg+Def`
  * `kring+A+Pl+Indef`

**samrøde** failures:

* `samrøde+V+Inf` does not generate!
* `samrøde` has no analyses either

**kite** failures:

* `kite+V+Inf` => `kit`
* `kite` has no analyses either

**ættleie** failures:

* `ættleie+V+Inf` does not generate!
* `ættleie` has no analyses either

**dele** failures:

* `dele+V+Inf` => `del`
* `dele` has no analyses either

**blautkoke** failures:

* `blautkoke+V+Inf` does not generate!
* `blautkoke` has no analyses either

**skotte** failures:

* `skotte+V+Inf` => `skott`
* `skotte` has no analyses either

**makeskifte** failures:

* `makeskifte+V+Inf` does not generate!
* `makeskifte` has no analyses either

**tafse** failures:

* `tafse+V+Inf` => `tafs`
* `tafse` has following analyses:
  * `tafsen+A+Neu+Sg+Indef`

**såle** failures:

* `såle+V+Inf` => `sål`
* `såle` has no analyses either

**slave** failures:

* `slave+V+Inf` => `slav`
* `slave` has no analyses either

**oppstøe** failures:

* `oppstøe+V+Inf` => `oppstø`
* `oppstøe` has no analyses either

**frysetørke** failures:

* `frysetørke+V+Inf` => `frysetørk`
* `frysetørke` has no analyses either

**inaugurere** failures:

* `inaugurere+V+Inf` => `inaugurer`
* `inaugurere` has no analyses either

**atterløyse** failures:

* `atterløyse+V+Inf` => `atterløys`
* `atterløyse` has no analyses either

**more** failures:

* `more+V+Inf` => `mor`
* `more` has no analyses either

**stjernemerke** failures:

* `stjernemerke+V+Inf` => `stjernemerk`
* `stjernemerke` has following analyses:
  * `stjernemerke+N`

**opalisere** failures:

* `opalisere+V+Inf` => `opaliser`
* `opalisere` has no analyses either

**frase** failures:

* `frase+V+Inf` => `fras`
* `frase` has no analyses either

**fenge** failures:

* `fenge+V+Inf` => `feng`
* `fenge` has following analyses:
  * `fengen+A+Neu+Sg+Indef`

**vanskjøtte** failures:

* `vanskjøtte+V+Inf` => `vanskjøtt`
* `vanskjøtte` has no analyses either

**uroppføre** failures:

* `uroppføre+V+Inf` => `uroppfør`
* `uroppføre` has no analyses either

**drille** failures:

* `drille+V+Inf` => `drill`
* `drille` has no analyses either

**svarpe** failures:

* `svarpe+V+Inf` => `svarp`
* `svarpe` has no analyses either

**soppe** failures:

* `soppe+V+Inf` => `sopp`
* `soppe` has no analyses either

**veitte** failures:

* `veitte+V+Inf` => `veitt`
* `veitte` has no analyses either

**stavre** failures:

* `stavre+V+Inf` => `stavr`
* `stavre` has no analyses either

**tilkomme** failures:

* `tilkomme+V+Inf` => `tilkkomme`
* `tilkomme+V+Inf` => `tilkkomma`
* `tilkomme` has no analyses either

**pendle** failures:

* `pendle+V+Inf` => `pendl`
* `pendle` has no analyses either

**klokke** failures:

* `klokke+V+Inf` => `klokk`
* `klokke` has no analyses either

**surkle** failures:

* `surkle+V+Inf` => `surkl`
* `surkle` has no analyses either

**kjeppjage** failures:

* `kjeppjage+V+Inf` => `kjeppjag`
* `kjeppjage` has no analyses either

**opne** failures:

* `opne+V+Inf` => `opn`
* `opne` has following analyses:
  * `open+A+Sg+Def`
  * `open+A+Pl+Indef`

**ordleie** failures:

* `ordleie+V+Inf` does not generate!
* `ordleie` has no analyses either

**søyre** failures:

* `søyre+V+Inf` does not generate!
* `søyre` has no analyses either

**samfarast** failures:

* `samfarast+V+Inf` => `samfaast`
* `samfarast` has no analyses either

**turvast** failures:

* `turvast+V+Inf` => `tuast`
* `turvast` has no analyses either

**sige** failures:

* `sige+V+Inf` => `sie`
* `sige+V+Inf` => `sia`
* `sige` has following analyses:
  * `sigg+A+A+Pl+Indef`
  * `sigg+A+A+Sg+Def`
  * `sigg+A+A+Pl+Def`

**dønne** failures:

* `dønne+V+Inf` does not generate!
* `dønne` has no analyses either

**turrsalte** failures:

* `turrsalte+V+Inf` => `turrsalt`
* `turrsalte` has no analyses either

**screene** failures:

* `screene+V+Inf` => `screen`
* `screene` has no analyses either

**hospitalisere** failures:

* `hospitalisere+V+Inf` => `hospitaliser`
* `hospitalisere` has no analyses either

**furnere** failures:

* `furnere+V+Inf` => `furner`
* `furnere` has no analyses either

**grune** failures:

* `grune+V+Inf` => `grun`
* `grune` has following analyses:
  * `grunn+A+A+Sg+Def`
  * `grunn+A+A+Pl+Indef`
  * `grunn+A+A+Pl+Def`

**skinke** failures:

* `skinke+V+Inf` => `skink`
* `skinke` has no analyses either

**flistre** failures:

* `flistre+V+Inf` => `flistr`
* `flistre` has no analyses either

**forspane** failures:

* `forspane+V+Inf` => `forspan`
* `forspane` has no analyses either

**rundlure** failures:

* `rundlure+V+Inf` => `rundlur`
* `rundlure` has no analyses either

**skamhogge** failures:

* `skamhogge+V+Inf` does not generate!
* `skamhogge` has no analyses either

**stenke** failures:

* `stenke+V+Inf` => `stenk`
* `stenke` has no analyses either

**distrahere** failures:

* `distrahere+V+Inf` => `distraher`
* `distrahere` has no analyses either

**monne** failures:

* `monne+V+Inf` => `monn`
* `monne` has no analyses either

**kappe** failures:

* `kappe+V+Inf` => `kapp`
* `kappe` has following analyses:
  * `kappete+V+Ind+Prs`
  * `kappete+V+Imprt`

**meinhalde** failures:

* `meinhalde+V+Inf` => `meinhaa`
* `meinhalde+V+Inf` => `meinhae`
* `meinhalde` has no analyses either

**gulne** failures:

* `gulne+V+Inf` => `guln`
* `gulne` has no analyses either

**touchere** failures:

* `touchere+V+Inf` => `toucher`
* `touchere` has no analyses either

**dekryptere** failures:

* `dekryptere+V+Inf` => `dekrypter`
* `dekryptere` has no analyses either

**samarbeide** failures:

* `samarbeide+V+Inf` does not generate!
* `samarbeide` has no analyses either

**fløygje** failures:

* `fløygje+V+Inf` does not generate!
* `fløygje` has no analyses either

**google** failures:

* `google+V+Inf` => `googl`
* `google` has no analyses either

**flaske** failures:

* `flaske+V+Inf` => `flask`
* `flaske` has no analyses either

**driple** failures:

* `driple+V+Inf` => `dripl`
* `driple` has no analyses either

**ringmerkje** failures:

* `ringmerkje+V+Inf` does not generate!
* `ringmerkje` has no analyses either

**gause** failures:

* `gause+V+Inf` => `gaus`
* `gause` has no analyses either

**ape** failures:

* `ape+V+Inf` does not generate!
* `ape` has no analyses either

**sibreie** failures:

* `sibreie+V+Inf` does not generate!
* `sibreie` has no analyses either

**stålsette** failures:

* `stålsette+V+Inf` does not generate!
* `stålsette` has no analyses either

**tekke** failures:

* `tekke+V+Inf` does not generate!
* `tekke` has no analyses either

**glire** failures:

* `glire+V+Inf` does not generate!
* `glire` has no analyses either

**døkkast** failures:

* `døkkast+V+Inf` => `døkkastast`
* `døkkast` has no analyses either

**skråle** failures:

* `skråle+V+Inf` does not generate!
* `skråle` has no analyses either

**svamle** failures:

* `svamle+V+Inf` => `svaml`
* `svamle` has no analyses either

**transplantere** failures:

* `transplantere+V+Inf` => `transplanter`
* `transplantere` has no analyses either

**kollokvere** failures:

* `kollokvere+V+Inf` => `kollokver`
* `kollokvere` has no analyses either

**streve** failures:

* `streve+V+Inf` does not generate!
* `streve` has no analyses either

**jatle** failures:

* `jatle+V+Inf` => `jatl`
* `jatle` has no analyses either

**finslipe** failures:

* `finslipe+V+Inf` does not generate!
* `finslipe` has no analyses either

**aktualisere** failures:

* `aktualisere+V+Inf` => `aktualiser`
* `aktualisere` has no analyses either

**skamfere** failures:

* `skamfere+V+Inf` => `skamfer`
* `skamfere` has no analyses either

**pilke** failures:

* `pilke+V+Inf` => `pilk`
* `pilke` has no analyses either

**krote** failures:

* `krote+V+Inf` => `krot`
* `krote` has no analyses either

**hundsette** failures:

* `hundsette+V+Inf` does not generate!
* `hundsette` has no analyses either

**overprøve** failures:

* `overprøve+V+Inf` does not generate!
* `overprøve` has no analyses either

**pudre** failures:

* `pudre+V+Inf` => `pudr`
* `pudre` has no analyses either

**kompensere** failures:

* `kompensere+V+Inf` => `kompenser`
* `kompensere` has no analyses either

**skreppe** failures:

* `skreppe+V+Inf` does not generate!
* `skreppe` has no analyses either

**attestere** failures:

* `attestere+V+Inf` => `attester`
* `attestere` has no analyses either

**hume** failures:

* `hume+V+Inf` => `hum`
* `hume` has following analyses:
  * `humen+A+Neu+Sg+Indef`

**stalle** failures:

* `stalle+V+Inf` => `stall`
* `stalle` has no analyses either

**kakse** failures:

* `kakse+V+Inf` => `kaks`
* `kakse` has no analyses either

**gjelle** failures:

* `gjelle+V+Inf` => `gjea`
* `gjelle+V+Inf` => `gjee`
* `gjelle` has no analyses either

**trulte** failures:

* `trulte+V+Inf` => `trult`
* `trulte` has no analyses either

**preferere** failures:

* `preferere+V+Inf` => `preferer`
* `preferere` has no analyses either

**avstyggast** failures:

* `avstyggast+V+Inf` => `avstygast`
* `avstyggast` has no analyses either

**solidarisere** failures:

* `solidarisere+V+Inf` => `solidariser`
* `solidarisere` has no analyses either

**poncere** failures:

* `poncere+V+Inf` => `poncer`
* `poncere` has no analyses either

**bie** failures:

* `bie+V+Inf` does not generate!
* `bie` has following analyses:
  * `bite+V+Inf`
  * `bite+V+PrfPrc`
  * `bide+V+PrfPrc`
  * `bite+V+Adj+Nøyt+Ub+Eint`
  * `bide+V+Inf`
  * `bide+V+Adj+Nøyt+Ub+Eint`

**bælskrike** failures:

* `bælskrike+V+Inf` => `bælskria`
* `bælskrike+V+Inf` => `bælskrie`
* `bælskrike` has no analyses either

**kleppe** failures:

* `kleppe+V+Inf` => `klepp`
* `kleppe` has no analyses either

**regularisere** failures:

* `regularisere+V+Inf` => `regulariser`
* `regularisere` has no analyses either

**skrivast** failures:

* `skrivast+V+Inf` => `skriast`
* `skrivast` has no analyses either

**sigre** failures:

* `sigre+V+Inf` => `sigr`
* `sigre` has no analyses either

**slage** failures:

* `slage+V+Inf` => `slag`
* `slage` has no analyses either

**tv-overvake** failures:

* `tv-overvake+V+Inf` => `tv-overvak`
* `tv-overvake` has no analyses either

**sose** failures:

* `sose+V+Inf` => `sos`
* `sose` has no analyses either

**sluttføre** failures:

* `sluttføre+V+Inf` => `sluttfør`
* `sluttføre` has no analyses either

**sålde** failures:

* `sålde+V+Inf` => `såld`
* `sålde` has no analyses either

**øyremerkje** failures:

* `øyremerkje+V+Inf` does not generate!
* `øyremerkje` has no analyses either

**mare** failures:

* `mare+V+Inf` => `mar`
* `mare` has no analyses either

**hefte** failures:

* `hefte+V+Inf` does not generate!
* `hefte` has no analyses either

**kleie** failures:

* `kleie+V+Inf` does not generate!
* `kleie` has no analyses either

**båsfôre** failures:

* `båsfôre+V+Inf` => `båsfôr`
* `båsfôre` has no analyses either

**flatne** failures:

* `flatne+V+Inf` => `flatn`
* `flatne` has no analyses either

**sande** failures:

* `sande+V+Inf` => `sand`
* `sande` has following analyses:
  * `søkke+V+PrsPrc`
  * `supe+V+PrsPrc`

**påkalle** failures:

* `påkalle+V+Inf` => `påkall`
* `påkalle` has no analyses either

**svekke** failures:

* `svekke+V+Inf` does not generate!
* `svekke` has no analyses either

**prove** failures:

* `prove+V+Inf` => `prov`
* `prove` has no analyses either

**ryte** failures:

* `ryte+V+Inf` => `rye`
* `ryte+V+Inf` => `rya`
* `ryte` has no analyses either

**delta** failures:

* `delta+V+Inf` => `deltta`
* `delta` has following analyses:
  * `delta+N`
  * `delta+N+N+Msc+Sg+Indef`

**spore** failures:

* `spore+V+Inf` => `spor`
* `spore` has no analyses either

**otre** failures:

* `otre+V+Inf` => `otr`
* `otre` has no analyses either

**veive** failures:

* `veive+V+Inf` does not generate!
* `veive` has no analyses either

**vinterføde** failures:

* `vinterføde+V+Inf` does not generate!
* `vinterføde` has no analyses either

**hete** failures:

* `hete+V+Inf` => `het`
* `hete` has no analyses either

**klove** failures:

* `klove+V+Inf` => `klov`
* `klove` has following analyses:
  * `kloven+A+Neu+Sg+Indef`

**sminke** failures:

* `sminke+V+Inf` => `smink`
* `sminke` has no analyses either

**knikse** failures:

* `knikse+V+Inf` => `kniks`
* `knikse` has no analyses either

**underrette** failures:

* `underrette+V+Inf` => `underrett`
* `underrette` has no analyses either

**søkje** failures:

* `søkje+V+Inf` does not generate!
* `søkje` has no analyses either

**hurre** failures:

* `hurre+V+Inf` => `hurr`
* `hurre` has no analyses either

**flage** failures:

* `flage+V+Inf` => `flag`
* `flage` has no analyses either

**forslumme** failures:

* `forslumme+V+Inf` => `forslum`
* `forslumme` has no analyses either

**doggast** failures:

* `doggast+V+Inf` => `doggastast`
* `doggast` has no analyses either

**splintre** failures:

* `splintre+V+Inf` => `splintr`
* `splintre` has no analyses either

**rotne** failures:

* `rotne+V+Inf` => `rotn`
* `rotne` has following analyses:
  * `roten+A+Sg+Def`
  * `roten+A+Pl+Indef`

**sentrere** failures:

* `sentrere+V+Inf` => `sentrer`
* `sentrere` has no analyses either

**skravere** failures:

* `skravere+V+Inf` => `skraver`
* `skravere` has no analyses either

**approbere** failures:

* `approbere+V+Inf` => `approber`
* `approbere` has no analyses either

**rosemåle** failures:

* `rosemåle+V+Inf` => `rosemål`
* `rosemåle` has no analyses either

**høljeregne** failures:

* `høljeregne+V+Inf` does not generate!
* `høljeregne` has no analyses either

**fordampe** failures:

* `fordampe+V+Inf` => `fordamp`
* `fordampe` has no analyses either

**reaktivere** failures:

* `reaktivere+V+Inf` => `reaktiver`
* `reaktivere` has no analyses either

**like** failures:

* `like+V+Inf` does not generate!
* `like` has following analyses:
  * `lik+A+Sg+Def`
  * `like+A+A+Neu+Sg+Indef`
  * `like+A+A+Fem+Sg+Indef`
  * `lik+A+Pl+Indef`
  * `like+A+A+Msc+Sg+Indef`

**antrast** failures:

* `antrast+V+Inf` => `antrastast`
* `antrast` has no analyses either

**røyse** failures:

* `røyse+V+Inf` => `røys`
* `røyse` has no analyses either

**morse** failures:

* `morse+V+Inf` => `mors`
* `morse` has no analyses either

**ettersjå** failures:

* `ettersjå+V+Inf` => `etterssjå`
* `ettersjå` has no analyses either

**forhalde** failures:

* `forhalde+V+Inf` => `forhae`
* `forhalde+V+Inf` => `forhaa`
* `forhalde` has no analyses either

**dagsette** failures:

* `dagsette+V+Inf` does not generate!
* `dagsette` has no analyses either

**kødde** failures:

* `kødde+V+Inf` => `kødd`
* `kødde` has no analyses either

**likne** failures:

* `likne+V+Inf` => `likn`
* `likne` has no analyses either

**avskape** failures:

* `avskape+V+Inf` => `avskap`
* `avskape` has no analyses either

**gjenvinne** failures:

* `gjenvinne+V+Inf` => `gjenvia`
* `gjenvinne+V+Inf` => `gjenvie`
* `gjenvinne` has no analyses either

**hasle** failures:

* `hasle+V+Inf` => `hasl`
* `hasle` has no analyses either

**skuldskrive** failures:

* `skuldskrive+V+Inf` => `skuldskrie`
* `skuldskrive+V+Inf` => `skuldskria`
* `skuldskrive` has no analyses either

**møyrne** failures:

* `møyrne+V+Inf` => `møyrn`
* `møyrne` has no analyses either

**holdast** failures:

* `holdast+V+Inf` => `holdastast`
* `holdast` has no analyses either

**romantisere** failures:

* `romantisere+V+Inf` => `romantiser`
* `romantisere` has no analyses either

**puste** failures:

* `puste+V+Inf` => `pust`
* `puste` has no analyses either

**feilberekne** failures:

* `feilberekne+V+Inf` => `feilberekn`
* `feilberekne` has no analyses either

**strukturere** failures:

* `strukturere+V+Inf` => `strukturer`
* `strukturere` has no analyses either

**nippe** failures:

* `nippe+V+Inf` => `nipp`
* `nippe` has no analyses either

**overdrive** failures:

* `overdrive+V+Inf` => `overdrie`
* `overdrive+V+Inf` => `overdria`
* `overdrive` has following analyses:
  * `overdriven+A+Neu+Sg+Indef`

**gate** failures:

* `gate+V+Inf` => `gat`
* `gate` has no analyses either

**romme** failures:

* `romme+V+Inf` => `rom`
* `romme` has no analyses either

**krete** failures:

* `krete+V+Inf` => `kret`
* `krete` has no analyses either

**reire** failures:

* `reire+V+Inf` => `reir`
* `reire` has no analyses either

**metallisere** failures:

* `metallisere+V+Inf` => `metalliser`
* `metallisere` has no analyses either

**kåtne** failures:

* `kåtne+V+Inf` => `kåtn`
* `kåtne` has no analyses either

**tanne** failures:

* `tanne+V+Inf` => `tann`
* `tanne` has no analyses either

**gugge** failures:

* `gugge+V+Inf` => `gugg`
* `gugge` has no analyses either

**type** failures:

* `type+V+Inf` => `typ`
* `type` has no analyses either

**orde** failures:

* `orde+V+Inf` => `ord`
* `orde` has no analyses either

**brise** failures:

* `brise+V+Inf` => `bris`
* `brise` has following analyses:
  * `brisel+N+Neu+Pl+Indef`
  * `brisel+N+Neu+Sg+Indef`
  * `brisen+A+Neu+Sg+Indef`

**fiple** failures:

* `fiple+V+Inf` => `fipl`
* `fiple` has no analyses either

**hauge** failures:

* `hauge+V+Inf` => `haug`
* `hauge` has no analyses either

**elske** failures:

* `elske+V+Inf` => `elsk`
* `elske` has following analyses:
  * `elsken+A+Neu+Sg+Indef`

**avgrense** failures:

* `avgrense+V+Inf` => `avgrens`
* `avgrense` has no analyses either

**framandgjere** failures:

* `framandgjere+V+Inf` does not generate!
* `framandgjere` has no analyses either

**konvoiere** failures:

* `konvoiere+V+Inf` => `konvoier`
* `konvoiere` has no analyses either

**fråskrive** failures:

* `fråskrive+V+Inf` => `fråskria`
* `fråskrive+V+Inf` => `fråskrie`
* `fråskrive` has no analyses either

**avfeitte** failures:

* `avfeitte+V+Inf` => `avfeitt`
* `avfeitte` has no analyses either

**nykkje** failures:

* `nykkje+V+Inf` does not generate!
* `nykkje` has no analyses either

**rote** failures:

* `rote+V+Inf` => `rot`
* `rote` has following analyses:
  * `roten+A+Neu+Sg+Indef`

**fjaske** failures:

* `fjaske+V+Inf` => `fjask`
* `fjaske` has no analyses either

**krite** failures:

* `krite+V+Inf` => `krit`
* `krite` has no analyses either

**intendere** failures:

* `intendere+V+Inf` => `intender`
* `intendere` has no analyses either

**firdoble** failures:

* `firdoble+V+Inf` => `firdobl`
* `firdoble` has no analyses either

**forsegle** failures:

* `forsegle+V+Inf` => `forsegl`
* `forsegle` has no analyses either

**degradere** failures:

* `degradere+V+Inf` => `degrader`
* `degradere` has no analyses either

**merittere** failures:

* `merittere+V+Inf` => `meritter`
* `merittere` has no analyses either

**høkre** failures:

* `høkre+V+Inf` => `høkr`
* `høkre` has no analyses either

**maske** failures:

* `maske+V+Inf` => `mask`
* `maske` has no analyses either

**grunne** failures:

* `grunne+V+Inf` => `grunn`
* `grunne` has no analyses either

**eige** failures:

* `eige+V+Inf` => `eigeeiga`
* `eige+V+Inf` => `eigeeige`
* `eige` has no analyses either

**lunke** failures:

* `lunke+V+Inf` => `lunk`
* `lunke` has no analyses either

**gane** failures:

* `gane+V+Inf` does not generate!
* `gane` has no analyses either

**deise** failures:

* `deise+V+Inf` does not generate!
* `deise` has following analyses:
  * `deisen+A+Neu+Sg+Indef`

**engste** failures:

* `engste+V+Inf` => `engst`
* `engste` has no analyses either

**fjåse** failures:

* `fjåse+V+Inf` => `fjås`
* `fjåse` has no analyses either

**vanstyre** failures:

* `vanstyre+V+Inf` does not generate!
* `vanstyre` has no analyses either

**dalle** failures:

* `dalle+V+Inf` => `dall`
* `dalle` has no analyses either

**kommersialisere** failures:

* `kommersialisere+V+Inf` => `kommersialiser`
* `kommersialisere` has no analyses either

**ølnast** failures:

* `ølnast+V+Inf` => `ølnastast`
* `ølnast` has following analyses:
  * `ølen+A+Superl+Indef`

**sive** failures:

* `sive+V+Inf` does not generate!
* `sive` has no analyses either

**lesse** failures:

* `lesse+V+Inf` does not generate!
* `lesse` has no analyses either

**halvere** failures:

* `halvere+V+Inf` => `halver`
* `halvere` has no analyses either

**vakne** failures:

* `vakne+V+Inf` => `vakn`
* `vakne` has following analyses:
  * `vaken+A+Sg+Def`
  * `vaken+A+Pl+Indef`

**inokulere** failures:

* `inokulere+V+Inf` => `inokuler`
* `inokulere` has no analyses either

**avhorne** failures:

* `avhorne+V+Inf` => `avhorn`
* `avhorne` has no analyses either

**besifre** failures:

* `besifre+V+Inf` => `besifr`
* `besifre` has no analyses either

**oppløyse** failures:

* `oppløyse+V+Inf` => `oppløys`
* `oppløyse` has no analyses either

**klosshale** failures:

* `klosshale+V+Inf` does not generate!
* `klosshale` has no analyses either

**svabbe** failures:

* `svabbe+V+Inf` => `svabb`
* `svabbe` has no analyses either

**denge** failures:

* `denge+V+Inf` does not generate!
* `denge` has no analyses either

**lægje** failures:

* `lægje+V+Inf` does not generate!
* `lægje` has no analyses either

**skjøre** failures:

* `skjøre+V+Inf` => `skjør`
* `skjøre` has following analyses:
  * `skjør+A+Sg+Def`
  * `skjør+A+Pl+Indef`

**hele** failures:

* `hele+V+Inf` => `hel`
* `hele` has no analyses either

**opponere** failures:

* `opponere+V+Inf` => `opponer`
* `opponere` has no analyses either

**hyve** failures:

* `hyve+V+Inf` does not generate!
* `hyve` has following analyses:
  * `hyvekse+V+Imprt`
  * `hyvekse+V+Ind+Prs`

**krenkje** failures:

* `krenkje+V+Inf` does not generate!
* `krenkje` has no analyses either

**balanseføre** failures:

* `balanseføre+V+Inf` => `balansefør`
* `balanseføre` has no analyses either

**ekstensivere** failures:

* `ekstensivere+V+Inf` => `ekstensiver`
* `ekstensivere` has no analyses either

**solvende** failures:

* `solvende+V+Inf` does not generate!
* `solvende` has no analyses either

**sneke** failures:

* `sneke+V+Inf` => `snek`
* `sneke` has no analyses either

**stutne** failures:

* `stutne+V+Inf` => `stutn`
* `stutne` has no analyses either

**klubbe** failures:

* `klubbe+V+Inf` => `klubb`
* `klubbe` has no analyses either

**plundre** failures:

* `plundre+V+Inf` => `plundr`
* `plundre` has no analyses either

**føde** failures:

* `føde+V+Inf` does not generate!
* `føde` has following analyses:
  * `fødd+A+A+Pl+Indef`
  * `fødd+A+A+Sg+Def`
  * `fødd+A+A+Pl+Def`

**lette** failures:

* `lette+V+Inf` => `lett`
* `lette` has following analyses:
  * `lett+A+A+Pl+Def`
  * `lett+A+A+Pl+Indef`
  * `lett+A+A+Sg+Def`

**purre** failures:

* `purre+V+Inf` => `purr`
* `purre` has no analyses either

**stable** failures:

* `stable+V+Inf` => `stabl`
* `stable` has no analyses either

**kraume** failures:

* `kraume+V+Inf` => `kraum`
* `kraume` has no analyses either

**lykkast** failures:

* `lykkast+V+Inf` => `lykkastast`
* `lykkast` has no analyses either

**saksøkje** failures:

* `saksøkje+V+Inf` does not generate!
* `saksøkje` has no analyses either

**omgåast** failures:

* `omgåast+V+Inf` => `omggåast`
* `omgåast` has following analyses:
  * `omgå+V+Inf+Pass`

**gufse** failures:

* `gufse+V+Inf` => `gufs`
* `gufse` has no analyses either

**grunnsette** failures:

* `grunnsette+V+Inf` does not generate!
* `grunnsette` has no analyses either

**klaffe** failures:

* `klaffe+V+Inf` => `klaff`
* `klaffe` has no analyses either

**nufse** failures:

* `nufse+V+Inf` => `nufs`
* `nufse` has no analyses either

**erkjenne** failures:

* `erkjenne+V+Inf` does not generate!
* `erkjenne` has no analyses either

**omskolere** failures:

* `omskolere+V+Inf` => `omskoler`
* `omskolere` has no analyses either

**styggast** failures:

* `styggast+V+Inf` => `stygast`
* `styggast` has no analyses either

**misminnast** failures:

* `misminnast+V+Inf` => `misminast`
* `misminnast` has no analyses either

**synkverve** failures:

* `synkverve+V+Inf` does not generate!
* `synkverve` has no analyses either

**skyre** failures:

* `skyre+V+Inf` does not generate!
* `skyre` has following analyses:
  * `skyr+A+Sg+Def`
  * `skyr+A+Pl+Indef`

**gleine** failures:

* `gleine+V+Inf` does not generate!
* `gleine` has no analyses either

**gigle** failures:

* `gigle+V+Inf` => `gigl`
* `gigle` has following analyses:
  * `giglen+A+Neu+Sg+Indef`

**avskjere** failures:

* `avskjere+V+Inf` => `avskjee`
* `avskjere+V+Inf` => `avskjea`
* `avskjere` has no analyses either

**patinere** failures:

* `patinere+V+Inf` => `patiner`
* `patinere` has no analyses either

**lagge** failures:

* `lagge+V+Inf` => `lagg`
* `lagge` has no analyses either

**klyve** failures:

* `klyve+V+Inf` => `klye`
* `klyve+V+Inf` => `klya`
* `klyve` has no analyses either

**molde** failures:

* `molde+V+Inf` => `mold`
* `molde` has no analyses either

**nistire** failures:

* `nistire+V+Inf` does not generate!
* `nistire` has no analyses either

**spare** failures:

* `spare+V+Inf` does not generate!
* `spare` has following analyses:
  * `spar+A+Pl+Indef`
  * `spar+A+Sg+Def`

**tabbe** failures:

* `tabbe+V+Inf` => `tabb`
* `tabbe` has no analyses either

**sjokkbehandle** failures:

* `sjokkbehandle+V+Inf` => `sjokkbehandl`
* `sjokkbehandle` has no analyses either

**skygne** failures:

* `skygne+V+Inf` does not generate!
* `skygne` has no analyses either

**krosskaste** failures:

* `krosskaste+V+Inf` => `krosskast`
* `krosskaste` has no analyses either

**flote** failures:

* `flote+V+Inf` => `flot`
* `flote` has following analyses:
  * `floten+A+Neu+Sg+Indef`
  * `flot+A+Pl+Indef`
  * `flot+A+Sg+Def`

**rundstrikke** failures:

* `rundstrikke+V+Inf` => `rundstrikk`
* `rundstrikke` has no analyses either

**dovne** failures:

* `dovne+V+Inf` => `dovn`
* `dovne` has following analyses:
  * `doven+A+Pl+Indef`
  * `doven+A+Sg+Def`

**overhøyre** failures:

* `overhøyre+V+Inf` does not generate!
* `overhøyre` has no analyses either

**påkjære** failures:

* `påkjære+V+Inf` => `påkjær`
* `påkjære` has no analyses either

**genierklære** failures:

* `genierklære+V+Inf` => `genierklær`
* `genierklære` has no analyses either

**replisere** failures:

* `replisere+V+Inf` => `repliser`
* `replisere` has no analyses either

**skråsetje** failures:

* `skråsetje+V+Inf` does not generate!
* `skråsetje` has no analyses either

**lukkønske** failures:

* `lukkønske+V+Inf` => `lukkønsk`
* `lukkønske` has no analyses either

**mørkne** failures:

* `mørkne+V+Inf` => `mørkn`
* `mørkne` has no analyses either

**sjugte** failures:

* `sjugte+V+Inf` => `sjugt`
* `sjugte` has no analyses either

**innsetje** failures:

* `innsetje+V+Inf` does not generate!
* `innsetje` has no analyses either

**trene** failures:

* `trene+V+Inf` => `tren`
* `trene` has following analyses:
  * `treen+A+Sg+Def`
  * `tro+N+N+Fem+Pl+Def`
  * `treen+A+Pl+Indef`

**pare** failures:

* `pare+V+Inf` => `par`
* `pare` has no analyses either

**pjuske** failures:

* `pjuske+V+Inf` => `pjusk`
* `pjuske` has no analyses either

**innlosjere** failures:

* `innlosjere+V+Inf` => `innlosjer`
* `innlosjere` has no analyses either

**lære** failures:

* `lære+V+Inf` does not generate!
* `lære` has no analyses either

**vægje** failures:

* `vægje+V+Inf` does not generate!
* `vægje` has no analyses either

**årette** failures:

* `årette+V+Inf` => `årett`
* `årette` has no analyses either

**kraune** failures:

* `kraune+V+Inf` => `kraun`
* `kraune` has no analyses either

**strøne** failures:

* `strøne+V+Inf` => `strøn`
* `strøne` has no analyses either

**sprengkøyre** failures:

* `sprengkøyre+V+Inf` does not generate!
* `sprengkøyre` has no analyses either

**oppe** failures:

* `oppe+V+Inf` => `opp`
* `oppe` has no analyses either

**vimre** failures:

* `vimre+V+Inf` => `vimr`
* `vimre` has no analyses either

**heimsøkje** failures:

* `heimsøkje+V+Inf` does not generate!
* `heimsøkje` has no analyses either

**semskgarve** failures:

* `semskgarve+V+Inf` => `semskgarv`
* `semskgarve` has no analyses either

**kalandrere** failures:

* `kalandrere+V+Inf` => `kalandrer`
* `kalandrere` has no analyses either

**bykse** failures:

* `bykse+V+Inf` => `byks`
* `bykse` has no analyses either

**spøke** failures:

* `spøke+V+Inf` => `spøk`
* `spøke` has following analyses:
  * `spøken+A+Neu+Sg+Indef`

**gjennomhòle** failures:

* `gjennomhòle+V+Inf` => `gjennomhòl`
* `gjennomhòle` has no analyses either

**klunke** failures:

* `klunke+V+Inf` => `klunk`
* `klunke` has no analyses either

**avklede** failures:

* `avklede+V+Inf` does not generate!
* `avklede` has following analyses:
  * `avkledd+A+A+Pl+Def`
  * `avkledd+A+A+Sg+Def`
  * `avkledd+A+A+Pl+Indef`

**harpikse** failures:

* `harpikse+V+Inf` => `harpiks`
* `harpikse` has no analyses either

**etablere** failures:

* `etablere+V+Inf` => `etabler`
* `etablere` has no analyses either

**grave** failures:

* `grave+V+Inf` => `graa`
* `grave+V+Inf` => `grae`
* `grave` has following analyses:
  * `graven+A+Neu+Sg+Indef`

**sjokkere** failures:

* `sjokkere+V+Inf` => `sjokker`
* `sjokkere` has no analyses either

**turrsvømme** failures:

* `turrsvømme+V+Inf` => `turrsvøme`
* `turrsvømme+V+Inf` => `turrsvøma`
* `turrsvømme` has no analyses either

**semske** failures:

* `semske+V+Inf` => `semsk`
* `semske` has no analyses either

**hengle** failures:

* `hengle+V+Inf` => `hengl`
* `hengle` has following analyses:
  * `henglen+A+Neu+Sg+Indef`

**gymnastisere** failures:

* `gymnastisere+V+Inf` => `gymnastiser`
* `gymnastisere` has no analyses either

**tysse** failures:

* `tysse+V+Inf` => `tyss`
* `tysse` has no analyses either

**spauke** failures:

* `spauke+V+Inf` => `spauk`
* `spauke` has no analyses either

**skambyde** failures:

* `skambyde+V+Inf` does not generate!
* `skambyde` has no analyses either

**djupfryse** failures:

* `djupfryse+V+Inf` => `djupfrys`
* `djupfryse` has no analyses either

**innstille** failures:

* `innstille+V+Inf` does not generate!
* `innstille` has no analyses either

**nonsjalere** failures:

* `nonsjalere+V+Inf` => `nonsjaler`
* `nonsjalere` has no analyses either

**influere** failures:

* `influere+V+Inf` => `influer`
* `influere` has no analyses either

**gutere** failures:

* `gutere+V+Inf` => `guter`
* `gutere` has no analyses either

**skogre** failures:

* `skogre+V+Inf` => `skogr`
* `skogre` has no analyses either

**gakke** failures:

* `gakke+V+Inf` => `gakk`
* `gakke` has no analyses either

**flense** failures:

* `flense+V+Inf` => `flens`
* `flense` has no analyses either

**glumse** failures:

* `glumse+V+Inf` => `glums`
* `glumse` has no analyses either

**ræpe** failures:

* `ræpe+V+Inf` => `ræp`
* `ræpe` has no analyses either

**skrye** failures:

* `skrye+V+Inf` does not generate!
* `skrye` has no analyses either

**rikte** failures:

* `rikte+V+Inf` => `rikt`
* `rikte` has following analyses:
  * `rike+V+PrfPrc`

**pasifisere** failures:

* `pasifisere+V+Inf` => `pasifiser`
* `pasifisere` has no analyses either

**munne** failures:

* `munne+V+Inf` => `munn`
* `munne` has no analyses either

**sameine** failures:

* `sameine+V+Inf` does not generate!
* `sameine` has no analyses either

**dufte** failures:

* `dufte+V+Inf` => `duft`
* `dufte` has no analyses either

**forlese** failures:

* `forlese+V+Inf` => `forlea`
* `forlese+V+Inf` => `forlee`
* `forlese` has no analyses either

**skaldre** failures:

* `skaldre+V+Inf` => `skaldr`
* `skaldre` has no analyses either

**mercerisere** failures:

* `mercerisere+V+Inf` => `merceriser`
* `mercerisere` has no analyses either

**stylte** failures:

* `stylte+V+Inf` => `stylt`
* `stylte` has no analyses either

**glymje** failures:

* `glymje+V+Inf` does not generate!
* `glymje` has no analyses either

**gonge** failures:

* `gonge+V+Inf` => `gong`
* `gonge` has no analyses either

**klemre** failures:

* `klemre+V+Inf` => `klemr`
* `klemre` has no analyses either

**mista** failures:

* `mista+V+Inf` => `mistta`
* `mista` has no analyses either

**floragrafere** failures:

* `floragrafere+V+Inf` => `floragrafer`
* `floragrafere` has no analyses either

**dulle** failures:

* `dulle+V+Inf` => `dull`
* `dulle` has no analyses either

**frådøme** failures:

* `frådøme+V+Inf` does not generate!
* `frådøme` has no analyses either

**knocke** failures:

* `knocke+V+Inf` => `knock`
* `knocke` has no analyses either

**ljugast** failures:

* `ljugast+V+Inf` => `ljuast`
* `ljugast` has no analyses either

**rape** failures:

* `rape+V+Inf` does not generate!
* `rape` has following analyses:
  * `rapp+A+A+Sg+Def`
  * `rapp+A+A+Pl+Indef`
  * `rapp+A+A+Pl+Def`

**glitre** failures:

* `glitre+V+Inf` => `glitr`
* `glitre` has no analyses either

**nåde** failures:

* `nåde+V+Inf` => `nåd`
* `nåde` has no analyses either

**blåse** failures:

* `blåse+V+Inf` => `blåa`
* `blåse+V+Inf` => `blåe`
* `blåse` has no analyses either

**utkomme** failures:

* `utkomme+V+Inf` => `utkkomme`
* `utkomme+V+Inf` => `utkkomma`
* `utkomme` has no analyses either

**ytre** failures:

* `ytre+V+Inf` => `ytr`
* `ytre` has following analyses:
  * `ytre+A+Comp`

**såne** failures:

* `såne+V+Inf` => `sån`
* `såne` has no analyses either

**automatisere** failures:

* `automatisere+V+Inf` => `automatiser`
* `automatisere` has no analyses either

**dempe** failures:

* `dempe+V+Inf` => `demp`
* `dempe` has no analyses either

**boste** failures:

* `boste+V+Inf` => `bost`
* `boste` has no analyses either

**akke** failures:

* `akke+V+Inf` => `akk`
* `akke` has no analyses either

**formalisere** failures:

* `formalisere+V+Inf` => `formaliser`
* `formalisere` has no analyses either

**lange** failures:

* `lange+V+Inf` => `lang`
* `lange` has no analyses either

**grevsette** failures:

* `grevsette+V+Inf` does not generate!
* `grevsette` has no analyses either

**punge** failures:

* `punge+V+Inf` => `pung`
* `punge` has no analyses either

**sneie** failures:

* `sneie+V+Inf` => `snei`
* `sneie` has no analyses either

**masseundersøkje** failures:

* `masseundersøkje+V+Inf` does not generate!
* `masseundersøkje` has no analyses either

**gaple** failures:

* `gaple+V+Inf` => `gaplle`
* `gaple` has no analyses either

**skorpne** failures:

* `skorpne+V+Inf` => `skorpn`
* `skorpne` has following analyses:
  * `skorpen+A+Sg+Def`
  * `skorpen+A+Pl+Indef`

**fråvike** failures:

* `fråvike+V+Inf` => `fråvia`
* `fråvike+V+Inf` => `fråvie`
* `fråvike` has no analyses either

**råne** failures:

* `råne+V+Inf` => `rån`
* `råne` has no analyses either

**våronne** failures:

* `våronne+V+Inf` => `våronn`
* `våronne` has no analyses either

**sisle** failures:

* `sisle+V+Inf` => `sisl`
* `sisle` has no analyses either

**understellsbehandle** failures:

* `understellsbehandle+V+Inf` => `understellsbehandl`
* `understellsbehandle` has no analyses either

**alliere** failures:

* `alliere+V+Inf` => `allier`
* `alliere` has no analyses either

**turrkoke** failures:

* `turrkoke+V+Inf` does not generate!
* `turrkoke` has no analyses either

**bite** failures:

* `bite+V+Inf` => `bie`
* `bite+V+Inf` => `bia`
* `bite` has following analyses:
  * `bitter+A+A+Pl+Indef`
  * `bitter+A+A+Sg+Def`
  * `bitter+A+A+Pl+Def`

**importere** failures:

* `importere+V+Inf` => `importer`
* `importere` has no analyses either

**gipe** failures:

* `gipe+V+Inf` => `gip`
* `gipe` has no analyses either

**terrorisere** failures:

* `terrorisere+V+Inf` => `terroriser`
* `terrorisere` has no analyses either

**tefte** failures:

* `tefte+V+Inf` => `teft`
* `tefte` has no analyses either

**røte** failures:

* `røte+V+Inf` => `røt`
* `røte` has no analyses either

**bandlegge** failures:

* `bandlegge+V+Inf` does not generate!
* `bandlegge` has no analyses either

**belme** failures:

* `belme+V+Inf` => `belm`
* `belme` has no analyses either

**glamorisere** failures:

* `glamorisere+V+Inf` => `glamoriser`
* `glamorisere` has no analyses either

**hegrele** failures:

* `hegrele+V+Inf` => `hegrelle`
* `hegrele` has no analyses either

**kyve** failures:

* `kyve+V+Inf` does not generate!
* `kyve` has no analyses either

**hugse** failures:

* `hugse+V+Inf` => `hugs`
* `hugse` has following analyses:
  * `hugsen+A+Neu+Sg+Indef`

**føretrekkje** failures:

* `føretrekkje+V+Inf` does not generate!
* `føretrekkje` has no analyses either

**passere** failures:

* `passere+V+Inf` => `passer`
* `passere` has no analyses either

**snapse** failures:

* `snapse+V+Inf` => `snaps`
* `snapse` has no analyses either

**styve** failures:

* `styve+V+Inf` does not generate!
* `styve` has following analyses:
  * `styven+A+Neu+Sg+Indef`

**klumse** failures:

* `klumse+V+Inf` => `klums`
* `klumse` has following analyses:
  * `klums+A+A+Pl+Def`
  * `klums+A+A+Pl+Indef`
  * `klums+A+A+Sg+Def`

**skjørne** failures:

* `skjørne+V+Inf` => `skjørn`
* `skjørne` has no analyses either

**blòte** failures:

* `blòte+V+Inf` => `blòt`
* `blòte` has no analyses either

**tilmåte** failures:

* `tilmåte+V+Inf` => `tilmåt`
* `tilmåte` has no analyses either

**øsle** failures:

* `øsle+V+Inf` => `øsl`
* `øsle` has no analyses either

**utprøve** failures:

* `utprøve+V+Inf` does not generate!
* `utprøve` has no analyses either

**summe** failures:

* `summe+V+Inf` => `sum`
* `summe` has no analyses either

**muge** failures:

* `muge+V+Inf` => `mug`
* `muge` has following analyses:
  * `muggen+A+Neu+Sg+Indef`

**obdusere** failures:

* `obdusere+V+Inf` => `obduser`
* `obdusere` has no analyses either

**underbygge** failures:

* `underbygge+V+Inf` does not generate!
* `underbygge` has no analyses either

**furasjere** failures:

* `furasjere+V+Inf` => `furasjer`
* `furasjere` has no analyses either

**gabbe** failures:

* `gabbe+V+Inf` => `gabb`
* `gabbe` has no analyses either

**serratere** failures:

* `serratere+V+Inf` => `serrater`
* `serratere` has no analyses either

**tasle** failures:

* `tasle+V+Inf` => `tasl`
* `tasle` has following analyses:
  * `taslen+A+Neu+Sg+Indef`

**mortifisere** failures:

* `mortifisere+V+Inf` => `mortifiser`
* `mortifisere` has no analyses either

**barrikadere** failures:

* `barrikadere+V+Inf` => `barrikader`
* `barrikadere` has no analyses either

**kole** failures:

* `kole+V+Inf` => `kol`
* `kole` has no analyses either

**rølpe** failures:

* `rølpe+V+Inf` => `rølp`
* `rølpe` has no analyses either

**heilage** failures:

* `heilage+V+Inf` => `heilag`
* `heilage` has following analyses:
  * `heilag+A+Pl+Indef`
  * `heilag+A+Sg+Def`

**teigpløye** failures:

* `teigpløye+V+Inf` does not generate!
* `teigpløye` has no analyses either

**tverr-rykkje** failures:

* `tverr-rykkje+V+Inf` does not generate!
* `tverr-rykkje` has no analyses either

**durre** failures:

* `durre+V+Inf` => `durr`
* `durre` has no analyses either

**gratinere** failures:

* `gratinere+V+Inf` => `gratiner`
* `gratinere` has no analyses either

**mumisere** failures:

* `mumisere+V+Inf` => `mumiser`
* `mumisere` has no analyses either

**vinse** failures:

* `vinse+V+Inf` => `vins`
* `vinse` has no analyses either

**mysje** failures:

* `mysje+V+Inf` => `mysj`
* `mysje` has no analyses either

**krulle** failures:

* `krulle+V+Inf` => `krull`
* `krulle` has no analyses either

**bogne** failures:

* `bogne+V+Inf` => `bogn`
* `bogne` has following analyses:
  * `bogen+A+Sg+Def`
  * `bogen+A+Pl+Indef`

**mediere** failures:

* `mediere+V+Inf` => `medier`
* `mediere` has no analyses either

**folke** failures:

* `folke+V+Inf` => `folk`
* `folke` has no analyses either

**utrivast** failures:

* `utrivast+V+Inf` => `utriast`
* `utrivast` has no analyses either

**togne** failures:

* `togne+V+Inf` => `togn`
* `togne` has no analyses either

**einglane** failures:

* `einglane+V+Inf` does not generate!
* `einglane` has no analyses either

**live** failures:

* `live+V+Inf` => `liv`
* `live` has following analyses:
  * `live+N`
  * `live+A+A+Msc+Sg+Indef`
  * `live+A+A+Neu+Sg+Indef`
  * `live+N+N+Sg`
  * `live+A+A+Fem+Sg+Indef`

**relokere** failures:

* `relokere+V+Inf` => `reloker`
* `relokere` has no analyses either

**friskne** failures:

* `friskne+V+Inf` => `friskn`
* `friskne` has no analyses either

**range** failures:

* `range+V+Inf` => `rang`
* `range` has following analyses:
  * `rang+A+Sg+Def`
  * `rang+A+Pl+Indef`

**nupre** failures:

* `nupre+V+Inf` => `nupr`
* `nupre` has no analyses either

**svampe** failures:

* `svampe+V+Inf` => `svamp`
* `svampe` has no analyses either

**umyndiggjere** failures:

* `umyndiggjere+V+Inf` does not generate!
* `umyndiggjere` has no analyses either

**sjoge** failures:

* `sjoge+V+Inf` => `sjog`
* `sjoge` has no analyses either

**kravle** failures:

* `kravle+V+Inf` => `kravl`
* `kravle` has no analyses either

**misfarge** failures:

* `misfarge+V+Inf` => `misfarg`
* `misfarge` has no analyses either

**forkynne** failures:

* `forkynne+V+Inf` does not generate!
* `forkynne` has no analyses either

**kjenneteikne** failures:

* `kjenneteikne+V+Inf` => `kjenneteikn`
* `kjenneteikne` has no analyses either

**synfare** failures:

* `synfare+V+Inf` => `synfaa`
* `synfare+V+Inf` => `synfae`
* `synfare` has no analyses either

**baktale** failures:

* `baktale+V+Inf` does not generate!
* `baktale` has no analyses either

**ernære** failures:

* `ernære+V+Inf` does not generate!
* `ernære` has no analyses either

**innleie** failures:

* `innleie+V+Inf` does not generate!
* `innleie` has no analyses either

**tore** failures:

* `tore+V+Inf` => `tor`
* `tore` has no analyses either

**gnirke** failures:

* `gnirke+V+Inf` => `gnirk`
* `gnirke` has no analyses either

**skræke** failures:

* `skræke+V+Inf` => `skræk`
* `skræke` has no analyses either

**svarve** failures:

* `svarve+V+Inf` => `svarv`
* `svarve` has no analyses either

**terpe** failures:

* `terpe+V+Inf` => `terp`
* `terpe` has no analyses either

**oppklåre** failures:

* `oppklåre+V+Inf` => `oppklår`
* `oppklåre` has no analyses either

**rete** failures:

* `rete+V+Inf` => `ret`
* `rete` has no analyses either

**sjeide** failures:

* `sjeide+V+Inf` => `sjeid`
* `sjeide` has no analyses either

**bokne** failures:

* `bokne+V+Inf` => `bokn`
* `bokne` has following analyses:
  * `boken+A+Pl+Indef`
  * `boken+A+Sg+Def`

**kollatere** failures:

* `kollatere+V+Inf` => `kollater`
* `kollatere` has no analyses either

**skilje** failures:

* `skilje+V+Inf` => `skil`
* `skilje` has no analyses either

**dekorere** failures:

* `dekorere+V+Inf` => `dekorer`
* `dekorere` has no analyses either

**forselje** failures:

* `forselje+V+Inf` => `forsel`
* `forselje` has no analyses either

**halte** failures:

* `halte+V+Inf` => `halt`
* `halte` has following analyses:
  * `halt+A+A+Pl+Indef`
  * `halt+A+A+Pl+Def`
  * `halt+A+A+Sg+Def`

**trivle** failures:

* `trivle+V+Inf` => `trivl`
* `trivle` has no analyses either

**forbyte** failures:

* `forbyte+V+Inf` => `forbyt`
* `forbyte` has no analyses either

**fugle** failures:

* `fugle+V+Inf` => `fugl`
* `fugle` has no analyses either

**forpurre** failures:

* `forpurre+V+Inf` => `forpurr`
* `forpurre` has no analyses either

**deltake** failures:

* `deltake+V+Inf` => `delttaka`
* `deltake+V+Inf` => `delttake`
* `deltake` has no analyses either

**brødføde** failures:

* `brødføde+V+Inf` does not generate!
* `brødføde` has no analyses either

**duskregne** failures:

* `duskregne+V+Inf` does not generate!
* `duskregne` has no analyses either

**transitivere** failures:

* `transitivere+V+Inf` => `transitiver`
* `transitivere` has no analyses either

**fyke** failures:

* `fyke+V+Inf` => `fya`
* `fyke+V+Inf` => `fye`
* `fyke` has no analyses either

**komplisere** failures:

* `komplisere+V+Inf` => `kompliser`
* `komplisere` has no analyses either

**pukklegge** failures:

* `pukklegge+V+Inf` does not generate!
* `pukklegge` has no analyses either

**elektrifisere** failures:

* `elektrifisere+V+Inf` => `elektrifiser`
* `elektrifisere` has no analyses either

**illremje** failures:

* `illremje+V+Inf` => `illremj`
* `illremje` has no analyses either

**slire** failures:

* `slire+V+Inf` => `slir`
* `slire` has no analyses either

**naglefare** failures:

* `naglefare+V+Inf` => `naglefae`
* `naglefare+V+Inf` => `naglefaa`
* `naglefare` has no analyses either

**initiere** failures:

* `initiere+V+Inf` => `initier`
* `initiere` has no analyses either

**naude** failures:

* `naude+V+Inf` => `naud`
* `naude` has no analyses either

**lasjere** failures:

* `lasjere+V+Inf` => `lasjer`
* `lasjere` has no analyses either

**demilitarisere** failures:

* `demilitarisere+V+Inf` => `demilitariser`
* `demilitarisere` has no analyses either

**løgste** failures:

* `løgste+V+Inf` => `løgst`
* `løgste` has no analyses either

**røykje** failures:

* `røykje+V+Inf` does not generate!
* `røykje` has no analyses either

**sjattere** failures:

* `sjattere+V+Inf` => `sjatter`
* `sjattere` has no analyses either

**titte** failures:

* `titte+V+Inf` => `titt`
* `titte` has no analyses either

**ferdighandsame** failures:

* `ferdighandsame+V+Inf` => `ferdighandsam`
* `ferdighandsame` has no analyses either

**fotokopiere** failures:

* `fotokopiere+V+Inf` => `fotokopier`
* `fotokopiere` has no analyses either

**ugreie** failures:

* `ugreie+V+Inf` does not generate!
* `ugreie` has following analyses:
  * `ugrei+A+Sg+Def`
  * `ugrei+A+Pl+Indef`

**salte** failures:

* `salte+V+Inf` => `salt`
* `salte` has following analyses:
  * `salt+A+A+Pl+Def`
  * `salt+A+A+Sg+Def`
  * `salt+A+A+Pl+Indef`

**truge** failures:

* `truge+V+Inf` => `trug`
* `truge` has no analyses either

**friskmelde** failures:

* `friskmelde+V+Inf` does not generate!
* `friskmelde` has no analyses either

**garte** failures:

* `garte+V+Inf` => `gart`
* `garte` has no analyses either

**krydre** failures:

* `krydre+V+Inf` => `krydr`
* `krydre` has no analyses either

**headhunte** failures:

* `headhunte+V+Inf` => `headhunt`
* `headhunte` has no analyses either

**blankskure** failures:

* `blankskure+V+Inf` => `blankskur`
* `blankskure` has no analyses either

**blanke** failures:

* `blanke+V+Inf` => `blank`
* `blanke` has following analyses:
  * `blank+A+Pl+Indef`
  * `blank+A+Sg+Def`

**dutte** failures:

* `dutte+V+Inf` => `dutt`
* `dutte` has no analyses either

**godsnakke** failures:

* `godsnakke+V+Inf` => `godsnakk`
* `godsnakke` has no analyses either

**tørrtrene** failures:

* `tørrtrene+V+Inf` => `tørrtren`
* `tørrtrene` has no analyses either

**kaste** failures:

* `kaste+V+Inf` => `kast`
* `kaste` has no analyses either

**bumse** failures:

* `bumse+V+Inf` => `bums`
* `bumse` has no analyses either

**bable** failures:

* `bable+V+Inf` => `babl`
* `bable` has no analyses either

**avklåre** failures:

* `avklåre+V+Inf` => `avklår`
* `avklåre` has no analyses either

**halvsole** failures:

* `halvsole+V+Inf` => `halvsol`
* `halvsole` has no analyses either

**beslaglegge** failures:

* `beslaglegge+V+Inf` does not generate!
* `beslaglegge` has no analyses either

**sjukmelde** failures:

* `sjukmelde+V+Inf` does not generate!
* `sjukmelde` has following analyses:
  * `sjukmeld+A+A+Sg+Def`
  * `sjukmeld+A+A+Pl+Indef`
  * `sjukmeld+A+A+Pl+Def`

**prosjektere** failures:

* `prosjektere+V+Inf` => `prosjekter`
* `prosjektere` has no analyses either

**dubbe** failures:

* `dubbe+V+Inf` => `dubb`
* `dubbe` has no analyses either

**røyke** failures:

* `røyke+V+Inf` => `røyk`
* `røyke` has no analyses either

**tikse** failures:

* `tikse+V+Inf` => `tiks`
* `tikse` has no analyses either

**vime** failures:

* `vime+V+Inf` => `vim`
* `vime` has no analyses either

**møykje** failures:

* `møykje+V+Inf` does not generate!
* `møykje` has no analyses either

**ærekrenke** failures:

* `ærekrenke+V+Inf` => `ærekrenk`
* `ærekrenke` has no analyses either

**svade** failures:

* `svade+V+Inf` does not generate!
* `svade` has no analyses either

**forlyste** failures:

* `forlyste+V+Inf` => `forlyst`
* `forlyste` has no analyses either

**kneise** failures:

* `kneise+V+Inf` => `kneis`
* `kneise` has no analyses either

**gase** failures:

* `gase+V+Inf` => `gas`
* `gase` has no analyses either

**nasalere** failures:

* `nasalere+V+Inf` => `nasaler`
* `nasalere` has no analyses either

**fleke** failures:

* `fleke+V+Inf` => `flek`
* `fleke` has no analyses either

**nydyrke** failures:

* `nydyrke+V+Inf` => `nydyrk`
* `nydyrke` has no analyses either

**kapseise** failures:

* `kapseise+V+Inf` => `kapseis`
* `kapseise` has no analyses either

**respondere** failures:

* `respondere+V+Inf` => `responder`
* `respondere` has no analyses either

**bide** failures:

* `bide+V+Inf` => `bia`
* `bide+V+Inf` => `bie`
* `bide` has following analyses:
  * `bide+N+N+Neu+Sg+Indef`
  * `bide+N+N+Neu+Pl+Indef`

**etterbehandle** failures:

* `etterbehandle+V+Inf` => `etterbehandl`
* `etterbehandle` has no analyses either

**game** failures:

* `game+V+Inf` => `gam`
* `game` has following analyses:
  * `game+A+A+Neu+Sg+Indef`
  * `game+A+A+Msc+Sg+Indef`
  * `game+A+A+Fem+Sg+Indef`

**ekte** failures:

* `ekte+V+Inf` => `ekt`
* `ekte` has following analyses:
  * `ekte+N+N+Sg`
  * `ekte+A+Msc+Sg+Indef`
  * `ekte+A+Pl+Indef`
  * `ekte+A+Sg+Def`
  * `ekte+A+Neu+Sg+Indef`
  * `ekte+A+Fem+Sg+Indef`

**namne** failures:

* `namne+V+Inf` => `namn`
* `namne` has no analyses either

**banderolere** failures:

* `banderolere+V+Inf` => `banderoler`
* `banderolere` has no analyses either

**blotte** failures:

* `blotte+V+Inf` => `blott`
* `blotte` has no analyses either

**dossere** failures:

* `dossere+V+Inf` => `dosser`
* `dossere` has no analyses either

**dyppe** failures:

* `dyppe+V+Inf` => `dypp`
* `dyppe` has no analyses either

**huske** failures:

* `huske+V+Inf` => `husk`
* `huske` has no analyses either

**lengte** failures:

* `lengte+V+Inf` => `lengt`
* `lengte` has following analyses:
  * `lenge+V+PrfPrc`

**avhumanisere** failures:

* `avhumanisere+V+Inf` => `avhumaniser`
* `avhumanisere` has no analyses either

**kjase** failures:

* `kjase+V+Inf` => `kjas`
* `kjase` has no analyses either

**fløe** failures:

* `fløe+V+Inf` => `flø`
* `fløe` has following analyses:
  * `flø+A+Pl+Indef`
  * `flø+A+Sg+Def`

**levre** failures:

* `levre+V+Inf` => `levr`
* `levre` has no analyses either

**nave** failures:

* `nave+V+Inf` => `nav`
* `nave` has no analyses either

**vade** failures:

* `vade+V+Inf` does not generate!
* `vade` has no analyses either

**slarpe** failures:

* `slarpe+V+Inf` => `slarp`
* `slarpe` has no analyses either

**høglove** failures:

* `høglove+V+Inf` => `høglov`
* `høglove` has no analyses either

**eie** failures:

* `eie+V+Inf` => `ei`
* `eie` has no analyses either

**blinke** failures:

* `blinke+V+Inf` => `blink`
* `blinke` has no analyses either

**kvalifisere** failures:

* `kvalifisere+V+Inf` => `kvalifiser`
* `kvalifisere` has no analyses either

**rivast** failures:

* `rivast+V+Inf` => `riast`
* `rivast` has following analyses:
  * `riv+A+Superl+Indef`

**pjatte** failures:

* `pjatte+V+Inf` => `pjatt`
* `pjatte` has no analyses either

**planlegge** failures:

* `planlegge+V+Inf` does not generate!
* `planlegge` has no analyses either

**dekontaminere** failures:

* `dekontaminere+V+Inf` => `dekontaminer`
* `dekontaminere` has no analyses either

**forkaste** failures:

* `forkaste+V+Inf` => `forkast`
* `forkaste` has no analyses either

**dumpe** failures:

* `dumpe+V+Inf` => `dump`
* `dumpe` has following analyses:
  * `dump+A+Pl+Indef`
  * `dump+A+Sg+Def`

**trulove** failures:

* `trulove+V+Inf` does not generate!
* `trulove` has no analyses either

**knalle** failures:

* `knalle+V+Inf` => `knall`
* `knalle` has no analyses either

**sertifisere** failures:

* `sertifisere+V+Inf` => `sertifiser`
* `sertifisere` has no analyses either

**tilråde** failures:

* `tilråde+V+Inf` does not generate!
* `tilråde` has no analyses either

**lute** failures:

* `lute+V+Inf` => `lut`
* `lute` has following analyses:
  * `lut+A+Pl+Indef`
  * `lut+A+Sg+Def`
  * `luten+A+Neu+Sg+Indef`

**eksponere** failures:

* `eksponere+V+Inf` => `eksponer`
* `eksponere` has no analyses either

**nitrifisere** failures:

* `nitrifisere+V+Inf` => `nitrifiser`
* `nitrifisere` has no analyses either

**kretse** failures:

* `kretse+V+Inf` => `krets`
* `kretse` has no analyses either

**troke** failures:

* `troke+V+Inf` => `trok`
* `troke` has following analyses:
  * `troke+N+N+Msc+Sg+Indef`
  * `trokk+A+A+Pl+Indef`
  * `trokk+A+A+Pl+Def`
  * `trok+A+Pl+Indef`
  * `trok+A+Sg+Def`
  * `troken+A+Neu+Sg+Indef`
  * `trokk+A+A+Sg+Def`

**sitre** failures:

* `sitre+V+Inf` => `sitr`
* `sitre` has no analyses either

**løynmyrde** failures:

* `løynmyrde+V+Inf` => `løynmyrd`
* `løynmyrde` has no analyses either

**feminisere** failures:

* `feminisere+V+Inf` => `feminiser`
* `feminisere` has no analyses either

**probere** failures:

* `probere+V+Inf` => `prober`
* `probere` has no analyses either

**dorme** failures:

* `dorme+V+Inf` => `dorm`
* `dorme` has following analyses:
  * `dormen+A+Neu+Sg+Indef`

**plagiere** failures:

* `plagiere+V+Inf` => `plagier`
* `plagiere` has no analyses either

**symbolisere** failures:

* `symbolisere+V+Inf` => `symboliser`
* `symbolisere` has no analyses either

**mishandle** failures:

* `mishandle+V+Inf` => `mishandl`
* `mishandle` has no analyses either

**krype** failures:

* `krype+V+Inf` => `krye`
* `krype+V+Inf` => `krya`
* `krype` has no analyses either

**glette** failures:

* `glette+V+Inf` does not generate!
* `glette` has no analyses either

**kitte** failures:

* `kitte+V+Inf` => `kitt`
* `kitte` has no analyses either

**omvende** failures:

* `omvende+V+Inf` does not generate!
* `omvende` has following analyses:
  * `omvend+A+A+Pl+Indef`
  * `omvend+A+A+Sg+Def`
  * `omvend+A+A+Pl+Def`

**avreagere** failures:

* `avreagere+V+Inf` => `avreager`
* `avreagere` has no analyses either

**arkivere** failures:

* `arkivere+V+Inf` => `arkiver`
* `arkivere` has no analyses either

**helde** failures:

* `helde+V+Inf` => `held`
* `helde` has no analyses either

**vasslipe** failures:

* `vasslipe+V+Inf` does not generate!
* `vasslipe` has no analyses either

**kjølsprenge** failures:

* `kjølsprenge+V+Inf` => `kjølspreng`
* `kjølsprenge` has no analyses either

**sakse** failures:

* `sakse+V+Inf` => `saks`
* `sakse` has following analyses:
  * `sakse+N+N+Msc+Sg+Indef`

**bokføre** failures:

* `bokføre+V+Inf` => `bokfør`
* `bokføre` has no analyses either

**tjone** failures:

* `tjone+V+Inf` => `tjon`
* `tjone` has no analyses either

**plage** failures:

* `plage+V+Inf` => `plag`
* `plage` has no analyses either

**prosentløne** failures:

* `prosentløne+V+Inf` => `prosentløn`
* `prosentløne` has no analyses either

**surne** failures:

* `surne+V+Inf` => `surn`
* `surne` has no analyses either

**innvandre** failures:

* `innvandre+V+Inf` => `innvandr`
* `innvandre` has no analyses either

**omleire** failures:

* `omleire+V+Inf` => `omleir`
* `omleire` has no analyses either

**fortrenge** failures:

* `fortrenge+V+Inf` does not generate!
* `fortrenge` has no analyses either

**injuriere** failures:

* `injuriere+V+Inf` => `injurier`
* `injuriere` has no analyses either

**søle** failures:

* `søle+V+Inf` => `søl`
* `søle` has no analyses either

**tømme** failures:

* `tømme+V+Inf` does not generate!
* `tømme` has no analyses either

**ormektast** failures:

* `ormektast+V+Inf` => `ormektastast`
* `ormektast` has no analyses either

**kule** failures:

* `kule+V+Inf` does not generate!
* `kule` has following analyses:
  * `kul+A+Pl+Indef`
  * `kul+A+Sg+Def`

**prisgi** failures:

* `prisgi+V+Inf` => `prisggi`
* `prisgi` has no analyses either

**herse** failures:

* `herse+V+Inf` => `hers`
* `herse` has no analyses either

**vedta** failures:

* `vedta+V+Inf` => `vedtta`
* `vedta` has no analyses either

**sjakse** failures:

* `sjakse+V+Inf` => `sjaks`
* `sjakse` has no analyses either

**trekte** failures:

* `trekte+V+Inf` => `trekt`
* `trekte` has no analyses either

**utmerke** failures:

* `utmerke+V+Inf` => `utmerk`
* `utmerke` has no analyses either

**rådgje** failures:

* `rådgje+V+Inf` => `rådggje`
* `rådgje` has no analyses either

**betakke** failures:

* `betakke+V+Inf` => `betakk`
* `betakke` has no analyses either

**trygde** failures:

* `trygde+V+Inf` => `trygd`
* `trygde` has no analyses either

**levrast** failures:

* `levrast+V+Inf` => `levrastast`
* `levrast` has no analyses either

**satinere** failures:

* `satinere+V+Inf` => `satiner`
* `satinere` has no analyses either

**svuppe** failures:

* `svuppe+V+Inf` => `svupp`
* `svuppe` has no analyses either

**spionere** failures:

* `spionere+V+Inf` => `spioner`
* `spionere` has no analyses either

**vettskremme** failures:

* `vettskremme+V+Inf` does not generate!
* `vettskremme` has no analyses either

**elgje** failures:

* `elgje+V+Inf` => `elg`
* `elgje` has no analyses either

**gule** failures:

* `gule+V+Inf` => `gul`
* `gule` has following analyses:
  * `gul+A+A+Pl+Indef`
  * `gul+A+A+Pl+Def`
  * `gul+A+A+Sg+Def`

**kannelere** failures:

* `kannelere+V+Inf` => `kanneler`
* `kannelere` has no analyses either

**kryste** failures:

* `kryste+V+Inf` => `kryst`
* `kryste` has no analyses either

**avskrive** failures:

* `avskrive+V+Inf` => `avskrie`
* `avskrive+V+Inf` => `avskria`
* `avskrive` has no analyses either

**svarteliste** failures:

* `svarteliste+V+Inf` => `svartelist`
* `svarteliste` has no analyses either

**illskrike** failures:

* `illskrike+V+Inf` => `illskrie`
* `illskrike+V+Inf` => `illskria`
* `illskrike` has no analyses either

**yllast** failures:

* `yllast+V+Inf` => `ylast`
* `yllast` has no analyses either

**suffigere** failures:

* `suffigere+V+Inf` => `suffiger`
* `suffigere` has no analyses either

**jue** failures:

* `jue+V+Inf` => `ju`
* `jue` has no analyses either

**vedplante** failures:

* `vedplante+V+Inf` => `vedplant`
* `vedplante` has no analyses either

**statuere** failures:

* `statuere+V+Inf` => `statuer`
* `statuere` has no analyses either

**kantre** failures:

* `kantre+V+Inf` => `kantr`
* `kantre` has no analyses either

**luke** failures:

* `luke+V+Inf` does not generate!
* `luke` has no analyses either

**kniple** failures:

* `kniple+V+Inf` => `knipl`
* `kniple` has no analyses either

**sedimentere** failures:

* `sedimentere+V+Inf` => `sedimenter`
* `sedimentere` has no analyses either

**verje** failures:

* `verje+V+Inf` does not generate!
* `verje` has no analyses either

**finrake** failures:

* `finrake+V+Inf` => `finrak`
* `finrake` has no analyses either

**skumple** failures:

* `skumple+V+Inf` => `skumpl`
* `skumple` has no analyses either

**hedde** failures:

* `hedde+V+Inf` => `hedd`
* `hedde` has no analyses either

**stenge** failures:

* `stenge+V+Inf` => `steng`
* `stenge` has no analyses either

**trulle** failures:

* `trulle+V+Inf` => `trull`
* `trulle` has no analyses either

**avnorske** failures:

* `avnorske+V+Inf` => `avnorsk`
* `avnorske` has no analyses either

**klippe** failures:

* `klippe+V+Inf` does not generate!
* `klippe` has no analyses either

**ivre** failures:

* `ivre+V+Inf` => `ivr`
* `ivre` has no analyses either

**maskere** failures:

* `maskere+V+Inf` => `masker`
* `maskere` has no analyses either

**etterutdanne** failures:

* `etterutdanne+V+Inf` => `etterutdann`
* `etterutdanne` has no analyses either

**glufse** failures:

* `glufse+V+Inf` => `glufs`
* `glufse` has no analyses either

**nonchalere** failures:

* `nonchalere+V+Inf` => `nonchaler`
* `nonchalere` has no analyses either

**busetje** failures:

* `busetje+V+Inf` does not generate!
* `busetje` has no analyses either

**tematisere** failures:

* `tematisere+V+Inf` => `tematiser`
* `tematisere` has no analyses either

**donere** failures:

* `donere+V+Inf` => `doner`
* `donere` has no analyses either

**lappe** failures:

* `lappe+V+Inf` => `lapp`
* `lappe` has no analyses either

**storspringe** failures:

* `storspringe+V+Inf` => `storsprie`
* `storspringe+V+Inf` => `storspria`
* `storspringe` has no analyses either

**flipe** failures:

* `flipe+V+Inf` => `flip`
* `flipe` has no analyses either

**operasjonalisere** failures:

* `operasjonalisere+V+Inf` => `operasjonaliser`
* `operasjonalisere` has no analyses either

**finhakke** failures:

* `finhakke+V+Inf` => `finhakk`
* `finhakke` has no analyses either

**samstave** failures:

* `samstave+V+Inf` => `samstav`
* `samstave` has no analyses either

**kulse** failures:

* `kulse+V+Inf` => `kuls`
* `kulse` has following analyses:
  * `kulsen+A+Neu+Sg+Indef`

**krossmerke** failures:

* `krossmerke+V+Inf` => `krossmerk`
* `krossmerke` has no analyses either

**heilsteike** failures:

* `heilsteike+V+Inf` => `heilsteik`
* `heilsteike` has no analyses either

**skjelme** failures:

* `skjelme+V+Inf` => `skjelm`
* `skjelme` has no analyses either

**talmast** failures:

* `talmast+V+Inf` => `talmastast`
* `talmast` has no analyses either

**gjennomlyse** failures:

* `gjennomlyse+V+Inf` => `gjennomlys`
* `gjennomlyse` has no analyses either

**slure** failures:

* `slure+V+Inf` => `slur`
* `slure` has no analyses either

**stølne** failures:

* `stølne+V+Inf` => `støln`
* `stølne` has no analyses either

**kaltre** failures:

* `kaltre+V+Inf` => `kaltr`
* `kaltre` has no analyses either

**slette** failures:

* `slette+V+Inf` => `slett`
* `slette` has following analyses:
  * `slett+A+A+Sg+Def`
  * `slett+A+A+Pl+Indef`
  * `slett+A+A+Pl+Def`

**krese** failures:

* `krese+V+Inf` => `kres`
* `krese` has no analyses either

**tverke** failures:

* `tverke+V+Inf` => `tverk`
* `tverke` has following analyses:
  * `tverke+N+N+Sg`

**ruse** failures:

* `ruse+V+Inf` => `rus`
* `ruse` has no analyses either

**storme** failures:

* `storme+V+Inf` => `storm`
* `storme` has no analyses either

**offerere** failures:

* `offerere+V+Inf` => `offerer`
* `offerere` has no analyses either

**rakse** failures:

* `rakse+V+Inf` => `raks`
* `rakse` has no analyses either

**sjingle** failures:

* `sjingle+V+Inf` => `sjingl`
* `sjingle` has no analyses either

**eksklamere** failures:

* `eksklamere+V+Inf` => `eksklamer`
* `eksklamere` has no analyses either

**fleirdoble** failures:

* `fleirdoble+V+Inf` => `fleirdobl`
* `fleirdoble` has no analyses either

**svipe** failures:

* `svipe+V+Inf` => `svip`
* `svipe` has no analyses either

**krakle** failures:

* `krakle+V+Inf` => `krakl`
* `krakle` has following analyses:
  * `kraklen+A+Neu+Sg+Indef`

**krykkje** failures:

* `krykkje+V+Inf` does not generate!
* `krykkje` has no analyses either

**glane** failures:

* `glane+V+Inf` does not generate!
* `glane` has no analyses either

**gigge** failures:

* `gigge+V+Inf` => `gigg`
* `gigge` has no analyses either

**levere** failures:

* `levere+V+Inf` => `lever`
* `levere` has no analyses either

**trompetere** failures:

* `trompetere+V+Inf` => `trompeter`
* `trompetere` has no analyses either

**primtalfaktorisere** failures:

* `primtalfaktorisere+V+Inf` => `primtalfaktoriser`
* `primtalfaktorisere` has no analyses either

**storskratte** failures:

* `storskratte+V+Inf` => `storskratt`
* `storskratte` has no analyses either

**tindre** failures:

* `tindre+V+Inf` => `tindr`
* `tindre` has no analyses either

**knaske** failures:

* `knaske+V+Inf` => `knask`
* `knaske` has no analyses either

**sløvast** failures:

* `sløvast+V+Inf` => `sløvastast`
* `sløvast` has following analyses:
  * `sløv+A+Superl+Indef`

**snype** failures:

* `snype+V+Inf` => `snya`
* `snype+V+Inf` => `snye`
* `snype` has no analyses either

**veifte** failures:

* `veifte+V+Inf` => `veift`
* `veifte` has no analyses either

**styrkje** failures:

* `styrkje+V+Inf` does not generate!
* `styrkje` has no analyses either

**tåte** failures:

* `tåte+V+Inf` => `tåt`
* `tåte` has no analyses either

**tokne** failures:

* `tokne+V+Inf` => `tokn`
* `tokne` has following analyses:
  * `token+A+Pl+Indef`
  * `token+A+Sg+Def`

**fortolle** failures:

* `fortolle+V+Inf` => `fortoll`
* `fortolle` has no analyses either

**klive** failures:

* `klive+V+Inf` => `klia`
* `klive+V+Inf` => `klie`
* `klive` has no analyses either

**blande** failures:

* `blande+V+Inf` => `bland`
* `blande` has following analyses:
  * `blekke+V+PrsPrc`

**imøtekomme** failures:

* `imøtekomme+V+Inf` => `imøtekkomma`
* `imøtekomme+V+Inf` => `imøtekkomme`
* `imøtekomme` has no analyses either

**sike** failures:

* `sike+V+Inf` does not generate!
* `sike` has following analyses:
  * `sikker+A+A+Pl+Indef`
  * `sikker+A+A+Sg+Def`
  * `sikker+A+A+Pl+Def`

**kontribuere** failures:

* `kontribuere+V+Inf` => `kontribuer`
* `kontribuere` has no analyses either

**skrape** failures:

* `skrape+V+Inf` => `skrap`
* `skrape` has following analyses:
  * `skrapen+A+Neu+Sg+Indef`

**fjuske** failures:

* `fjuske+V+Inf` => `fjusk`
* `fjuske` has no analyses either

**protesjere** failures:

* `protesjere+V+Inf` => `protesjer`
* `protesjere` has no analyses either

**gline** failures:

* `gline+V+Inf` => `glia`
* `gline+V+Inf` => `glie`
* `gline` has following analyses:
  * `gline+V+Adj+Bu+Eint`
  * `gline+V+Adj+Fl`

**nebbast** failures:

* `nebbast+V+Inf` => `nebbastast`
* `nebbast` has no analyses either

**vernalisere** failures:

* `vernalisere+V+Inf` => `vernaliser`
* `vernalisere` has no analyses either

**tale** failures:

* `tale+V+Inf` does not generate!
* `tale` has no analyses either

**spiralisere** failures:

* `spiralisere+V+Inf` => `spiraliser`
* `spiralisere` has no analyses either

**vurdere** failures:

* `vurdere+V+Inf` => `vurder`
* `vurdere` has no analyses either

**absorbere** failures:

* `absorbere+V+Inf` => `absorber`
* `absorbere` has no analyses either

**bølle** failures:

* `bølle+V+Inf` => `bøll`
* `bølle` has no analyses either

**komprimere** failures:

* `komprimere+V+Inf` => `komprimer`
* `komprimere` has no analyses either

**vikariere** failures:

* `vikariere+V+Inf` => `vikarier`
* `vikariere` has no analyses either

**tjafse** failures:

* `tjafse+V+Inf` => `tjafs`
* `tjafse` has no analyses either

**illustrere** failures:

* `illustrere+V+Inf` => `illustrer`
* `illustrere` has no analyses either

**anestesere** failures:

* `anestesere+V+Inf` => `anesteser`
* `anestesere` has no analyses either

**trampe** failures:

* `trampe+V+Inf` => `tramp`
* `trampe` has no analyses either

**desertere** failures:

* `desertere+V+Inf` => `deserter`
* `desertere` has no analyses either

**pervertere** failures:

* `pervertere+V+Inf` => `perverter`
* `pervertere` has no analyses either

**sverve** failures:

* `sverve+V+Inf` => `svee`
* `sverve+V+Inf` => `svea`
* `sverve` has no analyses either

**blakne** failures:

* `blakne+V+Inf` => `blakn`
* `blakne` has no analyses either

**afrikanisere** failures:

* `afrikanisere+V+Inf` => `afrikaniser`
* `afrikanisere` has no analyses either

**voltigere** failures:

* `voltigere+V+Inf` => `voltiger`
* `voltigere` has no analyses either

**versne** failures:

* `versne+V+Inf` => `versn`
* `versne` has no analyses either

**bone** failures:

* `bone+V+Inf` => `bon`
* `bone` has following analyses:
  * `bere+V+Adj+Fl`
  * `bere+V+Adj+Bu+Eint`

**tomte** failures:

* `tomte+V+Inf` => `tomt`
* `tomte` has no analyses either

**avvikle** failures:

* `avvikle+V+Inf` => `avvikl`
* `avvikle` has no analyses either

**tabuere** failures:

* `tabuere+V+Inf` => `tabuer`
* `tabuere` has no analyses either

**omfamne** failures:

* `omfamne+V+Inf` => `omfamn`
* `omfamne` has no analyses either

**fjuntre** failures:

* `fjuntre+V+Inf` => `fjuntr`
* `fjuntre` has no analyses either

**nyansere** failures:

* `nyansere+V+Inf` => `nyanser`
* `nyansere` has no analyses either

**bisne** failures:

* `bisne+V+Inf` => `bisn`
* `bisne` has no analyses either

**overspele** failures:

* `overspele+V+Inf` does not generate!
* `overspele` has no analyses either

**klarlegge** failures:

* `klarlegge+V+Inf` does not generate!
* `klarlegge` has no analyses either

**rotfylle** failures:

* `rotfylle+V+Inf` does not generate!
* `rotfylle` has no analyses either

**understreke** failures:

* `understreke+V+Inf` => `understrek`
* `understreke` has no analyses either

**detasjere** failures:

* `detasjere+V+Inf` => `detasjer`
* `detasjere` has no analyses either

**bedyre** failures:

* `bedyre+V+Inf` => `bedyr`
* `bedyre` has no analyses either

**eludere** failures:

* `eludere+V+Inf` => `eluder`
* `eludere` has no analyses either

**fåme** failures:

* `fåme+V+Inf` => `fåm`
* `fåme` has no analyses either

**fiksere** failures:

* `fiksere+V+Inf` => `fikser`
* `fiksere` has no analyses either

**kvarte** failures:

* `kvarte+V+Inf` => `kvart`
* `kvarte` has following analyses:
  * `kvart+A+A+Pl+Indef`
  * `kvart+A+A+Pl+Def`
  * `kvart+A+A+Sg+Def`

**forvisse** failures:

* `forvisse+V+Inf` => `forviss`
* `forvisse` has no analyses either

**artsbestemme** failures:

* `artsbestemme+V+Inf` => `artsbesteme`
* `artsbestemme+V+Inf` => `artsbestema`
* `artsbestemme` has no analyses either

**erodere** failures:

* `erodere+V+Inf` => `eroder`
* `erodere` has no analyses either

**fulltakke** failures:

* `fulltakke+V+Inf` => `fulltakk`
* `fulltakke` has no analyses either

**tildømme** failures:

* `tildømme+V+Inf` does not generate!
* `tildømme` has no analyses either

**utstasjonere** failures:

* `utstasjonere+V+Inf` => `utstasjoner`
* `utstasjonere` has no analyses either

**varne** failures:

* `varne+V+Inf` => `varn`
* `varne` has no analyses either

**skvette** failures:

* `skvette+V+Inf` does not generate!
* `skvette` has no analyses either

**skvatle** failures:

* `skvatle+V+Inf` => `skvatl`
* `skvatle` has no analyses either

**spørjast** failures:

* `spørjast+V+Inf` => `spspørjast`
* `spørjast` has no analyses either

**mishalde** failures:

* `mishalde+V+Inf` => `mishaa`
* `mishalde+V+Inf` => `mishae`
* `mishalde` has no analyses either

**åle** failures:

* `åle+V+Inf` does not generate!
* `åle` has no analyses either

**forvåge** failures:

* `forvåge+V+Inf` does not generate!
* `forvåge` has no analyses either

**konversere** failures:

* `konversere+V+Inf` => `konverser`
* `konversere` has no analyses either

**gjenopne** failures:

* `gjenopne+V+Inf` => `gjenopn`
* `gjenopne` has no analyses either

**uggre** failures:

* `uggre+V+Inf` => `uggr`
* `uggre` has no analyses either

**dataføre** failures:

* `dataføre+V+Inf` => `datafør`
* `dataføre` has no analyses either

**stulle** failures:

* `stulle+V+Inf` => `stull`
* `stulle` has no analyses either

**tvinle** failures:

* `tvinle+V+Inf` => `tvinl`
* `tvinle` has no analyses either

**augefare** failures:

* `augefare+V+Inf` => `augefae`
* `augefare+V+Inf` => `augefaa`
* `augefare` has no analyses either

**kappkøyre** failures:

* `kappkøyre+V+Inf` does not generate!
* `kappkøyre` has no analyses either

**hemmelegstemple** failures:

* `hemmelegstemple+V+Inf` => `hemmelegstempl`
* `hemmelegstemple` has no analyses either

**fordoble** failures:

* `fordoble+V+Inf` => `fordobl`
* `fordoble` has no analyses either

**valke** failures:

* `valke+V+Inf` => `valk`
* `valke` has no analyses either

**etterprøve** failures:

* `etterprøve+V+Inf` does not generate!
* `etterprøve` has no analyses either

**krepse** failures:

* `krepse+V+Inf` => `kreps`
* `krepse` has no analyses either

**skrinnast** failures:

* `skrinnast+V+Inf` => `skrinast`
* `skrinnast` has no analyses either

**smogne** failures:

* `smogne+V+Inf` => `smogn`
* `smogne` has no analyses either

**fordra** failures:

* `fordra+V+Inf` => `fordrdra`
* `fordra` has following analyses:
  * `fordre+V+PrfPrc`
  * `fordre+V+Ind+Prt`

**svelgje** failures:

* `svelgje+V+Inf` does not generate!
* `svelgje` has no analyses either

**uhappe** failures:

* `uhappe+V+Inf` => `uhapp`
* `uhappe` has no analyses either

**blokke** failures:

* `blokke+V+Inf` => `blokk`
* `blokke` has no analyses either

**kampast** failures:

* `kampast+V+Inf` => `kampastast`
* `kampast` has no analyses either

**aurvelte** failures:

* `aurvelte+V+Inf` does not generate!
* `aurvelte` has no analyses either

**myrje** failures:

* `myrje+V+Inf` does not generate!
* `myrje` has no analyses either

**gagne** failures:

* `gagne+V+Inf` => `gagn`
* `gagne` has no analyses either

**øne** failures:

* `øne+V+Inf` => `øn`
* `øne` has no analyses either

**avluse** failures:

* `avluse+V+Inf` => `avlus`
* `avluse` has no analyses either

**briske** failures:

* `briske+V+Inf` => `brisk`
* `briske` has following analyses:
  * `brisken+A+Neu+Sg+Indef`

**jafse** failures:

* `jafse+V+Inf` => `jafs`
* `jafse` has no analyses either

**lausrive** failures:

* `lausrive+V+Inf` => `lausria`
* `lausrive+V+Inf` => `lausrie`
* `lausrive` has following analyses:
  * `lausriven+A+Neu+Sg+Indef`

**krisle** failures:

* `krisle+V+Inf` => `krisl`
* `krisle` has no analyses either

**bremse** failures:

* `bremse+V+Inf` => `brems`
* `bremse` has no analyses either

**zoome** failures:

* `zoome+V+Inf` => `zoom`
* `zoome` has no analyses either

**avmilitarisere** failures:

* `avmilitarisere+V+Inf` => `avmilitariser`
* `avmilitarisere` has no analyses either

**fylgje** failures:

* `fylgje+V+Inf` does not generate!
* `fylgje` has no analyses either

**snafse** failures:

* `snafse+V+Inf` => `snafs`
* `snafse` has no analyses either

**fatne** failures:

* `fatne+V+Inf` => `fatn`
* `fatne` has no analyses either

**svi** failures:

* `svi+V+Inf` => `sv`
* `svi` has following analyses:
  * `svike+V+Ind+Prs`
  * `svie+N+N+Msc+Sg+Indef`
  * `svive+V+Ind+Prs`
  * `svive+V+Imprt`
  * `svinne+V+Imprt`
  * `svinne+V+Ind+Prs`
  * `svike+V+Imprt`

**mane** failures:

* `mane+V+Inf` does not generate!
* `mane` has following analyses:
  * `monsieur+N+N+Msc+Pl+Def`
  * `mor+N+N+Msc+Pl+Def`

**gifte** failures:

* `gifte+V+Inf` => `gift`
* `gifte` has following analyses:
  * `gift+A+A+Pl+Def`
  * `gift+A+A+Sg+Def`
  * `gift+A+A+Pl+Indef`

**apportere** failures:

* `apportere+V+Inf` => `apporter`
* `apportere` has no analyses either

**gage** failures:

* `gage+V+Inf` => `gag`
* `gage` has following analyses:
  * `gag+A+Pl+Indef`
  * `gag+A+Sg+Def`

**melere** failures:

* `melere+V+Inf` => `meler`
* `melere` has no analyses either

**hundse** failures:

* `hundse+V+Inf` => `hunds`
* `hundse` has no analyses either

**utdjupe** failures:

* `utdjupe+V+Inf` => `utdjup`
* `utdjupe` has no analyses either

**turrbanne** failures:

* `turrbanne+V+Inf` does not generate!
* `turrbanne` has no analyses either

**demle** failures:

* `demle+V+Inf` => `deml`
* `demle` has no analyses either

**smadre** failures:

* `smadre+V+Inf` => `smadr`
* `smadre` has no analyses either

**flake** failures:

* `flake+V+Inf` => `flak`
* `flake` has no analyses either

**lenge** failures:

* `lenge+V+Inf` => `leng`
* `lenge` has no analyses either

**hare** failures:

* `hare+V+Inf` => `har`
* `hare` has no analyses either

**ekstrahere** failures:

* `ekstrahere+V+Inf` => `ekstraher`
* `ekstrahere` has no analyses either

**sprutregne** failures:

* `sprutregne+V+Inf` does not generate!
* `sprutregne` has no analyses either

**stauke** failures:

* `stauke+V+Inf` => `stauk`
* `stauke` has no analyses either

**farsere** failures:

* `farsere+V+Inf` => `farser`
* `farsere` has no analyses either

**føresette** failures:

* `føresette+V+Inf` does not generate!
* `føresette` has following analyses:
  * `føresett+A+A+Pl+Def`
  * `føresett+A+A+Pl+Indef`
  * `føresett+A+A+Sg+Def`

**sløyfe** failures:

* `sløyfe+V+Inf` => `sløyf`
* `sløyfe` has no analyses either

**forstue** failures:

* `forstue+V+Inf` => `forstu`
* `forstue` has no analyses either

**vansmekte** failures:

* `vansmekte+V+Inf` => `vansmekt`
* `vansmekte` has no analyses either

**kavle** failures:

* `kavle+V+Inf` => `kavl`
* `kavle` has no analyses either

**missnakke** failures:

* `missnakke+V+Inf` => `missnakk`
* `missnakke` has no analyses either

**matberge** failures:

* `matberge+V+Inf` => `matberg`
* `matberge` has no analyses either

**emansipere** failures:

* `emansipere+V+Inf` => `emansiper`
* `emansipere` has no analyses either

**høgje** failures:

* `høgje+V+Inf` does not generate!
* `høgje` has no analyses either

**bronse** failures:

* `bronse+V+Inf` => `brons`
* `bronse` has no analyses either

**polymerisere** failures:

* `polymerisere+V+Inf` => `polymeriser`
* `polymerisere` has no analyses either

**tusje** failures:

* `tusje+V+Inf` => `tusj`
* `tusje` has no analyses either

**ramskrike** failures:

* `ramskrike+V+Inf` => `ramskrie`
* `ramskrike+V+Inf` => `ramskria`
* `ramskrike` has no analyses either

**knuselske** failures:

* `knuselske+V+Inf` => `knuselsk`
* `knuselske` has no analyses either

**lynne** failures:

* `lynne+V+Inf` => `lynn`
* `lynne` has no analyses either

**gjenforeine** failures:

* `gjenforeine+V+Inf` => `gjenforein`
* `gjenforeine` has no analyses either

**bronsere** failures:

* `bronsere+V+Inf` => `bronser`
* `bronsere` has no analyses either

**nyggje** failures:

* `nyggje+V+Inf` => `nyje`
* `nyggje+V+Inf` => `nyja`
* `nyggje` has no analyses either

**bære** failures:

* `bære+V+Inf` does not generate!
* `bære` has following analyses:
  * `bær+A+Pl+Indef`
  * `bær+A+Sg+Def`

**pose** failures:

* `pose+V+Inf` => `pos`
* `pose` has no analyses either

**velle** failures:

* `velle+V+Inf` does not generate!
* `velle` has no analyses either

**stabbe** failures:

* `stabbe+V+Inf` => `stabb`
* `stabbe` has no analyses either

**knisle** failures:

* `knisle+V+Inf` => `knisl`
* `knisle` has no analyses either

**svanse** failures:

* `svanse+V+Inf` => `svans`
* `svanse` has no analyses either

**pistre** failures:

* `pistre+V+Inf` => `pistr`
* `pistre` has no analyses either

**gni** failures:

* `gni+V+Inf` does not generate!
* `gni` has no analyses either

**varsle** failures:

* `varsle+V+Inf` => `varsl`
* `varsle` has no analyses either

**skjersetje** failures:

* `skjersetje+V+Inf` does not generate!
* `skjersetje` has no analyses either

**klusse** failures:

* `klusse+V+Inf` => `kluss`
* `klusse` has no analyses either

**substantivere** failures:

* `substantivere+V+Inf` => `substantiver`
* `substantivere` has no analyses either

**blidke** failures:

* `blidke+V+Inf` => `blidk`
* `blidke` has no analyses either

**storkne** failures:

* `storkne+V+Inf` => `storkn`
* `storkne` has no analyses either

**oppfordre** failures:

* `oppfordre+V+Inf` => `oppfordr`
* `oppfordre` has no analyses either

**forsverje** failures:

* `forsverje+V+Inf` => `forsveje`
* `forsverje+V+Inf` => `forsveja`
* `forsverje` has no analyses either

**råsteike** failures:

* `råsteike+V+Inf` => `råsteik`
* `råsteike` has no analyses either

**livsforsikre** failures:

* `livsforsikre+V+Inf` => `livsforsikr`
* `livsforsikre` has no analyses either

**inseminere** failures:

* `inseminere+V+Inf` => `inseminer`
* `inseminere` has no analyses either

**setje** failures:

* `setje+V+Inf` does not generate!
* `setje` has no analyses either

**mure** failures:

* `mure+V+Inf` => `mur`
* `mure` has no analyses either

**tunne** failures:

* `tunne+V+Inf` => `tunn`
* `tunne` has following analyses:
  * `tunne+N+N+Fem+Sg+Indef`

**flangre** failures:

* `flangre+V+Inf` => `flangr`
* `flangre` has no analyses either

**sprengfyre** failures:

* `sprengfyre+V+Inf` => `sprengfyr`
* `sprengfyre` has no analyses either

**ringje** failures:

* `ringje+V+Inf` does not generate!
* `ringje` has no analyses either

**kvisle** failures:

* `kvisle+V+Inf` => `kvisl`
* `kvisle` has no analyses either

**tåttespringe** failures:

* `tåttespringe+V+Inf` => `tåttesprie`
* `tåttespringe+V+Inf` => `tåttespria`
* `tåttespringe` has no analyses either

**sortere** failures:

* `sortere+V+Inf` => `sorter`
* `sortere` has no analyses either

**tariffere** failures:

* `tariffere+V+Inf` => `tariffer`
* `tariffere` has no analyses either

**vørdsle** failures:

* `vørdsle+V+Inf` => `vørdsl`
* `vørdsle` has no analyses either

**hamse** failures:

* `hamse+V+Inf` => `hams`
* `hamse` has no analyses either

**rangere** failures:

* `rangere+V+Inf` => `ranger`
* `rangere` has no analyses either

**skvale** failures:

* `skvale+V+Inf` => `skval`
* `skvale` has no analyses either

**nauve** failures:

* `nauve+V+Inf` => `nauv`
* `nauve` has following analyses:
  * `nauver+A+A+Sg+Def`
  * `nauver+A+A+Pl+Indef`
  * `nauv+A+Sg+Def`
  * `nauv+A+Pl+Indef`
  * `nauver+A+A+Pl+Def`

**gentrifisere** failures:

* `gentrifisere+V+Inf` => `gentrifiser`
* `gentrifisere` has no analyses either

**smigre** failures:

* `smigre+V+Inf` => `smigr`
* `smigre` has no analyses either

**fullne** failures:

* `fullne+V+Inf` => `fulln`
* `fullne` has no analyses either

**flaue** failures:

* `flaue+V+Inf` => `flau`
* `flaue` has following analyses:
  * `flau+A+Sg+Def`
  * `flau+A+Pl+Indef`

**bruse** failures:

* `bruse+V+Inf` does not generate!
* `bruse` has no analyses either

**sigrine** failures:

* `sigrine+V+Inf` => `sigrie`
* `sigrine+V+Inf` => `sigria`
* `sigrine` has following analyses:
  * `sigrine+V+Adj+Fl`
  * `sigrine+V+Adj+Bu+Eint`

**slunke** failures:

* `slunke+V+Inf` => `slunk`
* `slunke` has no analyses either

**avalere** failures:

* `avalere+V+Inf` => `avaler`
* `avalere` has no analyses either

**knarke** failures:

* `knarke+V+Inf` => `knark`
* `knarke` has no analyses either

**kvavne** failures:

* `kvavne+V+Inf` => `kvavn`
* `kvavne` has no analyses either

**tukle** failures:

* `tukle+V+Inf` => `tukl`
* `tukle` has no analyses either

**golve** failures:

* `golve+V+Inf` => `golv`
* `golve` has no analyses either

**nisse** failures:

* `nisse+V+Inf` => `niss`
* `nisse` has no analyses either

**mjuke** failures:

* `mjuke+V+Inf` => `mjuk`
* `mjuke` has following analyses:
  * `mjuk+A+Sg+Def`
  * `mjuk+A+Pl+Indef`

**pipe** failures:

* `pipe+V+Inf` => `pie`
* `pipe+V+Inf` => `pia`
* `pipe` has no analyses either

**lufse** failures:

* `lufse+V+Inf` => `lufs`
* `lufse` has no analyses either

**bake** failures:

* `bake+V+Inf` does not generate!
* `bake` has no analyses either

**urbanisere** failures:

* `urbanisere+V+Inf` => `urbaniser`
* `urbanisere` has no analyses either

**titrere** failures:

* `titrere+V+Inf` => `titrer`
* `titrere` has no analyses either

**forsyte** failures:

* `forsyte+V+Inf` => `forsyt`
* `forsyte` has no analyses either

**hekke** failures:

* `hekke+V+Inf` => `hekk`
* `hekke` has no analyses either

**fette** failures:

* `fette+V+Inf` => `fett`
* `fette` has no analyses either

**banalisere** failures:

* `banalisere+V+Inf` => `banaliser`
* `banalisere` has no analyses either

**gnuste** failures:

* `gnuste+V+Inf` => `gnust`
* `gnuste` has no analyses either

**fermentere** failures:

* `fermentere+V+Inf` => `fermenter`
* `fermentere` has no analyses either

**oste** failures:

* `oste+V+Inf` => `ost`
* `oste` has no analyses either

**boble** failures:

* `boble+V+Inf` => `bobl`
* `boble` has no analyses either

**bede** failures:

* `bede+V+Inf` => `bbeda`
* `bede+V+Inf` => `bbede`
* `bede` has following analyses:
  * `beden+A+Neu+Sg+Indef`

**brunde** failures:

* `brunde+V+Inf` => `brund`
* `brunde` has no analyses either

**sprikje** failures:

* `sprikje+V+Inf` does not generate!
* `sprikje` has no analyses either

**katalogisere** failures:

* `katalogisere+V+Inf` => `katalogiser`
* `katalogisere` has no analyses either

**grunnlegge** failures:

* `grunnlegge+V+Inf` does not generate!
* `grunnlegge` has no analyses either

**lede** failures:

* `lede+V+Inf` => `led`
* `lede` has no analyses either

**sake** failures:

* `sake+V+Inf` => `sak`
* `sake` has no analyses either

**krenelere** failures:

* `krenelere+V+Inf` => `kreneler`
* `krenelere` has no analyses either

**lodde** failures:

* `lodde+V+Inf` => `lodd`
* `lodde` has no analyses either

**byråkratisere** failures:

* `byråkratisere+V+Inf` => `byråkratiser`
* `byråkratisere` has no analyses either

**tilgje** failures:

* `tilgje+V+Inf` => `tilggje`
* `tilgje` has no analyses either

**amortisere** failures:

* `amortisere+V+Inf` => `amortiser`
* `amortisere` has no analyses either

**surmule** failures:

* `surmule+V+Inf` => `surmul`
* `surmule` has no analyses either

**fortenkje** failures:

* `fortenkje+V+Inf` does not generate!
* `fortenkje` has no analyses either

**folde** failures:

* `folde+V+Inf` => `fold`
* `folde` has no analyses either

**logne** failures:

* `logne+V+Inf` => `logn`
* `logne` has following analyses:
  * `logn+A+Pl+Indef`
  * `logn+A+Sg+Def`

**hemme** failures:

* `hemme+V+Inf` => `hem`
* `hemme` has no analyses either

**hugsvale** failures:

* `hugsvale+V+Inf` => `hugsval`
* `hugsvale` has no analyses either

**kartleggje** failures:

* `kartleggje+V+Inf` does not generate!
* `kartleggje` has no analyses either

**bukle** failures:

* `bukle+V+Inf` => `bukl`
* `bukle` has no analyses either

**surfe** failures:

* `surfe+V+Inf` => `surf`
* `surfe` has no analyses either

**svalle** failures:

* `svalle+V+Inf` => `svall`
* `svalle` has no analyses either

**assimilere** failures:

* `assimilere+V+Inf` => `assimiler`
* `assimilere` has no analyses either

**rekordbetre** failures:

* `rekordbetre+V+Inf` => `rekordbetr`
* `rekordbetre` has no analyses either

**heade** failures:

* `heade+V+Inf` => `head`
* `heade` has no analyses either

**avleire** failures:

* `avleire+V+Inf` => `avleir`
* `avleire` has no analyses either

**turke** failures:

* `turke+V+Inf` => `turk`
* `turke` has no analyses either

## Lemma statistics
* 8806 lemmas
* -12.128094481035667 % success

## Settings used

```json
{
    "adjectives": {
        "lemmatags": [
            "+A+Sg+Nom"
        ],
        "lexcfile": ".../adjectives.lexc"
    },
    "analyser": ".../analyser-gt-norm.hfstol",
    "generator": ".../generator-gt-norm.hfstol",
    "nouns": {
        "lemmatags": [
            "+N+Sg+Nom",
            "+N+Pl+Nom"
        ],
        "lexcfile": ".../nouns.lexc"
    },
    "propernouns": {
        "lemmatags": [
            "+N+Prop+Sg+Nom"
        ],
        "lexcfile": ".../propernouns.lexc"
    },
    "verbs": {
        "lemmatags": [
            "+V+Inf"
        ],
        "lexcfile": ".../verbs.lexc"
    }
}
```
